import{_ as i,o as a,c as t,a3 as e}from"./chunks/framework.CRbjHCyB.js";const c=JSON.parse('{"title":"模板解析","description":"","frontmatter":{},"headers":[],"relativePath":"zh/clice/design/template-resolver.md","filePath":"zh/clice/design/template-resolver.md"}'),l={name:"zh/clice/design/template-resolver.md"};function n(p,s,h,d,k,r){return a(),t("div",null,[...s[0]||(s[0]=[e(`<h1 id="模板解析" tabindex="-1">模板解析 <a class="header-anchor" href="#模板解析" aria-label="Permalink to &quot;模板解析&quot;">​</a></h1><h2 id="背景" tabindex="-1">背景 <a class="header-anchor" href="#背景" aria-label="Permalink to &quot;背景&quot;">​</a></h2><p>C++ 模板的核心设计是<strong>延迟实例化</strong>——模板代码在被具体类型参数实例化之前，编译器不会（也无法）解析依赖于模板参数的名称。这些名称被称为<strong>依赖名称（dependent names）</strong>，它们的类型在模板定义阶段是未知的。</p><p>对于语言服务器来说，这意味着模板代码内部是一个&quot;盲区&quot;：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> foo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">vec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    vec.</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 光标在这里——vec 的类型已知是 vector&lt;T&gt;，可以提供补全</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>对于简单的情况，假设使用主模板的定义即可提供基本的补全。但考虑更复杂的场景：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> foo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">vec2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    vec2[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">].</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // vec2[0] 的类型是什么？</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>vec2[0]</code> 的类型是 <code>std::vector&lt;std::vector&lt;T&gt;&gt;::reference</code>——一个依赖名称。在标准库实现中，解析它需要追踪数十层嵌套的模板 typedef 链：<code>reference</code> → <code>allocator_traits&lt;Alloc&gt;::value_type</code> → <code>__alloc_traits&lt;Alloc&gt;::reference</code> → ... 每一层都涉及偏特化匹配、默认模板参数、typedef 展开等复杂操作。</p><p>在 clangd 的社区中，模板代码的补全和悬停问题长期存在。用户反复报告在模板函数体内无法获得补全建议——光标处显示 <code>&lt;dependent type&gt;</code>，所有 LSP 功能失效。问题的根源在于 clangd 的解析策略存在几个根本性限制：</p><p><strong>不处理偏特化</strong>：clangd 假设总是使用主模板的定义来查找成员。但标准库大量使用偏特化（如 <code>allocator_traits</code> 对不同分配器的特化），主模板可能根本没有要查找的成员——成员只存在于特定的偏特化中。</p><p><strong>缺少参数映射</strong>：即使通过名称查找找到了成员的类型，这个类型仍然是用被查找模板的参数表达的（如 <code>allocator_traits&lt;Alloc&gt;::value_type</code> 中的 <code>Alloc</code>），而不是用调用方的参数（如 <code>T</code>）。clangd 不做实例化，因此无法建立参数之间的映射关系。</p><p><strong>忽略默认模板参数</strong>：<code>std::vector&lt;T&gt;</code> 实际上是 <code>std::vector&lt;T, std::allocator&lt;T&gt;&gt;</code>——第二个参数是默认的。clangd 不展开默认参数，导致依赖于默认参数的名称无法解析。</p><h2 id="设计方案" tabindex="-1">设计方案 <a class="header-anchor" href="#设计方案" aria-label="Permalink to &quot;设计方案&quot;">​</a></h2><h3 id="核心思想" tabindex="-1">核心思想 <a class="header-anchor" href="#核心思想" aria-label="Permalink to &quot;核心思想&quot;">​</a></h3><p>clice 实现了一个<strong>伪实例化器（PseudoInstantiator）</strong>——它在没有具体类型参数的情况下，通过启发式方法解析依赖名称。核心思路是：不需要知道 <code>T</code> 是 <code>int</code> 还是 <code>string</code>，只需要追踪 <code>T</code> 在模板 typedef 链中的传播路径，将最终结果用 <code>T</code> 表达出来。</p><p>例如：<code>std::vector&lt;std::vector&lt;T&gt;&gt;::reference</code> 经过伪实例化后被简化为 <code>std::vector&lt;T&gt;&amp;</code>——这是一个具体到足以提供补全的类型，同时保留了模板参数 <code>T</code> 的符号含义。</p><h3 id="两阶段变换" tabindex="-1">两阶段变换 <a class="header-anchor" href="#两阶段变换" aria-label="Permalink to &quot;两阶段变换&quot;">​</a></h3><p>解析器基于 Clang 的 TreeTransform 基础设施构建，分为两个阶段：</p><p><strong>第一阶段：PseudoInstantiator（启发式解析）</strong></p><p>这是主引擎，负责解析各种依赖名称：</p><ul><li><strong>DependentNameType</strong>（如 <code>typename Container::value_type</code>）：在模板的成员中查找 <code>value_type</code>，匹配偏特化，展开 typedef，将结果用调用方的参数重新表达</li><li><strong>DependentTemplateSpecializationType</strong>（如 <code>Alloc::rebind&lt;U&gt;</code>）：解析依赖的模板特化，会优先尝试标准库特殊路径</li><li><strong>TemplateTypeParmType</strong>（如 <code>T</code>）：通过实例化栈查找参数的绑定值或默认参数</li><li><strong>DecltypeType</strong>（如 <code>decltype(var)</code>）：对简单变量引用解析其声明类型</li></ul><p><strong>第二阶段：SubstituteOnly（循环打破）</strong></p><p>当第一阶段在展开 typedef 时，可能再次遇到需要启发式查找的依赖名称，形成循环：typedef A 的底层类型引用了依赖名称 B，查找 B 发现它的类型又涉及 typedef A。</p><p>SubstituteOnly 打破这种循环——它只做参数替换和 typedef 展开，不执行启发式查找。当第一阶段需要展开 typedef 时，委托给 SubstituteOnly 完成，确保不会触发递归的启发式查找。</p><h3 id="实例化栈" tabindex="-1">实例化栈 <a class="header-anchor" href="#实例化栈" aria-label="Permalink to &quot;实例化栈&quot;">​</a></h3><p>模板参数在嵌套模板中可能处于不同的深度层级。实例化栈（InstantiationStack）维护一个参数映射栈，每一帧记录一层模板的参数绑定。</p><p>当遇到 <code>TemplateTypeParmType</code>（一个用深度和索引标识的模板参数）时，解析器从栈顶（最内层）到栈底（最外层）线性搜索，匹配对应深度的参数绑定。如果找到，替换为绑定的类型；如果未找到，尝试使用参数的默认值。当栈为空时（独立的依赖类型），解析器会沿着外围模板声明向上走，推入它们的注入模板参数作为上下文。</p><p>这使得解析器能够处理多层嵌套模板：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> X</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Outer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Y</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Inner</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pair</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">X</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Y</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">first_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> member;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // X 在深度 0，Y 在深度 1</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 两者都需要在栈中追踪才能正确解析</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><h3 id="依赖名称的解析流程" tabindex="-1">依赖名称的解析流程 <a class="header-anchor" href="#依赖名称的解析流程" aria-label="Permalink to &quot;依赖名称的解析流程&quot;">​</a></h3><p>以 <code>typename A&lt;T&gt;::type</code> 为例：</p><ol><li><strong>缓存检查</strong>：如果该节点已解析过，直接返回缓存结果</li><li><strong>循环检测</strong>：如果该节点正在解析中，中止以防止无限递归</li><li><strong>限定符变换</strong>：变换 <code>A&lt;T&gt;</code> 部分——替换参数、匹配偏特化</li><li><strong>成员查找</strong>：在变换后的类型中查找 <code>type</code>。依次尝试每个偏特化及其基类，然后是主模板及其基类</li><li><strong>参数推导</strong>：使用 Clang 的模板参数推导机制，建立形参到实参的映射</li><li><strong>替换</strong>：通过 SubstituteOnly 展开找到的成员类型中的 typedef，用调用方的参数替换</li><li><strong>递归</strong>：如果结果仍包含依赖名称，递归解析</li></ol><p>整个过程有递归深度限制（16 层），防止极端情况下的无限递归。</p><p>解析器还维护了两层循环检测：一层防止对同一个 DependentNameType 节点的重入解析，另一层防止在同一个 ClassTemplateDecl 上的递归查找（处理 CRTP 等自引用模式）。</p><h3 id="偏特化匹配" tabindex="-1">偏特化匹配 <a class="header-anchor" href="#偏特化匹配" aria-label="Permalink to &quot;偏特化匹配&quot;">​</a></h3><p>偏特化匹配是伪实例化器相对于 clangd 的关键优势。标准库大量使用偏特化，例如：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 主模板——没有定义 value_type</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Alloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> allocator_traits</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 偏特化——定义了 value_type</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> allocator_traits</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">allocator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt; {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    using</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> value_type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>当解析 <code>allocator_traits&lt;allocator&lt;int&gt;&gt;::value_type</code> 时，clangd 在主模板中查找 <code>value_type</code> 失败。伪实例化器会尝试将实际参数与每个偏特化的模式进行匹配，找到正确的偏特化后在其中查找成员。</p><h3 id="标准库特殊处理" tabindex="-1">标准库特殊处理 <a class="header-anchor" href="#标准库特殊处理" aria-label="Permalink to &quot;标准库特殊处理&quot;">​</a></h3><p>标准库的分配器重绑定链是一个特别深的 typedef 链：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>vector&lt;T&gt; → __alloc_traits&lt;allocator&lt;T&gt;&gt; → allocator_traits&lt;allocator&lt;T&gt;&gt;</span></span>
<span class="line"><span>  → allocator_traits&lt;Alloc&gt;::rebind_alloc&lt;U&gt;</span></span></code></pre></div><p>这个链条可能超过深度限制。解析器对 <code>allocator_traits::rebind_alloc</code> 模式进行特殊短路处理：检测到该模式后，直接尝试 <code>Alloc::rebind&lt;T&gt;::other</code>（标准分配器协议），或回退到替换第一个模板参数。</p><h3 id="类型重糖化" tabindex="-1">类型重糖化 <a class="header-anchor" href="#类型重糖化" aria-label="Permalink to &quot;类型重糖化&quot;">​</a></h3><p>Clang 内部用规范化的 <code>TemplateTypeParmType</code>（深度+索引）表示模板参数。用户看到的应该是参数名（如 <code>T</code>），而不是 <code>parameter 0 of depth 0</code>。ResugarOnly 变换将规范化的参数类型映射回其原始声明，用于对用户展示友好的类型信息。</p><h3 id="优雅降级" tabindex="-1">优雅降级 <a class="header-anchor" href="#优雅降级" aria-label="Permalink to &quot;优雅降级&quot;">​</a></h3><p>解析过程中的任何步骤失败（查找失败、循环检测触发、深度超限），解析器返回原始的依赖类型，而不是报错。这意味着 LSP 功能不会因为解析失败而完全中断——只是退化到&quot;无法解析&quot;的状态，与不做伪实例化的效果相同。</p><h2 id="设计决策与权衡" tabindex="-1">设计决策与权衡 <a class="header-anchor" href="#设计决策与权衡" aria-label="Permalink to &quot;设计决策与权衡&quot;">​</a></h2><p><strong>为什么是伪实例化而不是真正的模板实例化？</strong> 真正的实例化需要具体的类型参数（如 <code>T = int</code>），但在模板定义阶段没有这些信息。而且语言服务器中的代码经常是不完整的，无法进行完整的实例化。伪实例化通过保留符号参数（<code>T</code> 本身），避免了对具体类型的依赖。</p><p><strong>为什么需要两阶段设计？</strong> 单阶段设计中，typedef 展开会触发新的启发式查找，而查找结果可能又需要 typedef 展开——形成循环。两阶段设计通过职责分离打破循环：启发式查找只在第一阶段执行，typedef 展开委托给不做查找的第二阶段。</p><p><strong>为什么要特殊处理标准库模式？</strong> 标准库的分配器重绑定是实际代码中最常见的深层 typedef 链。通用的递归解析在这里会超过深度限制。特殊处理虽然不优雅，但覆盖了最高频的用户场景。未来计划用通用机制替代这些特殊处理。</p><p><strong>为什么选择优雅降级而不是报错？</strong> 模板解析本质上是启发式的——不可能覆盖所有 C++ 模板的边界情况。优雅降级确保解析失败不会比&quot;不做解析&quot;更差，同时在能解析的情况下提供显著的体验提升。</p><h2 id="已知限制" tabindex="-1">已知限制 <a class="header-anchor" href="#已知限制" aria-label="Permalink to &quot;已知限制&quot;">​</a></h2><ul><li><strong>包展开（pack expansion）</strong>：目前只处理单元素包（如包转发），不支持多元素包（如 <code>Us... = {int, float}</code> 的展开）。</li><li><strong>非类型模板参数的默认值</strong>：只支持类型模板参数（TemplateTypeParmDecl）的默认值替换，不支持非类型参数（如 <code>template&lt;int N = 0&gt;</code>）和模板模板参数的默认值。</li><li><strong>依赖成员表达式</strong>：<code>x.template foo&lt;T&gt;()</code> 形式的依赖成员访问尚未实现。</li><li><strong>复杂的 decltype</strong>：只处理 <code>decltype(var)</code> 的简单情况，不支持成员访问、函数调用等复杂表达式。</li><li><strong>运算符查找</strong>：依赖上下文中的运算符（如 <code>a + b</code>，其中 <code>a</code> 类型依赖于模板参数）无法解析，因为运算符缺少标识符信息。</li></ul>`,53)])])}const g=i(l,[["render",n]]);export{c as __pageData,g as default};
