import{_ as o,o as a,c as t,a3 as i}from"./chunks/framework.aYYe0dfI.js";const p=JSON.parse('{"title":"Module extract:merge","description":"The extract:merge module is responsible for consolidating extracted symbol information into the project model, handling deduplication, namespace hierarchy resolution, and index reconstruction. It provides the public entry points merge_symbol_info (with both const‑reference and rvalue‑reference overloads) to merge symbol data from an incoming source into a target, along with rebuild_model_indexes and rebuild_lookup_maps to regenerate lookup structures after model modifications. Additionally, it exposes utility templates append_unique and deduplicate for efficient collection management, and an internal LocalIndexData structure to support parallel processing of merge operations.","frontmatter":{"title":"Module extract:merge","description":"The extract:merge module is responsible for consolidating extracted symbol information into the project model, handling deduplication, namespace hierarchy resolution, and index reconstruction. It provides the public entry points merge_symbol_info (with both const‑reference and rvalue‑reference overloads) to merge symbol data from an incoming source into a target, along with rebuild_model_indexes and rebuild_lookup_maps to regenerate lookup structures after model modifications. Additionally, it exposes utility templates append_unique and deduplicate for efficient collection management, and an internal LocalIndexData structure to support parallel processing of merge operations.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/extract/merge.md","filePath":"en/clore/modules/extract/merge.md"}'),d={name:"clore/modules/extract/merge.md"};function c(r,e,n,l,s,m){return a(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="module-extract-merge" tabindex="-1">Module <code>extract:merge</code> <a class="header-anchor" href="#module-extract-merge" aria-label="Permalink to &quot;Module `extract:merge`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>extract:merge</code> module is responsible for consolidating extracted symbol information into the project model, handling deduplication, namespace hierarchy resolution, and index reconstruction. It provides the public entry points <code>merge_symbol_info</code> (with both const‑reference and rvalue‑reference overloads) to merge symbol data from an incoming source into a target, along with <code>rebuild_model_indexes</code> and <code>rebuild_lookup_maps</code> to regenerate lookup structures after model modifications. Additionally, it exposes utility templates <code>append_unique</code> and <code>deduplicate</code> for efficient collection management, and an internal <code>LocalIndexData</code> structure to support parallel processing of merge operations.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../config/"><code>config</code></a></li><li><a href="./filter"><code>extract:filter</code></a></li><li><a href="./model"><code>extract:model</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="variables" tabindex="-1">Variables <a class="header-anchor" href="#variables" aria-label="Permalink to &quot;Variables&quot;">​</a></h2><h3 id="clore-extract-append-unique" tabindex="-1"><code>clore::extract::append_unique</code> <a class="header-anchor" href="#clore-extract-append-unique" aria-label="Permalink to &quot;`clore::extract::append_unique`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:12</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>Based on its name and signature, <code>append_unique</code> serves as a utility to add an element to a container while avoiding duplicates. The exact container type and comparison logic depend on template instantiation, but the purpose is to ensure uniqueness in the target collection.</p><h4 id="mutation" tabindex="-1">Mutation <a class="header-anchor" href="#mutation" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h3 id="clore-extract-append-unique-range" tabindex="-1"><code>clore::extract::append_unique_range</code> <a class="header-anchor" href="#clore-extract-append-unique-range" aria-label="Permalink to &quot;`clore::extract::append_unique_range`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:19</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The variable&#39;s role and how it participates in surrounding logic are not described in the provided evidence. No reads or mutations are observable from the snippets.</p><h4 id="mutation-1" tabindex="-1">Mutation <a class="header-anchor" href="#mutation-1" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h3 id="clore-extract-deduplicate" tabindex="-1"><code>clore::extract::deduplicate</code> <a class="header-anchor" href="#clore-extract-deduplicate" aria-label="Permalink to &quot;`clore::extract::deduplicate`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:49</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>It appears to be a function template, but the evidence does not provide enough context to determine its role.</p><h4 id="mutation-2" tabindex="-1">Mutation <a class="header-anchor" href="#mutation-2" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-extract-merge-symbol-info" tabindex="-1"><code>clore::extract::merge_symbol_info</code> <a class="header-anchor" href="#clore-extract-merge-symbol-info" aria-label="Permalink to &quot;`clore::extract::merge_symbol_info`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:55</code></p><p>Definition: <code>extract/merge.cppm:215</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The implementation of <code>merge_symbol_info</code> is a thin public entry point that immediately delegates to the internal helper <code>merge_symbol_info_impl</code>. This helper drives the entire merging algorithm, which processes the incoming symbol data into the existing model. Control flow begins by dividing work across hardware threads via <code>run_parallel_chunks</code>; each thread operates on its own per-thread data structure (<code>per_thread</code>) to store merged results. Within each chunk, the algorithm uses <code>ensure_namespace_hierarchy</code> and <code>find_enclosing_namespace</code> to maintain the namespace tree, and calls <code>append_unique</code> or <code>append_unique_range</code> to insert symbols while avoiding duplicates. Configuration flags such as <code>prefer_incoming_snippet</code> and <code>prefer_incoming_definition</code> guide attribute conflict resolution. After all chunks complete, the per-thread results are consolidated and <code>deduplicate</code> is applied on the final output. Finally, the function <code>rebuild_model_indexes</code> and <code>rebuild_lookup_maps</code> are invoked to reconstruct the model’s internal index structures, ensuring consistency for subsequent queries.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Modifies <code>current</code> <code>SymbolInfo</code> instance</li></ul><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>current</code> parameter (in/out)</li><li><code>incoming</code> parameter (read-only)</li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>current</code> parameter (mutated)</li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called by code that needs to combine symbol data from multiple sources</li><li>Used during symbol extraction or update phases</li></ul><h3 id="clore-extract-merge-symbol-info-1" tabindex="-1"><code>clore::extract::merge_symbol_info</code> <a class="header-anchor" href="#clore-extract-merge-symbol-info-1" aria-label="Permalink to &quot;`clore::extract::merge_symbol_info`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:54</code></p><p>Definition: <code>extract/merge.cppm:211</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The public overload <code>clore::extract::merge_symbol_info(SymbolInfo&amp; current, SymbolInfo&amp;&amp; incoming)</code> is a thin wrapper that moves <code>incoming</code> into the internal helper <code>merge_symbol_info_impl</code>. The implementation of <code>merge_symbol_info_impl</code> begins by inspecting <code>incoming</code> to determine the number of hardware threads (<code>hardware_threads</code>) and reserving per‑thread work partitions via <code>run_parallel_chunks</code>. For each partition, a worker block reads the symbol range, identifies the <code>incoming</code> model and its <code>config</code> (which includes flags like <code>prefer_incoming_snippet</code> and <code>prefer_incoming_definition</code>), and then runs the core merge for that chunk.</p><p>Inside each worker, the algorithm calls <code>ensure_namespace_hierarchy</code> to guarantee that the namespace <code>ns</code> from <code>incoming</code> exists in <code>model</code>’s namespace tree (creating missing ancestors), then locates the enclosing namespace via <code>find_enclosing_namespace</code> using the <code>owner_path</code>. For each symbol in the chunk, <code>sym</code> is compared with existing symbols in <code>model</code> at the same <code>parent_id</code>; if no match is found, <code>append_unique</code> adds it together with its <code>explicit_namespaces</code> and any derived <code>current_name</code>. Overlapping symbol ranges are handled by <code>append_unique_range</code> and later <code>deduplicate</code> cleans up duplication. After all chunks complete, <code>rebuild_model_indexes</code> re‑establishes internal indexes (e.g., <code>known_namespace_names</code>) and <code>rebuild_lookup_maps</code> updates the global lookup structures, ensuring the merged <code>model</code> is fully consistent.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>modifies the <code>current</code> <code>SymbolInfo</code> by merging data from <code>incoming</code></li><li>moves resources from <code>incoming</code>, leaving it in a valid but unspecified state</li></ul><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>current</code> (existing <code>SymbolInfo</code> data)</li><li><code>incoming</code> (data to be merged)</li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>current</code> (modified <code>SymbolInfo</code>)</li><li><code>incoming</code> (moved-from state)</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during symbol extraction to combine partial symbol information</li><li>used to merge newly discovered symbol attributes into an existing record</li></ul><h3 id="clore-extract-rebuild-lookup-maps" tabindex="-1"><code>clore::extract::rebuild_lookup_maps</code> <a class="header-anchor" href="#clore-extract-rebuild-lookup-maps" aria-label="Permalink to &quot;`clore::extract::rebuild_lookup_maps`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:59</code></p><p>Definition: <code>extract/merge.cppm:428</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The implementation of <code>clore::extract::rebuild_lookup_maps</code> regenerates two internal lookup structures of a <code>ProjectModel</code> instance. It first clears <code>model.symbol_ids_by_qualified_name</code> and <code>model.module_name_to_sources</code>, then iterates over all entries in <code>model.symbols</code>. For each symbol that has a non‑empty <code>qualified_name</code>, the function appends its <code>symbol_id</code> to the corresponding vector in the lookup map. After populating, each vector is sorted using a custom comparator that orders by <code>signature</code>, then <code>declaration_location.file</code>, then <code>declaration_location.line</code>, and finally by <code>symbol_id</code> as a tiebreaker. The sorted vectors are deduplicated via <code>std::unique</code>; if a qualified name maps to more than one symbol ID, an informational log message records the count of overload candidates.</p><p>The module side follows a similar pattern. The function clears <code>model.module_name_to_sources</code> and rebuilds it by iterating over <code>model.modules</code>; for each module unit with a non‑empty <code>name</code>, it adds the source file to the corresponding vector. Each source list is then sorted and deduplicated. Finally, the function counts how many interface units exist per module name. If more than one interface is found, a warning is logged listing the first two conflicting source files. No external dependencies beyond the model’s maps and the logging utilities are required.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Clears <code>model.symbol_ids_by_qualified_name</code> and <code>model.module_name_to_sources</code></li><li>Populates <code>model.symbol_ids_by_qualified_name</code> with sorted, deduplicated symbol <code>IDs</code></li><li>Populates <code>model.module_name_to_sources</code> with sorted, deduplicated source file paths</li><li>Logs info messages about overload candidate counts</li><li>Logs warning messages about duplicate module interfaces</li></ul><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>model.symbols</code></li><li><code>model.modules</code></li><li><code>SymbolInfo::qualified_name</code></li><li><code>SymbolInfo::signature</code></li><li><code>SymbolInfo::declaration_location.file</code></li><li><code>SymbolInfo::declaration_location.line</code></li><li><code>ModuleUnit::name</code></li><li><code>ModuleUnit::is_interface</code></li><li><code>SymbolID</code></li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>model.symbol_ids_by_qualified_name</code></li><li><code>model.module_name_to_sources</code></li><li>log output</li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called after merging symbol info into a <code>ProjectModel</code></li><li>Invoked to refresh lookup caches when symbol or module data changes</li></ul><h3 id="clore-extract-rebuild-model-indexes" tabindex="-1"><code>clore::extract::rebuild_model_indexes</code> <a class="header-anchor" href="#clore-extract-rebuild-model-indexes" aria-label="Permalink to &quot;`clore::extract::rebuild_model_indexes`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:57</code></p><p>Definition: <code>extract/merge.cppm:219</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The function first resets the existing model indexes by clearing per-file symbols, deduplicating includes, clearing all namespaces, and cleaning per-symbol relation lists such as <code>calls</code>, <code>references</code>, <code>children</code>, <code>derived</code>, <code>called_by</code>, and <code>referenced_by</code>. It then captures all symbol <code>IDs</code> and builds a set of known namespace qualified names for strict validation. The core indexing is performed in parallel via <code>run_parallel_chunks</code>: each thread processes a range of symbol <code>IDs</code>, checking each symbol’s declaration location against the filter via <code>matches_filter</code>, inferring its enclosing namespace through <code>find_enclosing_namespace</code>, and recording explicit namespace names and parent–child pairs. Results are accumulated into thread‑local <code>LocalIndexData</code> structures and later merged into global maps (<code>file_symbols</code>, <code>namespace_symbols</code>, <code>parent_children</code>, <code>explicit_namespaces</code>).</p><p>After merging, the function populates <code>model.files</code> entries with the owner‑path mappings, ensures the namespace hierarchy using <code>ensure_namespace_hierarchy</code> for both explicit and inferred namespace names, and injects symbol <code>IDs</code> into namespace info objects. Parent–child relationships are applied by appending child <code>IDs</code> to the parent symbol’s <code>children</code> list. Finally, three parallel passes deduplicate lists within file info objects, namespace info objects (both <code>symbols</code> and <code>children</code>), and symbol info objects (<code>children</code> and <code>derived</code>), ensuring index consistency after the rebuild.</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>clears <code>model.files[].symbols</code> and <code>model.files[].includes</code></li><li>clears <code>model.namespaces</code></li><li>clears <code>model.symbols[].children</code></li><li>deduplicates <code>model.symbols[].calls</code>, <code>model.symbols[].references</code>, <code>model.symbols[].derived</code>, <code>model.symbols[].called_by</code>, <code>model.symbols[].referenced_by</code></li><li>rebuilds <code>model.files</code> with updated path and symbol lists</li><li>rebuilds <code>model.namespaces</code> with namespace info and symbol lists</li><li>adds parent-child relationships to <code>model.symbols</code></li></ul><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>reads <code>config.filter</code> and <code>config.root_path</code> via <code>filter_root_path(config)</code></li><li>reads <code>model.symbols</code> keys and values (including <code>declaration_location.file</code>, <code>kind</code>, <code>qualified_name</code>, <code>parent</code>)</li><li>reads <code>model.files</code> keys</li><li>reads <code>model.namespaces</code> for known namespace names</li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>writes to <code>model.files[].symbols</code> and <code>model.files[].path</code></li><li>writes to <code>model.namespaces</code> entries and <code>model.namespaces[].symbols</code>, <code>model.namespaces[].name</code>, <code>model.namespaces[].children</code></li><li>writes to <code>model.symbols[].children</code></li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called after extraction or merging to refresh indexes</li><li>part of the model finalization pipeline</li><li>ensures consistency of file, namespace, and parent-child associations</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>extract:merge</code> module is the component responsible for combining extracted symbol data into a consistent project model. It imports the <code>extract:model</code> for core data structures, <code>extract:filter</code> for path resolution, the <code>config</code> module for settings, and the <code>support</code> module for utility types. The module is decomposed into a set of internal helper functions and public entry points. The helper layer includes template utilities for deduplication (<code>append_unique</code>, <code>append_unique_range</code>, <code>deduplicate</code>) and namespace hierarchy management (<code>ensure_namespace_hierarchy</code>, <code>find_enclosing_namespace</code>). A private <code>LocalIndexData</code> struct supports per-thread state during parallel index rebuilding. On top of this, the public API provides <code>merge_symbol_info</code> (with overloads for const and rvalue references) to merge individual symbol records, <code>rebuild_model_indexes</code> to reconstruct internal indices after modifications, and <code>rebuild_lookup_maps</code> to repopulate name‑ and location‑based lookup tables. The implementation structure separates these concerns: low‑level merging of symbol fields, namespace resolution, and index maintenance are each handled by dedicated functions, promoting clarity and reuse.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../config/">Module config</a></li><li><a href="./filter">Module extract:filter</a></li><li><a href="./model">Module extract:model</a></li><li><a href="./../support/">Module support</a></li></ul>',84)])])}const h=o(d,[["render",c]]);export{p as __pageData,h as default};
