import{_ as r,C as i,o,c as n,a3 as a,b as c,w as t,a as l,E as s,a4 as p}from"./chunks/framework.aYYe0dfI.js";const k=JSON.parse('{"title":"Module generate:page","description":"The generate:page module is responsible for constructing and rendering the full set of documentation pages produced by the generation pipeline. It owns the page‑building and page‑rendering logic for all page types: module pages, file pages, namespace pages, index pages, and symbol‑specific pages. The module provides public entry points such as build_page_root (which dispatches to type‑specific builders like build_module_page_root, build_file_page_root, build_namespace_page_root, and build_index_page_root), render_page_markdown (which produces the final Markdown content for a single page), render_page_bundle (which renders a logical group of related pages together), and write_page (which serialises a completed page to its output file).","frontmatter":{"title":"Module generate:page","description":"The generate:page module is responsible for constructing and rendering the full set of documentation pages produced by the generation pipeline. It owns the page‑building and page‑rendering logic for all page types: module pages, file pages, namespace pages, index pages, and symbol‑specific pages. The module provides public entry points such as build_page_root (which dispatches to type‑specific builders like build_module_page_root, build_file_page_root, build_namespace_page_root, and build_index_page_root), render_page_markdown (which produces the final Markdown content for a single page), render_page_bundle (which renders a logical group of related pages together), and write_page (which serialises a completed page to its output file).","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/generate/page.md","filePath":"en/clore/modules/generate/page.md"}'),u={name:"clore/modules/generate/page.md"};function g(h,e,m,f,_,b){const d=i("Mermaid");return o(),n("div",null,[e[1]||(e[1]=a('<h1 id="module-generate-page" tabindex="-1">Module <code>generate:page</code> <a class="header-anchor" href="#module-generate-page" aria-label="Permalink to &quot;Module `generate:page`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>generate:page</code> module is responsible for constructing and rendering the full set of documentation pages produced by the generation pipeline. It owns the page‑building and page‑rendering logic for all page types: module pages, file pages, namespace pages, index pages, and symbol‑specific pages. The module provides public entry points such as <code>build_page_root</code> (which dispatches to type‑specific builders like <code>build_module_page_root</code>, <code>build_file_page_root</code>, <code>build_namespace_page_root</code>, and <code>build_index_page_root</code>), <code>render_page_markdown</code> (which produces the final Markdown content for a single page), <code>render_page_bundle</code> (which renders a logical group of related pages together), and <code>write_page</code> (which serialises a completed page to its output file).</p><p>Beyond these orchestrating functions, the module also exposes internal helpers that operate on page components, such as <code>build_frontmatter_page</code>, <code>select_primary_description_source_page</code>, <code>prompt_output_of_local_page</code>, and the templated <code>append_standard_symbol_sections</code> (responsible for assembling the standard symbol documentation sections with links, analyses, and prompt outputs). The module depends on the <code>generate:model</code>, <code>generate:markdown</code>, <code>generate:common</code>, <code>generate:symbol</code>, and <code>extract</code> modules, using their types and utilities to build and render pages that are correct, link‑aware, and structured according to the project’s documentation plan.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../config/"><code>config</code></a></li><li><a href="./../extract/"><code>extract</code></a></li><li><a href="./common"><code>generate:common</code></a></li><li><a href="./markdown"><code>generate:markdown</code></a></li><li><a href="./model"><code>generate:model</code></a></li><li><a href="./symbol"><code>generate:symbol</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./scheduler"><code>generate:scheduler</code></a></li></ul><h2 id="dependency-diagram" tabindex="-1">Dependency Diagram <a class="header-anchor" href="#dependency-diagram" aria-label="Permalink to &quot;Dependency Diagram&quot;">​</a></h2>',9)),(o(),c(p,null,{default:t(()=>[s(d,{id:"mermaid-70",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20M0%5B%22generate%22%5D%0A%20%20%20%20I0%5B%22config%22%5D%0A%20%20%20%20I0%20--%3E%20M0%0A%20%20%20%20I1%5B%22extract%22%5D%0A%20%20%20%20I1%20--%3E%20M0%0A%20%20%20%20I2%5B%22support%22%5D%0A%20%20%20%20I2%20--%3E%20M0%0A"})]),fallback:t(()=>[...e[0]||(e[0]=[l(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=a('<h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-generate-build-file-page-root" tabindex="-1"><code>clore::generate::build_file_page_root</code> <a class="header-anchor" href="#clore-generate-build-file-page-root" aria-label="Permalink to &quot;`clore::generate::build_file_page_root`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:345</code></p><p>Definition: <code>generate/render/page.cppm:345</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::build_file_page_root</code> constructs the root <code>SemanticSectionPtr</code> for a file page by assembling several sub‑sections in order. It first creates a root section of <code>SemanticKind::File</code> with the file’s title. If the file is present in the <code>model</code>, it adds two <code>BulletList</code> sections: one for &quot;Includes&quot; that iterates over <code>file_it-&gt;second.includes</code> and calls <code>append_file_item</code> for each, and another for &quot;Included By&quot; that collects all other files in <code>model.files</code> whose includes contain the current file — the list is sorted by relative path before appending. Next, if <code>render_file_dependency_diagram_code</code> returns a non‑empty diagram, a section with that diagram is appended via <code>make_mermaid</code>. Then <code>append_standard_symbol_sections</code> is invoked with a predicate from <code>collect_implementation_symbols</code>, a lambda to add declaration links via <code>push_optional_link_paragraph</code>, and an empty lambda for additional symbol adornments. If <code>find_module_for_file</code> discovers a module for the file, a &quot;Module Information&quot; section is added showing the module name as a link (if resolved) or as inline code. Finally, a &quot;Related Pages&quot; section is built from <code>build_related_page_targets</code>. The completed root is returned. The control flow is linear with conditional checks; dependencies include <code>config::TaskConfig</code>, <code>extract::ProjectModel</code>, <code>SymbolAnalysisStore</code>, <code>LinkResolver</code>, and numerous helper functions in the <code>generate</code> namespace.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Allocates dynamic memory for the semantic section tree and its child nodes</li><li>Modifies the children vector of the root section by appending markdown nodes</li></ul><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>plan</li><li>config</li><li>model</li><li>analyses</li><li>links</li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returned <code>SemanticSectionPtr</code> pointing to allocated root section tree</li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as a step in generating the content of a file documentation page</li><li>Called by higher-level page generation functions</li></ul><h3 id="clore-generate-build-index-page-root" tabindex="-1"><code>clore::generate::build_index_page_root</code> <a class="header-anchor" href="#clore-generate-build-index-page-root" aria-label="Permalink to &quot;`clore::generate::build_index_page_root`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:447</code></p><p>Definition: <code>generate/render/page.cppm:447</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::build_index_page_root</code> assembles an index page by constructing a <code>SemanticSectionPtr</code> of kind <code>SemanticKind::Index</code>. It first creates a root <code>SemanticSection</code> with the page title and then sequentially appends child sections built from the provided <code>PagePlan</code> plan, <code>config::TaskConfig</code> config, <code>extract::ProjectModel</code> model, outputs map, and <code>LinkResolver</code> links. The overview section is generated via <code>build_prompt_section</code> using the prompt output keyed by <code>PromptKind::IndexOverview</code>. If the model indicates module usage, a &quot;Modules&quot; bullet list is added, containing links to each unique interface module name resolved through the link resolver. Next, a &quot;Files&quot; list links each resolvable file path (sorted by its source-relative label), followed by a &quot;Namespaces&quot; list that excludes anonymous namespaces and includes only resolvable names. A &quot;Types&quot; list collects all type-kind symbols, filters out those with anonymous namespace qualifiers, sorts them by qualified name, and renders them via <code>build_symbol_link_list</code>. Finally, if the model has a non-empty module dependency diagram (produced by <code>render_module_dependency_diagram_code</code>), a &quot;Module Dependency Diagram&quot; section containing a Mermaid code block is appended. The function returns the fully constructed root section pointer.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>plan</code></li><li><code>config</code></li><li><code>model</code></li><li><code>outputs</code></li><li><code>links</code></li><li><code>model.modules</code></li><li><code>model.files</code></li><li><code>model.namespaces</code></li><li><code>model.symbols</code></li><li><code>config.project_root</code></li><li><code>plan.relative_path</code></li><li><code>plan.title</code></li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Allocates and returns a new <code>SemanticSection</code> root tree representing the index page</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during page generation to create the root content of an index page</li><li>Used by higher-level page building functions that assemble full pages</li></ul><h3 id="clore-generate-build-module-page-root" tabindex="-1"><code>clore::generate::build_module_page_root</code> <a class="header-anchor" href="#clore-generate-build-module-page-root" aria-label="Permalink to &quot;`clore::generate::build_module_page_root`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:255</code></p><p>Definition: <code>generate/render/page.cppm:255</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function constructs a <code>SemanticSection</code> root with <code>SemanticKind::Module</code> using the provided <code>PagePlan</code>, <code>config</code>, and <code>links</code>. It first inserts a summary prompt section sourced from the <code>outputs</code> map. When the module is found in the <code>extract::ProjectModel</code>, it builds &quot;Imports&quot; and &quot;Imported By&quot; bullet lists by calling <code>append_module_item</code> for each imported module name and for each module that imports this one (discovered via a linear scan across <code>model.modules</code>), respecting the <code>plan.relative_path</code> for link resolution. A &quot;Dependency Diagram&quot; section with a mermaid code block is conditionally appended via <code>render_import_diagram_code</code>. The core content is populated through <code>append_standard_symbol_sections</code>, which collects symbols via <code>collect_implementation_symbols</code> and for each symbol adds a declaration link from <code>find_declaration_page</code> and documentation links from <code>symbol_doc_view_for</code>. The function then appends an &quot;Internal Structure&quot; prompt section and a &quot;Related Pages&quot; list using <code>build_related_page_targets</code>. Every section and list item uses <code>make_section</code> and <code>build_list_section</code> helpers, and the entire tree is returned as the page root.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>plan</code></li><li><code>config</code></li><li><code>model</code></li><li><code>outputs</code></li><li><code>analyses</code></li><li><code>links</code></li><li><code>layout</code></li><li><code>extract::find_module_by_name(model, plan.owner_keys.front())</code></li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returned <code>SemanticSectionPtr</code> (heap-allocated <code>root</code> and its children)</li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during page generation to build the root section of a module page</li><li>used in <code>build_page_root</code> or similar page assembly functions</li></ul><h3 id="clore-generate-build-namespace-page-root" tabindex="-1"><code>clore::generate::build_namespace_page_root</code> <a class="header-anchor" href="#clore-generate-build-namespace-page-root" aria-label="Permalink to &quot;`clore::generate::build_namespace_page_root`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:165</code></p><p>Definition: <code>generate/render/page.cppm:165</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function constructs a hierarchical <code>SemanticSectionPtr</code> representing the root of a namespace documentation page. It first creates a root section with <code>make_section</code> using <code>SemanticKind::Namespace</code>, the first owner key from <code>plan</code>, and the plan’s title. A summary prompt is added via <code>build_prompt_section</code> using the output for <code>PromptKind::NamespaceSummary</code>. If <code>render_namespace_diagram_code</code> returns a non-empty diagram, a diagram subsection is appended inside a <code>make_section</code> with <code>SemanticKind::Section</code>. Next, a &quot;Subnamespaces&quot; list is built by iterating over the model’s children of the current namespace, filtering out anonymous namespaces, and resolving each child link through <code>links.resolve</code> to produce a relative link via <code>make_relative_link_target</code>. The function then calls <code>append_standard_symbol_sections</code> with three lambdas: the first collects symbols from <code>collect_namespace_symbols</code> using the model and owner key; the second adds an implementation link paragraph via <code>push_link_paragraph</code> and <code>find_implementation_pages</code>; the third adds symbol documentation links via <code>add_symbol_doc_links</code> and <code>symbol_doc_view_for</code>. Finally, a &quot;Related Pages&quot; list is appended using <code>build_list_section</code> and <code>build_related_page_targets</code>. The resulting root section is returned.</p><p>The function depends on several internal components: <code>PagePlan</code>, <code>config::TaskConfig</code>, <code>extract::ProjectModel</code>, <code>SymbolAnalysisStore</code>, <code>LinkResolver</code>, and <code>PageDocLayout</code> for inputs; it leverages helpers like <code>make_section</code>, <code>build_prompt_section</code>, <code>build_list_section</code>, <code>render_namespace_diagram_code</code>, <code>append_standard_symbol_sections</code>, <code>collect_namespace_symbols</code>, <code>push_link_paragraph</code>, <code>find_implementation_pages</code>, <code>add_symbol_doc_links</code>, and <code>build_related_page_targets</code>. Control flow is linear with early returns only for diagram emptiness and missing namespace in model for the subnamespaces section.</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>const <code>PagePlan</code>&amp; plan</li><li>const <code>config::TaskConfig</code>&amp; config</li><li>const <code>extract::ProjectModel</code>&amp; model</li><li>const <code>std::unordered_map&lt;std::string, std::string&gt;</code>&amp; outputs</li><li>const <code>SymbolAnalysisStore</code>&amp; analyses</li><li>const <code>LinkResolver</code>&amp; links</li><li>const <code>PageDocLayout</code>&amp; layout</li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called when building the root node for a namespace documentation page</li><li>Used in conjunction with <code>append_standard_symbol_sections</code> and <code>build_related_page_targets</code></li></ul><h3 id="clore-generate-build-page-root" tabindex="-1"><code>clore::generate::build_page_root</code> <a class="header-anchor" href="#clore-generate-build-page-root" aria-label="Permalink to &quot;`clore::generate::build_page_root`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:546</code></p><p>Definition: <code>generate/render/page.cppm:546</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p><code>clore::generate::build_page_root</code> acts as a dispatcher that selects the appropriate page construction strategy based on the <code>plan.page_type</code> field. It accepts a <code>PagePlan</code>, <code>config::TaskConfig</code>, <code>extract::ProjectModel</code>, a map of output paths, a <code>SymbolAnalysisStore</code>, a <code>LinkResolver</code>, and a <code>PageDocLayout</code>, then returns a <code>SemanticSectionPtr</code>. The function uses a <code>switch</code> statement over <code>PageType</code> values: for <code>PageType::Index</code> it delegates to <code>build_index_page_root</code>; for <code>PageType::Namespace</code> to <code>build_namespace_page_root</code>; for <code>PageType::Module</code> to <code>build_module_page_root</code>; and for <code>PageType::File</code> to <code>build_file_page_root</code>. A default fallback constructs a minimal section from the plan’s title using <code>make_section</code>. The function does no algorithm work itself; its sole responsibility is to route control to the dedicated builder corresponding to the page kind, centralizing the mapping of page types to their generation logic.</p><h4 id="side-effects-4" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-4" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-4" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-4" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>plan</code> (<code>PagePlan&amp;</code>)</li><li><code>config</code> (<code>config::TaskConfig&amp;</code>)</li><li><code>model</code> (<code>extract::ProjectModel&amp;</code>)</li><li><code>outputs</code> (<code>const std::unordered_map&lt;std::string, std::string&gt;&amp;</code>)</li><li><code>analyses</code> (<code>const SymbolAnalysisStore&amp;</code>)</li><li><code>links</code> (<code>const LinkResolver&amp;</code>)</li><li><code>layout</code> (<code>const PageDocLayout&amp;</code>)</li><li><code>plan.page_type</code> (<code>PageType</code>)</li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during page generation to obtain the root semantic section for a given page plan.</li></ul><h3 id="clore-generate-render-page-bundle" tabindex="-1"><code>clore::generate::render_page_bundle</code> <a class="header-anchor" href="#clore-generate-render-page-bundle" aria-label="Permalink to &quot;`clore::generate::render_page_bundle`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:565</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::render_page_bundle</code> implements the core page-generation loop. It consumes a <code>PagePlan</code> to determine which pages must be produced, then iterates over each page entry. For each page, it retrieves the relevant content from the <code>ProjectModel</code> and <code>prompt_outputs</code>, applies the <code>LinkResolver</code> to resolve cross-references, and uses the <code>SymbolAnalysisStore</code> to attach symbol-level annotations. The <code>TaskConfig</code> supplies formatting and output settings that influence rendering. Each page’s output is collected, and any error (for example, missing template data or resolution failure) is propagated via the <code>std::expected</code> return type, bundling all successfully rendered pages on success. The algorithm’s control flow is sequential: validate input consistency, iterate over the plan in a single pass, accumulate results, and short‑circuit on the first unrecoverable error. Dependencies include the page‑plan data structure, the project model from extraction, the prompt‑output map from earlier generation phases, and the link‑resolver and analysis stores that were built during analysis and indexing.</p><h4 id="side-effects-5" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-5" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Generates rendered page content that may ultimately be written to files</li><li>Invokes functions that perform I/O and state mutation during page construction</li></ul><h4 id="reads-from-5" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-5" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>PagePlan plan</code></li><li><code>config::TaskConfig config</code></li><li><code>extract::ProjectModel model</code></li><li><code>std::unordered_map&lt;std::string, std::string&gt; prompt_outputs</code></li><li><code>SymbolAnalysisStore analyses</code></li><li><code>LinkResolver links</code></li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Return value of type <code>std::expected</code> containing rendered page bundle</li></ul><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called by <code>generate_pages</code> and <code>generate_pages_async</code> to render a batch of pages</li><li>Used in page generation pipeline after planning and analysis phases</li></ul><h3 id="clore-generate-render-page-bundle-1" tabindex="-1"><code>clore::generate::render_page_bundle</code> <a class="header-anchor" href="#clore-generate-render-page-bundle-1" aria-label="Permalink to &quot;`clore::generate::render_page_bundle`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:573</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::render_page_bundle</code> serves as the orchestration point for rendering a complete bundle of output pages. It accepts a <code>PagePlan</code> describing the page structure, a <code>config::TaskConfig</code> with generation parameters, an <code>extract::ProjectModel</code> containing the extracted project data, a map of <code>prompt_outputs</code> keyed by string identifiers, and a <code>LinkResolver</code> for resolving inter-page references. Internally, the implementation iterates over the plan&#39;s sections, applying template logic and integrating the provided model and prompt outputs to produce a rendered result. Errors during processing are communicated through the <code>std::expected</code> return type, allowing the caller to handle failures gracefully without exceptions. Dependencies include the render module&#39;s internal utilities and the external model and configuration types.</p><h4 id="side-effects-6" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-6" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>writes generated markdown files to disk</li></ul><h4 id="reads-from-6" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-6" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>plan</li><li>config</li><li>model</li><li><code>prompt_outputs</code></li><li>links</li></ul><h4 id="writes-to-4" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-4" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>generated markdown files</li></ul><h4 id="usage-patterns-6" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-6" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>invoked by <code>generate_pages</code> and <code>generate_pages_async</code> to produce final documentation output</li></ul><h3 id="clore-generate-render-page-markdown" tabindex="-1"><code>clore::generate::render_page_markdown</code> <a class="header-anchor" href="#clore-generate-render-page-markdown" aria-label="Permalink to &quot;`clore::generate::render_page_markdown`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:602</code></p><p>Definition: <code>generate/render/page.cppm:602</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The five-parameter overload of <code>clore::generate::render_page_markdown</code> is a convenience wrapper that delegates all work to the six-parameter overload. It receives a <code>PagePlan</code>, <code>config::TaskConfig</code>, <code>extract::ProjectModel</code>, a map of <code>prompt_outputs</code>, and a <code>LinkResolver</code>, then forwards them along with a default-constructed empty <code>SymbolAnalysisStore</code>. This allows callers to invoke page rendering without providing symbol analysis data when such information is not needed. The six-parameter overload (not shown in the snippet) contains the core algorithm that orchestrates building page roots, appending module and file items, resolving links, and assembling the final Markdown content. The delegation keeps the interface simple while retaining the option to supply analysis results for richer documentation output.</p><h4 id="side-effects-7" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-7" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-7" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-7" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>const <code>PagePlan</code> &amp;plan</li><li>const <code>config::TaskConfig</code> &amp;config</li><li>const <code>extract::ProjectModel</code> &amp;model</li><li>const <code>std::unordered_map&lt;std::string, std::string&gt;</code> &amp;<code>prompt_outputs</code></li><li>const <code>LinkResolver</code> &amp;links</li></ul><h4 id="usage-patterns-7" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-7" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called to obtain rendered page markdown for a given plan and configuration</li></ul><h3 id="clore-generate-render-page-markdown-1" tabindex="-1"><code>clore::generate::render_page_markdown</code> <a class="header-anchor" href="#clore-generate-render-page-markdown-1" aria-label="Permalink to &quot;`clore::generate::render_page_markdown`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:582</code></p><p>Definition: <code>generate/render/page.cppm:582</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::render_page_markdown</code> acts as a thin extraction layer over page rendering. It delegates all generation work to <code>clore::generate::render_page_bundle</code>, which produces a collection of <code>GeneratedPage</code> objects (a bundle). After the bundle is obtained (and checked for success), the function uses <code>std::ranges::find_if</code> to locate the page whose <code>relative_path</code> matches <code>plan.relative_path</code>. If such a page is found, its <code>content</code> is returned as the result; otherwise, an error containing the missing path is produced via <code>std::unexpected</code>. The function has a single, straightforward control flow: validate the bundle, search for the intended page, and return either the content or an error. Its primary dependency is <code>render_page_bundle</code>, and it also uses <code>std::format</code> for error construction and <code>std::ranges</code> for the search operation.</p><h4 id="side-effects-8" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-8" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-8" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-8" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the <code>plan</code> parameter</li><li>the <code>config</code> parameter</li><li>the <code>model</code> parameter</li><li>the <code>prompt_outputs</code> parameter</li><li>the <code>analyses</code> parameter</li><li>the <code>links</code> parameter</li><li>the <code>bundle</code> returned by <code>render_page_bundle</code></li><li>the <code>GeneratedPage</code> entries in the bundle</li></ul><h4 id="usage-patterns-8" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-8" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Extracts a single page&#39;s markdown from a full bundle</li><li>Used to obtain the final markdown string for a specific page plan</li><li>Called after generating the entire page bundle</li></ul><h3 id="clore-generate-write-page" tabindex="-1"><code>clore::generate::write_page</code> <a class="header-anchor" href="#clore-generate-write-page" aria-label="Permalink to &quot;`clore::generate::write_page`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:666</code></p><p>Definition: <code>generate/render/page.cppm:666</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function first constructs a <code>std::filesystem::path</code> from <code>output_root</code> and the page’s relative path, then validates that the relative path is non‑absolute and contains no <code>.</code> or <code>..</code> components, returning a <code>RenderError</code> on violation. After sanitization, it normalises the combined path with <code>lexically_normal</code> and extracts the parent directory. If the parent is non‑empty, it creates the directory tree via <code>fs::create_directories</code>, capturing any filesystem error into <code>ec</code> and returning an error with a descriptive message if creation fails. Finally, it delegates the actual file write to <code>clore::support::write_utf8_text_file</code>, forwarding any failure as a <code>RenderError</code>. The function depends on the filesystem library, the <code>write_utf8_text_file</code> utility, and the expected‑based error handling pattern using <code>RenderError</code>.</p><h4 id="side-effects-9" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-9" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Creates directories on disk</li><li>Writes a UTF-8 text file to disk</li></ul><h4 id="reads-from-9" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-9" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>page.relative_path</code></li><li><code>page.content</code></li><li><code>output_root</code></li></ul><h4 id="writes-to-5" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-5" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>File at <code>target</code> path</li><li>Parent directories of target path</li></ul><h4 id="usage-patterns-9" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-9" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called by <code>write_pages</code> to persist generated documentation pages</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The module <code>generate:page</code> is the top-level orchestrator for constructing and rendering all documentation pages. It decomposes page generation into specialized builders—<code>build_index_page_root</code>, <code>build_module_page_root</code>, <code>build_file_page_root</code>, <code>build_namespace_page_root</code>, and <code>build_page_root</code> (the dispatcher)—each responsible for assembling the root content for a specific page type. Lower-level helpers in an anonymous namespace, such as <code>append_module_item</code>, <code>append_file_item</code>, <code>build_frontmatter_page</code>, and <code>select_primary_description_source_page</code>, encapsulate common substructures. The module imports <code>generate:model</code> for page plans and analysis data, <code>generate:markdown</code> for the Markdown AST, <code>generate:symbol</code> for symbol-pages, <code>generate:common</code> for linking and list utilities, and <code>config</code>, <code>extract</code>, <code>support</code>, and <code>std</code> for configuration, extraction, and foundational utilities. Internally, the module first calls the appropriate <code>build_*_page_root</code> to construct a page root (likely a <code>MarkdownDocument</code> or similar node), then uses <code>render_page_markdown</code> or <code>render_page_bundle</code> to produce the final output, and finally <code>write_page</code> to serialize it to disk, forming a clean three‑stage pipeline: build, render, write.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../config/">Module config</a></li><li><a href="./../extract/">Module extract</a></li><li><a href="./common">Module generate:common</a></li><li><a href="./markdown">Module generate:markdown</a></li><li><a href="./model">Module generate:model</a></li><li><a href="./symbol">Module generate:symbol</a></li><li><a href="./../support/">Module support</a></li></ul>',126))])}const q=r(u,[["render",g]]);export{k as __pageData,q as default};
