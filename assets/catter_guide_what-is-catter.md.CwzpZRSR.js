import{_ as a,o as t,c as i,a3 as o}from"./chunks/framework.CRbjHCyB.js";const u=JSON.parse('{"title":"What is Catter?","description":"","frontmatter":{},"headers":[],"relativePath":"catter/guide/what-is-catter.md","filePath":"en/catter/guide/what-is-catter.md"}'),r={name:"catter/guide/what-is-catter.md"};function n(s,e,l,c,d,h){return t(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="what-is-catter" tabindex="-1">What is Catter? <a class="header-anchor" href="#what-is-catter" aria-label="Permalink to &quot;What is Catter?&quot;">​</a></h1><p>Catter is a build process interception tool. It hooks into child process creation during a build, captures compilation and linker commands, and lets you process them with JavaScript scripts powered by an embedded QuickJS runtime.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Catter was created for <a href="https://github.com/clice-io/clice" target="_blank" rel="noreferrer">clice</a>, a new C++ language server. Language servers depend on a <a href="https://clang.llvm.org/docs/JSONCompilationDatabase.html" target="_blank" rel="noreferrer">Compilation Database (CDB)</a> to understand how each source file is compiled, but obtaining one is often painful:</p><ul><li><strong>Limited build system support</strong> -- CMake only generates CDB when using Makefile or Ninja backends. Many other popular C++ build systems have no native CDB support at all.</li><li><strong>Inaccessible build directories</strong> -- When C++ code is built as part of a Python package (or similar), the build directory is managed externally and the CDB file is out of reach.</li><li><strong>Code generators require a full build</strong> -- Projects like LLVM use tools such as <code>tablegen</code> that generate header files at build time. These files are referenced by <code>-I</code> flags in the compilation commands, so the language server cannot resolve includes until a full build has run -- even if you only want to read the code.</li></ul><p>No existing cross-platform tool solves all of these problems. That is why we created catter.</p><h2 id="how-it-works" tabindex="-1">How It Works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How It Works&quot;">​</a></h2><p>Catter&#39;s approach is similar to <a href="https://github.com/rizsotto/Bear" target="_blank" rel="noreferrer">Bear</a> and <a href="https://github.com/rizsotto/scan-build" target="_blank" rel="noreferrer">scan-build</a>. It intercepts child process creation using platform-specific hooks (<code>LD_PRELOAD</code> on Linux, <code>DYLD_INSERT_LIBRARIES</code> on macOS, DLL injection on Windows). Every intercepted command is sent to a decision-making daemon that runs user-provided JavaScript scripts via QuickJS, determining what to do with each command.</p><h2 id="core-capabilities" tabindex="-1">Core Capabilities <a class="header-anchor" href="#core-capabilities" aria-label="Permalink to &quot;Core Capabilities&quot;">​</a></h2><h3 id="compilation-database-generation" tabindex="-1">Compilation Database Generation <a class="header-anchor" href="#compilation-database-generation" aria-label="Permalink to &quot;Compilation Database Generation&quot;">​</a></h3><p>The primary use case. Catter captures compiler invocations across any build system and generates a standard <code>compile_commands.json</code>. No build system plugins or special configuration needed.</p><h3 id="linker-analysis" tabindex="-1">Linker Analysis <a class="header-anchor" href="#linker-analysis" aria-label="Permalink to &quot;Linker Analysis&quot;">​</a></h3><p>By capturing linker commands, catter can reconstruct the dependency graph between build targets. This is essential for C++20 modules support: the C++ standard says a program may have at most one module with a given name, and &quot;program&quot; is defined by targets (libraries, executables). This target information is missing from the CDB standard, but catter can infer it from linker commands.</p><h3 id="fake-compilation-planned" tabindex="-1">Fake Compilation (Planned) <a class="header-anchor" href="#fake-compilation-planned" aria-label="Permalink to &quot;Fake Compilation (Planned)&quot;">​</a></h3><p>Instead of forwarding compilation to the real compiler, catter will be able to generate placeholder object files. This lets the build system run to completion without actually compiling, producing a complete CDB in a fraction of the time. Code generators (like <code>tablegen</code>) are still built normally -- catter analyzes dependencies to determine the minimal set of tools that must be genuinely compiled.</p><h3 id="build-profiling-planned" tabindex="-1">Build Profiling (Planned) <a class="header-anchor" href="#build-profiling-planned" aria-label="Permalink to &quot;Build Profiling (Planned)&quot;">​</a></h3><p>Capture process timing, durations, and parent-child relationships during a build. The data can be rendered visually in a browser, letting you analyze parallelism and identify bottlenecks.</p><h3 id="custom-script-patching" tabindex="-1">Custom Script Patching <a class="header-anchor" href="#custom-script-patching" aria-label="Permalink to &quot;Custom Script Patching&quot;">​</a></h3><p>With the embedded QuickJS engine, users can write JavaScript scripts to dynamically modify, redirect, or inject logic into build commands -- without changing the original build files.</p><h2 id="architecture-at-a-glance" tabindex="-1">Architecture at a Glance <a class="header-anchor" href="#architecture-at-a-glance" aria-label="Permalink to &quot;Architecture at a Glance&quot;">​</a></h2><p>Catter is a three-part system:</p><ul><li><strong>HOOK</strong> -- Platform-specific library injected into processes to intercept child process creation.</li><li><strong>PROXY</strong> (<code>catter-proxy</code>) -- Manages hook injection and acts as a compiler wrapper. Relays intercepted commands to the decision daemon.</li><li><strong>DECISION</strong> (<code>catter</code>) -- The main daemon. Runs the JS runtime, receives intercepted commands from the proxy, and decides how to handle each one.</li></ul><p>For a detailed walkthrough of the architecture and IPC flow, see the <a href="./../design/architecture">design documentation</a>.</p>',23)])])}const m=a(r,[["render",n]]);export{u as __pageData,m as default};
