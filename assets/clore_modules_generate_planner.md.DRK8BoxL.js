import{_ as r,C as i,o as a,c as l,a3 as t,b as d,w as o,a as s,E as c,a4 as p}from"./chunks/framework.aYYe0dfI.js";const x=JSON.parse('{"title":"Module generate:planner","description":"The generate:planner module is responsible for constructing a complete set of page plans that guide the documentation generation process. It takes the extracted symbol and module model from the extract module and the generation configuration, then enumerates page plans for modules, files, namespaces, and an index page. Internal helper functions determine renderable namespaces, perform topological sorting of plans, and build plan identifiers from the model. The module also handles error conditions through the PlanError type, which contains a descriptive message.","frontmatter":{"title":"Module generate:planner","description":"The generate:planner module is responsible for constructing a complete set of page plans that guide the documentation generation process. It takes the extracted symbol and module model from the extract module and the generation configuration, then enumerates page plans for modules, files, namespaces, and an index page. Internal helper functions determine renderable namespaces, perform topological sorting of plans, and build plan identifiers from the model. The module also handles error conditions through the PlanError type, which contains a descriptive message.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/generate/planner.md","filePath":"en/clore/modules/generate/planner.md"}'),u={name:"clore/modules/generate/planner.md"};function h(m,e,g,f,b,_){const n=i("Mermaid");return a(),l("div",null,[e[1]||(e[1]=t('<h1 id="module-generate-planner" tabindex="-1">Module <code>generate:planner</code> <a class="header-anchor" href="#module-generate-planner" aria-label="Permalink to &quot;Module `generate:planner`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>generate:planner</code> module is responsible for constructing a complete set of page plans that guide the documentation generation process. It takes the extracted symbol and module model from the <code>extract</code> module and the generation configuration, then enumerates page plans for modules, files, namespaces, and an index page. Internal helper functions determine renderable namespaces, perform topological sorting of plans, and build plan identifiers from the model. The module also handles error conditions through the <code>PlanError</code> type, which contains a descriptive message.</p><p>The public-facing interface is limited to the function <code>clore::generate::build_page_plan_set</code>, which accepts two identifiers (likely representing a model and configuration) and returns an integer handle to a set of page plans. This handle can be used by downstream generation routines, such as the link resolver and page root builder. All other symbols in the module, including the <code>PlanBuilder</code> helper struct and the various enumeration functions, are internal to the implementation.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../config/"><code>config</code></a></li><li><a href="./../extract/"><code>extract</code></a></li><li><a href="./model"><code>generate:model</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./scheduler"><code>generate:scheduler</code></a></li></ul><h2 id="dependency-diagram" tabindex="-1">Dependency Diagram <a class="header-anchor" href="#dependency-diagram" aria-label="Permalink to &quot;Dependency Diagram&quot;">​</a></h2>',9)),(a(),d(p,null,{default:o(()=>[c(n,{id:"mermaid-55",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20M0%5B%22generate%22%5D%0A%20%20%20%20I0%5B%22config%22%5D%0A%20%20%20%20I0%20--%3E%20M0%0A%20%20%20%20I1%5B%22extract%22%5D%0A%20%20%20%20I1%20--%3E%20M0%0A%20%20%20%20I2%5B%22support%22%5D%0A%20%20%20%20I2%20--%3E%20M0%0A"})]),fallback:o(()=>[...e[0]||(e[0]=[s(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=t('<h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-generate-planerror" tabindex="-1"><code>clore::generate::PlanError</code> <a class="header-anchor" href="#clore-generate-planerror" aria-label="Permalink to &quot;`clore::generate::PlanError`&quot;">​</a></h3><p>Declaration: <code>generate/planner.cppm:11</code></p><p>Definition: <code>generate/planner.cppm:11</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::PlanError</code> is a trivial aggregate type whose sole member is the <code>message</code> field of type <code>std::string</code>. It serves as a lightweight error carrier within the planning subsystem, with no custom constructors, destructors, or assignment <code>operator</code>s; all special member functions are implicitly defined. The internal invariant is simply that <code>message</code> may contain any <code>std::string</code> value, including an empty string, and the struct imposes no additional constraints. Because the type is an aggregate, direct brace-initialization is the intended way to construct instances, and the layout is determined solely by the single <code>std::string</code> member.</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>message</code> must contain a non-empty string describing the error.</li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>message</code>: a <code>std::string</code> holding error details.</li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Returned or thrown by generation functions to indicate failure.</li><li>Checked by callers to determine the cause of a plan generation error.</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-generate-build-page-plan-set" tabindex="-1"><code>clore::generate::build_page_plan_set</code> <a class="header-anchor" href="#clore-generate-build-page-plan-set" aria-label="Permalink to &quot;`clore::generate::build_page_plan_set`&quot;">​</a></h3><p>Declaration: <code>generate/planner.cppm:15</code></p><p>Definition: <code>generate/planner.cppm:369</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The implementation proceeds in four sequential enumeration phases, each delegating to a dedicated helper that populates a shared <code>PlanBuilder</code> instance. First, the function branches on <code>builder.model.uses_modules</code> to call either <code>enumerate_module_pages</code> or <code>enumerate_file_pages</code>; these two paths are mutually exclusive, corresponding to module‑based or header‑based projects. After recording the count of content pages, <code>enumerate_namespace_pages</code> and <code>enumerate_index_page</code> are invoked in order, ensuring that the index page is built after all content pages are known. Every enumeration helper returns an <code>expected</code>; a failure at any point immediately propagates the <code>PlanError</code> message via <code>std::unexpected</code>.</p><p>Once the page plans are assembled, the function validates that no two pages share the same output path using <code>validate_no_path_conflicts</code>. If that check passes, a topological sort is computed over the plans using <code>topological_sort(builder.plans, builder.id_to_index)</code> to determine the final <code>generation_order</code>. The result is packaged as a <code>PagePlanSet</code> containing both the <code>plans</code> vector and the sorted order. The entire algorithm is structured as a linear pipeline, with error handling at every step and a final empty‑plans guard that returns an error if no pages were generated.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>logs informational messages about page counts</li></ul><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>config::TaskConfig</code> parameter <code>config</code></li><li><code>extract::ProjectModel</code> parameter <code>model</code></li><li><code>PlanBuilder</code> local variable <code>builder</code> (including its <code>plans</code>, <code>path_entries</code>, <code>id_to_index</code>)</li><li>internal state of helper functions <code>enumerate_module_pages</code>, <code>enumerate_file_pages</code>, <code>enumerate_namespace_pages</code>, <code>enumerate_index_page</code>, <code>validate_no_path_conflicts</code>, <code>topological_sort</code></li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>local variable <code>builder.plans</code> (via emplace/move from helper enumeration functions)</li><li>local variable <code>builder.path_entries</code> (likely mutated during enumeration)</li><li>local variable <code>builder.id_to_index</code> (used in topological sort)</li><li>return value <code>PagePlanSet</code> (contains moved <code>plans</code> and <code>generation_order</code>)</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called as part of the page generation pipeline</li><li>invoked after configuration and project model are available</li><li>returned value used to drive subsequent page rendering steps</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>generate:planner</code> module acts as the orchestration layer that transforms extracted project data and configuration into a complete set of page plans. Its decomposition centers on a private <code>PlanBuilder</code> class that carries the configuration, model, and an incremental plan collection along with a mapping from entity identifiers to plan indices. Several internal helper functions—such as <code>enumerate_module_pages</code>, <code>enumerate_namespace_pages</code>, <code>enumerate_file_pages</code>, and <code>enumerate_index_page</code>—each populate the builder with plans for a specific document category, while utility functions like <code>namespace_of</code>, <code>has_reserved_identifier_prefix</code>, and <code>is_renderable_namespace_name</code> provide low‑level namespace and identifier screening. A topological sort is then performed on the accumulated plans to establish a consistent generation order. The module imports <code>config</code>, <code>extract</code>, <code>generate:model</code>, and <code>support</code>, leveraging the extracted symbol graph, configuration parameters, and foundational utility types. The public entry point <code>build_page_plan_set</code> accepts two identifiers (likely representing the project configuration and model) and returns an integer handle to the resulting ordered plan set, which downstream consumers such as the link resolver and page root builder can then use.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../config/">Module config</a></li><li><a href="./../extract/">Module extract</a></li><li><a href="./model">Module generate:model</a></li><li><a href="./../support/">Module support</a></li></ul>',31))])}const P=r(u,[["render",h]]);export{x as __pageData,P as default};
