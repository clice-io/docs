import{_ as t,o,c as a,a3 as i}from"./chunks/framework.CRbjHCyB.js";const u=JSON.parse('{"title":"Command Resolution","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"Command Resolution | clice.io"}],["meta",{"property":"og:url","content":"https://docs.clice.io/clice/design/command-resolve"}],["meta",{"property":"og:image","content":"https://docs.clice.io/og.png"}],["meta",{"property":"og:image:width","content":"1200"}],["meta",{"property":"og:image:height","content":"630"}],["meta",{"property":"og:locale","content":"en_US"}],["link",{"rel":"canonical","href":"https://docs.clice.io/clice/design/command-resolve"}],["link",{"rel":"alternate","hreflang":"en","href":"https://docs.clice.io/clice/design/command-resolve"}],["link",{"rel":"alternate","hreflang":"zh-CN","href":"https://docs.clice.io/zh/clice/design/command-resolve"}],["link",{"rel":"alternate","hreflang":"x-default","href":"https://docs.clice.io/clice/design/command-resolve"}]]},"headers":[],"relativePath":"clice/design/command-resolve.md","filePath":"en/clice/design/command-resolve.md","lastUpdated":1789769625000}'),n={name:"clice/design/command-resolve.md"};function r(s,e,c,d,l,h){return o(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="command-resolution" tabindex="-1">Command Resolution <a class="header-anchor" href="#command-resolution" aria-label="Permalink to &quot;Command Resolution&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>A C++ language server needs to know how to compile every file in a project. This information comes from the compilation database (CDB), typically a <code>compile_commands.json</code> file generated by the build system. Each entry in the CDB contains a source file path and a compilation command, for example:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">g++</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -std=c++20</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -O2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -fPIC</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -I../include</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -DNDEBUG</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/foo.cpp</span></span></code></pre></div><p>This command records how the build system invokes the compiler during an actual build. However, the language server cannot use it directly, for three reasons.</p><p><strong>First, this is a driver-level command, not a frontend command.</strong> The <code>g++</code> above is a compiler driver -- it selects the correct frontend, linker, and standard library. The language server actually uses Clang&#39;s frontend (cc1), and converting from <code>g++</code> to cc1 involves a large amount of implicit work: determining the target triple, injecting system header search paths, setting default language standards, and more. None of this information appears explicitly in the CDB -- it is implicitly provided by the compiler.</p><p><strong>Second, the command contains semantically irrelevant options.</strong> <code>-O2</code> and <code>-fPIC</code> only affect code generation, not semantic analysis -- the language server does not do code generation and has no use for these options. Similarly, <code>-c</code> (compile mode) and <code>-o</code> (output file) are build-artifact directives that are meaningless to a language server. Passing them unfiltered to the frontend adds unnecessary complexity and can even cause errors.</p><p><strong>Third, commands in large projects are highly redundant.</strong> In a project with tens of thousands of source files, the vast majority use the same compiler and semantic options, differing only in include paths and macro definitions. Without deduplication, each file requires an independent toolchain query, wasting memory and startup time.</p><p>Of these three problems, the first has the greatest impact on user experience: missing implicit information. When the language server cannot correctly obtain system header paths, users see errors on standard library headers -- <code>#include &lt;vector&gt;</code> reports &quot;file not found,&quot; or GCC built-in type traits are flagged as undeclared identifiers. In clangd&#39;s issue tracker, these problems are consistently among the most frequently reported (<a href="https://github.com/clangd/clangd/issues/1262" target="_blank" rel="noreferrer">clangd#1262</a>, <a href="https://github.com/clangd/clangd/issues/1691" target="_blank" rel="noreferrer">clangd#1691</a>).</p><p>clangd&#39;s solution is the <code>--query-driver</code> flag: users manually specify which compilers need probing, and clangd then queries those compilers for system paths. This approach has two problems. First, it is manual -- users must know which compiler their project uses and correctly configure a glob pattern. Second, the flag is not independently useful -- it relies on existing CDB commands to trigger probing (<a href="https://github.com/clangd/clangd/issues/1219" target="_blank" rel="noreferrer">clangd#1219</a>). For cross-compilation and embedded development scenarios, users frequently need to debug their configuration repeatedly before clangd correctly identifies the toolchain.</p><p>clice automates the entire command processing pipeline: after reading commands from the CDB, it automatically identifies the compiler family (GCC, Clang, MSVC, etc.), automatically probes the toolchain, and minimizes startup overhead through multi-level deduplication. Users do not need to manually configure any toolchain-related parameters.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><p>The core task of command processing is to convert the raw driver commands in the CDB into cc1 arguments consumable by the Clang frontend. This conversion involves four conceptual layers: argument classification, command separation, toolchain probing, and search path extraction.</p><h3 id="argument-classification" tabindex="-1">Argument Classification <a class="header-anchor" href="#argument-classification" aria-label="Permalink to &quot;Argument Classification&quot;">​</a></h3><p>When loading the CDB, each compilation option is classified into one of seven categories:</p><ul><li><p><strong>Discarded</strong>: Options related to build artifacts that the language server does not need. These include output file (<code>-o</code>), compile mode (<code>-c</code>), dependency scanning (<code>-M</code> family), PCH building (<code>-emit-pch</code>), and options the driver itself would ignore for this input.</p></li><li><p><strong>Codegen</strong>: Options that only affect the code generation backend and do not affect semantic analysis. These include position-independent code (<code>-fPIC</code>), stack protection (<code>-fstack-protector</code>), frame pointer (<code>-fomit-frame-pointer</code>), debug info (<code>-g</code> family), LTO, etc. They do not change the AST or diagnostic output.</p><blockquote><p>Note: <code>-O</code> and <code>-fsanitize=</code> are not in this category despite appearing code-generation-related. <code>-O</code> defines the <code>__OPTIMIZE__</code> macro, and <code>-fsanitize=address</code> affects <code>__has_feature(address_sanitizer)</code>. They alter preprocessor state and are therefore semantic options.</p></blockquote></li><li><p><strong>Diagnostics</strong>: Warning-control options (<code>-Wall</code>, <code>-Wno-*</code>, <code>-Werror</code>). They change which diagnostics are emitted but neither the AST nor toolchain probing results.</p></li><li><p><strong>User-content</strong>: Options that may differ per file but do not affect toolchain probing results. These include include paths (<code>-I</code>, <code>-isystem</code>, <code>-iquote</code>, <code>-idirafter</code>), macro definitions (<code>-D</code>, <code>-U</code>), and forced includes (<code>-include</code>). Their meaning is &quot;what this particular file additionally needs.&quot;</p></li><li><p><strong>Semantic</strong>: The remaining recognized options -- they affect compilation semantics and play a role in toolchain probing. Examples include <code>-std=c++20</code>, <code>-target</code>, <code>-march=</code>, etc.</p></li><li><p><strong>Input</strong>: The source file itself, which occupies an explicit slot in the parsed command rather than being a plain string among others.</p></li><li><p><strong>Unknown</strong>: Options the parser does not recognize (for example, flags from a compiler newer than clice&#39;s embedded LLVM). They participate in command identity, but unknown tokens coming from the CDB are not rendered into the compile commands clice executes; unknown tokens the user wrote in configuration rules are kept.</p></li></ul><p>Classification is based on Clang&#39;s own option table (<code>OptTable</code>), using option IDs rather than string matching.</p><h3 id="structured-commands" tabindex="-1">Structured Commands <a class="header-anchor" href="#structured-commands" aria-label="Permalink to &quot;Structured Commands&quot;">​</a></h3><p>After classification, each command is parsed <strong>once</strong> into a structured configuration (<code>CompileConfig</code>): an interned sequence of classified arguments in which the input file occupies an explicit slot. Identical configurations deduplicate to a single instance identified by a stable <code>ConfigID</code>; strings are interned for pointer-stable comparison. Everything downstream — toolchain probing, rendering, rules, identity — consumes the structured form; nothing re-parses command strings.</p><p>Two derived views matter:</p><ul><li><strong>Rendering</strong>: the same structured configuration can be rendered on demand as a driver command line (for probing or for handing agents a runnable command) or combined with probe results into frontend arguments. Rendering normalizes alias spellings, so identity is not disturbed by cosmetic variation in the CDB.</li><li><strong>Entry identity</strong>: the frontend-relevant view of the configuration, together with the input&#39;s slot position and working directory, is hashed into the entry&#39;s <strong>identity hash</strong> — the stable identity used for CDB diffing, index snapshot validation, and pinned context choices.</li></ul><p>The classification also buys toolchain probing cache efficiency. Probing requires actually invoking the compiler driver (e.g., running <code>g++ -dumpmachine</code> or <code>clang++ -###</code>), typically taking 100ms or more. The probing result depends only on the driver and probe-relevant options -- user-content options (<code>-I</code>, <code>-D</code>) do not affect the system paths or target triple output by the driver. Therefore, regardless of what different <code>-I</code> paths files may have, files sharing the probe-relevant view share one probing result. In real projects, tens of thousands of files typically collapse to a few dozen distinct probe keys.</p><h3 id="compilation-database" tabindex="-1">Compilation Database <a class="header-anchor" href="#compilation-database" aria-label="Permalink to &quot;Compilation Database&quot;">​</a></h3><p><code>CompilationDatabase</code> holds the entries of every loaded <strong>source</strong> — a <code>compile_commands.json</code> registered by the configuration or discovered under the workspace. Each source loads, reloads and unloads independently; its entries are parsed and deduplicated into <code>ConfigID</code>s and kept in file order, because generators emit a file&#39;s several entries in a fixed configuration order. The database stores facts only: it does not decide which of a file&#39;s entries wins.</p><p>Commands written by hand — a rule&#39;s <code>default_command</code>, the builtin fallback — go through the same normalization as an entry (<code>intern_command</code>), with the input slot synthesized at the end, so a declared command is probed, edited and rendered exactly like a database entry.</p><h3 id="build" tabindex="-1">Build <a class="header-anchor" href="#build" aria-label="Permalink to &quot;Build&quot;">​</a></h3><p><code>Build</code> is the one reader of the configuration&#39;s <code>[[rules]]</code> and the one place that knows which command a file compiles with — a pure function of the configuration and the database, shared by the server and every CLI entry point (<code>clice index</code>, <code>clice lint</code>, <code>clice inspect</code> load a workspace the same way). Every consumer — the context resolver, the dependency scan, the indexer, the context protocol — asks it rather than the database:</p><ul><li><strong>Entries.</strong> A file&#39;s database entries in build order: the sources of the rules matching the file first, then those of the other active rules, each in declaration order, discovered sources last; within one source, file order. The first entry is the default selection; a user&#39;s pin (<code>clice/switchContext</code>) can choose another. Entries never disappear because a pattern does not name their file — the rules only decide priority.</li><li><strong>Edits.</strong> The <code>remove</code> and <code>append</code> lists of every matching active rule, applied in declaration order — a rule&#39;s removes before its appends, a later <code>remove</code> reaching what an earlier rule appended. A header borrowing a host&#39;s command carries the edits of both files, each rule once, so it sees the same macros the host compiles with.</li><li><strong>Commands.</strong> What a file compiles under: its entries, or — when it has none — the <code>default_command</code> of the first matching rule that declares one. A file with neither gets the builtin command, whose driver follows the language clang assigns to the extension (<code>clang++ -std=c++20</code> for every C++ spelling and for the ambiguous <code>.h</code>, <code>-x cuda --cuda-device-only</code> for CUDA, plain <code>clang</code> for the rest).</li><li><strong>Members.</strong> The translation units of the build: files with entries, plus the sources on disk that a <code>default_command</code> rule&#39;s patterns claim (headers never count), enumerated from the directories the patterns name. The dependency scan runs every command of every member, so a header reachable through only one of a file&#39;s entries still finds that host. The background index admits a member unless a matching rule says <code>index = false</code>; that check sits at the index queue, so every path that enqueues work — startup, a database reload, a save — honours it.</li></ul><h3 id="hosting" tabindex="-1">Hosting <a class="header-anchor" href="#hosting" aria-label="Permalink to &quot;Hosting&quot;">​</a></h3><p>A header without a command of its own compiles as part of a translation unit that includes it. The hosting layer ranks the includers the build compiles in a language the header can be part of — a <code>.h</code> in any, a <code>.hpp</code> in C++ and the languages built on it (Objective-C++, CUDA), a <code>.cuh</code> only in CUDA, so a C++ header is never compiled as C: units whose entries come from the databases the header&#39;s own rules name first, then the unit sharing the header&#39;s stem, then one in its directory, then path proximity. The first with an include chain to the header is its default host — the same answer for the editor, the background index, a save-time rescan and <code>clice inspect</code>. A file the build does not compile (one on the builtin command) never hosts.</p><h3 id="discovery" tabindex="-1">Discovery <a class="header-anchor" href="#discovery" aria-label="Permalink to &quot;Discovery&quot;">​</a></h3><p>When no rule declares a source, the build&#39;s databases are the <code>compile_commands.json</code> files found under the workspace: at startup the root&#39;s and those of its direct subdirectories, and whenever a file without an entry is opened, the ones in the directories from the file&#39;s up to the root — a project deeper in the tree is loaded the first time one of its files is opened, and never scanned for otherwise. Discovered databases rank after declared ones, shallower before deeper, then by path; a file several of them list compiles under the first by default and offers the others as candidates, while a file only a later database lists takes its command from it. Finding more than one is reported once at startup, with the tagged rules that would turn them into switchable configurations instead. The file tracker keeps discovering on its poll, so a database generated after startup — at the root or in a new subdirectory — loads when it appears. A discovered database that vanishes keeps serving its entries, but yields to the present ones: a <code>build/</code> directory regenerated as <code>out/</code> hands the files both list to <code>out/</code> at once, and takes them back when it returns.</p><h3 id="inference" tabindex="-1">Inference <a class="header-anchor" href="#inference" aria-label="Permalink to &quot;Inference&quot;">​</a></h3><p>A file with neither an entry nor a host, and no matching rule with a <code>default_command</code>, borrows the command of a nearby translation unit of its own language — a <code>.h</code> matches any, a <code>.c</code> never borrows a C++ command and a <code>.cpp</code> never a C one. The lender is a unit in the file&#39;s directory, the one sharing its stem first and then the first by name; else, for a header, the unit whose header search directories (<code>-I</code>, <code>-isystem</code>, <code>-iquote</code>) contain it, the nearest directory first — the unit&#39;s own code finds the header by that path, so its command is the one the header is written for; else the unit closest by path. The borrowed command carries the rule edits of both files and is labeled <code>Inferred</code>: the decision log names the lender, diagnostics about missing files get a guidance note, and the file stays outside the build — it is not indexed in the background, and <code>clice inspect</code> resolves it the same way.</p><h3 id="resolution" tabindex="-1">Resolution <a class="header-anchor" href="#resolution" aria-label="Permalink to &quot;Resolution&quot;">​</a></h3><p>The final command of a file is composed from those layers in a fixed order: the user&#39;s selection for the file in the editor (a pinned entry or a pinned host and include occurrence), else the file&#39;s first entry, else — for a header — its default host&#39;s command, else the first matching rule&#39;s <code>default_command</code>, else a nearby unit&#39;s command (inference), else the builtin; then the rule edits of the file (and of the host or lender it borrows from), then a run&#39;s extras (a lint plan&#39;s clang-tidy arguments). Selections apply to editor compiles only; background compiles resolve without them and cache nothing, so the index and the CLI see the build&#39;s own answer.</p><p>Patterns are compiled against absolute paths: a relative pattern is anchored at the configuration file&#39;s directory (<code>..</code> segments included), or at the workspace root for rules passed through <code>initializationOptions</code>; an absolute or <code>**</code>-led one matches as written. Rules carrying a <code>configuration</code> tag apply only while that tag is active; the distinct tags form the configuration menu, and the active one is resolved at startup from <code>--configuration</code>, the persisted selection, or <code>default_configuration</code> (see <a href="./compilation-context#switching-build-configurations">Compilation Context</a>). When any rule declares a source (a database or a <code>default_command</code>), discovery is off: the declaration is the whole intent, and a database sitting at the workspace root is not consulted. Only a configuration declaring no source at all falls back to <a href="#discovery">discovery</a>.</p><p>Configuration granularity is preserved throughout the pipeline: search-path extraction for dependency scanning is per effective command (different <code>-I</code> sets produce different search configs), while toolchain probing deduplicates further still, since user-content options do not affect probe results.</p><h3 id="toolchain" tabindex="-1">Toolchain <a class="header-anchor" href="#toolchain" aria-label="Permalink to &quot;Toolchain&quot;">​</a></h3><p><code>Toolchain</code> converts driver-level commands into cc1 arguments. Its design centers on two core capabilities:</p><p><strong>Compiler family identification.</strong> <code>Toolchain</code> identifies the compiler family from the executable name -- the <code>CompilerFamily</code> enum includes GCC, Clang, MSVC, ClangCL, NVCC, Intel, and Zig. The identification logic handles various naming variants: version suffixes (<code>clang++-17</code>), architecture prefixes (<code>arm-none-eabi-g++</code>), and Windows <code>.exe</code> suffixes. The family determines which probing strategy is used.</p><p><strong>Caching strategy.</strong> The cache has two layers. The <strong>probe layer</strong> caches raw driver invocation results, keyed by the probe-relevant shape of the command (driver, probe-relevant flags, input kind — <code>.c</code> and <code>.cpp</code> may trigger different driver rules; the working directory joins the key when the command is sensitive to it). The <strong>synthesis layer</strong> turns probe results into the per-input-kind flag sets that command rendering consumes. Failed probes are cached too (negative caching), but transient failures are retried after a cooldown rather than being remembered forever.</p><h3 id="search-paths" tabindex="-1">Search Paths <a class="header-anchor" href="#search-paths" aria-label="Permalink to &quot;Search Paths&quot;">​</a></h3><p><code>SearchConfig</code> extracts header search paths from cc1 arguments, organizing them into a four-tier structure:</p><ol><li><strong>Quoted</strong> (<code>-iquote</code>): Search paths for <code>#include &quot;foo.h&quot;</code></li><li><strong>Angled</strong> (<code>-I</code>): Search paths for <code>#include &lt;foo.h&gt;</code></li><li><strong>System</strong> (<code>-isystem</code>, <code>-internal-isystem</code>, etc.): System header paths</li><li><strong>After</strong> (<code>-idirafter</code>): Paths searched after system directories</li></ol><p>This four-tier model corresponds to Clang&#39;s internal search layout. Paths within tiers are deduplicated (starting from the Angled tier), with the deduplication algorithm replicating Clang&#39;s behavior: if the same path appears in both Angled and System tiers, the one in Angled is kept. This ensures <code>#include_next</code> correctness.</p><p><code>SearchConfig</code> is the foundational input for include path completion, include path resolution, <a href="./dependency-scanning">dependency graph construction</a>, and other features.</p><h2 id="implementation" tabindex="-1">Implementation <a class="header-anchor" href="#implementation" aria-label="Permalink to &quot;Implementation&quot;">​</a></h2><h3 id="loading-and-parsing" tabindex="-1">Loading and Parsing <a class="header-anchor" href="#loading-and-parsing" aria-label="Permalink to &quot;Loading and Parsing&quot;">​</a></h3><p>CDB loading uses simdjson for streaming JSON parsing, processing entries one by one:</p><ol><li>Read each entry&#39;s <code>directory</code>, <code>file</code>, and <code>arguments</code> (or <code>command</code>) fields</li><li>Expand response files (<code>@file</code>), driver-mode aware, tolerating UTF-16 encoded files and nesting</li><li>Translate NVCC commands into an equivalent clang CUDA invocation</li><li>Filter out non-C/C++ files (e.g., <code>.rc</code>, <code>.asm</code>, <code>.def</code>)</li><li>Resolve relative file paths to absolute paths</li><li>Parse and classify each option once into the structured configuration, absolutizing relative include paths against <code>directory</code></li><li>Deduplicate identical configurations into shared <code>ConfigID</code>s</li></ol><p>The parsing process also handles a special case: CMake-generated CDBs sometimes contain <code>-Xclang -include-pch -Xclang &lt;pchfile&gt;</code> sequences (CMake&#39;s PCH workaround), which are detected and discarded during loading.</p><h3 id="toolchain-probing" tabindex="-1">Toolchain Probing <a class="header-anchor" href="#toolchain-probing" aria-label="Permalink to &quot;Toolchain Probing&quot;">​</a></h3><p>Different compiler families use different probing strategies:</p><p><strong>GCC</strong>: A two-step process. First, call the GCC driver to obtain two key pieces of information -- the target triple (<code>-dumpmachine</code>) and the install path (<code>-print-search-dirs</code>). Second, inject this information into Clang&#39;s driver (<code>--target=</code> and <code>--gcc-install-dir=</code>), letting Clang&#39;s driver emulate GCC&#39;s behavior and produce cc1 arguments. This allows the Clang frontend to correctly locate GCC&#39;s standard library and system headers.</p><p><strong>Clang / Zig</strong>: Invoke the driver with the <code>-###</code> option, which prints the complete cc1 command line without actually executing compilation. Parse the first cc1 line from the output. For Zig, the driver path consists of two parts (<code>zig cc</code> or <code>zig c++</code>), requiring special handling during probing.</p><blockquote><p>The external driver&#39;s version may be newer than clice&#39;s embedded LLVM version, and its cc1 output may contain options that clice does not recognize. During parsing, unknown options are silently dropped to ensure compatibility.</p></blockquote><p><strong>MSVC / ClangCL</strong>: Switch Clang&#39;s driver to MSVC-compatible mode via the <code>--driver-mode=cl</code> directive, then use Clang&#39;s driver to obtain cc1 arguments.</p><p>After probing completes, the temporary probe file&#39;s path and module-output-related flags (which reference the deleted temp file) are stripped from the results. If clice&#39;s resource dir differs from what the probe returned, all related paths are replaced to ensure the frontend uses builtin headers from a matching version.</p><p><strong>Startup warm-up.</strong> During the dependency scanning phase at server startup, all unique toolchain cache keys are collected and probed in parallel. Pipe draining and process waiting for probe subprocesses also run concurrently to avoid deadlocks from full pipes. Once warm-up completes, all subsequent toolchain queries hit the cache.</p><h3 id="search-path-extraction" tabindex="-1">Search Path Extraction <a class="header-anchor" href="#search-path-extraction" aria-label="Permalink to &quot;Search Path Extraction&quot;">​</a></h3><p>After toolchain probing yields cc1 arguments, search path extraction iterates through these arguments, routing include path options by type into the four tiers. All paths are resolved to absolute paths and normalized (removing <code>.</code> and <code>..</code>).</p><p>The <code>-iprefix</code> / <code>-iwithprefix</code> / <code>-iwithprefixbefore</code> options must be processed cooperatively in order of appearance: <code>-iprefix</code> sets a prefix, subsequent <code>-iwithprefix</code> prepends the prefix to its path and places it in the After tier, and <code>-iwithprefixbefore</code> places it in the Angled tier.</p><p>After the four tiers are concatenated, deduplication begins from the Angled tier. The Quoted tier does not participate in deduplication -- the same path appearing in both Quoted and Angled is legitimate, and both are preserved. This replicates Clang&#39;s internal behavior.</p><h2 id="faq" tabindex="-1">FAQ <a class="header-anchor" href="#faq" aria-label="Permalink to &quot;FAQ&quot;">​</a></h2><ul><li><p><strong>Why classify by option ID rather than string matching?</strong></p><p>clice&#39;s argument parsing is based on Clang&#39;s own option table (generated from <code>Options.inc</code>), using option IDs for classification. String matching is prone to missing edge cases: Clang&#39;s option syntax has many forms -- <code>-std=c++20</code> is joined form, <code>-I /path</code> is separate form, <code>-Wall</code> is flag form, and some options have <code>/</code> prefixes (MSVC style). Classifying by ID means all these syntax variants are correctly handled by Clang&#39;s parser, and the classification logic only needs to care about &quot;what is this option,&quot; not &quot;how is it spelled.&quot;</p></li><li><p><strong>Why don&#39;t user-content options participate in the toolchain cache key?</strong></p><p>This is the core benefit of the classification design. <code>-I</code> and <code>-D</code> do not change the system paths, target triple, or language defaults output by the compiler driver. Excluding them from the cache key reduces the number of keys from &quot;one per file&quot; to &quot;one per configuration.&quot; A project with tens of thousands of files typically has only a few dozen distinct cache keys, requiring only a few dozen subprocess calls at startup.</p></li><li><p><strong>Why must search path deduplication precisely match Clang&#39;s behavior?</strong></p><p>If the language server&#39;s header search order differs from the actual compiler&#39;s, <code>#include</code> may resolve to different files -- when identically named headers exist in different directories, search order determines which one is used. The semantics of <code>#include_next</code> depend even more directly on the deduplication result of search directories. Strict matching ensures that the language server sees exactly the same code as the compiler.</p></li><li><p><strong>Why does GCC probing use a two-step process instead of directly parsing GCC&#39;s <code>-v</code> output?</strong></p><p>The Clang frontend needs GCC&#39;s target triple and install path to correctly locate GCC&#39;s standard library headers. Directly parsing GCC&#39;s <code>-v</code> output is feasible but would introduce additional text parsing logic that is fragile across different GCC versions with varying output formats. By injecting the information into Clang&#39;s driver via <code>--target=</code> and <code>--gcc-install-dir=</code>, Clang itself handles the search path assembly, which is more reliable.</p></li><li><p><strong>Why is compiler family identification based on executable filename rather than path resolution?</strong></p><p>Compiler driver behavior is affected by the name used to invoke it. For example, <code>/usr/bin/clang++</code> is typically a symlink to <code>/usr/lib/llvm-20/bin/clang</code>, but invoking it as <code>clang++</code> automatically enables C++ mode and links C++ libraries. Using <code>realpath</code> to resolve to the actual path before identification would lose the semantic information carried by the invocation name. Similarly, if <code>arm-none-eabi-g++</code> were resolved to some generic GCC binary path, the cross-compilation context would be lost.</p></li><li><p><strong>How are files without a CDB entry handled?</strong></p><p>A header first looks for a source file that includes it through the dependency graph and borrows that file&#39;s command (see <a href="./compilation-context">Compilation Context</a>). Otherwise the first matching rule with a <code>default_command</code> supplies the command — the way to describe a project whose files all share one set of flags, or a scratch directory. A file with none of those borrows a nearby unit&#39;s command (see <a href="#inference">Inference</a>); only one with no compatible unit either gets the builtin command: <code>clang</code> or <code>clang++ -std=c++20</code> based on the file&#39;s language, with the resource dir injected, so basic semantic analysis remains available and a guidance note explains that the command was guessed.</p></li></ul><h2 id="known-limitations" tabindex="-1">Known Limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known Limitations&quot;">​</a></h2><ul><li><p><strong>Discovery never scans the tree.</strong> A nested project&#39;s database loads when one of its files is opened; until then its units are not indexed. A rule naming the database (or <code>default_command</code>) covers it from the start.</p></li><li><p><strong>A borrowed command serves the editor only.</strong> A file compiling under an inferred command is not a member of the build: it is not indexed in the background and cannot host a header. Listing it in a database or under a <code>default_command</code> rule makes it one.</p></li><li><p><strong>Incomplete support for some compiler families.</strong> Intel compilers (<code>icc</code>, <code>icx</code>, <code>dpcpp</code>) are recognized but currently fall through to the generic Clang driver path without dedicated probing logic, so their special system paths may not be correctly discovered. NVCC has dedicated probing (parsing <code>nvcc --dryrun</code> output into a clang CUDA invocation), but only with a GCC or Clang host compiler -- nvcc driving MSVC <code>cl</code> is not supported yet, and multi-architecture commands parse the newest architecture only.</p></li><li><p><strong>SearchConfig does not support all search path options.</strong> <code>-cxx-isystem</code> (system directories effective only in C++ mode), <code>-iwithsysroot</code> (prepends sysroot to path), and HeaderMap support are not yet implemented. These options are uncommon in practice but may appear in specific Apple or cross-compilation toolchains.</p></li><li><p><strong>Global impact of configuration rules.</strong> <code>[[rules]]</code> in <code>clice.toml</code> can append or remove options from compilation commands. If the user modifies a rule that affects all files (e.g., appending a global <code>-I</code>), all files&#39; compilation configurations change, potentially triggering a full re-index. There is currently no mechanism to detect which rule changes actually affect which files.</p></li><li><p><strong>MSVC-style option parsing.</strong> On non-Windows systems, MSVC-style option prefixes (<code>/U</code>, <code>/D</code>, <code>/I</code>) must be handled specially to prevent Unix absolute paths (such as <code>/Users/...</code>) from being misparsed as MSVC options. This is currently resolved by dynamically adjusting option visibility based on the driver name, but edge cases may still exist.</p></li><li><p><strong>Compiler launchers are not recognized.</strong> CDB commands wrapped in a launcher (<code>ccache g++ ...</code>, <code>sccache clang++ ...</code>) are parsed as if the launcher were the compiler, so family identification and toolchain probing target the wrong binary. Strip the launcher from the compilation database, or override the flags with configuration rules.</p></li></ul>',68)])])}const m=t(n,[["render",r]]);export{u as __pageData,m as default};
