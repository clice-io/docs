import{_ as t,o as i,c as n,a3 as a}from"./chunks/framework.CRbjHCyB.js";const u=JSON.parse('{"title":"Symbol Index","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"Symbol Index | clice.io"}],["meta",{"property":"og:url","content":"https://docs.clice.io/clice/design/symbol-index"}],["meta",{"property":"og:image","content":"https://docs.clice.io/og.png"}],["meta",{"property":"og:image:width","content":"1200"}],["meta",{"property":"og:image:height","content":"630"}],["meta",{"property":"og:locale","content":"en_US"}],["link",{"rel":"canonical","href":"https://docs.clice.io/clice/design/symbol-index"}],["link",{"rel":"alternate","hreflang":"en","href":"https://docs.clice.io/clice/design/symbol-index"}],["link",{"rel":"alternate","hreflang":"zh-CN","href":"https://docs.clice.io/zh/clice/design/symbol-index"}],["link",{"rel":"alternate","hreflang":"x-default","href":"https://docs.clice.io/clice/design/symbol-index"}]]},"headers":[],"relativePath":"clice/design/symbol-index.md","filePath":"en/clice/design/symbol-index.md","lastUpdated":1788608469000}'),o={name:"clice/design/symbol-index.md"};function s(r,e,l,d,c,h){return i(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="symbol-index" tabindex="-1">Symbol Index <a class="header-anchor" href="#symbol-index" aria-label="Permalink to &quot;Symbol Index&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>Many features of a language server need to work across files. A user triggers &quot;go to definition&quot; in one file, and the target may be in any other file in the project; &quot;find references&quot; needs to scan every file in the project that might mention the symbol; call hierarchy and type hierarchy involve chains of symbol relationships across multiple files. To support these features, the language server must maintain a project-wide symbol index that records which files each symbol appears in and the semantic relationships between them (definition, reference, call, inheritance, etc.).</p><p>C++ makes index construction and maintenance particularly difficult.</p><p>The first level of difficulty is the compilation-context problem with header files. C++&#39;s <code>#include</code> is textual substitution — the contents of a header file are inserted verbatim into the source file that includes it at compile time. This means the same header file can produce entirely different symbols under different compilation contexts:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// crypto.h</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#ifdef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> USE_OPENSSL</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    using</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TLSContext</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OpenSSLContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#else</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    using</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TLSContext</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BoringSSLContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span></code></pre></div><p>When <code>crypto.h</code> is included by a source file that defines <code>USE_OPENSSL</code>, <code>TLSContext</code> is <code>OpenSSLContext</code>; when included by another source file, it is <code>BoringSSLContext</code>. Conditional compilation is the most obvious example, but include order and template instantiation can also cause header files to produce different symbol relationships under different contexts. If the index records only one context&#39;s results, users will see incorrect jump targets or incomplete reference lists after switching contexts.</p><p>The second level is scale. A mid-sized C++ project (a few thousand source files) produces hundreds of thousands of symbols after compilation; large projects (LLVM, Chromium) reach millions. The index system must maintain reasonable memory usage, build time, and query latency at this scale.</p><p>clangd&#39;s handling of both levels is insufficient. On the compilation-context front, clangd&#39;s background index stores only the last compilation result for each header — whichever source file is compiled last overwrites the previous index data. If a symbol reference exists only under a particular compilation context that is not the last one indexed, that reference is lost.</p><p>On the cross-file lookup front, clangd&#39;s background index stores symbol information in the index shard corresponding to the file where the symbol is declared. This declaration-file-centric storage model prevents reference counts from accumulating correctly across files (clangd <a href="https://github.com/clangd/clangd/issues/23" target="_blank" rel="noreferrer">#23</a>). Users also frequently encounter incomplete &quot;find references&quot; results — certain references only appear after manually opening the relevant files, at which point the dynamic index fills in the missing data (clangd <a href="https://github.com/clangd/clangd/issues/516" target="_blank" rel="noreferrer">#516</a>, <a href="https://github.com/clangd/clangd/issues/802" target="_blank" rel="noreferrer">#802</a>). Additionally, when compilation commands change, clangd&#39;s staleness detection does not trigger re-indexing (clangd <a href="https://github.com/clangd/clangd/issues/199" target="_blank" rel="noreferrer">#199</a>), leaving the index data out of sync with the actual compilation state for extended periods.</p><p>clice&#39;s index system is redesigned to address these problems: it separates the global symbol directory from per-file sharded relation data, merges different compilation contexts of the same file through content-identical variant deduplication, stores all index blobs in a single embedded database with zero-copy on-demand access, and overlays live data for open files to ensure query result freshness during editing.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="symbol-identity" tabindex="-1">Symbol Identity <a class="header-anchor" href="#symbol-identity" aria-label="Permalink to &quot;Symbol Identity&quot;">​</a></h3><p>The index system needs a way to identify the same symbol across files and translation units. clice uses <code>SymbolHash</code> (a 64-bit integer) as the unique identifier for each symbol.</p><p><code>SymbolHash</code> is generated from Clang&#39;s USR (Unified Symbol Resolution). USR is a canonical string representation of symbol identity that encodes the symbol&#39;s fully qualified name: namespace, class name, function signature, template parameters, etc. For example, <code>std::vector&lt;int&gt;::push_back</code> and <code>std::vector&lt;double&gt;::push_back</code> produce different USRs. <code>SymbolHash</code> is the hash of the USR string.</p><p><code>SymbolHash</code> has two key properties. First, cross-file consistency: the same symbol always has the same <code>SymbolHash</code> regardless of which file it appears in. <code>std::string</code> seen in file A and <code>std::string</code> seen in file B have the same hash, allowing all definitions and references to be associated through it — this is the foundation of cross-file navigation. Second, compactness: a 64-bit integer is better suited as a hash table key and for serialized storage than a variable-length USR string.</p><h3 id="symbol-occurrences-and-relations" tabindex="-1">Symbol Occurrences and Relations <a class="header-anchor" href="#symbol-occurrences-and-relations" aria-label="Permalink to &quot;Symbol Occurrences and Relations&quot;">​</a></h3><p>The index stores two kinds of core data: symbol occurrences (<code>Occurrence</code>) and symbol relations (<code>Relation</code>).</p><p>An <code>Occurrence</code> records a symbol&#39;s presence at a source location, containing only a source range and the target symbol&#39;s <code>SymbolHash</code>. It answers the question &quot;what symbol is under the cursor.&quot;</p><p>A <code>Relation</code> records richer semantic information, consisting of three elements: the relation kind (<code>RelationKind</code>), a source location, and a target symbol. Relation kinds cover common inter-symbol semantics:</p><ul><li>Definition and declaration (Definition, Declaration)</li><li>References (Reference, WeakReference)</li><li>Inheritance (Base, Derived)</li><li>Calls (Caller, Callee)</li><li>Type relationships (Interface, Implementation, TypeDefinition)</li><li>Construction and destruction (Constructor, Destructor)</li></ul><p>The two are stored separately because their query patterns differ. <code>Occurrence</code> is indexed by position — given a byte offset, binary search quickly locates the symbol under the cursor. <code>Relation</code> is indexed by <code>SymbolHash</code> — given a symbol, look up all its definitions, references, and call relationships. These two queries have contradictory sorting requirements; separate storage allows both to execute efficiently.</p><h3 id="index-hierarchy" tabindex="-1">Index Hierarchy <a class="header-anchor" href="#index-hierarchy" aria-label="Permalink to &quot;Index Hierarchy&quot;">​</a></h3><p>clice&#39;s index is organized into layers, each with a different lifecycle and responsibility:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TUIndex        Raw artifact from one compilation, discarded after merging</span></span>
<span class="line"><span>    ↓ merge</span></span>
<span class="line"><span>ProjectIndex   Global directory: external symbols + which TUs contributed which files</span></span>
<span class="line"><span>Shard          Per-file variant storage (exact positions and relations), loaded on demand</span></span>
<span class="line"><span>    ↑ overlay</span></span>
<span class="line"><span>Live rows      The open buffer&#39;s latest compile results, owned by its published projection</span></span></code></pre></div><p><strong>TUIndex</strong> is the raw index data produced by compiling a translation unit, projected from the unified semantic map built during the compile. Since a compilation involves the main file and all included headers, <code>TUIndex</code> internally maintains separate per-file row sets. It also carries a symbol table (hashes to names and kinds) and the include graph of this compilation. <code>TUIndex</code> is transient data, discarded after being merged into the persistent indexes.</p><p><strong>ProjectIndex</strong> is the global directory. It has two jobs: the global symbol table for <strong>externally visible</strong> symbols (<code>SymbolHash</code> → name, kind, reference-file bitmap, compressed as Roaring Bitmaps), and the record of which translation units contributed which files (the basis for variant liveness and reconciliation when TUs disappear). It does not store exact symbol positions — it tells you which files a symbol exists in; the positions live in the corresponding shard. This separation keeps <code>ProjectIndex</code> compact enough to reside in memory at all times.</p><p><strong>Shard</strong> is the per-file storage unit and the layer that actually serves queries. Each compilation context that preprocesses the file differently contributes one <strong>variant</strong> — a self-contained, canonically encoded blob of the file&#39;s rows. Names that are local to the file (internal linkage, function locals, anonymous-namespace members) live in the shard&#39;s own local-name table; only external names enter the <code>ProjectIndex</code>. Shards are loaded on demand — most are never touched in a session.</p><p><strong>Live rows</strong> for an open file come from its most recent in-memory compilation and are owned by the document&#39;s published projection. They are never written to global state — they only overlay queries, so results reflect the buffer the user is actually editing.</p><h3 id="symbol-table-layering" tabindex="-1">Symbol Table Layering <a class="header-anchor" href="#symbol-table-layering" aria-label="Permalink to &quot;Symbol Table Layering&quot;">​</a></h3><p>Symbol metadata (name, kind) is looked up in layers: an open file&#39;s live data first, then the <code>ProjectIndex</code> global table for external names, then the shard&#39;s local-name table for file-local names. Storage follows visibility — a symbol that no other file can reference never pollutes the global table.</p><h2 id="implementation" tabindex="-1">Implementation <a class="header-anchor" href="#implementation" aria-label="Permalink to &quot;Implementation&quot;">​</a></h2><h3 id="index-construction" tabindex="-1">Index Construction <a class="header-anchor" href="#index-construction" aria-label="Permalink to &quot;Index Construction&quot;">​</a></h3><p><code>TUIndex</code> construction is a projection over the unified semantic map: one AST traversal per compilation records every interesting node, and the index projection emits <code>Occurrence</code> and <code>Relation</code> rows per file. After projection, each file&#39;s rows are canonicalized — deduplicated and sorted (occurrences by position for binary search, relations by kind and position for filtering) — so that the encoded blob is deterministic.</p><p>During construction, the main file&#39;s rows are kept separate from the headers&#39;. This allows different treatment during merging — the main file is merged as the translation unit&#39;s own contribution, while headers are merged as context variants.</p><h3 id="variant-deduplication-by-content-identity" tabindex="-1">Variant Deduplication by Content Identity <a class="header-anchor" href="#variant-deduplication-by-content-identity" aria-label="Permalink to &quot;Variant Deduplication by Content Identity&quot;">​</a></h3><p>The core problem for shards is: the same header file is included by N source files, producing N row sets. If each were stored in full, storage would grow linearly with the number of translation units. But in practice, the vast majority of headers produce identical index data under different compilation contexts. Only headers like the <code>crypto.h</code> example above, affected by conditional compilation, produce different content under different contexts.</p><p>clice solves this by making <strong>the encoded bytes themselves the identity</strong>. Each variant is encoded canonically and deterministically — same rows, same bytes — and its identity (<code>RowsHash</code>) is the xxh3 hash of those bytes. When a new compilation contributes a variant:</p><ol><li>Encode the file&#39;s rows into the canonical blob and hash it</li><li>If a variant with this identity already exists in the shard, the merge is pure bookkeeping — the contributing TU is recorded, nothing is decoded or rewritten</li><li>If it is a new identity, the blob is added as a new variant</li></ol><p>In practice the bookkeeping-only path covers the overwhelming majority of merges, which is what makes full-project indexing cheap: re-indexing an unchanged header touches no shard bytes at all.</p><p>Which variants are <strong>live</strong> is controlled by variant masks derived from the contribution records: when a translation unit is removed or re-indexed, the variants it no longer vouches for drop out of the live set and are filtered from queries.</p><h3 id="zero-copy-storage" tabindex="-1">Zero-Copy Storage <a class="header-anchor" href="#zero-copy-storage" aria-label="Permalink to &quot;Zero-Copy Storage&quot;">​</a></h3><p>All index blobs — shards, manifests, the serialized <code>ProjectIndex</code> — live in a single embedded LMDB database. Blob encodings are canonical and self-describing; on load a blob is validated once and then used directly as a memory-mapped, zero-copy view. There is no deserialize-into-structs step on the read path.</p><p>At startup, only the <code>ProjectIndex</code> and the per-TU manifests (compact) are loaded. Shards are opened on demand, and most are never accessed in a single session.</p><h3 id="per-tu-context" tabindex="-1">Per-TU Context <a class="header-anchor" href="#per-tu-context" aria-label="Permalink to &quot;Per-TU Context&quot;">​</a></h3><p>Context information — which files a translation unit saw and through which include tree — is stored once per translation unit in its <strong>manifest</strong>, not duplicated into every shard. Shards know only variants; the manifests know which TU contributed which variant of which file. Staleness and reconciliation decisions (a TU disappeared from the CDB, a command changed, a dependency changed) are driven from the manifests and the contribution records.</p><h3 id="query-flow" tabindex="-1">Query Flow <a class="header-anchor" href="#query-flow" aria-label="Permalink to &quot;Query Flow&quot;">​</a></h3><p>Using &quot;find references&quot; as an example to illustrate the full cross-file query flow:</p><ol><li>In the current file, use the cursor&#39;s byte offset to binary-search the <code>Occurrence</code> rows and obtain the <code>SymbolHash</code> of the symbol under the cursor</li><li>Look up the <code>SymbolHash</code>&#39;s reference-file bitmap in <code>ProjectIndex</code> to get all files containing the symbol</li><li>Query each file in the list: <ul><li>If the file is open, its live rows (from the latest in-memory compile) answer, provided they are current for the buffer</li><li>Disk data for an <strong>open</strong> file is consulted only when the buffer content is byte-identical to the content the shard indexed — an edited buffer never serves stale disk positions</li><li>Files that are not open answer from their shard&#39;s live variants</li></ul></li><li>Aggregate all <code>Relation</code> rows found across files (filtering by the target <code>RelationKind</code>), convert to LSP positions, and return to the client</li></ol><p>For open files whose preamble is compiled into a PCH, the paired preamble-state blob (see below) supplies the rows the PCH swallowed.</p><blockquote><p>Converting offsets to LSP positions requires line-start information for the file. Shards store the line table (and enough of the content) for this conversion, so it works even for files that are not open.</p></blockquote><h3 id="staleness-detection" tabindex="-1">Staleness Detection <a class="header-anchor" href="#staleness-detection" aria-label="Permalink to &quot;Staleness Detection&quot;">​</a></h3><p>Staleness detection determines whether a file needs to be re-indexed. Each indexed artifact records the identity and observed content version of its inputs; validation is delegated to the master&#39;s shared file table, which performs the same two-layer check used everywhere else — a (size, mtime) stat fast path, then content-hash confirmation with stamp repair (see <a href="./incremental-parse">Incremental Compilation</a>). Re-indexing triggers only when input content actually changed; command changes are caught separately through the entry identity hashes recorded in the manifests.</p><h3 id="background-indexing-scheduling" tabindex="-1">Background Indexing Scheduling <a class="header-anchor" href="#background-indexing-scheduling" aria-label="Permalink to &quot;Background Indexing Scheduling&quot;">​</a></h3><p>Background indexing scheduling must balance index timeliness against interference with user interaction. The index module employs the following strategies:</p><ul><li><strong>Queue with idle delay</strong>: Files that need indexing are added to a queue, and processing begins only after the editor has been idle for a configurable period. This avoids triggering index tasks during rapid editing.</li><li><strong>Foreground-aware budget</strong>: Background index runs are capped to a fraction of the worker pool while the user is active and expand to full capacity when the foreground goes idle; memory pressure further shrinks the allowance (see <a href="./multi-process">Multi-process Architecture</a>).</li><li><strong>Result merging and persistence</strong>: Each index task compiles a file and builds a <code>TUIndex</code> in a stateless subprocess. The result is serialized and sent back to the main process, which merges it into the project index and shards. Modified blobs are committed to the database in batched transactions so they can be loaded directly on the next startup.</li></ul><h2 id="faq" tabindex="-1">FAQ <a class="header-anchor" href="#faq" aria-label="Permalink to &quot;FAQ&quot;">​</a></h2><ul><li><p><strong>Why separate <code>ProjectIndex</code> and shards instead of using a single unified index?</strong></p><p>If position information were also stored in <code>ProjectIndex</code>, its size would balloon dramatically, making it impossible to keep in memory. Without <code>ProjectIndex</code>, every cross-file query would need to traverse all shards to locate files containing the symbol — in a project with tens of thousands of files, loading that many shards is unacceptable. <code>ProjectIndex</code> serves as a lightweight directory layer that first narrows the search scope to a handful of specific files, then precise lookup happens in the corresponding shards.</p></li><li><p><strong>Why don&#39;t open-file indexes write to global state?</strong></p><p>Buffer content being edited by the user may be incomplete code with syntax errors. If this temporary state were written to the global index, it would pollute query results for other files. For example, a symbol definition temporarily disappearing from a header being edited would affect find-references results for every file that references that symbol. The global index only accepts stable state saved to disk, built through background indexing from disk files.</p></li><li><p><strong>Is there a hash collision risk with content-identity deduplication?</strong></p><p>Variant identity is a 64-bit hash of the encoded blob, so collisions are astronomically unlikely but not cryptographically impossible. A collision would make two genuinely different variants share one stored copy — wrong rows served for one context — but cannot corrupt data structures or crash the server. The trade-off (a much cheaper hash on the hot merge path) is deliberate.</p></li><li><p><strong>Why a custom canonical encoding instead of a general serialization framework?</strong></p><p>Two reasons. First, zero-copy: blobs validate once and serve queries directly from mapped memory, which general frameworks support to varying degrees. Second — the deeper one — <strong>byte identity is the merge currency</strong>: because the encoding is a strict, deterministic function of the rows, &quot;same content&quot; and &quot;same bytes&quot; coincide, and deduplication becomes a hash comparison instead of a structural diff. A framework whose output can vary (field ordering, padding, versioned wire formats) would break that equivalence.</p></li><li><p><strong>Why store <code>Occurrence</code> and <code>Relation</code> separately?</strong></p><p><code>Occurrence</code> is indexed by position — given an offset, binary search locates the symbol under the cursor, requiring position-sorted data. <code>Relation</code> is indexed by <code>SymbolHash</code> — given a symbol, look up all its relationships, requiring symbol-grouped data. Combining them into a single data structure would inevitably sacrifice efficiency in at least one of the two query patterns.</p></li><li><p><strong>Why are cross-file queries performed in the main process rather than subprocesses?</strong></p><p>clice uses a multi-process architecture where each open file is compiled in its own stateful subprocess. Cross-file queries (such as find-references) need to combine the live rows of all open files with the persistent index. If those live rows were spread across different subprocesses, each query would require cross-process communication with multiple workers and then result aggregation — unacceptable in both latency and complexity. Instead, subprocesses send their index rows back to the main process after compilation, and the main process performs queries uniformly — it can see all open files&#39; live rows as well as the project index and shards, completing the full query flow in a single process.</p><p>This design also has a semantic consideration: query results for open files should reflect the editor&#39;s buffer state, not the disk state. Even if a file on disk has been modified by an external tool, as long as the editor has not sent a <code>didChange</code> notification, query results should be based on the version the editor holds. Centralizing the live rows in the main process makes this semantic invariant easier to maintain.</p></li><li><p><strong>Why an embedded database for index storage, but plain files for PCH/PCM?</strong></p><p>They have opposite shapes. Index blobs are numerous (one shard per project file), individually small, and written in bursts during background indexing — exactly the profile where a single LMDB database wins: batched atomic commits, no per-file dirent pressure at scale, and memory-mapped reads that preserve the zero-copy path. PCH and PCM files are the opposite: few, large (hundreds of MB), memory-mapped directly by Clang itself, with a trivial create/read/evict lifecycle — the filesystem plus the artifact store&#39;s atomic-rename commits handles them well, and Clang could not consume them out of a database anyway. On filesystems where the database cannot operate safely (network mounts), index persistence is disabled rather than degraded.</p></li></ul><h2 id="known-limitations" tabindex="-1">Known Limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known Limitations&quot;">​</a></h2><ul><li><p><strong>Cross-TU queries for internal-linkage symbols.</strong> File-local names are stored in shard local-name tables, but the cross-file relation query path currently reaches disk only through the external-symbol directory — so &quot;find references&quot; on a <code>static</code> function returns only what the live overlay can see. The storage layering is in place; the query path for local symbols is not yet.</p></li><li><p><strong>Fuzzy symbol search</strong>. The current workspace symbol search (workspace/symbol) is a simple substring match that does a linear scan over all symbols in <code>ProjectIndex</code>. This is insufficient for large projects and does not support fuzzy matching.</p><p>C++ symbol names have structure: <code>getSymbolHash</code> is camelCase, <code>get_symbol_hash</code> is snake_case, <code>std::vector&lt;int&gt;::push_back</code> has namespace qualification. When searching, users typically type abbreviations or fragments (e.g., <code>symhash</code>, <code>gSH</code>, <code>vec_pb</code>), expecting them to match the full symbol name. Substring matching cannot handle these queries.</p><p>The improvement direction is to build a dedicated search index over symbol names. A tokenizer is needed to split symbol names by naming conventions (<code>getSymbolHash</code> → <code>[get, Symbol, Hash]</code>, <code>push_back</code> → <code>[push, back]</code>), then build an inverted index over the tokens. For example, trigrams (three-character groups) can be used as index keys, and at query time trigram intersections produce a candidate set that is then scored precisely. clangd&#39;s Dex index uses this trigram posting list approach and serves as a useful reference implementation. Another direction is to adopt a mature full-text search library, though the cost of introducing an external dependency needs to be evaluated.</p></li><li><p><s><strong>PCH-induced index split</strong></s> (resolved). When using PCH optimization, a file&#39;s compilation is split into two phases: the preamble is compiled into the PCH, then the PCH compiles the rest of the file. The PCH swallows everything before the preamble bound — the main file&#39;s compilation cannot see the headers&#39; contents or the preamble region&#39;s own directives, and an open buffer&#39;s preamble may describe a compilation context that no disk translation unit was ever indexed with.</p><p>This is now addressed by pairing each PCH with a preamble-state blob, produced by the same worker build while the freshly parsed preamble AST is still in memory (the only moment its index is obtainable without deserializing the whole PCH). The blob carries the preamble&#39;s full symbol index — every covered header plus the main file&#39;s preamble region — together with per-file content and line tables for position mapping, document links, inactive regions and the open conditional stack. It is stored, hit and evicted together with the PCH, opened as a memory-mapped FlatBuffer and queried without deserialization. Open files overlay these blobs onto index queries: set queries take the union with disk shards (identical rows collapse by location), single-answer queries prefer the overlay, and the buffer&#39;s preamble region resolves through the blob&#39;s main-file entry. This keeps navigation working for open files whose translation unit the background indexer has not (or cannot) index, and keeps results faithful to unsaved preamble edits.</p></li></ul>`,60)])])}const m=t(o,[["render",s]]);export{u as __pageData,m as default};
