import{_ as a,o,c,a3 as d}from"./chunks/framework.aYYe0dfI.js";const u=JSON.parse('{"title":"Module generate:evidence","description":"generate:evidence 模块负责为文档生成管线构建和组织结构化的证据数据，这些数据随后用于格式化文本或直接构造面向语言模型的提示。它公开了一系列 build_evidence_for_* 函数，针对命名空间摘要、模块架构、索引概览以及各类符号（函数、类型、变量）的分析与实现摘要等不同页面类型生成证据包。此外，模块还提供了 format_evidence_text 和 format_evidence_text_bounded 用于将证据包渲染为文本，以及 build_prompt 用于将证据组装为完整的提示模板。核心数据结构 SymbolFact 和 EvidencePack 也是该模块公共实现的一部分，为下游消费提供统一的证据表示。","frontmatter":{"title":"Module generate:evidence","description":"generate:evidence 模块负责为文档生成管线构建和组织结构化的证据数据，这些数据随后用于格式化文本或直接构造面向语言模型的提示。它公开了一系列 build_evidence_for_* 函数，针对命名空间摘要、模块架构、索引概览以及各类符号（函数、类型、变量）的分析与实现摘要等不同页面类型生成证据包。此外，模块还提供了 format_evidence_text 和 format_evidence_text_bounded 用于将证据包渲染为文本，以及 build_prompt 用于将证据组装为完整的提示模板。核心数据结构 SymbolFact 和 EvidencePack 也是该模块公共实现的一部分，为下游消费提供统一的证据表示。","layout":"doc","template":"doc"},"headers":[],"relativePath":"zh/clore/modules/generate/evidence.md","filePath":"zh/clore/modules/generate/evidence.md"}'),t={name:"zh/clore/modules/generate/evidence.md"};function r(i,e,l,n,s,m){return o(),c("div",null,[...e[0]||(e[0]=[d('<h1 id="module-generate-evidence" tabindex="-1">Module <code>generate:evidence</code> <a class="header-anchor" href="#module-generate-evidence" aria-label="Permalink to &quot;Module `generate:evidence`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p><code>generate:evidence</code> 模块负责为文档生成管线构建和组织结构化的证据数据，这些数据随后用于格式化文本或直接构造面向语言模型的提示。它公开了一系列 <code>build_evidence_for_*</code> 函数，针对命名空间摘要、模块架构、索引概览以及各类符号（函数、类型、变量）的分析与实现摘要等不同页面类型生成证据包。此外，模块还提供了 <code>format_evidence_text</code> 和 <code>format_evidence_text_bounded</code> 用于将证据包渲染为文本，以及 <code>build_prompt</code> 用于将证据组装为完整的提示模板。核心数据结构 <code>SymbolFact</code> 和 <code>EvidencePack</code> 也是该模块公共实现的一部分，为下游消费提供统一的证据表示。</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../extract/"><code>extract</code></a></li><li><a href="./model"><code>generate:model</code></a></li><li><code>std</code></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./analysis"><code>generate:analysis</code></a></li><li><a href="./"><code>generate:evidence_builder</code></a></li><li><a href="./scheduler"><code>generate:scheduler</code></a></li></ul><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-generate-evidencepack" tabindex="-1"><code>clore::generate::EvidencePack</code> <a class="header-anchor" href="#clore-generate-evidencepack" aria-label="Permalink to &quot;`clore::generate::EvidencePack`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:22</code></p><p>Definition: <code>generate/evidence.cppm:22</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p><code>clore::generate::EvidencePack</code> 将生成提示所需的全部证据汇总为单一结构，通过划分不同的事实向量（<code>local_context</code>、<code>dependency_context</code>、<code>reverse_usage_context</code> 和 <code>target_facts</code>）分别承载主体自身的上下文、其依赖项的上下文、被使用方的反向上下文以及目标事实。<code>subject_name</code> 与 <code>subject_kind</code> 标识证据所描述的对象，而 <code>source_snippets</code> 保留原始代码片段，<code>related_page_summaries</code> 收集关联文档页的摘要。<code>page_id</code> 和 <code>prompt_kind</code> 记录来源页面与提示种类，用于后续编排。这些字段均以标准容器存储，空向量表示对应证据缺失，但结构本身不强制非空约束——所有字段在构造后可被自由填充，调用方需保证在构建提示前已按需组装完整。</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>All fields are intended to be fully populated before use</li><li><code>page_id</code> and <code>prompt_kind</code> uniquely identify the generation task</li><li><code>subject_name</code> and <code>subject_kind</code> describe the target symbol</li><li>Vectors may be empty if no relevant evidence exists</li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>subject_name</code> and <code>subject_kind</code></li><li><code>target_facts</code></li><li><code>local_context</code>, <code>dependency_context</code>, <code>reverse_usage_context</code></li><li><code>page_id</code> and <code>prompt_kind</code></li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Constructed by evidence gathering subsystems</li><li>Passed to prompt builders or directly to LLM-based documenters</li><li>Consumed by documentation generation pipelines for a single symbol</li></ul><h3 id="clore-generate-prompterror" tabindex="-1"><code>clore::generate::PromptError</code> <a class="header-anchor" href="#clore-generate-prompterror" aria-label="Permalink to &quot;`clore::generate::PromptError`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:90</code></p><p>Definition: <code>generate/evidence.cppm:90</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该结构体仅包含一个类型为 <code>std::string</code> 的数据成员 <code>message</code>，用于携带错误描述文本。其默认构造函数、拷贝构造函数、移动构造函数、拷贝赋值运算符、移动赋值运算符以及析构函数均由编译器隐式生成，这些操作均会按成员对 <code>std::string</code> 执行相应的浅层或深层复制/移动及析构。由于不涉及自定义资源管理或其他非平凡成员，该类型在对象生命周期内的行为完全由 <code>std::string</code> 的语义保证，无额外不变式需要维持。所有隐式特殊成员函数均为 inline 并在定义处由编译器合成，因此该结构体可以高效地用于值传递或异常抛出等场景。</p><h4 id="invariants-1" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-1" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>message</code> 包含可读的错误描述</li></ul><h4 id="key-members-1" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-1" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>message</code></li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>作为错误结果返回</li><li>用于传递生成阶段的错误信息</li></ul><h3 id="clore-generate-symbolfact" tabindex="-1"><code>clore::generate::SymbolFact</code> <a class="header-anchor" href="#clore-generate-symbolfact" aria-label="Permalink to &quot;`clore::generate::SymbolFact`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:9</code></p><p>Definition: <code>generate/evidence.cppm:9</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p><code>SymbolFact</code> 是一个纯数据聚合类型，用作从符号提取到代码生成管线的中间传输载体。其所有成员均为公有且无自定义构造或复制控制，保持平凡聚合性质。核心字段包括唯一标识符 <code>extract::SymbolID id</code>、<code>qualified_name</code>、<code>signature</code>、<code>kind_label</code>、<code>access</code>、模板标志 <code>is_template</code>（默认为 <code>false</code>）与 <code>template_params</code>、声明位置 <code>declaration_file</code> 和 <code>declaration_line</code>（默认 <code>0</code>），以及可选文档注释 <code>doc_comment</code>。结构内部无额外不变式：任何字段均可独立设置，且默认值仅用于未显式初始化时的安全状态。整个结构体的设计在于简单、零开销地传递符号事实，不承担任何业务逻辑。</p><h4 id="invariants-2" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-2" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>id</code> is globally unique</li><li><code>qualified_name</code> is the fully qualified name</li><li><code>is_template</code> is <code>true</code> if the symbol is a template specialization</li></ul><h4 id="key-members-2" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-2" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>id</code></li><li><code>qualified_name</code></li><li><code>kind_label</code></li><li><code>doc_comment</code></li><li><code>declaration_file</code></li><li><code>declaration_line</code></li><li><code>signature</code></li><li><code>access</code></li><li><code>is_template</code></li><li><code>template_params</code></li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Populated by extraction phase</li><li>Consumed by generation phase to produce documentation output</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-generate-detail-collect-analysis-summaries" tabindex="-1"><code>clore::generate::__detail::collect_analysis_summaries</code> <a class="header-anchor" href="#clore-generate-detail-collect-analysis-summaries" aria-label="Permalink to &quot;`clore::generate::__detail::collect_analysis_summaries`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:121</code></p><p>Definition: <code>generate/evidence.cppm:241</code></p><p>函数 <code>clore::generate::__detail::collect_analysis_summaries</code> 作为对外的转发入口，其实现完全委托给 <code>clore::generate::__detail::collect_analysis_summaries_impl</code>。传入的 <code>analyses</code>（<code>SymbolAnalysisStore</code> 类型）、<code>model</code>（<code>extract::ProjectModel</code> 类型）和 <code>ids</code>（<code>std::vector&lt;extract::SymbolID&gt;</code> 类型）原样传递，同时提供一个恒等映射 lambda 作为 <code>SymbolIDOf</code> 参数，使得每个符号 ID 直接映射到自身。内部控制流由此进入 <code>collect_analysis_summaries_impl</code>，该函数会遍历 <code>ids</code>，为每个符号 ID 调用 <code>collect_facts</code> 收集事实，并依据符号种类（如函数、类型、变量、命名空间等）调用对应的 <code>build_evidence_*</code> 系列函数（例如 <code>build_evidence_for_function_analysis</code>、<code>build_evidence_for_type_declaration_summary</code> 等）构建 <code>EvidencePack</code>，再通过 <code>format_evidence_text_bounded</code> 或 <code>format_evidence_text</code> 格式化证据为文本摘要。此外，该实现还依赖 <code>collect_namespace_facts_cached</code> 进行跨符号的缓存查询，并利用 <code>render_summary_item</code>、<code>render_detailed_fact</code> 等辅助函数控制输出格式和长度约束，最终生成 <code>std::vector&lt;std::string&gt;</code> 返回。</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>参数 <code>analyses</code></li><li>参数 <code>model</code></li><li>参数 <code>ids</code></li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>作为收集分析摘要的顶层入口</li></ul><h3 id="clore-generate-detail-collect-analysis-summaries-impl" tabindex="-1"><code>clore::generate::__detail::collect_analysis_summaries_impl</code> <a class="header-anchor" href="#clore-generate-detail-collect-analysis-summaries-impl" aria-label="Permalink to &quot;`clore::generate::__detail::collect_analysis_summaries_impl`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:116</code></p><p>Definition: <code>generate/evidence.cppm:213</code></p><p>The function iterates over each element in the <code>items</code> range, applying the callable <code>symbol_id_of</code> to extract an <code>extract::SymbolID</code>. A <code>std::unordered_set</code> named <code>seen</code> deduplicates these <code>IDs</code>; if an ID is already present, the element is skipped. For a new ID, the function performs a <code>extract::lookup_symbol</code> on the <code>model</code> to obtain a pointer to the corresponding symbol. If the pointer is null, the iteration continues. Otherwise, it retrieves an analysis overview string by calling <code>analysis_overview_markdown</code> on the <code>analyses</code> and the symbol; if that result is null or empty, the element is also skipped.</p><p>Only after passing all checks is the non‑empty summary string appended to the result vector. The result is pre‑reserved using <code>items.size()</code> to minimize reallocations. The function therefore implements a filtered, deduplicated collection of existing analysis summaries for the symbols identified by the given range and projection.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>analyses</code> parameter</li><li><code>model</code> parameter</li><li><code>items</code> parameter</li><li><code>symbol_id_of</code> callable</li><li><code>extract::lookup_symbol</code> return value</li><li><code>analysis_overview_markdown</code> return value</li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to collect analysis summaries for a range of items</li><li>Used by <code>collect_analysis_summaries</code></li><li>Filters duplicates and missing summaries</li></ul><h3 id="clore-generate-detail-collect-facts" tabindex="-1"><code>clore::generate::__detail::collect_facts</code> <a class="header-anchor" href="#clore-generate-detail-collect-facts" aria-label="Permalink to &quot;`clore::generate::__detail::collect_facts`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:103</code></p><p>Definition: <code>generate/evidence.cppm:163</code></p><p>该函数遍历 <code>ids</code> 中的每个 symbol ID，对每个 ID 检查是否已在 <code>seen</code> 集合中出现；若重复则跳过，否则尝试通过 <code>extract::lookup_symbol</code> 在当前 <code>model</code> 中查找对应的符号对象。若找到，则调用 <code>to_symbol_fact</code> 构建 <code>SymbolFact</code> 并加入结果列表。最后返回去重后的 <code>facts</code> 向量。</p><p>内部控制流完全依赖于 <code>std::unordered_set</code> 实现的线性去重以及 <code>extract::lookup_symbol</code> 的查找结果。函数本身不执行任何分析或摘要生成，仅负责将原始的符号 ID 集合转换为对应的 <code>SymbolFact</code> 对象集合，并为后续的摘要收集阶段提供结构化的输入数据。</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>model (parameter)</li><li>ids (parameter)</li><li><code>project_root</code> (parameter)</li><li><code>extract::lookup_symbol</code> (reads model&#39;s internal data)</li></ul><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to collect facts for a set of symbol <code>IDs</code> from a project model</li><li>Used by higher-level functions that need a deduplicated list of <code>SymbolFact</code> objects</li></ul><h3 id="clore-generate-detail-collect-merged-facts" tabindex="-1"><code>clore::generate::__detail::collect_merged_facts</code> <a class="header-anchor" href="#clore-generate-detail-collect-merged-facts" aria-label="Permalink to &quot;`clore::generate::__detail::collect_merged_facts`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:108</code></p><p>Definition: <code>generate/evidence.cppm:179</code></p><p>该函数将多个传入的符号 ID 集合（<code>groups</code>）合并为一个无重复的 <code>SymbolFact</code> 向量。它通过一个 <code>seen</code> 无序集合来跟踪已处理过的 <code>extract::SymbolID</code>，确保每个符号只被转换一次。内部使用折叠表达式 <code>(append_group(groups), ...)</code> 依次处理每组 ID，对每个组内的每个 ID 检查是否已在 <code>seen</code> 中；若未出现过则插入并调用 <code>extract::lookup_symbol</code> 在 <code>model</code> 中查找符号，再通过 <code>to_symbol_fact</code> 转换为 <code>SymbolFact</code> 追加到结果列表中。</p><p>控制流完全由 <code>append_group</code> lambda 驱动，依赖的外部函数包括 <code>extract::lookup_symbol</code>（从项目模型中按 ID 获取符号）和 <code>to_symbol_fact</code>（将符号表示转换为带项目根路径的事实对象）。整个过程是纯线性扫描，利用集合去重保证最终返回的 <code>facts</code> 中每个符号仅出现一次。</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>const <code>extract::ProjectModel</code>&amp; model</li><li>const <code>std::string</code>&amp; <code>project_root</code></li><li>const Groups&amp;... groups</li><li>elements of each group</li><li>data accessed by <code>extract::lookup_symbol</code> and <code>to_symbol_fact</code></li></ul><h4 id="usage-patterns-6" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-6" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called with multiple group containers to aggregate facts</li><li>used in generating documentation by merging facts from different sources</li><li>employs fold expression to process variadic group arguments</li></ul><h3 id="clore-generate-detail-collect-namespace-facts-cached" tabindex="-1"><code>clore::generate::__detail::collect_namespace_facts_cached</code> <a class="header-anchor" href="#clore-generate-detail-collect-namespace-facts-cached" aria-label="Permalink to &quot;`clore::generate::__detail::collect_namespace_facts_cached`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:133</code></p><p>Definition: <code>generate/evidence.cppm:275</code></p><p>该函数从 <code>model</code> 中收集指定 <code>namespace_name</code> 下属于给定 <code>target_kind</code>（函数、类型或变量）的符号事实，并利用静态缓存 <code>namespace_facts_cache()</code> 避免重复遍历。缓存键由 <code>namespace_name</code> 和 <code>project_root</code> 拼接构成。若缓存未命中，则遍历命名空间内的所有符号，通过 <code>to_symbol_fact</code> 转换为 <code>SymbolFact</code>，并按符号种类分别存入 <code>NamespaceFactsCacheEntry</code> 的 <code>all_functions</code>、<code>all_types</code> 或 <code>all_variables</code> 向量中；若命名空间不存在，则对应向量保持为空。后续调用根据 <code>target_kind</code> 选择对应子向量，并排除 <code>exclude_id</code> 对应的符号后返回结果。若命名空间为空或 <code>target_kind</code> 不在指定范围内，则返回空向量。</p><h4 id="side-effects-4" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-4" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Mutates namespace facts cache (<code>namespace_facts_cache</code>)</li><li>Allocates memory for cache entries and result vector</li></ul><h4 id="reads-from-4" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-4" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>model<code>.namespaces</code></li><li><code>namespace_name</code></li><li><code>project_root</code></li><li><code>target_kind</code></li><li><code>exclude_id</code></li><li>global <code>namespace_facts_cache</code></li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>namespace_facts_cache</code></li></ul><h4 id="usage-patterns-7" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-7" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called when collecting facts for multiple symbols in the same namespace to avoid redundant traversals</li><li>Used by higher-level fact collection functions</li></ul><h3 id="clore-generate-detail-collect-summaries" tabindex="-1"><code>clore::generate::__detail::collect_summaries</code> <a class="header-anchor" href="#clore-generate-detail-collect-summaries" aria-label="Permalink to &quot;`clore::generate::__detail::collect_summaries`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:112</code></p><p>Definition: <code>generate/evidence.cppm:200</code></p><p>函数 <code>clore::generate::__detail::collect_summaries</code> 遍历 <code>keys</code> 容器中的每一个键，对每个键在 <code>cache</code>（一个 <code>PageSummaryCache</code> 类型的映射）中执行查找。若查找成功且对应的摘要值非空，则将该摘要追加到结果向量中。最终返回包含所有非空摘要的 <code>std::vector&lt;std::string&gt;</code>。该函数的控制流是线性迭代与条件筛选的结合，其依赖仅涉及 <code>PageSummaryCache</code> 的 <code>find</code> 接口以及 <code>keys</code> 的迭代能力；没有嵌套循环或递归分支，逻辑简洁且专注于从缓存中按指定键提取有效数据。</p><h4 id="side-effects-5" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-5" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-5" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-5" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>cache</code> (a <code>PageSummaryCache</code> object)</li><li><code>keys</code> (a <code>std::vector&lt;std::string&gt;</code>)</li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>the returned <code>std::vector&lt;std::string&gt;</code></li></ul><h4 id="usage-patterns-8" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-8" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to collect summary strings from a cache by a list of keys</li><li>Called to filter out empty summary entries</li></ul><h3 id="clore-generate-detail-to-symbol-fact" tabindex="-1"><code>clore::generate::__detail::to_symbol_fact</code> <a class="header-anchor" href="#clore-generate-detail-to-symbol-fact" aria-label="Permalink to &quot;`clore::generate::__detail::to_symbol_fact`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:101</code></p><p>Definition: <code>generate/evidence.cppm:147</code></p><p>函数 <code>clore::generate::__detail::to_symbol_fact</code> 通过聚合初始化直接构造并返回一个 <code>clore::generate::SymbolFact</code> 对象。它将输入的 <code>extract::SymbolInfo</code> 的 <code>id</code>、<code>qualified_name</code>、<code>signature</code>、<code>access</code>、<code>is_template</code>、<code>template_params</code> 和 <code>doc_comment</code> 字段原样复制到 <code>SymbolFact</code> 的对应成员；通过调用 <code>extract::symbol_kind_name</code> 将 <code>kind</code> 枚举转换为字符串标签填入 <code>kind_label</code>；并将 <code>declaration_location.file</code> 经 <code>clore::generate::make_source_relative</code> 处理为相对项目根的路径后赋给 <code>declaration_file</code>，同时复制 <code>declaration_location.line</code> 至 <code>declaration_line</code>。</p><p>整个函数不包含分支或循环，完全依赖外部函数 <code>extract::symbol_kind_name</code> 进行类型名称转换以及 <code>clore::generate::make_source_relative</code> 进行路径规范化，其依赖的输入结构体 <code>extract::SymbolInfo</code> 的字段布局决定了输出的映射关系。</p><h4 id="side-effects-6" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-6" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-6" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-6" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>sym</code> (all fields)</li><li><code>project_root</code></li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returned <code>SymbolFact</code> object (local, not externally observable)</li></ul><h4 id="usage-patterns-9" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-9" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during symbol fact collection pipelines</li><li>Converts extraction results to internal fact representation</li></ul><h3 id="clore-generate-build-evidence-for-function-analysis" tabindex="-1"><code>clore::generate::build_evidence_for_function_analysis</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-function-analysis" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_function_analysis`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:40</code></p><p>Definition: <code>generate/evidence_builder.cppm:53</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数的核心逻辑围绕遍历 <code>project_root</code> 下的索引树展开，通过 <code>root</code> 和 <code>file_it</code> 等迭代器定位目标模块与命名空间。内部控制流首先利用 <code>maybe_resolve_snippet</code> 检查并解析可能存在的代码片段，随后收集来自 <code>page_summaries</code>、<code>imported_summaries</code> 以及直接分析的 <code>analyses</code> 信息，这些信息通过 <code>pack</code> 逐步聚合。</p><p>在数据组装阶段，<code>sym</code>、<code>target</code> 以及 <code>namespace_fact</code> 等变量用于标识待分析函数的目标上下文，而 <code>model</code> 则作为最终证据结构被填充。函数依赖 <code>mod</code> 和 <code>ns</code> 进行模块与命名空间的解析，并通过 <code>root</code> 和 <code>project_root</code> 维持路径关系，最终返回一个 <code>int</code> 类型的摘要标识（如成功码或证据索引），完成从原始索引到结构化分析证据的构建。</p><h4 id="side-effects-7" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-7" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-7" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-7" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>reads the first <code>const int &amp;</code> parameter (likely a function analysis context)</li><li>reads the second <code>const int &amp;</code> parameter (likely an evidence pack or related data)</li><li>reads the <code>int</code> parameter (likely a limit or index)</li></ul><h4 id="usage-patterns-10" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-10" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during evidence construction for function analysis</li><li>Possibly used in a builder pattern for generating evidence packs</li></ul><h3 id="clore-generate-build-evidence-for-function-declaration-summary" tabindex="-1"><code>clore::generate::build_evidence_for_function_declaration_summary</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-function-declaration-summary" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_function_declaration_summary`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:67</code></p><p>Definition: <code>generate/evidence_builder.cppm:238</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数从 <code>project_root</code> 出发遍历 <code>pack</code> 中的每个模块，对每个模块使用 <code>model</code> 获取命名空间事实与函数声明。核心循环对每个 <code>target</code> 声明调用匿名辅助函数 <code>maybe_resolve_snippet</code> 来判断相关的代码片段能否内联解决，然后将结果收集到 <code>page_summaries</code> 中。同时，函数利用 <code>analyses</code> 中的分析对象驱动证据生成，并通过 <code>imported_summaries</code> 处理跨模块的摘要依赖，最终将各模块的摘要归并到 <code>root</code> 结构中。</p><h4 id="side-effects-8" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-8" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h3 id="clore-generate-build-evidence-for-function-implementation-summary" tabindex="-1"><code>clore::generate::build_evidence_for_function_implementation_summary</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-function-implementation-summary" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_function_implementation_summary`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:72</code></p><p>Definition: <code>generate/evidence_builder.cppm:268</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数通过整合多个内部数据源来构建证据包。首先，它从 <code>project_root</code> 和 <code>root</code> 初始化基础路径，随后遍历 <code>page_summaries</code> 并依赖 <code>maybe_resolve_snippet</code> 解析其中嵌入的代码片段引用，确保所有动态内容均被展开为具体文本。之后，函数组合 <code>model</code>、<code>analyses</code>、<code>imported_summaries</code> 以及命名空间事实 <code>namespace_fact</code>，并借助 <code>sym</code>、<code>target</code> 等变量建立符号映射与目标关联，最终将所有元素打包为 <code>pack</code> 结构返回。<code>file_it</code> 和 <code>mod</code> 用于遍历文件与模块层次结构，控制整个循环的进程。</p><h4 id="side-effects-9" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-9" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-8" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-8" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameters <code>const int &amp;</code>, <code>const int &amp;</code>, <code>int</code></li><li>associated analysis data from <code>SymbolAnalysisStore</code> or <code>EvidencePack</code></li></ul><h4 id="usage-patterns-11" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-11" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called when generating page markdown for function implementation summaries</li><li>used in conjunction with <code>build_evidence_for_function_analysis</code> and <code>render_page_markdown</code></li></ul><h3 id="clore-generate-build-evidence-for-index-overview" tabindex="-1"><code>clore::generate::build_evidence_for_index_overview</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-index-overview" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_index_overview`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:64</code></p><p>Definition: <code>generate/evidence_builder.cppm:204</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该实现首先在 <code>project_root</code> 下定位 <code>root</code>，并通过 <code>file_it</code> 遍历文件系统结构以收集 <code>page_summaries</code>。对于每个页面摘要，算法调用 <code>clore::generate::(anonymous namespace)::maybe_resolve_snippet</code> 来判断是否需要解析内联片段，若返回真则触发片段解析过程。在此过程中，函数维护一个 <code>pack</code> 容器，用于累积模块（<code>mod</code>）、命名空间（<code>ns</code>、<code>namespace_fact</code>）及符号（<code>sym</code>）等分析单元，并借助 <code>analyses</code> 和 <code>model</code> 对象来存储推理结果与索引模型。最终，<code>imported_summaries</code> 从外部模型合并，所有信息被整合为 <code>target</code> 的证据结构，以支持索引概览的生成。控制流主要围绕对 <code>page_summaries</code> 的循环、对 <code>maybe_resolve_snippet</code> 的条件分支，以及通过 <code>pack</code> 对多层级命名空间和模块的递归处理。</p><h4 id="side-effects-10" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-10" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-9" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-9" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>The two <code>const int &amp;</code> parameters, likely representing symbol or page identifiers for the index overview.</li></ul><h4 id="usage-patterns-12" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-12" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during page generation to produce evidence for the index overview.</li></ul><h3 id="clore-generate-build-evidence-for-module-architecture" tabindex="-1"><code>clore::generate::build_evidence_for_module_architecture</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-module-architecture" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_module_architecture`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:58</code></p><p>Definition: <code>generate/evidence_builder.cppm:173</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数首先获取 <code>project_root</code> 和 <code>root</code> 节点，然后遍历模块层次结构，为每个 <code>pack</code>、<code>mod</code> 和 <code>namespace_fact</code> 调用 <code>clore::generate::(anonymous namespace)::maybe_resolve_snippet</code> 来解析代码片段，并将结果累积到 <code>analyses</code>、<code>page_summaries</code> 和 <code>imported_summaries</code> 中。内部控制流通过嵌套循环处理 <code>target</code> 下的子模块，并使用 <code>model</code> 对象来暂存分析数据，同时依赖 <code>project_root</code> 和 <code>root</code> 来定位资源并避免重复导入。最终，函数将这些收集到的证据整合为模块架构的完整描述。</p><h4 id="side-effects-11" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-11" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-10" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-10" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>All integer parameters (module identifier, file identifiers, analysis context)</li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Return value (int) indicating success or failure</li></ul><h4 id="usage-patterns-13" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-13" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during page generation for module-level documentation</li><li>Used to populate evidence for architecture summary sections</li><li>Invoked by <code>clore::generate::build_page_root</code> or similar page builders</li></ul><h3 id="clore-generate-build-evidence-for-module-summary" tabindex="-1"><code>clore::generate::build_evidence_for_module_summary</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-module-summary" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_module_summary`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:52</code></p><p>Definition: <code>generate/evidence_builder.cppm:142</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数通过迭代根结点 <code>root</code> 下的文件结构来构建证据。它使用 <code>file_it</code> 遍历每个文件，结合 <code>project_root</code> 确定文件路径，并对每个文件调用 <code>clore::generate::(anonymous namespace)::maybe_resolve_snippet</code> 判断是否需要解析其中出现的代码片段。解析后收集的摘要信息被存入 <code>page_summaries</code>，并进一步汇入 <code>analyses</code> 和 <code>model</code> 对象中。</p><p>后续处理涉及 <code>imported_summaries</code> 与 <code>namespace_fact</code>，函数将这些数据整合到 <code>pack</code>、<code>mod</code>、<code>ns</code> 等更粗粒度的表示中，同时更新 <code>target</code> 和 <code>sym</code> 等符号信息。最终 <code>model</code> 结合 <code>analyses</code> 与 <code>project_root</code> 生成证据，返回一个表示构建结果的值。整个控制流以内联函数 <code>maybe_resolve_snippet</code> 和各类集合操作为依赖，未依赖外部公开接口。</p><h4 id="side-effects-12" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-12" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-11" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-11" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the four <code>const int&amp;</code> parameters</li><li>the <code>int</code> parameter</li></ul><h4 id="usage-patterns-14" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-14" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called when generating module documentation pages</li></ul><h3 id="clore-generate-build-evidence-for-namespace-summary" tabindex="-1"><code>clore::generate::build_evidence_for_namespace_summary</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-namespace-summary" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_namespace_summary`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:35</code></p><p>Definition: <code>generate/evidence_builder.cppm:21</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数通过遍历 <code>page_summaries</code> 和 <code>pack</code> 来构建证据。首先，它从 <code>root</code> 和 <code>project_root</code> 出发，对每个 <code>file_it</code> 迭代，调用 <code>clore::generate::(anonymous namespace)::maybe_resolve_snippet</code> 解析 <code>pack</code> 中的片段，并将结果存入 <code>imported_summaries</code>。随后，函数为每个 <code>target</code> 收集 <code>analyses</code>，并与 <code>model</code> 中的 <code>namespace_fact</code> 和 <code>ns</code> 信息结合，形成 <code>page_summaries</code>。</p><p>内部控制流依赖 <code>analyses</code> 和 <code>model</code> 之间的交互：它从 <code>pack</code> 中提取 <code>mod</code> 和 <code>sym</code>，利用 <code>model</code> 查询相关摘要，最后汇总到 <code>page_summaries</code> 中。函数返回一个 <code>int</code> 值，该值可能代表成功构建的摘要数量或处理状态。最终结果依赖于 <code>project_root</code>、<code>imported_summaries</code> 和 <code>pack</code> 的一致性。</p><h4 id="side-effects-13" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-13" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-12" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-12" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the three <code>const int &amp;</code> parameters (namespace, module, file identifiers)</li><li>the <code>int</code> parameter (depth)</li></ul><h4 id="usage-patterns-15" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-15" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called from page building functions for namespace summaries</li><li>used in <code>clore::generate::build_namespace_page_root</code> or similar</li></ul><h3 id="clore-generate-build-evidence-for-type-analysis" tabindex="-1"><code>clore::generate::build_evidence_for_type_analysis</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-type-analysis" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_type_analysis`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:44</code></p><p>Definition: <code>generate/evidence_builder.cppm:82</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::build_evidence_for_type_analysis</code> 通过遍历 <code>project_root</code> 下的文件（由 <code>file_it</code> 驱动），收集 <code>page_summaries</code>，并调用 <code>maybe_resolve_snippet</code> 对代码片段进行解析，构建用于类型分析的证据。内部流程围绕 <code>pack</code> 对象展开，将 <code>root</code>、<code>sym</code>、<code>target</code>、<code>ns</code> 与 <code>mod</code> 等符号实体关联到 <code>model</code> 和 <code>analyses</code> 结构上，并处理 <code>imported_summaries</code> 和 <code>namespace_fact</code>。该函数依赖 <code>clore::generate</code> 命名空间内的模型抽象和文件系统迭代，最终生成一个表示证据数量的整型返回值。</p><h4 id="side-effects-14" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-14" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-13" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-13" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameters <code>const int &amp;</code> (likely analysis store or symbol <code>IDs</code>)</li><li>parameters <code>const int &amp;</code> (likely analysis store or symbol <code>IDs</code>)</li><li>parameter <code>int</code> (likely depth or index)</li></ul><h4 id="usage-patterns-16" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-16" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called to build evidence for type analysis pages</li><li>used in documentation generation pipeline</li></ul><h3 id="clore-generate-build-evidence-for-type-declaration-summary" tabindex="-1"><code>clore::generate::build_evidence_for_type_declaration_summary</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-type-declaration-summary" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_type_declaration_summary`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:77</code></p><p>Definition: <code>generate/evidence_builder.cppm:302</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数遍历 <code>root</code> 所表示的语法树节点，聚焦于类型声明节点。对于每个由 <code>target</code> 标识的类型声明，它从 <code>project_root</code> 定位源文件，并使用 <code>file_it</code> 迭代文件内容。内部控制流首先检查并解析代码片段，调用 <code>clore::generate::(anonymous namespace)::maybe_resolve_snippet</code> 以确定是否可以直接引用嵌入的上下文。随后，函数从 <code>analyses</code> 和 <code>model</code> 中提取相关分析结果，包括模块 <code>mod</code>、命名空间事实 <code>namespace_fact</code> 以及符号信息 <code>sym</code>。它会合并 <code>imported_summaries</code> 中来自其他翻译单元的摘要数据，并将结果整理为 <code>page_summaries</code> 条目。依赖关系主要围绕 <code>model</code> 提供的类型层次结构和 <code>pack</code> 中封装的声明集合，最终将证据汇总到 <code>root</code> 对应的页面摘要结构中。</p><h4 id="side-effects-15" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-15" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-14" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-14" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the <code>const int &amp;</code> parameters representing the type declaration identifier, analysis store, and page context</li></ul><h4 id="writes-to-4" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-4" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>the returned integer value representing an evidence pack or summary identifier</li></ul><h4 id="usage-patterns-17" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-17" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during <code>build_page_root</code> or <code>render_page_markdown</code> to provide evidence for type declaration pages</li><li>used in conjunction with <code>build_evidence_for_type_analysis</code> and <code>build_evidence_for_type_implementation_summary</code></li></ul><h3 id="clore-generate-build-evidence-for-type-implementation-summary" tabindex="-1"><code>clore::generate::build_evidence_for_type_implementation_summary</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-type-implementation-summary" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_type_implementation_summary`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:82</code></p><p>Definition: <code>generate/evidence_builder.cppm:334</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::build_evidence_for_type_implementation_summary</code> 的执行始于初始化 <code>root</code> 和 <code>project_root</code>，然后构造空的 <code>page_summaries</code> 容器。内部主循环遍历 <code>pack</code> 及其包含的 <code>mod</code> 与 <code>sym</code>，对每个符号位点调用 <code>maybe_resolve_snippet</code> 以尝试解析代码片段，并将成功解析的结果与当前 <code>target</code>、<code>analyses</code> 以及 <code>model</code> 上下文关联，逐步填充 <code>imported_summaries</code>。算法通过 <code>namespace_fact</code> 收集命名空间层面的证据，最终将每个符号的汇总信息写入 <code>page_summaries</code>。</p><p>核心控制流依赖 <code>project_root</code> 定位项目文件，依赖 <code>analyses</code> 容器提供静态分析数据，并通过 <code>model</code> 维护类型层次与导入关系。内部调用了匿名命名空间中的 <code>maybe_resolve_snippet</code> 来按需解析代码片段，其返回值直接影响证据是否被采纳。整个过程紧密整合了 <code>pack</code> 的模块结构、<code>root</code> 的目录布局以及 <code>ns</code> 的命名空间信息，最终将多源证据合并到 <code>page_summaries</code> 中。</p><h4 id="side-effects-16" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-16" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Allocates and stores evidence data in an internal registry or cache</li></ul><h4 id="reads-from-15" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-15" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>SymbolAnalysisStore</code> global instance</li><li>type analysis data</li><li>implementation page plan</li><li>function parameters <code>a</code>, <code>b</code>, <code>c</code></li></ul><h4 id="writes-to-5" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-5" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Internal evidence cache or registry</li><li>return value representing evidence identifier</li></ul><h4 id="usage-patterns-18" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-18" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during page generation for type implementation summary pages</li><li>Used by page planning functions like <code>build_page_plan_set</code></li></ul><h3 id="clore-generate-build-evidence-for-variable-analysis" tabindex="-1"><code>clore::generate::build_evidence_for_variable_analysis</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-variable-analysis" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_variable_analysis`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:48</code></p><p>Definition: <code>generate/evidence_builder.cppm:113</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数通过遍历当前项目中的源文件来构建变量分析的证据。首先，它获取 <code>project_root</code> 并初始化 <code>page_summaries</code> 和 <code>analyses</code> 等容器，然后循环处理每个文件，调用 <code>maybe_resolve_snippet</code> 决定是否解析代码片段，并从 <code>pack</code> 中提取 <code>sym</code> 和 <code>target</code> 等符号信息。对于每个分析目标，它使用 <code>model</code> 进行上下文建模，并收集 <code>imported_summaries</code> 和 <code>namespace_fact</code> 等事实。最后，它将生成的证据与 <code>root</code> 关联，并可能对 <code>mod</code> 和 <code>ns</code> 进行递归处理，所有结果均写入 <code>page_summaries</code> 和 <code>analyses</code> 结构中。</p><h4 id="side-effects-17" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-17" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-16" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-16" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>const int &amp; (analysis store)</li><li>const int &amp; (variable identifier)</li><li>int (additional parameter)</li></ul><h4 id="usage-patterns-19" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-19" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called to build evidence for variable analysis prompts</li><li>used in symbol analysis pipeline for variables</li></ul><h3 id="clore-generate-build-prompt" tabindex="-1"><code>clore::generate::build_prompt</code> <a class="header-anchor" href="#clore-generate-build-prompt" aria-label="Permalink to &quot;`clore::generate::build_prompt`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:94</code></p><p>Definition: <code>generate/evidence.cppm:651</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::build_prompt</code> 依据给定的 <code>PromptKind</code> 选择对应的提示词模板，然后结合证据包中的结构化数据生成最终的提示文本。内部流程首先调用 <code>prompt_template_of(kind)</code> 获取模板字符串；若返回空，则构造并返回一个包含错误消息的 <code>PromptError</code>。否则，将模板、<code>evidence</code> 以及通过 <code>format_evidence_text(evidence)</code> 生成的证据文本一同传递给 <code>instantiate_prompt_with_evidence</code>，由后者完成模板填充并返回结果字符串。核心依赖包括 <code>prompt_template_of</code> 的模板选择逻辑、<code>instantiate_prompt_with_evidence</code> 的模板实例化机制，以及 <code>format_evidence_text</code> 对 <code>EvidencePack</code> 的文本格式化能力。</p><h4 id="side-effects-18" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-18" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-17" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-17" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>PromptKind kind</code></li><li><code>const EvidencePack&amp; evidence</code></li><li><code>prompt_template_of(kind)</code></li><li><code>format_evidence_text(evidence)</code></li></ul><h4 id="usage-patterns-20" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-20" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during prompt construction for different prompt kinds</li><li>Used by higher-level prompt builders</li></ul><h3 id="clore-generate-format-evidence-text" tabindex="-1"><code>clore::generate::format_evidence_text</code> <a class="header-anchor" href="#clore-generate-format-evidence-text" aria-label="Permalink to &quot;`clore::generate::format_evidence_text`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:86</code></p><p>Definition: <code>generate/evidence.cppm:580</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::format_evidence_text</code> 的实现直接委托给 <code>clore::generate::format_evidence_text_bounded</code>，并将 <code>std::numeric_limits&lt;std::size_t&gt;::max()</code> 作为最大长度参数传入，从而生成完整的、未截断的证据文本。<code>format_evidence_text_bounded</code> 内部将 <code>EvidencePack</code> 分解为符号节和文本节，依次使用 <code>append_section_bounded</code> 填充内容，并依赖一系列具体构建函数（如 <code>build_evidence_for_function_analysis</code>、<code>build_evidence_for_type_analysis</code>）和事实收集辅助函数（如 <code>collect_facts</code>、<code>collect_namespace_facts_cached</code>）来组装各节。最终通过 <code>instantiate_prompt_with_evidence</code> 将组合后的文本与对应提示模板合并，得到最终的证据文本字符串。这种设计使 <code>format_evidence_text</code> 保持简洁的入口，将长度约束和复杂的布局逻辑完全交由有界版本处理。</p><h4 id="side-effects-19" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-19" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-18" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-18" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>pack</code> parameter of type <code>EvidencePack</code></li></ul><h4 id="usage-patterns-21" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-21" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called when the full evidence text is needed without truncation</li></ul><h3 id="clore-generate-format-evidence-text-bounded" tabindex="-1"><code>clore::generate::format_evidence_text_bounded</code> <a class="header-anchor" href="#clore-generate-format-evidence-text-bounded" aria-label="Permalink to &quot;`clore::generate::format_evidence_text_bounded`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:88</code></p><p>Definition: <code>generate/evidence.cppm:584</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数首先预留 <code>4096</code> 字节的字符串容量，然后尝试追加标题 <code>## EVIDENCE\\n\\n</code>；若空间不足（通过 <code>append_if_fits</code> 判断）则返回空字符串。随后，它依次处理四个 <code>SymbolSection</code>（<code>Target</code>、<code>Local Context</code>、<code>Dependencies</code>、<code>Used By</code>）和两个 <code>TextSection</code>（<code>Source Snippets</code>、<code>Related Page Summaries</code>）。每个章节的结构包含标题、指向对应 <code>EvidencePack</code> 字段的指针（如 <code>pack.target_facts</code>、<code>pack.local_context</code> 等）以及一个渲染回调函数（例如 <code>render_detailed_fact</code>、<code>render_context_fact</code>、<code>render_source_snippet</code>、<code>render_summary_item</code>）。遍历章节时调用辅助函数 <code>append_section_bounded</code>，该函数负责在不超过 <code>max_length</code> 的前提下逐个追加项目标题和渲染后的内容；若某个项目超出剩余容量则跳过。最终返回拼接好的字符串。</p><p>依赖项包括匿名命名空间中的 <code>append_if_fits</code>、<code>append_section_bounded</code>，以及多个渲染函数；数据结构依赖 <code>EvidencePack</code> 的各字段和内部结构体 <code>SymbolSection</code>、<code>TextSection</code>。控制流核心是多次调用边界检查的追加操作，无复杂分支或递归。</p><h4 id="side-effects-20" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-20" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-19" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-19" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>pack<code>.target_facts</code></li><li>pack<code>.local_context</code></li><li>pack<code>.dependency_context</code></li><li>pack<code>.reverse_usage_context</code></li><li>pack<code>.source_snippets</code></li><li>pack<code>.related_page_summaries</code></li></ul><h4 id="usage-patterns-22" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-22" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called by <code>clore::generate::build_prompt</code> to format evidence with a size limit.</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>该模块将符号提取和模型定义的结果组装为供下游提示生成使用的证据包。公开接口位于 <code>clore::generate</code> 命名空间，包含一系列 <code>build_evidence_for_*</code> 函数，每种函数针对一种分析类型（函数、类型、变量等）生成对应的 <code>EvidencePack</code>，后者内部持有目标事实、上下文标记和相关页面摘要。对证据文本的格式化与边界约束由 <code>format_evidence_text_bounded</code> 和 <code>append_section_bounded</code> 等辅助函数处理。</p><p>内部实现了分层的缓存和收集机制：<code>__detail</code> 子命名空间通过 <code>collect_facts</code>、<code>collect_summaries</code> 和带缓存的 <code>collect_namespace_facts_cached</code> 避免在同一命名空间内重复遍历符号，匿名命名空间则封装了具体的提示模板常量、渲染逻辑和键值构造。模块依赖 <code>generate:model</code> 定义的核心数据类型（如 <code>SymbolFact</code>、<code>EvidencePack</code>），并导入 <code>extract</code> 模块以获取原始符号和关系数据。</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../extract/">Module extract</a></li><li><a href="./model">Module generate:model</a></li></ul>',288)])])}const f=a(t,[["render",r]]);export{u as __pageData,f as default};
