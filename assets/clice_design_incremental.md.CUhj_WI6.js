import{_ as t,o as i,c as n,a3 as a}from"./chunks/framework.CRbjHCyB.js";const u=JSON.parse('{"title":"Incremental Compilation","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/incremental.md","filePath":"en/clice/design/incremental.md"}'),o={name:"clice/design/incremental.md"};function s(r,e,l,c,d,h){return i(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="incremental-compilation" tabindex="-1">Incremental Compilation <a class="header-anchor" href="#incremental-compilation" aria-label="Permalink to &quot;Incremental Compilation&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>A language server must recompile files every time the user edits code in order to provide up-to-date diagnostics, completions, and semantic information. Full compilation of a C++ file can take several seconds -- a typical source file may expand to tens or even hundreds of thousands of lines after <code>#include</code> expansion. Recompiling from scratch on every keystroke would produce an unacceptable user experience.</p><p>clice achieves incremental compilation through <strong>preamble separation</strong>: each source file is split into two parts -- the leading <code>#include</code> region (the preamble) and the remaining user code. The preamble is compiled into a precompiled header (PCH) and cached on disk; subsequent compilations load the PCH directly and only reprocess the user code. For example, a file containing <code>#include &lt;iostream&gt;</code> expands to roughly 20,000 lines; once the PCH is built, recompilation covers only the few dozen lines the user actually wrote.</p><p>The core challenges of this mechanism lie in PCH <strong>invalidation detection</strong> and <strong>lifecycle management</strong>:</p><p><strong>Precision of invalidation detection</strong>: A PCH caches the preprocessing results of every <code>#include</code>d header. When any header is modified, the PCH is out of date. However, naively checking file modification times (mtime) causes many false positives -- build tools frequently touch files without changing their contents (e.g., <code>cmake --build</code> dependency scanning, VCS branch switches), triggering unnecessary PCH rebuilds.</p><p><strong>Controlling rebuild overhead</strong>: Building a PCH requires full preprocessing and serialization, typically taking hundreds of milliseconds to several seconds. If every save triggers a PCH rebuild, users will notice perceptible lag. The system must accurately determine whether the PCH is truly stale and only rebuild when necessary.</p><p><strong>Concurrency coordination</strong>: Multiple files may share the same preamble. When a PCH needs to be rebuilt, several compilation requests may attempt to trigger the rebuild simultaneously. The system must ensure only one build executes while other requests wait for the result.</p><p><strong>Incomplete edits</strong>: While the user is typing <code>#include &quot;</code>, the preamble is in an incomplete state. Triggering a PCH rebuild at this point would produce an erroneous PCH, causing a flood of spurious errors in subsequent compilations.</p><p>In clangd, incremental compilation issues have long plagued users: diagnostics not updating after header modifications (requiring the user to reopen the file or restart the server), overly frequent PCH rebuilds causing editing lag in large projects, and occasional PCH corruption during simultaneous multi-file editing that prevents the entire project from compiling correctly.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="core-idea" tabindex="-1">Core Idea <a class="header-anchor" href="#core-idea" aria-label="Permalink to &quot;Core Idea&quot;">​</a></h3><p>The fundamental strategy of incremental compilation is to split a source file into the <strong>slowly changing part</strong> (preamble) and the <strong>rapidly changing part</strong> (user code), handling each separately. The preamble is compiled into a PCH cached on disk and only rebuilt when the content of a dependent header actually changes; the user code is recompiled by loading the PCH after each edit, which is very cheap.</p><p>The core challenge of PCH management is <strong>precise invalidation detection</strong> -- finding the right balance between &quot;rebuilding too aggressively&quot; and &quot;using a stale cache.&quot; clice achieves precise detection through two layers of invalidation checking (mtime fast screening + content hash verification), combined with preamble completeness checking and serialized concurrent builds, ensuring the PCH is always in a correct and efficient state.</p><h3 id="preamble-boundary-detection" tabindex="-1">Preamble Boundary Detection <a class="header-anchor" href="#preamble-boundary-detection" aria-label="Permalink to &quot;Preamble Boundary Detection&quot;">​</a></h3><p>At the heart of PCH is the preamble -- the region at the top of a source file consisting of preprocessor directives. The language server must precisely determine where the preamble ends (as a byte offset) so that only this portion is compiled into the PCH.</p><p>Preamble boundary detection is based on lexical scanning: scanning line by line from the start of the file, identifying preprocessor directives beginning with <code>#</code> (<code>#include</code>, <code>#define</code>, <code>#pragma</code>, etc.) and global module fragment declarations (<code>module;</code>). Scanning stops at the first line of non-preprocessor content. The returned byte offset marks the preamble boundary.</p><p>If the boundary is zero (the file has no preprocessor directives), no PCH is needed -- the PCH build step is skipped entirely and any existing PCH cache entry for that file is cleared.</p><h3 id="two-layer-invalidation-detection" tabindex="-1">Two-Layer Invalidation Detection <a class="header-anchor" href="#two-layer-invalidation-detection" aria-label="Permalink to &quot;Two-Layer Invalidation Detection&quot;">​</a></h3><p>PCH invalidation detection must answer the question: &quot;Since the last build, has the content of any header file included in the PCH changed?&quot;</p><p>clice uses a two-layer detection strategy that balances precision and performance:</p><p><strong>Layer 1: Modification Time (mtime) Fast Screening</strong></p><p>The system iterates over all dependency files of the PCH and retrieves each file&#39;s mtime. If every file&#39;s mtime is no later than the PCH&#39;s build timestamp (build_at), no file has been modified and the PCH is still valid -- it is reused directly. This layer requires only stat system calls, making it very cheap.</p><p><strong>Layer 2: Content Hash Precise Verification</strong></p><p>For files whose mtime is later than build_at (flagged as &quot;possibly modified&quot; by the first layer), the system recomputes their content&#39;s xxh3 hash and compares it against the hash recorded at build time. If the hashes match, the file content has not actually changed (it was merely touched), and the PCH is still valid. A rebuild is triggered only when the hashes differ.</p><p>The core value of this two-layer strategy is <strong>eliminating false positives</strong>: build tool touch operations, VCS checkout operations, and similar actions update mtime without changing content. Relying solely on mtime would cause many unnecessary PCH rebuilds. The second layer&#39;s content hashing filters out these false positives with modest additional overhead (hashing only the &quot;suspect&quot; files).</p><h3 id="depssnapshot-dependency-snapshot" tabindex="-1">DepsSnapshot: Dependency Snapshot <a class="header-anchor" href="#depssnapshot-dependency-snapshot" aria-label="Permalink to &quot;DepsSnapshot: Dependency Snapshot&quot;">​</a></h3><p>The foundation of two-layer detection is DepsSnapshot -- a snapshot of dependency state captured when a PCH build completes. It records:</p><ul><li><strong>path_ids</strong>: Path identifiers for all dependency files</li><li><strong>hashes</strong>: The content hash (xxh3_64bits) of each dependency file at build time</li><li><strong>build_at</strong>: The timestamp when the snapshot was captured</li></ul><p><strong>Timing correctness</strong>: build_at is obtained <strong>before</strong> computing file hashes. This ensures that if a file is modified during hash computation (its mtime becomes later than build_at), the next detection cycle will flag it as &quot;possibly modified&quot; in the first layer and pass it to the second layer for verification. There is no time window (TOCTOU) in which a modification could be missed.</p><h3 id="pch-build-pipeline" tabindex="-1">PCH Build Pipeline <a class="header-anchor" href="#pch-build-pipeline" aria-label="Permalink to &quot;PCH Build Pipeline&quot;">​</a></h3><p>When a compilation request requires a PCH, the following pipeline is triggered via ensure_pch():</p><p><strong>1. Compute preamble boundary and hash</strong></p><p>The preamble portion of the file content is extracted and its xxh3 hash is computed. This hash is used for PCH file naming on disk and also serves as a cache validity check.</p><p><strong>2. Look up the cache</strong></p><p>The system checks whether a cache entry already exists for the file in the Workspace&#39;s pch_cache. If an entry exists and:</p><ul><li>The preamble hash matches (preamble content has not changed)</li><li>Two-layer invalidation detection passes (dependency file contents have not changed)</li></ul><p>The cached PCH is reused directly with no rebuild needed.</p><p><strong>3. Check preamble completeness</strong></p><p>If the cache misses or is invalidated, the system checks whether the preamble is complete before triggering a rebuild -- specifically, whether there are unclosed quotes (e.g., <code>#include &quot;lib</code>) or unterminated module declarations (e.g., <code>import foo</code> missing a semicolon). If incomplete, the user is likely editing the preamble region; the rebuild is deferred and the old PCH (if one exists) continues to be used.</p><p><strong>4. Serialize concurrent builds</strong></p><p>The system checks whether another coroutine is already building a PCH for this file. If so, it waits for the completion event and then uses the build result. This is implemented through a shared event (kota::event) in PCHState -- the first coroutine to initiate the build sets the event, and subsequent coroutines wait on the same event.</p><p><strong>5. Dispatch to a stateless worker process</strong></p><p>The PCH build is dispatched as a high-priority task to a stateless worker process. The worker uses Clang&#39;s Preamble compilation mode (CompilationKind::Preamble), passing the full file content and preamble boundary to Clang via file remapping; Clang processes only the preamble portion before the boundary. Upon completion, the worker returns the PCH file path and the list of dependency files.</p><p><strong>6. Update the cache</strong></p><p>The build result is stored in pch_cache: the PCH file path, preamble hash, preamble boundary, and dependency snapshot. DocumentLink information from the PCH (positions and targets of <code>#include</code> directives) is also captured, enabling the editor to display clickable header links.</p><h3 id="pch-file-naming-on-disk" tabindex="-1">PCH File Naming on Disk <a class="header-anchor" href="#pch-file-naming-on-disk" aria-label="Permalink to &quot;PCH File Naming on Disk&quot;">​</a></h3><p>PCH files on disk are named by the hash of the preamble content, implementing content-addressable storage. This allows different files with identical preamble content to <strong>share the same PCH file on disk</strong>. When preamble content changes, the new PCH file uses a different hash for its name, and the old file becomes orphaned. A cleanup mechanism periodically reclaims orphaned PCH files that have not been used beyond a certain age.</p><h3 id="pch-cache-storage-model" tabindex="-1">PCH Cache Storage Model <a class="header-anchor" href="#pch-cache-storage-model" aria-label="Permalink to &quot;PCH Cache Storage Model&quot;">​</a></h3><p>The PCH cache (pch_cache) is currently keyed by the source file&#39;s path_id. This means that even if two files have identical preambles, they have independent cache entries -- each performing its own invalidation detection and rebuild. While the PCH files on disk are shared through content-addressable naming, the cache metadata (dependency snapshots, build timestamps, etc.) is not shared.</p><p>PCH cache metadata (path, hash, boundary, dependency snapshot) is persisted to a cache.json file on disk. This allows recovery on server restart, avoiding the need to rebuild all PCHs on a cold start.</p><h3 id="compilation-triggering-and-overall-flow" tabindex="-1">Compilation Triggering and Overall Flow <a class="header-anchor" href="#compilation-triggering-and-overall-flow" aria-label="Permalink to &quot;Compilation Triggering and Overall Flow&quot;">​</a></h3><p>clice uses a <strong>pull-based</strong> compilation model -- compilation is not triggered immediately on file change but is triggered on demand when a feature request (hover, completion, semantic highlighting, etc.) requires an up-to-date AST. This avoids wasteful compilations during rapid successive keystrokes.</p><p>When a feature request requires an up-to-date AST, the compilation pipeline proceeds through these steps:</p><ol><li>Check whether the AST is cached and not dirty -- if so, reuse it directly</li><li>Check whether the AST&#39;s dependencies are stale (using the same two-layer invalidation detection)</li><li>If recompilation is needed, first ensure C++20 module dependencies are ready (via CompileGraph; see the module compilation documentation)</li><li>Then ensure the PCH is ready (via ensure_pch)</li><li>Finally, dispatch the compilation task to a stateful worker process with the PCH path</li></ol><p>The stateful worker loads the PCH and only needs to compile the user code after the preamble -- this is the core benefit of incremental compilation, reducing compilation time from several seconds to typically under one second.</p><h3 id="pch-and-header-compilation-context" tabindex="-1">PCH and Header Compilation Context <a class="header-anchor" href="#pch-and-header-compilation-context" aria-label="Permalink to &quot;PCH and Header Compilation Context&quot;">​</a></h3><p>For non-self-contained headers, the compilation context system synthesizes a prefix file (see the compilation context documentation) and injects it via <code>-include</code> to simulate the header&#39;s inclusion environment within its host source file. This injected prefix file appears in the header&#39;s compilation preamble and is therefore covered by PCH caching.</p><p>ensure_pch() handles headers and source files uniformly: whether the preamble contains the file&#39;s own <code>#include</code> region or injected prefix content, the same hashing, caching, invalidation detection, and build pipeline applies.</p><h2 id="design-decisions-and-trade-offs" tabindex="-1">Design Decisions and Trade-offs <a class="header-anchor" href="#design-decisions-and-trade-offs" aria-label="Permalink to &quot;Design Decisions and Trade-offs&quot;">​</a></h2><p><strong>Why two-layer detection instead of content hashing alone?</strong> While content hashing is precise, it requires reading and hashing the entire contents of every dependency file. A typical C++ file may depend on hundreds of headers, making the I/O cost of hashing all of them non-trivial. The mtime fast-screening layer reduces the number of files that need hashing to those &quot;touched since the last build,&quot; which is typically far smaller than the total dependency count.</p><p><strong>Why not incremental PCH patching?</strong> Ideally, when only one header undergoes a small change, it should be possible to patch the PCH incrementally rather than fully rebuilding it. However, Clang&#39;s PCH format does not support incremental patching -- it is a monolithic serialized snapshot of AST and preprocessor state. Incremental patching would require deep modifications to Clang&#39;s serialization layer, and the complexity is disproportionate to the benefit.</p><p><strong>Why doesn&#39;t the PCH hash include compilation flags?</strong> Currently, the PCH hash is based solely on the preamble text content and does not include compilation flags (<code>-D</code>, <code>-std</code>, etc.). This is a deliberate trade-off -- it allows files with the same preamble text but different compilation flags to share a PCH file on disk. In practice, the preprocessing result of the same preamble under different compilation flags is usually identical (differences mainly come from macro definitions, which are typically reflected in <code>#define</code> directives within the preamble text). However, there are edge cases where this is incorrect -- for example, when a command-line <code>-D</code> defines a macro that affects header behavior, different compilation flags should use different PCHs.</p><p><strong>Why is preamble completeness checking important?</strong> Building a PCH from an incomplete preamble produces a PCH containing erroneous preprocessor state. Subsequent compilations using this erroneous PCH will exhibit a flood of spurious errors. Deferring the rebuild until the preamble is complete avoids this cascade of errors.</p><h2 id="known-limitations" tabindex="-1">Known Limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known Limitations&quot;">​</a></h2><ul><li><strong>Per-file independent caching</strong>: pch_cache is keyed by path_id and cannot share cache metadata (dependency snapshots, build state, etc.) across different files with identical preambles. A future improvement is to key the cache by preamble content hash plus compilation flags, enabling true cross-file cache sharing while also addressing the issue of compilation flags not participating in the cache key, along with introducing LRU eviction.</li><li><strong>Full rebuild</strong>: Any content change in a dependency file triggers a full PCH rebuild. There is no way to rebuild only the affected portion. This is an inherent limitation of the Clang PCH format.</li></ul>',66)])])}const m=t(o,[["render",s]]);export{u as __pageData,m as default};
