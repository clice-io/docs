import{_ as t,o as n,c as i,a3 as o}from"./chunks/framework.CRbjHCyB.js";const h=JSON.parse('{"title":"Module Compilation","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/module.md","filePath":"en/clice/design/module.md"}'),a={name:"clice/design/module.md"};function r(s,e,l,c,d,u){return n(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="module-compilation" tabindex="-1">Module Compilation <a class="header-anchor" href="#module-compilation" aria-label="Permalink to &quot;Module Compilation&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>C++20 introduced modules, the largest change to the C++ compilation model since the language&#39;s inception. Traditional C++ compilation is &quot;compile each file independently&quot; — each source file compiles to an object file, and files share declarations via header files. Modules break this independence: when a file <code>import</code>s another module, the imported module&#39;s <strong>module interface unit</strong> must be compiled first, producing a <strong>precompiled module file (PCM)</strong>, before the importing file can be compiled.</p><p>This introduces <strong>compile-time dependency relationships</strong> — a directed acyclic graph (DAG), where nodes are module files and edges are <code>import</code> relationships. Build systems (CMake, Ninja, etc.) naturally support this kind of DAG scheduling, but a language server faces fundamentally different challenges:</p><p><strong>Real-time requirements</strong>: A build system can scan all files at once, build a complete dependency graph, and compile in topological order. A language server cannot — after a user opens a file, they expect editing feedback in milliseconds. Waiting for the entire module graph to compile is unacceptable.</p><p><strong>On-demand compilation</strong>: The user has only opened a few files in the project; there is no need to compile the entire module graph. What the language server needs is &quot;compile only the modules the current file requires&quot; — a lazy, incremental compilation strategy.</p><p><strong>Cascading updates on file changes</strong>: When the user modifies a module interface file and saves it, all PCMs of modules that directly or indirectly depend on it become stale. The language server must transparently cancel in-progress compilations, mark affected modules as dirty, and recompile them when next needed.</p><p><strong>Concurrency and races</strong>: Multiple files may simultaneously need the same module&#39;s PCM. If two requests trigger compilation of the same module at the same time, duplicate compilation must be avoided while ensuring the later request can wait for the earlier compilation to finish.</p><p><strong>Cyclic dependencies</strong>: Although C++ modules do not allow circular <code>import</code>s, users may temporarily introduce cycles during editing. The language server must detect this and report an error gracefully, rather than deadlocking.</p><p>In clangd, C++20 module support has long been in an experimental stage. Recurring issues in the community include: incomplete dependency resolution for module compilation, leading to missing PCM files; module files modified without dependents being recompiled, leading to stale diagnostics; lack of real-time tracking of module dependencies, requiring a manual server restart to get correct compilation results.</p><p>The root cause of these problems is the lack of a dedicated module compilation scheduling system. clice solves these problems at their source through CompileGraph — a reference-counted compilation DAG.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="core-idea" tabindex="-1">Core Idea <a class="header-anchor" href="#core-idea" aria-label="Permalink to &quot;Core Idea&quot;">​</a></h3><p>CompileGraph is a compilation dependency DAG built at runtime. Each node (CompileUnit) represents a module file, and edges represent <code>import</code> dependency relationships. Unlike a build system&#39;s static DAG, CompileGraph is <strong>lazily built</strong> — a module&#39;s dependencies are resolved and added to the graph only when the module is actually needed.</p><p>CompileGraph&#39;s scheduling is based on <strong>interest counting</strong>: when a file needs a module, that module&#39;s reference count is incremented; when it is no longer needed, the count is decremented. A reference count reaching zero means no active request currently cares about this module, and its in-progress compilation can be cancelled to free resources.</p><h3 id="key-concepts-of-compileunit" tabindex="-1">Key Concepts of CompileUnit <a class="header-anchor" href="#key-concepts-of-compileunit" aria-label="Permalink to &quot;Key Concepts of CompileUnit&quot;">​</a></h3><p>Each CompileUnit is organized around the following core concepts:</p><p><strong>Dependencies</strong>: Each unit maintains forward dependencies (modules it <code>import</code>s) and reverse dependencies (modules that depend on it). Forward dependencies are obtained via lazy resolution; reverse dependencies are automatically populated during resolution. Reverse dependencies are the basis for cascading notifications when files change.</p><p><strong>Dirty state and generations</strong>: The dirty flag indicates the unit needs recompilation. A generation counter is incremented on each file update, used to detect whether an asynchronous compilation result is stale — the compilation task captures the generation value at start and compares it upon completion.</p><p><strong>Compilation rounds</strong>: Each compilation produces a round containing a completion event and a result. Waiters learn of completion through the event and decide their next action based on the result. A cancellation source is used to cancel the current round&#39;s compilation task.</p><p><strong>Reference count</strong>: Tracks how many active requests care about this unit. When it reaches zero, compilation can be cancelled to free resources.</p><h3 id="reference-counting-and-raii-guards" tabindex="-1">Reference Counting and RAII Guards <a class="header-anchor" href="#reference-counting-and-raii-guards" aria-label="Permalink to &quot;Reference Counting and RAII Guards&quot;">​</a></h3><p>Reference counting is the core scheduling mechanism of CompileGraph. It tracks &quot;how many active requests currently care about a given compilation unit&quot; and determines when compilation tasks are started and cancelled.</p><p>CompileGraph provides two RAII guards for managing reference counts:</p><p><strong>RefGuard (request-level guard)</strong>: The entry point of a compilation request creates a RefGuard, which performs acquire (increment reference count) on all required compilation units. When the RefGuard is destroyed, it automatically performs release (decrement reference count). This ensures that even if a request is cancelled (coroutine frame destroyed), reference counts are correctly released.</p><p><strong>UnitGuard (compilation-round guard)</strong>: Each compilation task internally uses a UnitGuard to maintain the compilation round&#39;s state. It is responsible for: acquiring reference counts on all direct dependencies after dependency resolution; upon completion or cancellation, publishing the result, clearing the compiling flag, releasing all acquired dependency references, and firing the completion event to notify waiters.</p><p>Using destructors rather than coroutine finally blocks for cleanup is a key design choice — cancellation is implemented via coroutine frame destruction, so cleanup logic must be placed in destructors to guarantee execution.</p><h3 id="lazy-dependency-resolution" tabindex="-1">Lazy Dependency Resolution <a class="header-anchor" href="#lazy-dependency-resolution" aria-label="Permalink to &quot;Lazy Dependency Resolution&quot;">​</a></h3><p>Dependency resolution is an expensive operation — it requires scanning the module file&#39;s contents and parsing <code>import</code> declarations. CompileGraph does not resolve dependencies at node creation time; instead, it calls resolve_fn for the first time the node needs to be compiled.</p><p>resolve_fn is provided externally (typically based on precise scanning of the module file) and returns the list of files that the module directly depends on. Resolution results are cached in the CompileUnit&#39;s dependencies, and the dependents lists are back-filled at the same time. Subsequent compilations reuse the cached dependency information unless a file update resets the resolved flag.</p><h3 id="compilation-flow" tabindex="-1">Compilation Flow <a class="header-anchor" href="#compilation-flow" aria-label="Permalink to &quot;Compilation Flow&quot;">​</a></h3><p>CompileGraph provides two entry points:</p><p><strong>compile(path_id)</strong> — compile the specified module and all its transitive dependencies:</p><ol><li>RefGuard performs acquire on the target module</li><li>If the target module is not dirty, return immediately</li><li>If no compilation is in progress, start a compilation task</li><li>Wait for the compilation round to complete</li><li>Based on the result, return success, failure, or retry (retry if stale)</li></ol><p><strong>compile_deps(path_id)</strong> — compile all transitive module dependencies of the specified file, but not the file itself. This is used for ordinary source files (non-module files) — they are not part of the module DAG themselves but may <code>import</code> modules.</p><p>The internal flow of a compilation task (unit_body):</p><ol><li>Resolve dependencies (ensure_resolved)</li><li>Check for self-cycles (self-importing)</li><li>Acquire reference counts on all direct dependencies</li><li>Recursively wait for all dependencies to finish compiling</li><li>Dispatch the actual compilation work to a stateless worker (dispatch_fn)</li><li>Check the generation counter — if the file was updated during compilation, the result is stale and the dirty flag is not cleared</li></ol><h3 id="zero-reference-deferred-cancellation" tabindex="-1">Zero-Reference Deferred Cancellation <a class="header-anchor" href="#zero-reference-deferred-cancellation" aria-label="Permalink to &quot;Zero-Reference Deferred Cancellation&quot;">​</a></h3><p>When a compilation unit&#39;s reference count drops to zero, it means no active request currently cares about it. But cancelling immediately may be too aggressive — during dependency switching, the reference count might drop to zero and then increase again within the same event loop tick.</p><p>CompileGraph uses a strategy of deferring by one event loop tick: when the reference count drops to zero, it does not cancel immediately but instead schedules a deferred check. The deferred check runs on the next event loop tick — if the reference count is still zero at that point, cancellation actually proceeds. This avoids transient zero-references triggering unnecessary cancellation and restart.</p><h3 id="generation-counter-and-race-detection" tabindex="-1">Generation Counter and Race Detection <a class="header-anchor" href="#generation-counter-and-race-detection" aria-label="Permalink to &quot;Generation Counter and Race Detection&quot;">​</a></h3><p>In an asynchronous system, a compilation task dispatched to a worker process must wait for the result to come back. During this time, the user may have modified the file, meaning the file content has already changed. If the old compilation result were marked as &quot;clean,&quot; it would lead to use of stale PCMs.</p><p>The generation counter solves this problem: the compilation task captures the current generation value at start, and upon completion, compares it against the current value. If they differ, a file update occurred during compilation, and the result is stale — the dirty flag is not cleared, and subsequent requests will trigger recompilation.</p><h3 id="file-updates-and-cascading-propagation" tabindex="-1">File Updates and Cascading Propagation <a class="header-anchor" href="#file-updates-and-cascading-propagation" aria-label="Permalink to &quot;File Updates and Cascading Propagation&quot;">​</a></h3><p>When a module file is modified (via didSave), CompileGraph&#39;s update() method performs a cascading update:</p><ol><li>Reset the resolved flag of the modified file (forcing the next compilation to re-resolve dependencies)</li><li>Clear old dependency edges</li><li>Mark as dirty and increment the generation counter</li><li>Traverse all transitive dependents along reverse dependency edges</li><li>For each affected dependent: cancel its compilation round, mark it as dirty, and increment its generation counter</li></ol><p>update() returns the list of all modules marked dirty, allowing the caller to clear the corresponding PCM caches.</p><p>Note that update() does not modify reference counts — existing waiters retain their references. When they observe that the compilation result is &quot;stale,&quot; they automatically retry, driving a new round of compilation.</p><h3 id="cycle-detection" tabindex="-1">Cycle Detection <a class="header-anchor" href="#cycle-detection" aria-label="Permalink to &quot;Cycle Detection&quot;">​</a></h3><p>Although legal C++20 modules do not allow cyclic dependencies, they may be introduced temporarily during editing. CompileGraph detects wait cycles before waiting for a dependency&#39;s compilation to complete: it searches along the target&#39;s dependency chain, following only nodes that are currently compiling, checking whether the chain leads back to the current waiter. If a cycle is detected, it returns failure immediately, avoiding deadlock.</p><h3 id="compilation-results-and-retry-semantics" tabindex="-1">Compilation Results and Retry Semantics <a class="header-anchor" href="#compilation-results-and-retry-semantics" aria-label="Permalink to &quot;Compilation Results and Retry Semantics&quot;">​</a></h3><p>A compilation round has three possible results:</p><ul><li><strong>Success</strong>: Compilation succeeded; the dirty flag is cleared</li><li><strong>Failed</strong>: Compilation failed (dependency failure, dispatch failure, or cyclic dependency). The dirty flag is not cleared — the next explicit request can retry</li><li><strong>Stale</strong>: Compilation was cancelled (due to a file update or reference count reaching zero). Waiters automatically retry a new compilation round</li></ul><p>Stale retries are bounded — each update() increments the generation only once, so retries do not accumulate. Without a continuous stream of updates, retries terminate naturally.</p><h3 id="structured-concurrency-and-shutdown" tabindex="-1">Structured Concurrency and Shutdown <a class="header-anchor" href="#structured-concurrency-and-shutdown" aria-label="Permalink to &quot;Structured Concurrency and Shutdown&quot;">​</a></h3><p>All compilation tasks are managed through kota::task_group. task_group provides a structured concurrency guarantee — it waits for all child tasks to complete upon destruction. CompileGraph&#39;s shutdown() method achieves graceful shutdown by cancelling all tasks in the task_group and waiting for them to exit.</p><h2 id="design-decisions-and-trade-offs" tabindex="-1">Design Decisions and Trade-offs <a class="header-anchor" href="#design-decisions-and-trade-offs" aria-label="Permalink to &quot;Design Decisions and Trade-offs&quot;">​</a></h2><p><strong>Why reference counting instead of a simple task queue?</strong> A task queue cannot express the semantics of &quot;no one needs this compilation anymore.&quot; In module compilation scenarios, a user might close the file that triggered a compilation while that compilation is still in progress; continuing to compile at that point wastes resources. Reference counting precisely tracks demand, giving cancellation decisions a solid basis.</p><p><strong>Why defer by one tick instead of cancelling immediately?</strong> In an event loop model, multiple steps of an operation may complete within the same tick. If an old reference is released before a new one is established (transient zero-reference), immediate cancellation would cause unnecessary rework. Deferring by one tick ensures that reference handoffs within the same tick do not trigger cancellation.</p><p><strong>Why use a generation counter instead of locks?</strong> The master process is a single-threaded event loop — there are no data races. The &quot;races&quot; here come from the timing of asynchronous operations. The generation counter detects &quot;whether a file update occurred during compilation&quot; with minimal overhead, without introducing the complexity of locks.</p><p><strong>Why is dependency resolution lazy?</strong> Dependency resolution requires reading and scanning file contents, which is not cheap. If all dependencies were resolved at node creation time, opening a single file could trigger scanning of the entire module graph. Lazy resolution ensures that only modules actually needed for compilation are scanned.</p><p><strong>Why are failures non-sticky?</strong> Module compilation failures are usually transient — the user is editing code, and syntax errors are the norm. Making failures persistent would prevent the user from getting correct results after fixing an error. Not clearing the dirty flag means the next request naturally retries.</p><h2 id="known-limitations" tabindex="-1">Known Limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known Limitations&quot;">​</a></h2><ul><li><strong>Compilation graph memory accumulation</strong>: Each compilation round creates a task in the task_group. Completed task frames are only reclaimed when the task_group is destroyed, not immediately upon completion. In a long-running server, a large number of completed task frames may accumulate.</li><li><strong>Dependency resolution determinism</strong>: The result of resolve_fn depends on the file&#39;s current content. If the file is modified between resolve and the actual compilation, the resolved dependencies may be inconsistent with those at compilation time. The generation counter can detect this, but it results in additional retries.</li></ul>',64)])])}const m=t(a,[["render",r]]);export{h as __pageData,m as default};
