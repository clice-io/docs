import{_ as a,o as t,c as o,a3 as r}from"./chunks/framework.aYYe0dfI.js";const h=JSON.parse('{"title":"Module generate","description":"The generate module owns the documentation page generation pipeline, transforming extracted project data into final rendered output. Its public interface comprises four functions: generate_dry_run for validation without disk writes, generate_pages as the synchronous entry point with configurable concurrency and output extension, write_pages for writing already‑generated content to a given output path, and generate_pages_async for non‑blocking generation on a caller‑provided event loop. The module manages internal state such as output root, rate limiting, LLM model selection, and page planning, and it relies on imported configuration and extraction dependencies to drive the full generation workflow.","frontmatter":{"title":"Module generate","description":"The generate module owns the documentation page generation pipeline, transforming extracted project data into final rendered output. Its public interface comprises four functions: generate_dry_run for validation without disk writes, generate_pages as the synchronous entry point with configurable concurrency and output extension, write_pages for writing already‑generated content to a given output path, and generate_pages_async for non‑blocking generation on a caller‑provided event loop. The module manages internal state such as output root, rate limiting, LLM model selection, and page planning, and it relies on imported configuration and extraction dependencies to drive the full generation workflow.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/generate/index.md","filePath":"en/clore/modules/generate/index.md"}'),n={name:"clore/modules/generate/index.md"};function d(i,e,c,s,l,u){return t(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="module-generate" tabindex="-1">Module <code>generate</code> <a class="header-anchor" href="#module-generate" aria-label="Permalink to &quot;Module `generate`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>generate</code> module owns the documentation page generation pipeline, transforming extracted project data into final rendered output. Its public interface comprises four functions: <code>generate_dry_run</code> for validation without disk writes, <code>generate_pages</code> as the synchronous entry point with configurable concurrency and output extension, <code>write_pages</code> for writing already‑generated content to a given output path, and <code>generate_pages_async</code> for non‑blocking generation on a caller‑provided event loop. The module manages internal state such as output root, rate limiting, LLM model selection, and page planning, and it relies on imported configuration and extraction dependencies to drive the full generation workflow.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../config/"><code>config</code></a></li><li><a href="./../extract/"><code>extract</code></a></li><li><code>std</code></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./../agent/"><code>agent</code></a></li><li><a href="./../agent/tools"><code>agent:tools</code></a></li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-generate-generate-dry-run" tabindex="-1"><code>clore::generate::generate_dry_run</code> <a class="header-anchor" href="#clore-generate-generate-dry-run" aria-label="Permalink to &quot;`clore::generate::generate_dry_run`&quot;">​</a></h3><p>Declaration: <code>generate/generate.cppm:25</code></p><p>Definition: <code>generate/scheduler.cppm:1888</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function begins by calling <code>prepare_generation_context</code> to construct a <code>PreparedGenerationContext</code> from the input configuration and model data. Using <code>prepare_symbol_analyses_for_dry_run</code>, it builds a <code>PreparedSymbolAnalyses</code> instance that contains the minimal metadata needed for a dry run, omitting actual LLM prompts. A <code>PageGenerationScheduler</code> is then constructed with the context, model, output root, event loop, and the <code>dry_run</code> flag set to true; the scheduler delegates its internal work to a <code>DependencyTracker</code>, a <code>WorkQueue</code>, and a <code>PageRenderer</code> (which in dry‑run mode sets <code>dry_run_</code> to true and records page output in <code>dry_run_pages_</code> instead of writing files).</p><p>After construction, the scheduler’s <code>run</code> method is invoked. During execution, it populates the <code>DependencyTracker</code> from the prepared context, iterates over each plan in <code>context.id_to_plan</code>, and for each page that has no unsatisfied symbol dependencies, it submits a <code>PagePromptWork</code> item into the <code>WorkQueue</code>. The scheduler’s worker loop (<code>worker_task</code>) dequeues work, calls <code>run_page_prompt_task</code> (which bypasses the LLM and directly records the prompt output as a placeholder), and updates page states via <code>finish_page_prompt_work</code>. Once all pages are either written or marked failed, the scheduler checks the counts from the <code>DependencyTracker</code> and <code>PageRenderer</code>. Finally, <code>generate_dry_run</code> returns <code>0</code> on success or a non‑zero error code produced by <code>make_generate_error</code> if any page failures or internal inconsistencies are detected.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>first <code>const int &amp;</code> parameter</li><li>second <code>const int &amp;</code> parameter</li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to test generation logic without outputting generated pages.</li><li>Used in validation or pre-flight checks before a full generation.</li></ul><h3 id="clore-generate-generate-pages" tabindex="-1"><code>clore::generate::generate_pages</code> <a class="header-anchor" href="#clore-generate-generate-pages" aria-label="Permalink to &quot;`clore::generate::generate_pages`&quot;">​</a></h3><p>Declaration: <code>generate/generate.cppm:28</code></p><p>Definition: <code>generate/scheduler.cppm:1947</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The implementation of <code>clore::generate::generate_pages</code> builds a <code>PageGenerationScheduler</code> from a <code>PreparedGenerationContext</code> (produced by <code>prepare_generation_context</code>) and drives the scheduler’s <code>run</code> method to completion. Internally, the scheduler orchestrates a multi‑stage pipeline: it first collects documentable symbols and queues symbol‑analysis tasks via <code>schedule_symbol_analysis</code>, feeding results into a <code>DependencyTracker</code> that tracks inter‑page and inter‑symbol dependencies. Once dependencies are satisfied, page‑prompt tasks are submitted to a <code>WorkQueue</code> and executed by a pool of worker threads running <code>worker_task</code>. Each worker calls <code>run_page_prompt_task</code> which performs LLM requests (with caching via <code>perform_prompt_request</code> and identity computation through <code>prompt_cache_identity_for_page_request</code>), then feeds the output into <code>finish_page_prompt_work</code>. Completed pages are dispatched to a <code>PageRenderer</code> for dry‑run accumulation or final file emission via <code>emit_pages</code>. A <code>DependencyTracker</code> instance (<code>tracker_</code>) manages states via <code>id_to_state_</code>, tracks dependents with <code>dependents_</code>, and releases blocked pages through <code>release_dependents</code> once symbol analysis finishes. Consecutive failures are counted by <code>record_consecutive_failure</code> and can trigger early termination through <code>retry_limit_exceeded</code>. The function also handles directory index page generation via <code>build_directory_index_pages</code> and collects page summaries from prompt outputs for later reuse. The overall flow is LLM‑request driven, dependency‑aware, and resilient to failures and caching.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Writes documentation page files to the filesystem at the path specified by the last <code>std::string_view</code> parameter</li></ul><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>Input analysis handles (first two <code>const int &amp;</code> parameters)</li><li>Configuration strings (the <code>std::string_view</code> parameters)</li><li>Potential global or cached analysis stores and symbol databases</li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Output documentation page files in the specified directory</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Main entry point for generating documentation pages after analysis</li><li>Typically called once per documentation generation run</li></ul><h3 id="clore-generate-generate-pages-async" tabindex="-1"><code>clore::generate::generate_pages_async</code> <a class="header-anchor" href="#clore-generate-generate-pages-async" aria-label="Permalink to &quot;`clore::generate::generate_pages_async`&quot;">​</a></h3><p>Declaration: <code>generate/generate.cppm:37</code></p><p>Definition: <code>generate/scheduler.cppm:1925</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function first prepares the generation context by calling the internal helper <code>prepare_generation_context</code> on the two integer parameters, producing a <code>PreparedGenerationContext</code> that contains the page plans, prompt requests, symbol analysis targets, and link metadata. It then constructs a <code>PageGenerationScheduler</code> bound to the provided <code>kota::event_loop</code>, passing the configuration, model, context, LLM model string, rate limit, and output root. The scheduler internally owns a <code>WorkQueue</code>, a <code>DependencyTracker</code>, and a <code>PageRenderer</code>. After construction, the function invokes the scheduler’s <code>run</code> method, which orchestrates the entire asynchronous pipeline: it initializes dependency tracking from the prepared context, spawns worker tasks that dequeue symbol analysis work (via <code>schedule_symbol_analysis</code> / <code>run_symbol_analysis_task</code>) and page prompt work (via <code>run_page_prompt_task</code>), manages LLM requests through <code>request_llm_async</code>, coordinates state transitions as prompts complete, pages become ready, rendered (via <code>render_ready_page</code> and <code>emit_pages_async</code>), and output is written. The pipeline respects the configured rate limit, retry limits, and dry-run mode, and updates internal counters for cache hits/misses and consecutive failures. The function returns an integer representing the overall result or task identifier for the asynchronous generation.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Schedules an asynchronous task on the provided <code>kota::event_loop</code></li><li>Writes generated pages to the output directory when the task is executed</li></ul><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>first <code>const int&amp;</code> parameter (likely module or file index)</li><li>second <code>const int&amp;</code> parameter</li><li><code>std::string_view</code> output directory</li><li><code>std::uint32_t</code> limit</li><li><code>std::string_view</code> base path</li><li><code>kota::event_loop&amp;</code> event loop</li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>the <code>kota::event_loop</code> (schedules tasks)</li><li>output directory (via internal page writing functions)</li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called when asynchronous page generation is needed</li><li>Callers must schedule the returned task on the event loop and run it</li></ul><h3 id="clore-generate-write-pages" tabindex="-1"><code>clore::generate::write_pages</code> <a class="header-anchor" href="#clore-generate-write-pages" aria-label="Permalink to &quot;`clore::generate::write_pages`&quot;">​</a></h3><p>Declaration: <code>generate/generate.cppm:44</code></p><p>Definition: <code>generate/scheduler.cppm:1966</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function first gathers the documentable symbol set from the configuration via <code>collect_documentable_symbols</code>. It then builds the full generation context with <code>prepare_generation_context</code>, which populates plan‑level prompt requests, symbol‑analysis targets, and cross‑page link data. Afterward it constructs directory index pages using <code>build_directory_index_pages</code> and initializes a <code>PageGenerationScheduler</code> object that owns a <code>DependencyTracker</code>, a <code>WorkQueue</code>, and a <code>PageRenderer</code>. The scheduler’s <code>run</code> method drives the core pipeline: it submits page‑prompt and symbol‑analysis tasks, respects rate limits and retry limits, resolves dependency chains, caches LLM responses, and eventually calls <code>render_generated_pages</code> to emit final output files. Once the scheduler completes, the function sets evidence metadata via <code>set_evidence_metadata</code> and returns a status code produced by <code>make_generate_error</code> (or zero on success).</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>const int &amp;</li><li><code>std::string_view</code></li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>return int</li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during page generation to write rendered pages to files</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>generate</code> module is decomposed into synchronous and asynchronous generation pathways, both sharing a common output‑writing step. It depends on the <code>config</code> module for generation parameters and on the <code>extract</code> module for pre‑processed symbol and page‑plan data. Internally, the module separates concerns by exposing a dry‑run entry point (<code>generate_dry_run</code>) for validation, a blocking pipeline (<code>generate_pages</code>) that orchestrates rendering and writing via <code>write_pages</code>, and an asynchronous variant (<code>generate_pages_async</code>) that accepts a <code>kota::event_loop</code> to drive non‑blocking execution. Shared state such as rate limits, model selections, and output paths is managed through module‑private variables, keeping the public interface focused on integer result codes and <code>std::string_view</code> parameters.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../config/">Module config</a></li><li><a href="./../extract/">Module extract</a></li></ul>',63)])])}const g=a(n,[["render",d]]);export{h as __pageData,g as default};
