import{_ as t,o,c as i,ae as a}from"./chunks/framework.DeBUZsJm.js";const p=JSON.parse('{"title":"Catter: A New Build Process Interception Tool","description":"","frontmatter":{},"headers":[],"relativePath":"catter/README.md","filePath":"catter/README.md"}'),r={name:"catter/README.md"};function s(n,e,l,c,d,u){return o(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="catter-a-new-build-process-interception-tool" tabindex="-1">Catter: A New Build Process Interception Tool <a class="header-anchor" href="#catter-a-new-build-process-interception-tool" aria-label="Permalink to &quot;Catter: A New Build Process Interception Tool&quot;">​</a></h1><p><img src="https://img.shields.io/badge/C++-23-blue.svg" alt="C++ Standard"><a href="https://github.com/clice-io/catter/blob/main/LICENSE" target="_blank" rel="noreferrer"><img src="https://img.shields.io/github/license/clice-io/catter" alt="GitHub license"></a><a href="https://deepwiki.com/clice-io/catter" target="_blank" rel="noreferrer"><img src="https://deepwiki.com/badge.svg" alt="Ask DeepWiki"></a></p><div class="important custom-block github-alert"><p class="custom-block-title">IMPORTANT</p><p>This project is under active development. Stay tuned for future updates!</p></div><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>We are developing a new C++ language server, <a href="https://github.com/clice-io/clice" target="_blank" rel="noreferrer">clice</a>. For it to work correctly, users often need to provide a <a href="https://clang.llvm.org/docs/JSONCompilationDatabase.html" target="_blank" rel="noreferrer">Compilation Database (CDB)</a>. This is a JSON file that records the compilation commands for all source files.</p><p>Unfortunately, not all C++ build systems support the generation of CDB files. For example, CMake only supports this feature when using <code>Makefile</code> or <code>Ninja</code> as the build backend. Furthermore, C++ has many other popular build systems, and many of them do not natively support CDB generation, often requiring special workarounds. In some situations, you might not even be able to access a CDB even if it is generated; for instance, when a Python package builds C++ code, you often have no control over the build directory and thus cannot retrieve the corresponding CDB file.</p><p>Moreover, even with a CDB, a language server might still not function correctly. A classic example is LLVM. Why? Because LLVM uses its own compiled <code>tablegen</code> tool for code generation. The generated files are placed in a specific directory, which is then added to the compilation flags with <code>-I</code>. This means that if you only run <code>cmake configure</code>, these generated files will be missing, causing the language server to fail. You must run <code>cmake build</code> to generate them. Unfortunately, it&#39;s difficult to configure LLVM to <em>only</em> generate these necessary headers for a minimal build, especially if your goal is just to read the code. A full build of LLVM takes a very long time. It would be ideal if we could build only the minimal required components.</p><p>Currently, there is no cross-platform tool that completely solves all these issues. This is why we decided to create <code>catter</code>.</p><h2 id="core-features" tabindex="-1">Core Features <a class="header-anchor" href="#core-features" aria-label="Permalink to &quot;Core Features&quot;">​</a></h2><p><code>catter</code>&#39;s underlying principle is similar to <a href="https://github.com/rizsotto/Bear" target="_blank" rel="noreferrer">Bear</a> or <a href="https://github.com/rizsotto/scan-build" target="_blank" rel="noreferrer">scan-build</a>. It works by using hooks to intercept any child process creation (including compilation tasks) initiated by the target process. It also integrates QuickJS, allowing you to write JavaScript scripts to process these events—for example, to log or modify compilation arguments, analyze build times, and so on.</p><p>Although its initial motivation was to record compilation commands to generate a CDB, we quickly realized it could be used for much more:</p><ul><li><p><strong>Analyze Linker Commands to Infer Target Information</strong>: By capturing linker commands, <code>catter</code> can analyze the dependency graph between targets. This provides richer target information, which is essential for language servers to properly support C++20 modules. The C++ standard stipulates that a program can have at most one module with the same name, and it is the target information that distinguishes different programs (e.g., libraries vs. executables). This crucial information is missing from the current CDB standard, with no prospects for improvement in the near future. With <code>catter</code>, we can solve this problem once and for all, across all build systems.</p></li><li><p><strong>Perform &quot;Fake&quot; Compilations</strong>: Instead of forwarding compilation tasks to the actual compiler, <code>catter</code> can generate fake placeholder object files. This allows the build system to proceed with its tasks without performing a full, time-consuming compilation. This way, you can obtain a complete CDB without waiting for the entire build process to finish. <strong>Note</strong>: If the build involves code generators, those tools must still be genuinely built. <code>catter</code> can automatically analyze dependencies to ensure that only these essential tools are compiled, achieving a true minimal build.</p></li><li><p><strong>Profile the Build Process</strong>: Due to issues in the build system&#39;s design or poorly written build scripts, the actual degree of build parallelism can be very low. We can capture process information (start times, durations, parent-child relationships) and render it visually, allowing users to inspect the build&#39;s parallelism and performance bottlenecks in real-time in a browser.</p></li><li><p><strong>Patch the Build Process with Custom Scripts</strong>: Thanks to the embedded QuickJS engine, users can write their own scripts to patch any command during the build process. This gives you the power to dynamically modify arguments, redirect commands, or inject custom logic into your build without altering the original build files.</p></li></ul>',12)])])}const m=t(r,[["render",s]]);export{p as __pageData,m as default};
