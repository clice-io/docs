import{_ as e,o,c as a,a3 as d}from"./chunks/framework.CRbjHCyB.js";const u=JSON.parse('{"title":"Configuration","description":"","frontmatter":{"outline":2,"head":[["meta",{"property":"og:title","content":"Configuration | clice.io"}],["meta",{"property":"og:url","content":"https://docs.clice.io/clice/guide/configuration"}],["meta",{"property":"og:image","content":"https://docs.clice.io/og.png"}],["meta",{"property":"og:image:width","content":"1200"}],["meta",{"property":"og:image:height","content":"630"}],["meta",{"property":"og:locale","content":"en_US"}],["link",{"rel":"canonical","href":"https://docs.clice.io/clice/guide/configuration"}],["link",{"rel":"alternate","hreflang":"en","href":"https://docs.clice.io/clice/guide/configuration"}],["link",{"rel":"alternate","hreflang":"zh-CN","href":"https://docs.clice.io/zh/clice/guide/configuration"}],["link",{"rel":"alternate","hreflang":"x-default","href":"https://docs.clice.io/clice/guide/configuration"}]]},"headers":[],"relativePath":"clice/guide/configuration.md","filePath":"en/clice/guide/configuration.md","lastUpdated":1789814526000}'),i={name:"clice/guide/configuration.md"};function n(s,t,c,r,l,h){return o(),a("div",null,[...t[0]||(t[0]=[d(`<h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h1><p>clice reads configuration from <code>clice.toml</code> in the workspace root, or from <code>.clice/config.toml</code> if the former does not exist. Configuration can also be passed via LSP <code>initializationOptions</code> (JSON format); values from <code>initializationOptions</code> override the config file, and defaults fill in whatever remains unset after the merge.</p><p>Configuration is read once at server startup. Changing it — either file — requires restarting the server; there is no hot reload.</p><p>A JSON schema of the whole configuration is published at <a href="/clice-config.schema.json"><code>clice-config.schema.json</code></a>; editors that validate TOML or JSON against a schema can point at it.</p><p>Relative paths and patterns in the file resolve against the directory of the configuration file itself; values passed through <code>initializationOptions</code> resolve against the workspace root.</p><h2 id="variable-substitution" tabindex="-1">Variable Substitution <a class="header-anchor" href="#variable-substitution" aria-label="Permalink to &quot;Variable Substitution&quot;">​</a></h2><p>The following variable is supported in string values:</p><table tabindex="0"><thead><tr><th>Variable</th><th>Description</th></tr></thead><tbody><tr><td><code>\${workspace}</code></td><td>The workspace directory provided by the client</td></tr></tbody></table><h2 id="workspace" tabindex="-1">Workspace <a class="header-anchor" href="#workspace" aria-label="Permalink to &quot;Workspace&quot;">​</a></h2><p>Top-level options, written before any section.</p><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>default_configuration</code></td><td><code>string</code></td><td><code>&quot;&quot;</code></td></tr></tbody></table><p>The build configuration to fall back on when neither <code>--configuration</code> nor a persisted selection names one: a tag declared on rules. When rules carry tags and this names none of them, the first declared tag is used and a warning is logged.</p></div><h2 id="project" tabindex="-1"><code>[project]</code> <a class="header-anchor" href="#project" aria-label="Permalink to &quot;\`[project]\`&quot;">​</a></h2><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>cache_dir</code></td><td><code>string</code></td><td><code>&quot;&quot;</code></td></tr></tbody></table><p>Directory for the unified on-disk cache (PCH, PCM and index artifacts). Empty defaults to <code>\${workspace}/.clice</code>, which keeps itself out of version control and backups via generated .gitignore and CACHEDIR.TAG markers (a <code>.clice/config.toml</code> stays visible to Git; backup tools honoring CACHEDIR.TAG skip the whole directory); an explicitly configured directory is never marked. The resolved path is printed at startup.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>logging_dir</code></td><td><code>string</code></td><td><code>&quot;&quot;</code></td></tr></tbody></table><p>Directory for log files; empty derives <code>\${cache_dir}/logs</code>. Each server session logs into its own timestamped subdirectory.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>enable_indexing</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>Build the background index that serves cross-TU features (find references, workspace symbols, ...).</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>readonly</code></td><td><code>string</code></td><td><code>&quot;off&quot;</code></td></tr></tbody></table><p>Read-only serving for open files: &quot;off&quot; targets a full AST for every open file — builds are pulled by the first request that needs them, with the index answering in the meantime; &quot;on&quot; never builds a PCH — reads serve from the index alone (a cold file jumps the indexing queue), while completion and signature help still compile on demand without a preamble; &quot;auto&quot; starts every file as &quot;on&quot;, switches it to &quot;off&quot; at the first edit intent (edit, completion, signature help, context switch), and falls back to &quot;off&quot; for a file the index cannot serve. Feature routing always answers from the best source currently available.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>idle_timeout_ms</code></td><td><code>uint32</code></td><td><code>3000</code></td></tr></tbody></table><p>Idle delay in milliseconds before background indexing starts.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>test_hooks</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>Enable the clice/internal test hooks used by the test harness.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>stateful_worker_count</code></td><td><code>uint32</code></td><td><code>2</code></td></tr></tbody></table><p>Number of stateful workers — they hold ASTs in memory and serve queries (hover, semantic tokens, ...); <code>0</code> is invalid and falls back to the default.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>stateless_worker_count</code></td><td><code>uint32</code></td><td>—</td></tr></tbody></table><p>Initial number of stateless workers — they handle ephemeral tasks (PCH/PCM builds, completion, signature help); defaults to half the machine&#39;s parallelism, at least 2. <code>0</code> is invalid and falls back to that default.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>min_stateless_worker_count</code></td><td><code>uint32</code></td><td><code>1</code></td></tr></tbody></table><p>Lower bound for dynamic stateless-worker scaling; <code>0</code> is invalid and falls back to the default.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>max_stateless_worker_count</code></td><td><code>uint32</code></td><td>—</td></tr></tbody></table><p>Upper bound for dynamic stateless-worker scaling; <code>0</code> means the machine&#39;s parallelism, which is also the default.</p></div><h2 id="tracker" tabindex="-1"><code>[tracker]</code> <a class="header-anchor" href="#tracker" aria-label="Permalink to &quot;\`[tracker]\`&quot;">​</a></h2><p>The file tracker polls for changes that happen outside the editor (a <code>git checkout</code>, a regenerated <code>compile_commands.json</code>, code generators writing headers) so the server picks them up without a restart. Setting an interval to <code>0</code> disables that polling loop.</p><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>cdb_poll_seconds</code></td><td><code>uint32</code></td><td><code>3</code></td></tr></tbody></table><p>Compilation database poll interval in seconds; 0 disables polling.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>workspace_poll_seconds</code></td><td><code>uint32</code></td><td><code>30</code></td></tr></tbody></table><p>Workspace file sweep interval in seconds; 0 disables polling.</p></div><h2 id="hover" tabindex="-1"><code>[hover]</code> <a class="header-anchor" href="#hover" aria-label="Permalink to &quot;\`[hover]\`&quot;">​</a></h2><p>The <code>[hover]</code> section controls how hover cards render.</p><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>parse_comment_as_markdown</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>Render the hover card as markdown; <code>false</code> produces plain text for clients that cannot display it.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>show_aka</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>Show the desugared form of a type, e.g. <code>vector&lt;int&gt;::size_type (aka unsigned long)</code>.</p></div><h2 id="inlay-hints" tabindex="-1"><code>[inlay_hints]</code> <a class="header-anchor" href="#inlay-hints" aria-label="Permalink to &quot;\`[inlay_hints]\`&quot;">​</a></h2><p>The <code>[inlay_hints]</code> section controls which inlay hint categories the server produces. A client-side refresh then requests hints with the updated values; no recompile is involved.</p><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>enabled</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>Master switch: <code>false</code> disables all inlay hints.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>parameters</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>Parameter name hints at call sites, e.g. <code>draw(width: 800, height: 600)</code>, including <code>&amp;</code> markers for arguments passed by mutable reference.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>deduced_types</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>Deduced type hints for <code>auto</code> variables, structured bindings and deduced return types.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>designators</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>Field designator hints in aggregate initialization, e.g. <code>.x=</code> and <code>.y=</code> in <code>Point{1, 2}</code>.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>block_end</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p><code>// name</code> hints after the closing brace of long blocks (functions, types, namespaces, control flow).</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>default_arguments</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>Show the default arguments a call omitted, abbreviated when long.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>type_name_limit</code></td><td><code>uint32</code></td><td><code>32</code></td></tr></tbody></table><p>Byte budget for rendered hint text: over-long deduced types fall back to a sugared spelling or are dropped, over-long default arguments are abbreviated. <code>0</code> means no limit.</p></div><h2 id="code-completion" tabindex="-1"><code>[code_completion]</code> <a class="header-anchor" href="#code-completion" aria-label="Permalink to &quot;\`[code_completion]\`&quot;">​</a></h2><p>The <code>[code_completion]</code> section controls completion item assembly.</p><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>enable_keyword_snippet</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>Complete keywords as snippets (not yet implemented).</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>enable_function_arguments_snippet</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>Insert function arguments as a snippet when completing a call. For functions this applies to individually listed overloads, so it requires <code>bundle_overloads = false</code>; function-like macros have no overload sets and always take the snippet.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>enable_template_arguments_snippet</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>Insert template arguments as a snippet on completion (not yet implemented).</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>insert_paren_in_function_call</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>Insert parentheses when completing a function call (not yet implemented).</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>bundle_overloads</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>Collapse an overload set into a single completion item.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>limit</code></td><td><code>uint32</code></td><td><code>0</code></td></tr></tbody></table><p>Maximum number of completion items (not yet implemented).</p></div><h2 id="rules" tabindex="-1"><code>[[rules]]</code> <a class="header-anchor" href="#rules" aria-label="Permalink to &quot;\`[[rules]]\`&quot;">​</a></h2><p>A rule names files by pattern and says where they take their compile commands from and how those commands are edited. Every rule matching a file applies, in declaration order: an earlier rule&#39;s databases rank first among the file&#39;s candidates, the first matching rule with a <code>default_command</code> supplies the command of a file without an entry, <code>append</code> and <code>remove</code> accumulate with a later <code>remove</code> cancelling an earlier <code>append</code>, and <code>index = false</code> on any matching rule keeps the file out of the index. A rule carrying a <code>configuration</code> tag applies only while that configuration is active; the distinct tags form the configuration menu.</p><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>patterns</code></td><td><code>array of string</code></td><td><code>[]</code></td></tr></tbody></table><p>Glob patterns selecting the files this rule applies to. A relative pattern is anchored at this configuration file&#39;s directory (<code>..</code> segments allowed), or at the workspace root for a rule passed through initializationOptions; an absolute pattern or one starting with <code>**</code> matches the file&#39;s absolute path. <code>*</code> matches within a path segment, <code>?</code> a single character, <code>**</code> any number of segments, <code>{a,b}</code> alternatives, <code>[0-9]</code> a character range, <code>[!...]</code> a negated range. Omitted means every file.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>configuration</code></td><td><code>string</code></td><td><code>&quot;&quot;</code></td></tr></tbody></table><p>Build configuration tag. A tagged rule applies only while that configuration is active; an untagged rule always applies. The distinct tags form the configuration menu; <code>--configuration</code>, the persisted selection and <code>default_configuration</code> pick the active one, in that order.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>compile_commands</code></td><td><code>array of string</code></td><td><code>[]</code></td></tr></tbody></table><p>Compilation databases, in priority order: a compile_commands.json or a directory containing one, relative to this configuration file (to the workspace root for a rule passed through initializationOptions). All of them load, and every entry applies to its own file whatever the patterns say; the patterns and the order decide which entry a file present in several databases gets by default. A rule without patterns names the workspace&#39;s databases. When no rule declares a source, every compile_commands.json in the workspace root and its immediate subdirectories loads, and so do the ones in the directories above a file when it is opened.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>default_command</code></td><td><code>string</code> or <code>array of string</code></td><td><code>&quot;&quot;</code></td></tr></tbody></table><p>The compile command for matching files without a database entry, without the source file: a string tokenized like a shell command line, or an argv array. It runs from the directory of the configuration file it was read from (the workspace root for a rule passed through initializationOptions), and the matching source files on disk join the background index (unless the rule turns <code>index</code> off) — enumerated at startup and again on every workspace poll, so a file created later compiles when opened and joins the index within a poll period. Omitted means none.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>append</code></td><td><code>array of string</code></td><td><code>[]</code></td></tr></tbody></table><p>Compilation flags appended for matching files, e.g. <code>[&quot;-std=c++20&quot;, &quot;-DNDEBUG&quot;]</code>.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>remove</code></td><td><code>array of string</code></td><td><code>[]</code></td></tr></tbody></table><p>Compilation flags removed for matching files, e.g. <code>[&quot;-Wall&quot;]</code>.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>index</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>Whether matching translation units join the background index. <code>false</code> keeps them out; they still compile when opened and still host the headers they include. Any matching rule saying <code>false</code> wins.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>lint</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>Whether <code>clice lint</code> checks matching files. <code>false</code> keeps them out: their findings are dropped and a translation unit they head is not parsed, unless <code>--index</code> needs it for the index. Files outside the workspace are never checked. Any matching rule saying <code>false</code> wins.</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><code>format</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>Whether <code>clice format</code> formats matching files. <code>false</code> keeps them out. Files outside the workspace are never formatted. Any matching rule saying <code>false</code> wins.</p></div><h2 id="example" tabindex="-1">Example <a class="header-anchor" href="#example" aria-label="Permalink to &quot;Example&quot;">​</a></h2><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">compile_commands = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;build&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">append = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;-std=c++23&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">patterns = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;test/**&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">append = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;-DTEST_MODE&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>Two build directories as switchable configurations, and a project without a compilation database:</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">default_configuration = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;debug&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">configuration = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;debug&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">compile_commands = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;build/debug&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">configuration = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;release&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">compile_commands = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;build/release&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">patterns = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;src/**&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;include/**&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">default_command = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;arm-none-eabi-gcc -std=c23 -mcpu=cortex-m4 -Iinclude&quot;</span></span></code></pre></div><h2 id="switching-configurations" tabindex="-1">Switching Configurations <a class="header-anchor" href="#switching-configurations" aria-label="Permalink to &quot;Switching Configurations&quot;">​</a></h2><p>The <code>configuration</code> tags on rules form a menu, and one tag is active per server process; untagged rules always apply. The active one is, in priority order, the <code>--configuration &lt;tag&gt;</code> argument (accepted by <code>clice serve</code>, <code>clice index</code>, <code>clice lint</code> and <code>clice inspect</code>), the persisted selection, or <code>default_configuration</code>. A selection is made from the editor — in VS Code the status bar shows the active configuration and clicking it opens the menu, other clients call <code>clice/switchConfiguration</code> — and is stored in <code>state.json</code> under <code>cache_dir</code>, never in <code>clice.toml</code>; it takes effect when the server is started again, which the VS Code extension does on its own. Each configuration keeps its own index under <code>cache_dir</code>, so switching back and forth never reindexes what a configuration already indexed. The tags are also how the batch commands choose an index: <code>clice index --configuration release</code> builds the release index, and <code>clice index --stats</code> reports the index of the configuration it resolves the same way.</p>`,65)])])}const g=e(i,[["render",n]]);export{u as __pageData,g as default};
