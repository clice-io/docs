import{_ as r,C as s,o as t,c as n,a3 as o,b as c,w as a,a as d,E as l,a4 as p}from"./chunks/framework.CRbjHCyB.js";const C=JSON.parse('{"title":"System Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"catter/design/architecture.md","filePath":"en/catter/design/architecture.md"}'),h={name:"catter/design/architecture.md"};function m(u,e,g,y,k,x){const i=s("Mermaid");return t(),n("div",null,[e[1]||(e[1]=o('<h1 id="system-architecture" tabindex="-1">System Architecture <a class="header-anchor" href="#system-architecture" aria-label="Permalink to &quot;System Architecture&quot;">​</a></h1><p>Catter is a three-part system for intercepting and processing build commands. Each component has a distinct responsibility, and they communicate over IPC to form a pipeline that captures every compiler invocation a build system makes.</p><h2 id="the-three-components" tabindex="-1">The Three Components <a class="header-anchor" href="#the-three-components" aria-label="Permalink to &quot;The Three Components&quot;">​</a></h2><h3 id="_1-hook-process-creation-interceptor" tabindex="-1">1. HOOK -- Process Creation Interceptor <a class="header-anchor" href="#_1-hook-process-creation-interceptor" aria-label="Permalink to &quot;1. HOOK -- Process Creation Interceptor&quot;">​</a></h3><p>A platform-specific shared library injected into build system processes. It intercepts calls to process creation functions (<code>execve</code> on Unix, <code>CreateProcess</code> on Windows) and rewrites them so that every child process is routed through <code>catter-proxy</code> before execution.</p><ul><li><strong>Linux</strong>: <code>libcatter-hook-unix.so</code>, injected via <code>LD_PRELOAD</code></li><li><strong>macOS</strong>: <code>libcatter-hook-unix.dylib</code>, injected via <code>DYLD_INSERT_LIBRARIES</code></li><li><strong>Windows</strong>: <code>catter-hook-win64.dll</code>, injected via DLL injection (<code>VirtualAllocEx</code> + <code>LoadLibraryA</code>)</li></ul><p>The hook is passive -- it does not make decisions. It simply redirects process creation to the proxy.</p><h3 id="_2-proxy-catter-proxy-compiler-wrapper-and-hook-manager" tabindex="-1">2. PROXY (<code>catter-proxy</code>) -- Compiler Wrapper and Hook Manager <a class="header-anchor" href="#_2-proxy-catter-proxy-compiler-wrapper-and-hook-manager" aria-label="Permalink to &quot;2. PROXY (`catter-proxy`) -- Compiler Wrapper and Hook Manager&quot;">​</a></h3><p>A standalone executable that operates in two modes:</p><ul><li><strong>Injector Mode</strong>: Launched by <code>catter</code> to start the build system with the hook library attached. This is the first proxy instance in a session.</li><li><strong>Wrapper Mode</strong>: Launched by the hook library when a build command is intercepted. Each intercepted command spawns a new <code>catter-proxy</code> process that acts as a stand-in for the original compiler/tool.</li></ul><p>In both modes, the proxy connects to the <code>catter</code> daemon over IPC, sends information about the captured command, receives a decision (execute, drop, or modify), and acts on it.</p><h3 id="_3-decision-catter-the-daemon" tabindex="-1">3. DECISION (<code>catter</code>) -- The Daemon <a class="header-anchor" href="#_3-decision-catter-the-daemon" aria-label="Permalink to &quot;3. DECISION (`catter`) -- The Daemon&quot;">​</a></h3><p>The main process that the user invokes. It:</p><ol><li>Loads and initializes a JavaScript script via the embedded QuickJS runtime</li><li>Spawns <code>catter-proxy</code> in injector mode to start the build</li><li>Listens on a Unix domain socket (or named pipe on Windows) for IPC connections</li><li>Receives intercepted commands from proxy instances</li><li>Invokes JavaScript callbacks (<code>onCommand</code>, <code>onExecution</code>, <code>onStart</code>, <code>onFinish</code>) to decide how to handle each command</li><li>Maintains a session tree that mirrors the process tree of the build</li></ol><p>The JS runtime is single-threaded -- all script callbacks run sequentially on the event loop, so scripts do not need to handle concurrency.</p><h2 id="complete-workflow" tabindex="-1">Complete Workflow <a class="header-anchor" href="#complete-workflow" aria-label="Permalink to &quot;Complete Workflow&quot;">​</a></h2><p>Here is a step-by-step walkthrough of what happens when you run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">catter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> script::cdb</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -o</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compile_commands.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> make</span></span></code></pre></div><ol><li><p><strong>User invokes <code>catter</code></strong>. The DECISION daemon starts, loads the <code>script::cdb</code> script, and initializes the QuickJS runtime. The script&#39;s <code>onStart()</code> callback runs.</p></li><li><p><strong><code>catter</code> spawns <code>catter-proxy -- make</code></strong>. This is the proxy in <strong>injector mode</strong>. The proxy is a child process of <code>catter</code>.</p></li><li><p><strong>Proxy connects to <code>catter</code> via IPC</strong>. It sends a <code>CHECK_MODE</code> request to confirm the daemon is in inject mode.</p></li><li><p><strong><code>catter</code> responds: inject mode</strong>. The proxy now knows it should launch the build command with the hook library attached.</p></li><li><p><strong>Proxy starts <code>make</code> with the hook injected</strong>. On Linux, this means adding <code>libcatter-hook-unix.so</code> to <code>LD_PRELOAD</code> and setting environment variables (<code>__key_catter_proxy_path_v1</code>, <code>__key_catter_command_id_v1</code>) before calling the real <code>execve</code>. On Windows, the process is created suspended, the hook DLL is injected, then the process is resumed.</p></li><li><p><strong><code>make</code> runs and tries to spawn <code>g++ main.cpp -o main.o</code></strong>. The build system calls <code>execve(&quot;g++&quot;, ...)</code> (or <code>CreateProcess</code> on Windows).</p></li><li><p><strong>The HOOK intercepts the <code>execve()</code> call</strong>. The hook library, loaded in <code>make</code>&#39;s address space, catches the call before it reaches the kernel.</p></li><li><p><strong>HOOK rewrites the command</strong>. Instead of executing <code>g++</code> directly, the hook rewrites the command to:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>catter-proxy -p &lt;parent_id&gt; --exec /usr/bin/g++ -- g++ main.cpp -o main.o</span></span></code></pre></div><p>It also cleans the environment: removes catter-specific variables and strips the hook library from <code>LD_PRELOAD</code> to prevent the proxy itself from being hooked.</p></li><li><p><strong>A new <code>catter-proxy</code> instance starts</strong> (PROXY in <strong>wrapper mode</strong>). This proxy instance calls the real <code>execve</code> with the rewritten command.</p></li><li><p><strong>Wrapper proxy connects to <code>catter</code> via IPC</strong>. It sends a <code>CREATE</code> request (registering itself with its parent ID), then a <code>MAKE_DECISION</code> request containing the full command details: working directory, resolved executable path, arguments, and environment.</p></li><li><p><strong><code>catter</code> invokes <code>onCommand(ctx)</code> in the JS script</strong>. The script inspects the command and returns an action: execute as-is, execute with modifications, or drop.</p></li><li><p><strong>Proxy acts on the decision</strong>:</p><ul><li><strong>INJECT</strong>: Execute the command with the hook library re-attached (so grandchild processes are also intercepted)</li><li><strong>WRAP</strong>: Execute the command directly, capturing stdout/stderr</li><li><strong>DROP</strong>: Skip execution, return exit code 0</li></ul></li><li><p><strong>After execution, the proxy sends <code>FINISH</code> to <code>catter</code></strong>. The result includes the exit code, captured stdout, and captured stderr.</p></li><li><p><strong><code>catter</code> invokes <code>onExecution(ctx)</code> in the JS script</strong> with the execution result.</p></li><li><p><strong>When <code>make</code> finishes</strong>, the original injector proxy exits, and <code>catter</code> invokes <code>onFinish(result)</code>.</p></li><li><p><strong><code>catter</code> shuts down</strong> and writes any output (e.g., <code>compile_commands.json</code>).</p></li></ol><h2 id="sequence-diagram" tabindex="-1">Sequence Diagram <a class="header-anchor" href="#sequence-diagram" aria-label="Permalink to &quot;Sequence Diagram&quot;">​</a></h2>',20)),(t(),c(p,null,{default:a(()=>[l(i,{id:"mermaid-210",class:"mermaid",graph:"sequenceDiagram%0A%20%20%20%20participant%20User%0A%20%20%20%20participant%20Catter%20as%20catter%20(DECISION)%0A%20%20%20%20participant%20Proxy1%20as%20catter-proxy%20(Injector)%0A%20%20%20%20participant%20Make%20as%20make%20(Build%20System)%0A%20%20%20%20participant%20Hook%20as%20HOOK%20(in%20make)%0A%20%20%20%20participant%20Proxy2%20as%20catter-proxy%20(Wrapper)%0A%20%20%20%20participant%20GCC%20as%20g%2B%2B%20(Compiler)%0A%0A%20%20%20%20User-%3E%3ECatter%3A%20catter%20script%3A%3Acdb%20--%20make%0A%20%20%20%20Catter-%3E%3ECatter%3A%20Load%20script%2C%20call%20onStart()%0A%20%20%20%20Catter-%3E%3EProxy1%3A%20Spawn%20catter-proxy%20--%20make%0A%20%20%20%20Proxy1-%3E%3ECatter%3A%20IPC%3A%20CHECK_MODE%0A%20%20%20%20Catter--%3E%3EProxy1%3A%20INJECT%20mode%0A%20%20%20%20Proxy1-%3E%3EMake%3A%20Start%20make%20with%20HOOK%0A%20%20%20%20Make-%3E%3EHook%3A%20execve(%22g%2B%2B%22%2C%20...)%0A%20%20%20%20Hook-%3E%3EProxy2%3A%20Rewrite%20to%20catter-proxy%20-p%20ID%20--exec%20g%2B%2B%20--%20...%0A%20%20%20%20Proxy2-%3E%3ECatter%3A%20IPC%3A%20CREATE(parent_id)%0A%20%20%20%20Catter--%3E%3EProxy2%3A%20New%20session%20ID%0A%20%20%20%20Proxy2-%3E%3ECatter%3A%20IPC%3A%20MAKE_DECISION(command)%0A%20%20%20%20Catter-%3E%3ECatter%3A%20Call%20onCommand(ctx)%0A%20%20%20%20Catter--%3E%3EProxy2%3A%20Action%3A%20execute%0A%20%20%20%20Proxy2-%3E%3EGCC%3A%20Execute%20g%2B%2B%20main.cpp%0A%20%20%20%20GCC--%3E%3EProxy2%3A%20Exit%20code%200%0A%20%20%20%20Proxy2-%3E%3ECatter%3A%20IPC%3A%20FINISH(result)%0A%20%20%20%20Catter-%3E%3ECatter%3A%20Call%20onExecution(ctx)%0A%20%20%20%20Make--%3E%3EProxy1%3A%20make%20exits%0A%20%20%20%20Proxy1--%3E%3ECatter%3A%20Process%20result%0A%20%20%20%20Catter-%3E%3ECatter%3A%20Call%20onFinish(result)%0A"})]),fallback:a(()=>[...e[0]||(e[0]=[d(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=o('<h2 id="two-modes-of-catter-proxy" tabindex="-1">Two Modes of catter-proxy <a class="header-anchor" href="#two-modes-of-catter-proxy" aria-label="Permalink to &quot;Two Modes of catter-proxy&quot;">​</a></h2><p>The proxy binary serves dual purposes depending on how it is invoked:</p><h3 id="injector-mode" tabindex="-1">Injector Mode <a class="header-anchor" href="#injector-mode" aria-label="Permalink to &quot;Injector Mode&quot;">​</a></h3><p>Launched by <code>catter</code> to run the build system with hooks. This is always the first proxy instance in a session.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>catter-proxy -- make -j8</span></span></code></pre></div><p>The injector:</p><ol><li>Connects to the daemon, confirms inject mode</li><li>Prepares the environment with <code>LD_PRELOAD</code> (or performs DLL injection on Windows)</li><li>Sets catter-specific environment variables for the hook to read</li><li>Launches the build command</li><li>Waits for the build to complete, capturing stdout/stderr</li></ol><h3 id="wrapper-mode" tabindex="-1">Wrapper Mode <a class="header-anchor" href="#wrapper-mode" aria-label="Permalink to &quot;Wrapper Mode&quot;">​</a></h3><p>Launched by the hook library when a child process is intercepted. Each intercepted command creates a new wrapper instance.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>catter-proxy -p &lt;parent_id&gt; --exec /usr/bin/g++ -- g++ main.cpp -o main.o</span></span></code></pre></div><p>The wrapper:</p><ol><li>Connects to the daemon</li><li>Registers itself as a child of <code>parent_id</code> via <code>CREATE</code></li><li>Sends the captured command via <code>MAKE_DECISION</code></li><li>Executes (or drops) based on the daemon&#39;s response</li><li>Reports the result via <code>FINISH</code></li></ol><h2 id="key-design-decisions" tabindex="-1">Key Design Decisions <a class="header-anchor" href="#key-design-decisions" aria-label="Permalink to &quot;Key Design Decisions&quot;">​</a></h2><p><strong>Single-threaded JS runtime</strong>. All JavaScript callbacks run sequentially on the event loop. The daemon handles IPC connections concurrently (via async I/O with <code>kota</code>), but script execution is serialized. This eliminates race conditions in user scripts.</p><p><strong>Binary IPC over Unix domain sockets / named pipes</strong>. Communication between proxy and daemon uses Bincode serialization via <code>kota::ipc::BincodePeer</code>. This is fast and avoids the overhead of text-based protocols. See the <a href="./ipc-protocol">IPC Protocol</a> document for details.</p><p><strong>Recursive hooking</strong>. On Unix, <code>LD_PRELOAD</code> is inherited by child processes, so any subprocess spawned by the build system (including nested <code>make</code> invocations, shell scripts, or build tool wrappers) is automatically hooked. On Windows, the hook DLL&#39;s <code>CreateProcess</code> detour ensures every child process gets the DLL injected before it starts. The interception is transparent and recursive -- the build system has no way to tell it is being monitored.</p><p><strong>Environment scrubbing</strong>. The hook cleans its own traces from the environment before launching the proxy. If it did not strip itself from <code>LD_PRELOAD</code>, the proxy binary would itself be hooked, causing infinite recursion. The proxy re-adds <code>LD_PRELOAD</code> only when launching commands that need interception (INJECT action).</p><p><strong>Session tree</strong>. The daemon maintains a tree of session IDs that mirrors the build process tree. Each proxy instance registers with its parent&#39;s session ID, allowing the daemon to track which processes spawned which. This is essential for features like target tree reconstruction and build profiling.</p>',18))])}const E=r(h,[["render",m]]);export{C as __pageData,E as default};
