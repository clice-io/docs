import{_ as a,o,c as t,a3 as c}from"./chunks/framework.aYYe0dfI.js";const h=JSON.parse('{"title":"Module generate:evidence","description":"The generate:evidence module is responsible for assembling and formatting the evidence used to drive documentation generation. It provides a family of public build_evidence_for_* entry points (e.g., build_evidence_for_namespace_summary, build_evidence_for_function_analysis, build_evidence_for_type_implementation_summary) that collect relevant facts, analyses, and summaries from extraction and model data, packaging them into EvidencePack structs. These evidence packs are then consumed by formatting functions (format_evidence_text, format_evidence_text_bounded) and a prompt builder (build_prompt), which produce the final text or AI‑prompt inputs for each documentation page.","frontmatter":{"title":"Module generate:evidence","description":"The generate:evidence module is responsible for assembling and formatting the evidence used to drive documentation generation. It provides a family of public build_evidence_for_* entry points (e.g., build_evidence_for_namespace_summary, build_evidence_for_function_analysis, build_evidence_for_type_implementation_summary) that collect relevant facts, analyses, and summaries from extraction and model data, packaging them into EvidencePack structs. These evidence packs are then consumed by formatting functions (format_evidence_text, format_evidence_text_bounded) and a prompt builder (build_prompt), which produce the final text or AI‑prompt inputs for each documentation page.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/generate/evidence.md","filePath":"en/clore/modules/generate/evidence.md"}'),r={name:"clore/modules/generate/evidence.md"};function d(i,e,n,s,l,p){return o(),t("div",null,[...e[0]||(e[0]=[c('<h1 id="module-generate-evidence" tabindex="-1">Module <code>generate:evidence</code> <a class="header-anchor" href="#module-generate-evidence" aria-label="Permalink to &quot;Module `generate:evidence`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>generate:evidence</code> module is responsible for assembling and formatting the evidence used to drive documentation generation. It provides a family of public <code>build_evidence_for_*</code> entry points (e.g., <code>build_evidence_for_namespace_summary</code>, <code>build_evidence_for_function_analysis</code>, <code>build_evidence_for_type_implementation_summary</code>) that collect relevant facts, analyses, and summaries from extraction and model data, packaging them into <code>EvidencePack</code> structs. These evidence packs are then consumed by formatting functions (<code>format_evidence_text</code>, <code>format_evidence_text_bounded</code>) and a prompt builder (<code>build_prompt</code>), which produce the final text or AI‑prompt inputs for each documentation page.</p><p>The module owns the public types <code>EvidencePack</code> and <code>SymbolFact</code>, along with internal detail helpers for caching namespace‑level facts, merging fact collections, and rendering source snippets. Its public interface covers all major page categories in the generation pipeline—namespace, module, index, architecture, function (declaration, implementation, analysis), variable analysis, and type (analysis, declaration, implementation)—and provides the bounded text formatting needed for length‑constrained contexts.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../extract/"><code>extract</code></a></li><li><a href="./model"><code>generate:model</code></a></li><li><code>std</code></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./analysis"><code>generate:analysis</code></a></li><li><a href="./"><code>generate:evidence_builder</code></a></li><li><a href="./scheduler"><code>generate:scheduler</code></a></li></ul><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-generate-evidencepack" tabindex="-1"><code>clore::generate::EvidencePack</code> <a class="header-anchor" href="#clore-generate-evidencepack" aria-label="Permalink to &quot;`clore::generate::EvidencePack`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:22</code></p><p>Definition: <code>generate/evidence.cppm:22</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The <code>clore::generate::EvidencePack</code> struct is a plain aggregate data type that consolidates all evidence collected for a single generation request. It holds the target <code>page_id</code> and <code>prompt_kind</code> to identify the generation context, along with <code>subject_name</code> and <code>subject_kind</code> that describe the symbol or element being documented. The vectors of <code>SymbolFact</code> — <code>target_facts</code>, <code>local_context</code>, <code>dependency_context</code>, and <code>reverse_usage_context</code> — organize facts by scoping category, while <code>source_snippets</code> stores relevant code excerpts and <code>related_page_summaries</code> holds brief descriptions of linked pages.</p><p>The struct does not enforce invariants internally; correctness depends on the caller populating each field consistently. For example, <code>target_facts</code> should contain facts directly about the subject, whereas <code>local_context</code> and <code>dependency_context</code> provide surrounding or transitive facts. The <code>page_id</code> and <code>prompt_kind</code> are expected to be non‑empty, and all vectors are typically filled with data from the preceding analysis passes. No member functions are provided — the struct serves purely as a container for downstream generation logic.</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>Fields are set externally and not validated internally.</li><li>Vectors may be empty; no guarantee of non‑emptiness.</li><li><code>page_id</code> and <code>prompt_kind</code> are typically non‑empty strings.</li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>page_id</code></li><li><code>prompt_kind</code></li><li><code>subject_name</code></li><li><code>subject_kind</code></li><li><code>target_facts</code></li><li><code>local_context</code></li><li><code>dependency_context</code></li><li><code>reverse_usage_context</code></li><li><code>related_page_summaries</code></li><li><code>source_snippets</code></li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Populated by evidence collectors and passed to generation pipelines.</li><li>Consumed by prompt builders to construct input for LLM or template engines.</li><li>Acts as the primary input for <code>clore::generate</code> functions.</li></ul><h3 id="clore-generate-prompterror" tabindex="-1"><code>clore::generate::PromptError</code> <a class="header-anchor" href="#clore-generate-prompterror" aria-label="Permalink to &quot;`clore::generate::PromptError`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:90</code></p><p>Definition: <code>generate/evidence.cppm:90</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::PromptError</code> is a lightweight error type that stores a single <code>std::string</code> member <code>message</code>. Its implementation consists solely of this data member, with no additional invariants beyond the natural properties of <code>std::string</code>. The class is trivially constructible, copyable, and movable, serving as a simple container for an error description without any validation or resource management logic. All behavior is delegated to the <code>std::string</code> member, making the struct suitable for use in error‑handling mechanisms such as <code>std::expected</code> or exception wrappers where the only required information is a human‑readable explanation of the failure.</p><h4 id="invariants-1" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-1" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>The <code>message</code> member may contain any string, including an empty string.</li></ul><h4 id="key-members-1" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-1" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>The <code>message</code> member of type <code>std::string</code></li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Constructed with an error description string to represent a prompt generation error.</li></ul><h3 id="clore-generate-symbolfact" tabindex="-1"><code>clore::generate::SymbolFact</code> <a class="header-anchor" href="#clore-generate-symbolfact" aria-label="Permalink to &quot;`clore::generate::SymbolFact`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:9</code></p><p>Definition: <code>generate/evidence.cppm:9</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::SymbolFact</code> is a plain aggregate that stores the extracted metadata of a single symbol for use during documentation generation. All members are public and directly accessible. The field <code>id</code> of type <code>extract::SymbolID</code> uniquely identifies the symbol, while the string members such as <code>qualified_name</code>, <code>signature</code>, <code>kind_label</code>, <code>access</code>, <code>template_params</code>, <code>declaration_file</code>, and <code>doc_comment</code> hold the textual attributes. The boolean <code>is_template</code> defaults to <code>false</code>, and the integral <code>declaration_line</code> defaults to <code>0</code>, forming the only implicit invariants. No additional structural invariants are enforced by the type; the struct is intended to be populated by the extraction phase and then consumed by the generation phase as a flat collection of facts.</p><h4 id="invariants-2" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-2" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>No documented invariants beyond default member initializers</li><li>Fields may be empty or zero if not populated</li></ul><h4 id="key-members-2" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-2" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>id</code></li><li><code>qualified_name</code></li><li><code>signature</code></li><li><code>kind_label</code></li><li><code>access</code></li><li><code>is_template</code></li><li><code>template_params</code></li><li><code>declaration_file</code></li><li><code>declaration_line</code></li><li><code>doc_comment</code></li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Populated during extraction and used to represent a symbol&#39;s metadata</li><li>Consumed by code generation stages</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-generate-detail-collect-analysis-summaries" tabindex="-1"><code>clore::generate::__detail::collect_analysis_summaries</code> <a class="header-anchor" href="#clore-generate-detail-collect-analysis-summaries" aria-label="Permalink to &quot;`clore::generate::__detail::collect_analysis_summaries`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:121</code></p><p>Definition: <code>generate/evidence.cppm:241</code></p><p>This function is a thin forwarding wrapper that delegates all work to <code>collect_analysis_summaries_impl</code>. It accepts the same three parameters (<code>analyses</code>, <code>model</code>, and <code>ids</code>) and passes them directly to the implementation, along with an identity lambda <code>[](extract::SymbolID id) { return id; }</code>. The lambda is used as the <code>SymbolIDOf</code> callback; by returning the id unchanged, it indicates that the provided <code>ids</code> already represent the exact symbol identifiers to be processed, without any remapping or filtering. Consequently, the function itself contains no additional algorithm or control flow—its sole purpose is to serve as a public entry point that invokes the shared implementation with a trivial projection.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>analyses</code></li><li><code>model</code></li><li><code>ids</code></li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>wrapping <code>collect_analysis_summaries_impl</code> with identity projection</li><li>retrieving analysis summaries for a set of symbol <code>IDs</code></li></ul><h3 id="clore-generate-detail-collect-analysis-summaries-impl" tabindex="-1"><code>clore::generate::__detail::collect_analysis_summaries_impl</code> <a class="header-anchor" href="#clore-generate-detail-collect-analysis-summaries-impl" aria-label="Permalink to &quot;`clore::generate::__detail::collect_analysis_summaries_impl`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:116</code></p><p>Definition: <code>generate/evidence.cppm:213</code></p><p>The function iterates over each element in the <code>items</code> range, deduplicating by calling <code>symbol_id_of</code> to derive an <code>extract::SymbolID</code> and checking against a <code>seen</code> unordered set. If the ID is new, it looks up the corresponding symbol via <code>extract::lookup_symbol</code> on the <code>model</code>; if the symbol exists, it retrieves its analysis summary through <code>analysis_overview_markdown</code> from the <code>analyses</code> store. Only non-null, non-empty summary strings are appended to the result vector, which is returned after processing all items. This implementation relies on <code>extract::lookup_symbol</code> for symbol resolution and <code>analysis_overview_markdown</code> for retrieving pre-computed analysis text from the analysis store.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>analyses</code> (const <code>SymbolAnalysisStore&amp;</code>)</li><li><code>model</code> (const <code>extract::ProjectModel&amp;</code>)</li><li><code>items</code> (const <code>Range&amp;</code>)</li><li><code>symbol_id_of</code> callable</li><li>symbol data pointed by <code>extract::lookup_symbol</code> result</li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to collect summaries for a range of symbols or declarations</li><li>Provides deduplication by symbol ID</li></ul><h3 id="clore-generate-detail-collect-facts" tabindex="-1"><code>clore::generate::__detail::collect_facts</code> <a class="header-anchor" href="#clore-generate-detail-collect-facts" aria-label="Permalink to &quot;`clore::generate::__detail::collect_facts`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:103</code></p><p>Definition: <code>generate/evidence.cppm:163</code></p><p><code>clore::generate::__detail::collect_facts</code> accepts an <code>extract::ProjectModel</code>, a vector of <code>extract::SymbolID</code>, and a project root string. It deduplicates the input <code>IDs</code> using an internal <code>std::unordered_set&lt;extract::SymbolID&gt;</code>, then for each unique ID it calls <code>extract::lookup_symbol</code> to retrieve the corresponding symbol node. If the lookup succeeds, the function converts the node into a <code>SymbolFact</code> via the helper <code>to_symbol_fact</code> and appends it to a local <code>std::vector&lt;SymbolFact&gt;</code>, which becomes the return value. The function relies on the <code>extract</code> library for symbol resolution and on the <code>to_symbol_fact</code> utility for mapping the raw extracted data into the higher‑level fact representation used by the evidence‑generation pipeline.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>model</code> parameter</li><li><code>ids</code> parameter</li><li><code>project_root</code> parameter</li><li>result of <code>extract::lookup_symbol</code></li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>local <code>std::vector&lt;SymbolFact&gt; facts</code></li><li>local <code>std::unordered_set&lt;extract::SymbolID&gt; seen</code></li></ul><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used to convert a list of symbol <code>IDs</code> into a deduplicated vector of facts</li><li>likely called by higher‑level generation functions that need symbol facts</li></ul><h3 id="clore-generate-detail-collect-merged-facts" tabindex="-1"><code>clore::generate::__detail::collect_merged_facts</code> <a class="header-anchor" href="#clore-generate-detail-collect-merged-facts" aria-label="Permalink to &quot;`clore::generate::__detail::collect_merged_facts`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:108</code></p><p>Definition: <code>generate/evidence.cppm:179</code></p><p>The function <code>clore::generate::__detail::collect_merged_facts</code> merges the symbol facts from multiple heterogeneous groups of symbol <code>IDs</code> into a single deduplicated vector. It receives an <code>extract::ProjectModel</code>, a <code>project_root</code> path, and a variadic pack <code>Groups...</code> (each group is a container of <code>extract::SymbolID</code>). An <code>std::unordered_set&lt;extract::SymbolID&gt;</code> named <code>seen</code> tracks already processed identifiers, preventing duplicates across groups.</p><p>A local lambda <code>append_group</code> iterates over a single group’s <code>IDs</code>. For each ID, it attempts insertion into <code>seen</code>; if the ID is already present, the lambda skips it. Otherwise, it calls <code>extract::lookup_symbol</code> on the model to obtain the corresponding symbol object, and if found, appends the result of <code>to_symbol_fact</code> to the <code>facts</code> vector. The variadic expansion <code>(append_group(groups), ...)</code> applies <code>append_group</code> to each group in left‑to‑right order via a fold expression. After all groups are processed, the function returns <code>facts</code>—a <code>std::vector&lt;SymbolFact&gt;</code> containing the deduplicated, collected facts.</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>model</li><li><code>project_root</code></li><li>groups (each being a container of <code>extract::SymbolID</code>)</li><li>symbol data retrieved via <code>extract::lookup_symbol</code></li></ul><h4 id="usage-patterns-6" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-6" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Merging symbol facts from multiple analysis groups</li><li>Deduplicating symbol facts by symbol ID</li><li>Collecting facts for batch documentation generation</li></ul><h3 id="clore-generate-detail-collect-namespace-facts-cached" tabindex="-1"><code>clore::generate::__detail::collect_namespace_facts_cached</code> <a class="header-anchor" href="#clore-generate-detail-collect-namespace-facts-cached" aria-label="Permalink to &quot;`clore::generate::__detail::collect_namespace_facts_cached`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:133</code></p><p>Definition: <code>generate/evidence.cppm:275</code></p><p>The function <code>clore::generate::__detail::collect_namespace_facts_cached</code> implements a memoized retrieval of all <code>SymbolFact</code> entries belonging to a given namespace. It takes a <code>extract::ProjectModel</code>, a <code>namespace_name</code>, a <code>project_root</code>, a <code>target_kind</code> indicating the kind of symbol being collected for, and an <code>exclude_id</code> used to omit the requesting symbol itself. The function first constructs a <code>cache_key</code> from <code>namespace_name</code> and <code>project_root</code>, then looks it up in a static cache returned by <code>namespace_facts_cache</code>. On a cache miss, it locates the namespace in the model and iterates over its symbols, calling <code>to_symbol_fact</code> on each one. The resulting facts are partitioned into three vectors inside a <code>NamespaceFactsCacheEntry</code>: <code>all_functions</code>, <code>all_types</code>, and <code>all_variables</code>, based on the result of <code>is_function_kind</code>, <code>is_type_kind</code>, or <code>is_variable_kind</code>. The entry is then inserted into the cache.</p><p>On cache hit, the function selects the appropriate vector according to <code>target_kind</code> using the same set of kind predicates. If no matching vector exists (e.g., an unknown kind) or if <code>namespace_name</code> is empty, it returns an empty result. Otherwise, it copies all facts from the selected source vector, skipping any fact whose <code>id</code> equals <code>exclude_id</code> to avoid self‑reference. The returned vector contains the remaining namespace‑level facts, ready for downstream evidence construction. The caching design avoids re‑traversing the namespace symbols for every sibling symbol during a multi‑symbol generation pass.</p><h4 id="side-effects-4" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-4" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>updates the static namespace facts cache by inserting new entries</li><li>may cause dynamic memory allocation for cache entries and result vectors</li></ul><h4 id="reads-from-4" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-4" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>model</code> (project model)</li><li>parameter <code>namespace_name</code></li><li>parameter <code>project_root</code></li><li>parameter <code>target_kind</code></li><li>parameter <code>exclude_id</code></li><li>static <code>namespace_facts_cache()</code> cache map</li><li>results from <code>extract::lookup_symbol</code></li><li>results from <code>to_symbol_fact</code></li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>static <code>namespace_facts_cache()</code> via insertion/emplace</li></ul><h4 id="usage-patterns-7" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-7" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called when building evidence for multiple symbols in the same namespace to avoid repeated namespace traversals</li><li>used by higher-level fact collection functions such as <code>collect_facts</code></li></ul><h3 id="clore-generate-detail-collect-summaries" tabindex="-1"><code>clore::generate::__detail::collect_summaries</code> <a class="header-anchor" href="#clore-generate-detail-collect-summaries" aria-label="Permalink to &quot;`clore::generate::__detail::collect_summaries`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:112</code></p><p>Definition: <code>generate/evidence.cppm:200</code></p><p>The function iterates over each key in the input <code>keys</code> vector. For each key, it performs a lookup in the <code>cache</code> map via <code>cache.find(key)</code>. If the iterator <code>it</code> is found (i.e., not equal to <code>cache.end()</code>) and the associated value (<code>it-&gt;second</code>) is not empty, that value is appended to the <code>result</code> vector. The function depends on the <code>PageSummaryCache</code> type supporting standard map lookup semantics; the returned vector preserves the order of keys from the input that had a non‑empty entry.</p><p>Control flow is linear—no branching beyond the condition check, no special handling for duplicate keys or missing entries. The function is a pure retrieval utility, extracting pre‑computed summary strings from a cache by their identifiers. It relies implicitly on the caller ensuring that <code>keys</code> contains valid lookup identifiers and that <code>cache</code> has been populated beforehand.</p><h4 id="side-effects-5" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-5" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-5" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-5" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>cache</code> parameter</li><li><code>keys</code> parameter</li></ul><h4 id="usage-patterns-8" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-8" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Retrieve a subset of cached summaries by specified keys</li></ul><h3 id="clore-generate-detail-to-symbol-fact" tabindex="-1"><code>clore::generate::__detail::to_symbol_fact</code> <a class="header-anchor" href="#clore-generate-detail-to-symbol-fact" aria-label="Permalink to &quot;`clore::generate::__detail::to_symbol_fact`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:101</code></p><p>Definition: <code>generate/evidence.cppm:147</code></p><p>The function <code>clore::generate::__detail::to_symbol_fact</code> converts a <code>SymbolInfo</code> object extracted by the <code>extract</code> layer into a <code>SymbolFact</code> which is the canonical fact representation used in prompt construction. It performs a direct field‑wise copy from <code>sym</code> to the returned <code>SymbolFact</code>: copying <code>sym.id</code>, <code>sym.qualified_name</code>, <code>sym.signature</code>, <code>sym.access</code>, <code>sym.is_template</code>, <code>sym.template_params</code>, and <code>sym.doc_comment</code>. The <code>kind_label</code> is produced by calling <code>extract::symbol_kind_name</code> on <code>sym.kind</code> and converting the result to <code>std::string</code>. The <code>declaration_file</code> is made project‑relative by passing the absolute path from <code>sym.declaration_location.file</code> through <code>clore::generate::make_source_relative</code> with <code>project_root</code>. The <code>declaration_line</code> is taken directly from <code>sym.declaration_location.line</code>. The function is a trivial aggregator with no branching or iteration; its only dependencies are the <code>extract</code> module’s symbol‑info types and <code>make_source_relative</code> for path normalisation.</p><h4 id="side-effects-6" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-6" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-6" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-6" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>sym.id</code></li><li><code>sym.qualified_name</code></li><li><code>sym.signature</code></li><li><code>sym.kind</code></li><li><code>sym.access</code></li><li><code>sym.is_template</code></li><li><code>sym.template_params</code></li><li><code>sym.declaration_location.file</code></li><li><code>sym.declaration_location.line</code></li><li><code>sym.doc_comment</code></li><li><code>project_root</code></li></ul><h4 id="usage-patterns-9" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-9" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to produce a <code>SymbolFact</code> from an extracted symbol for further analysis or serialization.</li></ul><h3 id="clore-generate-build-evidence-for-function-analysis" tabindex="-1"><code>clore::generate::build_evidence_for_function_analysis</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-function-analysis" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_function_analysis`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:40</code></p><p>Definition: <code>generate/evidence_builder.cppm:53</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function constructs evidence for function analysis by orchestrating several sub‑processes. It first initializes a <code>root</code> data structure and a <code>project_root</code> context, then iterates over a <code>pack</code> of symbols (<code>sym</code>). For each symbol, it invokes <code>clore::generate::(anonymous namespace)::maybe_resolve_snippet</code> to conditionally resolve snippet references, and collects the results into <code>analyses</code> and <code>page_summaries</code>. The function then builds a <code>model</code> by aggregating data from <code>analyses</code>, <code>imported_summaries</code>, and <code>namespace_fact</code> entries, using <code>file_it</code> to traverse translation units and assemble per‑file evidence. Finally, it returns an <code>int</code> representing the count of successfully processed function analyses.</p><p>Control flow proceeds linearly: setup → symbol resolution loop → model construction → evidence packing. The function depends on <code>model</code> to store intermediate analysis results, <code>pack</code> for symbol iteration, <code>project_root</code> for file path resolution, and the helper <code>maybe_resolve_snippet</code> to handle snippet‑based declarations. The <code>target</code> parameter (third argument) governs which specific function analysis is built, while <code>analyses</code> and <code>page_summaries</code> are gradually populated and later merged into the final output <code>pack</code>.</p><h4 id="side-effects-7" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-7" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="usage-patterns-10" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-10" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used to construct evidence for function analysis documentation</li></ul><h3 id="clore-generate-build-evidence-for-function-declaration-summary" tabindex="-1"><code>clore::generate::build_evidence_for_function_declaration_summary</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-function-declaration-summary" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_function_declaration_summary`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:67</code></p><p>Definition: <code>generate/evidence_builder.cppm:238</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function iterates over each <code>analyses</code> collection and its associated <code>pack</code> structures, using the <code>project_root</code> and <code>root</code> (the document or AST entry point) to navigate the declaration context. For each <code>pack</code>, it calls <code>maybe_resolve_snippet</code> to resolve inline code references, then collects resolved content into <code>page_summaries</code> and <code>imported_summaries</code>. These summaries are assembled into a <code>model</code> object, which also incorporates namespace context via <code>namespace_fact</code> and <code>ns</code>. The <code>model</code> is then aggregated with information from <code>mod</code> (module context) and combined with the original <code>target</code> symbol to produce the final evidence. The entire algorithm relies on the interplay between the <code>pack</code>-based analysis structure and the <code>maybe_resolve_snippet</code> helper to transform raw analysis data into a structured summary model.</p><h4 id="side-effects-8" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-8" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-7" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-7" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameters: <code>const int &amp;</code> (first, second, third, fourth) and <code>int</code> (fifth)</li></ul><h4 id="usage-patterns-11" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-11" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during evidence construction for function declaration summary pages</li><li>Used in the page generation pipeline for symbol documentation</li></ul><h3 id="clore-generate-build-evidence-for-function-implementation-summary" tabindex="-1"><code>clore::generate::build_evidence_for_function_implementation_summary</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-function-implementation-summary" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_function_implementation_summary`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:72</code></p><p>Definition: <code>generate/evidence_builder.cppm:268</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::build_evidence_for_function_implementation_summary</code> constructs the evidence data for a given target function by traversing the project structure from a specified <code>project_root</code>. It iterates over file entries (<code>file_it</code>) and corresponding page summaries, and for each relevant location it calls <code>maybe_resolve_snippet</code> to verify whether a snippet matches. The evidence is built by assembling <code>analyses</code> and <code>model</code> objects, which are then stored in a <code>pack</code> along with the <code>target</code>. The internal control flow follows a linear path: it first resolves the root assembly, then iterates over the files, collecting evidence from each snippet, and finally populates the model with the gathered information before packaging it for output. Dependencies include the project root directory, the target identifier, and helper functions like <code>maybe_resolve_snippet</code> that handle snippet validation.</p><h4 id="side-effects-9" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-9" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="usage-patterns-12" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-12" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during evidence construction for function pages</li><li>part of the page generation pipeline</li></ul><h3 id="clore-generate-build-evidence-for-index-overview" tabindex="-1"><code>clore::generate::build_evidence_for_index_overview</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-index-overview" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_index_overview`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:64</code></p><p>Definition: <code>generate/evidence_builder.cppm:204</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function begins by traversing the directory tree rooted at <code>root</code>, using <code>project_root</code> as the base path for resolution. For each file encountered via <code>file_it</code>, it invokes <code>clore::generate::(anonymous namespace)::maybe_resolve_snippet</code> to conditionally resolve snippet references and accumulate the results into <code>analyses</code>. It then builds a <code>model</code> by processing each resolved <code>sym</code> and <code>target</code> within the current <code>pack</code>, constructing <code>page_summaries</code> and <code>imported_summaries</code> that reflect the index’s structure.</p><p>After the traversal, the function consolidates the collected <code>analyses</code>, <code>pack</code>, and <code>model</code> into a <code>namespace_fact</code> representation, combining data from the current module <code>mod</code> and any imported summaries. Finally, it returns an integer status code indicating success or failure of the evidence generation process. The entire flow depends on the internal <code>maybe_resolve_snippet</code> utility and the iterative assembly of <code>page_summaries</code> from the filesystem contents under <code>project_root</code>.</p><h4 id="side-effects-10" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-10" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-8" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-8" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the two integer parameters</li></ul><h4 id="usage-patterns-13" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-13" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during page generation for index overview</li><li>used in documentation generation pipeline</li></ul><h3 id="clore-generate-build-evidence-for-module-architecture" tabindex="-1"><code>clore::generate::build_evidence_for_module_architecture</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-module-architecture" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_module_architecture`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:58</code></p><p>Definition: <code>generate/evidence_builder.cppm:173</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function first sets up the <code>project_root</code> and <code>root</code> directories, then initializes a data structure <code>pack</code> to hold the module information. It iterates over files or packages using <code>file_it</code>, and for each, performs initial analysis via <code>analyses</code>. For each target symbol (<code>sym</code>) or module (<code>mod</code>), it calls <code>clore::generate::(anonymous namespace)::maybe_resolve_snippet</code> to conditionally resolve snippet references, collecting the results into <code>page_summaries</code>. A <code>model</code> is built from the resolved data, incorporating <code>namespace_fact</code> and <code>imported_summaries</code>, and the evidence is finally constructed by consolidating this model with the original <code>root</code> and <code>target</code> information. The algorithm relies on the <code>pack</code> container to aggregate per‑module data and the <code>analyses</code> variable to drive per‑source processing, with <code>maybe_resolve_snippet</code> serving as the key dependency for conditional snippet resolution.</p><h4 id="side-effects-11" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-11" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-9" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-9" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>integer parameters representing module context (e.g., module ID, file ID, analysis store)</li></ul><h4 id="usage-patterns-14" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-14" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called when building documentation for a module to generate architecture evidence</li><li>used in module page generation pipelines</li></ul><h3 id="clore-generate-build-evidence-for-module-summary" tabindex="-1"><code>clore::generate::build_evidence_for_module_summary</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-module-summary" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_module_summary`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:52</code></p><p>Definition: <code>generate/evidence_builder.cppm:142</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function begins by establishing a <code>project_root</code> and iterating over <code>pack</code> or <code>mod</code> entities. For each <code>target</code>, it invokes <code>clore::generate::(anonymous namespace)::maybe_resolve_snippet</code> to conditionally process snippet references, using <code>namespace_fact</code> and <code>analyses</code> to resolve context. It then populates <code>page_summaries</code> by aggregating data from <code>analyses</code> and <code>model</code> structures, and incorporates <code>imported_summaries</code> when cross-module references are detected. The control flow uses a <code>root</code> variable to track the current context and <code>file_it</code> to traverse source files, with <code>sym</code>, <code>ns</code>, and <code>target</code> guiding the resolution. The algorithm relies on the internal helper <code>maybe_resolve_snippet</code> to decide whether a snippet should be expanded or left as a placeholder, and ultimately returns an integer representing the number of successfully built summary entries or an error indicator.</p><h4 id="side-effects-12" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-12" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-10" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-10" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameters (const int &amp;) representing module identifiers and analysis data</li><li>internal analysis store (e.g., <code>SymbolAnalysisStore</code>, implied by local context functions)</li></ul><h4 id="usage-patterns-15" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-15" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during generation of module summary pages</li><li>used to produce the <code>EvidencePack</code> or markdown content for a module overview</li></ul><h3 id="clore-generate-build-evidence-for-namespace-summary" tabindex="-1"><code>clore::generate::build_evidence_for_namespace_summary</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-namespace-summary" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_namespace_summary`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:35</code></p><p>Definition: <code>generate/evidence_builder.cppm:21</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function begins by deriving a <code>project_root</code> and then iterates over <code>file_it</code> entries within a <code>root</code> structure to accumulate <code>page_summaries</code>. For each page summary, it optionally invokes the helper <code>clore::generate::(anonymous namespace)::maybe_resolve_snippet</code> to conditionally resolve snippet references. After gathering all summaries, it processes a targeted <code>pack</code> and <code>sym</code> to extract relevant <code>analyses</code> and <code>model</code> data. The core loop builds a <code>namespace_fact</code> by traversing <code>ns</code> and <code>mod</code> elements, incorporating <code>imported_summaries</code> from external packs. Finally, the function constructs the evidence for the namespace summary by combining the resolved <code>target</code> with the collected <code>page_summaries</code> and <code>analyses</code>, returning an integer result that indicates the success or count of the operation. The entire flow depends on the helper function for snippet resolution and on the local data structures <code>root</code>, <code>pack</code>, and <code>project_root</code> to navigate the module hierarchy.</p><h4 id="side-effects-13" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-13" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-11" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-11" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the three <code>const int &amp;</code> parameters (likely representing namespace identifier, analysis store, and other context)</li><li>the <code>int</code> parameter (possibly a page plan or other index)</li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>the returned <code>int</code> (an identifier for the built evidence)</li></ul><h4 id="usage-patterns-16" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-16" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during namespace summary page generation</li><li>used in <code>build_namespace_page_root</code> or similar page assembly functions</li></ul><h3 id="clore-generate-build-evidence-for-type-analysis" tabindex="-1"><code>clore::generate::build_evidence_for_type_analysis</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-type-analysis" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_type_analysis`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:44</code></p><p>Definition: <code>generate/evidence_builder.cppm:82</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::build_evidence_for_type_analysis</code> orchestrates the collection of type‑analysis evidence across a project. It first traverses the <code>page_summaries</code> rooted at <code>project_root</code>, applying the helper <code>maybe_resolve_snippet</code> to filter resolvable code snippets. For each valid snippet, it populates an <code>analyses</code> structure, which is then used to construct a <code>model</code> that incorporates <code>pack</code>‑level metadata (e.g., module and namespace facts). The control flow iterates over <code>root</code>, resolves <code>target</code> elements, and builds <code>imported_summaries</code> from external dependencies before finalizing the evidence model. The function depends on <code>maybe_resolve_snippet</code> for snippet resolution, <code>pack</code> for package context, and <code>project_root</code> for file‑system navigation.</p><h4 id="side-effects-14" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-14" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-12" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-12" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the first <code>const int &amp;</code> parameter</li><li>the second <code>const int &amp;</code> parameter</li><li>the third <code>int</code> parameter</li></ul><h4 id="usage-patterns-17" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-17" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during documentation generation to produce evidence for type analysis pages</li><li>likely invoked by higher-level page building functions such as <code>build_page_root</code> or <code>build_page_plan_set</code></li></ul><h3 id="clore-generate-build-evidence-for-type-declaration-summary" tabindex="-1"><code>clore::generate::build_evidence_for_type_declaration_summary</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-type-declaration-summary" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_type_declaration_summary`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:77</code></p><p>Definition: <code>generate/evidence_builder.cppm:302</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function begins by establishing a working <code>root</code> directory and <code>project_root</code> path, then iterates over a range represented by <code>file_it</code>. For each element in the pack, it calls the helper <code>clore::generate::(anonymous namespace)::maybe_resolve_snippet</code> to conditionally resolve snippet references. The core logic then builds a collection of <code>page_summaries</code> and <code>imported_summaries</code> by inspecting <code>analyses</code>, <code>model</code>, and the target <code>sym</code> from the current <code>pack</code>. A <code>namespace_fact</code> is extracted and used together with <code>ns</code> and <code>mod</code> to populate the evidence. The function synthesises information from <code>analyses</code>, <code>model</code>, and <code>pack</code> to produce a final summary, returning an integer that likely represents a result code or count.</p><h4 id="side-effects-15" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-15" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-13" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-13" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameters of type <code>const int &amp;</code> and <code>int</code> (likely representing type ID, analysis store, or other identifiers)</li></ul><h4 id="usage-patterns-18" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-18" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called by other evidence-building functions or page generation routines to produce type declaration documentation</li></ul><h3 id="clore-generate-build-evidence-for-type-implementation-summary" tabindex="-1"><code>clore::generate::build_evidence_for_type_implementation_summary</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-type-implementation-summary" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_type_implementation_summary`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:82</code></p><p>Definition: <code>generate/evidence_builder.cppm:334</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function builds an evidence summary for a given type implementation by iterating over a project root and collecting relevant facts. It first resolves the target type symbol, then traverses the module hierarchy using <code>root</code> and <code>project_root</code> to locate the associated source files. For each relevant file, it processes <code>page_summaries</code> and <code>imported_summaries</code> to gather documentation snippets, calling <code>clore::generate::(anonymous namespace)::maybe_resolve_snippet</code> to conditionally resolve inline references. The algorithm aggregates namespace and module facts (<code>ns</code>, <code>mod</code>, <code>namespace_fact</code>) and updates the <code>model</code> and <code>analyses</code> collections, finalizing the evidence payload into <code>pack</code>. Control flow depends on the presence of <code>sym</code>, <code>target</code>, and the availability of resolved snippets, with early exits if the type cannot be located.</p><h4 id="side-effects-16" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-16" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-14" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-14" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>clore::generate::build_evidence_for_type_implementation_summary</code> parameters (analysis store and type identifier)</li></ul><h4 id="usage-patterns-19" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-19" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during page building for type implementation summaries</li><li>Part of the evidence generation step before rendering markdown</li></ul><h3 id="clore-generate-build-evidence-for-variable-analysis" tabindex="-1"><code>clore::generate::build_evidence_for_variable_analysis</code> <a class="header-anchor" href="#clore-generate-build-evidence-for-variable-analysis" aria-label="Permalink to &quot;`clore::generate::build_evidence_for_variable_analysis`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:48</code></p><p>Definition: <code>generate/evidence_builder.cppm:113</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function begins by initializing a <code>root</code> variable and then iterates over a <code>pack</code> of analyses. For each <code>target</code> in the <code>analyses</code> range, it calls the internal helper <code>maybe_resolve_snippet</code> to conditionally resolve a code snippet; if that succeeds, it constructs a <code>namespace_fact</code> from the current <code>ns</code> and <code>mod</code> context. The function then builds a <code>model</code> by combining <code>root</code>, <code>project_root</code>, and <code>imported_summaries</code> together with the current <code>page_summaries</code>. For each analysis, it records evidence by associating the <code>target</code> with the derived <code>model</code> and an incrementing integer counter. The dependencies include the anonymous-namespace function <code>maybe_resolve_snippet</code> and the types or structures represented by <code>project_root</code>, <code>pack</code>, <code>analyses</code>, <code>ns</code>, <code>mod</code>, and <code>model</code>, all of which are assumed to be defined within the <code>clore::generate</code> module.</p><h4 id="side-effects-17" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-17" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-15" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-15" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the <code>SymbolAnalysisStore</code> (via an opaque parameter)</li><li>variable identifiers passed as integer references</li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>an evidence pack (opaque integer handle)</li></ul><h4 id="usage-patterns-20" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-20" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called by page building functions to include variable analysis evidence</li><li>used in <code>build_symbol_analysis_prompt</code> and <code>render_page_markdown</code></li></ul><h3 id="clore-generate-build-prompt" tabindex="-1"><code>clore::generate::build_prompt</code> <a class="header-anchor" href="#clore-generate-build-prompt" aria-label="Permalink to &quot;`clore::generate::build_prompt`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:94</code></p><p>Definition: <code>generate/evidence.cppm:651</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::build_prompt</code> constructs a prompt string by first resolving the appropriate template via <code>prompt_template_of(kind)</code>. If the returned template is empty, it returns an error of type <code>PromptError</code> with a descriptive message. Otherwise, it delegates to <code>instantiate_prompt_with_evidence</code>, passing the template, the <code>evidence</code> pack, and a pre‑formatted evidence text obtained from <code>format_evidence_text(evidence)</code>.</p><p>The internal flow is a straightforward retrieval‑and‑fill pattern: the template is selected based on the <code>kind</code> discriminator, and then the evidence pack is rendered into a textual representation by <code>format_evidence_text</code>, which internally uses multiple bounded formatting routines and helpers like <code>format_evidence_text_bounded</code> to collect symbol facts, namespaces, analysis summaries, and other context fields such as <code>local_context</code>, <code>dependency_context</code>, <code>reverse_usage_context</code>, <code>related_page_summaries</code>, <code>source_snippets</code>, etc. The final filled prompt is returned or propagated through the caller.</p><h4 id="side-effects-18" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-18" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-16" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-16" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>kind</li><li>evidence</li><li><code>prompt_template_of</code>(kind)</li></ul><h4 id="usage-patterns-21" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-21" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>construct prompt message for LLM</li><li>generate prompt text from template and evidence</li></ul><h3 id="clore-generate-format-evidence-text" tabindex="-1"><code>clore::generate::format_evidence_text</code> <a class="header-anchor" href="#clore-generate-format-evidence-text" aria-label="Permalink to &quot;`clore::generate::format_evidence_text`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:86</code></p><p>Definition: <code>generate/evidence.cppm:580</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::format_evidence_text</code> serves as the unbounded public entry point for formatting evidence text. Its entire implementation delegates to <code>format_evidence_text_bounded</code>, passing <code>std::numeric_limits&lt;std::size_t&gt;::max()</code> as the maximum length, effectively disabling any truncation. This design separates the bounded and unbounded formatting paths, keeping the core logic in <code>format_evidence_text_bounded</code> while providing a convenience wrapper that avoids requiring callers to supply an explicit size limit. The function’s control flow is a single call with no additional validation or preprocessing; all structural decisions (section building, prompt instantiation, and fact collection) are inherited from the bounded implementation.</p><h4 id="side-effects-19" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-19" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-17" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-17" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>pack (const <code>EvidencePack</code>&amp;)</li></ul><h4 id="usage-patterns-22" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-22" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to format evidence text when no size bound is required.</li></ul><h3 id="clore-generate-format-evidence-text-bounded" tabindex="-1"><code>clore::generate::format_evidence_text_bounded</code> <a class="header-anchor" href="#clore-generate-format-evidence-text-bounded" aria-label="Permalink to &quot;`clore::generate::format_evidence_text_bounded`&quot;">​</a></h3><p>Declaration: <code>generate/evidence.cppm:88</code></p><p>Definition: <code>generate/evidence.cppm:584</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function constructs a structured evidence string by iterating over two categories of sections: symbol sections and text sections. It first attempts to append a top-level <code>## EVIDENCE</code> header, returning an empty string if the header alone exceeds <code>max_length</code>. The four symbol sections—target, local context, dependencies, and used by—are represented by <code>SymbolSection</code> instances that hold a title, a pointer to a vector of <code>SymbolFact</code>, and a render function pointer; each section is passed to <code>append_section_bounded</code> along with the cumulative <code>text</code> and <code>max_length</code>. The same pattern is then applied to two text sections for source snippets and related page summaries, using <code>TextSection</code> with <code>std::string</code> items and corresponding render functions. The algorithm relies on <code>append_section_bounded</code> (from an internal namespace) to progressively fill the buffer, and uses <code>append_if_fits</code> to guard the header. All rendering delegates to function pointers (<code>render_detailed_fact</code>, <code>render_context_fact</code>, <code>render_source_snippet</code>, <code>render_summary_item</code>), which consume the <code>EvidencePack</code>’s target facts, context vectors, source snippets, and summary items.</p><h4 id="side-effects-20" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-20" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-18" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-18" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>const <code>EvidencePack</code> &amp;pack</li><li>pack<code>.target_facts</code></li><li>pack<code>.local_context</code></li><li>pack<code>.dependency_context</code></li><li>pack<code>.reverse_usage_context</code></li><li>pack<code>.source_snippets</code></li><li>pack<code>.related_page_summaries</code></li><li>int <code>max_length</code></li></ul><h4 id="writes-to-4" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-4" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>local <code>std::string</code> text returned by value</li></ul><h4 id="usage-patterns-23" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-23" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used to format evidence text with a bounded length for inclusion in prompts or documents</li><li>called by higher-level generation functions to prepare evidence content</li><li>likely used when truncation of evidence is necessary to fit size constraints</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The module <code>generate:evidence</code> decomposes into a public collection of evidence‑building functions (e.g., <code>build_evidence_for_namespace_summary</code>, <code>build_evidence_for_function_analysis</code>, <code>format_evidence_text</code>) and an internal implementation layer nested in the <code>__detail</code> namespace. The public functions accept symbol identifiers, analysis stores, and context parameters to produce <code>EvidencePack</code> structs that aggregate facts, source snippets, and related page summaries. Internal helpers such as <code>collect_facts</code>, <code>collect_summaries</code>, and <code>collect_namespace_facts_cached</code> handle the gathering and caching of <code>SymbolFact</code> objects, with a dedicated <code>NamespaceFactsCacheEntry</code> avoiding redundant traversal when multiple symbols belong to the same namespace. The module imports from <code>std</code>, <code>extract</code>, and <code>generate:model</code>, relying on the <code>extract</code> module for symbol extraction results and on <code>generate:model</code> for analysis records (e.g., <code>FunctionAnalysis</code>, <code>TypeAnalysis</code>) and prompt‑kind enumeration. The implementation structure thus separates high‑level evidence composition from low‑level fact collection, using template‑based merging (<code>collect_merged_facts</code>) and length‑bounded formatting (<code>format_evidence_text_bounded</code>) to support flexible rendering within the generation pipeline.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../extract/">Module extract</a></li><li><a href="./model">Module generate:model</a></li></ul>',284)])])}const u=a(r,[["render",d]]);export{h as __pageData,u as default};
