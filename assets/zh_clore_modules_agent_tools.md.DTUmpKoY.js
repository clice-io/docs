import{_ as d,C as r,o,c as l,a3 as a,b as i,w as t,a as n,E as s,a4 as h}from"./chunks/framework.aYYe0dfI.js";const q=JSON.parse('{"title":"Module agent:tools","description":"这个模块实现了 clore::agent 中所有可使用的工具，为智能代理提供与代码库交互的标准化能力。它定义了涵盖文件列表、符号搜索、模块浏览、命名空间查看、依赖查询、指南读写等任务的十余种具体工具，每种工具都封装为结构体并公开 name、description、cacheable 等元数据以及 run 方法，通过统一的 ToolSpec 注册表管理。模块的核心入口 dispatch_tool_call 接收工具名称与 JSON 参数，将其分派到对应的工具实现并返回结果；build_tool_definitions 用于构建所有工具的 OpenAI 兼容定义，供外部协议层使用。错误被统一表示为 ToolError，同时提供 ToolContext（包含项目根、输出目录、模型等上下文信息）和线程安全的 ToolResultCache 来优化重复查询。","frontmatter":{"title":"Module agent:tools","description":"这个模块实现了 clore::agent 中所有可使用的工具，为智能代理提供与代码库交互的标准化能力。它定义了涵盖文件列表、符号搜索、模块浏览、命名空间查看、依赖查询、指南读写等任务的十余种具体工具，每种工具都封装为结构体并公开 name、description、cacheable 等元数据以及 run 方法，通过统一的 ToolSpec 注册表管理。模块的核心入口 dispatch_tool_call 接收工具名称与 JSON 参数，将其分派到对应的工具实现并返回结果；build_tool_definitions 用于构建所有工具的 OpenAI 兼容定义，供外部协议层使用。错误被统一表示为 ToolError，同时提供 ToolContext（包含项目根、输出目录、模型等上下文信息）和线程安全的 ToolResultCache 来优化重复查询。","layout":"doc","template":"doc"},"headers":[],"relativePath":"zh/clore/modules/agent/tools.md","filePath":"zh/clore/modules/agent/tools.md"}'),u={name:"zh/clore/modules/agent/tools.md"};function p(m,e,f,g,_,b){const c=r("Mermaid");return o(),l("div",null,[e[1]||(e[1]=a('<h1 id="module-agent-tools" tabindex="-1">Module <code>agent:tools</code> <a class="header-anchor" href="#module-agent-tools" aria-label="Permalink to &quot;Module `agent:tools`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>这个模块实现了 <code>clore::agent</code> 中所有可使用的工具，为智能代理提供与代码库交互的标准化能力。它定义了涵盖文件列表、符号搜索、模块浏览、命名空间查看、依赖查询、指南读写等任务的十余种具体工具，每种工具都封装为结构体并公开 <code>name</code>、<code>description</code>、<code>cacheable</code> 等元数据以及 <code>run</code> 方法，通过统一的 <code>ToolSpec</code> 注册表管理。模块的核心入口 <code>dispatch_tool_call</code> 接收工具名称与 JSON 参数，将其分派到对应的工具实现并返回结果；<code>build_tool_definitions</code> 用于构建所有工具的 <code>OpenAI</code> 兼容定义，供外部协议层使用。错误被统一表示为 <code>ToolError</code>，同时提供 <code>ToolContext</code>（包含项目根、输出目录、模型等上下文信息）和线程安全的 <code>ToolResultCache</code> 来优化重复查询。</p><p>在实现层面，模块大量借助 <code>extract</code>、<code>generate</code>、<code>support</code> 等底层模块完成符号提取、文档生成和文件读写，并依赖 <code>schema</code> 和 <code>protocol</code> 模块进行参数验证与响应格式化。内部通过匿名命名空间封装了辅助函数（如符号格式、文件名规范化、缓存管理）和工具参数类型（如 <code>NameArgs</code>、<code>SymbolQueryArgs</code>、<code>GuideArgs</code>），仅在模块间暴露 <code>dispatch_tool_call</code>、<code>build_tool_definitions</code> 和 <code>extract_string_arg</code> 三个公共函数，形成清晰的职责边界。</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../extract/"><code>extract</code></a></li><li><a href="./../generate/"><code>generate</code></a></li><li><a href="./../protocol/"><code>protocol</code></a></li><li><a href="./../schema/"><code>schema</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="dependency-diagram" tabindex="-1">Dependency Diagram <a class="header-anchor" href="#dependency-diagram" aria-label="Permalink to &quot;Dependency Diagram&quot;">​</a></h2>',7)),(o(),i(h,null,{default:t(()=>[s(c,{id:"mermaid-50",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20M0%5B%22agent%22%5D%0A%20%20%20%20I0%5B%22extract%22%5D%0A%20%20%20%20I0%20--%3E%20M0%0A%20%20%20%20I1%5B%22generate%22%5D%0A%20%20%20%20I1%20--%3E%20M0%0A%20%20%20%20I2%5B%22protocol%22%5D%0A%20%20%20%20I2%20--%3E%20M0%0A%20%20%20%20I3%5B%22schema%22%5D%0A%20%20%20%20I3%20--%3E%20M0%0A%20%20%20%20I4%5B%22support%22%5D%0A%20%20%20%20I4%20--%3E%20M0%0A"})]),fallback:t(()=>[...e[0]||(e[0]=[n(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=a('<h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-agent-toolerror" tabindex="-1"><code>clore::agent::ToolError</code> <a class="header-anchor" href="#clore-agent-toolerror" aria-label="Permalink to &quot;`clore::agent::ToolError`&quot;">​</a></h3><p>Declaration: <code>agent/tools.cppm:16</code></p><p>Definition: <code>agent/tools.cppm:16</code></p><p>Declaration: <a href="./../../namespaces/clore/agent/"><code>Namespace clore::agent</code></a></p><p>结构体 <code>clore::agent::ToolError</code> 的内部实现仅包含一个 <code>std::string</code> 类型的数据成员 <code>message</code>。该字段用于存储与工具执行错误相关的描述性文本，是整个类型的唯一状态来源。由于没有自定义的构造函数、析构函数或赋值运算符，编译器会生成默认的特殊成员函数，因此对象复制、移动和销毁均遵循 <code>std::string</code> 的对应规则。该类型不维护额外的不变量：<code>message</code> 可以为任意字符串（包括空字符串），调用者负责保证其内容在错误上下文中具有语义含义。</p><h2 id="variables" tabindex="-1">Variables <a class="header-anchor" href="#variables" aria-label="Permalink to &quot;Variables&quot;">​</a></h2><h3 id="arguments" tabindex="-1"><code>arguments</code> <a class="header-anchor" href="#arguments" aria-label="Permalink to &quot;`arguments`&quot;">​</a></h3><p>Declaration: <code>agent/tools.cppm:621</code></p><p>该变量提供了对 JSON 值的只读访问，常用于函数参数，以避免复制并确保数据不被修改。</p><h4 id="mutation" tabindex="-1">Mutation <a class="header-anchor" href="#mutation" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h3 id="context" tabindex="-1"><code>context</code> <a class="header-anchor" href="#context" aria-label="Permalink to &quot;`context`&quot;">​</a></h3><p>Declaration: <code>agent/tools.cppm:621</code></p><p>As a <code>const</code> reference, <code>context</code> provides read-only access to the underlying <code>ToolContext</code> object, likely used to query tool state or parameters without modifying it.</p><h4 id="mutation-1" tabindex="-1">Mutation <a class="header-anchor" href="#mutation-1" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-agent-build-tool-definitions" tabindex="-1"><code>clore::agent::build_tool_definitions</code> <a class="header-anchor" href="#clore-agent-build-tool-definitions" aria-label="Permalink to &quot;`clore::agent::build_tool_definitions`&quot;">​</a></h3><p>Declaration: <code>agent/tools.cppm:23</code></p><p>Definition: <code>agent/tools.cppm:887</code></p><p>Declaration: <a href="./../../namespaces/clore/agent/"><code>Namespace clore::agent</code></a></p><p>函数 <code>clore::agent::build_tool_definitions</code> 遍历由 <code>tool_registry()</code> 返回的静态工具规范数组，依次调用每个 <code>ToolSpec</code> 的 <code>build_definition</code> 成员函数。一旦某个工具的定义构建失败，函数立即返回 <code>std::unexpected</code>，携带相应的 <code>ToolError</code>；只有所有工具都成功生成 <code>clore::net::FunctionToolDefinition</code> 后，函数才返回包含完整定义向量的成功结果。该函数依赖 <code>tool_registry()</code> 作为唯一的外部数据源，其余逻辑完全由内部循环和错误传播构成。</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>tool_registry()</code></li><li><code>ToolSpec</code> objects</li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returned <code>std::vector&lt;FunctionToolDefinition&gt;</code></li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called to prepare tool definitions for a chat API request</li><li>used in agent initialization</li></ul><h3 id="clore-agent-dispatch-tool-call" tabindex="-1"><code>clore::agent::dispatch_tool_call</code> <a class="header-anchor" href="#clore-agent-dispatch-tool-call" aria-label="Permalink to &quot;`clore::agent::dispatch_tool_call`&quot;">​</a></h3><p>Declaration: <code>agent/tools.cppm:26</code></p><p>Definition: <code>agent/tools.cppm:902</code></p><p>Declaration: <a href="./../../namespaces/clore/agent/"><code>Namespace clore::agent</code></a></p><p>函数 <code>clore::agent::dispatch_tool_call</code> 首先将传入的 <code>arguments</code> JSON序列化为字符串，并以 <code>tool_name</code> 与序列化后的参数拼接生成 <code>cache_key</code>。通过 <code>tool_result_cache()</code> 获取全局的 <code>ToolResultCache&amp;</code>，在共享锁下查询缓存（<code>cache.result_by_key</code>）；若命中则直接返回缓存值。若未命中，则构造 <code>ToolContext</code>（携带 <code>model</code>、<code>project_root</code>、<code>output_root</code>），然后遍历 <code>tool_registry()</code> 返回的 <code>std::array&lt;ToolSpec, 12&gt;</code>，逐一比较 <code>tool.name</code> 与 <code>tool_name</code>。匹配成功后调用 <code>tool.dispatch</code> 执行对应的工具实现；若该 <code>tool.cacheable</code> 为 <code>true</code> 且调用成功，则在唯一锁下将结果存入缓存。若遍历结束未找到匹配工具，则返回包含未知工具名称的 <code>ToolError</code>。</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Modifies tool result cache</li><li>Acquires shared and unique locks on cache mutex</li></ul><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>tool_name</code></li><li><code>arguments</code></li><li><code>model</code></li><li><code>project_root</code></li><li><code>output_root</code></li><li><code>tool_registry()</code></li><li><code>tool_result_cache()</code></li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>tool_result_cache().result_by_key</code></li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to handle tool execution requests</li><li>Used by agent loop to dispatch tool calls</li></ul><h3 id="clore-agent-extract-string-arg" tabindex="-1"><code>clore::agent::extract_string_arg</code> <a class="header-anchor" href="#clore-agent-extract-string-arg" aria-label="Permalink to &quot;`clore::agent::extract_string_arg`&quot;">​</a></h3><p>Declaration: <code>agent/tools.cppm:20</code></p><p>Definition: <code>agent/tools.cppm:865</code></p><p>Declaration: <a href="./../../namespaces/clore/agent/"><code>Namespace clore::agent</code></a></p><p>This helper unbox a required string field from a JSON object argument. It first validates that the top‑level value is an object, returning a <code>ToolError</code> if not. It then schedules a linear scan of the object’s entries, comparing each entry key to the target <code>field_name</code>. On a match, it extracts a string via <code>entry.value.get_string()</code>—if that fails, a <code>ToolError</code> reports that the field is not a string. If the loop completes without a match, a <code>ToolError</code> indicates the field is missing.</p><p>The function relies on <code>json::Value</code>’s object inspection and iteration interface, <code>std::expected</code> for fallible return, and <code>std::format</code> for composing error messages. Its error‑first control flow ensures every exit path carries a descriptive <code>ToolError::message</code>.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>arguments</code> of type <code>const json::Value &amp;</code></li><li>parameter <code>field_name</code> of type <code>std::string_view</code></li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called by <code>dispatch_tool_call</code> to extract required string arguments from tool invocation JSON</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>模块 <code>agent:tools</code> 定义了一套可复用的工具集合，用于代码库浏览、符号查询和自动化文档生成。它通过内部注册表统一管理所有工具，每个工具由 <code>ToolSpec</code> 描述其名称、可缓存性、定义构建函数和调度函数。运行时上下文通过 <code>ToolContext</code> 提供项目根路径、输出根路径以及 LLM 模型引用。工具实现分散在 <code>ListFilesTool</code>、<code>SearchSymbolsTool</code>、<code>GetModuleTool</code> 等结构中，每个结构通过 <code>run</code> 方法执行具体逻辑，并依赖导入的 <code>extract</code>、<code>generate</code>、<code>protocol</code>、<code>schema</code>、<code>support</code> 等模块完成符号查询、协议解析和路径处理等底层操作。</p><p>从内部结构看，模块使用匿名命名空间将工具注册表（<code>tool_registry</code> 返回的 <code>std::array&lt;ToolSpec, 12&gt;</code>）、结果缓存（<code>ToolResultCache</code>，包含互斥锁和键值映射）以及大量辅助函数（如 <code>tool_list_modules</code>、<code>tool_get_dependencies</code>、<code>append_symbol_briefs</code> 等）封装为内部实现层。公共入口 <code>dispatch_tool_call</code> 根据工具名称查找注册表中的 <code>ToolSpec</code>，并调用其 <code>dispatch</code> 函数分发执行；<code>build_tool_definitions</code> 负责构建所有工具的定义集合。每个工具通过模板辅助函数 <code>dispatch_reflected_tool</code> 和 <code>make_tool_spec</code> 实现参数自动提取与结果格式化，保持了实现的一致性和可扩展性。同时，模块还封装了指南文件的读写（<code>write_guide</code>、<code>read_guide</code>），确保与 <code>generate</code> 模块的输出区域正确交互。</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../extract/">Module extract</a></li><li><a href="./../generate/">Module generate</a></li><li><a href="./../protocol/">Module protocol</a></li><li><a href="./../schema/">Module schema</a></li><li><a href="./../support/">Module support</a></li></ul>',61))])}const T=d(u,[["render",p]]);export{q as __pageData,T as default};
