import{_ as n,o as a,c as r,a3 as o}from"./chunks/framework.CRbjHCyB.js";const c=JSON.parse('{"title":"索引设计","description":"","frontmatter":{},"headers":[],"relativePath":"zh/clice/design/index-design.md","filePath":"zh/clice/design/index-design.md"}'),t={name:"zh/clice/design/index-design.md"};function l(i,e,s,d,p,g){return a(),r("div",null,[...e[0]||(e[0]=[o(`<h1 id="索引设计" tabindex="-1">索引设计 <a class="header-anchor" href="#索引设计" aria-label="Permalink to &quot;索引设计&quot;">​</a></h1><h2 id="背景" tabindex="-1">背景 <a class="header-anchor" href="#背景" aria-label="Permalink to &quot;背景&quot;">​</a></h2><p>语言服务器的跨文件功能——跳转到定义、查找引用、调用层次、符号搜索等——都依赖于符号索引。索引系统需要解决以下核心问题：</p><p><strong>大规模项目的性能</strong>：C++ 项目可能有上万个文件，每个文件编译后产生大量符号。索引系统必须在合理的时间和内存约束下完成索引构建、持久化和查询。</p><p><strong>增量更新</strong>：用户修改文件后，相关的索引应该能够增量更新，而不是重新索引整个项目。</p><p><strong>编译上下文感知</strong>：同一个头文件在不同的编译上下文下可能产生不同的符号。clangd 等现有方案只存储最后一次编译的结果，导致用户在切换上下文后看到过时的索引数据。</p><p><strong>打开文件的实时反馈</strong>：用户正在编辑的文件应该立即反映在查询结果中，而不是等待后台索引完成。</p><p>在 clangd 的 issue 中，以下问题反复出现：</p><ul><li>跳转到定义跳到了错误的位置（当多个编译单元定义了同名符号时）</li><li>后台索引速度慢，大型项目需要数十分钟才能完成首次索引</li><li>符号搜索结果不完整或包含过时条目</li><li>头文件中的模板代码在不同实例化上下文中有不同的引用关系</li></ul><h2 id="设计方案" tabindex="-1">设计方案 <a class="header-anchor" href="#设计方案" aria-label="Permalink to &quot;设计方案&quot;">​</a></h2><h3 id="三级索引层次" tabindex="-1">三级索引层次 <a class="header-anchor" href="#三级索引层次" aria-label="Permalink to &quot;三级索引层次&quot;">​</a></h3><p>clice 采用三级索引结构，每级服务于不同的目的：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TUIndex（编译产物）</span></span>
<span class="line"><span>    ↓ 合并</span></span>
<span class="line"><span>ProjectIndex（全局符号目录）+ MergedIndex（按文件分片的关系数据）</span></span>
<span class="line"><span>    ↑ 叠加</span></span>
<span class="line"><span>OpenFileIndex（打开文件的实时覆盖）</span></span></code></pre></div><h3 id="tuindex-单次编译产出" tabindex="-1">TUIndex：单次编译产出 <a class="header-anchor" href="#tuindex-单次编译产出" aria-label="Permalink to &quot;TUIndex：单次编译产出&quot;">​</a></h3><p>TUIndex 是一次编译产生的原始索引数据。当一个翻译单元被编译时，SemanticVisitor 遍历 AST，记录所有符号的出现和关系，产出一个 TUIndex。</p><p>TUIndex 包含：</p><ul><li><strong>按文件组织的索引数据</strong>：编译涉及的每个文件（主文件 + 所有包含的头文件）各有一份 FileIndex，记录该文件中的符号出现（Occurrence）和符号关系（Relation）</li><li><strong>符号表（SymbolTable）</strong>：将符号哈希映射到符号名称和种类</li><li><strong>include 图</strong>：记录文件间的 include 关系</li></ul><p>TUIndex 是临时数据——它在合并到 ProjectIndex 和 MergedIndex 后就不再需要。在后台索引场景下，TUIndex 在工作进程中生成并序列化，传输到主进程后合并。</p><h3 id="projectindex-全局符号目录" tabindex="-1">ProjectIndex：全局符号目录 <a class="header-anchor" href="#projectindex-全局符号目录" aria-label="Permalink to &quot;ProjectIndex：全局符号目录&quot;">​</a></h3><p>ProjectIndex 是全局的符号目录，汇聚所有已索引文件的符号信息。它的作用类似于搜索引擎的倒排索引——给定一个符号哈希，可以快速查到它的名称、种类、以及它出现在哪些文件中。</p><p>ProjectIndex 存储：</p><ul><li><strong>符号表</strong>：符号哈希 → 符号名称、符号种类、引用文件位图（Bitmap）</li><li><strong>路径池</strong>：文件路径的内部化映射</li></ul><p><strong>关键设计</strong>：ProjectIndex 不存储符号的具体位置信息（哪一行哪一列）。位置信息存储在 MergedIndex 的按文件分片中。ProjectIndex 的角色是&quot;目录&quot;——告诉你符号存在于哪些文件中，然后你去对应的 MergedIndex 分片中查找具体位置。</p><p>这种分离使得 ProjectIndex 保持紧凑。引用文件位图使用 Roaring Bitmap 压缩存储，内存效率很高。</p><h3 id="mergedindex-按文件分片的索引" tabindex="-1">MergedIndex：按文件分片的索引 <a class="header-anchor" href="#mergedindex-按文件分片的索引" aria-label="Permalink to &quot;MergedIndex：按文件分片的索引&quot;">​</a></h3><p>MergedIndex 是索引系统的核心存储层。它为项目中的每个文件维护一个分片，存储该文件中所有符号的具体出现位置和关系信息。</p><p>MergedIndex 的核心特性是<strong>编译上下文合并</strong>。同一个头文件可能被多个源文件包含，每次编译可能产生不同的符号关系（例如由于条件编译、模板实例化等）。MergedIndex 将这些来自不同编译上下文的索引数据合并存储在同一个分片中。</p><p>合并使用内容寻址的去重：对每次编译产出的 FileIndex 计算内容哈希，相同内容的不同编译上下文共享同一份数据。这避免了头文件被多次索引时的重复存储。</p><p>MergedIndex 分片同时存储文件内容，用于字节偏移到 LSP 位置（行号/列号）的转换。</p><h3 id="符号标识-symbolhash" tabindex="-1">符号标识：SymbolHash <a class="header-anchor" href="#符号标识-symbolhash" aria-label="Permalink to &quot;符号标识：SymbolHash&quot;">​</a></h3><p>系统中的每个符号通过 SymbolHash（<code>uint64_t</code>）标识。它基于 Clang 的 USR（Unified Symbol Resolution）生成——USR 是符号身份的规范字符串表示，编码了命名空间、类名、函数签名、模板参数等信息。</p><p>SymbolHash 的关键属性：</p><ul><li><strong>跨文件一致</strong>：同一个符号在不同文件中的 SymbolHash 相同，这是跨文件导航的基础</li><li><strong>紧凑高效</strong>：64 位整数比可变长度的 USR 字符串更适合作为 DenseMap 的键</li><li><strong>确定性</strong>：相同的声明总是产生相同的哈希值</li></ul><h3 id="符号关系模型" tabindex="-1">符号关系模型 <a class="header-anchor" href="#符号关系模型" aria-label="Permalink to &quot;符号关系模型&quot;">​</a></h3><p>索引的核心数据是符号之间的<strong>关系（Relation）</strong>。每条关系包含：</p><ul><li><strong>关系类型（RelationKind）</strong>：定义、声明、引用、弱引用、读、写、接口、实现、类型定义、基类、派生类、构造函数、析构函数、调用者、被调用者等</li><li><strong>位置</strong>：关系发生的源码范围</li><li><strong>目标符号</strong>：关系的另一端（用于类型关系，如继承和调用）</li></ul><p>与之对应的是<strong>出现（Occurrence）</strong>——记录符号在某个位置的简单出现，不带关系类型。</p><p>这两者的区分使得不同的查询可以高效完成：</p><ul><li><strong>跳转到定义</strong>：查找 RelationKind::Definition</li><li><strong>查找引用</strong>：查找 RelationKind::Reference</li><li><strong>调用层次</strong>：查找 RelationKind::Caller / Callee</li><li><strong>类型层次</strong>：查找 RelationKind::Base / Derived</li><li><strong>光标下的符号</strong>：查找 Occurrence</li></ul><h2 id="查询流程" tabindex="-1">查询流程 <a class="header-anchor" href="#查询流程" aria-label="Permalink to &quot;查询流程&quot;">​</a></h2><h3 id="跨文件查询" tabindex="-1">跨文件查询 <a class="header-anchor" href="#跨文件查询" aria-label="Permalink to &quot;跨文件查询&quot;">​</a></h3><p>以&quot;跳转到定义&quot;为例，查询流程为：</p><ol><li><strong>定位光标符号</strong>：在当前文件中，通过字节偏移查找光标位置的 Occurrence，获取 SymbolHash</li><li><strong>查询符号目录</strong>：在 ProjectIndex 中查找该 SymbolHash 引用的所有文件</li><li><strong>分文件查询关系</strong>：对于每个引用文件，判断它是否处于打开状态： <ul><li>如果该文件<strong>已打开</strong>（有 Session），使用其 OpenFileIndex 查找关系数据，<strong>跳过</strong>对应的 MergedIndex 分片</li><li>如果该文件<strong>未打开</strong>，使用 MergedIndex 分片查找关系数据</li></ul></li></ol><p>关键点：对于同一个文件，OpenFileIndex <strong>优先于</strong> MergedIndex——打开的文件优先查 OpenFileIndex，当 Session AST 为脏或不可用时回退到 MergedIndex。未打开的文件始终查 MergedIndex。这确保了打开文件的查询结果尽可能反映最新的缓冲区内容。</p><h3 id="打开文件的实时覆盖" tabindex="-1">打开文件的实时覆盖 <a class="header-anchor" href="#打开文件的实时覆盖" aria-label="Permalink to &quot;打开文件的实时覆盖&quot;">​</a></h3><p>每个打开的文件在 Session 中维护一个 OpenFileIndex，存储最近一次编译的索引数据。它包含与 MergedIndex 分片相同类型的数据（关系和出现），但来自内存中的 AST 编译结果而非后台索引。</p><p>设计原则：打开文件的索引不写入 Workspace（不影响全局状态），只有文件保存后才会通过后台索引更新 MergedIndex。这保证了全局索引的稳定性——不会因为用户正在输入一半的代码而污染其他文件的查询结果。</p><p>在查询时，如果某个引用文件处于打开状态，查询会优先使用该文件的 OpenFileIndex，当 AST 为脏或不可用时回退到 MergedIndex 分片。这确保了用户尽可能看到最新的缓冲区内容对应的结果。</p><h2 id="后台索引" tabindex="-1">后台索引 <a class="header-anchor" href="#后台索引" aria-label="Permalink to &quot;后台索引&quot;">​</a></h2><h3 id="调度策略" tabindex="-1">调度策略 <a class="header-anchor" href="#调度策略" aria-label="Permalink to &quot;调度策略&quot;">​</a></h3><p>后台索引由 Indexer 管理。它维护一个待索引文件队列，通过以下策略调度：</p><ul><li><strong>空闲超时批处理</strong>：文件加入队列后不会立即索引，而是设置一个空闲定时器。当定时器触发时，批量处理队列中的所有文件。这避免了频繁的小量索引。</li><li><strong>暂停/恢复</strong>：当用户发起交互式请求（hover、completion 等）时，后台索引可以暂停，优先处理用户请求。支持嵌套暂停语义。</li></ul><h3 id="索引合并" tabindex="-1">索引合并 <a class="header-anchor" href="#索引合并" aria-label="Permalink to &quot;索引合并&quot;">​</a></h3><p>后台索引完成后，TUIndex 的合并过程为：</p><ol><li>将 TUIndex 中的符号合并到 ProjectIndex 的全局符号表</li><li>将 TUIndex 中每个文件的 FileIndex 合并到对应的 MergedIndex 分片</li><li>更新引用文件位图</li></ol><p>合并是增量的——只处理新数据，不需要重建整个索引。</p><h2 id="序列化与持久化" tabindex="-1">序列化与持久化 <a class="header-anchor" href="#序列化与持久化" aria-label="Permalink to &quot;序列化与持久化&quot;">​</a></h2><p>索引使用 FlatBuffers 序列化，支持高效的磁盘持久化和惰性加载。FlatBuffers 的零拷贝特性使得索引可以通过内存映射直接访问，避免了完整的反序列化开销。</p><p>每个 MergedIndex 分片以独立文件存储在磁盘上，按需加载。ProjectIndex 作为全局数据整体序列化。</p><h2 id="设计决策与权衡" tabindex="-1">设计决策与权衡 <a class="header-anchor" href="#设计决策与权衡" aria-label="Permalink to &quot;设计决策与权衡&quot;">​</a></h2><p><strong>为什么是三级而不是两级？</strong> ProjectIndex 和 MergedIndex 的分离是关键。如果将位置信息也存入 ProjectIndex，它的体积会急剧膨胀，无法作为轻量的&quot;目录&quot;使用。而如果没有 ProjectIndex，每次跨文件查询都需要扫描所有 MergedIndex 分片来定位符号所在的文件，这在大型项目中不可接受。</p><p><strong>为什么打开文件的索引不写入全局状态？</strong> 这是两层状态模型（Workspace vs Session）的一部分。未保存的编辑可能是不完整的、有语法错误的代码。将其写入全局索引会导致其他文件的查询结果受到污染。只有保存到磁盘的稳定状态才应该影响全局索引。</p><p><strong>为什么使用内容寻址去重？</strong> 头文件经常被多个源文件包含，每次编译都产生相同的索引数据。内容寻址避免了存储 N 份相同的数据。当头文件内容变化时，旧的数据自然被新的替代。</p><h2 id="已知限制与改进方向" tabindex="-1">已知限制与改进方向 <a class="header-anchor" href="#已知限制与改进方向" aria-label="Permalink to &quot;已知限制与改进方向&quot;">​</a></h2><ul><li><strong>符号表的局部性</strong>：目前所有符号（包括函数内的局部变量等）都被合并到 ProjectIndex 的全局符号表中。这导致大量只在单个文件内部有意义的符号被全局存储，浪费内存和合并时间。改进方向是引入文件级的 SymbolTable——局部符号留在 TUIndex / MergedIndex 分片中，只有真正跨文件引用的符号才进入 ProjectIndex。</li><li><strong>模糊符号搜索</strong>：目前的符号搜索是简单的子串匹配。对于 agent 和 workspace symbol 等场景，需要更高效的模糊搜索索引（分词、前缀树或 n-gram 索引等）。</li></ul>`,65)])])}const x=n(t,[["render",l]]);export{c as __pageData,x as default};
