import{_ as a,o as c,c as t,a3 as o}from"./chunks/framework.aYYe0dfI.js";const u=JSON.parse('{"title":"Module extract:cache","description":"The extract:cache module is responsible for persisting and retrieving extraction results to enable incremental processing. It implements a cache subsystem that stores structured data for C++ modules (PCM and PCH entries), dependency snapshots, and build‑time signatures. The public interface provides functions to construct and decompose cache keys (build_cache_key, split_cache_key), compute compile‑command signatures (build_compile_signature), capture and compare dependency states (capture_dependency_snapshot, dependencies_changed), and load or save both simple integer entries (load_extract_cache, save_extract_cache) and full CliceCacheData structures (load_clice_cache, save_clice_cache). Additional utilities such as hash_file support content‑based change detection.","frontmatter":{"title":"Module extract:cache","description":"The extract:cache module is responsible for persisting and retrieving extraction results to enable incremental processing. It implements a cache subsystem that stores structured data for C++ modules (PCM and PCH entries), dependency snapshots, and build‑time signatures. The public interface provides functions to construct and decompose cache keys (build_cache_key, split_cache_key), compute compile‑command signatures (build_compile_signature), capture and compare dependency states (capture_dependency_snapshot, dependencies_changed), and load or save both simple integer entries (load_extract_cache, save_extract_cache) and full CliceCacheData structures (load_clice_cache, save_clice_cache). Additional utilities such as hash_file support content‑based change detection.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/extract/cache.md","filePath":"en/clore/modules/extract/cache.md"}'),r={name:"clore/modules/extract/cache.md"};function i(d,e,s,n,l,h){return c(),t("div",null,[...e[0]||(e[0]=[o('<h1 id="module-extract-cache" tabindex="-1">Module <code>extract:cache</code> <a class="header-anchor" href="#module-extract-cache" aria-label="Permalink to &quot;Module `extract:cache`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>extract:cache</code> module is responsible for persisting and retrieving extraction results to enable incremental processing. It implements a cache subsystem that stores structured data for C++ modules (PCM and PCH entries), dependency snapshots, and build‑time signatures. The public interface provides functions to construct and decompose cache keys (<code>build_cache_key</code>, <code>split_cache_key</code>), compute compile‑command signatures (<code>build_compile_signature</code>), capture and compare dependency states (<code>capture_dependency_snapshot</code>, <code>dependencies_changed</code>), and load or save both simple integer entries (<code>load_extract_cache</code>, <code>save_extract_cache</code>) and full <code>CliceCacheData</code> structures (<code>load_clice_cache</code>, <code>save_clice_cache</code>). Additional utilities such as <code>hash_file</code> support content‑based change detection.</p><p>The module owns a set of public types that form the cache contract, including <code>CacheKeyParts</code>, <code>CacheRecord</code>, <code>DependencySnapshot</code>, <code>CliceCacheDepEntry</code>, <code>CliceCachePCMEntry</code>, <code>CliceCachePCHEntry</code>, <code>CliceCacheData</code>, and <code>CacheError</code>. These types are designed for schema‑compatible serialization with the broader clice workspace caching system. Internal helpers in an anonymous namespace handle path normalization, serialization format versioning, and atomic file writes, while the public API exposes a coherent set of operations for building, storing, and verifying cached extraction data.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./ast"><code>extract:ast</code></a></li><li><a href="./compiler"><code>extract:compiler</code></a></li><li><a href="./scan"><code>extract:scan</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-extract-cache-cacheerror" tabindex="-1"><code>clore::extract::cache::CacheError</code> <a class="header-anchor" href="#clore-extract-cache-cacheerror" aria-label="Permalink to &quot;`clore::extract::cache::CacheError`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:20</code></p><p>Definition: <code>extract/cache.cppm:20</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The <code>clore::extract::cache::CacheError</code> struct is implemented as a simple wrapper around a single <code>std::string</code> member named <code>message</code>. This internal structure stores a human-readable description of the error condition. No additional invariants or constraints are imposed on the <code>message</code> value beyond those inherent to <code>std::string</code>. The compiler implicitly provides the default constructor, destructor, copy, and move operations, making the type trivially copyable and movable. The implementation deliberately keeps no further state, ensuring that error representation remains lightweight and directly accessible via the <code>message</code> field.</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>message</code> contains a human-readable error description</li><li>The struct is trivially constructible and copyable</li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>message</code></li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Returned or thrown to indicate an error during cache extraction</li><li>Used as an error type in fallible operations within the <code>clore::extract::cache</code> namespace</li></ul><h3 id="clore-extract-cache-cachekeyparts" tabindex="-1"><code>clore::extract::cache::CacheKeyParts</code> <a class="header-anchor" href="#clore-extract-cache-cachekeyparts" aria-label="Permalink to &quot;`clore::extract::cache::CacheKeyParts`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:24</code></p><p>Definition: <code>extract/cache.cppm:24</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The struct aggregates two members that together identify a unique cache entry: a <code>path</code> string representing the file or resource location, and a <code>compile_signature</code> that records a hash or versioning token from the compilation context. Both fields are set once during construction and remain unchanged for the lifetime of the object, forming an immutable key. No additional invariants are enforced by the struct itself; the caller is responsible for ensuring that the combination of <code>path</code> and <code>compile_signature</code> is unique within the containing cache.</p><h4 id="invariants-1" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-1" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>Both <code>path</code> and <code>compile_signature</code> must be populated before use as a cache key.</li><li>The <code>compile_signature</code> is expected to be derived deterministically from compilation parameters.</li></ul><h4 id="key-members-1" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-1" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>path</code> member</li><li><code>compile_signature</code> member</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as a key in associative containers (e.g., <code>std::map</code> or <code>std::unordered_map</code>) for caching extracted data.</li><li>Compared via default equality <code>operator</code>s for cache lookup.</li><li>Copied or moved when inserting or retrieving cache entries.</li></ul><h3 id="clore-extract-cache-cacherecord" tabindex="-1"><code>clore::extract::cache::CacheRecord</code> <a class="header-anchor" href="#clore-extract-cache-cacherecord" aria-label="Permalink to &quot;`clore::extract::cache::CacheRecord`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:36</code></p><p>Definition: <code>extract/cache.cppm:36</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The <code>clore::extract::cache::CacheRecord</code> struct bundles all data produced during extraction into a single record. Its fields are ordered such that the two identity hashes—<code>compile_signature</code> and <code>source_hash</code>—come first and are zero‑initialized by default, signalling a missing or invalid cache entry. The remaining fields, <code>ast_deps</code>, <code>scan</code>, and <code>ast</code>, hold the dependency snapshot, scan result, and abstract syntax tree result, respectively, and are only meaningful when the hashes match the source being loaded.</p><p>An invariant of the record is that the pair (<code>compile_signature</code>, <code>source_hash</code>) uniquely identifies the compilation context and input source; a cache hit requires both values to equal the caller’s computed hashes. The struct provides no custom constructors or assignment <code>operator</code>s, relying on default member initialization and aggregate initialization for simplicity. All fields are public, allowing direct access for efficient serialisation and comparison.</p><h4 id="invariants-2" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-2" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>All fields are public and default-initialized.</li><li><code>compile_signature</code> and <code>source_hash</code> are zero-initialized by default.</li><li>The types <code>DependencySnapshot</code>, <code>ScanResult</code>, and <code>ASTResult</code> are assumed to be default-constructible.</li></ul><h4 id="key-members-2" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-2" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>compile_signature</code></li><li><code>source_hash</code></li><li><code>ast_deps</code></li><li><code>scan</code></li><li><code>ast</code></li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as a record type for caching extracted data.</li><li>Instances are likely stored in a map or container keyed by source hash or compile signature.</li></ul><h3 id="clore-extract-cache-clicecachedata" tabindex="-1"><code>clore::extract::cache::CliceCacheData</code> <a class="header-anchor" href="#clore-extract-cache-clicecachedata" aria-label="Permalink to &quot;`clore::extract::cache::CliceCacheData`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:68</code></p><p>Definition: <code>extract/cache.cppm:68</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The implementation summary for <code>clore::extract::cache::CliceCacheData</code>: The struct aggregates three vectors that together represent the entire state of the clice cache. The <code>paths</code> field stores the list of source file paths that have been processed; the <code>pch</code> and <code>pcm</code> fields hold the corresponding cache entries for precompiled headers and precompiled modules, respectively. The vectors are expected to be populated in a coordinated manner, ensuring that indices across the three collections remain consistent relative to the same extraction session. No invariants are enforced beyond standard vector operations; the struct serves as a plain data container for serialization and runtime access.</p><h4 id="invariants-3" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-3" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>No explicit invariants are documented in the evidence.</li></ul><h4 id="key-members-3" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-3" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>paths</code></li><li><code>pch</code></li><li><code>pcm</code></li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to store and pass around cached extraction data.</li></ul><h3 id="clore-extract-cache-clicecachedepentry" tabindex="-1"><code>clore::extract::cache::CliceCacheDepEntry</code> <a class="header-anchor" href="#clore-extract-cache-clicecachedepentry" aria-label="Permalink to &quot;`clore::extract::cache::CliceCacheDepEntry`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:46</code></p><p>Definition: <code>extract/cache.cppm:46</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The struct <code>clore::extract::cache::CliceCacheDepEntry</code> holds a single dependency mapping within the clice workspace cache. Its two fields — <code>path</code> (a <code>std::uint32_t</code>) and <code>hash</code> (a <code>std::uint64_t</code>) — together form a compact, fixed‑size record that is deliberately kept schema‑compatible with the external <code>CacheData</code> structure in <code>clice/src/server/workspace.cpp</code>. The <code>path</code> field stores an identifier (likely an index into a path table or a directory‑relative token), while <code>hash</code> stores a checksum or content‑hash for the referenced file. Both fields are zero‑initialized by default, so an entry with <code>path == 0</code> and <code>hash == 0</code> represents an empty or invalid slot. The struct is trivially copyable and intended for use in contiguous storage (e.g., <code>std::vector&lt;CliceCacheDepEntry&gt;</code>), enabling efficient serialisation and on‑disk compatibility with the upstream workspace cache format. No extra invariants are enforced beyond the default‑constructed state and the schema‑matching layout.</p><h4 id="invariants-4" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-4" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>Fields are default-initialized to zero</li><li>Schema compatible with external <code>CacheData</code></li></ul><h4 id="key-members-4" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-4" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>path</code></li><li><code>hash</code></li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used in clice workspace cache structures</li><li>Schema-compatible with clice/server/workspace<code>.cpp</code> <code>CacheData</code></li></ul><h3 id="clore-extract-cache-clicecachepchentry" tabindex="-1"><code>clore::extract::cache::CliceCachePCHEntry</code> <a class="header-anchor" href="#clore-extract-cache-clicecachepchentry" aria-label="Permalink to &quot;`clore::extract::cache::CliceCachePCHEntry`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:51</code></p><p>Definition: <code>extract/cache.cppm:51</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The struct <code>clore::extract::cache::CliceCachePCHEntry</code> stores metadata for a cached precompiled header (PCH) file. Its fields include <code>filename</code> (the PCH path), <code>source_file</code> (a zero‑based file index), <code>hash</code> (a content hash for the PCH), <code>bound</code> (an unsigned counter or index), <code>build_at</code> (a timestamp), and <code>deps</code> (a vector of <code>CliceCacheDepEntry</code> objects listing dependencies). The invariant is that <code>hash</code> uniquely represents the PCH content, while <code>deps</code> captures all file dependencies that influenced its production. The combination of <code>source_file</code> and <code>filename</code> ties the entry to a specific source translation unit.</p><h4 id="invariants-5" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-5" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>hash</code> uniquely identifies the PCH content</li><li><code>source_file</code> references a valid source file index</li><li><code>deps</code> contains all source-level dependencies</li></ul><h4 id="key-members-5" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-5" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>filename</code></li><li><code>source_file</code></li><li><code>hash</code></li><li><code>bound</code></li><li><code>build_at</code></li><li><code>deps</code></li></ul><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>looked up by <code>hash</code> to find cached PCH</li><li>compared against current source file dependencies</li></ul><h3 id="clore-extract-cache-clicecachepcmentry" tabindex="-1"><code>clore::extract::cache::CliceCachePCMEntry</code> <a class="header-anchor" href="#clore-extract-cache-clicecachepcmentry" aria-label="Permalink to &quot;`clore::extract::cache::CliceCachePCMEntry`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:60</code></p><p>Definition: <code>extract/cache.cppm:60</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The struct <code>clore::extract::cache::CliceCachePCMEntry</code> aggregates the persistent metadata of a cached precompiled module (PCM) entry. Internally it holds a <code>filename</code> identifying the PCM file path, a <code>source_file</code> index (defaulting to <code>0</code>), the <code>module_name</code>, a <code>build_at</code> timestamp (defaulting to <code>0</code>), and a <code>deps</code> vector of <code>CliceCacheDepEntry</code> objects representing its dependencies. The numeric defaults (<code>0</code> for <code>source_file</code> and <code>build_at</code>) serve as sentinel values indicating an uninitialised or invalid state, while the <code>deps</code> vector remains empty until populated. The struct is a plain aggregate with no custom constructors, assignment <code>operator</code>s, or invariants enforced beyond the zero‑initialisation of its scalar fields; all member data is directly accessible.</p><h4 id="invariants-6" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-6" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>filename</code> and <code>module_name</code> together should uniquely identify a PCM entry.</li><li><code>source_file</code> is an index referencing an entry in an external source file table.</li><li><code>build_at</code> stores a timestamp in <code>int64_t</code> format, likely milliseconds or seconds since epoch.</li><li><code>deps</code> contains all direct dependencies of this PCM entry.</li></ul><h4 id="key-members-6" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-6" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>filename</code></li><li><code>module_name</code></li><li><code>build_at</code></li><li><code>source_file</code></li><li><code>deps</code></li></ul><h4 id="usage-patterns-6" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-6" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Stored in a cache container indexed by <code>filename</code> or <code>module_name</code>.</li><li>Used to compare timestamps and dependencies against the current build state.</li><li>Serialized and deserialized for persistent caching between builds.</li><li>Populated by the extraction phase and consulted during compilation to reuse <code>PCMs</code>.</li></ul><h3 id="clore-extract-cache-dependencysnapshot" tabindex="-1"><code>clore::extract::cache::DependencySnapshot</code> <a class="header-anchor" href="#clore-extract-cache-dependencysnapshot" aria-label="Permalink to &quot;`clore::extract::cache::DependencySnapshot`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:29</code></p><p>Definition: <code>extract/cache.cppm:29</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The <code>clore::extract::cache::DependencySnapshot</code> struct captures a consistent view of a dependency set at a specific build time. Internally, it stores three parallel vectors — <code>files</code>, <code>hashes</code>, and <code>mtimes</code> — each of which must contain the same number of elements, with the index into one vector corresponding to the same dependency file in the others. The <code>build_at</code> member records the timestamp (as <code>std::int64_t</code>) when the snapshot was created, defaulting to <code>0</code>. This structure guarantees that file paths, content hashes, and modification times are recorded atomically; the absence of any separate constructor or mutation method means the snapshot is effectively immutable after aggregate initialization, preserving the invariant that the three vectors remain aligned and represent a single coherent dependency state.</p><h4 id="invariants-7" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-7" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>The <code>files</code>, <code>hashes</code>, and <code>mtimes</code> vectors have the same size when representing a consistent snapshot.</li><li><code>build_at</code> is expected to be a monotonic timestamp indicating when the snapshot was captured.</li></ul><h4 id="key-members-7" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-7" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>files</code> - the list of dependency file paths.</li><li><code>hashes</code> - the hash values for each dependency file.</li><li><code>mtimes</code> - the last modification times for each dependency file.</li><li><code>build_at</code> - the timestamp of the snapshot creation.</li></ul><h4 id="usage-patterns-7" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-7" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to cache and compare the state of dependency files across builds.</li><li>Stored or serialized to detect changes in dependencies since the last build.</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-extract-cache-build-cache-key" tabindex="-1"><code>clore::extract::cache::build_cache_key</code> <a class="header-anchor" href="#clore-extract-cache-build-cache-key" aria-label="Permalink to &quot;`clore::extract::cache::build_cache_key`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:76</code></p><p>Definition: <code>extract/cache.cppm:228</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The function constructs a cache key by concatenating the normalized path and the compile signature, separated by a delimiter constant, <code>kCacheKeyDelimiter</code>. It first reserves sufficient memory to avoid reallocation, then appends the path, the delimiter, and the string representation of the signature. The resulting string serves as a unique identifier for the cache entry. The only external dependency is the delimiter constant, which is used to later split the key when parsing cache entries (e.g., in <code>split_cache_key</code>).</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Allocates and returns a new <code>std::string</code></li></ul><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>normalized_path</code> parameter</li><li><code>compile_signature</code> parameter</li><li><code>kCacheKeyDelimiter</code></li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Returned <code>std::string</code>&#39;s internal buffer</li></ul><h4 id="usage-patterns-8" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-8" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to generate a unique key for caching extraction results</li><li>Called when saving or loading cache entries</li></ul><h3 id="clore-extract-cache-build-compile-signature" tabindex="-1"><code>clore::extract::cache::build_compile_signature</code> <a class="header-anchor" href="#clore-extract-cache-build-compile-signature" aria-label="Permalink to &quot;`clore::extract::cache::build_compile_signature`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:74</code></p><p>Definition: <code>extract/cache.cppm:224</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The function <code>clore::extract::cache::build_compile_signature</code> acts as a thin delegation wrapper: it forwards the provided <code>CompileEntry</code> argument to the core function <code>clore::extract::build_compile_signature</code> and returns its <code>std::uint64_t</code> result directly. No additional computation, validation, or transformation is performed at this level. The internal control flow is therefore a single call, with the entire algorithmic responsibility residing in the referenced <code>clore::extract::build_compile_signature</code>. Dependencies are minimal—only the target function and the type <code>CompileEntry</code>—reflecting its role as a cache‑layer adapter that reuses an existing signature computation for cache‑key generation.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>entry (const <code>CompileEntry</code>&amp;)</li></ul><h4 id="usage-patterns-9" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-9" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Computes a signature for cache key generation</li><li>Called by <code>load_extract_cache</code> and <code>save_extract_cache</code></li></ul><h3 id="clore-extract-cache-capture-dependency-snapshot" tabindex="-1"><code>clore::extract::cache::capture_dependency_snapshot</code> <a class="header-anchor" href="#clore-extract-cache-capture-dependency-snapshot" aria-label="Permalink to &quot;`clore::extract::cache::capture_dependency_snapshot`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:83</code></p><p>Definition: <code>extract/cache.cppm:282</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The function <code>clore::extract::cache::capture_dependency_snapshot</code> begins by normalising the provided file list via <code>normalize_path_string</code>, sorting the result, and removing duplicates. It then initialises a <code>DependencySnapshot</code> with a current timestamp stored in <code>snapshot.build_at</code>. For parallel hashing, it computes a thread count based on <code>std::thread::hardware_concurrency</code> and partitions work into <code>per_thread</code> chunks, spawning <code>std::thread</code> workers. Each worker iterates over its assigned range, checking a mutex‑protected <code>first_error</code> at the start of every iteration to bail early on failure. For each file it uses <code>llvm::sys::fs::status</code> to obtain the last‑modification time and <code>hash_file</code> to compute a content hash. If <code>hash_file</code> fails (e.g., a generated header was cleaned between runs), the worker records a hash of <code>0</code> instead of aborting, so the cache entry will appear changed on the next check. After all threads join, the results—<code>files</code>, <code>hashes</code>, and <code>mtimes</code>—are collected into the snapshot from the per‑index <code>task_results</code> vector. A single <code>CacheError</code> (if any) is propagated via <code>std::unexpected</code>.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Reads file system to compute file hashes and modification times</li></ul><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>Input parameter <code>files</code> (vector of file paths)</li><li>File system via <code>hash_file</code> and <code>llvm::sys::fs::status</code></li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Local variables: <code>snapshot</code>, <code>task_results</code>, <code>normalized</code>, <code>first_error</code></li><li>Memory allocation for <code>DependencySnapshot</code> and internal containers</li></ul><h4 id="usage-patterns-10" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-10" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to capture the current state of dependency files before caching</li><li>Used to compare against previously stored snapshots to detect changes</li></ul><h3 id="clore-extract-cache-dependencies-changed" tabindex="-1"><code>clore::extract::cache::dependencies_changed</code> <a class="header-anchor" href="#clore-extract-cache-dependencies-changed" aria-label="Permalink to &quot;`clore::extract::cache::dependencies_changed`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:86</code></p><p>Definition: <code>extract/cache.cppm:401</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The function begins with guard checks: if <code>build_at</code> is non‑positive or <code>files</code> is empty, or if the sizes of <code>files</code>, <code>hashes</code>, and <code>mtimes</code> are inconsistent, it returns <code>true</code> (i.e., dependencies have changed). For small dependency sets (up to <code>kParallelThreshold</code> of 16), it iterates sequentially over each index, calling <code>check_single_dependency</code>; on the first <code>true</code> result it returns immediately. For larger sets, it spawns a number of threads equal to the hardware concurrency (defaulting to 1), each processing a contiguous chunk of indices. An <code>std::atomic&lt;bool&gt;</code> flag <code>changed</code> allows early exit: each worker checks <code>changed.load</code> with <code>memory_order_relaxed</code> before calling <code>check_single_dependency</code> and sets the flag if a change is detected. After joining all threads, the function returns the final value of <code>changed</code>. The only internal dependency is the helper <code>check_single_dependency</code>, which evaluates whether a single dependency file has changed relative to the stored snapshot data.</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>snapshot.build_at</code></li><li><code>snapshot.files</code></li><li><code>snapshot.hashes</code></li><li><code>snapshot.mtimes</code></li><li><code>std::thread::hardware_concurrency()</code></li></ul><h4 id="usage-patterns-11" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-11" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used to decide whether to reuse cached compilation results</li><li>called after loading a dependency snapshot to detect changes</li></ul><h3 id="clore-extract-cache-hash-file" tabindex="-1"><code>clore::extract::cache::hash_file</code> <a class="header-anchor" href="#clore-extract-cache-hash-file" aria-label="Permalink to &quot;`clore::extract::cache::hash_file`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:81</code></p><p>Definition: <code>extract/cache.cppm:270</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The implementation of <code>clore::extract::cache::hash_file</code> reads the entire file at the given path into a memory buffer via <code>llvm::MemoryBuffer::getFile</code>. If the file cannot be read (e.g., due to permission errors or missing file), it returns a <code>std::unexpected</code> containing a <code>CacheError</code> with a descriptive message generated using <code>std::format</code>. On success, the function applies the <code>xxHash</code> 64-bit algorithm (<code>llvm::xxh3_64bits</code>) to the raw buffer contents and returns the resulting hash value wrapped in a <code>std::expected</code>. The function depends on the LLVM support library for file I/O and hashing, and on the <code>CacheError</code> type defined within the <code>clore::extract::cache</code> namespace for error reporting.</p><h4 id="side-effects-4" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-4" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Reads file content from the filesystem via <code>llvm::MemoryBuffer::getFile</code>.</li></ul><h4 id="reads-from-4" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-4" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>path</code></li><li>file at <code>path</code></li></ul><h4 id="usage-patterns-12" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-12" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to compute hash of source files for cache key generation</li><li>Likely called by cache build or verification logic</li></ul><h3 id="clore-extract-cache-load-clice-cache" tabindex="-1"><code>clore::extract::cache::load_clice_cache</code> <a class="header-anchor" href="#clore-extract-cache-load-clice-cache" aria-label="Permalink to &quot;`clore::extract::cache::load_clice_cache`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:95</code></p><p>Definition: <code>extract/cache.cppm:670</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The function first calls <code>clice_cache_file_path</code> to obtain the expected cache file path for the given <code>workspace_root</code>. If this fails, it returns the error immediately. Otherwise, it checks whether the file exists via <code>fs::exists</code>; if the file is absent (or an <code>std::error_code</code> signals an error), it returns an empty <code>CliceCacheData</code> – meaning no prior cache is available. When the file does exist, it reads the entire contents with <code>clore::support::read_utf8_text_file</code>; a read failure yields a <code>CacheError</code> describing the issue. The JSON content is then deserialised into <code>CliceCacheData</code> using <code>json::from_json</code>. If deserialisation fails, the function logs a warning via <code>logging::warn</code> and returns an empty <code>CliceCacheData</code> (treating the cache as stale). On success, the populated <code>CliceCacheData</code> is returned.</p><p>Key dependencies are <code>clice_cache_file_path</code> for path resolution, <code>read_utf8_text_file</code> for I/O, and <code>json::from_json</code> for structured deserialisation. The function deliberately absorbs parse failures into a silent empty result rather than propagating errors, on the assumption that a corrupted cache should be discarded without blocking the caller.</p><h4 id="side-effects-5" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-5" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>filesystem existence check</li><li>file I/O reading the clice cache file</li><li>logging a warning on JSON parse failure</li></ul><h4 id="reads-from-5" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-5" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the clice cache file at the path derived from <code>workspace_root</code></li></ul><h4 id="usage-patterns-13" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-13" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>loading cached clice data for incremental compilation</li><li>checking cache validity before extraction</li></ul><h3 id="clore-extract-cache-load-extract-cache" tabindex="-1"><code>clore::extract::cache::load_extract_cache</code> <a class="header-anchor" href="#clore-extract-cache-load-extract-cache" aria-label="Permalink to &quot;`clore::extract::cache::load_extract_cache`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:88</code></p><p>Definition: <code>extract/cache.cppm:457</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The function <code>clore::extract::cache::load_extract_cache</code> computes the on‑disk cache file path via <code>cache_file_path</code> and checks for its existence. If the file is missing or inaccessible, it returns an empty map or a <code>CacheError</code>. Otherwise, it reads the file as UTF‑8 text using <code>clore::support::read_utf8_text_file</code>, then deserialises the JSON content into a <code>SerializedCacheData</code> struct via <code>json::from_json</code>. A format‑version mismatch against <code>kExtractCacheFormatVersion</code> (or a parse failure) causes a warning and an empty map to be returned, effectively treating the cache as stale.</p><p>After validation, the function iterates over <code>data.entries</code>. For each entry it validates the <code>source_file</code> index against <code>data.paths</code>, decodes the dependency snapshot using <code>decode_dependency_snapshot</code>, normalises the source path via <code>normalize_path_string</code>, and builds a cache key with <code>build_cache_key</code>. It then populates the result map with a <code>CacheRecord</code> holding the <code>compile_signature</code>, <code>source_hash</code>, decoded <code>ast_deps</code>, <code>scan</code>, and <code>ast</code> from the entry. The function returns the assembled map of cache keys to records.</p><h4 id="side-effects-6" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-6" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>reads a file from disk</li><li>logs warnings via <code>logging::warn</code></li></ul><h4 id="reads-from-6" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-6" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>workspace_root</code> parameter</li><li>cache file on disk (path derived from <code>workspace_root</code>)</li><li>global constant <code>kExtractCacheFormatVersion</code></li><li>contents of the cache file (JSON, paths, entries)</li></ul><h4 id="usage-patterns-14" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-14" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during initialization to restore previously saved extract cache</li><li>typically used in conjunction with <code>save_extract_cache</code> and <code>dependencies_changed</code></li></ul><h3 id="clore-extract-cache-save-clice-cache" tabindex="-1"><code>clore::extract::cache::save_clice_cache</code> <a class="header-anchor" href="#clore-extract-cache-save-clice-cache" aria-label="Permalink to &quot;`clore::extract::cache::save_clice_cache`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:97</code></p><p>Definition: <code>extract/cache.cppm:710</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The implementation of <code>clore::extract::cache::save_clice_cache</code> first computes the target cache file path via <code>clore::extract::cache::(anonymous namespace)::clice_cache_file_path</code> and ensures its parent directory exists. It serializes the provided <code>CliceCacheData</code> struct to a JSON string using <code>json::to_json</code>; any serialization failure is immediately returned as a <code>CacheError</code>.</p><p>To achieve atomic file replacement, the function constructs a unique temporary file path by combining the final cache path with a suffix built from the process ID, a nanosecond‑precision timestamp, and a nonce derived via <code>llvm::xxh3_64bits</code>. It retries up to 32 times to avoid collisions, checking each candidate’s existence with <code>fs::exists</code>. Once a fresh temporary path is obtained, the serialized content is written using <code>clore::support::write_utf8_text_file</code>. Finally, the temporary file is renamed to the final cache path via <code>fs::rename</code>. If the rename fails with <code>permission_denied</code>, <code>file_exists</code>, or <code>operation_not_permitted</code>, the function attempts to remove the existing cache file and retries the rename; any remaining error is propagated. On success, an empty <code>std::expected</code> is returned.</p><h4 id="side-effects-7" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-7" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>creates directories in the file system</li><li>writes a UTF-8 text file to a temporary path</li><li>renames a temporary file to the final cache path</li><li>may remove an existing cache file before rename</li></ul><h4 id="reads-from-7" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-7" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>workspace_root</code> parameter</li><li><code>data</code> parameter</li><li>system clock for timestamp</li><li>process ID via <code>llvm::sys::Process::getProcessId</code></li><li>xxhash for nonce generation</li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>clice cache directory and file beneath <code>workspace_root</code></li><li>temporary files in the same directory</li></ul><h4 id="usage-patterns-15" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-15" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to persist extracted clice data to disk</li><li>Used after successful compilation or extraction phases</li></ul><h3 id="clore-extract-cache-save-extract-cache" tabindex="-1"><code>clore::extract::cache::save_extract_cache</code> <a class="header-anchor" href="#clore-extract-cache-save-extract-cache" aria-label="Permalink to &quot;`clore::extract::cache::save_extract_cache`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:91</code></p><p>Definition: <code>extract/cache.cppm:533</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The function first resolves the cache directory via <code>cache_directory(workspace_root)</code> and ensures it exists using <code>fs::create_directories</code>. It then constructs a <code>SerializedCacheData</code> object: it interns each path from the record map with <code>normalize_path_string</code> and assigns a unique index, validates that each record’s <code>compile_signature</code> matches the signature extracted by <code>split_cache_key</code> (returning an error on mismatch), and encodes the dependency snapshot via <code>encode_dependency_snapshot</code>. The resulting <code>SerializedCacheEntry</code> objects are serialized to JSON with <code>json::to_json</code>. A unique temporary file path is generated by iterating up to 32 candidate names composed from the cache path, process ID, timestamp, and an xxhash3 nonce; the first candidate that does not already exist is chosen. The serialized content is written to this temp file using <code>clore::support::write_utf8_text_file</code>, then atomically renamed to the target <code>cache.json</code>. If the rename fails due to permission or existing file errors, the target file is removed and the rename retried; any other rename failure returns an error. On success, the function returns an empty expected.</p><h4 id="side-effects-8" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-8" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Creates cache directory if missing</li><li>Writes temporary file to filesystem</li><li>Renames temporary file to final cache path (atomic on same filesystem)</li><li>Removes existing cache file if rename fails due to permission</li><li>May update directory metadata</li></ul><h4 id="reads-from-8" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-8" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>Parameter <code>workspace_root</code></li><li>Parameter <code>records</code> (the <code>unordered_map</code> of <code>CacheRecord</code>)</li><li>Helper function <code>split_cache_key</code> returns internal key parts</li><li>Helper function <code>json::to_json</code> processes the data structure</li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Filesystem cache directory (created by <code>create_directories</code>)</li><li>Temporary file near the cache path</li><li>Final cache file <code>cache.json</code> in the cache directory</li><li>Local variables: <code>path_ids</code> map and <code>data</code> structure (internal)</li></ul><h4 id="usage-patterns-16" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-16" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to persist the extract cache after building or updating cache records</li><li>Used in the extract pipeline to save results for future reuse</li></ul><h3 id="clore-extract-cache-split-cache-key" tabindex="-1"><code>clore::extract::cache::split_cache_key</code> <a class="header-anchor" href="#clore-extract-cache-split-cache-key" aria-label="Permalink to &quot;`clore::extract::cache::split_cache_key`&quot;">​</a></h3><p>Declaration: <code>extract/cache.cppm:79</code></p><p>Definition: <code>extract/cache.cppm:238</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/cache/"><code>Namespace clore::extract::cache</code></a></p><p>The function first locates the separator between the path and the compile signature by calling <code>cache_key.rfind(kCacheKeyDelimiter)</code>. If the delimiter is not found, or if either the left-hand (path) or right-hand (signature) substring is empty, it returns a <code>CacheError</code> with a descriptive message. Otherwise, it parses the signature portion using <code>std::from_chars</code> with a <code>std::uint64_t</code> target. Parsing must consume the entire substring and succeed with no error; otherwise an error is returned. On success, the function returns a <code>CacheKeyParts</code> containing the path string and the parsed signature.</p><h4 id="side-effects-9" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-9" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-9" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-9" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>cache_key</code></li><li>constant <code>kCacheKeyDelimiter</code></li></ul><h4 id="writes-to-4" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-4" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returned <code>std::expected&lt;CacheKeyParts, CacheError&gt;</code> value</li></ul><h4 id="usage-patterns-17" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-17" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used in cache load/save operations to decompose a combined cache key</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>extract:cache</code> module provides a persistent caching subsystem for extraction results, reducing redundant work by storing computed dependency snapshots, compile signatures, AST data, and clice workspace records. It is decomposed into a public API surface—<code>load_extract_cache</code>, <code>save_extract_cache</code>, <code>load_clice_cache</code>, <code>save_clice_cache</code>, <code>capture_dependency_snapshot</code>, <code>dependencies_changed</code>, and related key‑building functions—and an internal implementation that handles serialization, path normalization, and file I/O. Public types such as <code>CacheError</code>, <code>CacheKeyParts</code>, <code>DependencySnapshot</code>, and <code>CliceCacheData</code> define the data contracts, while anonymous‑namespace helpers like <code>SerializedCacheEntry</code>, <code>SerializedDependencySnapshot</code>, <code>CachedPathHash</code>, and functions (<code>normalize_path_string</code>, <code>clice_cache_file_path</code>, <code>encode_dependency_snapshot</code>, <code>decode_dependency_snapshot</code>) implement the low‑level serialization and disk access logic.</p><p>The module imports <code>extract:ast</code>, <code>extract:compiler</code>, and <code>extract:scan</code> for AST extraction, compilation entry handling, and source scanning respectively, together with the <code>support</code> module for foundational utilities (e.g., file I/O, path normalization, hashing). Internally, the cache is organized around workspace‑compatible structures (<code>CliceCacheDepEntry</code>, <code>CliceCachePCMEntry</code>, <code>CliceCachePCHEntry</code>) that mirror the schema defined in the larger clice workspace cache (<code>clice/src/server/workspace.cpp</code>). The implementation layers separate serialization concerns from cache‑lookup logic, using a parallel worker pool (with tunable thresholds) to hash dependencies concurrently, and retry mechanisms for atomic temporary‑file writes to ensure data integrity.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./ast">Module extract:ast</a></li><li><a href="./compiler">Module extract:compiler</a></li><li><a href="./scan">Module extract:scan</a></li><li><a href="./../support/">Module support</a></li></ul>',225)])])}const m=a(r,[["render",i]]);export{u as __pageData,m as default};
