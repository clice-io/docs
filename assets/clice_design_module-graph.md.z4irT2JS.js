import{_ as a,o as n,c as t,a3 as i}from"./chunks/framework.CRbjHCyB.js";const u=JSON.parse('{"title":"Module Compilation","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/module-graph.md","filePath":"en/clice/design/module-graph.md"}'),s={name:"clice/design/module-graph.md"};function o(r,e,l,d,c,p){return n(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="module-compilation" tabindex="-1">Module Compilation <a class="header-anchor" href="#module-compilation" aria-label="Permalink to &quot;Module Compilation&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>C++20 introduced modules, changing the independent compilation model that C++ has used since its inception. In traditional C++, each source file compiles independently into an object file, sharing declarations between files via headers. Modules break this independence:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// math.cppm — module interface unit</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> math;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b) { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b; }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// main.cpp — imports the module</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">import math;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); }</span></span></code></pre></div><p>Before compiling <code>main.cpp</code>, the module interface unit <code>math.cppm</code> must be compiled first, producing a precompiled module file (PCM). If a project has multi-level module dependencies — A imports B, B imports C — the compilation order must be C → B → A. This forms a directed acyclic graph (DAG), where nodes are module files and edges are <code>import</code> relationships.</p><p>Build systems (CMake, Ninja, etc.) are naturally suited for this kind of DAG scheduling: scan all files, build a complete dependency graph, compile in topological order. But a language server faces different challenges:</p><p><strong>Real-time requirements.</strong> After a user opens a file that imports modules, they expect editing feedback within hundreds of milliseconds. Waiting for the entire module graph to compile is unacceptable. A language server needs a lazy, on-demand compilation strategy — compile only the modules the current file actually needs.</p><p><strong>Cascading file changes.</strong> When the user modifies a module interface file and saves, all PCMs of modules that directly or indirectly depend on it become stale. The language server must detect this, cancel in-progress compilations, mark affected modules as dirty, and recompile them when next needed.</p><p><strong>Concurrency and cancellation.</strong> Multiple files may simultaneously need the same module&#39;s PCM. The language server must avoid duplicate compilations and let later requests wait for earlier ones to finish. At the same time, when the user closes the file that triggered a compilation and no longer needs a module, the in-progress compilation should be cancellable to free resources.</p><p><strong>Temporary cyclic dependencies.</strong> Although C++ modules do not allow circular <code>import</code>s, users may temporarily introduce cycles during editing. The language server must detect and report errors rather than deadlocking.</p><p>In clangd, C++20 module support has long been in an experimental stage, lacking dedicated design. <a href="https://github.com/clangd/clangd/issues/1293" target="_blank" rel="noreferrer">clangd/clangd#1293</a> is the clangd team&#39;s summary of the module support problem — concluding that &quot;these are not just bugs that can be fixed, a design and new infrastructure is needed,&quot; and at the time &quot;nobody has plans/availability to work on this soon.&quot; Years later, related issues continue to appear: <a href="https://github.com/clangd/clangd/issues/2569" target="_blank" rel="noreferrer">clangd/clangd#2569</a> lists critical missing features in modules (rename, find references, etc.); <a href="https://github.com/clangd/clangd/issues/2292" target="_blank" rel="noreferrer">clangd/clangd#2292</a> and <a href="https://github.com/clangd/clangd/issues/2497" target="_blank" rel="noreferrer">clangd/clangd#2497</a> report file-locking conflicts caused by clangd sharing PCM files with the build system — clangd holds the PCM open, preventing the build system from overwriting it.</p><p>The common root cause of these problems is the lack of a module compilation scheduling system designed specifically for language server use cases. clice addresses this through CompileGraph — an interest-counted compilation DAG with lazy construction, on-demand compilation, real-time cancellation, and dependency cascading.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><p>CompileGraph is a compilation dependency graph built at runtime. Each node (CompileUnit) represents a module file, and edges represent <code>import</code> dependency relationships.</p><h3 id="lazy-construction" tabindex="-1">Lazy Construction <a class="header-anchor" href="#lazy-construction" aria-label="Permalink to &quot;Lazy Construction&quot;">​</a></h3><p>Unlike build systems that scan all files and build a complete DAG before compilation, CompileGraph is lazily constructed — a node&#39;s dependencies are resolved and added to the graph only when first touched by a compilation request. When the user opens a file, only the module chain actually needed by that file is scanned and compiled, not the entire project&#39;s module graph.</p><p>CompileGraph has two compilation entry points:</p><ul><li><strong>Compile module</strong>: Compile the specified module and all its transitive dependencies, producing a PCM file. Used for module interface units themselves.</li><li><strong>Compile dependencies</strong>: Compile all module dependencies of the specified file, but not the file itself. Used for ordinary source files — they are not part of the module DAG but may use modules via <code>import</code>.</li></ul><h3 id="interest-counting" tabindex="-1">Interest Counting <a class="header-anchor" href="#interest-counting" aria-label="Permalink to &quot;Interest Counting&quot;">​</a></h3><p>Interest counting is CompileGraph&#39;s core scheduling mechanism, tracking &quot;how many active requests currently care about a given compilation unit.&quot; It answers two questions: should compilation be started? Can compilation be cancelled?</p><p>When a file needs a module, that module&#39;s reference count is incremented (acquire); when a request finishes or is cancelled, the count is decremented (release). A reference count reaching zero means no active request currently cares about this module.</p><p>Reference counts are managed through RAII guards: creating a guard automatically acquires, and its destructor automatically releases. This naturally aligns with coroutine cancellation semantics — cancellation destroys the coroutine frame, the destructor releases the reference count, with no extra cleanup code needed.</p><p>Interest counting operates at two levels: request-level and compilation-task-level. Request-level references mean &quot;this request is waiting for a module&#39;s PCM&quot;; compilation-task-level references mean &quot;this module&#39;s compilation task is waiting for its direct dependencies to complete.&quot; The latter ensures a module is not prematurely cancelled when another module&#39;s compilation task depends on it, even if the original request has been cancelled.</p><h3 id="compilation-rounds" tabindex="-1">Compilation Rounds <a class="header-anchor" href="#compilation-rounds" aria-label="Permalink to &quot;Compilation Rounds&quot;">​</a></h3><p>Each compilation attempt constitutes a compilation round. Waiters learn of completion through a completion event and decide their next action based on the result. A round has three possible outcomes:</p><ul><li><strong>Success</strong>: PCM produced successfully, dirty flag cleared</li><li><strong>Failed</strong>: Compilation error (dependency failure, cycle detection, etc.), dirty flag retained</li><li><strong>Stale</strong>: Compilation cancelled (file modified during compilation, or reference count reached zero), waiters automatically drive a new round</li></ul><p>Failure is not sticky — retaining the dirty flag means that after the user fixes an error, the next request naturally triggers a retry without requiring a server restart.</p><h3 id="dirty-state-and-generation-counter" tabindex="-1">Dirty State and Generation Counter <a class="header-anchor" href="#dirty-state-and-generation-counter" aria-label="Permalink to &quot;Dirty State and Generation Counter&quot;">​</a></h3><p>CompileUnit uses a dirty flag to indicate that compilation is needed. The generation counter is a monotonically increasing value, incremented on each file update, used to detect asynchronous races: the compilation task records the current generation at start and compares upon completion — a mismatch means the file was modified during compilation, and the result is stale.</p><h3 id="dependencies" tabindex="-1">Dependencies <a class="header-anchor" href="#dependencies" aria-label="Permalink to &quot;Dependencies&quot;">​</a></h3><p>Each CompileUnit maintains forward dependencies (modules it imports) and reverse dependencies (modules that import it). Forward dependencies are obtained via lazy resolution; reverse dependencies are back-filled at resolution time. Reverse dependencies are the basis for cascading notifications on file changes — starting from the modified module, following reverse edges finds all affected modules.</p><p>The module name to file mapping is maintained by DependencyGraph (see <a href="./dependency-scanning">Dependency Scanning</a>) — the startup fast scan discovers all module declarations and builds a module name → file path registry. CompileGraph uses this registry to resolve <code>import</code> statements to concrete file paths.</p><h2 id="implementation" tabindex="-1">Implementation <a class="header-anchor" href="#implementation" aria-label="Permalink to &quot;Implementation&quot;">​</a></h2><h3 id="compilation-flow" tabindex="-1">Compilation Flow <a class="header-anchor" href="#compilation-flow" aria-label="Permalink to &quot;Compilation Flow&quot;">​</a></h3><p>The complete flow of a module compilation request:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Request enters</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ├─ RAII guard acquires on target module</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ├─ Target not dirty? ──→ Return immediately (PCM available)</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ├─ No compilation in progress? ──→ Start compilation task</span></span>
<span class="line"><span>  │                                    │</span></span>
<span class="line"><span>  │                                    ├─ Lazily resolve dependencies (scan import declarations)</span></span>
<span class="line"><span>  │                                    ├─ Check for self-cycle</span></span>
<span class="line"><span>  │                                    ├─ Acquire on direct dependencies</span></span>
<span class="line"><span>  │                                    ├─ Wait for all deps to compile (parallel)</span></span>
<span class="line"><span>  │                                    ├─ Dispatch to worker process (produce PCM)</span></span>
<span class="line"><span>  │                                    └─ Check generation counter ──→ Mismatch = Stale</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ├─ Wait for compilation round to complete</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  └─ Based on result: Success → return / Failed → error / Stale → retry</span></span></code></pre></div><p>Lazy resolution uses the Clang preprocessor for precise scanning, which differs from the fast lexer-based scan used during startup <a href="./dependency-scanning">dependency scanning</a>. The fast scan does not expand macros or evaluate conditionals, suitable for building a global overview of include relationships; precise scanning expands all preprocessor directives to obtain the file&#39;s actual module dependencies under its current compile command. Resolution results are cached and reused by subsequent compilations until reset by a file update.</p><blockquote><p>Module implementation units (those with <code>module X;</code> but no <code>export</code>) implicitly depend on their corresponding module interface unit. Precise scanning detects this and automatically adds the dependency.</p></blockquote><h3 id="deferred-zero-interest-cancellation" tabindex="-1">Deferred Zero-Interest Cancellation <a class="header-anchor" href="#deferred-zero-interest-cancellation" aria-label="Permalink to &quot;Deferred Zero-Interest Cancellation&quot;">​</a></h3><p>When the reference count drops to zero, compilation is not cancelled immediately. Instead, a check is deferred by one event loop tick — if the reference count is still zero at that point, cancellation proceeds.</p><p>This handles the scenario where a compilation request is superseded. When the user edits during compilation, a new request replaces the old one: the old request releases its references (count momentarily drops to zero), then the new request establishes references within the same tick (count rises back). Immediate cancellation would unnecessarily terminate shared dependency compilations. The one-tick delay allows such reference handoffs to complete smoothly, avoiding disruption to in-progress shared compilations.</p><h3 id="cascading-updates" tabindex="-1">Cascading Updates <a class="header-anchor" href="#cascading-updates" aria-label="Permalink to &quot;Cascading Updates&quot;">​</a></h3><p>When a module file is saved (didSave), CompileGraph performs a cascading update:</p><ol><li>Reset the modified file&#39;s resolved flag — the next compilation will rescan dependencies, since the file may have added or removed <code>import</code> statements</li><li>Clear old forward dependency edges</li><li>Mark as dirty, increment the generation counter</li><li>Traverse all transitive dependents along reverse dependency edges; for each affected module: cancel its compilation round, mark dirty, increment generation</li><li>Return the list of all files marked dirty, so the caller can clear corresponding PCM caches</li></ol><p>Cascading updates do not modify reference counts — existing waiters retain their references. Upon observing a Stale result, they automatically drive a new compilation round.</p><h3 id="cycle-detection" tabindex="-1">Cycle Detection <a class="header-anchor" href="#cycle-detection" aria-label="Permalink to &quot;Cycle Detection&quot;">​</a></h3><p>Before waiting for a dependency&#39;s compilation to complete, CompileGraph checks for wait cycles: starting from the target node, it searches along the dependency chain, following only nodes currently being compiled, checking whether the chain leads back to the current waiter. If a cycle is detected, it returns failure immediately, avoiding deadlock.</p><h3 id="raii-guards-and-structured-concurrency" tabindex="-1">RAII Guards and Structured Concurrency <a class="header-anchor" href="#raii-guards-and-structured-concurrency" aria-label="Permalink to &quot;RAII Guards and Structured Concurrency&quot;">​</a></h3><p>Coroutine cancellation in kotatsu means destroying the coroutine frame — code after suspension points never executes; only destructors of already-constructed objects are guaranteed to run. CompileGraph leverages this by placing all cleanup logic in two layers of RAII guards:</p><ul><li><strong>RefGuard</strong> (request level): Holds root references from a request to modules. Releases reference counts on destruction when the request completes or is cancelled.</li><li><strong>UnitGuard</strong> (compilation round level): Manages all state for one compilation round. On destruction: publishes the outcome, clears the compiling flag, releases all acquired dependency references, and fires the completion event to notify waiters.</li></ul><p>All compilation tasks are managed through kota::task_group, providing structured concurrency guarantees: shutdown cancels all tasks first, then waits for their frames to unwind, ensuring no dangling compilation tasks remain.</p><h3 id="pcm-caching" tabindex="-1">PCM Caching <a class="header-anchor" href="#pcm-caching" aria-label="Permalink to &quot;PCM Caching&quot;">​</a></h3><p>PCM files use content-addressed path naming — the filename is determined by the module name and a hash of the compilation arguments, stored in a dedicated cache directory. This is fully isolated from build system artifacts, avoiding file-locking conflicts.</p><p>PCM cache uses two-layer staleness detection: first comparing dependency files&#39; modification times (mtime), then re-hashing content when times have changed. Recompilation only occurs when dependency content has actually changed, avoiding unnecessary rebuilds caused by &quot;touch without modification.&quot; Cache metadata is persisted to <code>cache.json</code> on disk and can be restored on server restart.</p><h3 id="integration-with-the-compilation-pipeline" tabindex="-1">Integration with the Compilation Pipeline <a class="header-anchor" href="#integration-with-the-compilation-pipeline" aria-label="Permalink to &quot;Integration with the Compilation Pipeline&quot;">​</a></h3><p>CompileGraph is initialized during server startup. Before every file compilation, Compiler uses CompileGraph to ensure all module dependencies are ready — this is the first step of compilation preparation, executed before PCH construction.</p><p>For <code>import</code> statements that the user has added in the editor but not yet saved (which CompileGraph is unaware of), Compiler performs an additional buffer scan to identify new module dependencies and attempts to build the corresponding PCMs. This is a compensation mechanism that covers the experience when the user is editing but has not saved.</p><h2 id="faq" tabindex="-1">FAQ <a class="header-anchor" href="#faq" aria-label="Permalink to &quot;FAQ&quot;">​</a></h2><ul><li><p><strong>Why interest counting instead of a task queue?</strong> A task queue cannot express &quot;no one needs this compilation anymore.&quot; When the user closes the file that triggered a compilation, continuing wastes resources. Interest counting precisely tracks demand, making cancellation decisions grounded — not based on timeouts or heuristics, but on whether any request is still waiting for the result.</p></li><li><p><strong>Why defer by one tick instead of cancelling immediately?</strong> In an event loop model, multiple steps of an operation complete within the same tick. When a compilation request is superseded, the old reference is released and the new one established shortly after, with a momentary zero-reference in between. Immediate cancellation would unnecessarily terminate shared dependencies — deferring by one tick ensures reference handoffs within the same tick don&#39;t trigger cancellation.</p></li><li><p><strong>Why a generation counter instead of locks?</strong> The master process is a single-threaded event loop with no data races. The &quot;races&quot; to detect come from asynchronous timing — &quot;was the file updated during compilation?&quot; The generation counter answers this with minimal overhead, without introducing locks.</p></li><li><p><strong>Why lazy dependency resolution?</strong> Dependency resolution requires running the Clang preprocessor (precise scanning) on each module file, which is not cheap. If all module dependencies were resolved at startup, it would add to startup time. Lazy resolution ensures only modules actually needed for compilation are scanned — opening a file doesn&#39;t trigger scanning the entire module graph.</p></li><li><p><strong>Why are failures not sticky?</strong> Users are actively editing code — syntax errors are the norm. If failures were marked as persistent, users couldn&#39;t get correct results after fixing errors without restarting the server. Retaining the dirty flag lets the next request naturally trigger a retry.</p></li><li><p><strong>Why store PCMs in a separate cache directory?</strong> clangd shares PCM files with the build system, which causes file-locking conflicts — the language server holds the PCM open, preventing the build system from overwriting it (see <a href="https://github.com/clangd/clangd/issues/2292" target="_blank" rel="noreferrer">clangd/clangd#2292</a>). clice uses content-addressed isolated caching, avoiding this problem. The trade-off is additional disk space usage and extra time for first-time compilation.</p></li></ul><h2 id="known-limitations" tabindex="-1">Known Limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known Limitations&quot;">​</a></h2><ul><li><p><strong>Compilation task memory accumulation.</strong> Each compilation round creates a task in the task_group. Completed task frames are only reclaimed when the task_group is destroyed, not immediately upon completion. In a long-running server, completed task frames accumulate over time.</p></li><li><p><strong>Dependency resolution determinism.</strong> A time window exists between resolving dependencies and the actual compilation. If a file is modified during this window, the resolved dependencies may not match the actual dependencies at compilation time. The generation counter detects this and triggers retries, but at the cost of extra compilation overhead.</p></li><li><p><strong>Limited coverage for unsaved module dependencies.</strong> CompileGraph builds dependency relationships from disk files. When the user adds an <code>import</code> in the editor without saving, Compiler compensates via buffer scanning, but if the imported module itself is not yet managed by CompileGraph (e.g., a new module file that hasn&#39;t been saved), the corresponding PCM cannot be built. The user needs to save the module interface file first, then save the importing file.</p></li></ul>`,61)])])}const m=a(s,[["render",o]]);export{u as __pageData,m as default};
