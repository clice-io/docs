import{_ as a,o,c as r,a3 as n}from"./chunks/framework.CRbjHCyB.js";const h=JSON.parse('{"title":"Source Code Overview","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/overview.md","filePath":"en/clice/design/overview.md"}'),i={name:"clice/design/overview.md"};function t(s,e,l,c,d,p){return o(),r("div",null,[...e[0]||(e[0]=[n('<h1 id="source-code-overview" tabindex="-1">Source Code Overview <a class="header-anchor" href="#source-code-overview" aria-label="Permalink to &quot;Source Code Overview&quot;">​</a></h1><p>This document describes the responsibilities and role of each module in the clice source tree, providing readers with a high-level understanding of the overall project structure. For detailed design information on each module, refer to the corresponding topic documents.</p><h2 id="project-vision" tabindex="-1">Project Vision <a class="header-anchor" href="#project-vision" aria-label="Permalink to &quot;Project Vision&quot;">​</a></h2><p>clice is a ground-up redesign of a C++ language server, architected to solve fundamental problems that have long plagued existing C++ language servers. Core innovations include:</p><ul><li><p><strong>Compilation Context</strong>: The same file can produce different results under different compilation contexts. clice treats this concept as a first-class citizen throughout the entire design -- from compilation to indexing to query responses. No existing language server (not just C++) formally addresses this.</p></li><li><p><strong>Multi-process Architecture</strong>: A master + worker process model isolates Clang&#39;s memory leaks and crashes while enabling priority-aware scheduling and real-time memory monitoring.</p></li><li><p><strong>Coroutine-based Async Model</strong>: Built on C++20 coroutines and the kotatsu library, replacing traditional callback-style async and making business logic clearer.</p></li><li><p><strong>Real-time Module Compilation System</strong>: A reference-counted C++20 module compilation DAG with support for real-time cancellation and dependency cascading -- the most advanced real-time module compilation scheduling approach known to date.</p></li></ul><p>clice&#39;s long-term goal extends beyond being a language server. It will integrate concurrent scheduling for tools like clang-tidy, enable cross-translation-unit optimizations (such as duplicate header detection), and become a unified, high-level platform for the C++ tooling ecosystem.</p><h2 id="foundation-layer" tabindex="-1">Foundation Layer <a class="header-anchor" href="#foundation-layer" aria-label="Permalink to &quot;Foundation Layer&quot;">​</a></h2><h3 id="src-support" tabindex="-1"><code>src/support/</code> <a class="header-anchor" href="#src-support" aria-label="Permalink to &quot;`src/support/`&quot;">​</a></h3><p>General-purpose utility library. Provides logging, filesystem abstractions, string operations, pattern matching, and other infrastructure.</p><p>Key components:</p><ul><li><strong>PathPool</strong>: Internalizes file paths as compact <code>uint32_t</code> identifiers, used as stable file identifiers throughout the system. This is globally shared infrastructure that nearly every module dealing with file paths depends on.</li><li><strong>Logging</strong>: Structured logging based on spdlog.</li><li><strong>FuzzyMatcher</strong>: Token-aware fuzzy matching for scenarios like code completion.</li><li><strong>Markup / Doxygen</strong>: Parsing and formatting of documentation comments.</li><li><strong>StringSet / ObjectSet</strong>: Deduplicating internalization of strings and objects, with pointer stability guaranteed by a bump allocator.</li></ul><h3 id="src-command" tabindex="-1"><code>src/command/</code> <a class="header-anchor" href="#src-command" aria-label="Permalink to &quot;`src/command/`&quot;">​</a></h3><p>CLI parsing and compilation command processing. Commands read from the compilation database (CDB) are raw commands generated by the build system and cannot be fed directly to the Clang frontend -- they may contain options meant only for code generation, lack system header search paths, or include parameters irrelevant to a language server. This module classifies, filters, probes toolchains, and deduplicates the raw commands, ultimately transforming them into compilation parameters consumable by the language server.</p><p>Key components:</p><ul><li><strong>CompilationDatabase</strong>: Loads <code>compile_commands.json</code> and performs a two-level separation of compilation commands -- splitting flags that affect semantics (canonical) from flags that only affect user content (patch, such as <code>-I</code> and <code>-D</code>), enabling cross-file command deduplication and sharing.</li><li><strong>Toolchain</strong>: Queries system compilers for complete compilation parameters (such as system header search paths), caching results by (driver, file extension, non-user-content flags) to avoid redundant probing of the same toolchain.</li><li><strong>ArgumentParser</strong>: Classifies Clang compilation options into three categories -- codegen-only (discarded), discarded, and user-content (extracted as patches) -- ensuring only semantically meaningful parameters are retained.</li><li><strong>SearchConfig</strong>: A four-tier model for header search paths (Quoted / Angled / System / After), matching Clang&#39;s internal search logic.</li></ul><h2 id="compilation-abstraction-layer" tabindex="-1">Compilation Abstraction Layer <a class="header-anchor" href="#compilation-abstraction-layer" aria-label="Permalink to &quot;Compilation Abstraction Layer&quot;">​</a></h2><h3 id="src-compile" tabindex="-1"><code>src/compile/</code> <a class="header-anchor" href="#src-compile" aria-label="Permalink to &quot;`src/compile/`&quot;">​</a></h3><p>Wraps the Clang compiler. Abstracts raw Clang APIs into safe, unified compilation interfaces that hide low-level details.</p><p>This layer is purely a compilation abstraction with no server logic. It takes compilation parameters, drives Clang through compilation, and produces results -- including AST, preprocessor state, source location mappings, and more.</p><p>Key components:</p><ul><li><strong>CompilationUnit / CompilationUnitRef</strong>: RAII wrappers around the Clang AST context. <code>CompilationUnitRef</code> provides a unified read-only view for accessing source location mappings, preprocessor directives, file contents, AST nodes, and more. This is the primary input for <code>src/feature/</code> and <code>src/semantic/</code>.</li><li><strong>CompilationParams</strong>: Describes the complete configuration for a single compilation operation, including compilation type (Preprocess / Content / Preamble / ModuleInterface / Completion / Indexing), file remapping, PCH/PCM reuse, cancellation flags, etc.</li><li><strong>Compilation Types</strong>: Different compilation types produce different outputs. Preamble produces PCH, ModuleInterface produces PCM, Content produces a full AST, and Completion produces completion candidates. They share the same parameter framework but have different semantics.</li></ul><h3 id="src-syntax" tabindex="-1"><code>src/syntax/</code> <a class="header-anchor" href="#src-syntax" aria-label="Permalink to &quot;`src/syntax/`&quot;">​</a></h3><p>Lightweight syntax processing that does not require a full AST.</p><p>This layer handles tasks that can be completed without full compilation: lexical analysis, dependency scanning, include path resolution, and more. It runs before compilation to quickly obtain structural information and dependency relationships for files.</p><p>Key components:</p><ul><li><strong>Lexer</strong>: A token-level utility built on Clang&#39;s raw lexer, providing a streaming interface. Does not run the preprocessor or expand macros. Used for directive scanning, include path resolution, and similar tasks.</li><li><strong>DependencyGraph</strong>: A global include/module dependency graph. Records the include relationships for each file (under each SearchConfig), supporting forward queries (what does this file include?), reverse queries (what includes this file?), and module-name-to-file mappings. Also provides BFS upward search for host source files, shortest include chain lookup, and other navigation features.</li><li><strong>Scan</strong>: Wraps Clang&#39;s <code>DependencyDirectivesScanner</code> to quickly extract include and module dependencies from files without full preprocessing.</li><li><strong>IncludeResolver</strong>: Resolves include paths to actual files based on search path configuration.</li><li><strong>Completion</strong>: Generates candidates for include path completion and module import completion.</li></ul><h2 id="semantic-analysis-layer" tabindex="-1">Semantic Analysis Layer <a class="header-anchor" href="#semantic-analysis-layer" aria-label="Permalink to &quot;Semantic Analysis Layer&quot;">​</a></h2><h3 id="src-semantic" tabindex="-1"><code>src/semantic/</code> <a class="header-anchor" href="#src-semantic" aria-label="Permalink to &quot;`src/semantic/`&quot;">​</a></h3><p>Semantic analysis capabilities beyond Clang&#39;s native APIs.</p><p>This layer takes a <code>CompilationUnitRef</code> and extracts higher-level semantic information -- symbol relations, symbol classification, template resolution, and more. Its output is the core data source for the indexing system and LSP features.</p><p>Key components:</p><ul><li><strong>SemanticVisitor</strong>: An AST traverser that records each symbol&#39;s occurrence location and relations (definition, reference, read, write, inheritance, call, etc.). This is the primary producer of index data.</li><li><strong>TemplateResolver</strong>: Resolves dependent names through pseudo-instantiation, enabling semantic analysis to see through template contexts. This is a key innovation of clice -- clangd has limited capability in this area.</li><li><strong>SymbolKind / RelationKind</strong>: Fine-grained symbol kinds and relation types, far more detailed than those defined by the LSP protocol.</li><li><strong>find_target</strong>: Resolves AST nodes to their declaration targets, handling various indirect references and implicit conversions.</li></ul><h2 id="index-layer" tabindex="-1">Index Layer <a class="header-anchor" href="#index-layer" aria-label="Permalink to &quot;Index Layer&quot;">​</a></h2><h3 id="src-index" tabindex="-1"><code>src/index/</code> <a class="header-anchor" href="#src-index" aria-label="Permalink to &quot;`src/index/`&quot;">​</a></h3><p>Symbol indexing system with cross-translation-unit query support.</p><p>The indexing system uses a three-tier structure, corresponding to different query scenarios and lifecycles:</p><ul><li><strong>TUIndex</strong>: Index data produced from a single compilation. Records symbol occurrences, relations, and include graphs for each file involved in the compilation. This is the raw index data source, generated by <code>SemanticVisitor</code> during compilation.</li><li><strong>ProjectIndex</strong>: The global symbol table. Aggregates symbol information from all indexed files, supporting lookup by symbol hash for names, kinds, and referencing files. Used for cross-file symbol search and navigation.</li><li><strong>MergedIndex</strong>: Per-file sharded index storage. Merges index data produced from the same file under different compilation contexts. MergedIndex is the core embodiment of compilation-context awareness -- a header file may be included by multiple source files, each inclusion producing different symbol relations, and MergedIndex unifies them.</li></ul><p>Index data is serialized with FlatBuffers, supporting on-disk persistence and lazy loading.</p><h2 id="feature-layer" tabindex="-1">Feature Layer <a class="header-anchor" href="#feature-layer" aria-label="Permalink to &quot;Feature Layer&quot;">​</a></h2><h3 id="src-feature" tabindex="-1"><code>src/feature/</code> <a class="header-anchor" href="#src-feature" aria-label="Permalink to &quot;`src/feature/`&quot;">​</a></h3><p>Concrete implementations of LSP features. Each feature takes a <code>CompilationUnitRef</code> (or <code>CompilationParams</code>) and returns the corresponding LSP response data.</p><p>This layer is purely computational -- it has no involvement with network communication, state management, or process scheduling. It only concerns itself with &quot;given a compilation result, how to produce the correct LSP response.&quot; Features include:</p><ul><li>Code completion, hover information, signature help</li><li>Semantic highlighting, inlay hints</li><li>Document symbols, document links</li><li>Folding ranges, formatting, diagnostics</li></ul><p>Each feature is implemented as a standalone function following a uniform pattern. When adding a new LSP feature, follow the existing implementations.</p><blockquote><p><strong>Note</strong>: <code>feature/</code> only covers single-file, AST-based feature implementations. Cross-file language features (such as go to definition, find references, call hierarchy, etc.) are handled by the Indexer in <code>src/server/compiler/</code> using index data. Additionally, some features involve multi-phase processing pipelines and do not necessarily reach the AST layer -- for example, code completion can sometimes be resolved at the syntax layer (include path completion, module import completion), and document links may be extractable during the preprocessing phase. The implementations in <code>feature/</code> correspond only to the phase that requires full compilation results.</p></blockquote><h2 id="server-layer" tabindex="-1">Server Layer <a class="header-anchor" href="#server-layer" aria-label="Permalink to &quot;Server Layer&quot;">​</a></h2><h3 id="src-server" tabindex="-1"><code>src/server/</code> <a class="header-anchor" href="#src-server" aria-label="Permalink to &quot;`src/server/`&quot;">​</a></h3><p>The language server&#39;s core runtime. This is the most complex layer, responsible for assembling all the layers above into a runnable service.</p><h4 id="src-server-protocol" tabindex="-1"><code>src/server/protocol/</code> <a class="header-anchor" href="#src-server-protocol" aria-label="Permalink to &quot;`src/server/protocol/`&quot;">​</a></h4><p>Protocol definitions. Describes the message formats for communication between the master process and worker processes, as well as between the server and clients.</p><ul><li><strong>Worker Protocol</strong>: Defines request/response messages for stateful and stateless worker processes -- compilation requests, query requests, build requests, document update notifications, etc. Uses bincode serialization.</li><li><strong>Extension Protocol</strong>: Extension requests beyond the LSP standard, such as compilation context query and switching.</li><li><strong>Agentic Protocol</strong>: High-level query interface for AI agents, providing compilation command queries, project file listings, file dependency analysis, impact analysis, symbol search, symbol detail retrieval, and more. These interfaces are accessible directly via the command line without needing an intermediary like MCP.</li></ul><h4 id="src-server-workspace" tabindex="-1"><code>src/server/workspace/</code> <a class="header-anchor" href="#src-server-workspace" aria-label="Permalink to &quot;`src/server/workspace/`&quot;">​</a></h4><p>Project-level global state -- the single source of truth from disk.</p><ul><li><strong>Workspace</strong>: Holds the compilation database, toolchain, path pool, dependency graph, PCH/PCM cache, project index, and all other project-level state. Core invariant: unsaved buffer contents of open files never modify the Workspace. Workspace state changes come from three paths: initial load, cascading updates triggered by file saves (didSave), and index merges after background indexing completes.</li><li><strong>Config</strong>: Loading and validation of TOML/JSON configuration files.</li></ul><h4 id="src-server-compiler" tabindex="-1"><code>src/server/compiler/</code> <a class="header-anchor" href="#src-server-compiler" aria-label="Permalink to &quot;`src/server/compiler/`&quot;">​</a></h4><p>Compilation scheduling and index management. This layer bridges <code>src/compile/</code> (low-level compilation abstraction) and <code>src/server/service/</code> (service layer).</p><ul><li><strong>Compiler</strong>: The scheduler for compilation lifecycles. Coordinates the ordering of PCH builds, module dependency resolution, and AST compilation, dispatching compilation tasks to worker processes. It holds no persistent data -- all data resides in Workspace and Session; the Compiler is solely responsible for orchestrating execution flow.</li><li><strong>CompileGraph</strong>: The DAG scheduler for C++20 module compilation. Uses reference counting for interest tracking -- when no requestor cares about a module unit, its compilation is automatically cancelled. Supports dependency cascade cancellation and recompilation triggered by file updates.</li><li><strong>Indexer</strong>: Handles background indexing scheduling and serves as the entry point for cross-file queries. Manages the indexing queue, deduplication, and idle-timeout batch processing. During queries, it combines three data sources: ProjectIndex serves as a directory (locating which files contain a symbol), MergedIndex provides per-file sharded relation data, and in-memory indexes for open files provide real-time coverage of unsaved state.</li></ul><h4 id="src-server-service" tabindex="-1"><code>src/server/service/</code> <a class="header-anchor" href="#src-server-service" aria-label="Permalink to &quot;`src/server/service/`&quot;">​</a></h4><p>Service entry point and session management.</p><ul><li><strong>MasterServer</strong>: The top-level coordinator. Holds Workspace, Session map, WorkerPool, Compiler, and Indexer, routing LSP requests to the appropriate handling logic. Manages the server lifecycle (Uninitialized -&gt; Initialized -&gt; Ready -&gt; ShuttingDown -&gt; Exited) and starts file-watching tasks.</li><li><strong>Session</strong>: The editing state for each open file. Holds the current buffer contents, compilation version number, ABA-protected generation counter, in-memory symbol index, PCH reference, dependency snapshot, and more. Sessions are created on didOpen and destroyed on didClose. Session modifications do not affect other files -- all cross-file dependencies point to on-disk files.</li><li><strong>LSPClient</strong>: The LSP protocol request handler, registering handlers for each LSP method.</li><li><strong>AgentClient</strong>: The Agentic protocol request handler.</li></ul><h4 id="src-server-worker" tabindex="-1"><code>src/server/worker/</code> <a class="header-anchor" href="#src-server-worker" aria-label="Permalink to &quot;`src/server/worker/`&quot;">​</a></h4><p>Worker process management.</p><ul><li><strong>WorkerPool</strong>: Manages worker process lifecycles, routing, and scheduling. <ul><li><strong>StatefulWorker</strong>: Holds AST in memory and serves query requests (hover, semantic tokens, etc.). Each open file is affinity-bound to a stateful worker by path_id; new files are assigned to the worker with the lightest current load.</li><li><strong>StatelessWorker</strong>: Executes one-shot tasks (PCH/PCM builds, completion, indexing, etc.). Uses priority-aware scheduling -- interactive requests (completion, signature help) take priority over background tasks (indexing). Concurrency is dynamically adjusted based on memory pressure and worker crash rates.</li></ul></li><li><strong>Process Fault Tolerance</strong>: Workers are automatically restarted on crash (with a maximum restart limit); when a stateful worker crashes, its owned documents are automatically reassigned.</li></ul><h2 id="inter-module-relationships" tabindex="-1">Inter-module Relationships <a class="header-anchor" href="#inter-module-relationships" aria-label="Permalink to &quot;Inter-module Relationships&quot;">​</a></h2><p>The data flow roughly follows this direction:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>command (compilation command parsing)</span></span>\n<span class="line"><span>    |</span></span>\n<span class="line"><span>compile (drives Clang compilation)</span></span>\n<span class="line"><span>    |</span></span>\n<span class="line"><span>semantic (extracts semantic information) --&gt; index (builds indexes)</span></span>\n<span class="line"><span>    |</span></span>\n<span class="line"><span>feature (produces LSP responses)</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>The server layer assembles all of the above via Workspace/Session/WorkerPool into a runnable service</span></span></code></pre></div><p><code>support</code> and <code>syntax</code> are cross-cutting layers shared by multiple modules. <code>server/protocol</code> defines the message contracts between processes and between server and client.</p>',67)])])}const m=a(i,[["render",t]]);export{h as __pageData,m as default};
