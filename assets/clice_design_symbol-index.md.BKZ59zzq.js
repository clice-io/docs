import{_ as i,o as t,c as n,a3 as s}from"./chunks/framework.CRbjHCyB.js";const u=JSON.parse('{"title":"Symbol Index","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/symbol-index.md","filePath":"en/clice/design/symbol-index.md"}'),a={name:"clice/design/symbol-index.md"};function o(r,e,d,l,c,h){return t(),n("div",null,[...e[0]||(e[0]=[s(`<h1 id="symbol-index" tabindex="-1">Symbol Index <a class="header-anchor" href="#symbol-index" aria-label="Permalink to &quot;Symbol Index&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>Many features of a language server need to work across files. A user triggers &quot;go to definition&quot; in one file, and the target may be in any other file in the project; &quot;find references&quot; needs to scan every file in the project that might mention the symbol; call hierarchy and type hierarchy involve chains of symbol relationships across multiple files. To support these features, the language server must maintain a project-wide symbol index that records which files each symbol appears in and the semantic relationships between them (definition, reference, call, inheritance, etc.).</p><p>C++ makes index construction and maintenance particularly difficult.</p><p>The first level of difficulty is the compilation-context problem with header files. C++&#39;s <code>#include</code> is textual substitution — the contents of a header file are inserted verbatim into the source file that includes it at compile time. This means the same header file can produce entirely different symbols under different compilation contexts:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// crypto.h</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#ifdef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> USE_OPENSSL</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    using</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TLSContext</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OpenSSLContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#else</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    using</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TLSContext</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BoringSSLContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span></code></pre></div><p>When <code>crypto.h</code> is included by a source file that defines <code>USE_OPENSSL</code>, <code>TLSContext</code> is <code>OpenSSLContext</code>; when included by another source file, it is <code>BoringSSLContext</code>. Conditional compilation is the most obvious example, but include order and template instantiation can also cause header files to produce different symbol relationships under different contexts. If the index records only one context&#39;s results, users will see incorrect jump targets or incomplete reference lists after switching contexts.</p><p>The second level is scale. A mid-sized C++ project (a few thousand source files) produces hundreds of thousands of symbols after compilation; large projects (LLVM, Chromium) reach millions. The index system must maintain reasonable memory usage, build time, and query latency at this scale.</p><p>clangd&#39;s handling of both levels is insufficient. On the compilation-context front, clangd&#39;s background index stores only the last compilation result for each header — whichever source file is compiled last overwrites the previous index data. If a symbol reference exists only under a particular compilation context that is not the last one indexed, that reference is lost.</p><p>On the cross-file lookup front, clangd&#39;s background index stores symbol information in the index shard corresponding to the file where the symbol is declared. This declaration-file-centric storage model prevents reference counts from accumulating correctly across files (clangd <a href="https://github.com/clangd/clangd/issues/23" target="_blank" rel="noreferrer">#23</a>). Users also frequently encounter incomplete &quot;find references&quot; results — certain references only appear after manually opening the relevant files, at which point the dynamic index fills in the missing data (clangd <a href="https://github.com/clangd/clangd/issues/516" target="_blank" rel="noreferrer">#516</a>, <a href="https://github.com/clangd/clangd/issues/802" target="_blank" rel="noreferrer">#802</a>). Additionally, when compilation commands change, clangd&#39;s staleness detection does not trigger re-indexing (clangd <a href="https://github.com/clangd/clangd/issues/199" target="_blank" rel="noreferrer">#199</a>), leaving the index data out of sync with the actual compilation state for extended periods.</p><p>clice&#39;s index system is redesigned to address these problems: it adopts a three-level index structure separating the global symbol directory from per-file sharded relation data, uses content-addressed deduplication to merge indexes from different compilation contexts, leverages FlatBuffers for on-demand lazy loading to control memory usage, and provides a real-time overlay for open files to ensure query result freshness during editing.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="symbol-identity" tabindex="-1">Symbol Identity <a class="header-anchor" href="#symbol-identity" aria-label="Permalink to &quot;Symbol Identity&quot;">​</a></h3><p>The index system needs a way to identify the same symbol across files and translation units. clice uses <code>SymbolHash</code> (a 64-bit integer) as the unique identifier for each symbol.</p><p><code>SymbolHash</code> is generated from Clang&#39;s USR (Unified Symbol Resolution). USR is a canonical string representation of symbol identity that encodes the symbol&#39;s fully qualified name: namespace, class name, function signature, template parameters, etc. For example, <code>std::vector&lt;int&gt;::push_back</code> and <code>std::vector&lt;double&gt;::push_back</code> produce different USRs. <code>SymbolHash</code> is the hash of the USR string.</p><p><code>SymbolHash</code> has two key properties. First, cross-file consistency: the same symbol always has the same <code>SymbolHash</code> regardless of which file it appears in. <code>std::string</code> seen in file A and <code>std::string</code> seen in file B have the same hash, allowing all definitions and references to be associated through it — this is the foundation of cross-file navigation. Second, compactness: a 64-bit integer is better suited as a hash table key and for serialized storage than a variable-length USR string.</p><h3 id="symbol-occurrences-and-relations" tabindex="-1">Symbol Occurrences and Relations <a class="header-anchor" href="#symbol-occurrences-and-relations" aria-label="Permalink to &quot;Symbol Occurrences and Relations&quot;">​</a></h3><p>The index stores two kinds of core data: symbol occurrences (<code>Occurrence</code>) and symbol relations (<code>Relation</code>).</p><p>An <code>Occurrence</code> records a symbol&#39;s presence at a source location, containing only a source range and the target symbol&#39;s <code>SymbolHash</code>. It answers the question &quot;what symbol is under the cursor.&quot;</p><p>A <code>Relation</code> records richer semantic information, consisting of three elements: the relation kind (<code>RelationKind</code>), a source location, and a target symbol. Relation kinds cover common inter-symbol semantics:</p><ul><li>Definition and declaration (Definition, Declaration)</li><li>References (Reference, WeakReference)</li><li>Inheritance (Base, Derived)</li><li>Calls (Caller, Callee)</li><li>Type relationships (Interface, Implementation, TypeDefinition)</li><li>Construction and destruction (Constructor, Destructor)</li></ul><p>The two are stored separately because their query patterns differ. <code>Occurrence</code> is indexed by position — given a byte offset, binary search quickly locates the symbol under the cursor. <code>Relation</code> is indexed by <code>SymbolHash</code> — given a symbol, look up all its definitions, references, and call relationships. These two queries have contradictory sorting requirements; separate storage allows both to execute efficiently.</p><h3 id="three-level-index-hierarchy" tabindex="-1">Three-Level Index Hierarchy <a class="header-anchor" href="#three-level-index-hierarchy" aria-label="Permalink to &quot;Three-Level Index Hierarchy&quot;">​</a></h3><p>clice&#39;s index is organized into three levels, each with a different lifecycle and responsibility:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TUIndex        Raw artifact from a single compilation, discarded after merging</span></span>
<span class="line"><span>    ↓ merge</span></span>
<span class="line"><span>ProjectIndex   Global symbol directory (which files a symbol appears in), resident in memory</span></span>
<span class="line"><span>MergedIndex    Per-file sharded relation data (exact positions and relations), loaded on demand</span></span>
<span class="line"><span>    ↑ overlay</span></span>
<span class="line"><span>FileIndex      Real-time overlay for open files (from in-memory AST)</span></span></code></pre></div><p><strong>TUIndex</strong> is the raw index data produced by compiling a translation unit. <code>SemanticVisitor</code> traverses the AST, generating <code>Occurrence</code> and <code>Relation</code> records for each symbol, organized by file into a <code>TUIndex</code>. Since a compilation involves the main file and all included headers, <code>TUIndex</code> internally maintains a separate <code>FileIndex</code> for each file involved. <code>TUIndex</code> also contains a <code>SymbolTable</code> (mapping symbol hashes to names and kinds) and an <code>IncludeGraph</code> (include relationships from this compilation). <code>TUIndex</code> is transient data, discarded after being merged into the persistent indexes.</p><p><strong>ProjectIndex</strong> is the global symbol directory. It aggregates symbol information from all indexed translation units, maintaining a global symbol table: <code>SymbolHash</code> → symbol name, symbol kind, reference file bitmap. The reference file bitmap records which files the symbol appears in, stored using Roaring Bitmap compression.</p><p><code>ProjectIndex</code> does not store exact symbol positions (offsets, line numbers). Its role is that of a &quot;directory&quot; — it tells you which files a symbol exists in, then you look up the exact positions in the corresponding <code>MergedIndex</code> shard. This separation keeps <code>ProjectIndex</code> compact enough to reside in memory at all times.</p><p><strong>MergedIndex</strong> is the per-file sharded index storage layer. Each file in the project corresponds to one <code>MergedIndex</code> shard, storing all symbol occurrence positions and relation information for that file. This is the largest part of the index system by volume and the layer that actually serves queries. It supports lazy loading from disk — shards that are never queried need not be loaded into memory.</p><p><code>MergedIndex</code>&#39;s core capability is merging and deduplicating index data from different compilation contexts of the same file, detailed in the Implementation section.</p><p><strong>FileIndex</strong> (the open-file overlay) resides in each open file&#39;s <code>Session</code>, produced by the most recent in-memory compilation. It stores the same types of data as a <code>MergedIndex</code> shard (<code>Occurrence</code> and <code>Relation</code>), but is never written to global state — it is used only as an overlay during queries, overriding disk-indexed data with results from the current edit buffer&#39;s compilation.</p><h3 id="symbol-table" tabindex="-1">Symbol Table <a class="header-anchor" href="#symbol-table" aria-label="Permalink to &quot;Symbol Table&quot;">​</a></h3><p><code>SymbolTable</code> maps <code>SymbolHash</code> to symbol metadata — name and kind (Class, Function, Variable, etc.). It appears in two places: the global symbol table in <code>ProjectIndex</code> and the local symbol table in each <code>Session</code>. When looking up a symbol&#39;s name, the <code>Session</code> is checked first (more current); if not found, <code>ProjectIndex</code> is consulted.</p><h3 id="includegraph" tabindex="-1">IncludeGraph <a class="header-anchor" href="#includegraph" aria-label="Permalink to &quot;IncludeGraph&quot;">​</a></h3><p><code>IncludeGraph</code> records the include relationships from a single compilation. It consists of two parts: a path list (all file paths involved in the compilation) and <code>IncludeLocation</code> records (each indicating a file was included at a certain line, along with the source of the inclusion).</p><p><code>IncludeGraph</code> serves two purposes. During <code>TUIndex</code> merging, it provides the mapping from compilation-unit-internal file IDs to project-global path IDs. Within <code>MergedIndex</code>, it is stored as part of the compilation context, with the include chain used for staleness detection.</p><h2 id="implementation" tabindex="-1">Implementation <a class="header-anchor" href="#implementation" aria-label="Permalink to &quot;Implementation&quot;">​</a></h2><h3 id="index-construction" tabindex="-1">Index Construction <a class="header-anchor" href="#index-construction" aria-label="Permalink to &quot;Index Construction&quot;">​</a></h3><p><code>TUIndex</code> construction is performed by <code>SemanticVisitor</code>: given a compilation unit, it traverses the AST, generating <code>Occurrence</code> and <code>Relation</code> records for each named declaration and macro. After traversal, each file&#39;s data is deduplicated and sorted — <code>Occurrence</code> entries are sorted by position to support binary search, <code>Relation</code> entries are sorted by kind and position for efficient filtering.</p><p>During construction, the main file&#39;s (source file&#39;s) <code>FileIndex</code> is extracted separately. This allows different treatment during merging — the main file is merged as a source-file context, while other files are merged as header contexts.</p><h3 id="index-merging" tabindex="-1">Index Merging <a class="header-anchor" href="#index-merging" aria-label="Permalink to &quot;Index Merging&quot;">​</a></h3><p><code>TUIndex</code> merging into the persistent indexes proceeds in two steps.</p><p>Step one: symbol information is merged into <code>ProjectIndex</code>. All symbols from the <code>TUIndex</code> are inserted into the global symbol table, and each symbol&#39;s reference file bitmap is updated — file IDs involved in this compilation are added to the bitmap. Path mapping is also completed at this step: path IDs internal to the <code>TUIndex</code> are converted to <code>ProjectIndex</code>&#39;s global path IDs.</p><p>Step two: each file&#39;s <code>FileIndex</code> is merged into the corresponding <code>MergedIndex</code> shard. For the main file, compilation context information (build timestamp, include chain) is attached; for header files, header context information (include location identifier) is attached.</p><h3 id="compilation-context-deduplication" tabindex="-1">Compilation-Context Deduplication <a class="header-anchor" href="#compilation-context-deduplication" aria-label="Permalink to &quot;Compilation-Context Deduplication&quot;">​</a></h3><p>The core problem for <code>MergedIndex</code> is: the same header file is included by N source files, producing N <code>FileIndex</code> entries. If each were stored in full, storage would grow linearly with the number of translation units. But in practice, the vast majority of headers produce identical index data under different compilation contexts — the same symbols appear at the same positions, producing the same relations. Only headers like the <code>crypto.h</code> example above, affected by conditional compilation, produce different index content under different contexts.</p><p><code>MergedIndex</code> solves this through content-addressed deduplication. Each <code>FileIndex</code> has its SHA-256 content hash computed before merging. <code>FileIndex</code> entries with the same hash have identical content and share the same canonical ID (an auto-incrementing integer identifier).</p><p>Specifically, <code>Occurrence</code> and <code>Relation</code> entries inside <code>MergedIndex</code> are not simple lists — each entry is associated with a Roaring Bitmap recording which canonical IDs it belongs to. When a new <code>FileIndex</code> is merged in:</p><ol><li>Compute its SHA-256 hash</li><li>Check the cache: if this hash already exists, the data is identical — reuse the existing canonical ID and increment its reference count</li><li>If it is a new hash, allocate a new canonical ID, insert all <code>Occurrence</code> and <code>Relation</code> entries, and associate them with this new ID</li></ol><p>When a compilation context is removed (e.g., a source file is deleted from the project), the corresponding canonical ID&#39;s reference count is decremented. Canonical IDs whose reference count reaches zero are marked into the &quot;removed&quot; set. During queries, data belonging to the removed set is filtered out.</p><p>This design means storage depends on the number of distinct index contents rather than the number of compilation contexts. For most headers, regardless of how many source files include them, only one copy of the data is stored.</p><h3 id="compilation-context-types" tabindex="-1">Compilation Context Types <a class="header-anchor" href="#compilation-context-types" aria-label="Permalink to &quot;Compilation Context Types&quot;">​</a></h3><p><code>MergedIndex</code> internally distinguishes two types of compilation contexts:</p><ul><li><strong>CompilationContext</strong>: Produced when a file is compiled directly as a source file. Records the build timestamp and include chain (for staleness detection), along with the corresponding canonical ID. A file can have multiple <code>CompilationContext</code> entries, corresponding to different compilation commands in the compilation database.</li><li><strong>HeaderContext</strong>: Produced when a file is included as a header by another source file. Records the including source file and include location, along with the corresponding canonical ID.</li></ul><p>These two context types work in concert with the <a href="./compilation-context">compilation context</a> system. During queries, context types need not be distinguished — all context data has already been unified through canonical ID Bitmaps. During staleness detection, the <code>CompilationContext</code>&#39;s include chain is used to determine whether re-indexing is needed.</p><h3 id="lazy-loading" tabindex="-1">Lazy Loading <a class="header-anchor" href="#lazy-loading" aria-label="Permalink to &quot;Lazy Loading&quot;">​</a></h3><p><code>MergedIndex</code> is serialized using FlatBuffers. FlatBuffers&#39; design allows queries to be executed directly on serialized data without deserializing into in-memory structures. <code>MergedIndex</code> leverages this to implement a two-tier access model:</p><ul><li><strong>Read-only path</strong>: After loading from disk, <code>MergedIndex</code> remains as the raw memory-mapped buffer. Query operations execute directly on the FlatBuffers data, with zero deserialization overhead.</li><li><strong>Read-write path</strong>: When modifications are needed (merging new data or removing old contexts), the FlatBuffers data is first deserialized into in-memory structures, and subsequent operations are performed on those structures. Modified shards are re-serialized when saved.</li></ul><p>At startup, only <code>ProjectIndex</code> (relatively compact) needs to be loaded. <code>MergedIndex</code> shards are loaded on demand, and most shards are never accessed in a single session.</p><h3 id="query-flow" tabindex="-1">Query Flow <a class="header-anchor" href="#query-flow" aria-label="Permalink to &quot;Query Flow&quot;">​</a></h3><p>Using &quot;find references&quot; as an example to illustrate the full cross-file query flow:</p><ol><li>In the current file, use the cursor&#39;s byte offset to binary-search the <code>Occurrence</code> list and obtain the <code>SymbolHash</code> of the symbol under the cursor</li><li>Look up the <code>SymbolHash</code>&#39;s reference file bitmap in <code>ProjectIndex</code> to get all files containing the symbol</li><li>Query each file in the list individually: <ul><li>If the file is currently open (has an active <code>Session</code>), use the <code>Session</code>&#39;s <code>FileIndex</code>, skipping the corresponding <code>MergedIndex</code> shard</li><li>If the file is not open, load the corresponding <code>MergedIndex</code> shard and look up relations in it</li></ul></li><li>Aggregate all <code>Relation</code> entries found across files (filtering by the target <code>RelationKind</code>), convert to LSP positions, and return to the client</li></ol><p>In step 3, open files preferentially use the <code>Session</code>&#39;s <code>FileIndex</code> rather than <code>MergedIndex</code>, because the buffer content may differ from disk. The <code>Session</code>&#39;s <code>FileIndex</code> comes from in-memory compilation results that more accurately reflect the code the user is currently seeing. Only when the <code>Session</code>&#39;s AST is in a dirty state (the user has edited but the file has not been recompiled) does the system fall back to <code>MergedIndex</code>.</p><blockquote><p>Converting offsets to LSP positions requires the file content and a line-start offset table. <code>MergedIndex</code> shards also store the corresponding file&#39;s content and line-start table, so this conversion can be performed even for files that are not open.</p></blockquote><h3 id="staleness-detection" tabindex="-1">Staleness Detection <a class="header-anchor" href="#staleness-detection" aria-label="Permalink to &quot;Staleness Detection&quot;">​</a></h3><p>Staleness detection determines whether a file needs to be re-indexed. The <code>MergedIndex</code> shard stores the build timestamp and include chain. During detection, the last modification time (mtime) of each file in the include chain is checked. If any file&#39;s mtime is later than the build timestamp, the dependency has been updated and re-indexing is needed.</p><p>This detection is conservative — an mtime change does not necessarily mean the content changed (e.g., a <code>touch</code> operation, branch switching). But a false positive only results in one extra indexing pass, never a missed update.</p><h3 id="background-indexing-scheduling" tabindex="-1">Background Indexing Scheduling <a class="header-anchor" href="#background-indexing-scheduling" aria-label="Permalink to &quot;Background Indexing Scheduling&quot;">​</a></h3><p>Background indexing scheduling must balance index timeliness against interference with user interaction. The index module employs the following strategies:</p><ul><li><strong>Queue with idle delay</strong>: Files that need indexing are added to a queue, and processing begins only after the editor has been idle for a configurable period. This avoids triggering index tasks during rapid editing.</li><li><strong>Concurrency control with memory monitoring</strong>: The number of concurrent index tasks has a configurable upper limit. During indexing, system memory usage is dynamically monitored — concurrency is automatically reduced under memory pressure and gradually restored when memory recovers.</li><li><strong>Priority management</strong>: User-initiated operations (such as compiling an open file) pause background indexing. Indexing resumes after the operation completes, ensuring user request latency is not affected by background indexing.</li><li><strong>Result merging and persistence</strong>: Each index task compiles a file and builds a <code>TUIndex</code> in a stateless subprocess. The result is serialized and sent back to the main process, which merges it into <code>ProjectIndex</code> and <code>MergedIndex</code>. After indexing completes, modified shards are written back to disk so they can be loaded directly on the next startup.</li></ul><h2 id="faq" tabindex="-1">FAQ <a class="header-anchor" href="#faq" aria-label="Permalink to &quot;FAQ&quot;">​</a></h2><ul><li><p><strong>Why separate <code>ProjectIndex</code> and <code>MergedIndex</code> instead of using a single unified index?</strong></p><p>If position information were also stored in <code>ProjectIndex</code>, its size would balloon dramatically, making it impossible to keep in memory. Without <code>ProjectIndex</code>, every cross-file query would need to traverse all <code>MergedIndex</code> shards to locate files containing the symbol — in a project with tens of thousands of files, loading that many shards is unacceptable. <code>ProjectIndex</code> serves as a lightweight directory layer that first narrows the search scope to a handful of specific files, then precise lookup happens in the corresponding shards.</p></li><li><p><strong>Why don&#39;t open-file indexes write to global state?</strong></p><p>Buffer content being edited by the user may be incomplete code with syntax errors. If this temporary state were written to the global index, it would pollute query results for other files. For example, a symbol definition temporarily disappearing from a header being edited would affect find-references results for every file that references that symbol. The global index only accepts stable state saved to disk, built through background indexing from disk files.</p></li><li><p><strong>Is there a hash collision risk with content-addressed deduplication?</strong></p><p>Theoretically SHA-256 collisions are possible, but the probability is negligible (on the order of 2^-128). In practice, treating SHA-256 collisions as &quot;will not happen&quot; is standard. Even if a collision occurred, the only consequence would be two different <code>FileIndex</code> entries sharing data — it would not cause a crash or data corruption.</p></li><li><p><strong>Why FlatBuffers rather than Protocol Buffers or a custom format?</strong></p><p>FlatBuffers allows queries to be executed directly on serialized data without deserializing first. For data like <code>MergedIndex</code> that may have thousands of shards, most shards are never accessed in a single session. FlatBuffers&#39; zero-copy property makes loading a shard nearly free — only a memory-mapped file is needed, and only actually accessed data is read into memory. Protocol Buffers requires a full deserialization step, making it unsuitable for this on-demand loading model.</p></li><li><p><strong>Why store <code>Occurrence</code> and <code>Relation</code> separately?</strong></p><p><code>Occurrence</code> is indexed by position — given an offset, binary search locates the symbol under the cursor, requiring position-sorted data. <code>Relation</code> is indexed by <code>SymbolHash</code> — given a symbol, look up all its relationships, requiring symbol-grouped data. Combining them into a single data structure would inevitably sacrifice efficiency in at least one of the two query patterns.</p></li><li><p><strong>Why are cross-file queries performed in the main process rather than subprocesses?</strong></p><p>clice uses a multi-process architecture where each open file is compiled in its own stateful subprocess. Cross-file queries (such as find-references) need to iterate over all open files&#39; <code>Session</code> instances and aggregate their <code>FileIndex</code> results. If these Sessions were spread across different subprocesses, each query would require cross-process communication with multiple workers and then result aggregation — unacceptable in both latency and complexity. Instead, subprocesses send their <code>FileIndex</code> back to the main process after compilation, and the main process performs queries uniformly — it can access all open files&#39; <code>FileIndex</code> entries as well as <code>ProjectIndex</code> and <code>MergedIndex</code>, completing the full query flow in a single process.</p><p>This design also has a semantic consideration: query results for open files should reflect the editor&#39;s buffer state, not the disk state. Even if a file on disk has been modified by an external tool, as long as the editor has not sent a <code>didChange</code> notification, query results should be based on the version the editor holds. Centralizing all <code>FileIndex</code> entries in the main process makes this semantic invariant easier to maintain.</p></li><li><p><strong>Why not use a database for index storage?</strong></p><p>clice needs to persist multiple types of cache files: index shards, PCH, PCM, etc. PCH and PCM files are large (potentially hundreds of MB) but few in number (roughly proportional to the number of open files or modules), and have a simple lifecycle — create, read, delete when stale. There are no complex query or transaction requirements. The capabilities databases excel at (transactions, indexing, complex queries) are irrelevant for these files; filesystem management is sufficient.</p><p>Index shards are the only part that could potentially benefit from a database: numerous (equal to the number of project files), small in size, and could benefit from atomic writes and automatic LRU eviction. But the current filesystem-based approach already handles these needs adequately. Whether the added complexity of introducing a database dependency is justified for this one use case needs to be evaluated when an actual bottleneck is encountered. For very large projects (tens of thousands of files), storing that many index shards in a single directory may create filesystem-level pressure; hierarchical storage or a lightweight database could be considered in the future.</p></li></ul><h2 id="known-limitations" tabindex="-1">Known Limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known Limitations&quot;">​</a></h2><ul><li><p><strong>Symbol table locality</strong>. Currently all symbols (including function-local variables) are merged into <code>ProjectIndex</code>&#39;s global symbol table. This causes a large number of symbols meaningful only within a single file to be stored globally, increasing hash table insertion overhead during merging and memory usage.</p><p>The improvement direction is to introduce multi-level symbol tables — not only <code>ProjectIndex</code> should have a <code>SymbolTable</code>, but <code>MergedIndex</code> shards should have their own as well. The rule for determining which level a symbol belongs to is: a symbol belongs to the <code>SymbolTable</code> of the file where it is defined, provided it is internal (will not be referenced by other files). For example:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// utils.h</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">inline</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> helper</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> x</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    auto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> temp </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // temp is a local symbol of utils.h</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> temp </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// main.cpp</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;utils.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> counter </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // counter is a local symbol of main.cpp</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    counter </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> helper</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">42</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>temp</code> is defined in <code>utils.h</code> and will never be referenced by any other file — it should be in the <code>SymbolTable</code> of <code>utils.h</code>&#39;s <code>MergedIndex</code> shard, not in <code>ProjectIndex</code>&#39;s global symbol table. <code>counter</code> is a static variable in <code>main.cpp</code> and similarly should be in <code>main.cpp</code>&#39;s <code>MergedIndex</code> shard. Note that although <code>temp</code> appears in a header file, it belongs to the header&#39;s <code>SymbolTable</code> rather than the including source file&#39;s <code>SymbolTable</code>, because it is defined in the header. Only symbols like <code>helper</code> and <code>main</code> that may be referenced cross-file need to enter <code>ProjectIndex</code>.</p><p>The goal is to minimize <code>ProjectIndex</code>&#39;s size and merging overhead, while avoiding duplicate storage of the same local symbol across multiple translation units.</p></li><li><p><strong>Staleness detection precision</strong>. The current staleness detection uses only mtime — re-indexing is triggered whenever a dependency file&#39;s mtime is later than the build timestamp. This produces unnecessary re-indexes in scenarios like <code>touch</code>, branch switching, or CI restores (file mtime changed but content is actually unchanged). The improvement direction is mtime + content hash dual-layer detection: the first layer uses mtime for a fast check — if unchanged, skip immediately (zero I/O); the second layer computes the content hash for files whose mtime changed — if the hash is unchanged, the content was not actually modified and can also be skipped. This approach is already used for compilation artifact staleness detection (PCH, AST); the index staleness detection should be aligned.</p></li><li><p><strong>Fuzzy symbol search</strong>. The current workspace symbol search (workspace/symbol) is a simple substring match that does a linear scan over all symbols in <code>ProjectIndex</code>. This is insufficient for large projects and does not support fuzzy matching.</p><p>C++ symbol names have structure: <code>getSymbolHash</code> is camelCase, <code>get_symbol_hash</code> is snake_case, <code>std::vector&lt;int&gt;::push_back</code> has namespace qualification. When searching, users typically type abbreviations or fragments (e.g., <code>symhash</code>, <code>gSH</code>, <code>vec_pb</code>), expecting them to match the full symbol name. Substring matching cannot handle these queries.</p><p>The improvement direction is to build a dedicated search index over symbol names. A tokenizer is needed to split symbol names by naming conventions (<code>getSymbolHash</code> → <code>[get, Symbol, Hash]</code>, <code>push_back</code> → <code>[push, back]</code>), then build an inverted index over the tokens. For example, trigrams (three-character groups) can be used as index keys, and at query time trigram intersections produce a candidate set that is then scored precisely. clangd&#39;s Dex index uses this trigram posting list approach and serves as a useful reference implementation. Another direction is to adopt a mature full-text search library, though the cost of introducing an external dependency needs to be evaluated.</p></li><li><p><strong>PCH-induced index split</strong>. When using PCH (precompiled header) optimization, a file&#39;s compilation is effectively split into two phases: first the preamble (the <code>#include</code> directives at the top of the file) is compiled to produce the PCH, then the PCH is used to compile the rest of the file. The PCH itself is a compilation unit and produces its own index data.</p><p>This split affects the index. Take document links (clickable <code>#include</code> directives in the editor) as an example: <code>#include</code> directives in the preamble belong to the PCH compilation phase, and the main file&#39;s compilation cannot see them. Since the index system does not currently store document link information, it cannot reconstruct the PCH portion&#39;s results through the index. The current workaround is to pre-serialize the PCH&#39;s document links as JSON during PCH construction and store it in the PCH metadata, then manually splice it into the main file&#39;s results at query time. This works but is not clean.</p><p>A better approach would be to incorporate document links into the PCH metadata system (which already stores dependency file lists and other information), or to leverage the include relationship information already present in the index to reconstruct document links. The latter has the problem that index construction takes time, and after PCH compilation completes it should be put to use as quickly as possible — waiting for indexing to complete would add latency. Neither approach is fully implemented yet.</p></li></ul>`,74)])])}const m=i(a,[["render",o]]);export{u as __pageData,m as default};
