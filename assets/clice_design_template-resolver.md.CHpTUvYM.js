import{_ as t,o as a,c as s,a3 as i}from"./chunks/framework.CRbjHCyB.js";const g=JSON.parse('{"title":"Template Resolution","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/template-resolver.md","filePath":"en/clice/design/template-resolver.md"}'),n={name:"clice/design/template-resolver.md"};function l(r,e,o,p,h,d){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="template-resolution" tabindex="-1">Template Resolution <a class="header-anchor" href="#template-resolution" aria-label="Permalink to &quot;Template Resolution&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>A core design principle of C++ templates is <strong>deferred instantiation</strong> -- before template code is instantiated with concrete type arguments, the compiler does not (and cannot) resolve names that depend on template parameters. These are called <strong>dependent names</strong>, and their types are unknown at template definition time.</p><p>For a language server, this means the interior of template code is a &quot;blind spot&quot;:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> foo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">vec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    vec.</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // cursor here -- vec&#39;s type is known to be vector&lt;T&gt;, completions can be provided</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>For simple cases, using the primary template&#39;s definition suffices to provide basic completions. But consider a more complex scenario:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> foo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">vec2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    vec2[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">].</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // What is the type of vec2[0]?</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The type of <code>vec2[0]</code> is <code>std::vector&lt;std::vector&lt;T&gt;&gt;::reference</code> -- a dependent name. In standard library implementations, resolving it requires tracing through dozens of layers of nested template typedef chains: <code>reference</code> -&gt; <code>allocator_traits&lt;Alloc&gt;::value_type</code> -&gt; <code>__alloc_traits&lt;Alloc&gt;::reference</code> -&gt; ... Each layer involves partial specialization matching, default template arguments, typedef expansion, and other complex operations.</p><p>In the clangd community, completion and hover issues in template code have persisted for a long time. Users repeatedly report the inability to get completion suggestions inside template function bodies -- the cursor position shows <code>&lt;dependent type&gt;</code> and all LSP features become non-functional. The root cause is that clangd&#39;s resolution strategy has several fundamental limitations:</p><p><strong>No partial specialization handling</strong>: clangd assumes the primary template&#39;s definition is always used for member lookup. However, the standard library makes heavy use of partial specializations (e.g., <code>allocator_traits</code> specialized for different allocators), and the primary template may not even have the member being looked up -- the member only exists in a specific partial specialization.</p><p><strong>Missing argument mapping</strong>: Even when name lookup finds a member&#39;s type, that type is still expressed in terms of the looked-up template&#39;s parameters (e.g., <code>Alloc</code> in <code>allocator_traits&lt;Alloc&gt;::value_type</code>), not the caller&#39;s parameters (e.g., <code>T</code>). Since clangd does not perform instantiation, it cannot establish mappings between parameters.</p><p><strong>Ignoring default template arguments</strong>: <code>std::vector&lt;T&gt;</code> is actually <code>std::vector&lt;T, std::allocator&lt;T&gt;&gt;</code> -- the second argument is defaulted. clangd does not expand default arguments, causing names that depend on defaulted arguments to be unresolvable.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="core-idea" tabindex="-1">Core Idea <a class="header-anchor" href="#core-idea" aria-label="Permalink to &quot;Core Idea&quot;">​</a></h3><p>clice implements a <strong>PseudoInstantiator</strong> -- it resolves dependent names through heuristic methods without requiring concrete type arguments. The core insight is: you do not need to know whether <code>T</code> is <code>int</code> or <code>string</code>; you only need to trace how <code>T</code> propagates through the template typedef chain and express the final result in terms of <code>T</code>.</p><p>For example: <code>std::vector&lt;std::vector&lt;T&gt;&gt;::reference</code> is simplified through pseudo-instantiation to <code>std::vector&lt;T&gt;&amp;</code> -- a type concrete enough to provide completions while preserving the symbolic meaning of the template parameter <code>T</code>.</p><h3 id="two-phase-transform" tabindex="-1">Two-Phase Transform <a class="header-anchor" href="#two-phase-transform" aria-label="Permalink to &quot;Two-Phase Transform&quot;">​</a></h3><p>The resolver is built on Clang&#39;s TreeTransform infrastructure and operates in two phases:</p><p><strong>Phase 1: PseudoInstantiator (Heuristic Resolution)</strong></p><p>This is the main engine, responsible for resolving various kinds of dependent names:</p><ul><li><strong>DependentNameType</strong> (e.g., <code>typename Container::value_type</code>): looks up <code>value_type</code> among the template&#39;s members, matches partial specializations, expands typedefs, and re-expresses the result using the caller&#39;s parameters</li><li><strong>DependentTemplateSpecializationType</strong> (e.g., <code>Alloc::rebind&lt;U&gt;</code>): resolves dependent template specializations, with a fast path for standard library patterns tried first</li><li><strong>TemplateTypeParmType</strong> (e.g., <code>T</code>): looks up the parameter&#39;s bound value or default argument via the instantiation stack</li><li><strong>DecltypeType</strong> (e.g., <code>decltype(var)</code>): resolves simple variable references to their declared type</li></ul><p><strong>Phase 2: SubstituteOnly (Cycle Breaking)</strong></p><p>When Phase 1 expands a typedef, it may encounter another dependent name requiring heuristic lookup, forming a cycle: typedef A&#39;s underlying type references dependent name B, and looking up B reveals its type involves typedef A again.</p><p>SubstituteOnly breaks such cycles -- it only performs argument substitution and typedef expansion without heuristic lookups. When Phase 1 needs to expand a typedef, it delegates to SubstituteOnly, ensuring that recursive heuristic lookups are never triggered.</p><h3 id="instantiation-stack" tabindex="-1">Instantiation Stack <a class="header-anchor" href="#instantiation-stack" aria-label="Permalink to &quot;Instantiation Stack&quot;">​</a></h3><p>Template parameters may exist at different depth levels in nested templates. The instantiation stack (InstantiationStack) maintains a stack of parameter mappings, with each frame recording one level of template parameter bindings.</p><p>When a <code>TemplateTypeParmType</code> (a template parameter identified by depth and index) is encountered, the resolver searches linearly from the top of the stack (innermost level) to the bottom (outermost level), matching parameter bindings at the corresponding depth. If a match is found, the parameter is substituted with the bound type; if not found, the parameter&#39;s default value is tried. When the stack is empty (a standalone dependent type), the resolver walks up through enclosing template declarations and pushes their injected template parameters as context.</p><p>This allows the resolver to handle multi-level nested templates:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> X</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Outer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Y</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Inner</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pair</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">X</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Y</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">first_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> member;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // X is at depth 0, Y is at depth 1</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // Both need to be tracked in the stack for correct resolution</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><h3 id="dependent-name-resolution-flow" tabindex="-1">Dependent Name Resolution Flow <a class="header-anchor" href="#dependent-name-resolution-flow" aria-label="Permalink to &quot;Dependent Name Resolution Flow&quot;">​</a></h3><p>Using <code>typename A&lt;T&gt;::type</code> as an example:</p><ol><li><strong>Cache check</strong>: If this node has been resolved before, return the cached result directly</li><li><strong>Cycle detection</strong>: If this node is currently being resolved, abort to prevent infinite recursion</li><li><strong>Qualifier transform</strong>: Transform the <code>A&lt;T&gt;</code> part -- substitute parameters, match partial specializations</li><li><strong>Member lookup</strong>: Look up <code>type</code> in the transformed type. Try each partial specialization and its bases, then the primary template and its bases</li><li><strong>Argument deduction</strong>: Use Clang&#39;s template argument deduction mechanism to establish a mapping from formal to actual parameters</li><li><strong>Substitution</strong>: Expand typedefs in the found member type via SubstituteOnly, substituting with the caller&#39;s parameters</li><li><strong>Recursion</strong>: If the result still contains dependent names, resolve recursively</li></ol><p>The entire process has a recursion depth limit (16 levels) to prevent infinite recursion in extreme cases.</p><p>The resolver also maintains two layers of cycle detection: one prevents re-entrant resolution of the same DependentNameType node, and another prevents recursive lookup on the same ClassTemplateDecl (handling CRTP and other self-referential patterns).</p><h3 id="partial-specialization-matching" tabindex="-1">Partial Specialization Matching <a class="header-anchor" href="#partial-specialization-matching" aria-label="Permalink to &quot;Partial Specialization Matching&quot;">​</a></h3><p>Partial specialization matching is a key advantage of the pseudo-instantiator over clangd. The standard library makes heavy use of partial specializations, for example:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Primary template -- does not define value_type</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Alloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> allocator_traits</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Partial specialization -- defines value_type</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typename</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> allocator_traits</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">allocator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt; {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    using</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> value_type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>When resolving <code>allocator_traits&lt;allocator&lt;int&gt;&gt;::value_type</code>, clangd fails because it looks up <code>value_type</code> in the primary template. The pseudo-instantiator tries matching the actual arguments against each partial specialization&#39;s pattern, finds the correct specialization, and then looks up the member within it.</p><h3 id="standard-library-special-handling" tabindex="-1">Standard Library Special Handling <a class="header-anchor" href="#standard-library-special-handling" aria-label="Permalink to &quot;Standard Library Special Handling&quot;">​</a></h3><p>The standard library&#39;s allocator rebinding chain is a particularly deep typedef chain:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>vector&lt;T&gt; -&gt; __alloc_traits&lt;allocator&lt;T&gt;&gt; -&gt; allocator_traits&lt;allocator&lt;T&gt;&gt;</span></span>
<span class="line"><span>  -&gt; allocator_traits&lt;Alloc&gt;::rebind_alloc&lt;U&gt;</span></span></code></pre></div><p>This chain may exceed the depth limit. The resolver short-circuits the <code>allocator_traits::rebind_alloc</code> pattern: upon detecting it, the resolver directly tries <code>Alloc::rebind&lt;T&gt;::other</code> (the standard allocator protocol), or falls back to substituting the first template parameter.</p><h3 id="type-resugaring" tabindex="-1">Type Resugaring <a class="header-anchor" href="#type-resugaring" aria-label="Permalink to &quot;Type Resugaring&quot;">​</a></h3><p>Internally, Clang represents template parameters as canonical <code>TemplateTypeParmType</code> values (depth + index). Users should see parameter names (e.g., <code>T</code>), not <code>parameter 0 of depth 0</code>. The ResugarOnly transform maps canonical parameter types back to their original declarations, producing user-friendly type information.</p><h3 id="graceful-degradation" tabindex="-1">Graceful Degradation <a class="header-anchor" href="#graceful-degradation" aria-label="Permalink to &quot;Graceful Degradation&quot;">​</a></h3><p>If any step in the resolution process fails (lookup failure, cycle detection triggered, depth limit exceeded), the resolver returns the original dependent type rather than reporting an error. This means LSP features are never completely broken due to a resolution failure -- they simply degrade to the &quot;unresolvable&quot; state, which is no worse than not performing pseudo-instantiation at all.</p><h2 id="design-decisions-and-trade-offs" tabindex="-1">Design Decisions and Trade-offs <a class="header-anchor" href="#design-decisions-and-trade-offs" aria-label="Permalink to &quot;Design Decisions and Trade-offs&quot;">​</a></h2><p><strong>Why pseudo-instantiation instead of real template instantiation?</strong> Real instantiation requires concrete type arguments (e.g., <code>T = int</code>), but this information is unavailable at template definition time. Moreover, code in a language server is frequently incomplete and cannot undergo full instantiation. Pseudo-instantiation avoids dependence on concrete types by preserving symbolic parameters (<code>T</code> itself).</p><p><strong>Why a two-phase design?</strong> In a single-phase design, typedef expansion would trigger new heuristic lookups, and lookup results might in turn require typedef expansion -- forming a cycle. The two-phase design breaks this cycle through separation of concerns: heuristic lookups are performed only in Phase 1, and typedef expansion is delegated to Phase 2, which does not perform lookups.</p><p><strong>Why special-case standard library patterns?</strong> The standard library&#39;s allocator rebinding is the most common deep typedef chain encountered in real-world code. Generic recursive resolution would exceed the depth limit here. While not elegant, the special-case handling covers the highest-frequency user scenarios. Future plans include replacing these special cases with a general mechanism.</p><p><strong>Why graceful degradation instead of error reporting?</strong> Template resolution is inherently heuristic -- it is impossible to cover every edge case in C++ templates. Graceful degradation ensures that resolution failure is never worse than &quot;not resolving at all,&quot; while providing a significant experience improvement in the cases where resolution succeeds.</p><h2 id="known-limitations" tabindex="-1">Known Limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known Limitations&quot;">​</a></h2><ul><li><strong>Pack expansion</strong>: Currently only single-element packs are handled (e.g., pack forwarding); multi-element pack expansion (e.g., <code>Us... = {int, float}</code>) is not supported.</li><li><strong>Default values of non-type template parameters</strong>: Only default value substitution for type template parameters (TemplateTypeParmDecl) is supported; default values for non-type parameters (e.g., <code>template&lt;int N = 0&gt;</code>) and template template parameters are not.</li><li><strong>Dependent member expressions</strong>: Dependent member access of the form <code>x.template foo&lt;T&gt;()</code> is not yet implemented.</li><li><strong>Complex decltype</strong>: Only the simple case <code>decltype(var)</code> is handled; member access, function calls, and other complex expressions are not supported.</li><li><strong>Operator lookup</strong>: Operators in dependent contexts (e.g., <code>a + b</code> where <code>a</code>&#39;s type depends on a template parameter) cannot be resolved because operators lack identifier information.</li></ul>`,53)])])}const k=t(n,[["render",l]]);export{g as __pageData,k as default};
