import{_ as a,o as t,c as o,a3 as c}from"./chunks/framework.aYYe0dfI.js";const p=JSON.parse('{"title":"Module extract:scan","description":"The extract:scan module is responsible for scanning C++ source files to extract module declaration and include information, as well as constructing inter-file dependency graphs. Its primary public functions are scan_file, which returns a ScanResult or a ScanError for a single file; scan_module_decl, which quickly populates module name, interface unit status, and imports using Clang’s dependency directives scanner; and build_dependency_graph_async, which asynchronously builds a complete DependencyGraph from a compilation database handle, optionally using a ScanCache for persistent caching across scans. A helper function topological_order computes a topological ordering of the graph’s nodes.","frontmatter":{"title":"Module extract:scan","description":"The extract:scan module is responsible for scanning C++ source files to extract module declaration and include information, as well as constructing inter-file dependency graphs. Its primary public functions are scan_file, which returns a ScanResult or a ScanError for a single file; scan_module_decl, which quickly populates module name, interface unit status, and imports using Clang’s dependency directives scanner; and build_dependency_graph_async, which asynchronously builds a complete DependencyGraph from a compilation database handle, optionally using a ScanCache for persistent caching across scans. A helper function topological_order computes a topological ordering of the graph’s nodes.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/extract/scan.md","filePath":"en/clore/modules/extract/scan.md"}'),r={name:"clore/modules/extract/scan.md"};function n(i,e,d,s,l,h){return t(),o("div",null,[...e[0]||(e[0]=[c('<h1 id="module-extract-scan" tabindex="-1">Module <code>extract:scan</code> <a class="header-anchor" href="#module-extract-scan" aria-label="Permalink to &quot;Module `extract:scan`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>extract:scan</code> module is responsible for scanning C++ source files to extract module declaration and include information, as well as constructing inter-file dependency graphs. Its primary public functions are <code>scan_file</code>, which returns a <code>ScanResult</code> or a <code>ScanError</code> for a single file; <code>scan_module_decl</code>, which quickly populates module name, interface unit status, and imports using Clang’s dependency directives scanner; and <code>build_dependency_graph_async</code>, which asynchronously builds a complete <code>DependencyGraph</code> from a compilation database handle, optionally using a <code>ScanCache</code> for persistent caching across scans. A helper function <code>topological_order</code> computes a topological ordering of the graph’s nodes.</p><p>The module owns the public types <code>ScanResult</code> (holding module name, interface flag, imports, includes, and errors), <code>ScanCache</code> (shared cache invalidated on compilation DB or filesystem changes), <code>DependencyGraph</code> (with file lists and directed edges), <code>DependencyEdge</code> (representing a single dependency), <code>IncludeInfo</code> (with path and angled flag), and <code>ScanError</code> (with a message string). Supporting internal machinery includes <code>ScanAction</code>, <code>ScanPPCallbacks</code>, <code>PreparedScanEntry</code>, and <code>MissingScanTask</code>, all of which work together to drive the scanning and graph-building pipeline.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./compiler"><code>extract:compiler</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./cache"><code>extract:cache</code></a></li></ul><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-extract-dependencyedge" tabindex="-1"><code>clore::extract::DependencyEdge</code> <a class="header-anchor" href="#clore-extract-dependencyedge" aria-label="Permalink to &quot;`clore::extract::DependencyEdge`&quot;">​</a></h3><p>Declaration: <code>extract/scan.cppm:51</code></p><p>Definition: <code>extract/scan.cppm:51</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct <code>clore::extract::DependencyEdge</code> models a directed dependency relationship between two named entities. It aggregates two <code>std::string</code> members, <code>from</code> and <code>to</code>, indicating the source and target of the dependency edge. The structure imposes no invariants beyond standard string validity; both fields can be empty, but in practice they are expected to contain non-empty identifiers to form meaningful edges. This aggregate type provides no custom constructors or member functions, relying on default copy, move, and comparison semantics. Its primary purpose is to serve as a lightweight value type within dependency graph construction and traversal logic.</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>from</code> and <code>to</code> are distinct identifiers (implied by edge semantics, not enforced)</li><li>Members are public and may be mutated directly</li><li>No ownership or lifetime constraints beyond those of <code>std::string</code></li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>from</code>: the source node identifier</li><li><code>to</code>: the target node identifier</li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to construct dependency graphs or lists</li><li>Stored in containers such as <code>std::vector&lt;DependencyEdge&gt;</code></li><li>Iterated over to extract source/target pairs for further processing</li></ul><h3 id="clore-extract-dependencygraph" tabindex="-1"><code>clore::extract::DependencyGraph</code> <a class="header-anchor" href="#clore-extract-dependencygraph" aria-label="Permalink to &quot;`clore::extract::DependencyGraph`&quot;">​</a></h3><p>Declaration: <code>extract/scan.cppm:56</code></p><p>Definition: <code>extract/scan.cppm:56</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct serves as a flat container for the extracted dependency graph, holding two parallel vectors: <code>files</code> stores the canonical paths of all source files encountered, and <code>edges</code> stores the directed dependencies between them. The invariant that each <code>DependencyEdge</code> must reference indices valid within <code>files</code> is expected to be maintained by the functions that populate the graph. No special construction or assignment logic is provided; the struct is a plain aggregate.</p><h4 id="invariants-1" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-1" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>No explicitly documented invariants.</li><li>All members are public and default-initialized to empty vectors.</li></ul><h4 id="key-members-1" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-1" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>files</code></li><li><code>edges</code></li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Instantiated and populated by dependency extraction routines.</li><li>Consumed by downstream analysis or serialization code.</li></ul><h3 id="clore-extract-includeinfo" tabindex="-1"><code>clore::extract::IncludeInfo</code> <a class="header-anchor" href="#clore-extract-includeinfo" aria-label="Permalink to &quot;`clore::extract::IncludeInfo`&quot;">​</a></h3><p>Declaration: <code>extract/scan.cppm:24</code></p><p>Definition: <code>extract/scan.cppm:24</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p><code>clore::extract::IncludeInfo</code> is a lightweight aggregate data structure that captures the two essential characteristics of a C++ <code>#include</code> directive: the included <code>path</code> and whether the directive used angle brackets (<code>&lt;&gt;</code>) versus quotes (<code>&quot;&quot;</code>), stored in the <code>is_angled</code> flag. The default member initializers ensure that a default‑constructed instance holds an empty <code>path</code> and <code>is_angled</code> set to <code>false</code>.</p><p>As an implementation detail, the struct imposes no invariants beyond the natural constraints of its members: <code>path</code> is an arbitrary <code>std::string</code>, and <code>is_angled</code> is a <code>bool</code>. No custom constructors, assignment <code>operator</code>s, or other special member functions are defined, so the compiler‑generated defaults apply. The primary role of this type is to serve as a simple data carrier between parsing steps, where its fields are populated by the include‑directive recognition logic and later consumed by higher‑level extraction routines.</p><h4 id="invariants-2" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-2" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>is_angled</code> distinguishes angle-bracket includes from quoted includes.</li><li><code>path</code> can be any string, including empty.</li></ul><h4 id="key-members-2" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-2" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>path</code></li><li><code>is_angled</code></li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Returned by parsing functions to represent a single include directive.</li><li>Consumed by downstream logic to determine include search behavior.</li></ul><h3 id="clore-extract-scancache" tabindex="-1"><code>clore::extract::ScanCache</code> <a class="header-anchor" href="#clore-extract-scancache" aria-label="Permalink to &quot;`clore::extract::ScanCache`&quot;">​</a></h3><p>Declaration: <code>extract/scan.cppm:40</code></p><p>Definition: <code>extract/scan.cppm:40</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct contains a single data member: an <code>std::unordered_map&lt;std::string, ScanResult&gt;</code> named <code>scan_results</code>. This map serves as the persistent storage for previously computed scan outcomes, keyed by the file path (as a <code>std::string</code>). The only invariant is that the map’s entries remain valid only while the compilation database and file system state are unchanged; when either of those changes, the entire cache must be cleared or discarded. Because the struct has no other members, all operations are forwarded to the underlying unordered map, and the implementation’s correctness depends on callers respecting the invalidation contract.</p><h4 id="invariants-3" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-3" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>Cache entries are valid only until compilation DB or file system state changes.</li><li>The <code>scan_results</code> map is unordered; iteration order is not guaranteed.</li></ul><h4 id="key-members-3" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-3" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>scan_results</code>: maps file paths to cached <code>ScanResult</code> objects.</li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Shared across successive dependency scans to avoid redundant work.</li><li>Callers must clear or discard the cache when compilation DB or file system state changes.</li></ul><h3 id="clore-extract-scanerror" tabindex="-1"><code>clore::extract::ScanError</code> <a class="header-anchor" href="#clore-extract-scanerror" aria-label="Permalink to &quot;`clore::extract::ScanError`&quot;">​</a></h3><p>Declaration: <code>extract/scan.cppm:20</code></p><p>Definition: <code>extract/scan.cppm:20</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct <code>clore::extract::ScanError</code> is defined with a single data member <code>message</code> of type <code>std::string</code>. It is an aggregate, so all special member functions (default constructor, copy, move, and assignment) are implicitly declared and defined by the compiler. No invariants are enforced within the struct; the content of <code>message</code> is arbitrary and is stored exactly as provided. This minimal implementation serves as a lightweight container for error descriptions, intended to be used as the error type in return values or thrown objects without any additional logic or validation.</p><h4 id="invariants-4" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-4" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>message</code> may be empty or contain any valid string</li></ul><h4 id="key-members-4" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-4" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>std::string message</code></li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Returned as an error result from scanning functions</li><li>Likely used with <code>std::expected</code> or similar error-handling mechanisms</li></ul><h3 id="clore-extract-scanresult" tabindex="-1"><code>clore::extract::ScanResult</code> <a class="header-anchor" href="#clore-extract-scanresult" aria-label="Permalink to &quot;`clore::extract::ScanResult`&quot;">​</a></h3><p>Declaration: <code>extract/scan.cppm:29</code></p><p>Definition: <code>extract/scan.cppm:29</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct <code>clore::extract::ScanResult</code> is an aggregate data container designed to hold the outcome of scanning a C++ source file for module and dependency information. Its internal structure consists of four public fields: <code>module_name</code> (a <code>std::string</code> defaulting to empty), <code>is_interface_unit</code> (a <code>bool</code> defaulting to <code>false</code>), <code>includes</code> (a <code>std::vector&lt;IncludeInfo&gt;</code> defaulting to empty), and <code>module_imports</code> (a <code>std::vector&lt;std::string&gt;</code> defaulting to empty). The default member initializers ensure that every default‑constructed <code>ScanResult</code> starts with a cleared state, avoiding any uninitialized values. No invariants are enforced beyond the guarantees of the underlying types; the fields are directly mutable and intended to be populated during scanning and read afterward. There are no custom constructors, assignment <code>operator</code>s, or member functions, making the struct a plain data carrier that serves as the output from the extraction process.</p><h4 id="invariants-5" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-5" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>Fields are default-initialized to empty strings, false, or empty vectors.</li></ul><h4 id="key-members-5" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-5" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>module_name</code></li><li><code>is_interface_unit</code></li><li>includes</li><li><code>module_imports</code></li></ul><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as a return type from scanning functions</li><li>Consumed to process module information</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-extract-build-dependency-graph-async" tabindex="-1"><code>clore::extract::build_dependency_graph_async</code> <a class="header-anchor" href="#clore-extract-build-dependency-graph-async" aria-label="Permalink to &quot;`clore::extract::build_dependency_graph_async`&quot;">​</a></h3><p>Declaration: <code>extract/scan.cppm:61</code></p><p>Definition: <code>extract/scan.cppm:370</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The function first clears the existing <code>graph.files</code> and <code>graph.edges</code>, then iterates over each compilation database entry to call <code>prepare_scan_entry</code> and populate a vector of <code>PreparedScanEntry</code> objects. During this pass it deduplicates normalized file paths into <code>graph.files</code>. It then partitions entries into those with cached results (looked up in <code>ScanCache::scan_results</code> if a <code>cache</code> pointer is provided) and those requiring re‑scanning. For the latter, it deduplicates by <code>cache_key</code> to create a set of <code>MissingScanTask</code> objects.</p><p>For each distinct missing task, it launches an asynchronous <code>run_scan_task</code> on the provided <code>kota::event_loop</code> and collects the resulting <code>kota::task</code> objects into a vector. The function then co‑awaits <code>kota::when_all</code> on all those tasks, handling a possible <code>ScanError</code> and verifying the result count. With all scan results available (either from cache or from the newly scanned batch), it iterates over every original entry. For each entry it normalizes include paths (from <code>scan_result-&gt;includes</code>) and adds a <code>DependencyEdge</code> to <code>graph.edges</code> if the target file is among the entry files and the edge has not already been emitted (deduplicated via a compound key). Finally, if a cache pointer exists, it stores every scan result back into <code>cache-&gt;scan_results</code>. The function co‑returns <code>void</code> upon success.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Clears and populates <code>graph.files</code> and <code>graph.edges</code></li><li>Inserts or assigns scan results into <code>cache-&gt;scan_results</code> if cache is non-null</li></ul><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>db</code> (compilation database entries)</li><li><code>cache</code> (previously cached scan results)</li><li><code>prepared_entries</code> (normalized file paths and cache keys)</li><li><code>cached_results</code> and <code>scanned_results</code> vectors</li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>graph.files</code></li><li><code>graph.edges</code></li><li><code>cache-&gt;scan_results</code> (if cache is non-null)</li><li>Local containers: <code>entry_files</code>, <code>file_set</code>, <code>prepared_entries</code>, <code>cached_results</code>, <code>missing_task_indices</code>, <code>missing_task_by_cache_key</code>, <code>missing_tasks</code>, <code>scanned_results</code>, <code>seen_files</code>, <code>emitted_edges</code></li></ul><h4 id="usage-patterns-6" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-6" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to asynchronously compute a dependency graph for a project given a compilation database</li></ul><h3 id="clore-extract-scan-file" tabindex="-1"><code>clore::extract::scan_file</code> <a class="header-anchor" href="#clore-extract-scan-file" aria-label="Permalink to &quot;`clore::extract::scan_file`&quot;">​</a></h3><p>Declaration: <code>extract/scan.cppm:44</code></p><p>Definition: <code>extract/scan.cppm:238</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The function first validates that the compile entry has a non-empty argument list, returning a <code>ScanError</code> if not. It then reads the source file content from <code>entry.normalized_file</code> (falling back to <code>normalize_entry_file</code>) and passes it to <code>scan_module_decl</code> to extract the module declaration and module name, populating the <code>ScanResult</code> early. Next, it creates a compiler instance via <code>create_compiler_instance</code>, configures it for preprocessor‑only execution, and instantiates a <code>ScanAction</code> bound to the <code>ScanResult</code>. The <code>ScanAction</code> (which internally uses <code>ScanPPCallbacks</code>) is called to preprocess the file, capturing include directives and module imports into <code>ScanResult::includes</code> and <code>ScanResult::module_imports</code>. After successful execution, the result is returned; any failure at any stage—file I/O errors, compiler instance creation failure, or preprocessor errors—produces a <code>ScanError</code> describing the issue. Dependencies include <code>scan_module_decl</code>, <code>normalize_entry_file</code>, <code>ScanAction</code>/<code>ScanPPCallbacks</code>, and the <code>CompileEntry</code>/<code>ScanResult</code>/<code>ScanError</code> types.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>reads source file content from the filesystem</li><li>creates a compiler instance (may allocate resources or spawn processes)</li><li>modifies the <code>ScanResult</code> object passed via the action</li><li>modifies frontend options on the compiler instance</li></ul><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>entry.arguments</code></li><li><code>entry.file</code></li><li><code>entry.normalized_file</code></li><li>source file content via <code>std::ifstream</code></li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>result</code> (of type <code>ScanResult</code>)</li><li><code>instance-&gt;getInvocation().getFrontendOpts()</code> fields</li></ul><h4 id="usage-patterns-7" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-7" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called for each source file during project extraction</li><li>used in conjunction with <code>extract_project_async</code> and dependency graph building</li><li>typically invoked after compiling a compilation database entry</li><li>may be called from worker threads in a parallel extraction pipeline</li></ul><h3 id="clore-extract-scan-module-decl" tabindex="-1"><code>clore::extract::scan_module_decl</code> <a class="header-anchor" href="#clore-extract-scan-module-decl" aria-label="Permalink to &quot;`clore::extract::scan_module_decl`&quot;">​</a></h3><p>Declaration: <code>extract/scan.cppm:49</code></p><p>Definition: <code>extract/scan.cppm:141</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>Implementation: <a href="./functions/scan-module-decl">Implementation</a></p><p>The implementation of <code>clore::extract::scan_module_decl</code> relies entirely on Clang&#39;s dependency directives scanner (<code>clang::scanSourceForDependencyDirectives</code>) to quickly tokenize the file content without running the full preprocessor. This produces a list of <code>dependency_directives_scan::Directive</code> entries, which are then iterated to identify module and import declarations. For each <code>cxx_module_decl</code> or <code>cxx_export_module_decl</code> directive, the function walks the associated tokens past the <code>module</code> keyword, collecting name tokens (identifiers, dots, colons) while skipping whitespace and ignoring punctuation-only tokens that indicate a module fragment. The collected name becomes <code>result.module_name</code>, and <code>result.is_interface_unit</code> is set based on whether the declaration was <code>export module</code>. For each <code>cxx_import_decl</code> directive, tokens after the <code>import</code> keyword are concatenated into an import name, which is then normalized via <code>normalize_partition_import</code> and inserted into <code>result.module_imports</code> after a duplicate check. If the scanner fails (returns non‑zero), the function silently returns without modifying the result, leaving it in its default state.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>modifies the <code>module_name</code>, <code>is_interface_unit</code>, and <code>module_imports</code> fields of the provided <code>ScanResult</code> object</li></ul><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the <code>file_content</code> string parameter</li><li>tokens and directives produced by <code>clang::scanSourceForDependencyDirectives</code></li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>result.module_name</code></li><li><code>result.is_interface_unit</code></li><li><code>result.module_imports</code></li></ul><h4 id="usage-patterns-8" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-8" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called by <code>clore::extract::scan_file</code> to extract module information from source content</li><li>used as a fast alternative to full preprocessing for module detection</li></ul><h3 id="clore-extract-topological-order" tabindex="-1"><code>clore::extract::topological_order</code> <a class="header-anchor" href="#clore-extract-topological-order" aria-label="Permalink to &quot;`clore::extract::topological_order`&quot;">​</a></h3><p>Declaration: <code>extract/scan.cppm:66</code></p><p>Definition: <code>extract/scan.cppm:495</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The function constructs an adjacency list and an in‑degree map from the provided <code>graph.files</code> and <code>graph.edges</code>. Each file is initialized with an in‑degree of zero, then every <code>DependencyEdge</code> is processed by adding <code>edge.from</code> to the adjacency list of <code>edge.to</code> and incrementing the in‑degree of <code>edge.from</code>. This inverted adjacency (mapping a file to its dependents) matches the expected interface of <code>clore::support::topological_order</code>, which receives the file list, the adjacency map, and the in‑degree map. If the support function returns a cycle (no valid order), the function returns a <code>std::unexpected</code> containing a <code>ScanError</code> with a descriptive message. Otherwise, the computed topological order is forwarded to the caller.</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>graph<code>.files</code></li><li>graph<code>.edges</code></li></ul><h4 id="usage-patterns-9" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-9" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Ordering files for sequential processing according to include dependencies.</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>extract:scan</code> module builds on three direct imports—<code>extract:compiler</code> for compilation database entries, <code>support</code> for file I/O and cache-key utilities, and the C++ standard library—to provide the core scanning pipeline. Its public surface is split into two major operations: synchronous file scanning via <code>scan_file</code> and <code>scan_module_decl</code> for lightweight module declaration extraction, and asynchronous dependency‑graph construction through <code>build_dependency_graph_async</code> and <code>topological_order</code>. These functions rely on shared data types (<code>ScanResult</code>, <code>ScanError</code>, <code>DependencyGraph</code>, <code>DependencyEdge</code>, <code>IncludeInfo</code>) and a persistent <code>ScanCache</code> that callers must invalidate when the compilation database or filesystem state changes.</p><p>Internally, the module is layered into an anonymous‑namespace implementation that encapsulates the Clang‑based preprocessor interaction. A <code>ScanPPCallbacks</code> class captures inclusion directives and module imports during a preprocessor run, driven by a <code>ScanAction</code> that wraps the scanning logic for a single file. Preparation and deduplication are handled by <code>PreparedScanEntry</code> and <code>MissingScanTask</code> structures, while <code>normalize_partition_import</code> and <code>prepare_scan_entry</code> provide lightweight normalization and cache‑key computation. The asynchronous graph builder uses a <code>kota::event_loop</code> to schedule scan tasks, manage a work queue of missing entries, and produce a topological ordering, with all intermediate state kept in local variables (e.g., <code>prepared_entries</code>, <code>missing_task_by_cache_key</code>, <code>scanned_results</code>) rather than exposed in the public interface.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./compiler">Module extract:compiler</a></li><li><a href="./../support/">Module support</a></li></ul>',134)])])}const m=a(r,[["render",n]]);export{p as __pageData,m as default};
