import{_ as c,C as i,o as a,c as t,a3 as d,b as l,w as o,a as n,E as s,a4 as m}from"./chunks/framework.aYYe0dfI.js";const k=JSON.parse('{"title":"Module generate:diagram","description":"模块 generate:diagram 负责将符号分析结果（模块、命名空间、文件依赖关系等）渲染为可视化的 Mermaid 图表代码。它提供一组面向外部的渲染函数（如 render_import_diagram_code、render_module_dependency_diagram_code、render_namespace_diagram_code、render_file_dependency_diagram_code），以及 should_emit_mermaid 和 escape_mermaid_label 等辅助工具，用于控制图表的启用条件与标签转义。这些函数接受符号或上下文的整数标识符，返回图表文本或状态码，调用者无需了解内部渲染细节。","frontmatter":{"title":"Module generate:diagram","description":"模块 generate:diagram 负责将符号分析结果（模块、命名空间、文件依赖关系等）渲染为可视化的 Mermaid 图表代码。它提供一组面向外部的渲染函数（如 render_import_diagram_code、render_module_dependency_diagram_code、render_namespace_diagram_code、render_file_dependency_diagram_code），以及 should_emit_mermaid 和 escape_mermaid_label 等辅助工具，用于控制图表的启用条件与标签转义。这些函数接受符号或上下文的整数标识符，返回图表文本或状态码，调用者无需了解内部渲染细节。","layout":"doc","template":"doc"},"headers":[],"relativePath":"zh/clore/modules/generate/diagram.md","filePath":"zh/clore/modules/generate/diagram.md"}'),_={name:"zh/clore/modules/generate/diagram.md"};function p(u,e,h,g,f,b){const r=i("Mermaid");return a(),t("div",null,[e[1]||(e[1]=d('<h1 id="module-generate-diagram" tabindex="-1">Module <code>generate:diagram</code> <a class="header-anchor" href="#module-generate-diagram" aria-label="Permalink to &quot;Module `generate:diagram`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>模块 <code>generate:diagram</code> 负责将符号分析结果（模块、命名空间、文件依赖关系等）渲染为可视化的 Mermaid 图表代码。它提供一组面向外部的渲染函数（如 <code>render_import_diagram_code</code>、<code>render_module_dependency_diagram_code</code>、<code>render_namespace_diagram_code</code>、<code>render_file_dependency_diagram_code</code>），以及 <code>should_emit_mermaid</code> 和 <code>escape_mermaid_label</code> 等辅助工具，用于控制图表的启用条件与标签转义。这些函数接受符号或上下文的整数标识符，返回图表文本或状态码，调用者无需了解内部渲染细节。</p><p>该模块的实现完全封装在 <code>generate/render/diagram.cppm</code> 中，依赖 <code>config</code>、<code>extract</code>、<code>generate:model</code> 和 <code>support</code> 模块提供的配置、提取和模型数据。内部通过匿名命名空间中的辅助函数（如 <code>collect_implementation_symbols_for_diagram</code>、<code>render_cached_diagram</code>）组织图表构建逻辑，并利用 <code>node_id</code>、<code>edge_count</code>、<code>node_count</code>、<code>result</code> 等局部变量存储中间状态，最终输出可供嵌入文档的 Mermaid 代码。</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../config/"><code>config</code></a></li><li><a href="./../extract/"><code>extract</code></a></li><li><a href="./model"><code>generate:model</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./scheduler"><code>generate:scheduler</code></a></li><li><a href="./symbol"><code>generate:symbol</code></a></li></ul><h2 id="dependency-diagram" tabindex="-1">Dependency Diagram <a class="header-anchor" href="#dependency-diagram" aria-label="Permalink to &quot;Dependency Diagram&quot;">​</a></h2>',9)),(a(),l(m,null,{default:o(()=>[s(r,{id:"mermaid-60",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20M0%5B%22generate%22%5D%0A%20%20%20%20I0%5B%22config%22%5D%0A%20%20%20%20I0%20--%3E%20M0%0A%20%20%20%20I1%5B%22extract%22%5D%0A%20%20%20%20I1%20--%3E%20M0%0A%20%20%20%20I2%5B%22support%22%5D%0A%20%20%20%20I2%20--%3E%20M0%0A"})]),fallback:o(()=>[...e[0]||(e[0]=[n(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=d('<h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-generate-escape-mermaid-label" tabindex="-1"><code>clore::generate::escape_mermaid_label</code> <a class="header-anchor" href="#clore-generate-escape-mermaid-label" aria-label="Permalink to &quot;`clore::generate::escape_mermaid_label`&quot;">​</a></h3><p>Declaration: <code>generate/render/diagram.cppm:13</code></p><p>Definition: <code>generate/render/diagram.cppm:109</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数遍历输入字符串中的每个字符，通过一个 <code>switch</code> 语句处理三种转义情况：反斜杠 <code>\\\\</code> 和双引号 <code>&quot;</code> 分别被替换为转义序列 <code>\\\\\\\\</code> 和 <code>\\\\&quot;</code>，换行符与回车符统一替换为空格字符，其余字符原样保留。所有转义结果依次追加到预先分配好容量的 <code>std::string</code> 对象中，最终返回转义后的字符串。整个实现不依赖任何外部库或模块，仅使用标准库的字符串操作。</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>text</code> parameter (the input string)</li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returns a new <code>std::string</code></li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used when rendering Mermaid diagram code to ensure labels do not break the diagram syntax.</li></ul><h3 id="clore-generate-render-file-dependency-diagram-code" tabindex="-1"><code>clore::generate::render_file_dependency_diagram_code</code> <a class="header-anchor" href="#clore-generate-render-file-dependency-diagram-code" aria-label="Permalink to &quot;`clore::generate::render_file_dependency_diagram_code`&quot;">​</a></h3><p>Declaration: <code>generate/render/diagram.cppm:20</code></p><p>Definition: <code>generate/render/diagram.cppm:222</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数首先检查 <code>plan.owner_keys</code> 是否为空，若为空则直接返回空字符串。随后调用 <code>render_cached_diagram</code> 并传入一个 lambda 作为渲染逻辑。在 lambda 内部，通过 <code>model.files</code> 查找当前文件对应的记录，若不存在则返回空字符串。接着从文件记录的 <code>includes</code> 列表构造包含文件标签（使用 <code>make_source_relative</code> 将路径转换为相对于项目根目录的短名，缺失时保留原路径），并经过排序和去重。随后调用 <code>collect_implementation_symbols_for_diagram</code> 收集满足谓词（<code>is_type_kind</code>、<code>is_variable_kind_local</code> 或 <code>is_function_kind</code>）的符号。结合包含文件数量和符号数量计算出总边数 <code>edge_count</code>，再计算总节点数 <code>node_count</code>，并调用 <code>should_emit_mermaid</code> 决定是否实际生成图表；若不应发射则返回空字符串。最后构造 Mermaid 源码：以文件自身作为主节点 <code>F</code>，为每个包含文件创建 <code>I</code> 前缀的节点并添加指向 <code>F</code> 的边，为每个符号创建 <code>S</code> 前缀的节点并添加从 <code>F</code> 指向它们的边，所有标签均经过 <code>escape_mermaid_label</code> 转义，符号名称则通过 <code>short_name_of_local</code> 简化。生成的字符串由 <code>render_cached_diagram</code> 返回。该函数依赖 <code>collect_implementation_symbols_for_diagram</code> 完成符号收集，并通过 <code>render_cached_diagram</code> 提供可能的缓存支持。</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>plan.owner_keys</code></li><li><code>model.files</code></li><li><code>config.project_root</code></li><li><code>file_it-&gt;second.includes</code></li><li><code>symbols</code> (result of <code>collect_implementation_symbols_for_diagram</code>)</li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>local string <code>result</code> (returned)</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called by page rendering functions to generate file dependency diagrams</li><li>Used in documentation generation to produce Mermaid diagram markup</li></ul><h3 id="clore-generate-render-import-diagram-code" tabindex="-1"><code>clore::generate::render_import_diagram_code</code> <a class="header-anchor" href="#clore-generate-render-import-diagram-code" aria-label="Permalink to &quot;`clore::generate::render_import_diagram_code`&quot;">​</a></h3><p>Declaration: <code>generate/render/diagram.cppm:15</code></p><p>Definition: <code>generate/render/diagram.cppm:124</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::render_import_diagram_code</code> 首先通过 <code>render_cached_diagram</code> 包装其核心逻辑以实现结果缓存。内部流程始于对 <code>mod_unit.imports</code> 的判空检查，若为空则直接返回空字符串。随后使用 <code>top_module</code> lambda 从模块全名中提取顶层模块标签（取冒号之前的部分），并调用 <code>is_std_name</code> 判断该标签是否为标准库命名空间，若是则同样提前返回。接着遍历所有导入，对每个导入也提取顶层标签，并利用 <code>seen</code> 集合进行去重、过滤掉与自身模块相同以及标准库标签，将剩余导入存入本地 <code>imports</code> 向量。在得到有效导入列表后，计算 <code>edge_count</code> 和 <code>node_count</code>，通过 <code>should_emit_mermaid</code> 决定是否生成图表；若决定不生成则返回空字符串。最后对 <code>imports</code> 排序，构造 Mermaid 格式的 <code>graph LR</code> 图：根节点 <code>M0</code> 代表当前模块，每个导入作为独立节点（依次命名为 <code>I0</code>、<code>I1</code>……），并向 <code>M0</code> 添加有向边。所有标签均经过 <code>escape_mermaid_label</code> 转义。</p><p>控制流的核心是三元分支：空导入、标准库模块、不满足 <code>should_emit_mermaid</code> 阈值时均提前终止；否则通过去重、排序、构建字符串完成输出。该函数依赖多个辅助工具：<code>render_cached_diagram</code> 提供缓存层，<code>is_std_name</code> 过滤标准库，<code>should_emit_mermaid</code> 控制图表规模（基于节点数和边数），以及 <code>escape_mermaid_label</code> 确保标签安全。所有内部处理均在 lambda 中完成，并由缓存函数统一管理生命周期。</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>mod_unit</code>&#39;s <code>name</code> field</li><li><code>mod_unit</code>&#39;s <code>imports</code> vector</li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>the returned <code>std::string</code> containing Mermaid diagram code</li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during documentation generation to produce import dependency diagrams</li><li>Used in page rendering pipelines for module pages</li></ul><h3 id="clore-generate-render-module-dependency-diagram-code" tabindex="-1"><code>clore::generate::render_module_dependency_diagram_code</code> <a class="header-anchor" href="#clore-generate-render-module-dependency-diagram-code" aria-label="Permalink to &quot;`clore::generate::render_module_dependency_diagram_code`&quot;">​</a></h3><p>Declaration: <code>generate/render/diagram.cppm:24</code></p><p>Definition: <code>generate/render/diagram.cppm:289</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数将实际图生成逻辑委托给 <code>render_cached_diagram</code>，由内部闭包实现 Mermaid 代码的构造。闭包首先遍历 <code>model.modules</code>，筛选出 <code>mod_unit.is_interface</code> 为真的模块接口单元，并利用辅助 lambda <code>top_module</code> 从每个模块全名中提取顶级模块名（即首个冒号之前的部分）。仅处理非标准库（通过 <code>is_std_name</code> 过滤）的模块，将其插入 <code>modules</code> 集合；同时遍历每个接口单元的 <code>imports</code>，对每个非自身、非标准库的导入也提取顶级模块名，并将依赖关系记录在 <code>deps</code> 映射中。若最终 <code>modules</code> 大小不足 2，则直接返回空字符串。随后计算总边数 <code>edge_count</code>，调用 <code>should_emit_mermaid</code> 检查是否值得生成图；若否，同样返回空字符串。</p><p>若需生成，则将模块名排序并分配形如 <code>M0</code>、<code>M1</code> 的节点 ID，使用 <code>escape_mermaid_label</code> 对标签进行转义。构建 <code>graph LR</code> 格式的 Mermaid 代码：先为每个模块添加节点，再按排序顺序遍历 <code>deps</code>，为每个依赖对添加从目标节点指向源节点的边（<code>to --&gt; from</code>）。最终返回完整字符串。该函数依赖 <code>render_cached_diagram</code> 提供缓存机制，并依赖 <code>is_std_name</code>、<code>escape_mermaid_label</code>、<code>should_emit_mermaid</code> 等内部辅助函数。</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>const extract::ProjectModel&amp; model</code></li><li><code>model.modules</code></li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returned <code>std::string</code> (diagram code)</li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during documentation generation to produce module dependency diagram</li><li>result embedded into Mermaid code blocks</li></ul><h3 id="clore-generate-render-namespace-diagram-code" tabindex="-1"><code>clore::generate::render_namespace_diagram_code</code> <a class="header-anchor" href="#clore-generate-render-namespace-diagram-code" aria-label="Permalink to &quot;`clore::generate::render_namespace_diagram_code`&quot;">​</a></h3><p>Declaration: <code>generate/render/diagram.cppm:17</code></p><p>Definition: <code>generate/render/diagram.cppm:168</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::render_namespace_diagram_code</code> 使用缓存机制 <code>render_cached_diagram</code> 生成表示命名空间结构的 Mermaid 图表代码。它首先通过 <code>extract::ProjectModel</code> 中的 <code>namespaces</code> 映射查找指定的 <code>namespace_name</code>；若不存在则返回空字符串。接着收集该命名空间内的类型符号（通过 <code>is_type_kind</code> 过滤、去重并按照 <code>qualified_name</code> 排序）以及子命名空间列表（过滤掉包含 <code>&quot;(anonymous namespace)&quot;</code> 或满足 <code>is_std_name</code> 的项，并使用 <code>short_name_of_local</code> 提取短名称后去重排序）。然后根据节点数（1 + 类型数量 + 子命名空间数量）和边数（类型与子命名空间的总数）调用 <code>should_emit_mermaid</code> 决定是否输出图表；若否，返回空字符串。最后构建 Mermaid <code>graph TD</code> 格式的字符串：根节点为当前命名空间的短名称（经 <code>escape_mermaid_label</code> 转义），每个类型和子命名空间作为独立节点，并从根节点引出箭头连接。生成的字符串由 <code>render_cached_diagram</code> 包装后返回。</p><h4 id="side-effects-4" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-4" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Caches the generated diagram string, potentially modifying an internal cache.</li></ul><h4 id="reads-from-4" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-4" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>extract::ProjectModel</code></li><li><code>namespace_name</code> (<code>string_view</code>)</li><li><code>model.namespaces</code></li><li>namespace symbols</li><li>children</li><li><code>should_emit_mermaid</code></li><li><code>is_type_kind</code></li><li><code>lookup_symbol</code></li><li><code>short_name_of_local</code></li><li><code>escape_mermaid_label</code></li></ul><h4 id="writes-to-4" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-4" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Returned string</li><li>Internal cache via <code>render_cached_diagram</code></li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during documentation generation to produce namespace diagram markdown.</li></ul><h3 id="clore-generate-should-emit-mermaid" tabindex="-1"><code>clore::generate::should_emit_mermaid</code> <a class="header-anchor" href="#clore-generate-should-emit-mermaid" aria-label="Permalink to &quot;`clore::generate::should_emit_mermaid`&quot;">​</a></h3><p>Declaration: <code>generate/render/diagram.cppm:11</code></p><p>Definition: <code>generate/render/diagram.cppm:105</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数根据给定的节点数和边数决定是否应该生成Mermaid图表。它通过将 <code>node_count</code> 与 <code>kMermaidMinNodes</code> 进行不小于比较，以及将 <code>edge_count</code> 与 <code>kMermaidMinEdges</code> 进行不小于比较，若任一条件成立则返回 <code>true</code>。整个控制流仅包含一条返回语句，逻辑直接且无分支或循环。</p><p><code>kMermaidMinNodes</code> 和 <code>kMermaidMinEdges</code> 是内部定义的常量，用于控制图表生成的阈值。该函数被多个上游渲染函数（如 <code>render_module_dependency_diagram_code</code>、<code>render_import_diagram_code</code> 等）作为判定依据，确保仅在图表具有足够信息量时才触发 <code>render_cached_diagram</code> 等渲染操作，从而避免生成规模过小的无意义图表。</p><h4 id="side-effects-5" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-5" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-5" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-5" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>node_count</code></li><li><code>edge_count</code></li><li><code>kMermaidMinNodes</code></li><li><code>kMermaidMinEdges</code></li></ul><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called before rendering mermaid diagrams such as dependency graphs</li><li>used to decide diagram inclusion in documentation pages</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>模块 <code>generate:diagram</code> 是文档生成管线中的图表渲染单元，负责将符号分析结果转换为 Mermaid 格式的结构化图代码。它通过导入 <code>extract</code> 模块获取符号依赖数据，依赖 <code>generate:model</code> 提供的页面计划和模块模型，并借助 <code>config</code> 与 <code>support</code> 模块处理配置与基础文本操作。内部实现沿用了清晰的层级划分：匿名命名空间内封装了 <code>is_std_name</code>、<code>short_name_of_local</code>、<code>is_variable_kind_local</code> 等类型/名称判断工具，以及 <code>collect_implementation_symbols_for_diagram</code> 和 <code>render_cached_diagram</code> 这类通用渲染辅助函数；公开接口则按图类型分离为多个独立的渲染函数（如 <code>render_import_diagram_code</code>、<code>render_file_dependency_diagram_code</code>、<code>render_namespace_diagram_code</code>、<code>render_module_dependency_diagram_code</code>），它们均依赖同一套节点/边计数（<code>kMermaidMinNodes</code>、<code>kMermaidMinEdges</code>）和转义工具（<code>escape_mermaid_label</code>），并通过 <code>should_emit_mermaid</code> 控制输出开关。这种正交分解使得不同种类的图表可独立开发与测试，同时复用底层的标签处理与 Mermaid 生成逻辑。</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../config/">Module config</a></li><li><a href="./../extract/">Module extract</a></li><li><a href="./model">Module generate:model</a></li><li><a href="./../support/">Module support</a></li></ul>',84))])}const P=c(_,[["render",p]]);export{k as __pageData,P as default};
