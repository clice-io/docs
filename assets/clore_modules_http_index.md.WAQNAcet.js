import{_ as t,o as a,c as o,a3 as r}from"./chunks/framework.aYYe0dfI.js";const u=JSON.parse('{"title":"Module http","description":"The http module provides the core networking layer for interacting with LLM APIs. It owns responsibilities ranging from environment‑based configuration discovery to building, dispatching, and processing HTTP requests. Public‑facing entities include the perform_http_request function for synchronous calls, perform_http_request_async for asynchronous invocations driven by an event loop, and the read_environment and read_required_env helpers that parse API credentials and base URLs from system variables. Error handling is consolidated in the LLMError type, while rate‑limiting controls are exposed through initialize_llm_rate_limit and shutdown_llm_rate_limit, backed by a global semaphore and a request counter.","frontmatter":{"title":"Module http","description":"The http module provides the core networking layer for interacting with LLM APIs. It owns responsibilities ranging from environment‑based configuration discovery to building, dispatching, and processing HTTP requests. Public‑facing entities include the perform_http_request function for synchronous calls, perform_http_request_async for asynchronous invocations driven by an event loop, and the read_environment and read_required_env helpers that parse API credentials and base URLs from system variables. Error handling is consolidated in the LLMError type, while rate‑limiting controls are exposed through initialize_llm_rate_limit and shutdown_llm_rate_limit, backed by a global semaphore and a request counter.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/http/index.md","filePath":"en/clore/modules/http/index.md"}'),i={name:"clore/modules/http/index.md"};function n(s,e,l,c,d,h){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="module-http" tabindex="-1">Module <code>http</code> <a class="header-anchor" href="#module-http" aria-label="Permalink to &quot;Module `http`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>http</code> module provides the core networking layer for interacting with LLM <code>APIs</code>. It owns responsibilities ranging from environment‑based configuration discovery to building, dispatching, and processing HTTP requests. Public‑facing entities include the <code>perform_http_request</code> function for synchronous calls, <code>perform_http_request_async</code> for asynchronous invocations driven by an event loop, and the <code>read_environment</code> and <code>read_required_env</code> helpers that parse API credentials and base <code>URLs</code> from system variables. Error handling is consolidated in the <code>LLMError</code> type, while rate‑limiting controls are exposed through <code>initialize_llm_rate_limit</code> and <code>shutdown_llm_rate_limit</code>, backed by a global semaphore and a request counter.</p><p>Internal details such as the <code>EnvironmentConfig</code> and <code>RawHttpResponse</code> structs, timeout constants, the request configuration helper <code>configure_request</code>, and the semaphore guard are part of the implementation scope. Together, these elements form a complete, self‑contained HTTP client tailored for LLM API integration, with support for synchronous and asynchronous workflows, request formatting, and concurrency throttling.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./../anthropic/"><code>anthropic</code></a></li><li><a href="./../client/"><code>client</code></a></li><li><a href="./../generate/scheduler"><code>generate:scheduler</code></a></li><li><a href="./../openai/"><code>openai</code></a></li><li><a href="./../protocol/"><code>protocol</code></a></li><li><a href="./../provider/"><code>provider</code></a></li><li><a href="./../schema/"><code>schema</code></a></li></ul><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-net-llmerror" tabindex="-1"><code>clore::net::LLMError</code> <a class="header-anchor" href="#clore-net-llmerror" aria-label="Permalink to &quot;`clore::net::LLMError`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:23</code></p><p>Definition: <code>network/http.cppm:23</code></p><p>Declaration: <a href="./../../namespaces/clore/net/"><code>Namespace clore::net</code></a></p><p>The struct stores a single <code>std::string</code> member <code>message</code> as its internal payload. Invariants: <code>message</code> is always a valid <code>std::string</code> (default-constructed empty or move-constructed from the argument). The default constructor leaves <code>message</code> empty. The explicit constructor from <code>std::string</code> moves the argument into <code>message</code> to avoid copying. The explicit constructor from <code>kota::error</code> extracts the error description by calling <code>err.message()</code> and stores the result, effectively wrapping a <code>kota::error</code> into a string-based error representation. No additional state or validation logic is present; the struct serves solely as a lightweight wrapper around a single error message string.</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>message</code> holds a human-readable error description</li><li>Default-constructed <code>LLMError</code> has an empty <code>message</code></li><li>Construction from <code>kota::error</code> copies the error&#39;s message</li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>std::string message</code></li><li><code>LLMError()</code> default constructor</li><li><code>explicit LLMError(std::string msg)</code></li><li><code>explicit LLMError(kota::error err)</code></li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Returned or thrown as an error type for LLM-related failures</li><li>Constructed from a <code>kota::error</code> or an explicit message string</li></ul><h4 id="member-functions" tabindex="-1">Member Functions <a class="header-anchor" href="#member-functions" aria-label="Permalink to &quot;Member Functions&quot;">​</a></h4><h5 id="clore-net-llmerror-llmerror" tabindex="-1"><code>clore::net::LLMError::LLMError</code> <a class="header-anchor" href="#clore-net-llmerror-llmerror" aria-label="Permalink to &quot;`clore::net::LLMError::LLMError`&quot;">​</a></h5><p>Declaration: <code>network/http.cppm:30</code></p><p>Definition: <code>network/http.cppm:30</code></p><p>Declaration: <a href="./../../namespaces/clore/net/"><code>Namespace clore::net</code></a></p><h6 id="implementation" tabindex="-1">Implementation <a class="header-anchor" href="#implementation" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">explicit</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LLMError</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kota</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::error err) : </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">message</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(err.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">message</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) {}</span></span></code></pre></div><h5 id="clore-net-llmerror-llmerror-1" tabindex="-1"><code>clore::net::LLMError::LLMError</code> <a class="header-anchor" href="#clore-net-llmerror-llmerror-1" aria-label="Permalink to &quot;`clore::net::LLMError::LLMError`&quot;">​</a></h5><p>Declaration: <code>network/http.cppm:28</code></p><p>Definition: <code>network/http.cppm:28</code></p><p>Declaration: <a href="./../../namespaces/clore/net/"><code>Namespace clore::net</code></a></p><h6 id="implementation-1" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-1" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">explicit</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LLMError</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string msg) : </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">message</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">move</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(msg)) {}</span></span></code></pre></div><h5 id="clore-net-llmerror-llmerror-2" tabindex="-1"><code>clore::net::LLMError::LLMError</code> <a class="header-anchor" href="#clore-net-llmerror-llmerror-2" aria-label="Permalink to &quot;`clore::net::LLMError::LLMError`&quot;">​</a></h5><p>Declaration: <code>network/http.cppm:26</code></p><p>Definition: <code>network/http.cppm:26</code></p><p>Declaration: <a href="./../../namespaces/clore/net/"><code>Namespace clore::net</code></a></p><h6 id="implementation-2" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-2" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">LLMError</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span></span></code></pre></div><h3 id="clore-net-detail-environmentconfig" tabindex="-1"><code>clore::net::detail::EnvironmentConfig</code> <a class="header-anchor" href="#clore-net-detail-environmentconfig" aria-label="Permalink to &quot;`clore::net::detail::EnvironmentConfig`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:37</code></p><p>Definition: <code>network/http.cppm:37</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>The <code>clore::net::detail::EnvironmentConfig</code> struct is a Plain-Old-Data aggregate that serves as an internal container for storing two configuration strings: <code>api_base</code> (the base URL for API requests) and <code>api_key</code> (the authentication key). Both members are <code>std::string</code> objects with no special invariants beyond typical string validity; the struct itself carries no member functions or additional logic, acting solely as a lightweight data carrier for environment settings used deeper in the networking layer. Its primary role is to bundle these two parameters together for convenient, consistent passing through internal interfaces.</p><h4 id="invariants-1" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-1" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>api_base</code> and <code>api_key</code> may be empty or contain configuration values</li></ul><h4 id="key-members-1" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-1" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>api_base</code></li><li><code>api_key</code></li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Passed to network client constructors or initialization functions</li><li>Populated from environment variables or configuration files</li></ul><h3 id="clore-net-detail-rawhttpresponse" tabindex="-1"><code>clore::net::detail::RawHttpResponse</code> <a class="header-anchor" href="#clore-net-detail-rawhttpresponse" aria-label="Permalink to &quot;`clore::net::detail::RawHttpResponse`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:42</code></p><p>Definition: <code>network/http.cppm:42</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>The struct <code>clore::net::detail::RawHttpResponse</code> is a plain aggregate that serves as a lightweight holder for the two essential parts of a raw HTTP response: the status code and the body content. Its only data members are <code>http_status</code> (a <code>long</code> defaulting to <code>0</code>) and <code>body</code> (a <code>std::string</code> defaulting to empty). No user‑defined constructors, assignment <code>operator</code>s, or destructors are declared; the compiler‑generated special member functions perform shallow copies and trivial destruction, which is sufficient for this internal struct. The key invariant is that <code>http_status</code> is expected to hold an integral HTTP status code (e.g., 200, 404) after parsing, while <code>body</code> contains the raw response payload. Because the struct is defined in a <code>detail</code> namespace, it is not part of the public API; its purpose is to be used internally by HTTP parsing machinery to transport the parsed result without additional processing or validation logic.</p><h4 id="invariants-2" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-2" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>http_status</code> is expected to hold a valid HTTP status code, though no validation is performed.</li><li><code>body</code> may be empty, representing an absent response body.</li></ul><h4 id="key-members-2" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-2" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>http_status</code></li><li><code>body</code></li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Constructed after parsing an HTTP response to hold the raw status and body.</li><li>Used internally within the <code>clore::net</code> library, likely passed to higher-level response wrappers.</li></ul><h2 id="variables" tabindex="-1">Variables <a class="header-anchor" href="#variables" aria-label="Permalink to &quot;Variables&quot;">​</a></h2><h3 id="clore-net-detail-g-llm-request-counter" tabindex="-1"><code>clore::net::detail::g_llm_request_counter</code> <a class="header-anchor" href="#clore-net-detail-g-llm-request-counter" aria-label="Permalink to &quot;`clore::net::detail::g_llm_request_counter`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:94</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>It is referenced by the function <code>clore::net::detail::perform_http_request_async</code>, suggesting it tracks the number of HTTP requests made, possibly for rate limiting or logging.</p><h4 id="mutation" tabindex="-1">Mutation <a class="header-anchor" href="#mutation" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Referenced in <code>clore::net::detail::perform_http_request_async</code></li></ul><h3 id="clore-net-detail-g-llm-semaphore" tabindex="-1"><code>clore::net::detail::g_llm_semaphore</code> <a class="header-anchor" href="#clore-net-detail-g-llm-semaphore" aria-label="Permalink to &quot;`clore::net::detail::g_llm_semaphore`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:47</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>This semaphore controls the maximum number of simultaneous LLM HTTP requests. It is initialized by <code>clore::net::initialize_llm_rate_limit</code> and shut down by <code>clore::net::shutdown_llm_rate_limit</code>. Inside <code>clore::net::detail::perform_http_request_async</code>, the semaphore is likely acquired before sending a request and released after completion.</p><h4 id="mutation-sources" tabindex="-1">Mutation Sources <a class="header-anchor" href="#mutation-sources" aria-label="Permalink to &quot;Mutation Sources&quot;">​</a></h4><ul><li><code>clore::net::initialize_llm_rate_limit</code></li><li><code>clore::net::shutdown_llm_rate_limit</code></li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Acquired before performing an LLM HTTP request in <code>perform_http_request_async</code></li><li>Released after the request completes</li></ul><h3 id="clore-net-detail-khttpconnecttimeoutms" tabindex="-1"><code>clore::net::detail::kHttpConnectTimeoutMs</code> <a class="header-anchor" href="#clore-net-detail-khttpconnecttimeoutms" aria-label="Permalink to &quot;`clore::net::detail::kHttpConnectTimeoutMs`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:96</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>Used in <code>configure_request</code> to set the connect timeout on outgoing HTTP connections, ensuring requests are not left hanging if the server does not respond within the specified period.</p><h4 id="mutation-1" tabindex="-1">Mutation <a class="header-anchor" href="#mutation-1" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Referenced in <code>configure_request</code> to set connect timeout on HTTP requests</li></ul><h3 id="clore-net-detail-khttprequesttimeout" tabindex="-1"><code>clore::net::detail::kHttpRequestTimeout</code> <a class="header-anchor" href="#clore-net-detail-khttprequesttimeout" aria-label="Permalink to &quot;`clore::net::detail::kHttpRequestTimeout`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:97</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>Used to set the timeout on HTTP requests made by the networking layer. Likely passed to underlying HTTP client functions to control how long to wait for a response.</p><h4 id="mutation-2" tabindex="-1">Mutation <a class="header-anchor" href="#mutation-2" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h4 id="usage-patterns-6" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-6" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>HTTP request timeout configuration</li><li>passed to HTTP client functions</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-net-detail-configure-request" tabindex="-1"><code>clore::net::detail::configure_request</code> <a class="header-anchor" href="#clore-net-detail-configure-request" aria-label="Permalink to &quot;`clore::net::detail::configure_request`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:126</code></p><p>Definition: <code>network/http.cppm:126</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>The function accepts a <code>kota::http::request</code> and a span of <code>kota::http::header</code> objects along with the request body as a <code>std::string</code>. It iterates over each header in the provided span, calling <code>request.header(header.name, header.value)</code> to attach them. Afterwards, it assigns the serialised <code>request_json</code> to the request body via <code>request.body()</code>. Several curl options are configured: <code>CURLOPT_CONNECTTIMEOUT_MS</code> is set to the constant <code>kHttpConnectTimeoutMs</code>, <code>CURLOPT_NOSIGNAL</code> is set to <code>1L</code> to avoid signal-based timeouts, and <code>CURLOPT_TCP_KEEPALIVE</code> is enabled with <code>1L</code>. No branching or error handling occurs; the function is purely a mutating preparation step for the HTTP request object.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>mutates the <code>request</code> object by adding headers, setting its body, and modifying curl options</li></ul><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>headers</code> parameter (span of headers)</li><li><code>request_json</code> parameter (string moved into request)</li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>request</code> parameter (mutable reference to <code>kota::http::request</code>)</li></ul><h4 id="usage-patterns-7" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-7" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called as part of HTTP request construction pipeline</li><li>used in <code>perform_http_request</code> or similar functions to finalize request setup</li></ul><h3 id="clore-net-detail-perform-http-request" tabindex="-1"><code>clore::net::detail::perform_http_request</code> <a class="header-anchor" href="#clore-net-detail-perform-http-request" aria-label="Permalink to &quot;`clore::net::detail::perform_http_request`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:52</code></p><p>Definition: <code>network/http.cppm:139</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>The function acts as a synchronous wrapper around the internal <code>perform_http_request_async</code> coroutine. It creates a local <code>async::event_loop</code> instance, copies the caller&#39;s <code>url</code>, <code>headers</code>, and <code>request_json</code> into owning containers required by the async variant, and then schedules the resulting operation on the loop. After running the loop to completion, it inspects the operation&#39;s outcome: if the operation was cancelled, it returns an <code>LLMError</code> with a cancellation message; if an error occurred, it moves the error into an unexpected result; otherwise, it moves the successful <code>RawHttpResponse</code> into an expected value. This function depends on the async infrastructure (<code>async::event_loop</code>, <code>perform_http_request_async</code>) and on <code>LLMError</code> for error reporting.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>performs an HTTP request via <code>perform_http_request_async</code></li><li>creates and runs an event loop</li><li>allocates and copies headers and request string</li><li>returns a result or error</li></ul><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>url parameter</li><li>headers parameter (span of headers)</li><li><code>request_json</code> parameter</li><li>result of async operation</li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>event loop (schedule, run)</li><li>result variable (moved from operation)</li><li>returned expected value</li></ul><h4 id="usage-patterns-8" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-8" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>synchronous wrapper over async HTTP request</li><li>used to perform HTTP requests with async internal implementation</li></ul><h3 id="clore-net-detail-perform-http-request-async" tabindex="-1"><code>clore::net::detail::perform_http_request_async</code> <a class="header-anchor" href="#clore-net-detail-perform-http-request-async" aria-label="Permalink to &quot;`clore::net::detail::perform_http_request_async`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:57</code></p><p>Definition: <code>network/http.cppm:165</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>The function <code>clore::net::detail::perform_http_request_async</code> is a coroutine that returns <code>async::task&lt;RawHttpResponse, LLMError&gt;</code>, performing an asynchronous HTTP POST request to an LLM endpoint. Upon entry, it checks the global semaphore <code>g_llm_semaphore</code>; if active, it co-awaits an acquire before creating a <code>SemaphoreGuard</code> that will release the semaphore on scope exit. It then fetches and logs a monotonically increasing request number from <code>g_llm_request_counter</code> along with the target <code>url</code>.</p><p>A <code>kota::http::client</code> is configured with <code>kHttpRequestTimeout</code> and a POST request is built on the provided <code>async::event_loop</code>. The helper <code>configure_request</code> attaches the given <code>headers</code> and <code>request_json</code> body. After sending the request via <code>co_await request.send().catch_cancel()</code>, the function handles three outcomes: cancellation produces an <code>LLMError</code> with a cancellation message; a transport error produces an <code>LLMError</code> with the error&#39;s message; on success, a <code>RawHttpResponse</code> is constructed from the response status and body text, logged, and co-returned. The control flow relies on <code>async::task</code>, the global semaphore for rate limiting, and the <code>kota::http</code> networking primitives.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Acquires global semaphore <code>g_llm_semaphore</code> if present</li><li>Releases global semaphore <code>g_llm_semaphore</code> on return</li><li>Atomically increments global counter <code>g_llm_request_counter</code></li><li>Sends an HTTP POST request over the network</li><li>Logs request and response information via <code>logging::info</code> and <code>logging::warn</code></li></ul><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>Global semaphore <code>g_llm_semaphore</code></li><li>Global counter <code>g_llm_request_counter</code></li><li>Parameter <code>url</code></li><li>Parameter <code>headers</code></li><li>Parameter <code>request_json</code></li><li>Parameter <code>loop</code> (event loop reference)</li><li>Timeout constant <code>kHttpRequestTimeout</code></li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Global counter <code>g_llm_request_counter</code> (incremented via <code>fetch_add</code>)</li><li>Local variables <code>client</code>, <code>request</code>, <code>response</code>, <code>raw_response</code></li><li>Logging output</li></ul><h4 id="usage-patterns-9" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-9" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called as part of an <code>async::task</code> coroutine in the LLM request pipeline</li><li>Used by higher-level async LLM functions to perform the actual HTTP call</li></ul><h3 id="clore-net-detail-read-environment" tabindex="-1"><code>clore::net::detail::read_environment</code> <a class="header-anchor" href="#clore-net-detail-read-environment" aria-label="Permalink to &quot;`clore::net::detail::read_environment`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:49</code></p><p>Definition: <code>network/http.cppm:108</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>The implementation of <code>clore::net::detail::read_environment</code> follows a straightforward two-step validation pattern. It first calls <code>read_required_env</code> with the <code>base_env</code> parameter to obtain the API base URL, and then calls <code>read_required_env</code> again with the <code>key_env</code> parameter to obtain the API key. Each call is checked immediately: if the result does not have a value, the function returns <code>std::unexpected</code> containing the moved <code>LLMError</code> from the failed result. Only when both environment variables are successfully retrieved does the function construct and return an <code>EnvironmentConfig</code> by moving the extracted <code>api_base</code> and <code>api_key</code> values into its designated initializer fields.</p><p>The function depends entirely on <code>read_required_env</code> for environment‑variable lookup and error handling, and on <code>EnvironmentConfig</code> as the output type. There is no branching beyond the two guard clauses, no asynchronous operations, and no direct interaction with HTTP requests or rate‑limiting constructs—those concerns are handled by callers or by deeper layers invoked later in the pipeline.</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Reads process environment variables</li></ul><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>process environment variables named by <code>base_env</code> and <code>key_env</code></li><li><code>read_required_env</code> function</li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returned <code>EnvironmentConfig</code> object (moved members)</li><li>error state via <code>std::unexpected</code></li></ul><h4 id="usage-patterns-10" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-10" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during network stack initialization to retrieve API credentials from environment</li></ul><h3 id="clore-net-detail-read-required-env" tabindex="-1"><code>clore::net::detail::read_required_env</code> <a class="header-anchor" href="#clore-net-detail-read-required-env" aria-label="Permalink to &quot;`clore::net::detail::read_required_env`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:99</code></p><p>Definition: <code>network/http.cppm:99</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>The function <code>clore::net::detail::read_required_env</code> first converts the input <code>std::string_view name</code> to a null‑terminated C string via <code>std::string</code>. It then calls <code>std::getenv</code> to retrieve the environment variable value. If the returned pointer is <code>nullptr</code> or points to an empty string (first character <code>\\0</code>), the function returns <code>std::unexpected</code> containing a <code>clore::net::LLMError</code> constructed with a formatted message indicating which variable is missing. Otherwise, it returns a <code>std::expected</code> success containing a <code>std::string</code> copy of the value. This function depends on the standard library environment query (<code>std::getenv</code>) and the custom error type (<code>clore::net::LLMError</code>) for error reporting. It does not perform any network calls or involve other module components beyond string formatting.</p><h4 id="side-effects-4" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-4" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>reads environment variable via <code>std::getenv</code></li><li>allocates heap memory for the returned string and error message</li></ul><h4 id="reads-from-4" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-4" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>name</code></li><li>process environment (via <code>std::getenv</code>)</li></ul><h4 id="writes-to-4" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-4" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>return value (string or <code>LLMError</code>)</li><li>heap memory for string and error message</li></ul><h4 id="usage-patterns-11" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-11" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>reading required configuration from environment variables</li><li>validating presence of mandatory environment variables</li></ul><h3 id="clore-net-detail-unwrap-caught-result" tabindex="-1"><code>clore::net::detail::unwrap_caught_result</code> <a class="header-anchor" href="#clore-net-detail-unwrap-caught-result" aria-label="Permalink to &quot;`clore::net::detail::unwrap_caught_result`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:63</code></p><p>Definition: <code>network/http.cppm:63</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>The function checks for cancellation first by calling <code>R::is_cancelled()</code> on the incoming <code>result</code>. If the result was cancelled, it <code>co_awaits</code> <code>kota::fail(LLMError(...))</code> using the provided <code>cancel_message</code> to construct the error. If the result is not cancelled, it then checks <code>R::has_error()</code>. When an error is present, the function <code>co_awaits</code> <code>kota::fail(...)</code> with the error moved from <code>result</code>. Finally, if neither condition holds, it <code>co_returns</code> the value extracted from the result via <code>std::move(*result)</code>. The function relies on <code>kota::task</code> and <code>kota::fail</code> from the coroutine library, and on <code>LLMError</code> as the error type for failure signals. The control flow is strictly sequential: cancellation takes precedence over error reporting, and a valid value is only produced when neither condition is met.</p><h4 id="side-effects-5" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-5" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>May cancel or fail the coroutine task via <code>co_await</code> <code>kota::fail</code></li></ul><h4 id="reads-from-5" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-5" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>result parameter (via <code>is_cancelled()</code>, <code>has_error()</code>, and dereference)</li><li><code>cancel_message</code> parameter</li></ul><h4 id="writes-to-5" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-5" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>coroutine task result (via <code>co_return</code> or failure)</li><li>result (moves error out)</li></ul><h4 id="usage-patterns-12" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-12" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to convert a caught result into a coroutine task that either yields the value or fails</li><li>Called in async contexts where a result may be cancelled or erroneous</li></ul><h3 id="clore-net-initialize-llm-rate-limit" tabindex="-1"><code>clore::net::initialize_llm_rate_limit</code> <a class="header-anchor" href="#clore-net-initialize-llm-rate-limit" aria-label="Permalink to &quot;`clore::net::initialize_llm_rate_limit`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:19</code></p><p>Definition: <code>network/http.cppm:78</code></p><p>Declaration: <a href="./../../namespaces/clore/net/"><code>Namespace clore::net</code></a></p><p>The function <code>clore::net::initialize_llm_rate_limit</code> modifies the module‑global rate‑limiting semaphore <code>detail::g_llm_semaphore</code>. If the provided <code>rate_limit</code> is zero, the semaphore is reset (set to <code>nullptr</code>), effectively disabling rate limiting for subsequent requests. Otherwise, a new <code>kota::semaphore</code> is constructed with a capacity equal to the given <code>rate_limit</code> (cast to <code>std::ptrdiff_t</code>). This semaphore is later acquired via a <code>SemaphoreGuard</code> inside <code>detail::perform_http_request_async</code> to control concurrency of LLM API calls. No other dependencies are invoked, and no error handling is performed; the function simply replaces or clears the shared synchronization primitive.</p><h4 id="side-effects-6" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-6" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Resets or creates the global LLM semaphore <code>detail::g_llm_semaphore</code>.</li></ul><h4 id="reads-from-6" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-6" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>rate_limit</code> parameter</li></ul><h4 id="writes-to-6" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-6" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>detail::g_llm_semaphore</code> global variable</li></ul><h4 id="usage-patterns-13" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-13" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during initialization to set the LLM rate limit.</li><li>Can be called with zero to disable rate limiting.</li></ul><h3 id="clore-net-shutdown-llm-rate-limit" tabindex="-1"><code>clore::net::shutdown_llm_rate_limit</code> <a class="header-anchor" href="#clore-net-shutdown-llm-rate-limit" aria-label="Permalink to &quot;`clore::net::shutdown_llm_rate_limit`&quot;">​</a></h3><p>Declaration: <code>network/http.cppm:21</code></p><p>Definition: <code>network/http.cppm:223</code></p><p>Declaration: <a href="./../../namespaces/clore/net/"><code>Namespace clore::net</code></a></p><p>The implementation of <code>clore::net::shutdown_llm_rate_limit</code> is a single direct invocation of <code>detail::g_llm_semaphore.reset()</code>. This call immediately clears the internal state of the global counting semaphore <code>clore::net::detail::g_llm_semaphore</code>, releasing any threads currently blocked on the semaphore and disabling future rate‑limiting enforcement until the semaphore is re‑initialized (e.g., via <code>clore::net::initialize_llm_rate_limit</code>). No error handling or external dependencies beyond the semaphore object are involved; the function is marked <code>noexcept</code> and simply resets the synchronization primitive.</p><h4 id="side-effects-7" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-7" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Reset of the global semaphore <code>detail::g_llm_semaphore</code></li></ul><h4 id="reads-from-7" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-7" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>global variable <code>detail::g_llm_semaphore</code></li></ul><h4 id="writes-to-7" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-7" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>global variable <code>detail::g_llm_semaphore</code></li></ul><h4 id="usage-patterns-14" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-14" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during shutdown or reinitialization of LLM rate limiting</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>http</code> module, a component of the <code>clore::net</code> namespace, is decomposed into a minimal public API and a more extensive internal implementation. The public side provides <code>initialize_llm_rate_limit</code> and <code>shutdown_llm_rate_limit</code> to manage a global rate‑limiting subsystem, plus the <code>LLMError</code> type for uniform error propagation. Everything else lives in the <code>clore::net::detail</code> namespace, which encapsulates environment configuration (<code>EnvironmentConfig</code>), raw HTTP response handling (<code>RawHttpResponse</code>), request preparation (<code>configure_request</code>), and both synchronous (<code>perform_http_request</code>) and asynchronous (<code>perform_http_request_async</code>) HTTP execution. The module imports <code>std</code> for standard types and the <code>support</code> module for UTF‑8 text processing, file I/O, and structured logging. Under the hood, a global semaphore and an atomic request counter enforce concurrency limits, while constants like <code>kHttpRequestTimeout</code> and <code>kHttpConnectTimeoutMs</code> define default timeouts. This layering keeps the public surface focused and allows the internal helpers to evolve independently, all built on the foundational utilities provided by the <code>support</code> module.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../support/">Module support</a></li></ul>',205)])])}const m=t(i,[["render",n]]);export{u as __pageData,m as default};
