import{_ as t,C as c,o as a,c as s,a3 as o,b as i,w as r,a as l,E as n,a4 as p}from"./chunks/framework.aYYe0dfI.js";const k=JSON.parse('{"title":"Module generate:analysis","description":"generate::analysis 模块负责将原始模型输出解析、规范化并合并为结构化的符号分析结果，构成文档生成管线中分析处理的核心层。它公开了从提示构建、响应解析、回退分析存储到分析结果应用的全套实现，包括 parse_structured_response、normalize_markdown_fragment、parse_markdown_prompt_output、build_symbol_analysis_prompt、apply_symbol_analysis_response 以及针对函数、类型、变量的专用解析与合并函数。该模块还提供提示种类判定函数（如 is_declaration_summary_prompt、is_base_symbol_analysis_prompt）和规范化工具，确保所有分析数据在进入后续生成步骤前保持一致、可用的格式。","frontmatter":{"title":"Module generate:analysis","description":"generate::analysis 模块负责将原始模型输出解析、规范化并合并为结构化的符号分析结果，构成文档生成管线中分析处理的核心层。它公开了从提示构建、响应解析、回退分析存储到分析结果应用的全套实现，包括 parse_structured_response、normalize_markdown_fragment、parse_markdown_prompt_output、build_symbol_analysis_prompt、apply_symbol_analysis_response 以及针对函数、类型、变量的专用解析与合并函数。该模块还提供提示种类判定函数（如 is_declaration_summary_prompt、is_base_symbol_analysis_prompt）和规范化工具，确保所有分析数据在进入后续生成步骤前保持一致、可用的格式。","layout":"doc","template":"doc"},"headers":[],"relativePath":"zh/clore/modules/generate/analysis.md","filePath":"zh/clore/modules/generate/analysis.md"}'),m={name:"zh/clore/modules/generate/analysis.md"};function u(h,e,f,_,y,b){const d=c("Mermaid");return a(),s("div",null,[e[1]||(e[1]=o('<h1 id="module-generate-analysis" tabindex="-1">Module <code>generate:analysis</code> <a class="header-anchor" href="#module-generate-analysis" aria-label="Permalink to &quot;Module `generate:analysis`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p><code>generate::analysis</code> 模块负责将原始模型输出解析、规范化并合并为结构化的符号分析结果，构成文档生成管线中分析处理的核心层。它公开了从提示构建、响应解析、回退分析存储到分析结果应用的全套实现，包括 <code>parse_structured_response</code>、<code>normalize_markdown_fragment</code>、<code>parse_markdown_prompt_output</code>、<code>build_symbol_analysis_prompt</code>、<code>apply_symbol_analysis_response</code> 以及针对函数、类型、变量的专用解析与合并函数。该模块还提供提示种类判定函数（如 <code>is_declaration_summary_prompt</code>、<code>is_base_symbol_analysis_prompt</code>）和规范化工具，确保所有分析数据在进入后续生成步骤前保持一致、可用的格式。</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../config/"><code>config</code></a></li><li><a href="./../extract/"><code>extract</code></a></li><li><a href="./evidence"><code>generate:evidence</code></a></li><li><a href="./markdown"><code>generate:markdown</code></a></li><li><a href="./model"><code>generate:model</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./dryrun"><code>generate:dryrun</code></a></li><li><a href="./scheduler"><code>generate:scheduler</code></a></li></ul><h2 id="dependency-diagram" tabindex="-1">Dependency Diagram <a class="header-anchor" href="#dependency-diagram" aria-label="Permalink to &quot;Dependency Diagram&quot;">​</a></h2>',8)),(a(),i(p,null,{default:r(()=>[n(d,{id:"mermaid-67",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20M0%5B%22generate%22%5D%0A%20%20%20%20I0%5B%22config%22%5D%0A%20%20%20%20I0%20--%3E%20M0%0A%20%20%20%20I1%5B%22extract%22%5D%0A%20%20%20%20I1%20--%3E%20M0%0A%20%20%20%20I2%5B%22support%22%5D%0A%20%20%20%20I2%20--%3E%20M0%0A"})]),fallback:r(()=>[...e[0]||(e[0]=[l(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=o('<h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-generate-analysis-prompt-kind-for-symbol" tabindex="-1"><code>clore::generate::analysis_prompt_kind_for_symbol</code> <a class="header-anchor" href="#clore-generate-analysis-prompt-kind-for-symbol" aria-label="Permalink to &quot;`clore::generate::analysis_prompt_kind_for_symbol`&quot;">​</a></h3><p>Declaration: <code>generate/analysis.cppm:27</code></p><p>Definition: <code>generate/analysis.cppm:286</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数根据符号种类确定分析提示类型。它依次调用 <code>is_function_kind</code>、<code>is_type_kind</code> 和 <code>is_variable_kind</code> 检测 <code>sym.kind</code>，若匹配则分别返回 <code>PromptKind::FunctionAnalysis</code>、<code>PromptKind::TypeAnalysis</code> 或 <code>PromptKind::VariableAnalysis</code>；若均不匹配则返回 <code>std::nullopt</code>。控制流为简单的分支顺序判断，依赖 <code>PromptKind</code> 枚举及三个种类谓词函数。</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>sym</code> parameter</li><li><code>sym.kind</code></li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to map a <code>SymbolInfo</code> to a <code>PromptKind</code> for analysis</li><li>Called during analysis prompt generation to determine which prompt to use for a symbol</li></ul><h3 id="clore-generate-apply-symbol-analysis-response" tabindex="-1"><code>clore::generate::apply_symbol_analysis_response</code> <a class="header-anchor" href="#clore-generate-apply-symbol-analysis-response" aria-label="Permalink to &quot;`clore::generate::apply_symbol_analysis_response`&quot;">​</a></h3><p>Declaration: <code>generate/analysis.cppm:39</code></p><p>Definition: <code>generate/analysis.cppm:348</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::apply_symbol_analysis_response</code> 通过一个 <code>PromptKind</code> 驱动的 <code>switch</code> 分支来编排符号分析结果的存储与合并。对于每种分析类型（函数、类型、变量），它首先调用相应的 <code>parse_*_lenient</code> 或 <code>parse_markdown_prompt_output</code> 对原始响应字符串进行宽松解析，若解析失败则立即返回 <code>std::unexpected</code> 包装的 <code>GenerateError</code>。函数和类型分析分支进一步利用已知的 <code>fallback_*_analysis</code> 生成回退数据，再通过 <code>merge_*_analysis</code> 将解析出的结构化分析合并到 <code>SymbolAnalysisStore</code> 中对应条目（例如 <code>analyses.functions[target_key]</code> 或 <code>analyses.types[target_key]</code>）。声明与实现摘要分支直接根据 <code>PromptKind</code> 将解析得到的 Markdown 片段存入 <code>analysis.overview_markdown</code> 或 <code>analysis.details_markdown</code>。变量分析分支则简单地将解析结果赋值给 <code>analyses.variables[target_key]</code>。未预期的 <code>PromptKind</code> 会导致返回错误。该函数依赖 <code>make_symbol_target_key</code> 生成存储键，以及多个位于匿名命名空间中的解析、回退与合并辅助函数。</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Mutates the <code>SymbolAnalysisStore</code> reference by inserting or updating entries in <code>functions</code>, <code>types</code>, or <code>variables</code> maps.</li></ul><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>analyses</code> (to access existing analyses)</li><li><code>sym</code></li><li><code>model</code></li><li><code>kind</code></li><li><code>raw_response</code></li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>analyses.functions[target_key]</code></li><li><code>analyses.types[target_key]</code></li><li><code>analyses.variables[target_key]</code></li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called after receiving a prompt response to persist parsed analysis data</li><li>Used in the generation pipeline to update symbol analysis results</li></ul><h3 id="clore-generate-build-symbol-analysis-prompt" tabindex="-1"><code>clore::generate::build_symbol_analysis_prompt</code> <a class="header-anchor" href="#clore-generate-build-symbol-analysis-prompt" aria-label="Permalink to &quot;`clore::generate::build_symbol_analysis_prompt`&quot;">​</a></h3><p>Declaration: <code>generate/analysis.cppm:46</code></p><p>Definition: <code>generate/analysis.cppm:429</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数根据传入的 <code>PromptKind</code> 选择对应的证据构建函数，通过 <code>switch</code> 语句分发到 <code>build_evidence_for_function_analysis</code>、<code>build_evidence_for_function_declaration_summary</code> 等七个专门化的证据收集器。每个收集器均基于 <code>sym</code>、<code>model</code>、<code>analyses</code> 和 <code>config.project_root</code> 生成一个 <code>EvidencePack</code> 实例。若 <code>kind</code> 不匹配任何已知类型，则直接返回 <code>std::unexpected</code> 错误。之后，将 <code>page_id</code>、<code>prompt_kind</code> 和 <code>subject_name</code> 填入证据包，并调用 <code>build_prompt</code> 将证据渲染为字符串。成功时通过移动语义返回字符串，否则返回构造的错误信息。</p><p>内部流程完全由 <code>kind</code> 驱动，依赖 <code>extract</code> 命名空间下的符号信息与模型访问、<code>build_prompt</code> 的模板化拼装逻辑，以及 <code>prompt_kind_name</code> 与 <code>make_symbol_target_key</code> 等辅助函数。错误处理集中体现在两个 <code>std::unexpected</code> 分支：分别对应未知的 prompt 种类和证据构建失败。</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>sym</code></li><li><code>kind</code></li><li><code>model</code></li><li><code>config</code></li><li><code>config.project_root</code></li><li><code>analyses</code></li><li><code>sym.qualified_name</code></li><li>return values of <code>build_evidence_for_*</code> functions</li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>local <code>evidence</code> object (fields <code>page_id</code>, <code>prompt_kind</code>, <code>subject_name</code>)</li><li>return value (the prompt string or error)</li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used to generate analysis prompts for symbols</li><li>called by prompt generation pipeline</li><li>dispatches based on prompt kind</li></ul><h3 id="clore-generate-is-base-symbol-analysis-prompt" tabindex="-1"><code>clore::generate::is_base_symbol_analysis_prompt</code> <a class="header-anchor" href="#clore-generate-is-base-symbol-analysis-prompt" aria-label="Permalink to &quot;`clore::generate::is_base_symbol_analysis_prompt`&quot;">​</a></h3><p>Declaration: <code>generate/analysis.cppm:31</code></p><p>Definition: <code>generate/analysis.cppm:325</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数通过单一返回语句实现，将传入的 <code>kind</code> 与 <code>PromptKind</code> 中的三个基础分析类别逐个比较：<code>PromptKind::FunctionAnalysis</code>、<code>PromptKind::TypeAnalysis</code> 和 <code>PromptKind::VariableAnalysis</code>。内部流程仅包含一个逻辑或表达式，无分支或循环。唯一的外部依赖是 <code>PromptKind</code> 枚举类型及其预定义的枚举值，这些值用于判断当前提示种类是否属于“基础符号分析”范畴。</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>kind</code> parameter</li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used to filter prompt kinds for base symbol analysis</li><li>called by other analysis functions to categorize prompts</li></ul><h3 id="clore-generate-is-declaration-summary-prompt" tabindex="-1"><code>clore::generate::is_declaration_summary_prompt</code> <a class="header-anchor" href="#clore-generate-is-declaration-summary-prompt" aria-label="Permalink to &quot;`clore::generate::is_declaration_summary_prompt`&quot;">​</a></h3><p>Declaration: <code>generate/analysis.cppm:33</code></p><p>Definition: <code>generate/analysis.cppm:330</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::is_declaration_summary_prompt</code> 的核心逻辑是一个简单的分类判断：它接收一个 <code>PromptKind</code> 值（通过底层 <code>int</code> 参数传递），并依次将其与 <code>PromptKind::FunctionDeclarationSummary</code> 和 <code>PromptKind::TypeDeclarationSummary</code> 进行相等性比较，若匹配任一枚举值则返回 <code>true</code>，否则返回 <code>false</code>。该函数不涉及循环、分支嵌套或依赖外部可变状态，其内部控制流仅由两个短路求值的 <code>||</code> 运算符串联而成。</p><p>该函数的唯一依赖是 <code>PromptKind</code> 枚举类型的定义（包含 <code>FunctionDeclarationSummary</code> 和 <code>TypeDeclarationSummary</code> 两个成员），以及 <code>int</code> 到 <code>PromptKind</code> 的隐式转换（或底层类型一致性）。在符号分析流程中，它充当快速筛选器：用于从已知的 prompt kind 集合中识别出那些对应“函数声明摘要”或“类型声明摘要”的项，从而决定后续是否执行特定的 prompt 构造或响应解析分支。</p><h4 id="side-effects-4" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-4" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-4" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-4" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the <code>kind</code> parameter</li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used in conditional logic to branch based on whether a prompt kind is a declaration summary</li><li>May be called during prompt construction or evidence building</li></ul><h3 id="clore-generate-normalize-markdown-fragment" tabindex="-1"><code>clore::generate::normalize_markdown_fragment</code> <a class="header-anchor" href="#clore-generate-normalize-markdown-fragment" aria-label="Permalink to &quot;`clore::generate::normalize_markdown_fragment`&quot;">​</a></h3><p>Declaration: <code>generate/analysis.cppm:21</code></p><p>Definition: <code>generate/analysis.cppm:267</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::normalize_markdown_fragment</code> 接收原始字符串 <code>raw</code> 和上下文描述 <code>context</code>，返回规范化后的 Markdown 片段或错误。算法首先通过 <code>clore::support::ensure_utf8</code> 保证输入为合法的 UTF-8 编码，随后调用 <code>clore::support::strip_utf8_bom</code> 移除可能存在的 BOM 头。接着调用 <code>trim_trailing_ascii_whitespace</code> 去除尾部空白字符，然后由 <code>contains_non_whitespace</code> 检查是否仅剩空白；若结果为空白，则返回携带格式化错误信息的 <code>std::unexpected</code>。最后调用 <code>normalize_analysis_markdown</code> 进行额外的 Markdown 内容规范化（如清理或格式化），并将最终结果返回。</p><p>该函数的控制流依赖于一系列内部辅助函数：<code>trim_trailing_ascii_whitespace</code>、<code>contains_non_whitespace</code> 和 <code>normalize_analysis_markdown</code>，其中 <code>normalize_analysis_markdown</code> 是负责具体 Markdown 规则的后处理步骤。返回值类型 <code>std::expected&lt;std::string, GenerateError&gt;</code> 提供了错误处理机制，错误信息通过 <code>std::format</code> 生成，包含触发空片段的 <code>context</code> 标识。</p><h4 id="side-effects-5" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-5" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-5" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-5" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>raw</code></li><li>parameter <code>context</code></li><li>call to <code>clore::support::ensure_utf8</code></li><li>call to <code>clore::support::strip_utf8_bom</code></li><li>local variable <code>normalized</code></li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>local variable <code>normalized</code></li><li>return value (moved <code>normalized</code>)</li></ul><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>normalize AI-generated markdown fragments before rendering</li><li>validate and clean markdown snippets for documentation pages</li></ul><h3 id="clore-generate-parse-markdown-prompt-output" tabindex="-1"><code>clore::generate::parse_markdown_prompt_output</code> <a class="header-anchor" href="#clore-generate-parse-markdown-prompt-output" aria-label="Permalink to &quot;`clore::generate::parse_markdown_prompt_output`&quot;">​</a></h3><p>Declaration: <code>generate/analysis.cppm:24</code></p><p>Definition: <code>generate/analysis.cppm:281</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::parse_markdown_prompt_output</code> serves as a thin wrapper around <code>clore::generate::normalize_markdown_fragment</code>. It receives the raw model output (<code>raw</code>) and a context string (<code>context</code>), then directly delegates the entire processing to <code>normalize_markdown_fragment</code>, which handles the actual normalization of the Markdown fragment. The return type is <code>std::expected&lt;std::string, GenerateError&gt;</code>, meaning any parsing or validation errors are propagated from the inner function without additional transformation. The function itself contains no conditional branches, loops, or state management beyond the delegation call.</p><h4 id="side-effects-6" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-6" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-6" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-6" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>raw</li><li>context</li></ul><h4 id="usage-patterns-6" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-6" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used in the generation pipeline to normalize markdown responses from prompt outputs</li></ul><h3 id="clore-generate-parse-structured-response" tabindex="-1"><code>clore::generate::parse_structured_response</code> <a class="header-anchor" href="#clore-generate-parse-structured-response" aria-label="Permalink to &quot;`clore::generate::parse_structured_response`&quot;">​</a></h3><p>Declaration: <code>generate/analysis.cppm:18</code></p><p>Definition: <code>generate/analysis.cppm:252</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::parse_structured_response</code> 首先尝试通过 <code>json::from_json&lt;T&gt;</code> 将传入的 <code>raw</code> 字符串解析为类型 <code>T</code>。若解析失败，立即构造一个 <code>std::unexpected&lt;GenerateError&gt;</code> 返回，错误信息中包含 <code>context</code> 参数与底层解析错误详情。若解析成功，则将结果值转移至局部变量后再调用 <code>normalize_analysis</code> 进行一次后处理规范化，最终返回成功值。该函数的核心依赖是外部的 JSON 解析能力以及内部定义的规范化操作 <code>normalize_analysis</code>。</p><h4 id="side-effects-7" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-7" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>calls <code>normalize_analysis</code> on the deserialized value, which may modify the object&#39;s fields</li></ul><h4 id="reads-from-7" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-7" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the <code>raw</code> input string</li><li>the <code>context</code> input string</li><li>parsed JSON data from <code>raw</code></li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>the returned <code>std::expected&lt;T, GenerateError&gt;</code> value</li><li>the deserialized object of type <code>T</code> via <code>normalize_analysis</code></li></ul><h4 id="usage-patterns-7" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-7" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called to parse a structured JSON response from an AI prompt into an analysis object</li></ul><h3 id="clore-generate-store-fallback-analysis" tabindex="-1"><code>clore::generate::store_fallback_analysis</code> <a class="header-anchor" href="#clore-generate-store-fallback-analysis" aria-label="Permalink to &quot;`clore::generate::store_fallback_analysis`&quot;">​</a></h3><p>Declaration: <code>generate/analysis.cppm:35</code></p><p>Definition: <code>generate/analysis.cppm:335</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::store_fallback_analysis</code> 充当一个调度器，根据符号的种类将回退分析结果写入 <code>SymbolAnalysisStore</code>。内部通过检查 <code>sym.kind</code> 并依次调用 <code>is_function_kind</code>、<code>is_type_kind</code> 和 <code>is_variable_kind</code> 来判断符号类型：对于函数类型调用 <code>fallback_function_analysis</code>，对于类型类型调用 <code>fallback_type_analysis</code>（额外接收 <code>model</code> 参数），对于变量类型调用 <code>fallback_variable_analysis</code>。每个回退函数均在匿名命名空间中定义，负责生成对应符号的默认分析数据。该函数依赖 <code>make_symbol_target_key</code> 构造存储键，并直接按种类索引 <code>analyses</code> 的 <code>functions</code>、<code>types</code> 或 <code>variables</code> 子映射进行赋值，从而在无模型响应或解析失败时提供备选的分析内容。</p><h4 id="side-effects-8" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-8" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Writes to <code>analyses.functions</code>, <code>analyses.types</code>, or <code>analyses.variables</code> depending on the symbol kind</li></ul><h4 id="reads-from-8" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-8" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>sym</code> (specifically <code>sym.kind</code>)</li><li><code>model</code> (passed to <code>fallback_type_analysis</code>)</li><li><code>make_symbol_target_key(sym)</code></li></ul><h4 id="writes-to-4" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-4" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>analyses.functions</code> (if function kind)</li><li><code>analyses.types</code> (if type kind)</li><li><code>analyses.variables</code> (if variable kind)</li></ul><h4 id="usage-patterns-8" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-8" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to insert fallback analysis entries into a <code>SymbolAnalysisStore</code></li><li>Called when primary analysis is unavailable or incomplete</li></ul><h3 id="clore-generate-symbol-prompt-kinds-for-symbol" tabindex="-1"><code>clore::generate::symbol_prompt_kinds_for_symbol</code> <a class="header-anchor" href="#clore-generate-symbol-prompt-kinds-for-symbol" aria-label="Permalink to &quot;`clore::generate::symbol_prompt_kinds_for_symbol`&quot;">​</a></h3><p>Declaration: <code>generate/analysis.cppm:29</code></p><p>Definition: <code>generate/analysis.cppm:299</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数首先调用 <code>clore::generate::analysis_prompt_kind_for_symbol</code> 以获取与给定符号对应的基础提示类型 <code>base_kind</code>。如果 <code>base_kind</code> 没有值，则直接返回空向量。否则，根据 <code>base_kind</code> 的具体值分支：当为 <code>PromptKind::FunctionAnalysis</code> 时，返回包含 <code>*base_kind</code>、<code>PromptKind::FunctionDeclarationSummary</code> 和 <code>PromptKind::FunctionImplementationSummary</code> 的向量；当为 <code>PromptKind::TypeAnalysis</code> 时，返回包含 <code>*base_kind</code>、<code>PromptKind::TypeDeclarationSummary</code> 和 <code>PromptKind::TypeImplementationSummary</code> 的向量；当为 <code>PromptKind::VariableAnalysis</code> 时，仅返回包含 <code>*base_kind</code> 的向量；其他情况返回空向量。函数不直接处理符号本身，其逻辑完全依赖于上游函数 <code>analysis_prompt_kind_for_symbol</code> 的返回值，并通过简单的条件分支映射到预定义的提示类型集合。</p><h4 id="side-effects-9" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-9" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-9" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-9" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>sym</code> parameter of type <code>const extract::SymbolInfo&amp;</code></li><li><code>analysis_prompt_kind_for_symbol(sym)</code> result</li></ul><h4 id="usage-patterns-9" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-9" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to obtain the list of prompts to generate for a symbol</li><li>Called by prompt-building logic to decide which summary prompts to include</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p><code>generate:analysis</code> 模块是文档生成管线中专门处理符号分析结果的核心环节。它从 <code>generate:model</code> 和 <code>generate:evidence</code> 导入数据模型与证据结构，并利用 <code>generate:markdown</code> 和 <code>support</code> 实现文本规范化与格式处理。模块内部采用分层设计：公共接口（如 <code>build_symbol_analysis_prompt</code>、<code>apply_symbol_analysis_response</code>、<code>store_fallback_analysis</code>、<code>parse_structured_response</code>）对外提供提示构建、响应解析和回退写入能力；匿名命名空间内则封装了针对函数、类型、变量的专用解析函数（例如 <code>parse_function_analysis_lenient</code>、<code>parse_type_analysis_lenient</code>）以及合并与规范化的辅助函数（如 <code>normalize_analysis</code>、<code>merge_function_analysis</code>），从而将结构化的原始响应转换为可供下游使用的分析对象，并在解析失败时依赖回退逻辑保证流程的健壮性。</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../config/">Module config</a></li><li><a href="./../extract/">Module extract</a></li><li><a href="./evidence">Module generate:evidence</a></li><li><a href="./markdown">Module generate:markdown</a></li><li><a href="./model">Module generate:model</a></li><li><a href="./../support/">Module support</a></li></ul>',128))])}const P=t(m,[["render",u]]);export{k as __pageData,P as default};
