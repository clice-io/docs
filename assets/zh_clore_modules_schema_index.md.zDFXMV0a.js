import{_ as d,C as i,o as a,c as r,a3 as o,b as l,w as c,a as s,E as n,a4 as h}from"./chunks/framework.aYYe0dfI.js";const k=JSON.parse('{"title":"Module schema","description":"This 模块负责从 C++ 类型自动推导并生成与 OpenAI API 兼容的 JSON Schema 表示。它提供了一套编译期类型特征（如 is_optional、is_vector、is_array）和运行时构建函数（make_schema_object、make_scalar_type_schema、make_any_of_schema 等），能够为标量、可选值、容器及复合类型正确地构造 schema 结构。同时，该模块包含一系列验证函数（validate_openai_schema、validate_openai_schema_value、validate_response_format、validate_tool_definition 等），用于确保生成的 schema 或传入的 JSON 数据满足 OpenAI 的格式约定。","frontmatter":{"title":"Module schema","description":"This 模块负责从 C++ 类型自动推导并生成与 OpenAI API 兼容的 JSON Schema 表示。它提供了一套编译期类型特征（如 is_optional、is_vector、is_array）和运行时构建函数（make_schema_object、make_scalar_type_schema、make_any_of_schema 等），能够为标量、可选值、容器及复合类型正确地构造 schema 结构。同时，该模块包含一系列验证函数（validate_openai_schema、validate_openai_schema_value、validate_response_format、validate_tool_definition 等），用于确保生成的 schema 或传入的 JSON 数据满足 OpenAI 的格式约定。","layout":"doc","template":"doc"},"headers":[],"relativePath":"zh/clore/modules/schema/index.md","filePath":"zh/clore/modules/schema/index.md"}'),p={name:"zh/clore/modules/schema/index.md"};function m(u,e,f,_,b,q){const t=i("Mermaid");return a(),r("div",null,[e[1]||(e[1]=o('<h1 id="module-schema" tabindex="-1">Module <code>schema</code> <a class="header-anchor" href="#module-schema" aria-label="Permalink to &quot;Module `schema`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>This 模块负责从 C++ 类型自动推导并生成与 <code>OpenAI</code> API 兼容的 JSON Schema 表示。它提供了一套编译期类型特征（如 <code>is_optional</code>、<code>is_vector</code>、<code>is_array</code>）和运行时构建函数（<code>make_schema_object</code>、<code>make_scalar_type_schema</code>、<code>make_any_of_schema</code> 等），能够为标量、可选值、容器及复合类型正确地构造 schema 结构。同时，该模块包含一系列验证函数（<code>validate_openai_schema</code>、<code>validate_openai_schema_value</code>、<code>validate_response_format</code>、<code>validate_tool_definition</code> 等），用于确保生成的 schema 或传入的 JSON 数据满足 <code>OpenAI</code> 的格式约定。</p><p>其公开接口以模板函数 <code>response_format</code> 和 <code>function_tool</code> 为核心，让用户能够将 C++ 类型映射为请求结构中的 <code>response_format</code> 或工具定义。整个模块的职责集中于简化类型到 JSON Schema 的映射与验证，为构建结构化输出提供一致的基础设施。</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../http/"><code>http</code></a></li><li><a href="./../protocol/"><code>protocol</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./../agent/tools"><code>agent:tools</code></a></li><li><a href="./../anthropic/"><code>anthropic</code></a></li><li><a href="./../client/"><code>client</code></a></li><li><a href="./../openai/"><code>openai</code></a></li><li><a href="./../provider/"><code>provider</code></a></li></ul><h2 id="dependency-diagram" tabindex="-1">Dependency Diagram <a class="header-anchor" href="#dependency-diagram" aria-label="Permalink to &quot;Dependency Diagram&quot;">​</a></h2>',9)),(a(),l(h,null,{default:c(()=>[n(t,{id:"mermaid-70",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20M0%5B%22schema%22%5D%0A%20%20%20%20I0%5B%22http%22%5D%0A%20%20%20%20I0%20--%3E%20M0%0A%20%20%20%20I1%5B%22protocol%22%5D%0A%20%20%20%20I1%20--%3E%20M0%0A%20%20%20%20I2%5B%22support%22%5D%0A%20%20%20%20I2%20--%3E%20M0%0A"})]),fallback:c(()=>[...e[0]||(e[0]=[s(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=o('<h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-net-openai-schema-detail-array-inner" tabindex="-1"><code>clore::net::openai::schema::detail::array_inner</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-array-inner" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::array_inner`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:72</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p><code>clore::net::openai::schema::detail::array_inner</code> 是一个模板结构体，定义于内部实现文件 <code>network/schema.cppm</code> 中。它没有公开的数据成员或成员函数，通常仅作为类型标签或类型萃取中的占位结构体使用。其唯一模板参数 <code>T</code> 代表数组所持有的元素类型，但结构体本身不存储任何运行时状态，也不公开任何操作。该结构体位于 <code>detail</code> 命名空间中，属于实现细节，因此其内部结构、不变量的具体定义不在公共契约中暴露，而是通过外部特化或元编程机制间接使用。</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>类型 <code>T</code> 无约束</li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>模板参数 <code>T</code></li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>可能用于类型映射或元编程中的标签</li><li>作为 <code>detail</code> 命名空间下的实现细节</li></ul><h3 id="clore-net-openai-schema-detail-is-array" tabindex="-1"><code>clore::net::openai::schema::detail::is_array</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-is-array" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::is_array`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:63</code></p><p>Definition: <code>network/schema.cppm:63</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p><code>clore::net::openai::schema::detail::is_array</code> 是一个模板类型特征，其主模板公开继承自 <code>std::false_type</code>。该结构体不定义任何额外的成员或函数，仅通过继承提供编译期常量值 <code>false</code>，为所有未特化的类型 <code>T</code> 指示“非数组”。该实现的设计意图是作为默认否定基类，后续通过显式特化（例如对 <code>T[]</code> 或 <code>T[N]</code>）覆盖为 <code>std::true_type</code>，从而在编译期区分数组类型。内部结构简单，不含状态或运行时成员，其不变性在于：对于任意未特化的 <code>T</code>，<code>is_array&lt;T&gt;::value</code> 恒为 <code>false</code>。</p><h4 id="invariants-1" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-1" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>默认 <code>value</code> 为 <code>false</code></li><li>可通过模板特化为数组类型提供 <code>value</code> 为 <code>true</code></li></ul><h4 id="key-members-1" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-1" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>继承自 <code>std::false_type</code> 的静态常量 <code>value</code></li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>作为编译期类型判断的基础特征</li><li>可供其他模板通过特化来针对数组类型启用特定行为</li></ul><h3 id="clore-net-openai-schema-detail-is-optional" tabindex="-1"><code>clore::net::openai::schema::detail::is_optional</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-is-optional" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::is_optional`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:23</code></p><p>Definition: <code>network/schema.cppm:23</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>该结构体定义在内部命名空间 <code>clore::net::openai::schema::detail</code> 中，作为检测类型是否为 <code>std::optional</code> 的基础类型特征（trait）。它公开继承 <code>std::false_type</code>，因此其静态成员常量 <code>value</code> 默认为 <code>false</code>。对于任意类型 <code>T</code>，主模板的 <code>value</code> 均为 <code>false</code>；库通过模板特化（如针对 <code>std::optional&lt;U&gt;</code>）来覆盖该默认值，使 <code>value</code> 变为 <code>true</code>。整个结构体仅由继承的 <code>value</code> 常量和类型定义组成，不存在其他成员或复杂不变量。</p><h4 id="invariants-2" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-2" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>Default value is <code>false</code></li><li>Inherits from <code>std::false_type</code></li><li>Value is constant at compile time</li></ul><h4 id="key-members-2" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-2" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>value</code> (inherited from <code>std::false_type</code>)</li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used in template metaprogramming to conditionally enable code</li><li>Specialized for types that are optional wrappers</li></ul><h3 id="clore-net-openai-schema-detail-is-vector" tabindex="-1"><code>clore::net::openai::schema::detail::is_vector</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-is-vector" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::is_vector`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:43</code></p><p>Definition: <code>network/schema.cppm:43</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>该结构体 <code>clore::net::openai::schema::detail::is_vector</code> 是一个模板类型特征，其主模板继承自 <code>std::false_type</code>。对于未显式特化的任意类型 <code>T</code>，其 <code>value</code> 成员常量恒为 <code>false</code>，标志着 <code>T</code> 并非向量类型。该实现遵循标准类型特征库的设计惯例，自身不包含额外数据成员或虚函数，完全通过基类继承机制提供编译期布尔值判断。</p><h4 id="invariants-3" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-3" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>value</code> 恒为 <code>false</code>，除非通过特化覆盖</li><li>所有实例化共享相同的 <code>false_type</code> 接口</li></ul><h4 id="key-members-3" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-3" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>继承的 <code>std::false_type::value</code> 常量</li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>作为基类用于定义向量类型的特征特化</li><li>在模板元编程中用作编译时布尔判定</li></ul><h3 id="clore-net-openai-schema-detail-optional-inner" tabindex="-1"><code>clore::net::openai::schema::detail::optional_inner</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-optional-inner" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::optional_inner`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:32</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>结构体 <code>clore::net::openai::schema::detail::optional_inner</code> 是 <code>optional</code> 类型的核心内部实现，负责存储值对象并跟踪其存在性。其内部通常包含一个通过 <code>aligned_storage</code> 或类似设施管理的原始内存缓冲区，以及一个 <code>bool</code> 标志位，指示缓冲区中是否已构造了 <code>T</code> 类型的对象。核心不变量是：当标志为 <code>true</code> 时，缓冲区中必须存在一个通过 placement new 完整构造的 <code>T</code> 实例；当标志为 <code>false</code> 时，缓冲区内容未初始化，任何读取操作均为未定义行为。重要成员实现包括：默认构造函数将标志置为 <code>false</code> 且不构造值对象；复制构造函数会根据源对象的标志状态，若源有值则复制构造新对象；析构函数仅在标志为 <code>true</code> 时调用该对象的析构函数并重置标志；赋值运算符通过复用现有存储或销毁旧值来精确管理生命周期，确保异常安全且不泄露资源。</p><h3 id="clore-net-openai-schema-detail-schema-subject" tabindex="-1"><code>clore::net::openai::schema::detail::schema_subject</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-schema-subject" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::schema_subject`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:83</code></p><p>Definition: <code>network/schema.cppm:83</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>结构体 <code>schema_subject</code> 是 <code>clore::net::openai::schema::detail</code> 命名空间下的一个内部类型转换工具。其核心实现仅通过一个公开的别名成员 <code>type</code>，对模板参数 <code>T</code> 应用 <code>std::remove_cvref_t</code> 来剥离引用和顶层 const/volatile 限定符，从而得到一个无引用的原始类型。该别名的存在意味着无论传入何种类型（包括左值引用、右值引用或 cv 限定类型），<code>schema_subject</code> 都能提供一个统一、干净的类型定义，供后续的模式推导或类型检查使用。整个结构体不维护任何运行时状态，其所有功能均在编译期完成，保证了零开销的抽象。</p><h4 id="invariants-4" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-4" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>type</code> 始终为 <code>T</code> 去除顶层 cv 和引用后的结果。</li><li>不存在运行时状态或可变成员。</li></ul><h4 id="key-members-4" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-4" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>成员别名 <code>type</code></li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>作为类型萃取工具，用于获取实参的底层类型。</li><li>可能用于 SFINAE 上下文中约束模板参数。</li></ul><h3 id="clore-net-openai-schema-detail-schema-subject-t" tabindex="-1"><code>clore::net::openai::schema::detail::schema_subject_t</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-schema-subject-t" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::schema_subject_t`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:95</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p><code>schema_subject_t</code> 是一个模板类型别名，它通过 <code>schema_subject&lt;T&gt;::type</code> 间接推导出最终的类型。该别名不增加任何额外的逻辑或成员，仅作为元函数的简写形式，隐藏了 <code>typename schema_subject&lt;T&gt;::type</code> 这一完整语法。其实现完全依赖于 <code>schema_subject</code> 类模板的特化或主模板定义。</p><p>在使用该别名时，必须保证 <code>schema_subject&lt;T&gt;</code> 已经被正确特化并提供了有效的 <code>type</code> 成员。该别名在整个 schema 实现层中充当类型萃取层，允许其他代码以统一的方式获取 “T 在 schema 主题体系中的对应类型”。由于它只是一个别名，其本身不维护任何运行时状态，编译期计算完全由 <code>schema_subject</code> 完成。</p><h3 id="clore-net-openai-schema-detail-vector-inner" tabindex="-1"><code>clore::net::openai::schema::detail::vector_inner</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-vector-inner" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::vector_inner`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:52</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The struct <code>clore::net::openai::schema::detail::vector_inner</code> is a template defined with a single type parameter <code>T</code> and declared in <code>network/schema.cppm</code>. It resides in the <code>detail</code> namespace, marking it as an internal implementation component.</p><p>The struct likely provides a lightweight wrapper around a contiguous block of <code>T</code> elements, modeling a dynamic array or vector. Key invariants include element contiguity, correct element lifetime management, and proper memory alignment. Important member implementations, such as constructors and element accessors, are designed for efficiency and are not part of the public API.</p><h2 id="variables" tabindex="-1">Variables <a class="header-anchor" href="#variables" aria-label="Permalink to &quot;Variables&quot;">​</a></h2><h3 id="clore-net-openai-schema-detail-is-array-v" tabindex="-1"><code>clore::net::openai::schema::detail::is_array_v</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-is-array-v" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::is_array_v`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:69</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>This variable is read at compile time in template metaprogramming contexts to detect array types. It participates in conditional logic or specialization, though its exact usage is not observed in the provided evidence.</p><h4 id="mutation" tabindex="-1">Mutation <a class="header-anchor" href="#mutation" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Compile-time type detection</li><li>Conditional template instantiation (inferred)</li></ul><h3 id="clore-net-openai-schema-detail-is-optional-v" tabindex="-1"><code>clore::net::openai::schema::detail::is_optional_v</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-is-optional-v" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::is_optional_v`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:29</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>This variable is used as a type trait to enable conditional logic in schema validation or serialization code. It is read at compile time to select appropriate code paths, typically in conjunction with SFINAE or <code>if constexpr</code>.</p><h4 id="mutation-1" tabindex="-1">Mutation <a class="header-anchor" href="#mutation-1" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h3 id="clore-net-openai-schema-detail-is-vector-v" tabindex="-1"><code>clore::net::openai::schema::detail::is_vector_v</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-is-vector-v" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::is_vector_v`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:49</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>This variable is used in template metaprogramming contexts to conditionally enable or select code paths based on whether a given type is a vector. It is read as a constant expression and participates in <code>if constexpr</code> logic or SFINAE constraints alongside similar traits like <code>is_optional_v</code> and <code>is_array_v</code>.</p><h4 id="mutation-2" tabindex="-1">Mutation <a class="header-anchor" href="#mutation-2" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h4 id="usage-patterns-6" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-6" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>template metaprogramming</li><li>type trait checks</li><li>compile-time branching</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-net-detail-validate-response-format" tabindex="-1"><code>clore::net::detail::validate_response_format</code> <a class="header-anchor" href="#clore-net-detail-validate-response-format" aria-label="Permalink to &quot;`clore::net::detail::validate_response_format`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:527</code></p><p>Definition: <code>network/schema.cppm:535</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>该函数首先检查传入的 <code>ResponseFormat</code> 对象中是否包含可选的 <code>schema</code> 字段：若 <code>format.schema</code> 无值，则直接返回一个空的 <code>expected</code> 表示校验通过。否则，继续校验 <code>format.name</code> 是否为空字符串，若为空则返回包含 <code>LLMError</code> 的 <code>unexpected</code> 结果。上述前置校验通过后，函数将实际的 Schema 对象、名称以及表示当前为根对象的布尔值 <code>true</code> 转发给 <code>openai::schema::detail::validate_openai_schema</code>，由该函数完成对 <code>OpenAI</code> 响应格式 Schema 的深层递归验证。整个流程依赖于 <code>validate_openai_schema</code> 这一核心验证例程，而本函数仅负责边界条件的快速退出和必要参数的准备。</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>format<code>.schema</code></li><li>format<code>.name</code></li></ul><h4 id="usage-patterns-7" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-7" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>validate response format before making API requests</li><li>check <code>response_format``.name</code> and schema</li></ul><h3 id="clore-net-detail-validate-tool-definition" tabindex="-1"><code>clore::net::detail::validate_tool_definition</code> <a class="header-anchor" href="#clore-net-detail-validate-tool-definition" aria-label="Permalink to &quot;`clore::net::detail::validate_tool_definition`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:529</code></p><p>Definition: <code>network/schema.cppm:545</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>函数 <code>clore::net::detail::validate_tool_definition</code> 的实现首先对传入的 <code>tool</code> 参数进行基本验证：若 <code>tool.name</code> 为空，则返回含 <code>LLMError</code> 错误的 <code>std::unexpected</code>，消息为 &quot;tool name must not be empty&quot;；若 <code>tool.description</code> 为空，则返回含格式化消息 &quot;tool &#39;...&#39; description must not be empty&quot; 的 <code>std::unexpected</code>。这两项检查确保了工具定义的关键字段不为空。</p><p>基本验证通过后，函数将实际校验委托给 <code>openai::schema::detail::validate_openai_schema</code>，传入 <code>tool.parameters</code>、<code>tool.name</code> 和布尔值 <code>true</code>（表示当前上下文为根级别）。该调用负责对参数模式的合法性进行深层验证，并最终将 <code>std::expected&lt;void, LLMError&gt;</code> 结果直接返回给调用者。函数本身依赖 <code>std::format</code> 进行字符串格式化，并依赖 <code>LLMError</code> 类型传递错误描述。</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>tool<code>.name</code></li><li>tool<code>.description</code></li><li>tool<code>.parameters</code></li></ul><h4 id="usage-patterns-8" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-8" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>validate tool definition before API call</li><li>ensure tool name and description are provided</li></ul><h3 id="clore-net-openai-schema-detail-make-any-of-schema" tabindex="-1"><code>clore::net::openai::schema::detail::make_any_of_schema</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-make-any-of-schema" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::make_any_of_schema`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:156</code></p><p>Definition: <code>network/schema.cppm:156</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>函数 <code>clore::net::openai::schema::detail::make_any_of_schema</code> 根据传入的 <code>std::vector&lt;json::Value&gt;</code> 构建一个包含 <code>anyOf</code> 字段的 JSON 模式对象。其内部控制流首先调用 <code>clore::net::detail::make_empty_object</code> 创建一个空 JSON 对象，若失败则立即返回 <code>std::unexpected</code>；接着调用 <code>clore::net::detail::make_empty_array</code> 创建一个空 JSON 数组，同样在失败时返回错误。随后，遍历 <code>choices</code> 中的每个元素，通过 <code>push_back</code> 将其移动至数组中。最后，将填充完毕的数组插入对象键 <code>&quot;anyOf&quot;</code> 下，并将整个对象包装为 <code>json::Value</code> 返回。该函数依赖于 <code>clore::net::detail</code> 的 <code>make_empty_object</code> 和 <code>make_empty_array</code> 来分配基础容器，并通过标准库容器与 <code>std::expected</code> 实现错误传播。</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>allocates JSON objects</li><li>transfers ownership of choice values into the schema</li></ul><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>function parameter <code>choices</code> (vector of JSON values)</li><li>return values from <code>make_empty_object</code> and <code>make_empty_array</code></li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>local JSON object and array built during execution</li><li>the returned JSON value (ownership transferred to caller)</li></ul><h4 id="usage-patterns-9" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-9" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>building an <code>anyOf</code> schema from a list of choices</li></ul><h3 id="clore-net-openai-schema-detail-make-scalar-type-schema" tabindex="-1"><code>clore::net::openai::schema::detail::make_scalar_type_schema</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-make-scalar-type-schema" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::make_scalar_type_schema`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:146</code></p><p>Definition: <code>network/schema.cppm:146</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>函数 <code>clore::net::openai::schema::detail::make_scalar_type_schema</code> 负责为标量类型构造一个基础 JSON Schema 对象。其实现流程如下：首先调用 <code>clore::net::detail::make_empty_object</code> 创建一个空 JSON 对象，并将失败信息包装为 <code>std::unexpected</code> 返回；若创建成功，则向该对象插入键值对 <code>&quot;type&quot;</code> 与传入的 <code>type_name</code> 字符串，最后将填充后的对象包装为 <code>json::Value</code> 并返回。该函数仅依赖 <code>make_empty_object</code> 创建 JSON 对象的能力以及 <code>json::Value</code> 的构造函数，无额外控制流分支（除错误传播外），非常适合作为标量模式生成的统一入口。</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Allocates a new <code>json::Object</code> internally via <code>make_empty_object</code></li><li>Transfers ownership of the resulting <code>json::Value</code> to the caller</li></ul><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>type_name</code> parameter</li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Returns a new <code>json::Value</code> object</li></ul><h4 id="usage-patterns-10" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-10" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to generate JSON schema for scalar types</li><li>Called by other schema construction functions like <code>make_schema_value</code></li></ul><h3 id="clore-net-openai-schema-detail-make-schema-object" tabindex="-1"><code>clore::net::openai::schema::detail::make_schema_object</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-make-schema-object" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::make_schema_object`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:132</code></p><p>Definition: <code>network/schema.cppm:132</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>函数 <code>clore::net::openai::schema::detail::make_schema_object</code> 依赖 <code>make_schema_value&lt;T&gt;()</code> 生成 JSON 模式值，并将结果存储在临时变量 <code>value</code> 中。若 <code>value</code> 不包含合法值，则立即返回 <code>std::unexpected</code> 包装的错误。随后通过 <code>value-&gt;get_object()</code> 提取底层 JSON 对象指针：如果指针为空（即生成的模式根不是 JSON 对象），同样返回错误；否则复制该 JSON 对象并返回成功结果。控制流仅在成功路径上分支，失败路径统一以 <code>LLMError</code> 类型传递错误信息。核心依赖为 <code>make_schema_value</code> 模板函数、<code>json::Object</code> 类型以及 <code>LLMError</code> 错误类型。</p><h4 id="side-effects-4" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-4" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>allocates a <code>json::Object</code></li><li>returns error via <code>std::unexpected</code></li></ul><h4 id="reads-from-4" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-4" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>template type <code>T</code></li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returns a <code>json::Object</code> by value</li></ul><h4 id="usage-patterns-11" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-11" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>generates <code>OpenAPI</code> schema object for type <code>T</code></li><li>used in schema serialization</li></ul><h3 id="clore-net-openai-schema-detail-make-schema-value" tabindex="-1"><code>clore::net::openai::schema::detail::make_schema_value</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-make-schema-value" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::make_schema_value`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:129</code></p><p>Definition: <code>network/schema.cppm:225</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>该函数是一个模板元编程驱动的JSON Schema生成器，通过编译期类型分发为不同类型生成对应的<code>OpenAI</code> schema结构。内部使用<code>if constexpr</code>依次检查<code>schema_subject_t&lt;T&gt;</code>是否为标量、<code>std::optional</code>、<code>std::vector</code>、<code>std::array</code>或反射类，并对每种情况递归构造嵌套schema。对于<code>std::optional</code>，先生成内层类型的schema，再与一个<code>null</code>类型schema组合成<code>anyOf</code>数组；对于<code>std::vector</code>和<code>std::array</code>，递归生成元素类型schema后作为<code>items</code>字段放入数组对象，其中<code>std::array</code>还会添加<code>minItems</code>和<code>maxItems</code>以固定大小；对于反射类，创建一个空JSON对象后调用<code>populate_object_schema</code>利用编译期反射填充属性。任何递归步骤失败（返回<code>std::unexpected</code>都会直接传播错误，未处理类型则触发<code>static_assert</code>。</p><p>函数依赖<code>make_scalar_type_schema</code>、<code>make_any_of_schema</code>和<code>populate_object_schema</code>等内部助手，以及<code>is_optional_v</code>、<code>vector_inner_t</code>、<code>meta::reflectable_class</code>等一系列类型萃取和反射设施，同时使用<code>clore::net::detail::make_empty_object</code>构造JSON容器。递归错误处理统一通过<code>std::expected</code>的<code>has_value</code>检查完成，若子调用失败则立即返回错误，避免了深层无效状态的累积。</p><h4 id="side-effects-5" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-5" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-5" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-5" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>template parameter <code>T</code></li><li>type traits <code>std::same_as</code>, <code>std::integral</code>, <code>std::floating_point</code>, <code>is_optional_v</code>, <code>is_vector_v</code>, <code>is_array_v</code>, <code>meta::reflectable_class</code></li><li>helper functions <code>make_scalar_type_schema</code>, <code>make_any_of_schema</code>, <code>populate_object_schema</code>, <code>make_empty_object</code></li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>constructed <code>json::Value</code> object</li><li>error <code>LLMError</code> via <code>std::unexpected</code></li></ul><h4 id="usage-patterns-12" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-12" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Generate JSON schema value for C++ types</li><li>Recursively called for nested types</li><li>Used within schema generation pipeline</li></ul><h3 id="clore-net-openai-schema-detail-populate-object-schema" tabindex="-1"><code>clore::net::openai::schema::detail::populate_object_schema</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-populate-object-schema" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::populate_object_schema`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:173</code></p><p>Definition: <code>network/schema.cppm:173</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>该函数负责从给定的 <code>Object</code> 类型反射其字段模式，并填充到一个传入的 <code>json::Object</code> 中，形成 <code>OpenAI</code> 兼容的 schema 表示。它首先通过 <code>meta_attrs::validate_field_schema&lt;Object&gt;()</code> 在编译期校验字段模式的有效性（如名称冲突或别名问题），然后创建空的 <code>properties</code> 对象和 <code>required</code> 数组。核心算法在一个变参 lambda <code>append_field</code> 中完成，针对每个模板索引 <code>Indices</code>，利用 <code>meta_attrs::resolve_field&lt;Object, index&gt;</code> 获取字段的 schema 属性；若字段标记为跳过则直接返回，若为展开字段则返回错误，否则调用 <code>make_schema_value&lt;field_type&gt;()</code> 生成字段值 schema，并将规范名称插入 <code>properties</code> 和 <code>required</code> 中。所有索引的处理通过参数包展开并行执行，结果存入 <code>statuses</code> 数组，随后逐个检查是否成功。最终在 <code>object</code> 中插入固定的 <code>type</code> 值 <code>&quot;object&quot;</code>、填充好的 <code>properties</code> 和 <code>required</code>，以及 <code>additionalProperties</code> 为 <code>false</code>。</p><p>依赖方面，该函数使用了 <code>clore::net::detail::make_empty_object</code> 和 <code>make_empty_array</code> 创建容器，依赖 <code>meta_attrs::resolve_field</code> 获取字段元数据，并通过 <code>make_schema_value</code> 递归地为每个字段类型生成子 schema。整个流程通过 <code>std::index_sequence</code> 展开和 <code>std::expected</code> 的错误传播机制确保异常安全。</p><h4 id="side-effects-6" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-6" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>修改传入的 <code>json::Object&amp;</code> 参数，插入多个键值对</li><li>创建临时 <code>json::Object</code> 和 <code>json::Array</code> 对象用于 <code>properties</code> 和 <code>required</code></li></ul><h4 id="reads-from-6" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-6" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>模板参数 <code>Object</code> 的编译时字段反射信息（通过 <code>meta_attrs::resolve_field</code>）</li><li>参数 <code>std::index_sequence&lt;Indices...&gt;</code> 用于展开字段索引</li></ul><h4 id="writes-to-4" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-4" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>参数 <code>object</code>（<code>json::Object&amp;</code>）</li></ul><h4 id="usage-patterns-13" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-13" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>在 <code>OpenAI</code> schema 生成管线的内部调用</li><li>通常由 <code>make_object_schema</code> 等高级函数调用</li></ul><h3 id="clore-net-openai-schema-detail-sanitize-schema-name" tabindex="-1"><code>clore::net::openai::schema::detail::sanitize_schema_name</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-sanitize-schema-name" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::sanitize_schema_name`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:97</code></p><p>Definition: <code>network/schema.cppm:97</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>该函数通过逐字符扫描输入字符串 <code>raw_name</code> 实现清理。对于每个字符 <code>ch</code>，将其转换为 <code>unsigned char</code> 后检查是否属于英文字母（大小写）或数字；若符合则直接追加到 <code>sanitized</code> 结果字符串，否则追加下划线 <code>&#39;_&#39;</code>。此替换逻辑不依赖外部函数或类型，仅使用标准库字符分类的等价手动比较。</p><p>追加完成后，连续移除前导和尾随的下划线字符：先用循环擦除开头的连续 <code>&#39;_&#39;</code>，再用循环弹出结尾的连续 <code>&#39;_&#39;</code>。最终返回修剪后的字符串。整个过程没有调用其他模块接口，仅依赖 <code>std::string</code> 的成员函数 <code>push_back</code>、<code>erase</code>、<code>pop_back</code> 和 <code>reserve</code>。</p><h4 id="side-effects-7" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-7" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-7" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-7" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>参数 <code>raw_name</code></li></ul><h4 id="usage-patterns-14" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-14" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>清理 <code>OpenAI</code> schema 的名称</li></ul><h3 id="clore-net-openai-schema-detail-schema-type-name" tabindex="-1"><code>clore::net::openai::schema::detail::schema_type_name</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-schema-type-name" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::schema_type_name`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:120</code></p><p>Definition: <code>network/schema.cppm:120</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>该函数首先通过 <code>meta::type_name&lt;T&gt;()</code> 获取类型 <code>T</code> 的原始名称字符串，然后将其传递给 <code>clore::net::openai::schema::detail::sanitize_schema_name</code> 进行清理。若清理后的结果为空字符串，则立即返回一个 <code>std::unexpected</code> 包装的 <code>LLMError</code>，表示生成的 schema 名称无效；否则直接返回清理后的字符串。整个实现仅依赖于 <code>sanitize_schema_name</code> 和 <code>meta::type_name</code>，控制流仅为一个条件判断，无额外分支或循环。</p><h4 id="side-effects-8" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-8" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-8" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-8" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>template parameter <code>T</code> via <code>meta::type_name&lt;T&gt;()</code></li><li>call to <code>sanitize_schema_name</code> which reads its argument</li></ul><h4 id="usage-patterns-15" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-15" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during schema generation to derive a schema name for a C++ type</li><li>used by <code>make_schema_object</code> and similar functions</li></ul><h3 id="clore-net-openai-schema-detail-validate-openai-schema" tabindex="-1"><code>clore::net::openai::schema::detail::validate_openai_schema</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-validate-openai-schema" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::validate_openai_schema`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:328</code></p><p>Definition: <code>network/schema.cppm:373</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>函数 <code>clore::net::openai::schema::detail::validate_openai_schema</code> 从输入的 JSON 对象中逐一检查 <code>OpenAI</code> 模式的关键字段。它首先检查是否存在 <code>anyOf</code> 字段：若存在且 <code>is_root</code> 为真则直接报错，否则递归调用 <code>validate_openai_schema_value</code> 验证每个备选方案。随后解析 <code>type</code> 字段，支持字符串或字符串数组形式，遇到数组时先通过 <code>validate_schema_array_of_types</code> 验证类型列表的合法性，再选取非 <code>null</code> 的类型作为 <code>schema_type</code>；若最终无法确定有效类型、根模式类型非 <code>object</code>、或类型字段缺失都会返回错误。根据解析出的 <code>schema_type</code>，若为 <code>object</code> 则要求必须存在 <code>properties</code>（验证为对象）、<code>required</code>（验证为数组）且 <code>additionalProperties</code> 必须显式设为 <code>false</code>，接着调用 <code>validate_required_properties</code> 确保必需属性全部出现在 <code>properties</code> 中，再递归验证每个属性值；若为 <code>array</code> 则验证 <code>items</code> 子模式。最后检查可选字段 <code>$defs</code>，若存在则将其视为对象并递归验证每个定义。整个流程依赖 <code>validate_openai_schema_value</code>、<code>validate_required_properties</code>、<code>validate_schema_array_of_types</code> 等内部辅助函数，并通过 <code>clore::net::detail::ObjectView</code> 和 <code>clore::net::detail::expect_array</code>、<code>expect_object</code> 等工具安全地访问 JSON 节点。</p><h4 id="side-effects-9" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-9" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-9" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-9" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>object</code> parameter (JSON object and its nested values via <code>ObjectView::get</code>)</li><li><code>path</code> parameter</li><li><code>is_root</code> parameter</li><li><code>validate_openai_schema_value</code> (recursively reads sub-schemas)</li></ul><h4 id="usage-patterns-16" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-16" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Validating root schema objects from <code>OpenAI</code> API requests</li><li>Validating nested schema definitions in tool-calling configurations</li><li>Called during schema registration or preprocessing to ensure compliance</li></ul><h3 id="clore-net-openai-schema-detail-validate-openai-schema-value" tabindex="-1"><code>clore::net::openai::schema::detail::validate_openai_schema_value</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-validate-openai-schema-value" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::validate_openai_schema_value`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:331</code></p><p>Definition: <code>network/schema.cppm:331</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>该函数是 <code>validate_openai_schema</code> 的入口包装器，负责将传入的任意 JSON 值强制转换为对象后再进行校验。内部通过 <code>clore::net::detail::expect_object</code> 将 <code>value</code> 解析为一个 <code>json::Object</code> 视图，如果解析失败（例如输入为数组或标量）则直接返回错误；成功则解引用对象并调用 <code>clore::net::openai::schema::detail::validate_openai_schema</code>，传递相同的 <code>path</code> 和 <code>is_root</code> 标志，由后者执行具体的模式校验逻辑。</p><p>算法上仅包含一层前处理与委托，控制流简单：首先校验值类型是否为对象，然后完全依赖 <code>validate_openai_schema</code> 的递归模式验证。依赖集中于 <code>expect_object</code>（用于类型断言与错误包装）和 <code>validate_openai_schema</code>（用于实际的 <code>OpenAPI</code> 模式语义检查）。</p><h4 id="side-effects-10" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-10" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-10" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-10" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>value</code> parameter (a <code>json::Value</code>)</li><li><code>path</code> parameter (a <code>std::string_view</code>)</li><li><code>is_root</code> parameter (a <code>bool</code>)</li></ul><h4 id="usage-patterns-17" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-17" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to validate a JSON value that is expected to be an <code>OpenAI</code> schema object</li><li>Called by higher-level schema validation logic</li></ul><h3 id="clore-net-openai-schema-detail-validate-openai-schema-value-1" tabindex="-1"><code>clore::net::openai::schema::detail::validate_openai_schema_value</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-validate-openai-schema-value-1" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::validate_openai_schema_value`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:340</code></p><p>Definition: <code>network/schema.cppm:340</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>该函数是 <code>validate_openai_schema</code> 的一个轻量级包装器，负责处理从 <code>json::Cursor</code> 到 <code>json::Object</code> 的转换。内部控制流首先调用 <code>clore::net::detail::expect_object</code> 从 <code>value</code> 中提取一个对象，若提取失败则直接返回包装后的 <code>std::unexpected</code> 错误。成功获取对象后，立即将实际对象、路径 <code>path</code> 和根标志 <code>is_root</code> 转发给 <code>clore::net::openai::schema::detail::validate_openai_schema</code>，其返回值即为该函数的最终结果。该函数不进行任何额外的验证逻辑，完全依赖于下游的 <code>validate_openai_schema</code> 和 <code>expect_object</code> 的正确性。</p><h4 id="side-effects-11" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-11" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-11" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-11" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>json::Cursor value</code> parameter</li><li><code>std::string_view path</code> parameter</li><li><code>bool is_root</code> parameter</li><li>The <code>json::Object</code> extracted from the cursor via <code>expect_object</code></li></ul><h4 id="writes-to-5" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-5" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>The return value of type <code>std::expected&lt;void, LLMError&gt;</code></li></ul><h4 id="usage-patterns-18" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-18" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Wrapping a JSON cursor into object validation</li><li>Entry point for validating schema values from external JSON sources</li></ul><h3 id="clore-net-openai-schema-detail-validate-required-properties" tabindex="-1"><code>clore::net::openai::schema::detail::validate_required_properties</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-validate-required-properties" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::validate_required_properties`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:349</code></p><p>Definition: <code>network/schema.cppm:349</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>函数 <code>clore::net::openai::schema::detail::validate_required_properties</code> 验证给定的 <code>properties</code> 对象视图与 <code>required</code> 数组视图的兼容性，以确保每个属性都出现在 <code>required</code> 列表中，反之亦然。它首先遍历 <code>required</code> 数组，调用 <code>clore::net::detail::expect_string</code> 将每个值解析为字符串，并将结果插入到 <code>required_names</code> 集合中；若解析失败，则立即返回对应的 <code>LLMError</code>。随后，它遍历 <code>properties</code> 的每个条目，检查条目的键是否存在于 <code>required_names</code> 中，若缺失则构建并返回一个描述路径和属性名称的错误。所有检查通过后返回空期望值。</p><p>该函数的核心依赖包括：<code>clore::net::detail::ObjectView</code> 和 <code>clore::net::detail::ArrayView</code> 用于封装 JSON 对象和数组视图的迭代接口；<code>clore::net::detail::expect_string</code> 负责从 JSON 值中安全提取字符串；以及 <code>LLMError</code> 错误类型用于报告验证失败。控制流完全基于两个线性遍历，没有深层递归或复杂分支，行为直接受限于输入数据的一致性。</p><h4 id="side-effects-12" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-12" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-12" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-12" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>properties</code> parameter</li><li><code>required</code> parameter</li><li><code>path</code> parameter</li></ul><h4 id="usage-patterns-19" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-19" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during schema validation when <code>strict</code> mode is enabled</li><li>Used to ensure all object properties are explicitly required</li></ul><h3 id="clore-net-openai-schema-detail-validate-schema-array-of-types" tabindex="-1"><code>clore::net::openai::schema::detail::validate_schema_array_of_types</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-validate-schema-array-of-types" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::validate_schema_array_of_types`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:295</code></p><p>Definition: <code>network/schema.cppm:295</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>该函数实现了一种受限的 JSON Schema <code>type</code> 数组校验，仅允许包含一个具体类型（如 <code>&quot;string&quot;</code>、<code>&quot;object&quot;</code>）与可选的 <code>&quot;null&quot;</code> 值。内部控制流首先遍历 <code>array</code>，对每个元素调用 <code>clore::net::detail::expect_string</code> 以提取类型字符串，若转换失败则立即返回错误。若类型为 <code>&quot;null&quot;</code>，则设置 <code>saw_null</code> 标志并跳过；否则，若已记录过 <code>primary_type</code>（即出现第二个具体类型），则返回错误以拒绝多类型联合。循环结束后，函数检查两个额外约束：如果 <code>is_root</code> 为真，则根模式不可为可空类型，直接报错；如果 <code>primary_type</code> 未设置或 <code>saw_null</code> 为假（即缺少具体类型或 <code>&quot;null&quot;</code>），也返回错误。仅当所有检查通过时返回成功。该函数依赖 <code>clore::net::detail::expect_string</code> 进行类型提取，以及 <code>LLMError</code> 构造错误信息。</p><h4 id="side-effects-13" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-13" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-13" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-13" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>array</code> parameter</li><li><code>path</code> parameter</li><li><code>is_root</code> parameter</li></ul><h4 id="usage-patterns-20" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-20" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used in schema validation pipelines</li><li>called when processing <code>type</code> arrays in <code>OpenAI</code> schema objects</li></ul><h3 id="clore-net-schema-function-tool" tabindex="-1"><code>clore::net::schema::function_tool</code> <a class="header-anchor" href="#clore-net-schema-function-tool" aria-label="Permalink to &quot;`clore::net::schema::function_tool`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:520</code></p><p>Definition: <code>network/schema.cppm:584</code></p><p>Declaration: <a href="./../../namespaces/clore/net/schema/"><code>Namespace clore::net::schema</code></a></p><p>函数的实现将模板参数 <code>T</code> 映射为根模式类型 <code>openai::schema::detail::schema_subject_t&lt;T&gt;</code>，并通过 <code>static_assert</code> 确保该类型支持反射。内部控制流首先校验 <code>name</code> 和 <code>description</code> 非空，若任一为空则直接返回 <code>std::unexpected</code> 错误。随后调用 <code>openai::schema::detail::make_schema_object&lt;root_type&gt;()</code> 生成完整的 JSON Schema 对象；若该过程失败，则将错误向上传播。成功时，构造并返回一个 <code>FunctionToolDefinition</code> 实例，其中 <code>parameters</code> 取自生成的 schema，<code>strict</code> 固定为 <code>true</code>。主要依赖包括类反射机制（<code>kota::meta::reflectable_class</code>）以及 <code>openai::schema::detail</code> 命名空间下的模式生成与验证函数。</p><h4 id="side-effects-14" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-14" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-14" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-14" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>name</code></li><li>parameter <code>description</code></li><li>template parameter <code>T</code> (used to deduce <code>root_type</code>)</li></ul><h4 id="usage-patterns-21" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-21" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Creates a function tool definition for an LLM from a reflectable type</li><li>Used to validate tool name and description before constructing the definition</li></ul><h3 id="clore-net-schema-response-format" tabindex="-1"><code>clore::net::schema::response_format</code> <a class="header-anchor" href="#clore-net-schema-response-format" aria-label="Permalink to &quot;`clore::net::schema::response_format`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:517</code></p><p>Definition: <code>network/schema.cppm:561</code></p><p>Declaration: <a href="./../../namespaces/clore/net/schema/"><code>Namespace clore::net::schema</code></a></p><p>该函数首先通过 <code>clore::net::openai::schema::detail::schema_subject_t&lt;T&gt;</code> 提取根类型，并静态断言该类型满足 <code>kota::meta::reflectable_class</code> 约束，确保其可用于结构化输出生成。随后依次调用 <code>clore::net::openai::schema::detail::schema_type_name&lt;root_type&gt;()</code> 和 <code>clore::net::openai::schema::detail::make_schema_object&lt;root_type&gt;()</code>，分别获取类型名称和对应的 JSON Schema 对象；若任一步出错，则直接向上传播 <code>std::unexpected</code> 错误。成功后将名称、Schema 对象以及硬编码的 <code>strict = true</code> 组合为 <code>clore::net::schema::ResponseFormat</code> 并返回。该函数完全依赖 <code>clore::net::openai::schema::detail</code> 命名空间下的反射与 Schema 构造工具链，不涉及额外的验证或后处理逻辑。</p><h4 id="side-effects-15" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-15" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-15" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-15" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>Type <code>T</code> via reflection (<code>static_assert</code> and helper functions <code>schema_type_name</code> and <code>make_schema_object</code>)</li></ul><h4 id="usage-patterns-22" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-22" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Call with a reflectable struct type to obtain a <code>ResponseFormat</code> for LLM structured output</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p><code>schema</code> 模块被分解为三个主要层次：公共接口层 <code>clore::net::schema</code>、<code>OpenAI</code> 规范适配层 <code>clore::net::openai::schema</code> 以及内部实现细节层 <code>detail</code>。该模块导入 <code>http</code>、<code>protocol</code>、<code>support</code> 和标准库，分别用于网络通信、协议定义、基础工具和容器支持。</p><p>内部层次上，<code>detail</code> 层提供了模板元编程基础设施，包括类型特征（如 <code>is_optional</code>、<code>is_vector</code>、<code>is_array</code>）和类型萃取工具（如 <code>optional_inner</code>、<code>schema_subject</code>），用于从 C++ 类型推导出 JSON Schema 的表示。上层函数（如 <code>make_schema_object</code>、<code>populate_object_schema</code>、<code>validate_openai_schema</code>）构建并验证 schema 对象，而公共接口 <code>response_format</code> 和 <code>function_tool</code> 则封装了这些机制，向调用者提供简洁的模板函数入口。整体实现结构围绕编译期类型映射与运行时 JSON 构造紧密结合，确保了 schema 生成的正确性与灵活性。</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../http/">Module http</a></li><li><a href="./../protocol/">Module protocol</a></li><li><a href="./../support/">Module support</a></li></ul>',292))])}const y=d(p,[["render",m]]);export{k as __pageData,y as default};
