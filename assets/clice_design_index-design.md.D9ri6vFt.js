import{_ as i,o as n,c as t,a3 as a}from"./chunks/framework.CRbjHCyB.js";const g=JSON.parse('{"title":"Index Design","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/index-design.md","filePath":"en/clice/design/index-design.md"}'),o={name:"clice/design/index-design.md"};function s(r,e,l,d,c,h){return n(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="index-design" tabindex="-1">Index Design <a class="header-anchor" href="#index-design" aria-label="Permalink to &quot;Index Design&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>Cross-file features of a language server — go-to-definition, find-references, call hierarchy, symbol search, etc. — all rely on a symbol index. The indexing system must address several core challenges:</p><p><strong>Performance at scale</strong>: C++ projects can contain tens of thousands of files, each producing a large number of symbols after compilation. The indexing system must build, persist, and query the index within reasonable time and memory constraints.</p><p><strong>Incremental updates</strong>: After a user modifies a file, the relevant index entries should update incrementally rather than re-indexing the entire project.</p><p><strong>Compilation-context awareness</strong>: The same header file can produce different symbols under different compilation contexts. Existing solutions like clangd store only the result of the last compilation, causing users to see stale index data after switching contexts.</p><p><strong>Real-time feedback for open files</strong>: Files the user is actively editing should be immediately reflected in query results, without waiting for background indexing to finish.</p><p>The following issues come up repeatedly in clangd&#39;s issue tracker:</p><ul><li>Go-to-definition jumps to the wrong location (when multiple translation units define identically named symbols)</li><li>Background indexing is slow — large projects need tens of minutes for the initial index pass</li><li>Symbol search results are incomplete or contain stale entries</li><li>Template code in header files has different reference relationships under different instantiation contexts</li></ul><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="three-level-index-hierarchy" tabindex="-1">Three-Level Index Hierarchy <a class="header-anchor" href="#three-level-index-hierarchy" aria-label="Permalink to &quot;Three-Level Index Hierarchy&quot;">​</a></h3><p>clice uses a three-level index structure, each level serving a different purpose:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TUIndex (compilation artifact)</span></span>
<span class="line"><span>    ↓ merge</span></span>
<span class="line"><span>ProjectIndex (global symbol directory) + MergedIndex (per-file sharded relation data)</span></span>
<span class="line"><span>    ↑ overlay</span></span>
<span class="line"><span>OpenFileIndex (real-time override for open files)</span></span></code></pre></div><h3 id="tuindex-single-compilation-output" tabindex="-1">TUIndex: Single-Compilation Output <a class="header-anchor" href="#tuindex-single-compilation-output" aria-label="Permalink to &quot;TUIndex: Single-Compilation Output&quot;">​</a></h3><p>A TUIndex is the raw index data produced by a single compilation. When a translation unit is compiled, SemanticVisitor traverses the AST, records all symbol occurrences and relations, and produces a TUIndex.</p><p>A TUIndex contains:</p><ul><li><strong>Per-file index data</strong>: Each file involved in the compilation (the main file plus all included headers) has its own FileIndex, recording occurrences and relations within that file</li><li><strong>Symbol table (SymbolTable)</strong>: Maps symbol hashes to symbol names and kinds</li><li><strong>Include graph</strong>: Records include relationships between files</li></ul><p>A TUIndex is transient data — it is no longer needed after being merged into ProjectIndex and MergedIndex. In the background indexing scenario, a TUIndex is generated and serialized in a worker process, then transmitted to the master process for merging.</p><h3 id="projectindex-global-symbol-directory" tabindex="-1">ProjectIndex: Global Symbol Directory <a class="header-anchor" href="#projectindex-global-symbol-directory" aria-label="Permalink to &quot;ProjectIndex: Global Symbol Directory&quot;">​</a></h3><p>ProjectIndex is the global symbol directory, aggregating symbol information from all indexed files. It functions like a search engine&#39;s inverted index — given a symbol hash, you can quickly find its name, kind, and which files it appears in.</p><p>ProjectIndex stores:</p><ul><li><strong>Symbol table</strong>: Symbol hash → symbol name, symbol kind, reference file bitmap (Bitmap)</li><li><strong>Path pool</strong>: Internalized mapping of file paths</li></ul><p><strong>Key design point</strong>: ProjectIndex does not store concrete location information (line and column numbers). Location information is stored in MergedIndex&#39;s per-file shards. ProjectIndex&#39;s role is that of a &quot;directory&quot; — it tells you which files a symbol exists in, then you look up the exact locations in the corresponding MergedIndex shard.</p><p>This separation keeps ProjectIndex compact. Reference file bitmaps are stored using Roaring Bitmap compression, which is highly memory-efficient.</p><h3 id="mergedindex-per-file-sharded-index" tabindex="-1">MergedIndex: Per-File Sharded Index <a class="header-anchor" href="#mergedindex-per-file-sharded-index" aria-label="Permalink to &quot;MergedIndex: Per-File Sharded Index&quot;">​</a></h3><p>MergedIndex is the core storage layer of the indexing system. It maintains one shard per file in the project, storing all symbol occurrence positions and relation information for that file.</p><p>The core feature of MergedIndex is <strong>compilation-context merging</strong>. The same header file may be included by multiple source files, and each compilation may produce different symbol relations (e.g., due to conditional compilation, template instantiation, etc.). MergedIndex merges index data from these different compilation contexts into the same shard.</p><p>Merging uses content-addressed deduplication: a content hash is computed for each FileIndex produced by a compilation, and different compilation contexts with identical content share the same data. This avoids redundant storage when a header file is indexed multiple times.</p><p>MergedIndex shards also store file contents, used for converting byte offsets to LSP positions (line/column numbers).</p><h3 id="symbol-identity-symbolhash" tabindex="-1">Symbol Identity: SymbolHash <a class="header-anchor" href="#symbol-identity-symbolhash" aria-label="Permalink to &quot;Symbol Identity: SymbolHash&quot;">​</a></h3><p>Every symbol in the system is identified by a SymbolHash (<code>uint64_t</code>). It is generated from Clang&#39;s USR (Unified Symbol Resolution) — a canonical string representation of symbol identity that encodes namespace, class name, function signature, template parameters, and other information.</p><p>Key properties of SymbolHash:</p><ul><li><strong>Cross-file consistency</strong>: The same symbol has the same SymbolHash across different files — this is the foundation for cross-file navigation</li><li><strong>Compact and efficient</strong>: A 64-bit integer is better suited as a DenseMap key than a variable-length USR string</li><li><strong>Deterministic</strong>: The same declaration always produces the same hash</li></ul><h3 id="symbol-relation-model" tabindex="-1">Symbol Relation Model <a class="header-anchor" href="#symbol-relation-model" aria-label="Permalink to &quot;Symbol Relation Model&quot;">​</a></h3><p>The core data in the index consists of <strong>relations</strong> between symbols. Each relation contains:</p><ul><li><strong>Relation kind (RelationKind)</strong>: Definition, declaration, reference, weak reference, read, write, interface, implementation, type definition, base class, derived class, constructor, destructor, caller, callee, etc.</li><li><strong>Location</strong>: The source range where the relation occurs</li><li><strong>Target symbol</strong>: The other end of the relation (for type relations such as inheritance and calls)</li></ul><p>Complementing relations are <strong>occurrences</strong> — records of a symbol&#39;s simple presence at a location, without a relation kind.</p><p>This distinction allows different queries to be served efficiently:</p><ul><li><strong>Go to definition</strong>: Look up RelationKind::Definition</li><li><strong>Find references</strong>: Look up RelationKind::Reference</li><li><strong>Call hierarchy</strong>: Look up RelationKind::Caller / Callee</li><li><strong>Type hierarchy</strong>: Look up RelationKind::Base / Derived</li><li><strong>Symbol under cursor</strong>: Look up Occurrence</li></ul><h2 id="query-flow" tabindex="-1">Query Flow <a class="header-anchor" href="#query-flow" aria-label="Permalink to &quot;Query Flow&quot;">​</a></h2><h3 id="cross-file-queries" tabindex="-1">Cross-File Queries <a class="header-anchor" href="#cross-file-queries" aria-label="Permalink to &quot;Cross-File Queries&quot;">​</a></h3><p>Taking &quot;go to definition&quot; as an example, the query flow is:</p><ol><li><strong>Locate the cursor symbol</strong>: In the current file, find the Occurrence at the cursor position via byte offset to obtain the SymbolHash</li><li><strong>Query the symbol directory</strong>: Look up all files referencing this SymbolHash in ProjectIndex</li><li><strong>Per-file relation query</strong>: For each referencing file, determine whether it is currently open: <ul><li>If the file <strong>is open</strong> (has a Session), use its OpenFileIndex to look up relation data, <strong>skipping</strong> the corresponding MergedIndex shard</li><li>If the file <strong>is not open</strong>, use the MergedIndex shard to look up relation data</li></ul></li></ol><p>Key point: For any given file, OpenFileIndex is <strong>preferred</strong> over MergedIndex — open files are queried via OpenFileIndex first, falling back to MergedIndex when the session AST is dirty or unavailable. Closed files are always queried via MergedIndex. This ensures that query results for open files reflect the latest buffer contents whenever possible.</p><h3 id="real-time-override-for-open-files" tabindex="-1">Real-Time Override for Open Files <a class="header-anchor" href="#real-time-override-for-open-files" aria-label="Permalink to &quot;Real-Time Override for Open Files&quot;">​</a></h3><p>Each open file maintains an OpenFileIndex in its Session, storing index data from the most recent compilation. It contains the same types of data as a MergedIndex shard (relations and occurrences), but sourced from in-memory AST compilation results rather than background indexing.</p><p>Design principle: The open file&#39;s index is not written to the Workspace (does not affect global state); only after the file is saved does background indexing update the MergedIndex. This preserves the stability of the global index — half-typed code that may be incomplete should not pollute query results for other files.</p><p>During queries, if a referenced file is open, the query prefers that file&#39;s OpenFileIndex over the MergedIndex shard, falling back to MergedIndex when the AST is dirty or unavailable. This ensures the user sees results corresponding to the latest buffer contents whenever possible.</p><h2 id="background-indexing" tabindex="-1">Background Indexing <a class="header-anchor" href="#background-indexing" aria-label="Permalink to &quot;Background Indexing&quot;">​</a></h2><h3 id="scheduling-strategy" tabindex="-1">Scheduling Strategy <a class="header-anchor" href="#scheduling-strategy" aria-label="Permalink to &quot;Scheduling Strategy&quot;">​</a></h3><p>Background indexing is managed by the Indexer, which maintains a queue of files awaiting indexing. It uses the following scheduling strategy:</p><ul><li><strong>Idle-timeout batching</strong>: Files are not indexed immediately upon entering the queue. Instead, an idle timer is set. When the timer fires, all files currently in the queue are processed as a batch. This avoids frequent small indexing runs.</li><li><strong>Pause/resume</strong>: When the user initiates interactive requests (hover, completion, etc.), background indexing can be paused to prioritize user requests. Nested pause semantics are supported.</li></ul><h3 id="index-merging" tabindex="-1">Index Merging <a class="header-anchor" href="#index-merging" aria-label="Permalink to &quot;Index Merging&quot;">​</a></h3><p>After background indexing completes, the TUIndex merge process is:</p><ol><li>Merge symbols from the TUIndex into ProjectIndex&#39;s global symbol table</li><li>Merge each file&#39;s FileIndex from the TUIndex into the corresponding MergedIndex shard</li><li>Update reference file bitmaps</li></ol><p>Merging is incremental — only new data is processed; there is no need to rebuild the entire index.</p><h2 id="serialization-and-persistence" tabindex="-1">Serialization and Persistence <a class="header-anchor" href="#serialization-and-persistence" aria-label="Permalink to &quot;Serialization and Persistence&quot;">​</a></h2><p>The index is serialized using FlatBuffers, supporting efficient on-disk persistence and lazy loading. FlatBuffers&#39; zero-copy property allows the index to be accessed directly via memory mapping, avoiding the overhead of full deserialization.</p><p>Each MergedIndex shard is stored as an independent file on disk and loaded on demand. ProjectIndex is serialized as a whole since it is global data.</p><h2 id="design-decisions-and-trade-offs" tabindex="-1">Design Decisions and Trade-offs <a class="header-anchor" href="#design-decisions-and-trade-offs" aria-label="Permalink to &quot;Design Decisions and Trade-offs&quot;">​</a></h2><p><strong>Why three levels instead of two?</strong> The separation of ProjectIndex and MergedIndex is critical. If location information were also stored in ProjectIndex, its size would balloon, making it unusable as a lightweight &quot;directory.&quot; Without ProjectIndex, every cross-file query would need to scan all MergedIndex shards to locate files containing a symbol — unacceptable for large projects.</p><p><strong>Why don&#39;t open-file indexes write to global state?</strong> This is part of the two-layer state model (Workspace vs. Session). Unsaved edits may consist of incomplete code with syntax errors. Writing them to the global index would pollute query results for other files. Only stable state that has been saved to disk should affect the global index.</p><p><strong>Why use content-addressed deduplication?</strong> Header files are frequently included by multiple source files, each compilation producing identical index data. Content addressing avoids storing N copies of the same data. When header content changes, old data is naturally replaced by the new version.</p><h2 id="known-limitations-and-future-directions" tabindex="-1">Known Limitations and Future Directions <a class="header-anchor" href="#known-limitations-and-future-directions" aria-label="Permalink to &quot;Known Limitations and Future Directions&quot;">​</a></h2><ul><li><strong>Symbol table locality</strong>: Currently all symbols (including function-local variables, etc.) are merged into ProjectIndex&#39;s global symbol table. This causes a large number of symbols that are meaningful only within a single file to be stored globally, wasting memory and merge time. The improvement direction is to introduce file-level SymbolTables — local symbols stay in TUIndex / MergedIndex shards, and only truly cross-file-referenced symbols enter ProjectIndex.</li><li><strong>Fuzzy symbol search</strong>: Current symbol search uses simple substring matching. For scenarios like agent and workspace symbol queries, a more efficient fuzzy search index is needed (tokenization, prefix trees, n-gram indexes, etc.).</li></ul>`,65)])])}const p=i(o,[["render",s]]);export{g as __pageData,p as default};
