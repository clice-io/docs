import{_ as t,o as i,c as n,a3 as o}from"./chunks/framework.CRbjHCyB.js";const u=JSON.parse('{"title":"Compilation Context","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/compilation-context.md","filePath":"en/clice/design/compilation-context.md"}'),s={name:"clice/design/compilation-context.md"};function a(r,e,l,c,h,d){return i(),n("div",null,[...e[0]||(e[0]=[o(`<h1 id="compilation-context" tabindex="-1">Compilation Context <a class="header-anchor" href="#compilation-context" aria-label="Permalink to &quot;Compilation Context&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>In C++, the same file can produce entirely different compilation results under different conditions. For source files, different compilation commands (<code>-std=c++17</code> vs <code>-std=c++20</code>, different <code>-D</code> macro definitions, etc.) lead to different ASTs. For header files, the situation is even more complex -- when the same header is included by different source files, the preceding <code>#include</code> and <code>#define</code> directives differ, potentially producing entirely different preprocessing results.</p><p>Yet no existing language server -- not just those for C++ -- formally addresses this problem. When a user opens a file in their editor, the language server simply picks a &quot;default&quot; compilation context to provide services. When that default choice is wrong, the user sees incorrect diagnostics, incorrect completion suggestions, and incorrect navigation targets.</p><p>Taking clangd as an example, the community has long struggled with numerous related issues:</p><ul><li>Headers display incorrect diagnostics after being opened, because clangd chose the wrong host source file</li><li>In multi-configuration projects (e.g., supporting both Debug/Release or different platforms), users cannot select which set of compilation commands to use</li><li>Non-self-contained headers lack proper context, leading to a flood of false positives</li></ul><p>clice treats <strong>Compilation Context</strong> as a first-class citizen throughout its entire design, fundamentally solving these problems.</p><h2 id="what-is-a-compilation-context" tabindex="-1">What is a Compilation Context <a class="header-anchor" href="#what-is-a-compilation-context" aria-label="Permalink to &quot;What is a Compilation Context&quot;">​</a></h2><p>A compilation context is &quot;the complete set of conditions needed to correctly compile a file.&quot; Its meaning varies by file type:</p><h3 id="compilation-context-for-source-files" tabindex="-1">Compilation Context for Source Files <a class="header-anchor" href="#compilation-context-for-source-files" aria-label="Permalink to &quot;Compilation Context for Source Files&quot;">​</a></h3><p>For source files (<code>.cpp</code>), the compilation context is simply their <strong>compilation command</strong> -- the compilation options read from the compilation database (CDB). A source file may correspond to multiple compilation commands (e.g., in multi-configuration projects), with each command representing a different compilation context.</p><p>This case is relatively straightforward: the user selects a compilation command, thereby selecting a compilation context.</p><h3 id="compilation-context-for-header-files" tabindex="-1">Compilation Context for Header Files <a class="header-anchor" href="#compilation-context-for-header-files" aria-label="Permalink to &quot;Compilation Context for Header Files&quot;">​</a></h3><p>For header files (<code>.h</code>), there are two cases:</p><p><strong>Self-contained headers</strong>: If a header can be compiled independently without a synthesized host prefix (for example, it has its own CDB entry and does not rely on preceding includes or macros from a host source file), it can be treated like a source file and compiled directly using its own compilation command.</p><p><strong>Non-self-contained headers</strong>: If a header depends on preceding context provided by the source file that includes it (earlier <code>#include</code> and <code>#define</code> directives, etc.), a compilation context must be <strong>synthesized</strong>. This synthesized context consists of:</p><ul><li><strong>Host source file</strong>: A source file that includes the target header</li><li><strong>Include chain</strong>: The include path from the host source file to the target header</li><li><strong>Prefix code (preamble)</strong>: Everything in the host source file that appears before <code>#include &quot;target.h&quot;</code></li></ul><p>This is the concept of a <strong>Header File Context</strong> -- essentially a specialization of compilation context for the header file scenario.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="compilation-context-resolution-flow" tabindex="-1">Compilation Context Resolution Flow <a class="header-anchor" href="#compilation-context-resolution-flow" aria-label="Permalink to &quot;Compilation Context Resolution Flow&quot;">​</a></h3><p>When a file needs to be compiled, its compilation context is determined by the following priority:</p><ol><li><strong>User&#39;s explicit selection</strong>: If the user has specified a compilation context via the extension protocol (<code>clice/switchContext</code>), use the user&#39;s selection</li><li><strong>Direct CDB lookup</strong>: If the file itself has an entry in the compilation database, use that entry&#39;s compilation command</li><li><strong>Header context synthesis</strong>: If the file is not in the CDB (the typical header file scenario), automatically synthesize a compilation context</li></ol><h3 id="header-context-synthesis" tabindex="-1">Header Context Synthesis <a class="header-anchor" href="#header-context-synthesis" aria-label="Permalink to &quot;Header Context Synthesis&quot;">​</a></h3><p>The header context synthesis process works as follows:</p><p><strong>Step 1: Find the host source file.</strong> Using the DependencyGraph&#39;s reverse include graph, perform a BFS upward search for all source files that transitively include the target header. Select the first source file that has a CDB entry as the host.</p><p><strong>Step 2: Compute the include chain.</strong> From the host source file to the target header, find the shortest path through the forward include graph. For example: <code>main.cpp -&gt; utils.h -&gt; math.h</code> -- if the target is <code>math.h</code>, the include chain is <code>[main.cpp, utils.h, math.h]</code>.</p><p><strong>Step 3: Synthesize the prefix code.</strong> For each file in the include chain (except the final target file), read its contents, scan for the <code>#include</code> line that includes the next file, extract everything before that line, and add <code>#line</code> directives for accurate error location reporting.</p><p>For example, if <code>main.cpp</code> contains:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;vector&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DEBUG</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;utils.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() { ... }</span></span></code></pre></div><p>And <code>utils.h</code> contains:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#pragma</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> once</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;string&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;math.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> util_func</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>Then the synthesized prefix code for <code>math.h</code> would be:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#line</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 1 </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;main.cpp&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;vector&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DEBUG</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#line</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 1 </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;utils.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#pragma</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> once</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;string&gt;</span></span></code></pre></div><p><strong>Step 4: Cache the prefix file.</strong> Write the synthesized prefix code to a disk cache, using the xxh3 hash of its content as the filename, achieving content-addressed deduplication. The cache path is <code>{cache_dir}/header_context/{hash}.h</code>.</p><p><strong>Step 5: Inject into the compilation command.</strong> Use the host source file&#39;s CDB compilation command, replace the source file path with the target header path, and inject the prefix file via the <code>-include</code> flag. The resulting compilation command looks like:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>clang -std=c++17 -Iinclude -include cache/header_context/abc123.h -c math.h</span></span></code></pre></div><p>This way, <code>math.h</code> is compiled as if it were at the correct position within the host source file, with the correct preprocessor state.</p><h3 id="context-switching" tabindex="-1">Context Switching <a class="header-anchor" href="#context-switching" aria-label="Permalink to &quot;Context Switching&quot;">​</a></h3><p>clice supports user-initiated compilation context switching through LSP extension protocols:</p><ul><li><strong>Query available contexts (<code>clice/queryContext</code>)</strong>: Lists all available compilation contexts for a file. For headers, this returns all possible host source files; for source files, it returns all matching CDB entries.</li><li><strong>Query current context (<code>clice/currentContext</code>)</strong>: Returns the explicitly selected compilation context (if the user has called <code>switchContext</code>); returns empty if using the default context.</li><li><strong>Switch context (<code>clice/switchContext</code>)</strong>: The user selects a new compilation context. This clears all cached compilation artifacts for the file (PCH reference, header context, dependency snapshot) and marks the AST as dirty, triggering recompilation.</li></ul><p>Context switching is a relatively heavyweight operation -- it causes recompilation. But this is the correct behavior: different contexts mean different compilation results.</p><h3 id="interaction-with-pch-optimization" tabindex="-1">Interaction with PCH Optimization <a class="header-anchor" href="#interaction-with-pch-optimization" aria-label="Permalink to &quot;Interaction with PCH Optimization&quot;">​</a></h3><p>PCH (precompiled headers) plays an important role in header contexts. A header&#39;s prefix code is essentially a preamble, which can be compiled into a PCH to accelerate subsequent compilations.</p><p>PCH files are named by the <strong>hash</strong> of the prefix content on disk, achieving file-level content-addressed deduplication -- identical prefix content will only be stored as a single PCH file on disk. However, PCH cache lookup currently uses the file&#39;s path_id as the key (each file has its own cache entry), and cross-file cache sharing is not yet implemented. Changing the cache key to the content hash for true cross-file PCH sharing is a planned improvement.</p><h3 id="interaction-between-compilation-context-and-indexing" tabindex="-1">Interaction Between Compilation Context and Indexing <a class="header-anchor" href="#interaction-between-compilation-context-and-indexing" aria-label="Permalink to &quot;Interaction Between Compilation Context and Indexing&quot;">​</a></h3><p>The indexing system is inherently aware of compilation contexts. The same header file under different compilation contexts may produce different symbol relations (for example, a function might be defined through conditional compilation in one context but not exist in another). MergedIndex merges index data produced from the same file under different compilation contexts, allowing users to query cross-context symbol information through a unified view.</p><h2 id="core-dependencies" tabindex="-1">Core Dependencies <a class="header-anchor" href="#core-dependencies" aria-label="Permalink to &quot;Core Dependencies&quot;">​</a></h2><ul><li><strong>DependencyGraph</strong>: Provides forward and reverse include relationship queries; the foundation for determining host source files and include chains</li><li><strong>CompilationDatabase</strong>: Provides compilation commands for source files</li><li><strong>PathPool</strong>: Internalized file path identifiers</li><li><strong>Session</strong>: Stores the compilation context state for each open file (<code>header_context</code>, <code>active_context</code>)</li><li><strong>Workspace</strong>: Stores the PCH cache, shared across multiple Sessions</li></ul><h2 id="design-decisions-and-trade-offs" tabindex="-1">Design Decisions and Trade-offs <a class="header-anchor" href="#design-decisions-and-trade-offs" aria-label="Permalink to &quot;Design Decisions and Trade-offs&quot;">​</a></h2><p><strong>Why inject via <code>-include</code> instead of modifying file contents?</strong> Using the <code>-include</code> flag lets Clang naturally handle the prefix code without modifying the target file&#39;s buffer. This preserves file content consistency, while <code>#line</code> directives ensure diagnostics point to the correct original file and line number.</p><p><strong>Why not &quot;compile the host source file and stop at the include&quot;?</strong> This approach (compiling the host source file directly and setting a hook to stop when the target header is reached) is conceptually simpler, but has a fundamental problem: it does not support PCH optimization. Since the host source file is the compilation subject, it is difficult to perform fine-grained preamble splitting and PCH caching. For frequently edited files, PCH caching is critical to performance.</p><p><strong>Why choose the shortest include chain?</strong> Among multiple possible include chains, the shortest one produces the least prefix code, meaning a smaller PCH and faster compilation. Additionally, a shorter chain typically represents a more direct inclusion relationship, making it a more intuitive default context choice.</p><h2 id="known-limitations" tabindex="-1">Known Limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known Limitations&quot;">​</a></h2><ul><li><strong>Recursive includes</strong>: If the include graph contains cycles (broken by include guards or <code>#pragma once</code>), prefix synthesis needs to handle deduplication correctly. The current dependency scanner already handles cycles, but the prefix synthesis path has not been fully validated for this scenario.</li><li><strong>Determinism of host selection</strong>: When multiple host source files are available, the current implementation selects the first one found. A better strategy might prioritize the host that is &quot;closest&quot; to the target header (e.g., a source file in the same directory), but this would require heuristics.</li></ul>`,54)])])}const g=t(s,[["render",a]]);export{u as __pageData,g as default};
