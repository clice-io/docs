import{_ as t,o as a,c as o,a3 as r}from"./chunks/framework.aYYe0dfI.js";const m=JSON.parse('{"title":"Module extract:ast","description":"The extract:ast module is responsible for extracting structured symbol and relation information from C++ source code by traversing the Clang AST. It implements internal visitor classes (SymbolExtractorVisitor, SymbolExtractorConsumer, SymbolExtractorAction) that walk declarations, expressions, and references to compute unique symbol identifiers, capture source snippet bounds, and record edges such as calls, inheritance, and references. Additional helpers handle lexical context description, doc comment extraction, function signature building, and dependency file collection.","frontmatter":{"title":"Module extract:ast","description":"The extract:ast module is responsible for extracting structured symbol and relation information from C++ source code by traversing the Clang AST. It implements internal visitor classes (SymbolExtractorVisitor, SymbolExtractorConsumer, SymbolExtractorAction) that walk declarations, expressions, and references to compute unique symbol identifiers, capture source snippet bounds, and record edges such as calls, inheritance, and references. Additional helpers handle lexical context description, doc comment extraction, function signature building, and dependency file collection.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/extract/ast.md","filePath":"en/clore/modules/extract/ast.md"}'),c={name:"clore/modules/extract/ast.md"};function i(n,e,s,d,l,u){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="module-extract-ast" tabindex="-1">Module <code>extract:ast</code> <a class="header-anchor" href="#module-extract-ast" aria-label="Permalink to &quot;Module `extract:ast`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>extract:ast</code> module is responsible for extracting structured symbol and relation information from C++ source code by traversing the Clang AST. It implements internal visitor classes (<code>SymbolExtractorVisitor</code>, <code>SymbolExtractorConsumer</code>, <code>SymbolExtractorAction</code>) that walk declarations, expressions, and references to compute unique symbol identifiers, capture source snippet bounds, and record edges such as calls, inheritance, and references. Additional helpers handle lexical context description, doc comment extraction, function signature building, and dependency file collection.</p><p>Publicly, the module exposes the <code>extract_symbols</code> function as the entry point for triggering extraction from a given resource, and two result types: <code>ASTResult</code> (which holds extracted symbols, relations, and dependencies) and <code>ASTError</code> (which encapsulates extraction failure details). Together these provide a complete interface for analyzing a single translation unit and producing a structured representation of its declarations and their interrelationships.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./compiler"><code>extract:compiler</code></a></li><li><a href="./model"><code>extract:model</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./cache"><code>extract:cache</code></a></li></ul><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-extract-asterror" tabindex="-1"><code>clore::extract::ASTError</code> <a class="header-anchor" href="#clore-extract-asterror" aria-label="Permalink to &quot;`clore::extract::ASTError`&quot;">​</a></h3><p>Declaration: <code>extract/ast.cppm:26</code></p><p>Definition: <code>extract/ast.cppm:26</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct <code>clore::extract::ASTError</code> is an aggregate error type with a single public data member <code>message</code> of type <code>std::string</code>. Internally, the struct imposes no invariants beyond those inherited from <code>std::string</code>; the <code>message</code> field may be empty or contain any valid character sequence. The struct has no user‑declared constructors, destructor, or assignment <code>operator</code>s, so the compiler implicitly generates a default constructor, copy/move constructors, and copy/move assignment <code>operator</code>s. No member functions or friend definitions are present, making <code>clore::extract::ASTError</code> a trivial wrapper used solely to convey an error string across the extraction boundary.</p><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>message</li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Thrown as an exception when AST extraction encounters an error.</li><li>Caught by callers to inspect the error message and handle failure.</li></ul><h3 id="clore-extract-astresult" tabindex="-1"><code>clore::extract::ASTResult</code> <a class="header-anchor" href="#clore-extract-astresult" aria-label="Permalink to &quot;`clore::extract::ASTResult`&quot;">​</a></h3><p>Declaration: <code>extract/ast.cppm:37</code></p><p>Definition: <code>extract/ast.cppm:37</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct <code>clore::extract::ASTResult</code> serves as a simple aggregate container for the output of AST extraction. Internally, it holds three public member vectors: <code>symbols</code> of type <code>std::vector&lt;SymbolInfo&gt;</code>, <code>relations</code> of type <code>std::vector&lt;ExtractedRelation&gt;</code>, and <code>dependencies</code> of type <code>std::vector&lt;std::string&gt;</code>. No invariants beyond those implied by the types are enforced; each vector may be empty or contain well-formed elements depending on the extraction context. The struct relies on the implicitly defined default constructor, destructor, and copy/move operations, making it trivially copyable and movable. There are no additional member functions or access controls, so all fields are directly modifiable. This design is intentionally lightweight, acting as a straightforward transfer object between the extraction logic and its consumers.</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>No explicit invariants documented beyond the type being an aggregate.</li></ul><h4 id="key-members-1" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-1" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>symbols</li><li>relations</li><li>dependencies</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Returned as the result type from AST extraction functions.</li><li>Consumed by downstream processes that analyze or transform the extracted data.</li></ul><h3 id="clore-extract-extractedrelation" tabindex="-1"><code>clore::extract::ExtractedRelation</code> <a class="header-anchor" href="#clore-extract-extractedrelation" aria-label="Permalink to &quot;`clore::extract::ExtractedRelation`&quot;">​</a></h3><p>Declaration: <code>extract/ast.cppm:30</code></p><p>Definition: <code>extract/ast.cppm:30</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct <code>clore::extract::ExtractedRelation</code> is an aggregate type that stores a single directed relation between two symbols. It holds two <code>SymbolID</code> members, <code>from</code> and <code>to</code>, which identify the source and target of the relation. Two boolean flags, <code>is_call</code> and <code>is_inheritance</code>, encode the relation kind; the implementation defaults both to <code>false</code> (via in-class initializers), meaning a default-constructed <code>ExtractedRelation</code> represents no specific edge type. The only implied invariant is that the flags can be used independently—when <code>is_inheritance</code> is <code>true</code>, <code>from</code> denotes the derived type and <code>to</code> the base type. There are no explicit constructors or member functions; the struct relies on default member initializers and aggregate initialization, making it a plain data carrier with trivial copy and move semantics.</p><h4 id="invariants-1" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-1" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>from</code> and <code>to</code> are valid <code>SymbolID</code> values.</li><li>At most one of <code>is_call</code> or <code>is_inheritance</code> may be true? Not specified; both can be false or true.</li></ul><h4 id="key-members-2" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-2" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>from</code></li><li><code>to</code></li><li><code>is_call</code></li><li><code>is_inheritance</code></li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as part of the extraction output to record symbol relationships.</li><li>Inspected to determine call or inheritance dependencies.</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-extract-extract-symbols" tabindex="-1"><code>clore::extract::extract_symbols</code> <a class="header-anchor" href="#clore-extract-extract-symbols" aria-label="Permalink to &quot;`clore::extract::extract_symbols`&quot;">​</a></h3><p>Declaration: <code>extract/ast.cppm:43</code></p><p>Definition: <code>extract/ast.cppm:669</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The implementation of <code>clore::extract::extract_symbols</code> orchestrates a full Clang-based extraction pass over a single translation unit. After validating that the <code>CompileEntry</code> provides a non-empty argument list and successfully constructing a compiler instance, the function forces the frontend into <code>ParseSyntaxOnly</code> mode—ensuring no output files are generated—and then drives the <code>SymbolExtractorAction</code> through the standard <code>BeginSourceFile</code>, <code>Execute</code>, and <code>EndSourceFile</code> lifecycle. This action, initialized with references to <code>result.symbols</code> and a local <code>raw_relations</code> vector, populates those containers during traversal via the <code>SymbolExtractorVisitor</code>’s <code>VisitNamedDecl</code>, <code>VisitDeclRefExpr</code>, <code>VisitCallExpr</code>, <code>VisitMemberExpr</code>, and recursion on <code>TraverseFunctionDecl</code> / <code>TraverseCXXMethodDecl</code>.</p><p>After extraction completes, dependencies are collected using <code>collect_dependency_files</code> from the <code>SourceManager</code>. Finally, every <code>RelationEdge</code> in <code>raw_relations</code> is mapped to an <code>ExtractedRelation</code>, translating the <code>RelationKind::Call</code> and <code>RelationKind::Inheritance</code> enumerators into boolean flags. The resulting <code>ASTResult</code> is returned, containing <code>symbols</code>, <code>relations</code>, and <code>dependencies</code>; any errors during instance creation or action execution propagate as an <code>ASTError</code>.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>const <code>CompileEntry</code> &amp;entry</li><li>entry<code>.arguments</code></li><li>entry<code>.file</code></li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called to extract AST symbols and relations for a single compile entry</li><li>used in vectorized or async extraction flows</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>extract:ast</code> module forms the core AST analysis layer of the extraction pipeline. It depends on <code>extract:compiler</code> for compilation‑entry data and on <code>extract:model</code> for the output data structures (<code>ASTResult</code>, <code>ASTError</code>, <code>ExtractedRelation</code>). The module also imports the standard library and the <code>support</code> utility module. Its public entry point is <code>extract_symbols</code>, which accepts a resource identifier and triggers extraction; the result is an <code>int</code> status code.</p><p>Internally, the module is decomposed into Clang‑based AST traversal machinery, all contained in an anonymous namespace. A <code>SymbolExtractorVisitor</code> (a <code>RecursiveASTVisitor</code>) traverses the AST, recording symbol metadata via <code>VisitNamedDecl</code>, <code>TraverseFunctionDecl</code>, and other overrides, and building dependency edges (<code>RelationEdge</code>) for calls, references, and inheritance through dedicated methods such as <code>VisitCallExpr</code>, <code>VisitDeclRefExpr</code>, and <code>VisitMemberExpr</code>. The visitor is driven by a <code>SymbolExtractorConsumer</code> that receives the translation unit and orchestrates the traversal. A <code>SymbolExtractorAction</code> (an <code>ASTFrontendAction</code>) wraps the consumer and exposes the extracted symbol and relation vectors to the caller. Supporting utilities—<code>compute_symbol_id</code>, <code>classify_decl</code>, <code>describe_lexical_context</code>, <code>get_source_snippet_bounds</code>, <code>hash_source_snippet_bytes</code>, and dependency‑file collection—are implemented as free functions in the same namespace, providing a clean internal layering that isolates Clang‑specific logic from the public API.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./compiler">Module extract:compiler</a></li><li><a href="./model">Module extract:model</a></li><li><a href="./../support/">Module support</a></li></ul>',58)])])}const p=t(c,[["render",i]]);export{m as __pageData,p as default};
