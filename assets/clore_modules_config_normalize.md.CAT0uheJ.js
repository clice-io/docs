import{_ as o,o as a,c as i,a3 as r}from"./chunks/framework.aYYe0dfI.js";const h=JSON.parse('{"title":"Module config:normalize","description":"The config:normalize module is responsible for normalizing configuration values within the clore system. Its public interface consists of the clore::config::normalize function, which accepts a mutable reference to an int representing a configuration value, modifies it according to normalization rules, and returns an int status code. The module also exposes the clore::config::NormalizeError struct containing a message field to communicate error details. Additional publicly accessible variables—such as make_absolute, workspace_root, normalize_separators, and configuration-related variables like config and path—define the normalization context and behavior. The implementation relies on the config:schema module for its core configuration types and on the standard library.","frontmatter":{"title":"Module config:normalize","description":"The config:normalize module is responsible for normalizing configuration values within the clore system. Its public interface consists of the clore::config::normalize function, which accepts a mutable reference to an int representing a configuration value, modifies it according to normalization rules, and returns an int status code. The module also exposes the clore::config::NormalizeError struct containing a message field to communicate error details. Additional publicly accessible variables—such as make_absolute, workspace_root, normalize_separators, and configuration-related variables like config and path—define the normalization context and behavior. The implementation relies on the config:schema module for its core configuration types and on the standard library.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/config/normalize.md","filePath":"en/clore/modules/config/normalize.md"}'),t={name:"clore/modules/config/normalize.md"};function c(n,e,l,s,d,u){return a(),i("div",null,[...e[0]||(e[0]=[r('<h1 id="module-config-normalize" tabindex="-1">Module <code>config:normalize</code> <a class="header-anchor" href="#module-config-normalize" aria-label="Permalink to &quot;Module `config:normalize`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>config:normalize</code> module is responsible for normalizing configuration values within the clore system. Its public interface consists of the <code>clore::config::normalize</code> function, which accepts a mutable reference to an <code>int</code> representing a configuration value, modifies it according to normalization rules, and returns an <code>int</code> status code. The module also exposes the <code>clore::config::NormalizeError</code> struct containing a <code>message</code> field to communicate error details. Additional publicly accessible variables—such as <code>make_absolute</code>, <code>workspace_root</code>, <code>normalize_separators</code>, and configuration-related variables like <code>config</code> and <code>path</code>—define the normalization context and behavior. The implementation relies on the <code>config:schema</code> module for its core configuration types and on the standard library.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./schema"><code>config:schema</code></a></li><li><code>std</code></li></ul><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-config-normalizeerror" tabindex="-1"><code>clore::config::NormalizeError</code> <a class="header-anchor" href="#clore-config-normalizeerror" aria-label="Permalink to &quot;`clore::config::NormalizeError`&quot;">​</a></h3><p>Declaration: <code>config/normalize.cppm:10</code></p><p>Definition: <code>config/normalize.cppm:10</code></p><p>Declaration: <a href="./../../namespaces/clore/config/"><code>Namespace clore::config</code></a></p><p>The struct <code>clore::config::NormalizeError</code> is implemented as a trivially-defined error type holding a single <code>std::string</code> member <code>message</code>. There are no user-declared constructors, destructors, or assignment <code>operator</code>s; the compiler‑generated special members suffice. No invariants are enforced beyond the default guarantees of <code>std::string</code>; the <code>message</code> field is expected to contain a human‑readable description of the normalization failure. Because the struct provides no further member functions or data, its role is limited to carrying an error string through the conventional error‑reporting path. The absence of any validation or formatting logic makes it an intentionally minimal error representation.</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>No explicit invariants are documented.</li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>message</code>: a <code>std::string</code> describing the error.</li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to report errors during configuration normalization.</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-config-normalize" tabindex="-1"><code>clore::config::normalize</code> <a class="header-anchor" href="#clore-config-normalize" aria-label="Permalink to &quot;`clore::config::normalize`&quot;">​</a></h3><p>Declaration: <code>config/normalize.cppm:14</code></p><p>Definition: <code>config/normalize.cppm:22</code></p><p>Declaration: <a href="./../../namespaces/clore/config/"><code>Namespace clore::config</code></a></p><p>The function <code>clore::config::normalize</code> processes a <code>TaskConfig</code> object by converting all path fields to absolute, normalized forms and ensuring consistent forward-slash separators. It first checks <code>config.workspace_root</code>; if empty, it defaults to <code>std::filesystem::current_path()</code>. The internal lambda <code>make_absolute</code> then converts each relevant path to an absolute path: for <code>workspace_root</code> it uses <code>std::filesystem::absolute</code>, while for <code>compile_commands_path</code>, <code>project_root</code>, and <code>output_root</code> it resolves relative paths against the now-absolute <code>workspace_root</code> (via <code>base / p</code>). If any path is empty, <code>make_absolute</code> returns a <code>std::unexpected</code> with a <code>NormalizeError</code> containing a descriptive <code>message</code> via <code>std::format</code>. After all absolute conversions succeed, a second lambda <code>normalize_separators</code> iterates over every character in the path string, replacing backslash <code>&#39;\\\\&#39;</code> with forward slash <code>&#39;/&#39;</code>. This normalization is applied not only to the four primary path fields but also to every element in <code>config.filter.include</code> and <code>config.filter.exclude</code>.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>modifies the input <code>TaskConfig</code> object in-place</li><li>normalizes path separators from backslash to forward slash</li><li>resolves relative paths to absolute paths</li></ul><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>config<code>.workspace_root</code></li><li>config<code>.compile_commands_path</code></li><li>config<code>.project_root</code></li><li>config<code>.output_root</code></li><li>config<code>.filter``.include</code></li><li>config<code>.filter``.exclude</code></li><li><code>std::filesystem::current_path()</code></li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>config<code>.workspace_root</code></li><li>config<code>.compile_commands_path</code></li><li>config<code>.project_root</code></li><li>config<code>.output_root</code></li><li>config<code>.filter``.include</code></li><li>config<code>.filter``.exclude</code></li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called to normalize a <code>TaskConfig</code> after loading</li><li>ensures all path fields are absolute and use forward slashes</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>config:normalize</code> module is a thin, focused layer that processes a single configuration value at a time. It imports the <code>config:schema</code> module to understand the structure of configuration data and the <code>std</code> module for fundamental types and utilities like <code>std::string</code>. Internally, it defines a <code>NormalizeError</code> struct (with a public <code>message</code> field) to carry error information and exposes a single public function, <code>clore::config::normalize</code>, which accepts a mutable reference to an integer configuration value and returns an integer status code. The module’s implementation is built around several file‑local variables—such as <code>normalize_separators</code> for separator‑replacement logic, <code>workspace_root</code> and <code>make_absolute</code> for resolving relative paths, and multiple <code>r</code> variables that likely act as temporary result holders. This decomposition keeps the main function’s logic modular without exposing internal helpers, while the error type allows callers to distinguish success from specific issues during normalization.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./schema">Module config:schema</a></li></ul>',35)])])}const f=o(t,[["render",c]]);export{h as __pageData,f as default};
