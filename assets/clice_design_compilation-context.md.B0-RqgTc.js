import{_ as i,o as s,c as t,a3 as n}from"./chunks/framework.CRbjHCyB.js";const k=JSON.parse('{"title":"Compilation Context","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/compilation-context.md","filePath":"en/clice/design/compilation-context.md"}'),a={name:"clice/design/compilation-context.md"};function o(l,e,h,r,c,d){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="compilation-context" tabindex="-1">Compilation Context <a class="header-anchor" href="#compilation-context" aria-label="Permalink to &quot;Compilation Context&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>C++&#39;s <code>#include</code> is a very primitive code reuse mechanism — it is essentially textual substitution. Unlike the module systems universally adopted by modern languages, it has no isolation: what a header file sees when included depends entirely on the preprocessor state before the inclusion point. This means <strong>the same file can produce entirely different compilation results under different conditions</strong>, and makes compiler caching optimizations much harder.</p><p>Source files produce different results due to different compilation commands (Debug/Release, different platforms); header files produce entirely different expansions depending on which source file includes them and what <code>#define</code> and <code>#include</code> directives precede the inclusion.</p><p>When a user opens a file in their editor, the language server must choose an appropriate compilation context to serve it — diagnostics, completion, and go-to-definition all depend on this context. Existing language servers (not just those for C++) have no formal concept of compilation context — they implicitly pick a context but give users no way to perceive or control this choice. When the context is wrong, compilation produces errors and the user sees a flood of false-positive diagnostics. clice is the first language server to introduce compilation context as a formal concept.</p><p>Taking clangd as an example, the lack of a compilation context concept has led to long-standing issues in the community:</p><ul><li><p><strong>Non-self-contained headers are not supported</strong>. This is one of the earliest reported and still unresolved issues in the clangd community (clangd <a href="https://github.com/clangd/clangd/issues/45" target="_blank" rel="noreferrer">#45</a>). Since clangd has no concept of header context, it cannot restore the correct preprocessor state for non-self-contained headers, and users see a flood of meaningless errors when opening such files.</p></li><li><p><strong>Guessing compilation commands for headers</strong>. When clangd selects a compilation command for a header file, it mainly relies on path heuristics — guessing the &quot;most relevant&quot; source file from the compilation database (CDB) based on filename and directory structure, then borrowing that source file&#39;s compilation command. This guessing is unreliable and frequently picks the wrong file, leading to spurious diagnostics (clangd <a href="https://github.com/clangd/clangd/issues/1505" target="_blank" rel="noreferrer">#1505</a>).</p></li><li><p><strong>Cannot select context in multi-configuration projects</strong>. When a file appears multiple times in <code>compile_commands.json</code> (Debug/Release, different platforms), clangd picks only one entry, and the user has no control over which one is chosen (clangd <a href="https://github.com/clangd/clangd/issues/681" target="_blank" rel="noreferrer">#681</a>).</p></li></ul><p>clice&#39;s design incorporates the compilation context framework from the start — this concept is naturally woven into the entire system. Users can not only see the currently active compilation context, but also query available contexts and switch between them.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><p>A compilation context is <strong>the complete set of conditions needed to fully restore the compilation state of a file</strong>. Its meaning varies by file type.</p><p>For <strong>source files</strong>, the compilation context is their compilation command. A source file may correspond to multiple compilation commands in the CDB — common in multi-configuration projects. Each command represents a different compilation context, and which one is selected determines the file&#39;s behavior in the language server. For example, the same file can produce different code under Debug and Release:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> initialize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    auto*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> conn </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> create_connection</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#ifndef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NDEBUG</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    conn-&gt;debug_label </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;main&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       // Only present in Debug builds</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;connection created&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    conn-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Under Debug builds (<code>-O0</code>, without <code>NDEBUG</code> defined) and Release builds (<code>-O2 -DNDEBUG</code>), the visible symbols, diagnostics, and completion suggestions can all differ.</p><p>For <strong>header files</strong>, the compilation context falls into two cases. First, it helps to understand what &quot;self-contained&quot; means: a header file is self-contained if it does not depend on <code>#define</code> or <code>#include</code> directives provided by the includer and can compile independently on its own. The vast majority of modern C++ headers are self-contained — they <code>#include</code> all their dependencies and make no assumptions about the includer&#39;s state.</p><p>Self-contained headers can be treated like source files. However, headers are typically not in the CDB (the CDB only records compilation commands for source files), so even self-contained headers need to find a source file that includes them via the dependency graph and borrow that source file&#39;s compilation command (search paths, language standard, etc.). The key distinction is that self-contained headers do not need synthesized prefix code — borrowing the compilation command is sufficient.</p><blockquote><p>Strictly speaking, &quot;self-contained&quot; is a tendency rather than an absolute property. Even if a header can compile independently, the includer can still alter its semantics through <code>-D</code> flags or preceding <code>#define</code> / <code>#undef</code> directives. In practice, however, this is rarely done — a header designed to be self-contained should not depend on external preprocessor state.</p></blockquote><p>Non-self-contained headers have compilation results that depend on the source file that includes them. A classic example is X macros:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// errors.def — not self-contained, requires the includer to define the X macro</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">X</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Ok,           </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;success&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">X</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(NotFound,     </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;not found&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">X</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(InvalidArg,   </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;invalid argument&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">X</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Timeout,      </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;operation timed out&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Different source files include the same <code>errors.def</code>, each defining a different <code>X</code> macro, expanding into entirely different code:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// error_enum.cpp — generates an enum</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> X</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">code</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">msg</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) name </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> code,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">enum</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ErrorCode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;errors.def&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#undef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> X</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// error_message.cpp — generates a string mapping</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> X</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">code</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">msg</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">case</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> code: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> msg;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char*</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> error_message</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> code</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    switch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(code) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;errors.def&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#undef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> X</span></span></code></pre></div><p><code>errors.def</code> expands into completely different ASTs in the two source files — one is an enum definition, the other a switch statement. For such non-self-contained headers, the compilation context is called a <strong>Header Context</strong>, determined by two elements:</p><ol><li><strong>Host source file</strong>: A source file that directly or indirectly includes the header, providing the base compilation command</li><li><strong>Include position</strong>: The specific position in the host source file&#39;s include tree where the header is included</li></ol><p>The include position is important because the same header may be included multiple times within the same source file, with different preprocessor states each time (e.g., in the X macro pattern, a different macro is defined before each inclusion). All include positions in a source file can be flattened into an ordered sequence, with each position assigned a unique number. The host source file plus this number uniquely identifies a header context.</p><h2 id="how-the-compilation-context-is-determined" tabindex="-1">How the Compilation Context is Determined <a class="header-anchor" href="#how-the-compilation-context-is-determined" aria-label="Permalink to &quot;How the Compilation Context is Determined&quot;">​</a></h2><p>When a file needs to be compiled, the compilation context is automatically determined by the following priority:</p><ol><li><p><strong>User selection takes priority</strong>. If the user has actively selected a compilation context via <code>clice/switchContext</code>, that selection is used.</p></li><li><p><strong>Direct CDB lookup</strong>. If the file has an entry in the compilation database, that entry&#39;s compilation command is used.</p></li><li><p><strong>Dependency graph lookup</strong>. If the file is not in the CDB (the common case for headers), the DependencyGraph&#39;s include relationships are used to find a source file that includes it, and that source file&#39;s compilation command is borrowed. For self-contained headers, borrowing the compilation command is sufficient; for non-self-contained headers, prefix code must also be synthesized to restore the preprocessor state.</p></li></ol><p>Beyond automatic resolution, clice also provides three LSP extension requests that let users explicitly query and switch compilation contexts:</p><p><strong><code>clice/queryContext</code></strong> lists all available compilation contexts for a file. For headers, it returns all source files that can serve as hosts; for source files, it returns all matching CDB entries, using key flags from the compilation command (<code>-D</code>, <code>-std</code>, <code>-O</code>, etc.) as human-readable descriptions to distinguish them. Results support pagination. The returned context list is deduplicated — if two contexts are judged to produce the same compilation result, they are merged into a single entry. Otherwise, a widely-included header like <code>&lt;vector&gt;</code> could list thousands of contexts.</p><p><strong><code>clice/currentContext</code></strong> returns the currently active user selection. If the user has not actively switched (i.e., the automatically selected default context is in use), it returns empty.</p><p><strong><code>clice/switchContext</code></strong> switches to the user&#39;s chosen new context.</p><h2 id="header-context-in-practice" tabindex="-1">Header Context in Practice <a class="header-anchor" href="#header-context-in-practice" aria-label="Permalink to &quot;Header Context in Practice&quot;">​</a></h2><p>The header context is conceptually clear (host source file + include position), but given a header context, how to actually make Clang compile the header under the correct preprocessor state is an engineering problem that requires choosing an approach.</p><blockquote><p>The prefix synthesis discussed here only applies to headers the user has opened. Headers on disk that are not open do not need special handling — they are included and processed normally when each source file is compiled. The indexing system collects symbol information from headers as part of each source file&#39;s indexing pass, and MergedIndex merges the index data produced for the same header under different source files (see <a href="./index-design">Index Design</a> for the merging mechanism).</p></blockquote><p>clice uses <strong>prefix synthesis + <code>-include</code> injection</strong>: based on the host source file and include position from the header context, it extracts all content before the target header along the include chain, synthesizes it into prefix code, writes it to a prefix file on disk, and then injects that file into the compilation command via Clang&#39;s <code>-include</code> flag. The core advantage of this approach is its natural compatibility with PCH optimization — the prefix file&#39;s content is the target header&#39;s preamble, which can be compiled into a PCH and cached. When the user subsequently edits the header body, the large volume of header inclusions in the prefix does not need to be reprocessed each time. Detailed rationale for this design choice is in the FAQ section below. PCH construction, caching, and invalidation mechanisms are described in <a href="./incremental">Incremental Compilation Design</a>.</p><p>The synthesis process has four stages. The following example illustrates the process. Suppose the project has these files:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// main.cpp</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;vector&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DEBUG</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;utils.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() { ... }</span></span></code></pre></div><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// utils.h</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#pragma</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> once</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;string&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;math.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> util_func</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>The user opens <code>math.h</code>, which is not in the CDB, so prefix code must be synthesized for it.</p><p><strong>Step 1: Find the host source file.</strong> Using the DependencyGraph&#39;s reverse include graph, perform a BFS upward from <code>math.h</code> to find source files that transitively include it. In this example, <code>math.h</code> is included by <code>utils.h</code>, which is included by <code>main.cpp</code>, and <code>main.cpp</code> has a CDB entry, so it is selected as the host. If the user has specified a preferred host via <code>clice/switchContext</code>, that preference takes priority.</p><p><strong>Step 2: Compute the include chain.</strong> From host <code>main.cpp</code> to target <code>math.h</code>, BFS on the forward include graph finds the shortest path: <code>[main.cpp, utils.h, math.h]</code>.</p><p><strong>Step 3: Synthesize the prefix code.</strong> Along the include chain, for each file (except the final <code>math.h</code>), read its content, find the <code>#include</code> line that includes the next file, and extract everything before that line. Each fragment is prefixed with a <code>#line</code> directive to ensure diagnostics point to the correct original positions. The synthesized prefix code for this example is:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#line</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 1 </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;main.cpp&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;vector&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DEBUG</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#line</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 1 </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;utils.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#pragma</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> once</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;string&gt;</span></span></code></pre></div><p>The content after <code>#include &quot;utils.h&quot;</code> in <code>main.cpp</code> (<code>int main() { ... }</code>) is truncated, and the content after <code>#include &quot;math.h&quot;</code> in <code>utils.h</code> (<code>void util_func();</code>) is also truncated. This prefix code restores the preprocessor state that <code>math.h</code> would see in the original compilation: <code>&lt;vector&gt;</code> and <code>&lt;string&gt;</code> have been included, and <code>DEBUG</code> is defined as 1.</p><p>The prefix code is written to a disk cache, named by the xxh3 hash of its content, so identical content is stored only once.</p><p><strong>Step 4: Inject into the compilation command.</strong> Using the host <code>main.cpp</code>&#39;s CDB compilation command, replace the source file path with <code>math.h</code> and inject the prefix file via the <code>-include</code> flag:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">clang</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -std=c++17</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -Iinclude</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cache/header_context/a1b2c3d4.h</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> math.h</span></span></code></pre></div><p>Clang processes the <code>-include</code> prefix file first, then compiles <code>math.h</code>. The effect is that <code>math.h</code> is compiled as if at the <code>#include &quot;math.h&quot;</code> position in <code>main.cpp</code>, with the correct preprocessor state.</p><p>The synthesis result (host ID, prefix file path, content hash) is cached in the Session. As long as the host source file has not changed, subsequent compilations reuse the cached result without re-synthesizing.</p><h2 id="multi-context-in-indexing" tabindex="-1">Multi-Context in Indexing <a class="header-anchor" href="#multi-context-in-indexing" aria-label="Permalink to &quot;Multi-Context in Indexing&quot;">​</a></h2><p>Compilation context affects not only live compilation but also index construction. The same header file may produce different symbols under different compilation contexts. For example:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// config.h</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#ifdef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> USE_V2</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    using</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Handler</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AsyncHandler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Visible in context A</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#else</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    using</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Handler</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SyncHandler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // Visible in context B</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span></code></pre></div><p>If the index only records the result from one context, go-to-definition or find-references would miss information from the other context. MergedIndex merges and stores index data produced from the same file under different compilation contexts, returning the union of all contexts on queries — for <code>config.h</code>, find-references can find references to both <code>AsyncHandler</code> and <code>SyncHandler</code>. See <a href="./index-design">Index Design</a> for the specific merging and deduplication mechanisms.</p><h2 id="faq" tabindex="-1">FAQ <a class="header-anchor" href="#faq" aria-label="Permalink to &quot;FAQ&quot;">​</a></h2><ul><li><p><strong>Can the same source file provide multiple contexts for the same header?</strong></p><p>Yes. If the header has a header guard or <code>#pragma once</code>, multiple <code>#include</code> directives for it are effective only the first time, so a source file provides at most one context. But if the header has no include protection (e.g., X macro-style <code>.def</code> files), every <code>#include</code> introduces a new context. As described above, all effective include positions within a source file are numbered in order, so even when the same header is included multiple times, each inclusion can be uniquely distinguished by its number.</p></li><li><p><strong>Is injecting the prefix file via <code>-include</code> fully equivalent to the original compilation?</strong></p><p>For mechanisms defined by the C++ standard, yes. The only ways user code can observe file names and line numbers are <code>__FILE__</code>, <code>__LINE__</code>, and <code>std::source_location</code>. According to the C++ standard, <a href="https://en.cppreference.com/w/cpp/preprocessor/line" target="_blank" rel="noreferrer"><code>#line</code> directives</a> change the values of <code>__LINE__</code> and <code>__FILE__</code>, and <a href="https://en.cppreference.com/w/cpp/utility/source_location" target="_blank" rel="noreferrer"><code>std::source_location</code></a> behavior is also determined by them. Therefore, the <code>#line</code> directives in the prefix file ensure that all three correctly reflect the target header&#39;s position information.</p><p>However, two GCC/Clang compiler extensions behave differently: <code>__INCLUDE_LEVEL__</code> is 0 under <code>-include</code> mode (the target file is the main file), whereas in the original compilation it reflects the nested include depth; <code>__BASE_FILE__</code> returns the target header&#39;s path under <code>-include</code> mode, whereas in the original compilation it returns the host source file&#39;s path. These two extensions are rarely used in practice and have no impact on language server functionality.</p></li><li><p><strong>Why write the prefix file to disk instead of using a virtual file?</strong></p><p>Clang supports a virtual file system, so in theory the synthesized prefix code could exist only in memory without being written to disk. The choice to write disk files is mainly for debuggability: when users encounter problems, they can directly inspect the prefix file contents on disk, making it easier to diagnose issues and file bug reports. There is no particularly deep technical reason.</p></li><li><p><strong>Why not use an approach that &quot;compiles the host source file as the main unit and stops at the target position&quot;?</strong></p><p>Another approach that was considered: instead of synthesizing a prefix file, compile the host source file as the main compilation unit directly. Clang provides a declaration-level callback mechanism that allows checking the current source location after each top-level declaration is parsed. Using this mechanism, compilation can be stopped when parsing reaches the end of the target header based on the include position from the header context. At that point, the compiler has naturally accumulated the correct preprocessor state and can provide language services for the target header directly in that state.</p><p>This approach has clear advantages: no files need to be synthesized, no include chains need to be tracked, and specifying the host source file alone solves the problem. Moreover, for non-self-contained headers like X macros that are embedded inside function bodies, since the compilation subject is the complete source file, the surrounding context (braces, statements, etc.) before and after the include position is intact, so there are no mismatched syntax issues.</p><p>However, this approach has a critical flaw: <strong>it cannot effectively leverage PCH optimization</strong>. PCH works by precompiling and caching a contiguous preamble segment (typically a sequence of <code>#include</code> directives) at the beginning of a file. In the prefix synthesis approach, the target header is the compilation subject, and the synthesized prefix code is its preamble, which can be fully cached by PCH. But in the host compilation approach, the compilation subject is the host source file, and the target header is embedded somewhere in the include tree — typically not in the host source file&#39;s preamble region but somewhere in the middle of the file. Clang&#39;s PCH mechanism cannot split at such a position, so none of the content before the target header can be cached by PCH.</p><p>For a header the user is actively editing, the file body is the most frequently updated part, requiring recompilation on each edit. Without PCH caching for the prefix content, every compilation would need to reprocess the large volume of preceding header inclusions in the host source file — an unacceptable cost in large projects. The prefix synthesis approach makes the target header the compilation subject and the prefix code a separately cacheable preamble, fully preserving PCH generation and usage.</p></li></ul><h2 id="known-limitations" tabindex="-1">Known Limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known Limitations&quot;">​</a></h2><ul><li><p><strong>Self-containedness detection is not yet automated</strong>. In practice, the vast majority of headers are self-contained; only a few (X macro lists, internal implementation fragments, etc.) are not. Prefix code synthesis has non-trivial overhead — finding the host, computing the include chain, synthesizing prefix code, and pulling in a large number of preceding dependencies — so it should not be performed for every opened header. The ideal strategy is: treat headers as self-contained by default; if compilation produces a large number of specific error types (e.g., undefined symbols), automatically fall back to non-self-contained mode and recompile with synthesized prefix code. Existing index information (e.g., whether the header produced errors during compilation in other source files) could also help with this judgment. This automatic detection mechanism is not yet implemented.</p></li><li><p><strong>Source file context switching is not fully implemented</strong>. <code>clice/queryContext</code> can already list multiple CDB entries for a source file, but <code>clice/switchContext</code> currently only supports switching the host for headers and does not yet support switching between multiple compilation commands for the same source file. This is a known improvement item.</p></li><li><p><strong>Determinism of host selection</strong>. When multiple hosts are available, the current implementation selects the first source file found by BFS that has a CDB entry. A better strategy would prioritize the host &quot;closest&quot; to the target header — source files in the same directory, files with similar names, etc. — but this requires more refined heuristics.</p></li><li><p><strong>No suffix handling</strong>. The current prefix synthesis approach only injects content before the include position and does not handle content after the include position. For most headers this is not an issue, but there are two edge cases.</p><p>First, non-self-contained headers like X macros that are embedded inside function bodies — the closing braces after the include position are lost:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// generate.cpp</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> register_all</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> X</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">code</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">msg</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">register_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(code, msg);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;errors.def&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Prefix truncates here, the } below is lost</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#undef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> X</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Second, when <code>#include</code> appears inside <code>#if</code>/<code>#ifndef</code> blocks, truncation produces unclosed conditional compilation directives:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// wrapper.h</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#ifndef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> USE_LEGACY</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;new_api.h&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;target.h&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // Prefix truncates here, #ifndef has no matching #endif</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#else</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;old_api.h&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span></code></pre></div><p>These incomplete syntax structures may cause Clang to report errors, but since the errors occur in the prefix file rather than the target header, they are typically filtered out. Suffix injection may need to be introduced in the future to handle these scenarios.</p></li><li><p><strong>Recursive includes in prefix synthesis</strong>. The include graph may contain cycles (broken by include guards or <code>#pragma once</code>). The dependency scanning stage already handles cycle detection correctly, but the prefix synthesis path has not been fully validated for this scenario.</p></li><li><p><strong>PCH cache does not yet support cross-file sharing</strong>. Currently, the PCH cache is keyed by the file&#39;s <code>path_id</code>, with each file maintaining its own PCH independently. If two different files happen to have identical preamble content, they each build their own PCH independently rather than sharing one. Changing the cache key to the preamble content hash to enable cross-file PCH sharing is a planned optimization.</p></li></ul>`,56)])])}const u=i(a,[["render",o]]);export{k as __pageData,u as default};
