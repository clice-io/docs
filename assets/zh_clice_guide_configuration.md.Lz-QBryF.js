import{_ as d,o as e,c as o,a3 as a}from"./chunks/framework.CRbjHCyB.js";const b=JSON.parse('{"title":"配置","description":"","frontmatter":{"outline":2,"head":[["meta",{"property":"og:title","content":"配置 | clice.io"}],["meta",{"property":"og:url","content":"https://docs.clice.io/zh/clice/guide/configuration"}],["meta",{"property":"og:image","content":"https://docs.clice.io/og.png"}],["meta",{"property":"og:image:width","content":"1200"}],["meta",{"property":"og:image:height","content":"630"}],["meta",{"property":"og:locale","content":"zh_CN"}],["link",{"rel":"canonical","href":"https://docs.clice.io/zh/clice/guide/configuration"}],["link",{"rel":"alternate","hreflang":"en","href":"https://docs.clice.io/clice/guide/configuration"}],["link",{"rel":"alternate","hreflang":"zh-CN","href":"https://docs.clice.io/zh/clice/guide/configuration"}],["link",{"rel":"alternate","hreflang":"x-default","href":"https://docs.clice.io/clice/guide/configuration"}]]},"headers":[],"relativePath":"zh/clice/guide/configuration.md","filePath":"zh/clice/guide/configuration.md","lastUpdated":1789832085000}'),i={name:"zh/clice/guide/configuration.md"};function c(s,t,h,n,l,r){return e(),o("div",null,[...t[0]||(t[0]=[a(`<h1 id="配置" tabindex="-1">配置 <a class="header-anchor" href="#配置" aria-label="Permalink to &quot;配置&quot;">​</a></h1><p>clice 从工作区根目录的 <code>clice.toml</code> 读取配置；若该文件不存在，则从 <code>.clice/config.toml</code> 读取。配置也可以通过 LSP <code>initializationOptions</code>（JSON 格式）传入；<code>initializationOptions</code> 中的值覆盖配置文件，合并后仍未设置的项由默认值填充。</p><p>配置只在服务器启动时读取一次。修改配置（无论哪个文件）都需要重启服务器，没有热重载。</p><p>完整配置的 JSON Schema 发布于 <a href="/clice-config.schema.json"><code>clice-config.schema.json</code></a>；支持根据 Schema 验证 TOML 或 JSON 的编辑器可以使用它。</p><p>文件中的相对路径和模式相对于配置文件自身所在的目录解析；通过 <code>initializationOptions</code> 传入的值则相对于工作区根目录解析。</p><h2 id="变量替换" tabindex="-1">变量替换 <a class="header-anchor" href="#变量替换" aria-label="Permalink to &quot;变量替换&quot;">​</a></h2><p>字符串值中支持以下变量：</p><table tabindex="0"><thead><tr><th>变量</th><th>说明</th></tr></thead><tbody><tr><td><code>\${workspace}</code></td><td>客户端提供的工作区目录</td></tr></tbody></table><h2 id="工作区" tabindex="-1">工作区 <a class="header-anchor" href="#工作区" aria-label="Permalink to &quot;工作区&quot;">​</a></h2><p>顶层选项，写在任何配置节之前。</p><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>default_configuration</code></td><td><code>string</code></td><td><code>&quot;&quot;</code></td></tr></tbody></table><p>当 <code>--configuration</code> 与持久化的选择都没有指定配置时，回退使用的构建配置，取规则上声明的标签之一。当规则带有标签而此项没有指向其中任何一个时，使用第一个声明的标签，并记录一条警告。</p></div><h2 id="project" tabindex="-1"><code>[project]</code> <a class="header-anchor" href="#project" aria-label="Permalink to &quot;\`[project]\`&quot;">​</a></h2><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>cache_dir</code></td><td><code>string</code></td><td><code>&quot;&quot;</code></td></tr></tbody></table><p>统一磁盘缓存的目录（PCH、PCM 和索引产物）。空值默认为 <code>\${workspace}/.clice</code>，该目录通过生成的 .gitignore 和 CACHEDIR.TAG 标记将自身排除在版本控制和备份之外（<code>.clice/config.toml</code> 对 Git 保持可见；遵循 CACHEDIR.TAG 的备份工具会跳过整个目录）；显式配置的目录不会添加这些标记。最终确定的路径会在启动时打印。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>logging_dir</code></td><td><code>string</code></td><td><code>&quot;&quot;</code></td></tr></tbody></table><p>日志文件目录；空值时使用 <code>\${cache_dir}/logs</code>。每次服务器会话都将日志写入各自带时间戳的子目录。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>enable_indexing</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>构建后台索引，为跨 TU 功能（查找引用、工作区符号等）提供服务。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>readonly</code></td><td><code>string</code></td><td><code>&quot;off&quot;</code></td></tr></tbody></table><p>为打开的文件提供只读服务：&quot;off&quot; 以为每个打开的文件构建完整 AST 为目标——构建由第一个需要它的请求触发，期间由索引应答；&quot;on&quot; 从不构建 PCH——读取操作仅由索引提供结果（冷文件会被插到索引队列前端），而代码补全和签名帮助仍会在没有 Preamble 的情况下按需编译；&quot;auto&quot; 使每个文件最初都处于 &quot;on&quot;，在首次出现编辑意图（编辑、代码补全、签名帮助、上下文切换）时切换到 &quot;off&quot;，如果索引无法为某个文件提供服务，则该文件回退到 &quot;off&quot;。功能路由始终选择当前可用的最佳来源进行应答。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>idle_timeout_ms</code></td><td><code>uint32</code></td><td><code>3000</code></td></tr></tbody></table><p>后台索引开始前的空闲等待时间，单位毫秒。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>test_hooks</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>启用测试框架使用的 clice/internal 测试钩子。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>stateful_worker_count</code></td><td><code>uint32</code></td><td><code>2</code></td></tr></tbody></table><p>有状态工作进程的数量——它们在内存中保存 AST 并处理查询（悬停、语义 Token 等）；<code>0</code> 无效，回退到默认值。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>stateless_worker_count</code></td><td><code>uint32</code></td><td>—</td></tr></tbody></table><p>无状态工作进程的初始数量——它们处理临时任务（PCH/PCM 构建、代码补全、签名帮助）；默认为机器并行度的一半，但至少为 2。<code>0</code> 无效，回退到该默认值。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>min_stateless_worker_count</code></td><td><code>uint32</code></td><td><code>1</code></td></tr></tbody></table><p>无状态工作进程动态伸缩的下限；<code>0</code> 无效，回退到默认值。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>max_stateless_worker_count</code></td><td><code>uint32</code></td><td>—</td></tr></tbody></table><p>无状态工作进程动态伸缩的上限；<code>0</code> 表示机器的并行度，这也是默认值。</p></div><h2 id="tracker" tabindex="-1"><code>[tracker]</code> <a class="header-anchor" href="#tracker" aria-label="Permalink to &quot;\`[tracker]\`&quot;">​</a></h2><p>文件跟踪器会轮询编辑器之外发生的变更（如 <code>git checkout</code>、重新生成的 <code>compile_commands.json</code>、代码生成器写出的头文件），使服务器无需重启即可检测到这些变更。将间隔设为 <code>0</code> 可禁用对应的轮询循环。</p><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>cdb_poll_seconds</code></td><td><code>uint32</code></td><td><code>3</code></td></tr></tbody></table><p>编译数据库轮询间隔，单位为秒；设为 0 将禁用轮询。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>workspace_poll_seconds</code></td><td><code>uint32</code></td><td><code>30</code></td></tr></tbody></table><p>工作区文件扫描间隔，单位为秒；设为 0 将禁用轮询。</p></div><h2 id="hover" tabindex="-1"><code>[hover]</code> <a class="header-anchor" href="#hover" aria-label="Permalink to &quot;\`[hover]\`&quot;">​</a></h2><p><code>[hover]</code> 节控制悬停卡片的渲染方式。</p><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>parse_comment_as_markdown</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>将悬停卡片渲染为 Markdown；<code>false</code> 时输出纯文本，供无法显示 Markdown 的客户端使用。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>show_aka</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>显示类型的去糖形式，例如 <code>vector&lt;int&gt;::size_type (aka unsigned long)</code>。</p></div><h2 id="inlay-hints" tabindex="-1"><code>[inlay_hints]</code> <a class="header-anchor" href="#inlay-hints" aria-label="Permalink to &quot;\`[inlay_hints]\`&quot;">​</a></h2><p><code>[inlay_hints]</code> 节控制服务器生成哪些内联提示类别。客户端刷新后会使用更新后的值请求提示；此过程不涉及重新编译。</p><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>enabled</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>总开关：<code>false</code> 会禁用所有内联提示。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>parameters</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>调用点的参数名提示，例如 <code>draw(width: 800, height: 600)</code>；以可变引用传递的实参还会带有 <code>&amp;</code> 标记。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>deduced_types</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>为 <code>auto</code> 变量、结构化绑定（structured bindings）和推导出的返回类型显示推导类型提示。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>designators</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>聚合初始化中的字段指示符提示，例如 <code>Point{1, 2}</code> 中的 <code>.x=</code> 和 <code>.y=</code>。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>block_end</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>在较长代码块（函数、类型、命名空间或控制流结构）的右花括号后显示 <code>// name</code> 提示。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>default_arguments</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>显示调用省略的默认实参，过长时缩写。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>type_name_limit</code></td><td><code>uint32</code></td><td><code>32</code></td></tr></tbody></table><p>渲染提示文本的字节预算：过长的推导类型会回退为带语法糖的拼写形式或直接省略，过长的默认实参会缩写。<code>0</code> 表示不设上限。</p></div><h2 id="code-completion" tabindex="-1"><code>[code_completion]</code> <a class="header-anchor" href="#code-completion" aria-label="Permalink to &quot;\`[code_completion]\`&quot;">​</a></h2><p><code>[code_completion]</code> 段控制补全项的组装。</p><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>enable_keyword_snippet</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>以代码片段形式补全关键字（尚未实现）。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>enable_function_arguments_snippet</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>补全函数调用时，以代码片段形式插入函数实参。对于函数，此设置适用于逐一列出的重载，因此需要 <code>bundle_overloads = false</code>；函数式宏没有重载集，始终使用代码片段。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>enable_template_arguments_snippet</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>补全时以代码片段形式插入模板实参（尚未实现）。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>insert_paren_in_function_call</code></td><td><code>bool</code></td><td><code>false</code></td></tr></tbody></table><p>补全函数调用时插入括号（尚未实现）。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>bundle_overloads</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>将重载集折叠为单个补全项。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>limit</code></td><td><code>uint32</code></td><td><code>0</code></td></tr></tbody></table><p>补全项的最大数量（尚未实现）。</p></div><h2 id="rules" tabindex="-1"><code>[[rules]]</code> <a class="header-anchor" href="#rules" aria-label="Permalink to &quot;\`[[rules]]\`&quot;">​</a></h2><p>规则通过模式指定文件，并说明这些文件从哪里获取编译命令，以及如何修改这些命令。匹配某个文件的每条规则都会生效，按声明顺序应用：声明在前的规则的数据库在该文件的候选中排在前面，第一条带 <code>default_command</code> 的匹配规则为没有条目的文件提供命令，<code>append</code> 与 <code>remove</code> 逐条累加，靠后的 <code>remove</code> 会抵消靠前的 <code>append</code>，而任何匹配规则上的 <code>index = false</code> 都会把文件排除在索引之外。带 <code>configuration</code> 标签的规则只在相应配置生效期间适用；不同的标签构成配置菜单。</p><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>patterns</code></td><td><code>array of string</code></td><td><code>[]</code></td></tr></tbody></table><p>用于选择此规则所适用文件的 glob 模式。相对模式以本配置文件所在目录为基准（允许 <code>..</code> 路径段），通过 initializationOptions 传入的规则则以工作区根目录为基准；绝对模式或以 <code>**</code> 开头的模式匹配文件的绝对路径。<code>*</code> 匹配一个路径段内的任意字符，<code>?</code> 匹配单个字符，<code>**</code> 匹配任意数量的路径段，<code>{a,b}</code> 表示备选项，<code>[0-9]</code> 表示字符范围，<code>[!...]</code> 表示取反的字符范围。省略时表示所有文件。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>configuration</code></td><td><code>string</code></td><td><code>&quot;&quot;</code></td></tr></tbody></table><p>构建配置标签。带标签的规则只在相应配置生效期间适用；不带标签的规则始终适用。不同的标签构成配置菜单；<code>--configuration</code>、持久化的选择和 <code>default_configuration</code> 按此顺序决定哪个配置生效。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>compile_commands</code></td><td><code>array of string</code></td><td><code>[]</code></td></tr></tbody></table><p>编译数据库，按优先级排列：可以是 compile_commands.json，也可以是包含它的目录，路径相对于本配置文件（通过 initializationOptions 传入的规则则相对于工作区根目录）。它们都会被加载，且无论模式如何，每个条目都适用于其对应的文件；模式和顺序决定同一文件出现在多个数据库中时默认使用哪个条目。不带匹配模式的规则指定的就是整个工作区的数据库。当所有规则都没有声明来源时，工作区根目录及其一级子目录中的每一份 compile_commands.json 都会被加载，打开某个文件时其上层各级目录中的那些也会被加载。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>default_command</code></td><td><code>string</code> 或 <code>array of string</code></td><td><code>&quot;&quot;</code></td></tr></tbody></table><p>匹配到但没有数据库条目的文件所用的编译命令，其中不含源文件本身：可以是一个按 shell 命令行方式分词的字符串，也可以是 argv 数组。它以读取到该命令的配置文件所在目录为工作目录运行（通过 initializationOptions 传入的规则则以工作区根目录为准），磁盘上匹配到的源文件会加入后台索引（除非规则关闭了 <code>index</code>）——这些文件在启动时枚举，之后每次工作区轮询时再枚举一遍，因此新建的文件打开时即可编译，并在一个轮询周期内加入索引。省略则不提供命令。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>append</code></td><td><code>array of string</code></td><td><code>[]</code></td></tr></tbody></table><p>为匹配的文件追加编译标志，例如 <code>[&quot;-std=c++20&quot;, &quot;-DNDEBUG&quot;]</code>。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>remove</code></td><td><code>array of string</code></td><td><code>[]</code></td></tr></tbody></table><p>为匹配的文件移除编译标志，例如 <code>[&quot;-Wall&quot;]</code>。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>index</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p>匹配到的翻译单元是否加入后台索引。<code>false</code> 会将它们排除在外；它们被打开时仍会编译，也仍会作为其所包含头文件的宿主。只要有一条匹配的规则写了 <code>false</code>，就以它为准。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>lint</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p><code>clice lint</code> 是否检查匹配到的文件。<code>false</code> 会将它们排除在外：它们的检查结果会被丢弃，以它们为主文件的翻译单元也不会被解析，除非 <code>--index</code> 需要它来构建索引。工作区之外的文件从不检查。只要有一条匹配的规则写了 <code>false</code>，就以它为准。</p></div><div class="config-option"><table tabindex="0"><thead><tr><th>选项</th><th>类型</th><th>默认值</th></tr></thead><tbody><tr><td><code>format</code></td><td><code>bool</code></td><td><code>true</code></td></tr></tbody></table><p><code>clice format</code> 是否格式化匹配到的文件。<code>false</code> 会将它们排除在外。工作区之外的文件从不格式化。只要有一条匹配的规则写了 <code>false</code>，就以它为准。</p></div><h2 id="示例" tabindex="-1">示例 <a class="header-anchor" href="#示例" aria-label="Permalink to &quot;示例&quot;">​</a></h2><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">compile_commands = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;build&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">append = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;-std=c++23&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">patterns = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;test/**&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">append = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;-DTEST_MODE&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>两个作为可切换配置的构建目录，以及一个没有编译数据库的项目：</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">default_configuration = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;debug&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">configuration = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;debug&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">compile_commands = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;build/debug&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">configuration = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;release&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">compile_commands = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;build/release&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">patterns = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;src/**&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;include/**&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">default_command = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;arm-none-eabi-gcc -std=c23 -mcpu=cortex-m4 -Iinclude&quot;</span></span></code></pre></div><h2 id="切换配置" tabindex="-1">切换配置 <a class="header-anchor" href="#切换配置" aria-label="Permalink to &quot;切换配置&quot;">​</a></h2><p>规则上的 <code>configuration</code> 标签构成一份菜单，每个服务器进程中有一个标签生效；不带标签的规则始终适用。生效的那个按优先级依次是 <code>--configuration &lt;tag&gt;</code> 参数（<code>clice serve</code>、<code>clice index</code>、<code>clice lint</code> 和 <code>clice inspect</code> 都接受它）、持久化的选择、<code>default_configuration</code>。选择在编辑器中完成——VS Code 的状态栏显示生效的配置，点击即可打开菜单，其他客户端调用 <code>clice/switchConfiguration</code>——并存放在 <code>state.json</code> 中，该文件位于 <code>cache_dir</code> 下，绝不写入 <code>clice.toml</code>；它在服务器再次启动时生效，VS Code 扩展会自行重启服务器。每个配置在 <code>cache_dir</code> 下保留各自的索引，因此来回切换绝不会重新索引某个配置已经索引过的内容。批处理命令也通过这些标签选择索引：<code>clice index --configuration release</code> 构建 release 配置的索引，<code>clice index --stats</code> 报告按同样方式解析出的配置所对应的索引。</p>`,65)])])}const u=d(i,[["render",c]]);export{b as __pageData,u as default};
