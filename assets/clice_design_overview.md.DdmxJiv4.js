import{_ as o,o as i,c as a,a3 as n}from"./chunks/framework.CRbjHCyB.js";const u=JSON.parse('{"title":"Source Code Overview","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/overview.md","filePath":"en/clice/design/overview.md"}'),t={name:"clice/design/overview.md"};function s(r,e,c,d,l,p){return i(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="source-code-overview" tabindex="-1">Source Code Overview <a class="header-anchor" href="#source-code-overview" aria-label="Permalink to &quot;Source Code Overview&quot;">​</a></h1><p>This document describes the responsibilities and role of each module in the clice source tree, providing readers with a high-level understanding of the overall project structure. For detailed design information on each module, refer to the corresponding topic documents.</p><h2 id="project-vision" tabindex="-1">Project Vision <a class="header-anchor" href="#project-vision" aria-label="Permalink to &quot;Project Vision&quot;">​</a></h2><p>clice is a brand-new C++ language server, redesigned from the architecture level to solve long-standing problems in previous C++ language servers. Key features:</p><ul><li><p><strong>Compilation Context</strong>: clice is the first language server to introduce compilation context as a formal concept. Every step of compilation, indexing, and querying explicitly distinguishes the current compilation context, and users can query and switch between them. See <a href="./compilation-context">Compilation Context</a>.</p></li><li><p><strong>Multi-process Architecture</strong>: A master + worker process model isolates Clang&#39;s memory leaks and crashes while enabling priority-aware scheduling and real-time memory monitoring. See <a href="./multi-process">Multi-process Architecture</a>.</p></li><li><p><strong>Coroutine-based Async Model</strong>: Built on C++20 coroutines and the kotatsu library, replacing traditional callback-style async and making business logic clearer.</p></li><li><p><strong>Unified Compile Scheduling</strong>: Every expensive build product — preamble PCHs, C++20 module PCMs, document ASTs, one-shot batch runs — is a node in a single compile task graph with shared builds, real-time cooperative cancellation, and dependency-aware invalidation. See <a href="./task-graph">Compile Task Graph</a>.</p></li></ul><h2 id="module-overview" tabindex="-1">Module Overview <a class="header-anchor" href="#module-overview" aria-label="Permalink to &quot;Module Overview&quot;">​</a></h2><h3 id="src-support-—-foundation-utility-library" tabindex="-1"><code>src/support/</code> — Foundation Utility Library <a class="header-anchor" href="#src-support-—-foundation-utility-library" aria-label="Permalink to &quot;`src/support/` — Foundation Utility Library&quot;">​</a></h3><p>General-purpose utilities and infrastructure shared by all other modules.</p><ul><li><code>CacheStore</code>: The on-disk artifact store for file-shaped build products (PCH/PCM and friends) — atomic pair commits, namespace quotas, LRU eviction, and crash recovery</li><li><code>FuzzyMatcher</code>: Token-aware fuzzy matching for code completion and symbol search</li><li>Markup / Doxygen: Parsing and formatting of documentation comments</li><li>Logging, filesystem abstractions, string utilities, etc.</li></ul><h3 id="src-vfs-—-file-identity-and-versions" tabindex="-1"><code>src/vfs/</code> — File Identity and Versions <a class="header-anchor" href="#src-vfs-—-file-identity-and-versions" aria-label="Permalink to &quot;`src/vfs/` — File Identity and Versions&quot;">​</a></h3><ul><li><code>FileTable</code>: Internalizes file paths as stable <code>Fid</code> identifiers used throughout the system, and owns the shared per-file facts derived from them — stat stamps, content versions, scan results, directory listings. The two-layer freshness check (stat fast path, then content hash with stamp repair) lives here once and is shared by every consumer: PCH validation, index staleness, and disk polling.</li></ul><h3 id="src-config-—-configuration" tabindex="-1"><code>src/config/</code> — Configuration <a class="header-anchor" href="#src-config-—-configuration" aria-label="Permalink to &quot;`src/config/` — Configuration&quot;">​</a></h3><ul><li><code>Config</code>: Loading and merging of <code>clice.toml</code> and LSP <code>initializationOptions</code>, strict unknown-key validation, and the JSON schema the configuration docs are generated from.</li></ul><h3 id="src-command-—-compilation-command-processing" tabindex="-1"><code>src/command/</code> — Compilation Command Processing <a class="header-anchor" href="#src-command-—-compilation-command-processing" aria-label="Permalink to &quot;`src/command/` — Compilation Command Processing&quot;">​</a></h3><p>Commands read from the compilation database (CDB) are raw commands generated by the build system and cannot be fed directly to the Clang frontend -- they may contain options meant only for code generation, lack system header search paths, or include parameters irrelevant to a language server. This module parses each command once into a structured form that the rest of the system consumes.</p><ul><li><code>CompilationDatabase</code>: Loads <code>compile_commands.json</code> and parses every entry once into an interned, classified argument structure identified by a stable <code>ConfigID</code>. On top of that it applies user configuration rules, synthesizes fallback commands for files without an entry, derives the input language, computes entry identity hashes, and renders driver or <code>-cc1</code> command lines on demand</li><li><code>Toolchain</code>: Queries system compilers for implicit compilation parameters (such as system header search paths), with a two-layer cache — probe results per driver invocation shape, and synthesized flag sets per input kind</li><li><code>SearchConfig</code>: A four-tier model for header search paths (Quoted / Angled / System / After), matching Clang&#39;s internal search logic</li></ul><p>See <a href="./command-resolve">Compilation Command Resolution</a>.</p><h3 id="src-compile-—-compilation-abstraction" tabindex="-1"><code>src/compile/</code> — Compilation Abstraction <a class="header-anchor" href="#src-compile-—-compilation-abstraction" aria-label="Permalink to &quot;`src/compile/` — Compilation Abstraction&quot;">​</a></h3><p>Wraps the Clang compiler, abstracting Clang APIs into safe, unified compilation interfaces. This layer is purely a compilation abstraction with no server logic.</p><ul><li><code>CompilationUnit</code> / <code>CompilationUnitRef</code>: RAII wrappers around the Clang AST context. <code>CompilationUnitRef</code> provides a unified read-only view for accessing source location mappings, preprocessor directives, AST nodes, and more. This is the primary input for <code>src/feature/</code> and <code>src/semantic/</code>.</li><li><code>CompilationParams</code>: Describes the complete configuration for a single compilation, including compilation type (Preamble / Content / Completion / Indexing, etc.), file remapping, PCH/PCM reuse, etc.</li><li>Diagnostic and clang-tidy collection during compilation.</li></ul><h3 id="src-syntax-—-lightweight-syntax-processing" tabindex="-1"><code>src/syntax/</code> — Lightweight Syntax Processing <a class="header-anchor" href="#src-syntax-—-lightweight-syntax-processing" aria-label="Permalink to &quot;`src/syntax/` — Lightweight Syntax Processing&quot;">​</a></h3><p>Syntax-level processing that does not require a full AST. Runs before compilation to quickly obtain structural information and dependency relationships for files.</p><ul><li><code>Lexer</code>: A token-level utility built on Clang&#39;s raw lexer. Does not run the preprocessor. Used for directive scanning, include path resolution, etc.</li><li><code>DependencyGraph</code>: A global include/module dependency graph supporting forward queries, reverse queries, host source file search, include chain lookup, etc.</li><li>Dependency scanning: Wraps Clang&#39;s <code>DependencyDirectivesScanner</code> to quickly extract include and module dependencies</li><li><code>IncludeResolver</code>: Resolves include paths to actual files based on search path configuration</li><li>Preamble synthesis: Builds the prefix/suffix files that let a header compile under its host&#39;s preprocessor state</li></ul><p>See <a href="./dependency-scanning">Dependency Scanning</a>.</p><h3 id="src-semantic-—-semantic-analysis" tabindex="-1"><code>src/semantic/</code> — Semantic Analysis <a class="header-anchor" href="#src-semantic-—-semantic-analysis" aria-label="Permalink to &quot;`src/semantic/` — Semantic Analysis&quot;">​</a></h3><p>Semantic analysis capabilities beyond Clang&#39;s native APIs. Takes a <code>CompilationUnitRef</code> and extracts higher-level semantic information.</p><ul><li><code>Semantics</code>: The unified semantic map — a single AST traversal per compilation records every interesting node and its token ownership; selection, feature projections, and index production are pure queries over the map rather than separate AST walks</li><li><code>TemplateResolver</code>: Resolves dependent names through pseudo-instantiation, enabling semantic analysis to see through template contexts. See <a href="./template-resolver">Template Resolver</a>.</li><li><code>SymbolKind</code> / <code>RelationKind</code>: Fine-grained symbol kinds and relation types</li></ul><h3 id="src-index-—-symbol-index" tabindex="-1"><code>src/index/</code> — Symbol Index <a class="header-anchor" href="#src-index-—-symbol-index" aria-label="Permalink to &quot;`src/index/` — Symbol Index&quot;">​</a></h3><p>Symbol indexing system with cross-translation-unit query support. Layered as:</p><ul><li><code>TUIndex</code>: Index rows produced from a single compilation, projected from the semantic map</li><li><code>ProjectIndex</code>: The global layer — externally visible symbols, plus the record of which translation units contributed which files</li><li><code>Shard</code>: The per-file storage unit. Each compilation context that preprocesses the file differently contributes a variant, deduplicated by the content identity of its encoded rows; names local to the file live in the shard itself, only external names enter the <code>ProjectIndex</code></li><li><code>BlobDatabase</code>: Persistence backend — all index blobs live in a single LMDB database</li></ul><p>See <a href="./symbol-index">Symbol Index</a>.</p><h3 id="src-feature-—-lsp-feature-implementations" tabindex="-1"><code>src/feature/</code> — LSP Feature Implementations <a class="header-anchor" href="#src-feature-—-lsp-feature-implementations" aria-label="Permalink to &quot;`src/feature/` — LSP Feature Implementations&quot;">​</a></h3><p>Concrete implementations of LSP features. Each feature takes a <code>CompilationUnitRef</code> and returns the corresponding LSP response data. This layer is purely computational -- it has no involvement with network communication, state management, or process scheduling.</p><p>Includes: code completion, hover information, signature help, semantic highlighting, inlay hints, document symbols, document links, folding ranges, formatting, diagnostics, etc.</p><blockquote><p><code>feature/</code> only covers single-file, AST-based feature implementations. Cross-file navigation features (go to definition, find references, etc.) are served from index data by the server&#39;s <code>service/</code> layer (<code>FeatureRouter</code>/<code>IndexQuery</code>). Some features involve multi-phase processing -- for example, include path completion in code completion can be resolved at the syntax layer without full compilation.</p></blockquote><h3 id="src-sched-—-compile-scheduling-core" tabindex="-1"><code>src/sched/</code> — Compile Scheduling Core <a class="header-anchor" href="#src-sched-—-compile-scheduling-core" aria-label="Permalink to &quot;`src/sched/` — Compile Scheduling Core&quot;">​</a></h3><p>The task-graph engine that decides what gets built, when, and shares the results.</p><ul><li><code>TaskGraph</code>: The shared build graph. Every expensive product is a node; concurrent requests for the same node join one build round instead of duplicating work, cancellation is cooperative, and edges drive dependency-aware invalidation</li><li><code>PCHFamily</code> / <code>PCMFamily</code> / <code>TURunFamily</code>: Node families for preamble PCHs (keyed by content), C++20 module PCMs (with import edges and provider tracking), and one-shot translation-unit runs shared by indexing and lint</li><li><code>Workspace</code>: The disk-truth aggregate — compilation database, dependency graph, artifact registries, project index. Core invariant: unsaved buffer contents of open files never modify the <code>Workspace</code>; it only reflects the state on disk</li><li><code>ContextResolver</code>: Resolves the compile command and includer context a file is compiled under, owning header-context verdicts, user context choices, and synthesized preambles</li><li><code>IndexStore</code> / <code>IndexPump</code>: Index persistence transactions (merge, save, reconcile with the CDB) and the background scheduler that feeds stale files through workers with foreground-aware budgeting</li><li>Bootstrap and batch drivers: cold-start orchestration for the server, and the headless execution mode behind <code>clice index</code> / <code>clice lint</code></li></ul><h3 id="src-worker-—-worker-processes" tabindex="-1"><code>src/worker/</code> — Worker Processes <a class="header-anchor" href="#src-worker-—-worker-processes" aria-label="Permalink to &quot;`src/worker/` — Worker Processes&quot;">​</a></h3><ul><li><code>WorkerPool</code>: Manages worker process lifecycles and scheduling — spawn/monitor/respawn with crash budgets and cooldown revival, stateful placement with document affinity, and stateless dispatch with priority queues and foreground-aware capacity</li><li><code>StatefulWorker</code>: Holds document ASTs and serves query requests</li><li>Stateless workers execute one-shot tasks (PCH/PCM builds, completion, formatting, indexing runs)</li></ul><h3 id="src-server-—-server-runtime" tabindex="-1"><code>src/server/</code> — Server Runtime <a class="header-anchor" href="#src-server-—-server-runtime" aria-label="Permalink to &quot;`src/server/` — Server Runtime&quot;">​</a></h3><p>The language server&#39;s core runtime, responsible for assembling all the layers above into a runnable service.</p><p><strong><code>protocol/</code></strong> — Protocol definitions. Describes the message formats for communication between the master process and worker processes, as well as between the server and clients. Includes Worker protocol (compilation/query/build requests), LSP extension protocol (compilation context switching, etc.), and the agentic protocol for AI agents.</p><p><strong><code>state/</code></strong> — Document state and the invalidation machinery.</p><ul><li><code>Session</code> / <code>SessionStore</code>: The open-buffer truth for each open file — content, document version, generation, and serving state — created on didOpen and destroyed on didClose. Compile products do not live here</li><li><code>ASTProjection</code> / <code>ASTProjectionTable</code>: The published products of each document&#39;s most recent compilation (feature results, PCH key, dependency snapshot) — an immutable read model replaced wholesale on each publication</li><li><code>Invalidator</code>: The invalidation engine — folds file events (buffer opens/saves, on-disk changes, compilation-database reloads, worker crashes) into a deduplicated set of invalidation effects</li><li><code>FileTracker</code>: Stat-polling discovery of changes that happen outside the editor (a regenerated <code>compile_commands.json</code>, <code>git checkout</code>), feeding events to the <code>Invalidator</code></li><li><code>Quarantine</code>: Per-document crash accounting — documents whose content keeps killing workers are isolated and recover through licensed probe attempts</li></ul><p><strong><code>service/</code></strong> — Read-side services consuming compilation and index results.</p><ul><li><code>FeatureRouter</code>: Routes each feature request by readiness — an up-to-date AST answers when available, otherwise index-backed projections answer immediately. Under <code>readonly = on/auto</code>, documents serve exclusively from the index until an edit escalates them to full AST service; compilation is pull-based throughout, triggered by requests rather than lifecycle events</li><li><code>ASTFamily</code>: The document-AST node family in the task graph — schedules compiles for open documents and publishes their results</li><li><code>WorkerForwarder</code>: Dispatches feature work to workers (stateful queries, interactive completion/signature builds, formatting) with quarantine admission and result landing</li><li><code>IndexQuery</code>: The query facade over the project index, per-file shards, and the live data of open files</li><li><code>ContextService</code>: The protocol adapter for compilation-context queries and switching</li></ul><p><strong><code>transport/</code></strong> — Protocol endpoints driving the server.</p><ul><li><code>MasterServer</code>: The composition root. Owns the workspace, sessions, worker pool, and all services above, and executes the <code>Invalidator</code>&#39;s effects through its single dispatch entry point</li><li><code>LSPClient</code> / <code>AgentClient</code>: Request handlers for the LSP protocol and agentic protocol</li></ul><p>See <a href="./multi-process">Multi-process Architecture</a>.</p><h3 id="src-driver-—-subcommands" tabindex="-1"><code>src/driver/</code> — Subcommands <a class="header-anchor" href="#src-driver-—-subcommands" aria-label="Permalink to &quot;`src/driver/` — Subcommands&quot;">​</a></h3><p>Entry points for the <code>clice</code> binary: <code>serve</code> (the LSP server), <code>worker</code>, <code>index</code> (batch indexing), <code>lint</code> (batch clang-tidy), <code>inspect</code>, <code>format</code>, <code>query</code>, and <code>doc</code>.</p><h2 id="inter-module-relationships" tabindex="-1">Inter-module Relationships <a class="header-anchor" href="#inter-module-relationships" aria-label="Permalink to &quot;Inter-module Relationships&quot;">​</a></h2><p>The data flow roughly follows this direction:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>command (structured compile commands)</span></span>\n<span class="line"><span>    ↓</span></span>\n<span class="line"><span>sched (task graph: PCH / PCM / AST / TU-run scheduling) ──→ worker (processes)</span></span>\n<span class="line"><span>    ↓ products</span></span>\n<span class="line"><span>compile (drives Clang compilation)</span></span>\n<span class="line"><span>    ↓</span></span>\n<span class="line"><span>semantic (semantic map) ──→ index (TUIndex / shards / ProjectIndex)</span></span>\n<span class="line"><span>    ↓</span></span>\n<span class="line"><span>feature (produces LSP responses)</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>The server layer assembles all of the above via sessions, routing,</span></span>\n<span class="line"><span>invalidation, and transports into a runnable service.</span></span></code></pre></div><p><code>support</code>, <code>vfs</code>, <code>config</code>, and <code>syntax</code> are cross-cutting layers shared by multiple modules.</p>',56)])])}const m=o(t,[["render",s]]);export{u as __pageData,m as default};
