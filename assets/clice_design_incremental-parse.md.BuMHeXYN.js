import{_ as a,o as i,c as n,a3 as t}from"./chunks/framework.CRbjHCyB.js";const u=JSON.parse('{"title":"Incremental Compilation","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/incremental-parse.md","filePath":"en/clice/design/incremental-parse.md"}'),s={name:"clice/design/incremental-parse.md"};function o(l,e,r,d,c,p){return i(),n("div",null,[...e[0]||(e[0]=[t(`<h1 id="incremental-compilation" tabindex="-1">Incremental Compilation <a class="header-anchor" href="#incremental-compilation" aria-label="Permalink to &quot;Incremental Compilation&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>C++ <code>#include</code> is textual substitution -- the preprocessor inserts the contents of included header files verbatim into the source file. A source file with only a few dozen lines of user code can expand to tens of thousands of lines or more after <code>#include</code> expansion. For example, a single <code>#include &lt;vector&gt;</code> directive pulls in thousands of lines of standard library code; add in project headers and the preprocessed output can easily exceed a hundred thousand lines.</p><p>A language server must recompile files after every user edit to provide up-to-date diagnostics, completions, and semantic information. Fully compiling those hundred thousand lines every time would take several seconds -- clearly unacceptable.</p><p>However, a key characteristic emerges when observing actual editing behavior: the <code>#include</code> region at the top of a file rarely changes, while the user code at the bottom changes constantly. In a typical editing session, the user might modify code hundreds of times without touching the <code>#include</code> region once.</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;vector&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;string&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;map&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;project/config.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;project/logging.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ── preamble ↑ changes slowly, compilation result can be cached ──</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ── user code ↓ changes rapidly, must be recompiled each time    ──</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> process</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Based on this observation, C++ language servers commonly adopt a <strong>preamble separation</strong> strategy: split the file into the preamble (the preprocessor directive region at the top) and the remaining user code. The preamble is compiled into a precompiled header (PCH) and cached; subsequent compilations load the PCH directly and only reprocess the user code. This reduces post-edit recompilation to just a few dozen to a few hundred lines, typically bringing latency under one second.</p><p>clangd also adopts this strategy, but has several design-level shortcomings in invalidation detection and lifecycle management:</p><ul><li><p><strong>High memory usage</strong>. clangd keeps preamble compilation artifacts in process memory. In large projects, preamble ASTs from multiple open files consume substantial memory, and usage grows over long-running sessions (clangd <a href="https://github.com/clangd/clangd/issues/251" target="_blank" rel="noreferrer">#251</a>, <a href="https://github.com/clangd/clangd/issues/115" target="_blank" rel="noreferrer">#115</a>).</p></li><li><p><strong>PCH file leaks on crash</strong>. clangd stores on-disk PCH in temporary files. On crash, RAII cleanup cannot execute and temp files persist in <code>/tmp</code>. On multi-user servers, accumulated leaked files can exhaust <code>/tmp</code> space (clangd <a href="https://github.com/clangd/clangd/issues/209" target="_blank" rel="noreferrer">#209</a>, <a href="https://github.com/clangd/clangd/issues/255" target="_blank" rel="noreferrer">#255</a>).</p></li><li><p><strong>Imprecise invalidation detection</strong>. When a header file is touched without changing its content (common during build tool dependency scanning, VCS branch switching), relying solely on modification time (mtime) triggers unnecessary PCH rebuilds. In large projects, these false positives noticeably degrade the editing experience.</p></li><li><p><strong>Cold start on restart</strong>. PCH is not persisted across sessions. After a server restart, PCH must be rebuilt for all open files.</p></li></ul><p>clice redesigns the incremental compilation mechanism to address these issues: disk-persisted content-addressed PCH storage, two-layer invalidation detection, a pull-based compilation model, and preamble completeness checking.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><p>Incremental compilation is organized around four core concepts: preamble separation defines &quot;what to cache,&quot; two-layer invalidation detection defines &quot;when to rebuild,&quot; pull-based compilation defines &quot;when to trigger,&quot; and content-addressed storage defines &quot;how to store.&quot;</p><h3 id="preamble-separation" tabindex="-1">Preamble Separation <a class="header-anchor" href="#preamble-separation" aria-label="Permalink to &quot;Preamble Separation&quot;">​</a></h3><p>The preamble is the region at the top of a source file consisting of preprocessor directives (<code>#include</code>, <code>#define</code>, <code>#pragma</code>, etc.) and module declarations (<code>module;</code>). Its end position is identified by a byte offset (bound) -- the byte position just before the first line of non-preprocessor content.</p><p>The preamble is compiled into a PCH file and cached on disk. Subsequent compilations load the PCH and only need to process user code after the bound. If the preamble is empty (bound is zero), no PCH is needed and the entire PCH flow is skipped.</p><p><code>PCHState</code> is a PCH cache entry, containing:</p><ul><li>The PCH file path on disk</li><li>A hash of the preamble content</li><li>The preamble byte boundary (bound)</li><li>A dependency snapshot (<code>DepsSnapshot</code>, see below)</li><li>DocumentLink information extracted from the PCH (<code>#include</code> directive positions and targets, for the editor to display clickable links)</li></ul><h3 id="two-layer-invalidation-detection" tabindex="-1">Two-Layer Invalidation Detection <a class="header-anchor" href="#two-layer-invalidation-detection" aria-label="Permalink to &quot;Two-Layer Invalidation Detection&quot;">​</a></h3><p>A PCH caches the preprocessing results of all headers included in the preamble. When the content of any dependency header changes, the PCH is stale and needs rebuilding. The challenge is precisely determining &quot;whether the content actually changed.&quot;</p><p>The most direct approach is checking file modification times (mtime): if all dependency files have mtimes no later than the PCH&#39;s build timestamp, no file has been modified. This check requires only <code>stat</code> system calls and is very fast. However, mtime checks produce false positives: build tool dependency scanning, VCS branch switching, editor auto-save, and similar operations update mtime without changing file content.</p><p>An alternative is directly comparing content hashes: recompute the hash of every dependency file on each check and compare against the hashes recorded at build time. This approach is perfectly precise but requires reading and hashing the contents of all dependency files. A typical C++ file may depend on hundreds of headers, making the I/O cost of full hashing on every check non-negligible.</p><p>clice combines both into a two-layer detection strategy:</p><ul><li><strong>Layer 1 (mtime fast screening)</strong>: Iterate over all dependency files and compare each file&#39;s mtime against the PCH&#39;s build timestamp. If all mtimes are no later than the build timestamp, the PCH is valid and can be reused directly.</li><li><strong>Layer 2 (content hash precise verification)</strong>: For files flagged as &quot;possibly modified&quot; by Layer 1 (mtime later than the build timestamp), recompute their xxh3 content hash and compare against the hash recorded at build time. Only trigger a rebuild when hashes differ.</li></ul><p>Layer 1 filters out the vast majority of unchanged files (the common-case path). Layer 2 eliminates mtime false positives (build tool touches, VCS checkouts, etc.). The combined effect: PCH is rebuilt only when the content of a dependency file has actually changed.</p><p><code>DepsSnapshot</code> is the underlying data structure for two-layer detection, captured when a PCH build completes. It records the path identifiers, content hashes, and build timestamp of all dependency files.</p><h3 id="pull-based-compilation" tabindex="-1">Pull-Based Compilation <a class="header-anchor" href="#pull-based-compilation" aria-label="Permalink to &quot;Pull-Based Compilation&quot;">​</a></h3><p>clice uses a pull-based compilation model: compilation is not triggered immediately on file change but on demand when a feature request (hover, completion, semantic highlighting, etc.) requires an up-to-date AST.</p><p>When the user edits a file (<code>didChange</code>), the master process only updates the in-memory file content and marks the AST as dirty (<code>ast_dirty</code>), without starting any compilation. When a feature request arrives, the compilation service checks whether the AST is dirty or has become stale due to external file changes. If recompilation is needed, it first ensures the PCH and module dependencies are ready, then dispatches the compilation task to a worker process.</p><p>The benefit of this model is avoiding wasteful compilations during rapid successive keystrokes. The user may trigger a dozen <code>didChange</code> events per second, but compilation only executes when an actual result is needed -- such as a hover or completion request.</p><blockquote><p>Note that &quot;external file changes&quot; and &quot;user edits&quot; are two independent dirty-marking paths. User edits mark <code>ast_dirty</code> via <code>didChange</code>; external file changes (e.g., a dependency header modified on disk) are discovered dynamically via two-layer invalidation detection before compilation.</p></blockquote><h3 id="content-addressed-pch-storage" tabindex="-1">Content-Addressed PCH Storage <a class="header-anchor" href="#content-addressed-pch-storage" aria-label="Permalink to &quot;Content-Addressed PCH Storage&quot;">​</a></h3><p>PCH files on disk are named by the hash of the preamble content (e.g., <code>a3f7e8c1d2b4f6e9.pch</code>), implementing content-addressed storage. This provides two benefits:</p><ul><li><strong>Disk sharing</strong>: Different files with identical preamble content naturally share the same PCH file on disk, with no additional deduplication logic needed.</li><li><strong>Cross-session persistence</strong>: PCH cache metadata (path, hash, boundary, dependency snapshot) is serialized to a <code>cache.json</code> file on disk. On server restart, this metadata is loaded and each PCH&#39;s validity is verified through two-layer invalidation detection, avoiding the need to rebuild all PCHs on a cold start.</li></ul><p>When preamble content changes, the new PCH uses a different hash for its filename and the old file becomes orphaned. A cleanup mechanism periodically reclaims orphaned PCH files that have not been used beyond a certain age.</p><h2 id="implementation" tabindex="-1">Implementation <a class="header-anchor" href="#implementation" aria-label="Permalink to &quot;Implementation&quot;">​</a></h2><h3 id="preamble-boundary-computation" tabindex="-1">Preamble Boundary Computation <a class="header-anchor" href="#preamble-boundary-computation" aria-label="Permalink to &quot;Preamble Boundary Computation&quot;">​</a></h3><p>The preamble boundary is determined through lexical scanning: the project&#39;s <code>Lexer</code> scans line by line from the start of the file, identifying preprocessor directives beginning with <code>#</code> and <code>module;</code> global module fragment declarations. Scanning stops at the first line of non-preprocessor content, and the byte offset at that point is returned as the boundary.</p><p>This lexer-based detection does not require starting a full preprocessor and is very fast.</p><h3 id="preamble-completeness-check" tabindex="-1">Preamble Completeness Check <a class="header-anchor" href="#preamble-completeness-check" aria-label="Permalink to &quot;Preamble Completeness Check&quot;">​</a></h3><p>Before triggering a PCH rebuild, the preamble must be checked for syntactic completeness. Two typical incomplete states:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;lib</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          // unclosed quote, user is typing a filename</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">import std.core</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // missing semicolon, user is typing a module declaration</span></span></code></pre></div><p>Building a PCH from an incomplete preamble produces a PCH containing erroneous preprocessor state. Subsequent compilations loading this erroneous PCH would see a flood of spurious errors. Therefore, when the preamble is detected as incomplete, the rebuild is deferred and the old PCH (if one exists) continues to be used.</p><h3 id="pch-build-pipeline" tabindex="-1">PCH Build Pipeline <a class="header-anchor" href="#pch-build-pipeline" aria-label="Permalink to &quot;PCH Build Pipeline&quot;">​</a></h3><p>When a compilation request requires a PCH, the following pipeline executes:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Compute preamble boundary and hash</span></span>
<span class="line"><span>         │</span></span>
<span class="line"><span>         ▼</span></span>
<span class="line"><span>   ┌─ Cache hit? ──── yes → Reuse cached PCH</span></span>
<span class="line"><span>   │     │</span></span>
<span class="line"><span>   │    no</span></span>
<span class="line"><span>   │     │</span></span>
<span class="line"><span>   │     ▼</span></span>
<span class="line"><span>   │  Preamble complete? ── no → Defer rebuild, keep old PCH</span></span>
<span class="line"><span>   │     │</span></span>
<span class="line"><span>   │   yes</span></span>
<span class="line"><span>   │     │</span></span>
<span class="line"><span>   │     ▼</span></span>
<span class="line"><span>   │  Another coroutine building? ── yes → Wait for completion, use result</span></span>
<span class="line"><span>   │     │</span></span>
<span class="line"><span>   │    no</span></span>
<span class="line"><span>   │     │</span></span>
<span class="line"><span>   │     ▼</span></span>
<span class="line"><span>   │  Dispatch to stateless worker to build PCH</span></span>
<span class="line"><span>   │     │</span></span>
<span class="line"><span>   │     ▼</span></span>
<span class="line"><span>   └─ Update cache, capture dependency snapshot</span></span></code></pre></div><p>Cache hit requires two conditions: the preamble hash matches the cached value (preamble content unchanged), and two-layer invalidation detection passes (dependency file contents unchanged). Both conditions must be satisfied simultaneously.</p><p>PCH builds are executed by stateless worker processes (see <a href="./multi-process">multi-process architecture</a>). The worker uses Clang&#39;s Preamble compilation mode, processing only the preamble portion before the bound. Upon completion, it returns the PCH file path and list of dependency files.</p><h3 id="concurrent-build-serialization" tabindex="-1">Concurrent Build Serialization <a class="header-anchor" href="#concurrent-build-serialization" aria-label="Permalink to &quot;Concurrent Build Serialization&quot;">​</a></h3><p>Multiple feature requests may simultaneously trigger a PCH build for the same file. <code>PCHState</code> contains a shared event (<code>building</code>): the first coroutine to initiate a build sets this event, and subsequent coroutines that find the event present wait for its completion and then use the build result. This ensures the PCH for a given file is built only once.</p><h3 id="dependency-snapshot-timing-guarantee" tabindex="-1">Dependency Snapshot Timing Guarantee <a class="header-anchor" href="#dependency-snapshot-timing-guarantee" aria-label="Permalink to &quot;Dependency Snapshot Timing Guarantee&quot;">​</a></h3><p>The <code>DepsSnapshot</code> build timestamp (<code>build_at</code>) is obtained <strong>before</strong> computing file hashes. This ordering ensures there is no time window in which a modification could be missed:</p><p>If a file is modified during hash computation, its mtime will be later than <code>build_at</code>. On the next two-layer detection, Layer 1 will flag this file as &quot;possibly modified,&quot; and Layer 2 will recompute its hash and discover the change.</p><p>If the order were reversed -- hashing first, then obtaining the timestamp -- a window could arise: a file modified between hash computation and timestamp acquisition would have an mtime no later than <code>build_at</code>, causing the modification to be missed.</p><h3 id="overall-compilation-flow" tabindex="-1">Overall Compilation Flow <a class="header-anchor" href="#overall-compilation-flow" aria-label="Permalink to &quot;Overall Compilation Flow&quot;">​</a></h3><p>When a feature request arrives, the compilation pipeline executes in this order:</p><ol><li>Check whether the AST is cached and not stale -- if so, reuse it directly</li><li>If C++20 modules are used, ensure module dependencies are ready (see <a href="./module-graph">module compilation</a>)</li><li>Ensure the PCH is ready</li><li>Dispatch the compilation task to a stateful worker process with the PCH path and module file paths</li><li>The worker loads the PCH and compiles only the user code after the preamble</li></ol><p>AST dependency files (headers <code>#include</code>d after the preamble) are also tracked via <code>DepsSnapshot</code>, using the same two-layer invalidation detection. Even if the user has not edited the current file, if a dependency header is modified on disk, the next feature request will trigger recompilation.</p><h3 id="interaction-with-compilation-context" tabindex="-1">Interaction with Compilation Context <a class="header-anchor" href="#interaction-with-compilation-context" aria-label="Permalink to &quot;Interaction with Compilation Context&quot;">​</a></h3><p>For non-self-contained headers, the compilation context system synthesizes a prefix file and injects it via <code>-include</code> into the compilation arguments (see <a href="./compilation-context">compilation context</a>). This injected prefix file is processed by Clang during the preamble compilation phase and is therefore naturally covered by PCH caching. The PCH build pipeline handles source files and headers uniformly.</p><h3 id="cache-persistence" tabindex="-1">Cache Persistence <a class="header-anchor" href="#cache-persistence" aria-label="Permalink to &quot;Cache Persistence&quot;">​</a></h3><p>PCH and PCM cache metadata are persisted to disk via a <code>cache.json</code> file. This file is updated after each successful build and loaded on server startup. Writes use a write-to-temp-then-atomic-rename pattern to prevent file corruption from mid-write crashes.</p><p>After loading the cache on startup, all PCH entries are validated through two-layer invalidation detection. Stale entries are automatically rebuilt on the next compilation, requiring no special cache consistency recovery logic.</p><h2 id="faq" tabindex="-1">FAQ <a class="header-anchor" href="#faq" aria-label="Permalink to &quot;FAQ&quot;">​</a></h2><ul><li><p><strong>Why two-layer detection instead of content hashing alone?</strong> Content hashing is precise but requires reading the contents of all dependency files. A typical C++ file may depend on hundreds of headers, making the I/O cost of full hashing on every check non-negligible. The mtime fast-screening layer reduces the number of files that need hashing to those &quot;touched since the last build,&quot; which in the common case is typically zero.</p></li><li><p><strong>Why full PCH rebuild each time? Can it be updated incrementally?</strong> Clang supports chained PCH: each <code>#include</code> in the preamble is built as an independent PCH link, with each link depending on the previous link&#39;s compilation artifact. When the user adds a new <code>#include</code> at the end of the preamble, only one new link needs to be appended to the existing chain rather than rebuilding the entire preamble. Benchmarks show (PR <a href="https://github.com/ykiko/clice/pull/405" target="_blank" rel="noreferrer">#405</a>) that for a preamble of 70 C++ standard library headers, incrementally appending one <code>#include</code> takes about 36ms compared to about 1230ms for a full rebuild (35x speedup). Chained PCH AST load latency is virtually unaffected (+2% to +6%). clice plans to adopt chained PCH to optimize incremental rebuild performance; this is currently in the experimental stage.</p></li><li><p><strong>Why pull-based compilation instead of push-based?</strong> The key reason is that clice persists all files&#39; PCH to disk, resulting in far more cached files than clangd&#39;s in-memory model (clangd keeps only a few active files&#39; preambles via LRU). When a header file is modified, a large number of files&#39; PCHs may be affected. With push-based compilation, all affected PCHs would need to be rebuilt immediately on header modification -- an unacceptable volume. Pull-based compilation defers rebuilding until a feature request arrives, rebuilding only the PCH for the file the user currently needs. Since PCH loading itself is fast (see next item), the latency introduced by on-demand rebuilding is small.</p></li><li><p><strong>Is disk PCH slower than in-memory PCH?</strong> The practical impact is minimal. Clang loads PCH using mmap to map the file into memory, avoiding a full read-copy. More importantly, Clang deserializes AST nodes from the PCH lazily -- only nodes actually referenced during compilation are deserialized, and most PCH content is never accessed. Thus PCH loading performance depends primarily on how the binary file is mapped, with no fundamental difference between a disk file and an in-memory buffer. The benefits of disk PCH -- cross-restart persistence, no resident process memory usage, content-addressed sharing -- make this trade-off worthwhile.</p></li></ul><h2 id="known-limitations" tabindex="-1">Known Limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known Limitations&quot;">​</a></h2><ul><li><p><strong>Per-file independent caching</strong>. The PCH cache is keyed by file path identifier. Even if two files have identical preamble content, they have independent cache entries -- each performing its own invalidation detection and build. While the PCH files on disk are shared through content-addressed naming, cache metadata (dependency snapshots, build state, etc.) is not shared. The improvement direction is to key the cache by preamble content hash plus compilation flags, enabling cross-file cache metadata sharing.</p></li><li><p><strong>Full rebuild</strong>. Any content change in a dependency file triggers a full PCH rebuild, with no way to rebuild only the affected portion. The improvement direction is to adopt chained PCH (see FAQ), limiting the rebuild scope to the chain links after the point of change.</p></li><li><p><strong>Compilation flags not part of cache key</strong>. PCH disk filenames and cache lookups do not consider compilation flags. When two files have the same preamble text but different compilation flags (e.g., <code>-D</code>), they may incorrectly share a PCH. The improvement direction is to include preprocessing-relevant compilation flags in the cache key.</p></li><li><p><strong>Incomplete preamble completeness check</strong>. The current completeness check only covers unclosed quotes and missing semicolons in <code>#include</code>/<code>import</code> directives. Other types of incomplete edits (e.g., typing a <code>#define</code> value) are not detected. The impact of building a PCH from such an incomplete preamble on subsequent compilation has not been thoroughly tested. Further investigation is needed into Clang&#39;s behavior when processing incomplete preprocessor directives, to determine whether the completeness check scope should be extended.</p></li><li><p><strong>No proactive diagnostics push after header save</strong>. Under the current pure pull-based model, after a user saves a header file, open source files that depend on it do not immediately update diagnostics -- the user must trigger an action on the source file (e.g., hover, edit) for the two-layer invalidation detection to discover the change and trigger recompilation. The improvement direction is to check affected open sessions on <code>didSave</code> and proactively trigger compilation for them (a hybrid push/pull model).</p></li></ul>`,66)])])}const m=a(s,[["render",o]]);export{u as __pageData,m as default};
