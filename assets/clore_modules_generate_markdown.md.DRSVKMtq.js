import{_ as a,o as t,c as o,a3 as n}from"./chunks/framework.aYYe0dfI.js";const p=JSON.parse('{"title":"Module generate:markdown","description":"The generate:markdown module is responsible for constructing and rendering structured Markdown documents. It provides a set of public data types—such as MarkdownDocument, MarkdownNode, Paragraph, CodeFence, BlockQuote, BulletList, MermaidDiagram, SemanticSection, and various fragment types (e.g., TextFragment, CodeFragment, LinkFragment)—that form an abstract syntax tree (AST) for generated documentation. The module owns the implementation of these node types and the functions that build them, including make_text, make_code, make_blockquote, make_paragraph, make_section, make_code_fence, make_link, make_mermaid, and make_raw_markdown.","frontmatter":{"title":"Module generate:markdown","description":"The generate:markdown module is responsible for constructing and rendering structured Markdown documents. It provides a set of public data types—such as MarkdownDocument, MarkdownNode, Paragraph, CodeFence, BlockQuote, BulletList, MermaidDiagram, SemanticSection, and various fragment types (e.g., TextFragment, CodeFragment, LinkFragment)—that form an abstract syntax tree (AST) for generated documentation. The module owns the implementation of these node types and the functions that build them, including make_text, make_code, make_blockquote, make_paragraph, make_section, make_code_fence, make_link, make_mermaid, and make_raw_markdown.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/generate/markdown.md","filePath":"en/clore/modules/generate/markdown.md"}'),r={name:"clore/modules/generate/markdown.md"};function i(c,e,d,s,l,m){return t(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="module-generate-markdown" tabindex="-1">Module <code>generate:markdown</code> <a class="header-anchor" href="#module-generate-markdown" aria-label="Permalink to &quot;Module `generate:markdown`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>generate:markdown</code> module is responsible for constructing and rendering structured Markdown documents. It provides a set of public data types—such as <code>MarkdownDocument</code>, <code>MarkdownNode</code>, <code>Paragraph</code>, <code>CodeFence</code>, <code>BlockQuote</code>, <code>BulletList</code>, <code>MermaidDiagram</code>, <code>SemanticSection</code>, and various fragment types (e.g., <code>TextFragment</code>, <code>CodeFragment</code>, <code>LinkFragment</code>)—that form an abstract syntax tree (AST) for generated documentation. The module owns the implementation of these node types and the functions that build them, including <code>make_text</code>, <code>make_code</code>, <code>make_blockquote</code>, <code>make_paragraph</code>, <code>make_section</code>, <code>make_code_fence</code>, <code>make_link</code>, <code>make_mermaid</code>, and <code>make_raw_markdown</code>.</p><p>The module also owns the rendering pipeline through functions like <code>render_markdown</code> and internal helpers that traverse the AST and produce final Markdown output, handling inline styling, code spans, fenced blocks, headings, blockquotes, lists, and YAML frontmatter. Its public API enables callers to programmatically assemble documentation content from source analysis data, with the module managing the conversion to Markdown syntax and the composition of document sections.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><code>std</code></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./analysis"><code>generate:analysis</code></a></li><li><a href="./common"><code>generate:common</code></a></li><li><a href="./page"><code>generate:page</code></a></li><li><a href="./scheduler"><code>generate:scheduler</code></a></li><li><a href="./symbol"><code>generate:symbol</code></a></li></ul><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-generate-blockquote" tabindex="-1"><code>clore::generate::BlockQuote</code> <a class="header-anchor" href="#clore-generate-blockquote" aria-label="Permalink to &quot;`clore::generate::BlockQuote`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:62</code></p><p>Definition: <code>generate/markdown.cppm:62</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::BlockQuote</code> is implemented as a plain aggregate with a single member, <code>fragments</code>, of type <code>std::vector&lt;InlineFragment&gt;</code>. This vector stores the ordered sequence of inline fragments that constitute the content of a block quote during the generation phase. There are no custom constructors, assignment <code>operator</code>s, or additional member functions; the struct relies entirely on default compiler‑generated special member functions for initialization, copying, and moving. The only internal invariant is that <code>fragments</code> holds the fragments in the exact order they are intended to appear in the rendered output, and the vector may be empty if the block quote has no content. This straightforward structure makes <code>clore::generate::BlockQuote</code> a lightweight data holder with no hidden state or complex lifecycle.</p><h3 id="clore-generate-bulletlist" tabindex="-1"><code>clore::generate::BulletList</code> <a class="header-anchor" href="#clore-generate-bulletlist" aria-label="Permalink to &quot;`clore::generate::BulletList`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:49</code></p><p>Definition: <code>generate/markdown.cppm:49</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::BulletList</code> is a simple aggregate that stores the content of a bullet list as a single <code>std::vector&lt;ListItem&gt;</code> member named <code>items</code>. This vector is the sole internal state and holds the individual list entries in order. As an aggregate, it has no user‑defined constructors, destructors, or member functions; initialization and modification rely on direct member access and standard vector operations. The only invariant is that <code>items</code> contains the sequence of <code>ListItem</code> objects that represent the list’s body, with no implicit constraints on its length or the validity of its elements beyond those enforced by <code>ListItem</code> itself.</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>items</code> contains all list items in order.</li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>items</code> : <code>std::vector&lt;ListItem&gt;</code></li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as a data container for generating markdown bullet lists.</li></ul><h3 id="clore-generate-codefence" tabindex="-1"><code>clore::generate::CodeFence</code> <a class="header-anchor" href="#clore-generate-codefence" aria-label="Permalink to &quot;`clore::generate::CodeFence`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:53</code></p><p>Definition: <code>generate/markdown.cppm:53</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p><code>clore::generate::CodeFence</code> is a plain data aggregate that holds the two string members <code>language</code> and <code>code</code>. Its sole design purpose is to represent the content and metadata of a Markdown fenced code block. As a struct with no custom constructors, special member functions, or invariants, the object is a transparent container whose lifetime and ownership of its <code>std::string</code> members follow standard value semantics. No member function implementations exist beyond the implicitly generated ones.</p><h4 id="invariants-1" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-1" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>language</code> and <code>code</code> are independent strings with no inherent constraints.</li></ul><h4 id="key-members-1" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-1" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>language</code></li><li><code>code</code></li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Created and populated with language and code content, then passed to other generation functions to produce Markdown fences.</li></ul><h3 id="clore-generate-codefragment" tabindex="-1"><code>clore::generate::CodeFragment</code> <a class="header-anchor" href="#clore-generate-codefragment" aria-label="Permalink to &quot;`clore::generate::CodeFragment`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:29</code></p><p>Definition: <code>generate/markdown.cppm:29</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::CodeFragment</code> is a lightweight value type that holds a single <code>std::string</code> member <code>code</code>. It serves as an opaque container for a snippet of generated code, intended to be passed or combined within the generation pipeline. No special invariants are imposed on the stored string—it may be empty or contain arbitrary text—and the struct relies entirely on default compiler-generated constructors, assignment <code>operator</code>s, and destructor, making it trivially copyable and movable. This simplicity allows <code>CodeFragment</code> to be used as a fundamental building block in code generation algorithms where fragments are accumulated, transformed, and concatenated.</p><h4 id="invariants-2" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-2" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>The struct is a simple container with no explicit invariants.</li></ul><h4 id="key-members-2" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-2" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>The member <code>code</code> is the sole data member.</li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to store and pass code fragments within the code generation process.</li></ul><h3 id="clore-generate-frontmatter" tabindex="-1"><code>clore::generate::Frontmatter</code> <a class="header-anchor" href="#clore-generate-frontmatter" aria-label="Permalink to &quot;`clore::generate::Frontmatter`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:18</code></p><p>Definition: <code>generate/markdown.cppm:18</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::Frontmatter</code> is a plain aggregate with four <code>std::string</code> members: <code>title</code>, <code>description</code>, <code>layout</code>, and <code>page_template</code>. The latter two are given default values of <code>&quot;doc&quot;</code>, establishing a common expectation that, unless overridden, the generated frontmatter will reference the <code>doc</code> layout and template. No custom constructors, assignment <code>operator</code>s, or invariants are enforced beyond those inherent to <code>std::string</code>; the struct simply bundles the data fields that are serialized into YAML frontmatter during Markdown generation.</p><h4 id="invariants-3" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-3" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>Fields <code>layout</code> and <code>page_template</code> default to <code>&quot;doc&quot;</code> if not explicitly set.</li></ul><h4 id="key-members-3" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-3" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>title</code></li><li><code>description</code></li><li><code>layout</code></li><li><code>page_template</code></li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Populated with page metadata before generating frontmatter in markdown output.</li><li>Defaults for <code>layout</code> and <code>page_template</code> are commonly used for standard documentation pages.</li></ul><h3 id="clore-generate-linkfragment" tabindex="-1"><code>clore::generate::LinkFragment</code> <a class="header-anchor" href="#clore-generate-linkfragment" aria-label="Permalink to &quot;`clore::generate::LinkFragment`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:33</code></p><p>Definition: <code>generate/markdown.cppm:33</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::LinkFragment</code> is an aggregate with three public data members: <code>label</code>, <code>target</code>, and <code>code_style</code>. It serves as a lightweight container for the components of a generated Markdown link fragment. A default member initializer sets <code>code_style</code> to <code>false</code>. No invariants are imposed beyond the plain text requirements of its <code>std::string</code> fields; the struct is intended for direct field access during link construction and requires no special member functions.</p><h4 id="invariants-4" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-4" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>label</code> and <code>target</code> are arbitrary strings with no specified format constraints.</li><li><code>code_style</code> defaults to <code>false</code> and is used as a boolean flag.</li></ul><h4 id="key-members-4" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-4" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>label</code>: the display text for the link.</li><li><code>target</code>: the URL or reference target.</li><li><code>code_style</code>: indicates whether the label should be rendered in code style.</li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Constructed with designated initializers or aggregate initialization.</li><li>Passed to markdown generation functions to produce link syntax.</li></ul><h3 id="clore-generate-listitem" tabindex="-1"><code>clore::generate::ListItem</code> <a class="header-anchor" href="#clore-generate-listitem" aria-label="Permalink to &quot;`clore::generate::ListItem`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:45</code></p><p>Definition: <code>generate/markdown.cppm:45</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::ListItem</code> is implemented as a plain aggregate with a single <code>std::vector&lt;InlineFragment&gt;</code> member named <code>fragments</code>. This design directly reflects its role as a holder of inline content for a list item within the generation process. The implementation imposes no additional invariants; the vector is expected to contain the ordered sequence of <code>InlineFragment</code> objects that constitute the item&#39;s text. Because <code>ListItem</code> contains no custom constructors, assignment <code>operator</code>s, or destructors, it relies entirely on the compiler-generated defaults, making it a straightforward value type that can be efficiently moved and copied. No member functions are provided beyond those implicitly defined, so all interaction with the list item&#39;s content occurs through direct access to <code>fragments</code>.</p><h4 id="key-members-5" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-5" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>fragments</li></ul><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to model individual items in a markdown list, where each item is composed of inline fragments.</li></ul><h3 id="clore-generate-markdowndocument" tabindex="-1"><code>clore::generate::MarkdownDocument</code> <a class="header-anchor" href="#clore-generate-markdowndocument" aria-label="Permalink to &quot;`clore::generate::MarkdownDocument`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:94</code></p><p>Definition: <code>generate/markdown.cppm:94</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::MarkdownDocument</code> aggregates the top-level parsed representation of a Markdown document. Its internal structure consists of two data members: an <code>std::optional&lt;Frontmatter&gt; frontmatter</code> that may hold document metadata, and an <code>std::vector&lt;MarkdownNode&gt; children</code> that stores the sequence of top-level nodes. No custom constructors, assignment <code>operator</code>s, or other member functions are defined, so initialization, copying, and moving rely on compiler-generated defaults. The only implicit invariant is that when <code>frontmatter</code> has a value, it logically precedes the <code>children</code> sequence; no enforcement of ordering or consistency is provided by the struct itself.</p><h4 id="invariants-5" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-5" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>frontmatter may be absent</li><li>children elements retain document order</li></ul><h4 id="key-members-6" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-6" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>frontmatter</li><li>children</li></ul><h4 id="usage-patterns-6" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-6" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Acts as the output data structure for document parsing</li><li>Consumed by rendering or further processing</li></ul><h3 id="clore-generate-markdownnode" tabindex="-1"><code>clore::generate::MarkdownNode</code> <a class="header-anchor" href="#clore-generate-markdownnode" aria-label="Permalink to &quot;`clore::generate::MarkdownNode`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:73</code></p><p>Definition: <code>generate/markdown.cppm:73</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::MarkdownNode</code> is a thin wrapper around a single <code>std::variant</code> member <code>value</code>, which serves as a discriminated union of all concrete block-level Markdown element types used in the generation process. Each instance holds exactly one of the seven alternatives: <code>Paragraph</code>, <code>BulletList</code>, <code>CodeFence</code>, <code>MermaidDiagram</code>, <code>BlockQuote</code>, <code>RawMarkdown</code>, or <code>SemanticSectionPtr</code>. The <code>SemanticSectionPtr</code> type is a pointer to another semantic section, enabling recursive or hierarchical node composition. Because the struct has no other data members and no user‑defined special member functions (likely relying on defaults), the variant’s type‑safe visitation pattern is the primary implementation concern: consumers must use <code>std::visit</code> or similar to operate on the node’s content. No invariants beyond those enforced by the variant itself are present, making the struct a simple, efficient building block for the Markdown generation graph.</p><h4 id="invariants-6" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-6" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>The variant <code>value</code> always holds exactly one of the listed alternatives.</li><li>The type of the contained element is known at compile time via the variant index.</li></ul><h4 id="key-members-7" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-7" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>value</code></li></ul><h4 id="usage-patterns-7" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-7" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Constructed with a specific markdown element type to initialize the variant.</li><li>Visited using <code>std::visit</code> to process different markdown constructs uniformly.</li><li>Stored in containers to build a markdown document tree.</li></ul><h3 id="clore-generate-mermaiddiagram" tabindex="-1"><code>clore::generate::MermaidDiagram</code> <a class="header-anchor" href="#clore-generate-mermaiddiagram" aria-label="Permalink to &quot;`clore::generate::MermaidDiagram`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:58</code></p><p>Definition: <code>generate/markdown.cppm:58</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::MermaidDiagram</code> is a trivial aggregate type that serves as a simple container for the textual representation of a generated Mermaid diagram. Its only member, <code>code</code> of type <code>std::string</code>, holds the complete Mermaid source string. The struct imposes no invariants on the content of this string—syntactic or semantic validity of the diagram is not enforced at this level and is instead delegated to the generating logic and the downstream rendering toolchain. As a plain aggregate, <code>clore::generate::MermaidDiagram</code> is default-constructible, copyable, and movable, and it functions purely as a value carrier within the code generation flow.</p><h4 id="invariants-7" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-7" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>The <code>code</code> member stores the diagram source text.</li></ul><h4 id="key-members-8" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-8" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>code</code> member of type <code>std::string</code></li></ul><h4 id="usage-patterns-8" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-8" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Constructed with or assigned a Mermaid diagram string.</li><li>Accessed to retrieve the diagram code for rendering or output.</li></ul><h3 id="clore-generate-paragraph" tabindex="-1"><code>clore::generate::Paragraph</code> <a class="header-anchor" href="#clore-generate-paragraph" aria-label="Permalink to &quot;`clore::generate::Paragraph`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:41</code></p><p>Definition: <code>generate/markdown.cppm:41</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The struct <code>clore::generate::Paragraph</code> is a simple aggregate whose sole data member is <code>fragments</code>, a <code>std::vector&lt;InlineFragment&gt;</code>. There are no user‑defined constructors, assignment <code>operator</code>s, or destructors; all special member functions are implicitly generated. The invariant of the struct is that the sequence of <code>InlineFragment</code> objects stored in <code>fragments</code> represents the logical content of a paragraph in the order they appear. Because the struct is an aggregate, it can be initialized using brace‑initialization. No additional invariants are enforced by the struct itself beyond those already guaranteed by the vector (e.g., valid iterators, contiguous storage). The absence of custom member implementations means the struct relies entirely on the default copy, move, and destruction semantics of its vector member.</p><h4 id="invariants-8" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-8" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>fragments are stored in order</li></ul><h4 id="key-members-9" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-9" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>fragments</li></ul><h4 id="usage-patterns-9" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-9" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>aggregated in lists or documents</li><li>populated with inline content during generation</li></ul><h3 id="clore-generate-rawmarkdown" tabindex="-1"><code>clore::generate::RawMarkdown</code> <a class="header-anchor" href="#clore-generate-rawmarkdown" aria-label="Permalink to &quot;`clore::generate::RawMarkdown`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:66</code></p><p>Definition: <code>generate/markdown.cppm:66</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The implementation of <code>clore::generate::RawMarkdown</code> is a trivial wrapper around a single public member <code>std::string markdown</code>. Its sole purpose is to type‑tag a plain string as raw Markdown content, distinguishing it from other string types within the generation pipeline. No invariants are enforced beyond those inherent to <code>std::string</code>; the struct contains no constructors, assignment <code>operator</code>s, or member functions. All operations on the stored text are performed externally by functions that accept or return a <code>RawMarkdown</code> value.</p><h4 id="invariants-9" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-9" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>No invariants are enforced; the struct is a plain aggregate.</li></ul><h4 id="key-members-10" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-10" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>markdown</code> of type <code>std::string</code></li></ul><h4 id="usage-patterns-10" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-10" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to represent and pass raw markdown content.</li></ul><h3 id="clore-generate-semantickind" tabindex="-1"><code>clore::generate::SemanticKind</code> <a class="header-anchor" href="#clore-generate-semantickind" aria-label="Permalink to &quot;`clore::generate::SemanticKind`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:7</code></p><p>Definition: <code>generate/markdown.cppm:7</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The <code>clore::generate::SemanticKind</code> enumeration is an internal discriminator used to identify the kind of semantic entity being processed during documentation generation. It is implemented as an <code>enum class</code> with an underlying <code>std::uint8_t</code> storage, ensuring a compact and type‑safe representation. Each enumerator — <code>Index</code>, <code>Namespace</code>, <code>Module</code>, <code>Type</code>, <code>Function</code>, <code>Variable</code>, <code>File</code>, and <code>Section</code> — corresponds to a distinct category of documented elements, such as types, functions, or file‑level groupings. The ordering of enumerators follows the declaration order and does not imply any inherent hierarchy or priority; the values are assigned automatically by the compiler starting from zero. The enum’s integral representation allows efficient mapping to other internal data structures (e.g., lookup tables or switch‑based dispatch) while preventing accidental mixing with plain integer or unrelated enumeration types.</p><h4 id="invariants-10" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-10" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>Each enumerator has a distinct integral value.</li><li>Underlying type is <code>std::uint8_t</code>.</li></ul><h4 id="key-members-11" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-11" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>Index</code></li><li><code>Namespace</code></li><li><code>Module</code></li><li><code>Type</code></li><li><code>Function</code></li><li><code>Variable</code></li><li><code>File</code></li><li><code>Section</code></li></ul><h4 id="usage-patterns-11" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-11" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>No direct usage patterns observed in the provided evidence.</li></ul><h4 id="member-variables" tabindex="-1">Member Variables <a class="header-anchor" href="#member-variables" aria-label="Permalink to &quot;Member Variables&quot;">​</a></h4><h5 id="clore-generate-semantickind-file" tabindex="-1"><code>clore::generate::SemanticKind::File</code> <a class="header-anchor" href="#clore-generate-semantickind-file" aria-label="Permalink to &quot;`clore::generate::SemanticKind::File`&quot;">​</a></h5><p>Declaration: <code>generate/markdown.cppm:14</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><h6 id="implementation" tabindex="-1">Implementation <a class="header-anchor" href="#implementation" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">File</span></span></code></pre></div><h5 id="clore-generate-semantickind-function" tabindex="-1"><code>clore::generate::SemanticKind::Function</code> <a class="header-anchor" href="#clore-generate-semantickind-function" aria-label="Permalink to &quot;`clore::generate::SemanticKind::Function`&quot;">​</a></h5><p>Declaration: <code>generate/markdown.cppm:12</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><h6 id="implementation-1" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-1" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Function</span></span></code></pre></div><h5 id="clore-generate-semantickind-index" tabindex="-1"><code>clore::generate::SemanticKind::Index</code> <a class="header-anchor" href="#clore-generate-semantickind-index" aria-label="Permalink to &quot;`clore::generate::SemanticKind::Index`&quot;">​</a></h5><p>Declaration: <code>generate/markdown.cppm:8</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><h6 id="implementation-2" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-2" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Index</span></span></code></pre></div><h5 id="clore-generate-semantickind-module" tabindex="-1"><code>clore::generate::SemanticKind::Module</code> <a class="header-anchor" href="#clore-generate-semantickind-module" aria-label="Permalink to &quot;`clore::generate::SemanticKind::Module`&quot;">​</a></h5><p>Declaration: <code>generate/markdown.cppm:10</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><h6 id="implementation-3" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-3" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Module</span></span></code></pre></div><h5 id="clore-generate-semantickind-namespace" tabindex="-1"><code>clore::generate::SemanticKind::Namespace</code> <a class="header-anchor" href="#clore-generate-semantickind-namespace" aria-label="Permalink to &quot;`clore::generate::SemanticKind::Namespace`&quot;">​</a></h5><p>Declaration: <code>generate/markdown.cppm:9</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><h6 id="implementation-4" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-4" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Namespace</span></span></code></pre></div><h5 id="clore-generate-semantickind-section" tabindex="-1"><code>clore::generate::SemanticKind::Section</code> <a class="header-anchor" href="#clore-generate-semantickind-section" aria-label="Permalink to &quot;`clore::generate::SemanticKind::Section`&quot;">​</a></h5><p>Declaration: <code>generate/markdown.cppm:15</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><h6 id="implementation-5" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-5" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Section</span></span></code></pre></div><h5 id="clore-generate-semantickind-type" tabindex="-1"><code>clore::generate::SemanticKind::Type</code> <a class="header-anchor" href="#clore-generate-semantickind-type" aria-label="Permalink to &quot;`clore::generate::SemanticKind::Type`&quot;">​</a></h5><p>Declaration: <code>generate/markdown.cppm:11</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><h6 id="implementation-6" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-6" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Type</span></span></code></pre></div><h5 id="clore-generate-semantickind-variable" tabindex="-1"><code>clore::generate::SemanticKind::Variable</code> <a class="header-anchor" href="#clore-generate-semantickind-variable" aria-label="Permalink to &quot;`clore::generate::SemanticKind::Variable`&quot;">​</a></h5><p>Declaration: <code>generate/markdown.cppm:13</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><h6 id="implementation-7" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-7" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Variable</span></span></code></pre></div><h3 id="clore-generate-semanticsection" tabindex="-1"><code>clore::generate::SemanticSection</code> <a class="header-anchor" href="#clore-generate-semanticsection" aria-label="Permalink to &quot;`clore::generate::SemanticSection`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:70</code></p><p>Definition: <code>generate/markdown.cppm:84</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p><code>clore::generate::SemanticSection</code> is a plain aggregate that holds the internal state for a semantically-typed documentation section. Its data members include an enumeration <code>kind</code> (defaulting to <code>SemanticKind::Section</code>), two string fields <code>subject_key</code> and <code>heading</code>, a <code>level</code> of type <code>std::uint8_t</code> (defaulting to 2), two boolean flags <code>omit_if_empty</code> and <code>code_style_heading</code>, and a <code>std::vector&lt;MarkdownNode&gt;</code> named <code>children</code>. The invariants are established entirely by these defaults: a new instance represents a second-level section that will be omitted from output when its content is empty unless overridden. The <code>children</code> vector stores the concrete Markdown content; the <code>kind</code> discriminator and <code>subject_key</code> identify which semantic category or generated entity this section corresponds to, while <code>heading</code> and <code>code_style_heading</code> control the textual label used. No custom constructors or member functions exist, so all initialization relies on default member initializers and aggregate initialization.</p><h4 id="invariants-11" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-11" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>level</code> is a non-zero unsigned integer suggesting heading depth</li><li><code>omit_if_empty</code> determines if a section with no children is skipped</li><li><code>kind</code> identifies the semantic type of the section</li><li><code>subject_key</code> is a key to associate with an entity</li><li><code>code_style_heading</code> toggles monospaced heading formatting</li></ul><h4 id="key-members-12" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-12" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>kind</code></li><li><code>subject_key</code></li><li><code>heading</code></li><li><code>level</code></li><li><code>omit_if_empty</code></li><li><code>code_style_heading</code></li><li><code>children</code></li></ul><h4 id="usage-patterns-12" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-12" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Instantiated as part of markdown generation from code</li><li>Populated with headings and child nodes</li><li>Passed to a renderer that outputs markdown</li></ul><h3 id="clore-generate-textfragment" tabindex="-1"><code>clore::generate::TextFragment</code> <a class="header-anchor" href="#clore-generate-textfragment" aria-label="Permalink to &quot;`clore::generate::TextFragment`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:25</code></p><p>Definition: <code>generate/markdown.cppm:25</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The <code>clore::generate::TextFragment</code> struct is a plain aggregate data carrier consisting of a single public member <code>std::string text</code>. No user‑defined constructors, destructors, or assignment <code>operator</code>s are declared; the type relies entirely on compiler‑generated special member functions. It provides no invariants beyond those inherent to <code>std::string</code> itself – the <code>text</code> member may hold any valid string, including an empty one. Internally, the struct serves as a lightweight wrapper to represent a contiguous piece of generated text within the markdown generation pipeline.</p><h4 id="invariants-12" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-12" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>No explicit invariants; the <code>text</code> member can be any string.</li></ul><h4 id="key-members-13" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-13" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>text</code>: The only data member, holding the fragment content.</li></ul><h4 id="usage-patterns-13" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-13" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>No specific usage patterns evident from evidence.</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-generate-code-spanned-fragments" tabindex="-1"><code>clore::generate::code_spanned_fragments</code> <a class="header-anchor" href="#clore-generate-code-spanned-fragments" aria-label="Permalink to &quot;`clore::generate::code_spanned_fragments`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:124</code></p><p>Definition: <code>generate/markdown.cppm:693</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::code_spanned_fragments</code> serves as a thin public interface that delegates the core algorithm to an internal helper. It accepts a <code>std::string_view</code> parameter <code>text</code>, creates an empty <code>std::vector&lt;InlineFragment&gt;</code> named <code>fragments</code>, and then calls the file‑scope function <code>clore::generate::(anonymous namespace)::append_code_spanned_fragments</code> to populate the vector. Once the helper returns, it returns the fully assembled <code>fragments</code> vector.</p><p>The actual logic inside <code>append_code_spanned_fragments</code> drives the parsing and segmentation of input <code>text</code> into inline fragments that respect code‑span boundaries. It iterates through the characters, identifies backtick‑enclosed code spans, link fragments, <code>operator</code> tokens, camel‑case boundaries, and identifier separators, and dispatches to sub‑routines such as <code>append_code_spanned_line</code>, <code>append_existing_code_span</code>, <code>append_code_aware_text</code>, and various readers (<code>read_backtick_span</code>, <code>read_call_suffix</code>, <code>read_link_span</code>, etc.). The result is a sequence of <code>InlineFragment</code> objects representing code‑style text, plain text, and other markdown inline elements, ready for further rendering or transformation.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>text</code> parameter</li></ul><h4 id="usage-patterns-14" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-14" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called when a vector of <code>InlineFragment</code> objects is needed from a <code>std::string_view</code>, likely as a building block for markdown rendering utilities like <code>code_spanned_markdown</code>.</li></ul><h3 id="clore-generate-code-spanned-markdown" tabindex="-1"><code>clore::generate::code_spanned_markdown</code> <a class="header-anchor" href="#clore-generate-code-spanned-markdown" aria-label="Permalink to &quot;`clore::generate::code_spanned_markdown`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:126</code></p><p>Definition: <code>generate/markdown.cppm:699</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function processes a Markdown string line by line, maintaining an <code>in_fence</code> state variable to track whether the current line is inside a fenced code block. At each line, it checks whether the line is a fence delimiter by calling the anonymous-namespace helper <code>is_fence_line</code>; if so, it toggles <code>in_fence</code> and appends the line unchanged. When <code>in_fence</code> is <code>true</code> and the line is not a fence line, the line is also appended verbatim. For all other lines (outside any fenced block), the function delegates to the anonymous-namespace helper <code>append_code_spanned_line</code>, which applies the code-span transformation to the line. The final output, allocated with an initial reservation equal to the input size, accumulates the processed lines with newlines preserved.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>markdown</code> parameter</li></ul><h4 id="usage-patterns-15" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-15" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during markdown rendering to apply code spans to non-fence lines</li></ul><h3 id="clore-generate-make-blockquote" tabindex="-1"><code>clore::generate::make_blockquote</code> <a class="header-anchor" href="#clore-generate-make-blockquote" aria-label="Permalink to &quot;`clore::generate::make_blockquote`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:113</code></p><p>Definition: <code>generate/markdown.cppm:169</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::make_blockquote</code> constructs a <code>MarkdownNode</code> representing a block quote from a given <code>std::string</code> input. Its implementation is straightforward: it moves the input string into <code>make_text</code>, which produces a <code>TextFragment</code>, then wraps that fragment inside a newly created <code>BlockQuote</code> struct (which holds a vector of <code>TextFragment</code> objects), and finally packs the <code>BlockQuote</code> into a <code>MarkdownNode</code> via its <code>MarkdownNode</code> value constructor. No branching, iteration, or dependency on other generation helpers occurs beyond <code>make_text</code>; the algorithm is a single delegation step with direct struct initialization.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>text</code></li></ul><h4 id="usage-patterns-16" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-16" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>wrap text in a blockquote Markdown node</li></ul><h3 id="clore-generate-make-code" tabindex="-1"><code>clore::generate::make_code</code> <a class="header-anchor" href="#clore-generate-make-code" aria-label="Permalink to &quot;`clore::generate::make_code`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:101</code></p><p>Definition: <code>generate/markdown.cppm:136</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::make_code</code> takes a single <code>std::string</code> parameter named <code>code</code> and returns an <code>InlineFragment</code>. Internally, it directly constructs a <code>CodeFragment</code> object using aggregate initialization, moving the input string into the <code>code</code> field of <code>CodeFragment</code>. No additional processing, validation, or dependencies on other functions are involved; it is a trivial factory function that produces a <code>CodeFragment</code> with the provided code text.</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>code</code></li></ul><h4 id="usage-patterns-17" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-17" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>wrapping a code string into a markdown inline fragment</li></ul><h3 id="clore-generate-make-code-fence" tabindex="-1"><code>clore::generate::make_code_fence</code> <a class="header-anchor" href="#clore-generate-make-code-fence" aria-label="Permalink to &quot;`clore::generate::make_code_fence`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:109</code></p><p>Definition: <code>generate/markdown.cppm:156</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::make_code_fence</code> directly constructs a <code>MarkdownNode</code> containing a <code>CodeFence</code> value. It moves the <code>language</code> and <code>code</code> string parameters into the corresponding fields of the <code>CodeFence</code> struct, then wraps it in the <code>MarkdownNode</code> variant. No additional validation, transformation, or formatting is applied; the implementation is a trivial factory that simply aggregates the two string arguments into the target node representation. The only dependencies are the <code>MarkdownNode</code> and <code>CodeFence</code> types, and the function performs no conditional logic or error handling.</p><h4 id="side-effects-4" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-4" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-4" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-4" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>language parameter</li><li>code parameter</li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returned <code>MarkdownNode</code></li></ul><h4 id="usage-patterns-18" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-18" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Creating markdown code fences for code snippets in generated documentation</li></ul><h3 id="clore-generate-make-link" tabindex="-1"><code>clore::generate::make_link</code> <a class="header-anchor" href="#clore-generate-make-link" aria-label="Permalink to &quot;`clore::generate::make_link`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:103</code></p><p>Definition: <code>generate/markdown.cppm:140</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The implementation of <code>clore::generate::make_link</code> is a trivial factory function that constructs a <code>LinkFragment</code> value. It accepts a <code>label</code> string, a <code>target</code> string, and a <code>code_style</code> boolean, then moves each argument directly into the corresponding field of the <code>LinkFragment</code> struct (<code>label</code>, <code>target</code>, <code>code_style</code>) using brace‑initialization. No validation, transformation, or branching occurs; the function serves solely as a convenient, type‑safe constructor for <code>LinkFragment</code> objects used elsewhere in the generation pipeline. Its only dependency is the shared <code>LinkFragment</code> definition, which is aggregated into the caller’s inline fragment stream.</p><h4 id="side-effects-5" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-5" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-5" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-5" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>label</code></li><li><code>target</code></li><li><code>code_style</code></li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returns a new <code>LinkFragment</code> instance</li></ul><h4 id="usage-patterns-19" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-19" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used to generate inline markdown link fragments</li><li>likely called by rendering functions that produce markdown nodes</li></ul><h3 id="clore-generate-make-mermaid" tabindex="-1"><code>clore::generate::make_mermaid</code> <a class="header-anchor" href="#clore-generate-make-mermaid" aria-label="Permalink to &quot;`clore::generate::make_mermaid`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:111</code></p><p>Definition: <code>generate/markdown.cppm:165</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::make_mermaid</code> constructs a <code>MarkdownNode</code> representing a Mermaid diagram from a provided <code>std::string code</code>. It moves the input <code>code</code> into a <code>MermaidDiagram</code> struct’s <code>code</code> field, then returns a <code>MarkdownNode</code> whose <code>value</code> is initialized with that <code>MermaidDiagram</code> via direct construction. The implementation is straightforward: there is no additional parsing, validation, or transformation of the code string; the function solely wraps the raw diagram text in the appropriate node type so that downstream rendering (<code>clore::generate::render_markdown</code>) can handle it as a code block with Mermaid syntax. The dependency is limited to the <code>MermaidDiagram</code> and <code>MarkdownNode</code> types defined in the same module.</p><h4 id="side-effects-6" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-6" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-6" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-6" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>code</code> parameter (<code>std::string</code>)</li></ul><h4 id="usage-patterns-20" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-20" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Create Mermaid diagram nodes for markdown output</li><li>Used in rendering page bundle markdown</li></ul><h3 id="clore-generate-make-paragraph" tabindex="-1"><code>clore::generate::make_paragraph</code> <a class="header-anchor" href="#clore-generate-make-paragraph" aria-label="Permalink to &quot;`clore::generate::make_paragraph`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:105</code></p><p>Definition: <code>generate/markdown.cppm:148</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The implementation of <code>clore::generate::make_paragraph</code> is a straightforward factory function. It accepts a single <code>std::string</code> parameter (<code>text</code>) and immediately delegates the actual content processing to <code>clore::generate::make_text</code>, which converts the raw string into a <code>TextFragment</code> (likely performing any necessary escaping or inline formatting). The resulting <code>TextFragment</code> is then placed into a <code>Paragraph</code> object’s <code>fragments</code> vector, and the <code>Paragraph</code> is wrapped in a <code>MarkdownNode</code> using aggregate (or brace) initialization. The entire operation is a thin wrapper: no branching, looping, or error handling occurs within this function; all semantic analysis is deferred to <code>make_text</code>. The only dependency is the <code>make_text</code> utility, which must already produce a valid <code>TextFragment</code>.</p><h4 id="side-effects-7" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-7" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-7" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-7" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>function parameter <code>text</code></li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returned <code>MarkdownNode</code></li></ul><h4 id="usage-patterns-21" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-21" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to create a paragraph Markdown node from a plain text string, likely as a helper within larger markdown generation functions.</li></ul><h3 id="clore-generate-make-raw-markdown" tabindex="-1"><code>clore::generate::make_raw_markdown</code> <a class="header-anchor" href="#clore-generate-make-raw-markdown" aria-label="Permalink to &quot;`clore::generate::make_raw_markdown`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:107</code></p><p>Definition: <code>generate/markdown.cppm:152</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::make_raw_markdown</code> constructs a <code>MarkdownNode</code> representing a raw, verbatim markdown block. It moves its <code>std::string</code> argument into a <code>RawMarkdown</code> struct and wraps that struct inside a <code>MarkdownNode</code> via aggregate initialization. No further processing, validation, or transformation is performed; the raw text is stored as-is. This node type is used downstream by <code>render_node</code> and related rendering functions, which may embed the content directly in the final document output.</p><h4 id="side-effects-8" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-8" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-8" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-8" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>markdown</code> of type <code>std::string</code></li></ul><h4 id="usage-patterns-22" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-22" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>create a markdown node from a raw markdown string</li><li>used to embed non-parsed markdown into a <code>MarkdownNode</code></li></ul><h3 id="clore-generate-make-section" tabindex="-1"><code>clore::generate::make_section</code> <a class="header-anchor" href="#clore-generate-make-section" aria-label="Permalink to &quot;`clore::generate::make_section`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:115</code></p><p>Definition: <code>generate/markdown.cppm:173</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::make_section</code> constructs a <code>SemanticSection</code> object by direct aggregate initialization of the struct&#39;s fields using the supplied parameters. It creates a <code>std::unique_ptr&lt;SemanticSection&gt;</code> via <code>std::make_unique</code>, moving the <code>subject_key</code> and <code>heading</code> strings into the allocated object and initializing the remaining scalar fields (<code>kind</code>, <code>level</code>, <code>omit_if_empty</code>, <code>code_style_heading</code>) directly. The <code>children</code> member is default-initialised as an empty vector. There is no branching, error handling, or external dependency beyond the <code>SemanticSection</code> type and standard memory allocation. The returned pointer is the sole outcome; no side effects on any global state occur.</p><h4 id="side-effects-9" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-9" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-9" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-9" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>SemanticKind kind</code></li><li><code>std::string subject_key</code></li><li><code>std::string heading</code></li><li><code>std::uint8_t level</code></li><li><code>bool omit_if_empty</code></li><li><code>bool code_style_heading</code></li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>creates a new <code>SemanticSection</code> object on the heap</li></ul><h4 id="usage-patterns-23" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-23" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to create section nodes for markdown document generation</li><li>Called within page building functions to structure content</li></ul><h3 id="clore-generate-make-text" tabindex="-1"><code>clore::generate::make_text</code> <a class="header-anchor" href="#clore-generate-make-text" aria-label="Permalink to &quot;`clore::generate::make_text`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:99</code></p><p>Definition: <code>generate/markdown.cppm:132</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p><code>clore::generate::make_text</code> is a factory function that constructs an <code>InlineFragment</code> from a raw string. It simply moves the input <code>text</code> into a newly created <code>TextFragment</code> via aggregate initialization, setting the <code>TextFragment::text</code> field. The function serves as a uniform entry point for creating plain-text inline fragments, used throughout the generation pipeline where a literal string needs to be represented as a markdown-compatible <code>InlineFragment</code>. No branching or additional logic is involved; the function relies only on the <code>TextFragment</code> struct definition.</p><h4 id="side-effects-10" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-10" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-10" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-10" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>std::string</code> text parameter</li></ul><h4 id="writes-to-4" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-4" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Return value of type <code>InlineFragment</code></li></ul><h4 id="usage-patterns-24" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-24" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Creating text fragments for markdown nodes</li><li>Wrapping raw strings into <code>InlineFragment</code> objects</li></ul><h3 id="clore-generate-render-markdown" tabindex="-1"><code>clore::generate::render_markdown</code> <a class="header-anchor" href="#clore-generate-render-markdown" aria-label="Permalink to &quot;`clore::generate::render_markdown`&quot;">​</a></h3><p>Declaration: <code>generate/markdown.cppm:122</code></p><p>Definition: <code>generate/markdown.cppm:730</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function first checks for an optional <code>Frontmatter</code> on the <code>MarkdownDocument</code>. If present, it serializes the <code>title</code>, <code>description</code>, <code>layout</code>, and <code>page_template</code> fields into YAML front matter, applying <code>yaml_quote</code> to the title and description strings to escape any special characters. After the front matter (or immediately if none exists), it iterates over each child node in <code>document.children</code> and concatenates the result of calling <code>render_node</code> on that child. The core rendering logic is thus delegated to the <code>render_node</code> dispatcher (and its recursively invoked helpers such as <code>render_section</code>, <code>render_inlines</code>, <code>render_code_span</code>, etc.), which collectively handle all supported <code>MarkdownNode</code> variants.</p><h4 id="side-effects-11" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-11" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-11" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-11" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>document<code>.frontmatter</code></li><li>document<code>.children</code></li><li>fm<code>.title</code></li><li>fm<code>.description</code></li><li>fm<code>.layout</code></li><li>fm<code>.page_template</code></li></ul><h4 id="usage-patterns-25" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-25" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called by page-level rendering functions like <code>render_page_markdown</code> and <code>render_page_bundle</code> to produce the final Markdown output</li><li>used to serialize a <code>MarkdownDocument</code> structure into a string</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>generate:markdown</code> module decomposes documentation generation into an in-memory AST of distinct node types (paragraphs, code fences, blockquotes, lists, mermaid diagrams, raw markdown, and structured semantic sections) along with metadata structures such as <code>Frontmatter</code> and <code>MarkdownDocument</code>. The public API consists of factory functions (<code>make_blockquote</code>, <code>make_code</code>, <code>make_link</code>, etc.) that construct these nodes, and a central <code>render_markdown</code> function that converts the tree to final Markdown output. The module imports only the C++ standard library and relies on an internal layer of anonymous namespace helpers for parsing and formatting inline content—including code-span detection, backtick spans, link spans, <code>operator</code> tokens, and identifier boundary checks—as well as for constructing block-level elements like block quotes and code fences. This separation of concerns keeps node construction, inline handling, and final rendering loosely coupled while maintaining a single translation unit for clarity.</p>',353)])])}const u=a(r,[["render",i]]);export{p as __pageData,u as default};
