import{_ as n,o as i,c as a,a3 as s}from"./chunks/framework.CRbjHCyB.js";const p=JSON.parse('{"title":"Dependency Scanning","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/dependency-scanning.md","filePath":"en/clice/design/dependency-scanning.md"}'),t={name:"clice/design/dependency-scanning.md"};function o(r,e,l,d,c,h){return i(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="dependency-scanning" tabindex="-1">Dependency Scanning <a class="header-anchor" href="#dependency-scanning" aria-label="Permalink to &quot;Dependency Scanning&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>A C++ language server needs to know the include relationships between files. This requirement comes from several directions:</p><p><strong>Compilation context for headers</strong>: Header files are not listed in the compilation database (CDB), so the language server must select a host source file for each header to obtain compilation commands. This requires knowing &quot;which source files include this header&quot; -- the reverse include relationship. Without this information, users opening a header file cannot get correct diagnostics, completions, or navigation features.</p><p><strong>Module dependency resolution</strong>: Compiling C++20 modules requires knowledge of inter-module dependencies. The mapping from module names to files and the import relationships between modules must be discovered by scanning file contents.</p><p><strong>Priority scheduling</strong>: Background indexing needs to determine the order in which files are indexed. Files that have an include relationship with the user&#39;s currently open files should be indexed first. The include graph enables tracking this relevance.</p><p>The problem is that precisely determining include relationships requires running the full C preprocessor -- expanding all macros and evaluating all conditional compilation directives. For large C++ projects (tens of thousands of files), running the preprocessor on every file during startup takes minutes, which is unacceptable.</p><p>clangd&#39;s approach is to build the include graph incrementally during background indexing -- each time a file is compiled, its include relationships are recorded. This means that until background indexing completes (which may take tens of minutes), the include graph is incomplete. Users opening header files during this period may not find the correct host source file, leading to incorrect diagnostics. In the clangd community, users have repeatedly reported that headers display incorrect diagnostics for extended periods after startup and that correct behavior requires waiting for background indexing to finish.</p><p>clice solves this problem with a purpose-built fast dependency scanner -- trading precision for results that are good enough, with the ability to scan tens of thousands of files within seconds during startup.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="core-idea" tabindex="-1">Core Idea <a class="header-anchor" href="#core-idea" aria-label="Permalink to &quot;Core Idea&quot;">​</a></h3><p>The core trade-off of dependency scanning is <strong>exchanging precision for speed</strong>. A full preprocessor can precisely determine which includes are active and which are excluded by conditional compilation, but it has a fundamental performance problem: the same header may produce different expansion results in different source file contexts, so each (source file, header) combination must be preprocessed independently -- results cannot be shared. The fast scanner abandons preprocessing, making scan results independent of inclusion context -- each file needs to be scanned only once, and results are shared across all contexts. The cost is that includes from all conditional branches are treated as active, yielding a <strong>superset of all possible include relationships</strong>. Although this superset is larger than the actual include relationships, it is safe for the primary use cases of dependency scanning:</p><ul><li>When finding host source files, the superset may yield more candidate hosts but will never miss a correct one</li><li>When judging file relevance, the superset may consider some unrelated files as related but will never miss truly related files</li></ul><h3 id="why-it-is-so-fast" tabindex="-1">Why It Is So Fast <a class="header-anchor" href="#why-it-is-so-fast" aria-label="Permalink to &quot;Why It Is So Fast&quot;">​</a></h3><p>The fast scanner&#39;s speed comes from one fundamental design choice and several key optimizations:</p><p><strong>Fundamental advantage: cross-context result sharing</strong></p><p>This is the root reason the fast scanner can be orders of magnitude faster than full preprocessing. Under full preprocessing, when different source files include the same header, the preceding <code>#define</code>s and <code>#include</code>s differ, so the preprocessing results may be entirely different. This means each (source file, header) combination must be preprocessed independently -- the workload is O(number of source files x average header depth). For a large project with tens of thousands of source files, each including hundreds of headers on average, this amounts to millions of preprocessing operations.</p><p>The fast scanner abandons preprocessing, so scan results are <strong>independent of inclusion context</strong> -- the same header yields the same <code>#include</code> list regardless of which source file includes it. This means each header needs to be scanned only once, and results are shared across all source files. The workload drops to O(number of unique files) -- typically just thousands to tens of thousands of files, rather than millions of operations.</p><p><strong>Lightweight lexical scanning</strong></p><p>The scanner uses Clang&#39;s dependency directive scanner (scanSourceForDependencyDirectives), which identifies only preprocessor directive lines beginning with <code>#</code> without performing macro expansion, evaluating conditional expressions, or expanding includes. Scanning each file takes on the order of microseconds.</p><p>The scan results are raw include names (e.g., <code>&quot;foo.h&quot;</code> or <code>&lt;vector&gt;</code>) that require a subsequent path resolution phase to map them to actual file paths.</p><p>For includes inside conditional compilation, the scanner tracks <code>#if</code>/<code>#ifdef</code>/<code>#ifndef</code> nesting depth to tag them: includes encountered at a depth greater than zero are marked as &quot;conditional.&quot; While this tag does not know the specific evaluation result of the condition, it provides useful metadata.</p><p><strong>Separate path resolution phase</strong></p><p>Mapping include names to actual file paths is a separate phase. The resolver uses a directory listing cache -- it pre-reads the file listing for each directory in the search paths, then checks file existence via in-memory string sets, avoiding large numbers of stat system calls.</p><p>Resolution results for angle-bracket includes (e.g., <code>&lt;vector&gt;</code>) can be cached across files -- under the same compilation configuration, the same header name always resolves to the same path. Double-quoted includes depend on the includer&#39;s directory and cannot be cached across files, but each file typically has very few double-quoted includes.</p><p><strong>Wavefront BFS discovery</strong></p><p>Rather than processing all files at once, scanning unfolds in waves:</p><ul><li><strong>Wave 0</strong>: Scan all source files in the compilation database (parallel I/O + lexical scanning)</li><li><strong>Path resolution</strong>: Map discovered include names to file paths, identifying newly discovered headers</li><li><strong>Wave 1</strong>: Scan the newly discovered headers, discovering their includes...</li><li>Repeat until no new files are found</li></ul><p>A key optimization: while the current wave is performing path resolution (serial), the next wave&#39;s files can already begin prefetching and scanning (parallel). This pipeline-style overlap hides most I/O latency.</p><h3 id="dependencygraph-storage-structure" tabindex="-1">DependencyGraph Storage Structure <a class="header-anchor" href="#dependencygraph-storage-structure" aria-label="Permalink to &quot;DependencyGraph Storage Structure&quot;">​</a></h3><p>DependencyGraph stores include relationships between files and supports both forward and reverse queries:</p><p><strong>Forward includes</strong>: Given a file and compilation configuration, returns all files it directly includes. Each edge records whether it is a conditional include (distinguished via bit flags). A file may have different include sets under different compilation configurations (because search paths differ), so the key is a (file, configuration) pair.</p><p><strong>Reverse includes</strong>: Given a file, returns all files that directly include it. This is a reverse index built in bulk after all forward includes have been established. It is used for &quot;finding host source files&quot; -- BFS upward from the target header along reverse edges until a source file with a CDB entry is found.</p><p><strong>Module mapping</strong>: A mapping from module names to module interface unit file paths, used for C++20 module dependency resolution. Only interface units (<code>export module</code>) are registered in the mapping. A module name may map to multiple paths (when the same module is discovered in different compilation configurations).</p><h3 id="handling-conditional-includes" tabindex="-1">Handling Conditional Includes <a class="header-anchor" href="#handling-conditional-includes" aria-label="Permalink to &quot;Handling Conditional Includes&quot;">​</a></h3><p>The scanner does not evaluate conditional expressions but uses nesting depth tracking to tag includes as &quot;conditional&quot; or &quot;unconditional&quot;:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;vector&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           // unconditional (depth 0)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#ifdef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> USE_BOOST</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;boost/any.hpp&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // conditional (depth 1)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#ifdef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BOOST_HAS_X</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;boost/x.hpp&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // conditional (depth 2)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;string&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           // unconditional (depth 0)</span></span></code></pre></div><p>When querying the union of a file&#39;s includes across all configurations, if the same header is unconditional in one configuration and conditional in another, unconditional takes precedence -- reflecting the semantics that &quot;in at least one configuration, it will definitely be included.&quot;</p><h3 id="handling-module-declarations" tabindex="-1">Handling Module Declarations <a class="header-anchor" href="#handling-module-declarations" aria-label="Permalink to &quot;Handling Module Declarations&quot;">​</a></h3><p>C++20 module declarations (<code>export module foo;</code>, <code>module foo:bar;</code>) typically appear at the top of a file, and the fast scanner can identify them directly. However, if a module declaration appears inside a conditional compilation block (<code>#ifdef</code>), the fast scanner cannot determine which declaration is in effect.</p><p>In this case, the scanner sets a <code>need_preprocess</code> flag, triggering a precise fallback -- running the preprocessor only on the file&#39;s header (up to the module declaration), evaluating conditional expressions to determine the actual module name. This fallback affects very few files (the vast majority of module declarations are at the top level) and does not impact overall scanning speed.</p><h3 id="caching-and-incremental-updates" tabindex="-1">Caching and Incremental Updates <a class="header-anchor" href="#caching-and-incremental-updates" aria-label="Permalink to &quot;Caching and Incremental Updates&quot;">​</a></h3><p>Scan results are cached at multiple levels:</p><ul><li><strong>Scan result cache</strong>: Each file&#39;s scan results (include list, module declarations) are cached by path_id and reused within a single scan to avoid redundant reads.</li><li><strong>Directory listing cache</strong>: File listings for each directory in the search paths are cached in memory, populated via concurrent readdir tasks during the initial scan.</li><li><strong>Include resolution cache</strong>: Resolution results for angle-bracket includes are cached by (configuration, header name), including negative caches for resolution failures.</li></ul><p>These caches are currently valid within a single scan invocation. The cache infrastructure is designed to support cross-scan persistence (warm starts), but this has not yet been enabled in the server integration.</p><h3 id="collaboration-with-host-source-file-lookup" tabindex="-1">Collaboration with Host Source File Lookup <a class="header-anchor" href="#collaboration-with-host-source-file-lookup" aria-label="Permalink to &quot;Collaboration with Host Source File Lookup&quot;">​</a></h3><p>One of the core uses of dependency scanning is finding host source files for headers. The process is as follows:</p><ol><li>Starting from the target header, traverse upward via the reverse include index</li><li>Find all root files that transitively include the target header (files not included by any other file)</li><li>Filter for source files that have compilation commands in the CDB as candidate hosts</li><li>Select the first candidate host with a valid include chain as the default host</li></ol><p>The include chain lookup uses BFS to guarantee the shortest path is found. The compilation context system then uses this include chain to synthesize the header&#39;s compilation environment.</p><h3 id="precise-scanning-and-background-indexing-as-supplements" tabindex="-1">Precise Scanning and Background Indexing as Supplements <a class="header-anchor" href="#precise-scanning-and-background-indexing-as-supplements" aria-label="Permalink to &quot;Precise Scanning and Background Indexing as Supplements&quot;">​</a></h3><p>The fast scan provides approximate results during the startup phase. As the server runs, the background indexing system gradually compiles every translation unit in the project, obtaining precise include relationships during compilation (with full preprocessing, evaluating all macros and conditional compilation). These precise include relationships are recorded in the index data (TUIndex/MergedIndex). Note that background indexing currently does not update the DependencyGraph built during startup — the fast-scan include graph remains in use for host source lookup and file-dependency queries throughout the server&#39;s lifetime.</p><p>Additionally, the system provides a precise scanning mode (scan_precise) -- running the full Clang preprocessor for specific scenarios that require accurate dependency information, such as lazy dependency resolution during module compilation (via CompileGraph&#39;s resolve_fn). Precise scan results are resolved file paths (rather than raw include names), accurately reflecting the actual include relationships under a specific compilation configuration.</p><p>Fast scanning and background indexing are complementary: fast scanning guarantees that a usable include graph is available within seconds of startup (so users immediately get header file support), while background indexing gradually supplements precise information over the following minutes. The two are not alternatives -- even after background indexing completes, fast scanning results are still used for initial file discovery and building the initial dependency graph.</p><h2 id="design-decisions-and-trade-offs" tabindex="-1">Design Decisions and Trade-offs <a class="header-anchor" href="#design-decisions-and-trade-offs" aria-label="Permalink to &quot;Design Decisions and Trade-offs&quot;">​</a></h2><p><strong>Why a superset instead of precise results?</strong> Precise results require running the preprocessor, which takes minutes for tens of thousands of files. The superset completes in seconds and is safe for host lookup and relevance judgment -- finding a few extra candidate hosts is far better than missing the correct one.</p><p><strong>Why not rely solely on background indexing to build a precise include graph?</strong> clangd relies entirely on background indexing to build include relationships, but background indexing may take tens of minutes to complete. During this period, the header file experience is incomplete -- users open a header file and see incorrect diagnostics that take a long time to self-correct. clice uses fast scanning to guarantee a usable include graph within seconds of startup, with background indexing gradually supplementing precise information afterward. The combination ensures both immediate availability and eventual precision.</p><p><strong>Why unfold in waves instead of scanning all files at once?</strong> The system does not know upfront which headers need scanning -- they are not in the CDB and can only be discovered by resolving source files&#39; includes. Wavefront expansion is the natural pattern for lazy discovery, while pipeline overlap maximizes parallelism.</p><p><strong>Why is the conditional include tag useful?</strong> Although the specific condition result is unknown, distinguishing &quot;will definitely be included&quot; from &quot;might be included&quot; provides guidance for host selection. A source file that includes the target header through an unconditional include chain is a more reliable host candidate than one connected through a conditional include chain.</p><h2 id="known-limitations" tabindex="-1">Known Limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known Limitations&quot;">​</a></h2><ul><li><strong>Macro-ized includes</strong>: Includes of the form <code>#include MACRO_NAME</code> cannot be resolved by the fast scanner -- they require macro expansion to determine the actual header name. This pattern is relatively uncommon in real projects; affected files are correctly handled during precise compilation.</li><li><strong>Conditional compilation precision</strong>: Includes from all conditional branches are recorded, making the include graph larger than the actual one. This is safe for host lookup (nothing is missed) but may cause relevance judgments to be overly broad.</li><li><strong>Case sensitivity</strong>: On case-insensitive file systems (macOS, Windows), if the case of an include does not match the file name, resolution may fail.</li><li><strong>Framework search paths</strong>: macOS Framework directories (<code>-F</code>, <code>-iframework</code>) are not yet supported; Framework includes of the form <code>&lt;Foo/Bar.h&gt;</code> cannot be resolved correctly.</li></ul>`,60)])])}const g=n(t,[["render",o]]);export{p as __pageData,g as default};
