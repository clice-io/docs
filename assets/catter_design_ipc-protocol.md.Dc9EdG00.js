import{_ as s,o as t,c as a,a3 as i}from"./chunks/framework.CRbjHCyB.js";const k=JSON.parse('{"title":"IPC Protocol","description":"","frontmatter":{},"headers":[],"relativePath":"catter/design/ipc-protocol.md","filePath":"en/catter/design/ipc-protocol.md"}'),n={name:"catter/design/ipc-protocol.md"};function o(r,e,d,p,c,l){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="ipc-protocol" tabindex="-1">IPC Protocol <a class="header-anchor" href="#ipc-protocol" aria-label="Permalink to &quot;IPC Protocol&quot;">​</a></h1><p>Catter uses inter-process communication between the daemon (<code>catter</code>) and proxy instances (<code>catter-proxy</code>). The daemon acts as a server, and each proxy instance connects as a client.</p><h2 id="transport" tabindex="-1">Transport <a class="header-anchor" href="#transport" aria-label="Permalink to &quot;Transport&quot;">​</a></h2><p>The transport layer is platform-specific:</p><table tabindex="0"><thead><tr><th>Platform</th><th>Mechanism</th><th>Path / Name</th></tr></thead><tbody><tr><td>Linux / macOS</td><td>Unix domain socket</td><td><code>$XDG_DATA_HOME/pipe-catter-ipc.sock</code> (typically <code>~/.local/share/pipe-catter-ipc.sock</code>)</td></tr><tr><td>Windows</td><td>Named pipe</td><td><code>\\\\.\\pipe\\catter-ipc</code></td></tr></tbody></table><p>The daemon creates the listening socket/pipe at startup. Each <code>catter-proxy</code> instance connects to it as a client when it starts. The connection persists for the lifetime of the proxy process.</p><h2 id="serialization" tabindex="-1">Serialization <a class="header-anchor" href="#serialization" aria-label="Permalink to &quot;Serialization&quot;">​</a></h2><p>All messages are encoded using <a href="https://github.com/bincode-org/bincode" target="_blank" rel="noreferrer">Bincode</a>, a compact binary serialization format. The implementation uses <code>kota::ipc::BincodePeer</code>, which provides a request-response abstraction over a stream transport (<code>kota::ipc::StreamTransport</code>).</p><p>Bincode was chosen for its efficiency -- messages are small and fast to encode/decode, which matters because every intercepted build command results in multiple IPC round-trips.</p><h2 id="request-response-model" tabindex="-1">Request-Response Model <a class="header-anchor" href="#request-response-model" aria-label="Permalink to &quot;Request-Response Model&quot;">​</a></h2><p>Communication follows a strict request-response pattern. The client (proxy) sends a request and waits for the server (daemon) to respond before proceeding. Each request type has a well-defined parameter type and result type.</p><p>The protocol is defined in <code>src/common/util/data.h</code> using C++ template specialization:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">enum</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RequestType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> : </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint8_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    CHECK_MODE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    CREATE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    MAKE_DECISION</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    REPORT_ERROR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    FINISH</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>Each request type maps to a <code>Request&lt;Type&gt;</code> specialization that declares <code>Params</code> (the request payload) and <code>Result</code> (the response payload).</p><h2 id="request-types" tabindex="-1">Request Types <a class="header-anchor" href="#request-types" aria-label="Permalink to &quot;Request Types&quot;">​</a></h2><h3 id="check-mode" tabindex="-1">CHECK_MODE <a class="header-anchor" href="#check-mode" aria-label="Permalink to &quot;CHECK_MODE&quot;">​</a></h3><p>Asks the daemon what service mode is active.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><strong>Params</strong></td><td><code>ServiceMode</code> (enum)</td><td>The mode to check (currently only <code>INJECT</code>)</td></tr><tr><td><strong>Result</strong></td><td><code>bool</code></td><td><code>true</code> if the daemon is in the requested mode</td></tr></tbody></table><p>This is the first request a proxy sends after connecting. It confirms the daemon is ready to handle intercepted commands.</p><h3 id="create" tabindex="-1">CREATE <a class="header-anchor" href="#create" aria-label="Permalink to &quot;CREATE&quot;">​</a></h3><p>Registers a new process session with the daemon.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><strong>Params</strong></td><td><code>ipcid_t</code> (<code>int32_t</code>)</td><td>The parent session ID</td></tr><tr><td><strong>Result</strong></td><td><code>ipcid_t</code> (<code>int32_t</code>)</td><td>A unique session ID assigned to this proxy instance</td></tr></tbody></table><p>The parent ID establishes the parent-child relationship in the session tree. For the first proxy (injector mode), the parent ID is provided by the daemon when it spawns the proxy. For wrapper-mode proxies, the parent ID comes from the <code>__key_catter_command_id_v1</code> environment variable (or <code>CATTER_IPC_ID</code> on Windows), which was set by the hook.</p><h3 id="make-decision" tabindex="-1">MAKE_DECISION <a class="header-anchor" href="#make-decision" aria-label="Permalink to &quot;MAKE_DECISION&quot;">​</a></h3><p>The core request. The proxy sends a captured command and the daemon decides what to do with it.</p><p><strong>Params</strong> -- <code>command</code>:</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>cwd</code></td><td><code>string</code></td><td>Working directory of the intercepted process</td></tr><tr><td><code>executable</code></td><td><code>string</code></td><td>Resolved absolute path to the executable</td></tr><tr><td><code>args</code></td><td><code>string[]</code></td><td>Full argument array (including <code>argv[0]</code>)</td></tr><tr><td><code>env</code></td><td><code>string[]</code></td><td>Environment variables (in <code>KEY=VALUE</code> format)</td></tr></tbody></table><p><strong>Result</strong> -- <code>action</code>:</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>type</code></td><td><code>uint8_t</code> enum</td><td>One of <code>DROP</code>, <code>INJECT</code>, or <code>WRAP</code></td></tr><tr><td><code>cmd</code></td><td><code>command</code></td><td>The command to execute (may be modified by the script)</td></tr></tbody></table><p><strong>Action types</strong>:</p><ul><li><strong><code>DROP</code> (0)</strong> -- Do not execute the command. The proxy returns exit code 0 immediately. Used when the script determines a command is irrelevant (e.g., a compiler invocation the user wants to skip).</li><li><strong><code>INJECT</code> (1)</strong> -- Execute the command with the hook library attached. The proxy re-adds <code>LD_PRELOAD</code> (or performs DLL injection on Windows) so that child processes of this command are also intercepted. This is the default for build commands whose children should be monitored.</li><li><strong><code>WRAP</code> (2)</strong> -- Execute the command directly without hooking. The proxy runs the command and captures its stdout/stderr, but does not inject the hook. Used for leaf commands (like actual compiler invocations) that do not spawn further build processes.</li></ul><p>The daemon may modify the command in the returned action. For example, a script could change compiler flags, redirect output paths, or substitute a different executable.</p><h3 id="report-error" tabindex="-1">REPORT_ERROR <a class="header-anchor" href="#report-error" aria-label="Permalink to &quot;REPORT_ERROR&quot;">​</a></h3><p>Reports an error condition from the hook or proxy back to the daemon.</p><p><strong>Params</strong>:</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>parent_id</code></td><td><code>ipcid_t</code></td><td>Session ID of the parent process</td></tr><tr><td><code>error_msg</code></td><td><code>string</code></td><td>Human-readable error description</td></tr></tbody></table><p><strong>Result</strong>: <code>null</code> (no response payload)</p><p>This is used when the hook encounters an invalid state (e.g., missing environment variables) or when the proxy catches an exception during command processing. The daemon logs the error and can notify the user.</p><h3 id="finish" tabindex="-1">FINISH <a class="header-anchor" href="#finish" aria-label="Permalink to &quot;FINISH&quot;">​</a></h3><p>Reports that a command has completed execution.</p><p><strong>Params</strong> -- <code>process_result</code>:</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>code</code></td><td><code>int64_t</code></td><td>Process exit code</td></tr><tr><td><code>std_out</code></td><td><code>string</code></td><td>Captured standard output</td></tr><tr><td><code>std_err</code></td><td><code>string</code></td><td>Captured standard error</td></tr></tbody></table><p><strong>Result</strong>: <code>null</code> (no response payload)</p><p>After the daemon receives this request, it invokes the <code>onExecution()</code> JavaScript callback with the result data. The proxy then disconnects.</p><h2 id="typical-message-sequence" tabindex="-1">Typical Message Sequence <a class="header-anchor" href="#typical-message-sequence" aria-label="Permalink to &quot;Typical Message Sequence&quot;">​</a></h2><p>A complete proxy lifecycle involves this sequence of IPC messages:</p><h3 id="injector-mode-first-proxy" tabindex="-1">Injector Mode (first proxy) <a class="header-anchor" href="#injector-mode-first-proxy" aria-label="Permalink to &quot;Injector Mode (first proxy)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Proxy -&gt; Daemon:  CHECK_MODE(INJECT)</span></span>
<span class="line"><span>Daemon -&gt; Proxy:  true</span></span>
<span class="line"><span>[Proxy launches build command with hook attached]</span></span>
<span class="line"><span>[Proxy waits for build to complete]</span></span>
<span class="line"><span>[Proxy exits when build finishes]</span></span></code></pre></div><p>The injector proxy also sends <code>CREATE</code>, <code>MAKE_DECISION</code>, and <code>FINISH</code> for the top-level build command, so the build system command itself passes through <code>onCommand</code>/<code>onExecution</code> like any other intercepted command.</p><h3 id="wrapper-mode-intercepted-command" tabindex="-1">Wrapper Mode (intercepted command) <a class="header-anchor" href="#wrapper-mode-intercepted-command" aria-label="Permalink to &quot;Wrapper Mode (intercepted command)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Proxy -&gt; Daemon:  CHECK_MODE(INJECT)</span></span>
<span class="line"><span>Daemon -&gt; Proxy:  true</span></span>
<span class="line"><span>Proxy -&gt; Daemon:  CREATE(parent_id)</span></span>
<span class="line"><span>Daemon -&gt; Proxy:  new_session_id</span></span>
<span class="line"><span>Proxy -&gt; Daemon:  MAKE_DECISION(command)</span></span>
<span class="line"><span>Daemon -&gt; Proxy:  action {type, cmd}</span></span>
<span class="line"><span>[Proxy executes or drops the command]</span></span>
<span class="line"><span>Proxy -&gt; Daemon:  FINISH(process_result)</span></span>
<span class="line"><span>Daemon -&gt; Proxy:  null</span></span>
<span class="line"><span>[Proxy disconnects]</span></span></code></pre></div><h3 id="error-case" tabindex="-1">Error Case <a class="header-anchor" href="#error-case" aria-label="Permalink to &quot;Error Case&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Proxy -&gt; Daemon:  REPORT_ERROR(parent_id, error_message)</span></span>
<span class="line"><span>Daemon -&gt; Proxy:  null</span></span>
<span class="line"><span>[Proxy exits with code -1]</span></span></code></pre></div><h2 id="session-model" tabindex="-1">Session Model <a class="header-anchor" href="#session-model" aria-label="Permalink to &quot;Session Model&quot;">​</a></h2><p>The daemon maintains a session tree that mirrors the process tree of the build:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Session 0 (root -- injector proxy)</span></span>
<span class="line"><span>  +-- Session 1 (make -&gt; gcc file1.c)</span></span>
<span class="line"><span>  +-- Session 2 (make -&gt; gcc file2.c)</span></span>
<span class="line"><span>  +-- Session 3 (make -&gt; ar rcs libfoo.a file1.o file2.o)</span></span>
<span class="line"><span>  +-- Session 4 (make -&gt; sh -c &quot;gcc file3.c&quot;)</span></span>
<span class="line"><span>       +-- Session 5 (sh -&gt; gcc file3.c)</span></span></code></pre></div><p>Each session is identified by a unique <code>ipcid_t</code> (32-bit integer). The tree is built incrementally as <code>CREATE</code> requests arrive, each specifying a <code>parent_id</code>. This structure enables:</p><ul><li><strong>Target tree reconstruction</strong> -- by knowing which object files are linked into which targets</li><li><strong>Build profiling</strong> -- by tracking timing data per session and visualizing the parallelism</li><li><strong>Error attribution</strong> -- by tracing errors back to the build command that caused them</li></ul><h2 id="connection-lifecycle" tabindex="-1">Connection Lifecycle <a class="header-anchor" href="#connection-lifecycle" aria-label="Permalink to &quot;Connection Lifecycle&quot;">​</a></h2><ol><li><strong>Daemon starts</strong> -- Creates and binds the Unix domain socket or named pipe.</li><li><strong>Proxy connects</strong> -- Opens a connection to the socket/pipe. The connection is established using <code>kota::pipe::connect()</code> on the proxy side.</li><li><strong>Daemon accepts</strong> -- For each incoming connection, the daemon creates a <code>kota::ipc::BincodePeer</code> and registers request handlers (<code>on_request&lt;Request&lt;Type&gt;&gt;(...)</code>) for each request type.</li><li><strong>Request handling</strong> -- The daemon&#39;s event loop dispatches incoming requests to the appropriate handler. Handlers may be async (using <code>co_await</code>) to interact with the JS runtime or other daemon state.</li><li><strong>Peer disconnects</strong> -- When the proxy process exits, the transport closes and the <code>BincodePeer::run()</code> coroutine completes. The daemon logs the disconnection and cleans up the session.</li></ol><h2 id="data-types" tabindex="-1">Data Types <a class="header-anchor" href="#data-types" aria-label="Permalink to &quot;Data Types&quot;">​</a></h2><p>The core data structures used across the IPC boundary:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Unique identifier for an IPC session</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ipcid_t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int32_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Captured command from the build system</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> command</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string cwd;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                   // Working directory</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string executable;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // Resolved executable path</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::vector</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> args;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // Argument array</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::vector</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> env;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // Environment (KEY=VALUE entries)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Result of executing a command</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> process_result</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> code </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 // Exit code</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string std_out;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               // Captured stdout</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string std_err;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               // Captured stderr</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Decision returned by the daemon</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    enum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> : </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint8_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        DROP</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Do not execute</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        INJECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Execute with hook attached</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        WRAP</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Execute without hook</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } type;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    command cmd;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                       // Possibly modified command</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div>`,63)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
