import{_ as c,C as r,o as a,c as n,a3 as t,b as s,w as o,a as d,E as l,a4 as h}from"./chunks/framework.aYYe0dfI.js";const _=JSON.parse('{"title":"Module schema","description":"The schema module is responsible for generating OpenAI‑compatible JSON schema representations from C++ types. It provides type‑aware utilities that produce schema objects for structured‑output response formats and function‑tool definitions, enabling type‑safe integration with OpenAI’s tool‑calling and structured‑response protocols. The module also includes internal validation machinery to ensure constructed schemas conform to the expected shape and constraints.","frontmatter":{"title":"Module schema","description":"The schema module is responsible for generating OpenAI‑compatible JSON schema representations from C++ types. It provides type‑aware utilities that produce schema objects for structured‑output response formats and function‑tool definitions, enabling type‑safe integration with OpenAI’s tool‑calling and structured‑response protocols. The module also includes internal validation machinery to ensure constructed schemas conform to the expected shape and constraints.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/schema/index.md","filePath":"en/clore/modules/schema/index.md"}'),p={name:"clore/modules/schema/index.md"};function m(u,e,f,b,y,g){const i=r("Mermaid");return a(),n("div",null,[e[1]||(e[1]=t('<h1 id="module-schema" tabindex="-1">Module <code>schema</code> <a class="header-anchor" href="#module-schema" aria-label="Permalink to &quot;Module `schema`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>schema</code> module is responsible for generating <code>OpenAI</code>‑compatible JSON schema representations from C++ types. It provides type‑aware utilities that produce schema objects for structured‑output response formats and function‑tool definitions, enabling type‑safe integration with <code>OpenAI</code>’s tool‑calling and structured‑response protocols. The module also includes internal validation machinery to ensure constructed schemas conform to the expected shape and constraints.</p><p>The public‑facing scope of the module includes the <code>clore::net::schema::response_format</code> and <code>clore::net::schema::function_tool</code> template functions, which return integer handles representing a schema for a given C++ type. Supporting public validation functions, such as <code>clore::net::detail::validate_response_format</code> and <code>clore::net::detail::validate_tool_definition</code>, are also provided. Under the hood, the <code>openai::schema::detail</code> namespace contains metaprogramming traits and construction routines that handle containers, optionals, and scalar types, together with recursive validation passes that check properties, types, and structural completeness.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../http/"><code>http</code></a></li><li><a href="./../protocol/"><code>protocol</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./../agent/tools"><code>agent:tools</code></a></li><li><a href="./../anthropic/"><code>anthropic</code></a></li><li><a href="./../client/"><code>client</code></a></li><li><a href="./../openai/"><code>openai</code></a></li><li><a href="./../provider/"><code>provider</code></a></li></ul><h2 id="dependency-diagram" tabindex="-1">Dependency Diagram <a class="header-anchor" href="#dependency-diagram" aria-label="Permalink to &quot;Dependency Diagram&quot;">​</a></h2>',9)),(a(),s(h,null,{default:o(()=>[l(i,{id:"mermaid-70",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20M0%5B%22schema%22%5D%0A%20%20%20%20I0%5B%22http%22%5D%0A%20%20%20%20I0%20--%3E%20M0%0A%20%20%20%20I1%5B%22protocol%22%5D%0A%20%20%20%20I1%20--%3E%20M0%0A%20%20%20%20I2%5B%22support%22%5D%0A%20%20%20%20I2%20--%3E%20M0%0A"})]),fallback:o(()=>[...e[0]||(e[0]=[d(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=t('<h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-net-openai-schema-detail-array-inner" tabindex="-1"><code>clore::net::openai::schema::detail::array_inner</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-array-inner" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::array_inner`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:72</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The template struct <code>clore::net::openai::schema::detail::array_inner</code> is an implementation detail that captures the inner type information for array schemas. It is designed to provide a type alias or metadata for the element type contained within an array, supporting the broader schema machinery without exposing internal helpers. As a template parameterized by <code>T</code>, it maintains a conceptual invariant that <code>T</code> represents the value type of the array, enabling consistent type‑based dispatch or deduction within the detail namespace. The struct does not store runtime state; its purpose is purely compile‑time, typically exposing a nested type alias or static member to denote the contained type. This design isolates the array‑inner logic from the public API while preserving type safety and composability.</p><h3 id="clore-net-openai-schema-detail-is-array" tabindex="-1"><code>clore::net::openai::schema::detail::is_array</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-is-array" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::is_array`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:63</code></p><p>Definition: <code>network/schema.cppm:63</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The template struct <code>clore::net::openai::schema::detail::is_array</code> is a type trait predicate that defaults to <code>std::false_type</code>. Its primary template provides a compile‑time constant <code>value</code> equal to <code>false</code> for any type <code>T</code>. This struct is intended to be specialised for array types, such that <code>is_array&lt;T&gt;::value</code> becomes <code>true</code> for those types. As a <code>detail</code> implementation helper, it supports the internal detection of array‑like categories within the schema module, typically used to dispatch or constrain template instantiations based on whether a type is an array. The struct itself contains no data members or custom logic beyond the inherited <code>std::false_type</code>; the invariants are maintained by the implicit <code>std::integral_constant</code> base, ensuring a consistent <code>value</code> member for all supported specialisations.</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>The primary template defines <code>value</code> as <code>false</code>.</li><li>Only specializations can change the boolean value.</li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>Base class <code>std::false_type</code></li><li>Inherited member <code>value</code> (static constexpr bool)</li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used in compile-time type checking, e.g., with <code>std::enable_if</code>.</li><li>Expected to be specialized for array types to enable or disable template overloads.</li></ul><h3 id="clore-net-openai-schema-detail-is-optional" tabindex="-1"><code>clore::net::openai::schema::detail::is_optional</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-is-optional" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::is_optional`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:23</code></p><p>Definition: <code>network/schema.cppm:23</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The struct <code>clore::net::openai::schema::detail::is_optional</code> is a primary template type trait that inherits from <code>std::false_type</code>. As a default definition, it serves as a negative sentinel: its static member <code>value</code> is always <code>false</code>, indicating that any type <code>T</code> not covered by a specialization is not considered an optional. The internal structure is trivial—no additional members or base classes beyond the inherited <code>std::false_type</code>—and it functions as a detection hook. Specializations (not shown here) that derive from <code>std::true_type</code> are expected to be provided elsewhere for types that should be recognized as optionals, thereby establishing a compile-time predicate for conditional logic in the schema or serialization machinery.</p><h4 id="invariants-1" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-1" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>Primary template always yields <code>value == false</code></li><li>Specializations must be consistent with the detected optional type</li><li>Inheritance from <code>std::false_type</code> implies <code>value</code> is a compile-time constant</li></ul><h4 id="key-members-1" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-1" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>T</code> template parameter representing the type to test</li><li>Inherited <code>value</code> static constexpr bool member</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used in SFINAE or <code>if constexpr</code> to conditionally handle optional types</li><li>Specialized for <code>std::optional</code> to enable <code>value == true</code></li><li>Consulted by serialization or conversion utilities to determine null handling</li></ul><h3 id="clore-net-openai-schema-detail-is-vector" tabindex="-1"><code>clore::net::openai::schema::detail::is_vector</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-is-vector" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::is_vector`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:43</code></p><p>Definition: <code>network/schema.cppm:43</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The primary template of <code>clore::net::openai::schema::detail::is_vector</code> inherits from <code>std::false_type</code>, establishing a default <code>value</code> of <code>false</code> for any type <code>T</code>. This invariant ensures that unqualified types are not recognized as vectors unless an explicit specialization overrides the base class. No additional members or functions are defined; the sole implementation detail is the inheritance chain that provides the compile-time boolean constant. The type trait is intended to be specialized for <code>std::vector</code> (and potentially other container types) to yield <code>std::true_type</code>, enabling template metaprogramming decisions based on vector semantics.</p><h4 id="invariants-2" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-2" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>is_vector&lt;T&gt;::value</code> is <code>false</code> for all types <code>T</code> in the primary template</li><li>The struct is trivially constructible and destructible</li></ul><h4 id="key-members-2" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-2" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li>Inherited <code>value</code> from <code>std::false_type</code> (static constexpr bool)</li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as a type trait to detect vector types via template specialization</li><li>Employed in template metaprogramming for conditional logic</li></ul><h3 id="clore-net-openai-schema-detail-optional-inner" tabindex="-1"><code>clore::net::openai::schema::detail::optional_inner</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-optional-inner" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::optional_inner`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:32</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The struct <code>clore::net::openai::schema::detail::optional_inner&lt;T&gt;</code> is a template implementation detail used internally to manage optional schema fields. It encapsulates a value of type <code>T</code> along with a presence flag, providing a storage layout that avoids unnecessary default construction when the value is absent. The class maintains the invariant that the stored object is only constructed when the flag is <code>true</code>, and it manually manages the lifetime of <code>T</code> through placement new and explicit destructor calls in its constructors, assignment <code>operator</code>s, and destructor. Accessor member functions such as <code>has_value</code> and <code>value</code> are provided, with <code>value</code> throwing an exception if the flag is <code>false</code>. The design supports copy and move semantics, ensuring proper handling of the underlying value’s state transitions.</p><h3 id="clore-net-openai-schema-detail-schema-subject" tabindex="-1"><code>clore::net::openai::schema::detail::schema_subject</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-schema-subject" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::schema_subject`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:83</code></p><p>Definition: <code>network/schema.cppm:83</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The template struct <code>clore::net::openai::schema::detail::schema_subject</code> is a type‑level metafunction that normalises its template parameter <code>T</code> by removing top‑level <code>const</code>, <code>volatile</code>, and reference qualifiers. Its sole internal member is the alias <code>type</code>, defined as <code>std::remove_cvref_t&lt;T&gt;</code>. This ensures that any type passed through <code>schema_subject</code> is reduced to its cv‑unqualified, non‑reference form, providing a consistent base type for subsequent schema‑related operations. The implementation is trivial but carries the invariant that <code>type</code> always names the underlying type without any outer qualifiers or references.</p><h4 id="invariants-3" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-3" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>The <code>type</code> member always yields the decayed type without cv-ref qualifiers.</li><li>The struct is trivially constructible and empty.</li></ul><h4 id="key-members-3" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-3" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>using type = std::remove_cvref_t&lt;T&gt;</code></li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to obtain a canonical type from possibly qualified types.</li><li>Likely used in type traits or metaprogramming contexts within the namespace.</li></ul><h3 id="clore-net-openai-schema-detail-schema-subject-t" tabindex="-1"><code>clore::net::openai::schema::detail::schema_subject_t</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-schema-subject-t" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::schema_subject_t`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:95</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The type alias <code>schema_subject_t</code> resolves to <code>typename schema_subject&lt;T&gt;::type</code>, providing a shorthand for accessing the nested <code>type</code> member of the <code>schema_subject</code> class template. Its definition is entirely dependent on the primary template or any partial/explicit specializations of <code>schema_subject&lt;T&gt;</code>, making it a compile-time alias whose concrete type is determined by the argument <code>T</code>. Internally, <code>schema_subject&lt;T&gt;</code> is expected to define a <code>type</code> member; the alias exists solely to simplify usage in template metaprogramming contexts within the detail namespace. As a dependent type alias, it carries the invariant that <code>schema_subject&lt;T&gt;::type</code> must be a valid type for the alias to be well‑formed; no additional constraints or implementations are introduced by the alias itself.</p><h4 id="invariants-4" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-4" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>The alias is only valid for types <code>T</code> for which <code>schema_subject&lt;T&gt;</code> has a visible specialization defining a <code>type</code> member.</li><li>The resolved type must be a valid type; otherwise, compilation fails.</li></ul><h4 id="key-members-4" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-4" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>schema_subject&lt;T&gt;</code> trait class</li><li><code>::type</code> nested type alias</li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to obtain the subject type of a schema type without requiring <code>typename</code>.</li><li>Likely used in template metaprogramming to constrain or transform types based on schema definitions.</li></ul><h3 id="clore-net-openai-schema-detail-vector-inner" tabindex="-1"><code>clore::net::openai::schema::detail::vector_inner</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-vector-inner" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::vector_inner`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:52</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The template struct <code>clore::net::openai::schema::detail::vector_inner</code> is an implementation detail within the <code>OpenAI</code> schema representation. It is parameterized by type <code>T</code> and serves as an internal container or marker for vector‑like schema types. The struct’s exact member layout is not publicly exposed; it is intended solely to support schema generation for arrays or sequences where the element type is <code>T</code>.</p><h2 id="variables" tabindex="-1">Variables <a class="header-anchor" href="#variables" aria-label="Permalink to &quot;Variables&quot;">​</a></h2><h3 id="clore-net-openai-schema-detail-is-array-v" tabindex="-1"><code>clore::net::openai::schema::detail::is_array_v</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-is-array-v" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::is_array_v`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:69</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The variable is a <code>constexpr bool</code> that is evaluated at compile time, providing a type trait for array detection. It is read in template metaprogramming contexts to conditionally select or generate schema representations. No mutation is observed because it is defined as <code>constexpr</code>.</p><h4 id="mutation" tabindex="-1">Mutation <a class="header-anchor" href="#mutation" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as a compile-time type trait to check if a type is an array</li><li>Referenced in template metaprogramming for schema generation logic</li></ul><h3 id="clore-net-openai-schema-detail-is-optional-v" tabindex="-1"><code>clore::net::openai::schema::detail::is_optional_v</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-is-optional-v" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::is_optional_v`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:29</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>As a <code>constexpr</code> template variable, <code>is_optional_v</code> is evaluated at compile time and participates in template metaprogramming to conditionally enable or disable code paths based on whether <code>T</code> is an optional type. It is read by the compiler during template instantiation, but no explicit read or usage sites are shown in the provided evidence.</p><h4 id="mutation-1" tabindex="-1">Mutation <a class="header-anchor" href="#mutation-1" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h3 id="clore-net-openai-schema-detail-is-vector-v" tabindex="-1"><code>clore::net::openai::schema::detail::is_vector_v</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-is-vector-v" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::is_vector_v`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:49</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>As a compile-time constant, <code>is_vector_v</code> participates in template metaprogramming to conditionally select schema generation logic for vector types. It is read via template instantiation but not mutated.</p><h4 id="mutation-2" tabindex="-1">Mutation <a class="header-anchor" href="#mutation-2" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h4 id="usage-patterns-6" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-6" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>type trait detection</li><li>compile-time conditional branching</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-net-detail-validate-response-format" tabindex="-1"><code>clore::net::detail::validate_response_format</code> <a class="header-anchor" href="#clore-net-detail-validate-response-format" aria-label="Permalink to &quot;`clore::net::detail::validate_response_format`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:527</code></p><p>Definition: <code>network/schema.cppm:535</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>The function <code>clore::net::detail::validate_response_format</code> implements a two‑step validation of a <code>ResponseFormat</code> object. It first performs trivial pre‑checks: if <code>format.schema</code> lacks a value, it returns an empty success; if <code>format.name</code> is empty, it returns a <code>std::unexpected</code> with an appropriate <code>LLMError</code>. Only after these checks pass does it delegate the actual schema compliance verification to <code>openai::schema::detail::validate_openai_schema</code>, passing the dereferenced <code>format.schema</code>, the <code>format.name</code> as the schema path, and <code>true</code> for the <code>is_root</code> flag. This delegation hands control to a recursive validator that traverses the JSON schema, resolves type markers, and enforces <code>OpenAI</code> Schema constraints. The outcome is a <code>std::expected&lt;void, LLMError&gt;</code> that signals either successful validation or a structured error containing the first violation encountered.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>format.schema</code></li><li><code>format.name</code></li></ul><h4 id="usage-patterns-7" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-7" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to validate response format parameters</li><li>Used before constructing a completion request</li></ul><h3 id="clore-net-detail-validate-tool-definition" tabindex="-1"><code>clore::net::detail::validate_tool_definition</code> <a class="header-anchor" href="#clore-net-detail-validate-tool-definition" aria-label="Permalink to &quot;`clore::net::detail::validate_tool_definition`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:529</code></p><p>Definition: <code>network/schema.cppm:545</code></p><p>Declaration: <a href="./../../namespaces/clore/net/detail/"><code>Namespace clore::net::detail</code></a></p><p>The function first performs lightweight validation of the tool’s metadata: if <code>tool.name</code> is empty, it returns an <code>LLMError</code> indicating that the tool name must not be empty. If <code>tool.name</code> is present but <code>tool.description</code> is empty, it returns a formatted <code>LLMError</code> specifying which tool lacks a description. After these checks pass, the core validation is delegated to <code>openai::schema::detail::validate_openai_schema</code>, passing <code>tool.parameters</code>, the tool’s name as a path prefix, and the boolean <code>true</code> to indicate that this schema is a root definition. The return value of that call becomes the return value of the function. All non‑local dependencies are limited to the <code>openai::schema::detail</code> validation machinery and the <code>LLMError</code> type.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>tool<code>.name</code></li><li>tool<code>.description</code></li><li>tool<code>.parameters</code></li></ul><h4 id="usage-patterns-8" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-8" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during tool registration or schema validation to ensure tool definitions are well-formed.</li></ul><h3 id="clore-net-openai-schema-detail-make-any-of-schema" tabindex="-1"><code>clore::net::openai::schema::detail::make_any_of_schema</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-make-any-of-schema" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::make_any_of_schema`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:156</code></p><p>Definition: <code>network/schema.cppm:156</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The function <code>clore::net::openai::schema::detail::make_any_of_schema</code> assembles a JSON Schema <code>anyOf</code> combinator from a vector of pre‑built JSON schema values. It first attempts to create an empty JSON object via <code>clore::net::detail::make_empty_object</code>. If that operation fails, the error is forwarded immediately through <code>std::unexpected</code>. The same pattern is repeated for an empty JSON array via <code>clore::net::detail::make_empty_array</code>. After both containers are successfully created, the function moves each element of the <code>choices</code> vector into the array, then inserts the completed array into the object under the key <code>&quot;anyOf&quot;</code>. Finally, the object is wrapped in a <code>json::Value</code> and returned. No schema validation or type‑specific logic is performed here—the function solely handles ownership transfer and structural composition, relying on the standard library and the <code>clore::net::detail</code> utility functions for error‑aware resource creation.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Allocates memory for JSON object and array structures</li><li>Transfers ownership of elements from <code>choices</code> parameter into the created JSON array</li></ul><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>Parameter <code>choices</code> (elements are moved)</li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Returned <code>json::Value</code> containing the constructed schema</li><li>Modified <code>choices</code> vector (elements are moved out)</li></ul><h4 id="usage-patterns-9" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-9" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to wrap multiple schema alternatives into a single <code>anyOf</code> schema</li><li>Used in template metaprogramming for generating union type schemas</li></ul><h3 id="clore-net-openai-schema-detail-make-scalar-type-schema" tabindex="-1"><code>clore::net::openai::schema::detail::make_scalar_type_schema</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-make-scalar-type-schema" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::make_scalar_type_schema`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:146</code></p><p>Definition: <code>network/schema.cppm:146</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The function <code>clore::net::openai::schema::detail::make_scalar_type_schema</code> begins by calling <code>clore::net::detail::make_empty_object</code> to allocate a new JSON object. If this allocation fails, the function immediately returns <code>std::unexpected</code> with the error from <code>make_empty_object</code>. Upon success, it inserts a <code>&quot;type&quot;</code> key into the object, setting its value to the provided <code>type_name</code> string (converted to <code>std::string</code>). The constructed object is then wrapped in a <code>json::Value</code> and returned as the function’s result. The implementation is straightforward and type‑agnostic; its only dependency is the <code>make_empty_object</code> helper, which supplies the initial empty JSON object and any associated error handling.</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>allocates a new <code>json::Object</code></li><li>inserts a key-value pair into the object</li><li>moves ownership of the object into a <code>json::Value</code></li></ul><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>type_name</code> parameter</li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>the returned <code>std::expected&lt;json::Value, LLMError&gt;</code></li><li>the internal <code>json::Object</code> created and returned</li></ul><h4 id="usage-patterns-10" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-10" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used to generate schema entries for scalar types</li><li>called when the element being schematized is a primitive</li></ul><h3 id="clore-net-openai-schema-detail-make-schema-object" tabindex="-1"><code>clore::net::openai::schema::detail::make_schema_object</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-make-schema-object" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::make_schema_object`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:132</code></p><p>Definition: <code>network/schema.cppm:132</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The function first delegates to <code>clore::net::openai::schema::detail::make_schema_value&lt;T&gt;</code> to generate the complete JSON schema representation for the type <code>T</code>. If that call fails, the error is immediately forwarded via <code>std::unexpected</code>. Otherwise, the resulting <code>json::Value</code> is inspected to ensure it contains a <code>json::Object</code>; if the top‑level value is not an object (e.g., it is an array or primitive), the function returns a <code>LLMError</code> indicating the generated schema root is not an object. On success, a copy of that object is returned as a <code>std::expected&lt;json::Object, LLMError&gt;</code>.</p><p>The algorithm is a thin wrapper that validates the shape of the schema produced by the internal schema‑generation machinery. Its control flow consists solely of two error‑handling steps: propagation of errors from <code>make_schema_value</code> and a null‑check on the extracted object pointer. The primary dependencies are the template‑level helper <code>make_schema_value</code> and the <code>json::Object</code> and <code>LLMError</code> types from the library.</p><h4 id="side-effects-4" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-4" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-4" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-4" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>make_schema_value&lt;T&gt;()</code> return value</li><li><code>json::Value::get_object()</code> method</li></ul><h4 id="usage-patterns-11" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-11" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to generate the top-level JSON schema object for a type <code>T</code></li><li>Used in the schema creation pipeline, often after validation</li></ul><h3 id="clore-net-openai-schema-detail-make-schema-value" tabindex="-1"><code>clore::net::openai::schema::detail::make_schema_value</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-make-schema-value" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::make_schema_value`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:129</code></p><p>Definition: <code>network/schema.cppm:225</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The function <code>make_schema_value</code> is a template that generates a JSON Schema representation for a given C++ type <code>T</code>, returning an <code>std::expected&lt;json::Value, LLMError&gt;</code>. Its internal control flow uses compile-time <code>if constexpr</code> branching based on the deduced <code>schema_subject_t&lt;T&gt;</code> type.</p><p>For scalar types (<code>std::string</code>, <code>bool</code>, <code>integral</code>, <code>floating_point</code>), it delegates to <code>make_scalar_type_schema</code> with the appropriate JSON type string. For <code>std::optional</code>, it recursively invokes <code>make_schema_value</code> on the inner type and combines the result with a <code>&quot;null&quot;</code> schema via <code>make_any_of_schema</code>. Containers (<code>std::vector</code>, <code>std::array</code>) are handled by first generating the item schema recursively, then constructing an object with <code>&quot;type&quot;: &quot;array&quot;</code> and an <code>&quot;items&quot;</code> field; fixed‑size arrays additionally add <code>&quot;minItems&quot;</code> and <code>&quot;maxItems&quot;</code>. For reflectable classes, it creates an empty JSON object and calls <code>populate_object_schema</code> to fill properties. Every step propagates errors from helper functions, ensuring the whole operation either produces a valid schema or an error.</p><h4 id="side-effects-5" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-5" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>allocates heap memory for JSON objects and values</li><li>moves ownership of allocated JSON data</li></ul><h4 id="usage-patterns-12" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-12" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>main entry for generating JSON schema from a type</li><li>recursively called for nested types inside optional, vector, array</li><li>used in higher-level schema generation functions</li></ul><h3 id="clore-net-openai-schema-detail-populate-object-schema" tabindex="-1"><code>clore::net::openai::schema::detail::populate_object_schema</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-populate-object-schema" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::populate_object_schema`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:173</code></p><p>Definition: <code>network/schema.cppm:173</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The function begins by performing a compile-time validation of the field schema using <code>meta_attrs::validate_field_schema&lt;Object&gt;()</code>; a failed assertion halts compilation. It then allocates an empty JSON object for <code>properties</code> and an empty array for <code>required</code> via <code>clore::net::detail::make_empty_object</code> and <code>clore::net::detail::make_empty_array</code>, respectively, returning an error on failure.</p><p>A local lambda <code>append_field</code> is defined to process each field index. For a given index constant, it calls <code>meta_attrs::resolve_field&lt;Object, index&gt;</code> to obtain the field’s schema attributes. If the field is marked <code>is_skipped</code>, it returns success immediately; if <code>is_flattened</code>, it returns an error because flattening is unsupported. Otherwise, it deduces the field type via <code>meta::field_type&lt;Object, index&gt;</code> and invokes <code>make_schema_value&lt;field_type&gt;()</code> to generate a JSON sub-schema. On success, the sub-schema is inserted into <code>properties</code> under the field’s <code>canonical_name</code>, and that name is appended to the <code>required</code> array.</p><p>The indices from the template parameter pack are expanded into an <code>std::array</code> of expected results by calling <code>append_field</code> for each index. The function then iterates over the array, returning early with an error if any element is invalid. Finally, the top-level <code>object</code> is populated with the <code>type</code> value <code>&quot;object&quot;</code>, the completed <code>properties</code> and <code>required</code> JSON values, and <code>&quot;additionalProperties&quot;</code> set to <code>false</code>. Dependencies include <code>meta_attrs</code> for field introspection, <code>meta::field_type</code>, <code>make_schema_value</code> for recursive schema generation, and the utility functions for creating empty JSON containers.</p><h4 id="side-effects-6" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-6" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>mutates the <code>json::Object</code> argument by inserting schema keys</li><li>allocates JSON objects and arrays via <code>make_empty_object</code> and <code>make_empty_array</code></li><li>potentially creates <code>LLMError</code> values on failure (returned as unexpected)</li></ul><h4 id="reads-from-5" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-5" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>compile-time field metadata from <code>meta_attrs::resolve_field&lt;Object, index&gt;()</code></li><li>template parameter <code>Object</code> for field types and names</li><li>parameter pack <code>Indices</code> for field indices</li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>the <code>json::Object&amp; object</code> parameter (inserts <code>&quot;type&quot;</code>, <code>&quot;properties&quot;</code>, <code>&quot;required&quot;</code>, <code>&quot;additionalProperties&quot;</code> and their values)</li></ul><h4 id="usage-patterns-13" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-13" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during automatic <code>OpenAI</code> schema generation for structured tool calls</li><li>used with a compile-time <code>std::index_sequence</code> from the object&#39;s field count</li></ul><h3 id="clore-net-openai-schema-detail-sanitize-schema-name" tabindex="-1"><code>clore::net::openai::schema::detail::sanitize_schema_name</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-sanitize-schema-name" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::sanitize_schema_name`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:97</code></p><p>Definition: <code>network/schema.cppm:97</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The implementation of <code>clore::net::openai::schema::detail::sanitize_schema_name</code> performs a simple character-by-character transformation on the input string view <code>raw_name</code>. It first reserves space for the output <code>sanitized</code> string to avoid reallocation, then iterates over each character <code>ch</code>. Each character is cast to <code>unsigned char</code> and checked against ASCII ranges for uppercase letters, lowercase letters, and digits. If the character falls within any of these ranges, it is appended unchanged; otherwise, an underscore is appended in its place. After the loop, any leading underscores are removed by erasing from the front, and any trailing underscores are removed by popping from the back. The final <code>sanitized</code> string is returned.</p><p>The algorithm is purely local and does not rely on any other functions or types from the codebase—it uses only standard library string operations (<code>std::string::reserve</code>, <code>push_back</code>, <code>erase</code>, <code>pop_back</code>). The control flow is a single <code>for</code> loop followed by two <code>while</code> loops for trimming. No external dependencies or validation logic are involved; the function’s purpose is to produce a valid identifier-like name from arbitrary input.</p><h4 id="side-effects-7" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-7" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-6" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-6" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>raw_name</code> parameter</li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>return value (<code>std::string</code>)</li></ul><h4 id="usage-patterns-14" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-14" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Sanitizing schema names for identifier generation</li><li>Converting arbitrary input strings to valid identifiers</li></ul><h3 id="clore-net-openai-schema-detail-schema-type-name" tabindex="-1"><code>clore::net::openai::schema::detail::schema_type_name</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-schema-type-name" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::schema_type_name`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:120</code></p><p>Definition: <code>network/schema.cppm:120</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The function <code>clore::net::openai::schema::detail::schema_type_name</code> generates a sanitized string name for a given type <code>T</code>. It first retrieves the raw, implementation-defined type name via <code>meta::type_name&lt;T&gt;()</code> and then passes it to <code>clore::net::openai::schema::detail::sanitize_schema_name</code>. If the sanitized result is an empty string, the function returns an error wrapped in <code>std::unexpected</code>. Otherwise, it returns the sanitized name. The implementation relies crucially on the reflection-like <code>meta::type_name&lt;T&gt;()</code> to obtain a string representation and on the name‑cleaning logic in <code>sanitize_schema_name</code> to produce a valid schema identifier.</p><h4 id="side-effects-8" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-8" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-7" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-7" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>template parameter <code>T</code> via <code>meta::type_name&lt;T&gt;()</code></li><li>string returned by <code>sanitize_schema_name</code></li></ul><h4 id="usage-patterns-15" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-15" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used by other schema detail functions to derive <code>OpenAPI</code> type names from C++ types</li><li>invoked in <code>make_scalar_type_schema</code> and similar conversion functions</li></ul><h3 id="clore-net-openai-schema-detail-validate-openai-schema" tabindex="-1"><code>clore::net::openai::schema::detail::validate_openai_schema</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-validate-openai-schema" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::validate_openai_schema`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:328</code></p><p>Definition: <code>network/schema.cppm:373</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The function first checks if the input Object contains an <code>anyOf</code> field. If so, it rejects any root-level schema and otherwise validates each entry in the <code>anyOf</code> array by recursively calling <code>validate_openai_schema_value</code>. After that, it reads the <code>type</code> field, which may be a string or an array of strings; the first non-null string becomes the effective schema type. The root schema must have type <code>&quot;object&quot;</code>. For an object schema, the function requires <code>properties</code>, <code>required</code>, and <code>additionalProperties</code> (set to <code>false</code>), then calls <code>validate_required_properties</code> to ensure every required key exists in the properties object, and recurses into each property value. For an array schema, it validates the <code>items</code> field. Finally, if a <code>$defs</code> entry is present, the function recurses into each definition. All structural validation errors produce an <code>LLMError</code> via <code>std::unexpected</code>, and the function relies on <code>clore::net::detail::ObjectView</code>, <code>clore::net::detail::expect_array</code>, and <code>clore::net::detail::expect_object</code> for safe JSON traversal.</p><h4 id="side-effects-9" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-9" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-8" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-8" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>object</code> (const <code>json::Object</code>&amp;)</li><li>parameter <code>path</code> (<code>std::string_view</code>)</li><li>parameter <code>is_root</code> (bool)</li><li>JSON object fields: <code>anyOf</code>, <code>type</code>, <code>properties</code>, <code>required</code>, <code>additionalProperties</code>, <code>items</code>, <code>$defs</code> via <code>ObjectView::get</code></li></ul><h4 id="usage-patterns-16" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-16" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called to validate a schema before registration or API call</li><li>used in schema generation pipeline to ensure compliance</li></ul><h3 id="clore-net-openai-schema-detail-validate-openai-schema-value" tabindex="-1"><code>clore::net::openai::schema::detail::validate_openai_schema_value</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-validate-openai-schema-value" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::validate_openai_schema_value`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:331</code></p><p>Definition: <code>network/schema.cppm:331</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The implementation delegates the actual validation to <code>clore::net::openai::schema::detail::validate_openai_schema</code> after first unwrapping a JSON object from the given <code>json::Value</code>. It uses <code>clore::net::detail::expect_object</code> to extract an <code>json::Object</code> reference; if that extraction fails, it immediately returns the error from <code>expect_object</code>. Otherwise, it dereferences the returned optional and passes the underlying object, along with the original <code>path</code> and <code>is_root</code> flag, into the core schema validation routine. This function therefore serves as a thin adapter that normalises an arbitrary JSON value into the object‑typed expected by the downstream <code>validate_openai_schema</code>.</p><h4 id="side-effects-10" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-10" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-9" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-9" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>const <code>json::Value</code>&amp; value</li><li><code>std::string_view</code> path</li><li>bool <code>is_root</code></li></ul><h4 id="usage-patterns-17" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-17" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to validate a schema represented as a JSON value</li><li>Used when the input is not already a known object reference</li><li>Part of the validation pipeline for <code>OpenAI</code> schema endpoints</li></ul><h3 id="clore-net-openai-schema-detail-validate-openai-schema-value-1" tabindex="-1"><code>clore::net::openai::schema::detail::validate_openai_schema_value</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-validate-openai-schema-value-1" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::validate_openai_schema_value`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:340</code></p><p>Definition: <code>network/schema.cppm:340</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The function first invokes <code>clore::net::detail::expect_object</code> on the provided <code>json::Cursor</code> to obtain an expected <code>json::Object</code>. If that extraction fails, it immediately returns the resulting <code>LLMError</code> by moving the error out of the <code>std::expected</code>. On success, it dereferences the object and delegates the actual validation to <code>clore::net::openai::schema::detail::validate_openai_schema</code>, passing the extracted <code>json::Object</code> along with the <code>path</code> and <code>is_root</code> parameters, and forwarding whatever <code>std::expected&lt;void, LLMError&gt;</code> that call returns.</p><h4 id="side-effects-11" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-11" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-10" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-10" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>value (<code>json::Cursor</code>)</li><li>path (<code>std::string_view</code>)</li><li><code>is_root</code> (bool)</li></ul><h4 id="usage-patterns-18" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-18" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to validate a JSON value against <code>OpenAI</code> schema starting from a cursor</li><li>Called internally during schema validation pipeline</li></ul><h3 id="clore-net-openai-schema-detail-validate-required-properties" tabindex="-1"><code>clore::net::openai::schema::detail::validate_required_properties</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-validate-required-properties" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::validate_required_properties`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:349</code></p><p>Definition: <code>network/schema.cppm:349</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The function processes two views — <code>properties</code> as an <code>ObjectView</code> and <code>required</code> as an <code>ArrayView</code> — alongside the error-context string <code>path</code>. It first builds a <code>std::unordered_set&lt;std::string&gt;</code> named <code>required_names</code> by iterating over each element of <code>required</code>; each element is expected to be a string (via <code>clore::net::detail::expect_string</code>), and extraction failure causes an early return with the resulting <code>LLMError</code>. After populating the set, the function iterates over each entry in <code>properties</code>. For every property key (<code>entry.key</code>), it checks whether that key is present in <code>required_names</code>. If any key is missing, it returns an <code>LLMError</code> indicating that the property must be listed in <code>required</code> when using strict structured output. If all property names are covered, the function returns a success (an empty <code>std::expected&lt;void, LLMError&gt;</code>). The validation ensures exact correspondence between the properties defined in the schema object and the names declared in its <code>required</code> array, a constraint enforced for strict mode.</p><h4 id="side-effects-12" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-12" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-11" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-11" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>properties</code> parameter (<code>clore::net::detail::ObjectView</code>)</li><li><code>required</code> parameter (<code>clore::net::detail::ArrayView</code>)</li><li><code>path</code> parameter (<code>std::string_view</code>)</li></ul><h4 id="usage-patterns-19" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-19" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used in <code>OpenAI</code> schema validation pipeline</li><li>Called to enforce required property lists in structured output schemas</li></ul><h3 id="clore-net-openai-schema-detail-validate-schema-array-of-types" tabindex="-1"><code>clore::net::openai::schema::detail::validate_schema_array_of_types</code> <a class="header-anchor" href="#clore-net-openai-schema-detail-validate-schema-array-of-types" aria-label="Permalink to &quot;`clore::net::openai::schema::detail::validate_schema_array_of_types`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:295</code></p><p>Definition: <code>network/schema.cppm:295</code></p><p>Declaration: <a href="./../../namespaces/clore/net/openai/schema/detail/"><code>Namespace clore::net::openai::schema::detail</code></a></p><p>The function iterates over each item in the input <code>json::Array</code> using a range-based <code>for</code> loop. For each value, it calls <code>clore::net::detail::expect_string</code> to obtain a validated string representation of a type name, returning an error immediately if that fails. If the type is the literal <code>&quot;null&quot;</code>, it sets a local <code>saw_null</code> flag and skips to the next element. Otherwise, if a <code>primary_type</code> has already been recorded (i.e., a non‑null type was seen earlier), the function returns an error because only one concrete non‑null type is permitted in the union. If <code>is_root</code> is <code>true</code>, any array‑of‑types is rejected outright since the root schema must be an object and cannot be nullable. After the loop, the function checks that exactly one concrete type was found and that <code>saw_null</code> is <code>true</code>; if either condition fails, an error is returned. On success, the function returns an empty expected result. The algorithm depends on <code>expect_string</code> for value extraction, <code>LLMError</code> for error representation, and <code>std::optional</code> to track the single allowed non‑null type.</p><h4 id="side-effects-13" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-13" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-12" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-12" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>array</code> parameter</li><li><code>path</code> parameter</li><li><code>is_root</code> parameter</li><li>elements of <code>array</code></li></ul><h4 id="usage-patterns-20" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-20" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called from <code>validate_openai_schema</code> when handling an array type</li><li>used to validate type union constraints in schema definitions</li></ul><h3 id="clore-net-schema-function-tool" tabindex="-1"><code>clore::net::schema::function_tool</code> <a class="header-anchor" href="#clore-net-schema-function-tool" aria-label="Permalink to &quot;`clore::net::schema::function_tool`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:520</code></p><p>Definition: <code>network/schema.cppm:584</code></p><p>Declaration: <a href="./../../namespaces/clore/net/schema/"><code>Namespace clore::net::schema</code></a></p><p>The implementation begins by deducing the root type via <code>openai::schema::detail::schema_subject_t&lt;T&gt;</code> and verifying at compile time that it satisfies <code>kota::meta::reflectable_class</code>, ensuring automatic schema generation is possible. It then validates the supplied <code>name</code> and <code>description</code> strings, returning a <code>std::unexpected</code> with an <code>LLMError</code> message if either is empty. The core schema construction is delegated to <code>openai::schema::detail::make_schema_object&lt;root_type&gt;()</code>; if this call fails, the error is forwarded directly. On success, a <code>FunctionToolDefinition</code> is assembled, setting the <code>name</code>, <code>description</code>, and the generated <code>parameters</code> object, with <code>strict</code> unconditionally set to <code>true</code>.</p><h4 id="side-effects-14" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-14" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>allocates memory for the returned <code>FunctionToolDefinition</code> and its string members</li><li>moves input strings <code>name</code> and <code>description</code></li><li>calls <code>openai::schema::detail::make_schema_object</code> which may allocate and construct a schema object</li></ul><h4 id="reads-from-13" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-13" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>function parameter <code>name</code></li><li>function parameter <code>description</code></li><li>template parameter <code>T</code> (via <code>root_type</code>)</li><li>result of <code>openai::schema::detail::make_schema_object&lt;root_type&gt;()</code></li></ul><h4 id="writes-to-4" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-4" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>returns a new <code>FunctionToolDefinition</code> object by value (ownership transferred to caller)</li></ul><h4 id="usage-patterns-21" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-21" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used to create function tool definitions for LLM calls with automatic schema generation</li><li>typically called with a reflectable type as the template argument</li></ul><h3 id="clore-net-schema-response-format" tabindex="-1"><code>clore::net::schema::response_format</code> <a class="header-anchor" href="#clore-net-schema-response-format" aria-label="Permalink to &quot;`clore::net::schema::response_format`&quot;">​</a></h3><p>Declaration: <code>network/schema.cppm:517</code></p><p>Definition: <code>network/schema.cppm:561</code></p><p>Declaration: <a href="./../../namespaces/clore/net/schema/"><code>Namespace clore::net::schema</code></a></p><p>The function first resolves the root schema subject type via <code>clore::net::openai::schema::detail::schema_subject_t&lt;T&gt;</code> and statically asserts that it is a reflectable class. It then retrieves a sanitized schema name by calling <code>clore::net::openai::schema::detail::schema_type_name&lt;root_type&gt;()</code>. If that call fails, the function returns an error early. Otherwise, it proceeds to construct the complete JSON schema object by invoking <code>clore::net::openai::schema::detail::make_schema_object&lt;root_type&gt;()</code>. This internal function is responsible for recursively building the schema, validating property types, handling optional and array fields, and populating object properties. If schema construction fails, an error is returned; otherwise, the result is assembled into a <code>ResponseFormat</code> structure with the obtained name, the generated schema, and <code>strict</code> set to <code>true</code>.</p><p>The main dependencies are the helper templates and functions within <code>clore::net::openai::schema::detail</code>, including <code>schema_subject_t</code>, <code>schema_type_name</code>, and <code>make_schema_object</code>. These rely on compile‑time reflection (<code>kota::meta::reflectable_class</code>) and the JSON value types (<code>json::Value</code>, <code>json::Object</code>, <code>json::Array</code>) used to represent the <code>OpenAI</code> schema structure. The function does not perform direct validation of the schema content but delegates that to the internal helpers; the overall flow is a linear sequence of type‑name extraction, schema object construction, and result packaging with error propagation at each step.</p><h4 id="side-effects-15" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-15" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-14" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-14" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>template parameter <code>T</code></li><li>static reflection metadata for <code>T</code></li></ul><h4 id="writes-to-5" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-5" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>return value (new <code>ResponseFormat</code> object)</li></ul><h4 id="usage-patterns-22" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-22" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used to obtain a structured output schema for LLM API calls requiring a <code>ResponseFormat</code></li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>schema</code> module is an internal component within the <code>clore::net</code> library responsible for generating and validating <code>OpenAI</code>‑compatible JSON Schema representations from C++ types. It depends on the <code>http</code>, <code>protocol</code>, <code>support</code>, and standard library modules, and is organized into two primary namespaces: <code>clore::net::openai::schema</code> for <code>OpenAI</code>‑specific schema construction (including a <code>detail</code> sub‑namespace) and <code>clore::net::schema</code> for higher‑level integration that produces response formats and function tool definitions. Internally, the module is layered with a compile‑time metaprogramming layer (traits like <code>is_vector</code>, <code>is_optional</code>, <code>is_array</code>, and unwrapping helpers <code>vector_inner</code>, <code>optional_inner</code>, <code>array_inner</code>) that strips container wrappers to identify the core subject type via <code>schema_subject</code> / <code>schema_subject_t</code>, a schema object construction layer (functions such as <code>make_schema_object</code>, <code>make_scalar_type_schema</code>, <code>make_any_of_schema</code>, <code>populate_object_schema</code>) that builds JSON schema structures, and a validation layer (<code>validate_openai_schema</code>, <code>validate_openai_schema_value</code>, <code>validate_required_properties</code>, <code>validate_schema_array_of_types</code>) that checks conformance. Sanitization utilities (<code>sanitize_schema_name</code>) ensure names are safe for schema identifiers. This layered design keeps type introspection, schema generation, and validation concerns separated, while the public entry points <code>response_format</code> and <code>function_tool</code> provide the module’s primary API for integrating with the rest of the networking stack.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../http/">Module http</a></li><li><a href="./../protocol/">Module protocol</a></li><li><a href="./../support/">Module support</a></li></ul>',287))])}const q=c(p,[["render",m]]);export{_ as __pageData,q as default};
