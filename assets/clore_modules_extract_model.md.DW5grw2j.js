import{_ as a,o,c as t,a3 as i}from"./chunks/framework.aYYe0dfI.js";const m=JSON.parse('{"title":"Module extract:model","description":"The extract:model module defines the core data structures and query functions that represent the extracted structure of a C++ project. It provides a unified model encompassing symbols (SymbolInfo, SymbolID, SymbolKind), source locations and ranges (SourceLocation, SourceRange), module units (ModuleUnit), file metadata (FileInfo), namespace hierarchies (NamespaceInfo), and the top-level project container (ProjectModel). These types capture declaration/definition positions, symbol relationships (parent, children, references, calls), qualified name decomposition, and on‑demand source‑snippet resolution.","frontmatter":{"title":"Module extract:model","description":"The extract:model module defines the core data structures and query functions that represent the extracted structure of a C++ project. It provides a unified model encompassing symbols (SymbolInfo, SymbolID, SymbolKind), source locations and ranges (SourceLocation, SourceRange), module units (ModuleUnit), file metadata (FileInfo), namespace hierarchies (NamespaceInfo), and the top-level project container (ProjectModel). These types capture declaration/definition positions, symbol relationships (parent, children, references, calls), qualified name decomposition, and on‑demand source‑snippet resolution.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/extract/model.md","filePath":"en/clore/modules/extract/model.md"}'),c={name:"clore/modules/extract/model.md"};function r(d,e,l,s,n,p){return o(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="module-extract-model" tabindex="-1">Module <code>extract:model</code> <a class="header-anchor" href="#module-extract-model" aria-label="Permalink to &quot;Module `extract:model`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>extract:model</code> module defines the core data structures and query functions that represent the extracted structure of a C++ project. It provides a unified model encompassing symbols (<code>SymbolInfo</code>, <code>SymbolID</code>, <code>SymbolKind</code>), source locations and ranges (<code>SourceLocation</code>, <code>SourceRange</code>), module units (<code>ModuleUnit</code>), file metadata (<code>FileInfo</code>), namespace hierarchies (<code>NamespaceInfo</code>), and the top-level project container (<code>ProjectModel</code>). These types capture declaration/definition positions, symbol relationships (parent, children, references, calls), qualified name decomposition, and on‑demand source‑snippet resolution.</p><p>The public interface includes lookup functions to retrieve symbols by ID or qualified name, locate modules by name or source file, resolve source snippets from disk, decompose and join qualified names, and obtain a stable integer representation for symbol kind names. Helper utilities such as <code>SymbolID</code> comparison and validity checks, <code>SourceLocation::is_known</code>, and caching mechanisms for qualified‑name splitting are also exposed. Collectively, the module owns the abstractions needed to store, navigate, and query the complete extraction result.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./ast"><code>extract:ast</code></a></li><li><a href="./merge"><code>extract:merge</code></a></li></ul><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-extract-fileinfo" tabindex="-1"><code>clore::extract::FileInfo</code> <a class="header-anchor" href="#clore-extract-fileinfo" aria-label="Permalink to &quot;`clore::extract::FileInfo`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:122</code></p><p>Definition: <code>extract/model.cppm:122</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct <code>clore::extract::FileInfo</code> is a plain aggregate type that bundles the core data produced by the extraction phase for a single source file. Its three public fields — <code>path</code>, <code>symbols</code>, and <code>includes</code> — hold the file’s location string, a vector of <code>SymbolID</code> values representing the symbols found in that file, and a vector of include path strings. Because <code>clore::extract::FileInfo</code> has no user‑defined constructors, assignment <code>operator</code>s, or access controls, it behaves as a simple value type that can be initialized using aggregate initialization or designated initializers.</p><p>No invariants are enforced by the struct beyond those inherited from its member types (e.g., <code>std::string</code> and <code>std::vector</code> are always in a valid state). The <code>path</code> field is expected to contain a valid filesystem path after extraction, and the <code>symbols</code> and <code>includes</code> vectors may be empty if no symbols or includes were encountered. All fields are directly writable, so downstream code can freely modify the data once the struct is populated.</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>path</code> should be a valid filesystem path</li><li><code>symbols</code> may be empty</li><li><code>includes</code> may be empty</li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>path</code></li><li><code>symbols</code></li><li><code>includes</code></li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as output of extraction to represent a translation unit</li><li>Consumed by downstream processing that expects symbol and include lists</li></ul><h3 id="clore-extract-moduleunit" tabindex="-1"><code>clore::extract::ModuleUnit</code> <a class="header-anchor" href="#clore-extract-moduleunit" aria-label="Permalink to &quot;`clore::extract::ModuleUnit`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:135</code></p><p>Definition: <code>extract/model.cppm:135</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct <code>clore::extract::ModuleUnit</code> is an aggregate data type that holds the extracted information for a single C++20 module unit, whether an interface unit or a partition. Its fields record the module&#39;s fully qualified <code>name</code> (e.g., <code>&quot;foo&quot;</code> or <code>&quot;foo:bar&quot;</code>), a boolean <code>is_interface</code> that distinguishes exported (<code>export module</code>) from non‑exported (<code>module</code>) units, and a normalized <code>source_file</code> path. The <code>imports</code> vector lists the names of modules imported by this unit, while the <code>symbols</code> vector stores the identifiers of all symbols declared within it. The structure imposes no invariants beyond standard well‑formedness: <code>name</code> should be non‑empty, and <code>symbols</code> may be empty if no declarations were encountered.</p><h4 id="invariants-1" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-1" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>is_interface</code> is true for export module declarations</li><li><code>name</code> is the fully qualified module name</li><li><code>source_file</code> is a normalized file path</li><li><code>imports</code> lists all module imports</li><li><code>symbols</code> contains all symbol <code>IDs</code> declared in this unit</li></ul><h4 id="key-members-1" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-1" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>name</code></li><li><code>is_interface</code></li><li><code>source_file</code></li><li><code>imports</code></li><li><code>symbols</code></li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Populated by a module parser or extractor</li><li>Gathered into a collection to represent an entire module&#39;s translation units</li><li>Iterated over to analyze or serialize module structure</li></ul><h3 id="clore-extract-namespaceinfo" tabindex="-1"><code>clore::extract::NamespaceInfo</code> <a class="header-anchor" href="#clore-extract-namespaceinfo" aria-label="Permalink to &quot;`clore::extract::NamespaceInfo`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:128</code></p><p>Definition: <code>extract/model.cppm:128</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct <code>clore::extract::NamespaceInfo</code> is an aggregate data structure that captures the extracted information for a single namespace scope. Its member <code>name</code> stores the unqualified namespace name, while <code>children</code> holds a list of direct child namespace names (as strings) that appear within this namespace. All symbol definitions directly belonging to this namespace are collected in <code>symbols</code>, a vector of <code>SymbolID</code> values. The order of elements in <code>symbols</code> and <code>children</code> reflects the order of declaration or discovery during extraction, but no invariant guarantees uniqueness or sorting. The struct’s simplicity allows it to be default‑constructed, copied, or moved using compiler‑generated operations, and it imposes no constraints on the content of its members beyond those inherent in the contained standard library types.</p><h4 id="invariants-2" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-2" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>All fields are public and can be freely modified.</li><li>No invariants are enforced by the type itself; correctness depends on external usage.</li></ul><h4 id="key-members-2" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-2" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>name</code></li><li><code>symbols</code></li><li><code>children</code></li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as a data container to represent a parsed namespace hierarchy.</li><li>Instances are aggregated to form the full namespace tree during extraction.</li></ul><h3 id="clore-extract-projectmodel" tabindex="-1"><code>clore::extract::ProjectModel</code> <a class="header-anchor" href="#clore-extract-projectmodel" aria-label="Permalink to &quot;`clore::extract::ProjectModel`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:143</code></p><p>Definition: <code>extract/model.cppm:143</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The <code>ProjectModel</code> struct serves as the central aggregation of all extracted project information. Its core data members are <code>std::unordered_map</code> containers: <code>symbols</code> maps <code>SymbolID</code> to <code>SymbolInfo</code>; <code>files</code> maps normalized file paths to <code>FileInfo</code>; <code>namespaces</code> maps qualified namespace names to <code>NamespaceInfo</code>; <code>modules</code> maps normalized source file paths to <code>ModuleUnit</code>; <code>symbol_ids_by_qualified_name</code> maps a qualified name to a <code>std::vector&lt;SymbolID&gt;</code> (to handle overloads); and <code>module_name_to_sources</code> maps a module name to the list of source files that declare it. The <code>file_order</code> member stores file paths in the sequence they were processed, preserving the extraction traversal order. Several of these maps—<code>modules</code>, <code>symbol_ids_by_qualified_name</code>, and <code>module_name_to_sources</code>—use <code>clore::support::TransparentStringHash</code> and <code>clore::support::TransparentStringEqual</code>, enabling heterogeneous lookup with <code>std::string_view</code> to avoid unnecessary allocations during queries. The <code>uses_modules</code> boolean is set to <code>true</code> if any C++20 module declaration has been encountered, serving as a flag for downstream processing stages that need to handle module‑specific logic.</p><h4 id="invariants-3" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-3" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>uses_modules</code> is true iff at least one module declaration exists</li><li><code>symbol_ids_by_qualified_name</code> may contain multiple <code>IDs</code> for overloaded names</li><li><code>modules</code> keys are normalized source file paths</li><li><code>module_name_to_sources</code> keys are exact module names</li><li><code>file_order</code> matches the order in which files were processed</li></ul><h4 id="key-members-3" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-3" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>symbols</code></li><li><code>files</code></li><li><code>namespaces</code></li><li><code>modules</code></li><li><code>symbol_ids_by_qualified_name</code></li><li><code>uses_modules</code></li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used by generation and evidence building passes for qualified name lookup</li><li>Used for cross-linking via module name lookup</li><li>Provides access to all extracted symbols, files, and namespaces</li></ul><h3 id="clore-extract-sourcelocation" tabindex="-1"><code>clore::extract::SourceLocation</code> <a class="header-anchor" href="#clore-extract-sourcelocation" aria-label="Permalink to &quot;`clore::extract::SourceLocation`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:64</code></p><p>Definition: <code>extract/model.cppm:64</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct holds a source location as three fields: a <code>file</code> path string, a <code>line</code> number, and a <code>column</code> number. The invariant is that a <code>line</code> value of <code>0</code> represents an unknown or invalid location; valid source lines start at <code>1</code>. This is enforced by the default initialization of <code>line</code> to <code>0</code>, and the <code>is_known()</code> member simply checks <code>line != 0</code>. The <code>column</code> field has no such sentinel; its semantics depend on the caller.</p><h4 id="invariants-4" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-4" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>line == 0</code> indicates the location is unknown</li><li>Valid source lines start at <code>1</code></li><li><code>is_known()</code> returns <code>false</code> only when <code>line == 0</code></li></ul><h4 id="key-members-4" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-4" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>file</code> (string)</li><li><code>line</code> (<code>uint32_t</code>)</li><li><code>column</code> (<code>uint32_t</code>)</li><li><code>is_known()</code> method</li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to capture source positions in extraction results</li><li>Checked for validity via <code>is_known()</code></li></ul><h4 id="member-functions" tabindex="-1">Member Functions <a class="header-anchor" href="#member-functions" aria-label="Permalink to &quot;Member Functions&quot;">​</a></h4><h5 id="clore-extract-sourcelocation-is-known" tabindex="-1"><code>clore::extract::SourceLocation::is_known</code> <a class="header-anchor" href="#clore-extract-sourcelocation-is-known" aria-label="Permalink to &quot;`clore::extract::SourceLocation::is_known`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:70</code></p><p>Definition: <code>extract/model.cppm:70</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation" tabindex="-1">Implementation <a class="header-anchor" href="#implementation" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nodiscard</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">]]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> bool</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> is_known</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> noexcept</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> line </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span></code></pre></div><h3 id="clore-extract-sourcerange" tabindex="-1"><code>clore::extract::SourceRange</code> <a class="header-anchor" href="#clore-extract-sourcerange" aria-label="Permalink to &quot;`clore::extract::SourceRange`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:75</code></p><p>Definition: <code>extract/model.cppm:75</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct <code>clore::extract::SourceRange</code> is a simple aggregate type that holds two <code>SourceLocation</code> members, <code>begin</code> and <code>end</code>. Its design purpose is to represent a contiguous source region by capturing the start and end locations. The primary invariant is that <code>begin</code> denotes the first location of the range and <code>end</code> the one‑past‑the‑last location, so the range is valid only if <code>begin &lt;= end</code> (according to the ordering defined by <code>SourceLocation</code>). No special member functions are declared or need to be defined; the compiler‑generated default operations suffice for its role as a lightweight value‑type pair.</p><h4 id="invariants-5" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-5" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>The <code>begin</code> and <code>end</code> members define the start and end of the range.</li><li>No ordering or validity guarantees are specified in the evidence.</li></ul><h4 id="key-members-5" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-5" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>begin</code>: the starting <code>SourceLocation</code> of the range.</li><li><code>end</code>: the ending <code>SourceLocation</code> of the range.</li></ul><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as a field in other parsing or extraction structures to store the source location of a parsed construct.</li><li>Returned by functions that produce a range covering a parsed token or node.</li></ul><h3 id="clore-extract-symbolid" tabindex="-1"><code>clore::extract::SymbolID</code> <a class="header-anchor" href="#clore-extract-symbolid" aria-label="Permalink to &quot;`clore::extract::SymbolID`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:28</code></p><p>Definition: <code>extract/model.cppm:28</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The internal state of <code>clore::extract::SymbolID</code> is a pair of unsigned integers: <code>hash</code> (a <code>std::uint64_t</code>) and <code>signature</code> (a <code>std::uint32_t</code>). The design enforces the invariant that a <code>hash</code> value of zero represents the invalid or null sentinel; all valid identifiers have a nonzero <code>hash</code>. The <code>signature</code> field stores an additional digest derived from the same USR source, serving to disambiguate the astronomically unlikely event of a 64‑bit hash collision. The member <code>is_valid()</code> simply tests whether <code>hash</code> is nonzero. Both <code>operator==</code> and <code>operator&lt;=&gt;</code> are defaulted, meaning they perform member‑wise comparison following declaration order (first <code>hash</code>, then <code>signature</code>). This gives <code>SymbolID</code> a total order and ensures that equality and three‑way comparison account for both the primary hash and the collision‑detection signature.</p><h4 id="invariants-6" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-6" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>Valid <code>IDs</code> have non-zero hash.</li><li>Zero hash represents invalid/null sentinel.</li><li>Equality and ordering are based on both hash and signature.</li></ul><h4 id="key-members-6" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-6" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>hash</code> field</li><li><code>signature</code> field</li><li><code>is_valid()</code> method</li><li><code>operator==</code></li><li><code>operator&lt;=&gt;</code></li></ul><h4 id="usage-patterns-6" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-6" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as a key or identifier for symbols in extraction logic.</li><li>Defaulted comparisons enable use in associative containers.</li><li>Collision disambiguation relies on the signature field.</li></ul><h4 id="member-functions-1" tabindex="-1">Member Functions <a class="header-anchor" href="#member-functions-1" aria-label="Permalink to &quot;Member Functions&quot;">​</a></h4><h5 id="clore-extract-symbolid-is-valid" tabindex="-1"><code>clore::extract::SymbolID::is_valid</code> <a class="header-anchor" href="#clore-extract-symbolid-is-valid" aria-label="Permalink to &quot;`clore::extract::SymbolID::is_valid`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:35</code></p><p>Definition: <code>extract/model.cppm:35</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-1" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-1" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nodiscard</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">]]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> bool</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> is_valid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> noexcept</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hash </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span></code></pre></div><h5 id="clore-extract-symbolid-operator" tabindex="-1"><code>clore::extract::SymbolID::operator&lt;=&gt;</code> <a class="header-anchor" href="#clore-extract-symbolid-operator" aria-label="Permalink to &quot;`clore::extract::SymbolID::operator&lt;=&gt;`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:40</code></p><p>Definition: <code>extract/model.cppm:40</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-2" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-2" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">auto</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> operator</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&lt;=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SymbolID</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span></span></code></pre></div><h5 id="clore-extract-symbolid-operator-1" tabindex="-1"><code>clore::extract::SymbolID::operator==</code> <a class="header-anchor" href="#clore-extract-symbolid-operator-1" aria-label="Permalink to &quot;`clore::extract::SymbolID::operator==`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:39</code></p><p>Definition: <code>extract/model.cppm:39</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-3" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-3" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">bool</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> operator</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SymbolID</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span></span></code></pre></div><h3 id="clore-extract-symbolinfo" tabindex="-1"><code>clore::extract::SymbolInfo</code> <a class="header-anchor" href="#clore-extract-symbolinfo" aria-label="Permalink to &quot;`clore::extract::SymbolInfo`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:80</code></p><p>Definition: <code>extract/model.cppm:80</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The struct <code>clore::extract::SymbolInfo</code> aggregates all extracted metadata about a single symbol. Its fields capture the symbol’s identity (<code>id</code>, <code>name</code>, <code>qualified_name</code>, <code>enclosing_namespace</code>), kind (<code>kind</code>), signature, and access specifier (<code>access</code>), along with its declaration and optional definition <code>SourceLocation</code>. Human‑readable documentation (<code>doc_comment</code>) and a source snippet (<code>source_snippet</code>) are stored directly; when the snippet is empty, the triad <code>source_snippet_offset</code>, <code>source_snippet_length</code>, <code>source_snippet_file_size</code>, and <code>source_snippet_hash</code> reference the raw source text in the file indicated by <code>declaration_location</code>, enabling deferred loading to reduce memory pressure. The structure models the symbol tree via <code>parent</code>, <code>children</code>, <code>lexical_parent_name</code>, and <code>lexical_parent_kind</code>, and tracks inheritance through <code>bases</code> and <code>derived</code>, call‑graph relations through <code>calls</code> and <code>called_by</code>, and general references through <code>references</code> and <code>referenced_by</code>. Template‑related state is captured by <code>is_template</code> and <code>template_params</code>. All collection fields store <code>SymbolID</code> values to maintain a lightweight cross‑reference graph without duplicating symbol data.</p><h4 id="invariants-7" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-7" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li><code>id</code> uniquely identifies the symbol across an extraction session</li><li><code>source_snippet_offset</code>, <code>source_snippet_length</code>, <code>source_snippet_file_size</code>, and <code>source_snippet_hash</code> are consistent if <code>source_snippet</code> is empty</li><li><code>declaration_location</code> is always present; <code>definition_location</code> is optional</li><li><code>parent</code>, <code>children</code>, <code>bases</code>, <code>derived</code>, <code>calls</code>, <code>called_by</code>, <code>references</code>, <code>referenced_by</code> store relationships as <code>SymbolID</code> values that refer to other <code>SymbolInfo</code> instances</li></ul><h4 id="key-members-7" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-7" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>id</code></li><li><code>kind</code></li><li><code>name</code></li><li><code>qualified_name</code></li><li><code>declaration_location</code></li><li><code>parent</code> and <code>children</code></li><li><code>bases</code> and <code>derived</code></li><li><code>calls</code> and <code>called_by</code></li><li><code>references</code> and <code>referenced_by</code></li><li><code>source_snippet</code> and related offset fields</li><li><code>doc_comment</code></li></ul><h4 id="usage-patterns-7" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-7" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Populated by extraction tools to describe each discovered symbol</li><li>Traversed by code analysis utilities to build dependency graphs or inheritance hierarchies</li><li>Linked via <code>SymbolID</code> fields to form a forest of symbol trees</li><li>Examined to generate cross-references, call graphs, or documentation</li></ul><h3 id="clore-extract-symbolkind" tabindex="-1"><code>clore::extract::SymbolKind</code> <a class="header-anchor" href="#clore-extract-symbolkind" aria-label="Permalink to &quot;`clore::extract::SymbolKind`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:8</code></p><p>Definition: <code>extract/model.cppm:8</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The enumeration <code>clore::extract::SymbolKind</code> is implemented as a scoped enum with an explicit underlying type of <code>std::uint8_t</code>, ensuring it occupies exactly one byte. This compaction is intentional: it reduces memory overhead when storing many symbol kind values, for example in arrays, bit fields, or when serializing symbol data. The enumerators are ordered roughly by conceptual category – type-like symbols (<code>Namespace</code>, <code>Class</code>, <code>Struct</code>, <code>Union</code>, <code>Enum</code>) come first, followed by member and function-like entries (<code>EnumMember</code>, <code>Function</code>, <code>Method</code>, <code>Variable</code>, <code>Field</code>), then declaration-level forms (<code>TypeAlias</code>, <code>Macro</code>, <code>Template</code>, <code>Concept</code>), and finally the sentinel <code>Unknown</code>. The placement of <code>Unknown</code> as the last enumerator is an internal invariant: it enables range checks such as <code>kind &lt; SymbolKind::Unknown</code> to test whether a symbol kind is a known value, and ensures that <code>Unknown</code> is the highest value, simplifying exhaustive switch‑statements or look‑up tables without requiring an explicit count constant. No additional member functions are provided; the enum relies solely on implicit conversion to <code>std::uint8_t</code> for direct use in numeric contexts.</p><h4 id="invariants-8" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-8" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>Each enumerator has a distinct integer value assigned by the compiler.</li><li>The <code>Unknown</code> enumerator serves as a default or error indicator.</li><li>The enum is intended to be used as a discriminator in variant-like structures or as a label in symbol metadata.</li></ul><h4 id="key-members-8" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-8" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>Namespace</code></li><li><code>Class</code></li><li><code>Struct</code></li><li><code>Union</code></li><li><code>Enum</code></li><li><code>EnumMember</code></li><li><code>Function</code></li><li><code>Method</code></li><li><code>Variable</code></li><li><code>Field</code></li><li><code>TypeAlias</code></li><li><code>Macro</code></li><li><code>Template</code></li><li><code>Concept</code></li><li><code>Unknown</code></li></ul><h4 id="usage-patterns-8" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-8" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Other code compares against these enumerators to determine the kind of a symbol.</li><li>The enum is likely used in switch statements or lookup tables.</li><li>It may be serialized or compared with equality.</li></ul><h4 id="member-variables" tabindex="-1">Member Variables <a class="header-anchor" href="#member-variables" aria-label="Permalink to &quot;Member Variables&quot;">​</a></h4><h5 id="clore-extract-symbolkind-class" tabindex="-1"><code>clore::extract::SymbolKind::Class</code> <a class="header-anchor" href="#clore-extract-symbolkind-class" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Class`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:10</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-4" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-4" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Class</span></span></code></pre></div><h5 id="clore-extract-symbolkind-concept" tabindex="-1"><code>clore::extract::SymbolKind::Concept</code> <a class="header-anchor" href="#clore-extract-symbolkind-concept" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Concept`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:22</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-5" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-5" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Concept</span></span></code></pre></div><h5 id="clore-extract-symbolkind-enum" tabindex="-1"><code>clore::extract::SymbolKind::Enum</code> <a class="header-anchor" href="#clore-extract-symbolkind-enum" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Enum`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:13</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-6" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-6" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Enum</span></span></code></pre></div><h5 id="clore-extract-symbolkind-enummember" tabindex="-1"><code>clore::extract::SymbolKind::EnumMember</code> <a class="header-anchor" href="#clore-extract-symbolkind-enummember" aria-label="Permalink to &quot;`clore::extract::SymbolKind::EnumMember`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:14</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-7" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-7" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EnumMember</span></span></code></pre></div><h5 id="clore-extract-symbolkind-field" tabindex="-1"><code>clore::extract::SymbolKind::Field</code> <a class="header-anchor" href="#clore-extract-symbolkind-field" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Field`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:18</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-8" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-8" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Field</span></span></code></pre></div><h5 id="clore-extract-symbolkind-function" tabindex="-1"><code>clore::extract::SymbolKind::Function</code> <a class="header-anchor" href="#clore-extract-symbolkind-function" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Function`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:15</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-9" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-9" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Function</span></span></code></pre></div><h5 id="clore-extract-symbolkind-macro" tabindex="-1"><code>clore::extract::SymbolKind::Macro</code> <a class="header-anchor" href="#clore-extract-symbolkind-macro" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Macro`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:20</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-10" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-10" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Macro</span></span></code></pre></div><h5 id="clore-extract-symbolkind-method" tabindex="-1"><code>clore::extract::SymbolKind::Method</code> <a class="header-anchor" href="#clore-extract-symbolkind-method" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Method`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:16</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-11" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-11" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Method</span></span></code></pre></div><h5 id="clore-extract-symbolkind-namespace" tabindex="-1"><code>clore::extract::SymbolKind::Namespace</code> <a class="header-anchor" href="#clore-extract-symbolkind-namespace" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Namespace`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:9</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-12" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-12" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Namespace</span></span></code></pre></div><h5 id="clore-extract-symbolkind-struct" tabindex="-1"><code>clore::extract::SymbolKind::Struct</code> <a class="header-anchor" href="#clore-extract-symbolkind-struct" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Struct`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:11</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-13" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-13" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Struct</span></span></code></pre></div><h5 id="clore-extract-symbolkind-template" tabindex="-1"><code>clore::extract::SymbolKind::Template</code> <a class="header-anchor" href="#clore-extract-symbolkind-template" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Template`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:21</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-14" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-14" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Template</span></span></code></pre></div><h5 id="clore-extract-symbolkind-typealias" tabindex="-1"><code>clore::extract::SymbolKind::TypeAlias</code> <a class="header-anchor" href="#clore-extract-symbolkind-typealias" aria-label="Permalink to &quot;`clore::extract::SymbolKind::TypeAlias`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:19</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-15" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-15" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">TypeAlias</span></span></code></pre></div><h5 id="clore-extract-symbolkind-union" tabindex="-1"><code>clore::extract::SymbolKind::Union</code> <a class="header-anchor" href="#clore-extract-symbolkind-union" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Union`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:12</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-16" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-16" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Union</span></span></code></pre></div><h5 id="clore-extract-symbolkind-unknown" tabindex="-1"><code>clore::extract::SymbolKind::Unknown</code> <a class="header-anchor" href="#clore-extract-symbolkind-unknown" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Unknown`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:23</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-17" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-17" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Unknown</span></span></code></pre></div><h5 id="clore-extract-symbolkind-variable" tabindex="-1"><code>clore::extract::SymbolKind::Variable</code> <a class="header-anchor" href="#clore-extract-symbolkind-variable" aria-label="Permalink to &quot;`clore::extract::SymbolKind::Variable`&quot;">​</a></h5><p>Declaration: <code>extract/model.cppm:17</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><h6 id="implementation-18" tabindex="-1">Implementation <a class="header-anchor" href="#implementation-18" aria-label="Permalink to &quot;Implementation&quot;">​</a></h6><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Variable</span></span></code></pre></div><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-extract-find-module-by-name" tabindex="-1"><code>clore::extract::find_module_by_name</code> <a class="header-anchor" href="#clore-extract-find-module-by-name" aria-label="Permalink to &quot;`clore::extract::find_module_by_name`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:188</code></p><p>Definition: <code>extract/model.cppm:416</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The function first delegates to <code>clore::extract::find_modules_by_name</code> to obtain a vector of candidate <code>ModuleUnit*</code> pointers for the given <code>module_name</code>. If the result is empty, it returns <code>nullptr</code>; if it contains exactly one element, that element is returned directly. When multiple candidates exist, the function iterates over them, counting those where <code>ModuleUnit::is_interface</code> is true. If exactly one interface unit is found, it is returned. If none of the candidates are interface units, a warning is emitted and the first candidate is returned as a fallback. If more than one interface unit exists, a warning about ambiguity is issued and <code>nullptr</code> is returned. The algorithm relies on the structure of <code>ModuleUnit</code> and the helper <code>find_modules_by_name</code>, and uses <code>logging::warn</code> for diagnostic output.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Logs warning messages via <code>logging::warn</code> when multiple modules match and disambiguation fails or when only implementation units exist.</li></ul><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>model</code> (const <code>ProjectModel</code>&amp;)</li><li><code>module_name</code> (<code>std::string_view</code>)</li><li><code>modules</code> result of <code>find_modules_by_name</code></li><li><code>ModuleUnit::is_interface</code> member</li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Logging subsystem (through <code>logging::warn</code>)</li></ul><h4 id="usage-patterns-9" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-9" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used by callers to resolve a module name to a single unambiguous module unit</li><li>Typically invoked during project extraction or symbol lookup when a module context is needed</li></ul><h3 id="clore-extract-find-module-by-source" tabindex="-1"><code>clore::extract::find_module_by_source</code> <a class="header-anchor" href="#clore-extract-find-module-by-source" aria-label="Permalink to &quot;`clore::extract::find_module_by_source`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:194</code></p><p>Definition: <code>extract/model.cppm:449</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The function performs a direct lookup in the <code>model.modules</code> map using the provided <code>source_file</code> as the key. If an entry is found, it returns a pointer to the corresponding <code>ModuleUnit</code>; otherwise it returns <code>nullptr</code>. The entire implementation is a single map find operation—no iteration, special handling, or fallback logic is involved.</p><p>Its only dependency is the <code>ProjectModel</code> type, which must expose the <code>modules</code> member as an associative container keyed by source file paths (e.g., <code>std::unordered_map&lt;std::string, clore::extract::ModuleUnit&gt;</code>). The return is a non‑owning pointer into that container, so the caller must ensure the <code>model</code> outlives the returned pointer.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>model<code>.modules</code></li><li><code>source_file</code></li></ul><h4 id="usage-patterns-10" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-10" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>used to retrieve a module unit by its source file path</li><li>likely called when mapping a source location to its containing module</li></ul><h3 id="clore-extract-find-modules-by-name" tabindex="-1"><code>clore::extract::find_modules_by_name</code> <a class="header-anchor" href="#clore-extract-find-modules-by-name" aria-label="Permalink to &quot;`clore::extract::find_modules_by_name`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:191</code></p><p>Definition: <code>extract/model.cppm:395</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The function <code>clore::extract::find_modules_by_name</code> performs a lookup in the <code>model.module_name_to_sources</code> map using the given <code>module_name</code>. If the key is absent or the associated source list is empty, an empty vector is returned immediately. Otherwise, it iterates over each source file in that list, calling <code>find_module_by_source(model, source)</code> to retrieve the corresponding <code>ModuleUnit</code> pointer; only non-null pointers are retained. The resulting modules are then sorted in ascending order by their <code>source_file</code> string to provide a deterministic ordering. Finally, the sorted vector is returned.</p><p>The algorithm depends on <code>clore::extract::ProjectModel</code> providing a pre‑built mapping from module names to their source file identifiers, and on <code>clore::extract::find_module_by_source</code> mapping a source file identifier to a <code>clore::extract::ModuleUnit</code>. Sorting uses the <code>source_file</code> field of <code>ModuleUnit</code>.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>model<code>.module_name_to_sources</code></li><li>model<code>.module_name_to_sources</code>[<code>module_name</code>]</li><li><code>ModuleUnit::source_file</code> (via sort lambda)</li><li>result of <code>find_module_by_source</code></li></ul><h4 id="usage-patterns-11" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-11" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to retrieve all <code>ModuleUnit</code> pointers with a specific module name.</li><li>Called when multiple sources define the same module name.</li></ul><h3 id="clore-extract-find-symbol" tabindex="-1"><code>clore::extract::find_symbol</code> <a class="header-anchor" href="#clore-extract-find-symbol" aria-label="Permalink to &quot;`clore::extract::find_symbol`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:179</code></p><p>Definition: <code>extract/model.cppm:371</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The function <code>clore::extract::find_symbol</code> is a convenience wrapper around <code>find_symbols</code>. It delegates the actual lookup to <code>find_symbols</code>, which performs name resolution across the <code>ProjectModel</code>’s files, namespaces, and modules using the provided <code>qualified_name</code>. After obtaining the collection of matching <code>SymbolInfo*</code> pointers, the function checks whether the collection contains exactly one element. If the match count is not exactly one—either because no symbol matches or multiple symbols share the same qualified name—it returns <code>nullptr</code> to signal ambiguity or absence. Otherwise, it returns the single candidate from the front of the collection. This design centralizes ambiguity handling and ensures callers receive a definitive result or a clear failure indication without additional processing.</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>model</code></li><li><code>qualified_name</code></li></ul><h4 id="usage-patterns-12" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-12" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Resolve a qualified name to a unique symbol</li><li>Lookup symbol by fully qualified name</li></ul><h3 id="clore-extract-find-symbol-1" tabindex="-1"><code>clore::extract::find_symbol</code> <a class="header-anchor" href="#clore-extract-find-symbol-1" aria-label="Permalink to &quot;`clore::extract::find_symbol`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:181</code></p><p>Definition: <code>extract/model.cppm:379</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The implementation of <code>clore::extract::find_symbol</code> handles the common case of resolving an overloaded or templated symbol by both its qualified name and its signature string. The function first checks whether the <code>signature</code> parameter is empty; if so, it delegates entirely to the single‑argument overload <code>find_symbol(model, qualified_name)</code>, which performs a straightforward name lookup.</p><p>When a non‑empty signature is provided, the function calls <code>find_symbols(model, qualified_name)</code> to obtain a flat container of candidate <code>const SymbolInfo*</code> pointers whose qualified name matches. It then performs a linear scan over these candidates, comparing each symbol’s <code>signature</code> field against the given signature using <code>operator==</code>. On the first exact match the corresponding pointer is returned; if no candidate satisfies the signature constraint, the function returns <code>nullptr</code>. This two‑stage approach separates name‑based filtering from signature‑based disambiguation, relying on <code>find_symbols</code> for the initial lookup and on the <code>SymbolInfo::signature</code> member for final identification.</p><h4 id="side-effects-4" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-4" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-4" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-4" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>model</code></li><li><code>qualified_name</code></li><li><code>signature</code></li><li><code>model</code> symbol lookup structures</li></ul><h4 id="usage-patterns-13" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-13" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Searching for a symbol by qualified name and signature</li><li>Obtaining a symbol pointer for further inspection</li></ul><h3 id="clore-extract-find-symbols" tabindex="-1"><code>clore::extract::find_symbols</code> <a class="header-anchor" href="#clore-extract-find-symbols" aria-label="Permalink to &quot;`clore::extract::find_symbols`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:185</code></p><p>Definition: <code>extract/model.cppm:354</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The implementation of <code>clore::extract::find_symbols</code> performs a direct lookup in the <code>model.symbol_ids_by_qualified_name</code> map (a member of <code>ProjectModel</code>) using the provided <code>qualified_name</code>. If the key is not found, an empty vector is returned immediately. Otherwise, the function reserves capacity for the expected number of matches and iterates over the stored <code>SymbolID</code> values, calling <code>lookup_symbol(model, symbol_id)</code> for each. Only valid <code>SymbolInfo</code> pointers (non-null results from the lookup) are appended to the result vector. This design decouples the name-to-ID mapping from the ID-to-info resolution, allowing multiple symbols to share the same qualified name (e.g., overloaded functions) while relying on <code>lookup_symbol</code> to translate the semantic identifier into the concrete metadata.</p><h4 id="side-effects-5" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-5" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-5" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-5" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>model<code>.symbol_ids_by_qualified_name</code></li><li>model (via <code>lookup_symbol</code>)</li></ul><h4 id="usage-patterns-14" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-14" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>retrieve all symbols matching a qualified name</li></ul><h3 id="clore-extract-join-qualified-name-parts" tabindex="-1"><code>clore::extract::join_qualified_name_parts</code> <a class="header-anchor" href="#clore-extract-join-qualified-name-parts" aria-label="Permalink to &quot;`clore::extract::join_qualified_name_parts`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:59</code></p><p>Definition: <code>extract/model.cppm:328</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The function <code>clore::extract::join_qualified_name_parts</code> iterates over a <code>std::vector&lt;std::string&gt;</code> of qualified name parts, joining the first <code>count</code> elements with the scope resolution <code>operator</code> <code>&quot;::&quot;</code>. Internally, it first clamps <code>count</code> to the vector’s actual size via a <code>safe_count</code> variable to avoid out‑of‑range access. A simple loop then appends each part to a result string, inserting the separator before every element after the first. The final assembled <code>std::string</code> is returned. The implementation depends only on standard library types (<code>std::vector</code>, <code>std::string</code>, <code>std::size_t</code>) and contains no calls to other <code>clore::extract</code> utilities.</p><h4 id="side-effects-6" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-6" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-6" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-6" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>parts</code> (const <code>std::vector&lt;std::string&gt;</code>&amp;)</li><li><code>count</code> (<code>std::size_t</code>)</li></ul><h4 id="usage-patterns-15" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-15" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>reconstructing fully qualified symbol names from component parts</li><li>building qualified names for lookup or display</li></ul><h3 id="clore-extract-lookup-symbol" tabindex="-1"><code>clore::extract::lookup_symbol</code> <a class="header-anchor" href="#clore-extract-lookup-symbol" aria-label="Permalink to &quot;`clore::extract::lookup_symbol`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:177</code></p><p>Definition: <code>extract/model.cppm:349</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The function performs a direct lookup in the <code>model.symbols</code> associative container using the given <code>SymbolID</code>. It calls <code>find</code> and checks the returned iterator against <code>end</code>; if a match is found, it returns a pointer to the corresponding <code>SymbolInfo</code> object, otherwise it returns <code>nullptr</code>. There is no additional fallback logic or nested search—the symbol map is the sole authority for symbol identity within the project model. The entire operation relies solely on the <code>find</code> method of the container and the equality semantics of <code>SymbolID</code>.</p><h4 id="side-effects-7" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-7" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-7" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-7" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>model</code> parameter</li><li><code>model.symbols</code></li><li><code>id</code> parameter</li></ul><h4 id="usage-patterns-16" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-16" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>retrieving symbol details for a specific ID</li><li>checking existence of a symbol</li><li>used by code generation or analysis passes</li></ul><h3 id="clore-extract-namespace-prefix-from-qualified-name" tabindex="-1"><code>clore::extract::namespace_prefix_from_qualified_name</code> <a class="header-anchor" href="#clore-extract-namespace-prefix-from-qualified-name" aria-label="Permalink to &quot;`clore::extract::namespace_prefix_from_qualified_name`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:62</code></p><p>Definition: <code>extract/model.cppm:341</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The function delegates its work to two internal utilities: <code>split_top_level_qualified_name</code> and <code>join_qualified_name_parts</code>. It first uses <code>split_top_level_qualified_name</code> to decompose <code>qualified_name</code> into a sequence of name segments. If the resulting list contains one or fewer parts, the qualified name has no namespace component, so an empty string is returned immediately. Otherwise, it calls <code>join_qualified_name_parts</code> on all segments except the last one, effectively discarding the innermost (leaf) name and reconstructing the prefix. This prefix is returned as the namespace portion. The approach is linear and relies solely on the two helper functions; no additional model lookups or state are involved.</p><h4 id="side-effects-8" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-8" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-8" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-8" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>qualified_name</code> parameter (<code>std::string_view</code>)</li></ul><h4 id="usage-patterns-17" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-17" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to derive the namespace scope of a qualified name</li><li>Called during symbol extraction to separate namespace from name</li></ul><h3 id="clore-extract-resolve-source-snippet" tabindex="-1"><code>clore::extract::resolve_source_snippet</code> <a class="header-anchor" href="#clore-extract-resolve-source-snippet" aria-label="Permalink to &quot;`clore::extract::resolve_source_snippet`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:200</code></p><p>Definition: <code>extract/model.cppm:455</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The function first acquires a per-symbol mutex via <code>source_snippet_mutex_for</code> to serialize reads for the same <code>SymbolInfo</code>. It returns early with <code>true</code> if <code>source_snippet</code> is already populated (cached). The core algorithm then validates that <code>source_snippet_length</code> is non‑zero and that <code>declaration_location.file</code> is not empty; failures cause a <code>false</code> return. After computing the file size via <code>std::filesystem::file_size</code>, it performs three integrity checks: the optional <code>source_snippet_file_size</code> must match the actual file size (if non‑zero), and the <code>source_snippet_offset</code> and <code>source_snippet_length</code> must lie within the file bounds. A binary <code>std::ifstream</code> then seeks to the stored offset and reads exactly <code>source_snippet_length</code> bytes. If the symbol’s <code>source_snippet_hash</code> is non‑zero, the read bytes are hashed via <code>hash_source_snippet_bytes</code> and compared; a mismatch also returns <code>false</code>. Finally, the raw bytes are normalized by stripping <code>\\r</code> before <code>\\n</code> (to handle Windows line endings) and the result is moved into <code>sym.source_snippet</code>, returning <code>true</code> to indicate success.</p><p>Key dependencies include <code>std::filesystem::file_size</code>, <code>std::ifstream</code> binary I/O, <code>hash_source_snippet_bytes</code> (an internal hashing utility), and <code>source_snippet_mutex_for</code> (which uses a striped mutex pool indexed by symbol pointer). The function tightly relies on <code>SymbolInfo</code> fields such as <code>source_snippet</code>, <code>source_snippet_offset</code>, <code>source_snippet_length</code>, <code>source_snippet_file_size</code>, <code>source_snippet_hash</code>, and <code>declaration_location.file</code>.</p><h4 id="side-effects-9" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-9" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Acquires a mutex (synchronization)</li><li>Reads bytes from a file on disk (I/O)</li><li>Modifies the <code><code>source_snippet</code></code> member of the <code><code>SymbolInfo</code></code> parameter (state mutation)</li><li>Allocates dynamic memory for the snippet string</li></ul><h4 id="reads-from-9" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-9" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>sym<code>.source_snippet</code></code></li><li><code>sym<code>.source_snippet_offset</code></code></li><li><code>sym<code>.source_snippet_length</code></code></li><li><code>sym<code>.declaration_location``.file</code></code></li><li><code>sym<code>.source_snippet_file_size</code></code></li><li><code>sym<code>.source_snippet_hash</code></code></li><li>the on-disk file at <code>sym<code>.declaration_location``.file</code></code></li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>sym<code>.source_snippet</code></code></li></ul><h4 id="usage-patterns-18" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-18" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to lazily load a symbol&#39;s source text after extraction</li><li>Invoked during symbol display or search result rendering</li><li>Used to populate the snippet view in documentation or code navigation</li></ul><h3 id="clore-extract-split-top-level-qualified-name" tabindex="-1"><code>clore::extract::split_top_level_qualified_name</code> <a class="header-anchor" href="#clore-extract-split-top-level-qualified-name" aria-label="Permalink to &quot;`clore::extract::split_top_level_qualified_name`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:57</code></p><p>Definition: <code>extract/model.cppm:265</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The implementation of <code>clore::extract::split_top_level_qualified_name</code> first handles the trivial case of an empty input by returning an empty vector. It then performs a thread‑safe lookup in a per‑process cache obtained via <code>clore::extract::(anonymous namespace)::split_qualified_name_cache()</code>, using a shared lock on the cache’s <code>mutex</code>; if the <code>qualified_name</code> is already present in the map <code>parts_by_qualified_name</code>, the cached result is returned immediately. On a cache miss, the function manually splits the qualified name on <code>::</code> delimiters while tracking template depth (incrementing on <code>&lt;</code>, decrementing on <code>&gt;</code>). Colons are only treated as separators when the template depth is zero, preventing false splits inside template arguments. After the loop, any leading empty segment (produced by a leading <code>::</code>) is removed. Finally, a unique lock is acquired to update the cache; if the cache size exceeds <code>kSplitQualifiedNameCacheMaxEntries</code>, the entire cache is cleared before inserting the new entry. The parsed vector of name parts is then returned.</p><h4 id="side-effects-10" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-10" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>modifies the global cache of split qualified names returned by <code>split_qualified_name_cache()</code></li><li>acquires and releases shared and unique locks on the cache mutex</li><li>clears the cache if it exceeds <code>kSplitQualifiedNameCacheMaxEntries</code> entries</li></ul><h4 id="reads-from-10" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-10" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>qualified_name</code></li><li>global cache <code>split_qualified_name_cache().parts_by_qualified_name</code></li><li><code>kSplitQualifiedNameCacheMaxEntries</code> constant</li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>split_qualified_name_cache().parts_by_qualified_name</code> map</li><li>a local <code>parts</code> vector that is returned</li></ul><h4 id="usage-patterns-19" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-19" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>parse qualified names for scope decomposition</li><li>used by other extract functions needing top-level name parts</li><li>cached to optimize repeated calls with the same qualified name</li></ul><h3 id="clore-extract-symbol-kind-name" tabindex="-1"><code>clore::extract::symbol_kind_name</code> <a class="header-anchor" href="#clore-extract-symbol-kind-name" aria-label="Permalink to &quot;`clore::extract::symbol_kind_name`&quot;">​</a></h3><p>Declaration: <code>extract/model.cppm:26</code></p><p>Definition: <code>extract/model.cppm:244</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The implementation of <code>clore::extract::symbol_kind_name</code> is a straightforward mapping function that converts a <code>SymbolKind</code> enumerator into a human‑readable <code>std::string_view</code>. The algorithm relies solely on a <code>switch</code> statement that exhaustively covers all members of the <code>SymbolKind</code> enum (including <code>Namespace</code>, <code>Class</code>, <code>Struct</code>, <code>Union</code>, <code>Enum</code>, <code>EnumMember</code>, <code>Function</code>, <code>Method</code>, <code>Variable</code>, <code>Field</code>, <code>TypeAlias</code>, <code>Macro</code>, <code>Template</code>, <code>Concept</code>, and <code>Unknown</code>). Each case returns a fixed string literal corresponding to the kind; the default branch returns the string <code>&quot;unknown&quot;</code> as a fallback. The function has no internal control flow beyond the switch, and its only dependency is the <code>SymbolKind</code> enum itself, defined within the same module. It returns <code>std::string_view</code> to avoid copying and to provide a lightweight reference to the constant string data.</p><h4 id="side-effects-11" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-11" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-11" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-11" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>parameter <code>kind</code></li></ul><h4 id="usage-patterns-20" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-20" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used for logging or display of symbol kinds</li><li>Obtain a string representation of a <code>SymbolKind</code></li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>extract:model</code> module is the core data‑model layer of the extraction pipeline. It decomposes the result of static analysis into three principal aggregates: <code>SymbolInfo</code> (identity, kind, location, relationships), <code>ModuleUnit</code> (C++20 module metadata, imports, symbol membership), and <code>ProjectModel</code> (top‑level container aggregating files, symbols, namespaces, and module units). Supporting value types (<code>SourceLocation</code>, <code>SourceRange</code>, <code>SymbolID</code>, <code>FileInfo</code>, <code>NamespaceInfo</code>) and the <code>SymbolKind</code> enumeration complete the model. All public types and free functions reside in <code>clore::extract</code>; the module imports <code>std</code> for containers and <code>support</code> for foundational utilities such as path normalization and hashing.</p><p>Internally, the module is organized around a flat public interface – query functions (<code>lookup_symbol</code>, <code>find_module_by_name</code>, <code>find_symbols</code>, etc.) operate directly on <code>ProjectModel</code> – while a private, unnamed‑namespace section houses caches and helpers. Chief among these is <code>SplitQualifiedNameCache</code>, a thread‑safe LRU‑like structure backed by a mutex‑protected map, used to avoid re‑parsing qualified name strings during repeated queries. The <code>resolve_source_snippet</code> function illustrates the module’s lazy‑resolution pattern: it reads snippet text from disk on demand, using a stripe of mutexes (<code>kSourceSnippetMutexStripeCount</code>) to minimise contention. The implementation avoids deep inheritance or layered abstractions; instead, it provides a stable, directly‑queryable snapshot of the extracted project, optimised for read‑only access after the extraction pass completes.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../support/">Module support</a></li></ul>',359)])])}const u=a(c,[["render",r]]);export{m as __pageData,u as default};
