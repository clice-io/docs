import{_ as o,o as c,c as d,a3 as a}from"./chunks/framework.CRbjHCyB.js";const h=JSON.parse('{"title":"命令解析","description":"","frontmatter":{},"headers":[],"relativePath":"zh/clice/design/command-resolve.md","filePath":"zh/clice/design/command-resolve.md"}'),i={name:"zh/clice/design/command-resolve.md"};function n(t,e,l,r,s,p){return c(),d("div",null,[...e[0]||(e[0]=[a('<h1 id="命令解析" tabindex="-1">命令解析 <a class="header-anchor" href="#命令解析" aria-label="Permalink to &quot;命令解析&quot;">​</a></h1><h2 id="背景" tabindex="-1">背景 <a class="header-anchor" href="#背景" aria-label="Permalink to &quot;背景&quot;">​</a></h2><p>C++ 语言服务器需要知道如何编译项目中的每一个文件。这些信息来自编译数据库（compilation database，简称 CDB），通常是构建系统生成的 <code>compile_commands.json</code> 文件。CDB 中的每条记录包含一个源文件路径和一条编译命令，例如：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">g++</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -std=c++20</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -O2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -fPIC</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -I../include</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -DNDEBUG</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/foo.cpp</span></span></code></pre></div><p>这条命令记录了构建系统在实际编译时调用编译器的方式。然而，语言服务器不能直接使用它，原因有三个。</p><p><strong>第一，这是驱动级命令，不是前端命令。</strong> 上面的 <code>g++</code> 是编译器驱动程序（driver），它负责选择正确的前端、链接器和标准库。语言服务器实际使用的是 Clang 的前端（cc1），而从 <code>g++</code> 到 cc1 的转换涉及大量隐式操作：确定目标三元组（target triple）、注入系统头文件搜索路径、设置默认语言标准等。这些信息在 CDB 中不会显式出现——它们是编译器隐式提供的。</p><p><strong>第二，命令中混杂了语义无关的选项。</strong> <code>-O2</code> 和 <code>-fPIC</code> 只影响代码生成，不影响语义分析——语言服务器不做代码生成，也不需要这些选项。类似地，<code>-c</code>（编译模式）和 <code>-o</code>（输出文件）是构建产物相关的指令，对语言服务器毫无意义。如果不加过滤地传给前端，它们会增加不必要的复杂度，甚至引起错误。</p><p><strong>第三，大型项目中的命令高度冗余。</strong> 一个拥有上万源文件的项目，绝大多数文件使用相同的编译器和语义选项，只有 include 路径和宏定义因文件而异。不做去重意味着每个文件都要独立查询工具链，浪费内存和启动时间。</p><p>这三个问题中，最影响用户体验的是第一个：隐式信息的缺失。当语言服务器无法正确获取系统头文件路径时，用户会看到标准库头文件报错——<code>#include &lt;vector&gt;</code> 报 &quot;file not found&quot;，或者 GCC 内置的 type traits 被标记为未声明标识符。在 clangd 的 issue 中，这类问题长期占据最高频率（<a href="https://github.com/clangd/clangd/issues/1262" target="_blank" rel="noreferrer">clangd#1262</a>、<a href="https://github.com/clangd/clangd/issues/1691" target="_blank" rel="noreferrer">clangd#1691</a>）。</p><p>clangd 的解决方案是 <code>--query-driver</code> 参数：用户手动指定哪些编译器需要探测，clangd 再去查询这些编译器获取系统路径。这个方案有两个问题：首先，它是手动的——用户必须知道自己的项目使用了哪个编译器，还要正确配置 glob 模式。其次，它作为一个 flag 并不独立可用——它依赖 CDB 中已有的命令来触发探测（<a href="https://github.com/clangd/clangd/issues/1219" target="_blank" rel="noreferrer">clangd#1219</a>）。对于交叉编译、嵌入式开发等场景，用户经常需要反复调试才能让 clangd 正确识别工具链。</p><p>clice 将整个命令处理流程自动化：从 CDB 中读取命令后，自动识别编译器家族（GCC、Clang、MSVC 等），自动探测工具链信息，并通过多级去重将启动开销降到最低。用户不需要手动配置任何工具链相关的参数。</p><h2 id="设计" tabindex="-1">设计 <a class="header-anchor" href="#设计" aria-label="Permalink to &quot;设计&quot;">​</a></h2><p>命令处理的核心任务是将 CDB 中的原始驱动命令转换为 Clang 前端可消费的 cc1 参数。这个转换涉及四个概念层次：参数分类、命令分离、工具链探测、搜索路径提取。</p><h3 id="参数分类" tabindex="-1">参数分类 <a class="header-anchor" href="#参数分类" aria-label="Permalink to &quot;参数分类&quot;">​</a></h3><p>加载 CDB 时，每个编译选项被分为四类：</p><ul><li><p><strong>丢弃（Discarded）</strong>：与构建产物相关的选项，语言服务器不需要。包括输出文件（<code>-o</code>）、编译模式（<code>-c</code>）、依赖扫描（<code>-M</code> 系列）、PCH 构建（<code>-emit-pch</code>）、C++20 模块（<code>-fmodule-file</code> 等，由语言服务器自行管理）。</p></li><li><p><strong>仅代码生成（Codegen-only）</strong>：只影响代码生成后端、不影响语义分析的选项。包括位置无关代码（<code>-fPIC</code>）、栈保护（<code>-fstack-protector</code>）、帧指针（<code>-fomit-frame-pointer</code>）、调试信息（<code>-g</code> 系列）、LTO 等。这些不会改变 AST 或诊断结果。</p><blockquote><p>注意：看起来像代码生成选项的 <code>-O</code> 和 <code>-fsanitize=</code> 不在此列。<code>-O</code> 会定义 <code>__OPTIMIZE__</code> 宏，<code>-fsanitize=address</code> 会影响 <code>__has_feature(address_sanitizer)</code> 的结果。它们改变预处理器状态，属于语义选项。</p></blockquote></li><li><p><strong>用户内容（User-content）</strong>：每个文件可能不同的选项，但不影响工具链探测结果。包括 include 路径（<code>-I</code>、<code>-isystem</code>、<code>-iquote</code>、<code>-idirafter</code>）、宏定义（<code>-D</code>、<code>-U</code>）和强制包含（<code>-include</code>）。这些选项的含义是&quot;这个特定文件额外需要什么&quot;。</p></li><li><p><strong>语义选项（Semantic）</strong>：剩余的所有选项，它们影响编译语义并在工具链探测中起作用。例如 <code>-std=c++20</code>、<code>-Wall</code>、<code>-target</code>、<code>-march=</code> 等。</p></li></ul><p>分类基于 Clang 自身的选项表（<code>OptTable</code>），按选项 ID 进行判断，而不是按字符串匹配。</p><h3 id="命令分离" tabindex="-1">命令分离 <a class="header-anchor" href="#命令分离" aria-label="Permalink to &quot;命令分离&quot;">​</a></h3><p>分类完成后，每条编译命令被拆分为两部分：</p><ul><li><strong><code>CanonicalCommand</code></strong>：驱动程序路径 + 所有语义选项。代表&quot;编译器的身份和语义配置&quot;。</li><li><strong>Patch</strong>：所有用户内容选项。代表&quot;这个文件额外需要的 include 路径和宏定义&quot;。</li></ul><p>两者组合加上工作目录构成 <code>CompilationInfo</code>，是一个文件完整编译配置的抽象表示。</p><p>这个分离的核心目的是工具链探测的缓存效率。工具链探测需要实际调用编译器驱动（如运行 <code>g++ -dumpmachine</code> 或 <code>clang++ -###</code>），耗时通常在 100ms 以上。探测结果只取决于驱动程序和语义选项——用户内容选项（<code>-I</code>、<code>-D</code>）不会影响驱动输出的系统路径或目标三元组。因此，无论文件有什么不同的 <code>-I</code> 路径，只要语义选项相同，就可以共享同一个探测结果。</p><p>在实际项目中，上万个文件可能只有几十个不同的 <code>CanonicalCommand</code>，这意味着工具链只需要探测几十次而非上万次。</p><p><code>CanonicalCommand</code>、<code>CompilationInfo</code> 都通过 <code>ObjectSet</code> 去重——内容相同的实例在内存中只存在一份，由指针共享。字符串参数通过 <code>StringSet</code> 内部化，保证指针稳定且可直接比较。</p><h3 id="编译数据库" tabindex="-1">编译数据库 <a class="header-anchor" href="#编译数据库" aria-label="Permalink to &quot;编译数据库&quot;">​</a></h3><p><code>CompilationDatabase</code> 负责加载 <code>compile_commands.json</code>，将每条记录解析、分类、去重后存储为 <code>CompilationEntry</code>（文件路径 ID → <code>CompilationInfo</code>）。所有条目按文件路径 ID 排序，支持二分查找。</p><p>查找时，<code>CompilationDatabase</code> 将 <code>CompilationInfo</code> 组装为 <code>CompileCommand</code>——这是命令处理流水线的最终输出，包含完整的编译选项和源文件路径，可直接提交给工具链探测或 Clang 前端。</p><p>对于没有 CDB 条目的文件（例如用户打开了一个不在项目中的文件），<code>CompilationDatabase</code> 会合成一个默认命令——根据文件扩展名选择 <code>clang</code> 或 <code>clang++ -std=c++20</code>。</p><p><code>CompilationDatabase</code> 还提供了按配置分组的能力：<code>ConfigGroup</code> 将共享相同 <code>CompilationInfo</code> 的文件聚合在一起。这是依赖扫描中提取搜索路径配置的正确粒度——不同的 <code>-I</code> 路径产生不同的分组。对于工具链探测，粒度更粗（用户内容选项不影响探测结果），因此 <code>Toolchain</code> 会在 <code>ConfigGroup</code> 的基础上进一步去重。</p><h3 id="配置规则" tabindex="-1">配置规则 <a class="header-anchor" href="#配置规则" aria-label="Permalink to &quot;配置规则&quot;">​</a></h3><p>除了 CDB 本身的编译命令，用户可以通过 <code>clice.toml</code> 中的 <code>[[rules]]</code> 配置追加或移除编译选项。每条规则包含文件匹配模式（glob）和要追加（append）/移除（remove）的选项列表。</p><p>在查找文件的编译命令时，匹配的规则会被应用到 CDB 命令之上——先从基础命令中移除指定的选项，再追加新选项。这使得用户可以项目级地微调编译参数，而不需要修改构建系统的输出。</p><h3 id="工具链" tabindex="-1">工具链 <a class="header-anchor" href="#工具链" aria-label="Permalink to &quot;工具链&quot;">​</a></h3><p><code>Toolchain</code> 负责将驱动级命令转换为 cc1 参数。它的设计围绕两个核心能力：</p><p><strong>编译器家族识别。</strong> <code>Toolchain</code> 通过可执行文件名识别编译器家族——<code>CompilerFamily</code> 枚举包括 GCC、Clang、MSVC、ClangCL、NVCC、Intel、Zig。识别规则处理了各种命名变体：版本后缀（<code>clang++-17</code>）、架构前缀（<code>arm-none-eabi-g++</code>）、Windows 的 <code>.exe</code> 后缀等。家族信息决定了后续使用哪种探测策略。</p><p><strong>缓存策略。</strong> 探测结果以 (驱动路径, 文件扩展名, 非用户内容选项) 为键缓存。文件扩展名参与缓存键，因为 <code>.c</code> 和 <code>.cpp</code> 可能触发不同的驱动规则。失败的探测也会被缓存（负缓存），避免对同一个不存在的编译器重复尝试。</p><h3 id="搜索路径" tabindex="-1">搜索路径 <a class="header-anchor" href="#搜索路径" aria-label="Permalink to &quot;搜索路径&quot;">​</a></h3><p><code>SearchConfig</code> 从 cc1 参数中提取头文件搜索路径，组织为四段式结构：</p><ol><li><strong>Quoted</strong>（<code>-iquote</code>）：<code>#include &quot;foo.h&quot;</code> 的搜索路径</li><li><strong>Angled</strong>（<code>-I</code>）：<code>#include &lt;foo.h&gt;</code> 的搜索路径</li><li><strong>System</strong>（<code>-isystem</code>、<code>-internal-isystem</code> 等）：系统头文件路径</li><li><strong>After</strong>（<code>-idirafter</code>）：在系统目录之后搜索的路径</li></ol><p>这个四段模型对应 Clang 内部的搜索布局。段内路径会去重（从 Angled 段开始），去重算法复制了 Clang 的行为：如果同一路径出现在 Angled 和 System 两个段中，保留 Angled 中的那个。这确保了 <code>#include_next</code> 的正确性。</p><p><code>SearchConfig</code> 是 include 路径补全、include 路径解析、<a href="./dependency-scanning">依赖图构建</a>等功能的基础输入。</p><h2 id="实现" tabindex="-1">实现 <a class="header-anchor" href="#实现" aria-label="Permalink to &quot;实现&quot;">​</a></h2><h3 id="加载与解析" tabindex="-1">加载与解析 <a class="header-anchor" href="#加载与解析" aria-label="Permalink to &quot;加载与解析&quot;">​</a></h3><p>CDB 加载使用 simdjson 流式解析 JSON，逐条处理：</p><ol><li>读取每条记录的 <code>directory</code>、<code>file</code>、<code>arguments</code>（或 <code>command</code>）字段</li><li>过滤非 C/C++ 文件（如 <code>.rc</code>、<code>.asm</code>、<code>.def</code>）</li><li>将相对文件路径解析为绝对路径</li><li>对 <code>arguments</code> 字段中的每个选项进行分类，分别放入 canonical 和 patch</li><li>将 include 路径选项中的相对路径绝对化（基于 <code>directory</code> 解析）</li><li>通过 <code>ObjectSet</code> 去重 <code>CanonicalCommand</code> 和 <code>CompilationInfo</code></li><li>所有条目按文件路径 ID 排序</li></ol><p>解析过程中还处理了一个特殊情况：CMake 生成的 CDB 中有时会包含 <code>-Xclang -include-pch -Xclang &lt;pchfile&gt;</code> 序列（CMake 的 PCH 变通方案），加载时会识别并丢弃这个模式。</p><h3 id="工具链探测" tabindex="-1">工具链探测 <a class="header-anchor" href="#工具链探测" aria-label="Permalink to &quot;工具链探测&quot;">​</a></h3><p>不同编译器家族使用不同的探测策略：</p><p><strong>GCC</strong>：分两步。第一步，调用 GCC 驱动获取两个关键信息——目标三元组（<code>-dumpmachine</code>）和安装路径（<code>-print-search-dirs</code>）。第二步，将这两个信息注入 Clang 的驱动（<code>--target=</code> 和 <code>--gcc-install-dir=</code>），让 Clang 驱动模拟 GCC 的行为，获取 cc1 参数。这样 Clang 前端就能正确找到 GCC 的标准库和系统头文件。</p><p><strong>Clang / Zig</strong>：调用驱动的 <code>-###</code> 选项，它会打印出完整的 cc1 命令行而不实际执行编译。解析输出中的第一行 cc1 命令即可。对于 Zig，驱动路径包含两部分（<code>zig cc</code> 或 <code>zig c++</code>），探测时需要特殊处理。</p><blockquote><p>外部驱动的版本可能比 clice 内嵌的 LLVM 版本更新，输出的 cc1 参数中可能包含 clice 不认识的选项。解析时会将未知选项静默丢弃，保证兼容性。</p></blockquote><p><strong>MSVC / ClangCL</strong>：通过 <code>--driver-mode=cl</code> 指令切换 Clang 驱动到 MSVC 兼容模式，然后用 Clang 驱动获取 cc1 参数。</p><p>探测完成后，会从结果中移除临时探测文件的路径和模块输出相关的选项（它们引用了已删除的临时文件）。如果 clice 的 resource dir 与探测结果中的不一致，还会替换所有相关路径，确保前端使用匹配版本的内置头文件。</p><p><strong>启动预热。</strong> 在服务器启动的依赖扫描阶段，会收集所有唯一的工具链缓存键并发起并行探测。探测子进程的管道读取和进程等待也是并发执行的，避免管道阻塞导致的死锁。预热完成后，后续所有的工具链查询都命中缓存。</p><h3 id="搜索路径提取" tabindex="-1">搜索路径提取 <a class="header-anchor" href="#搜索路径提取" aria-label="Permalink to &quot;搜索路径提取&quot;">​</a></h3><p>工具链探测得到 cc1 参数后，搜索路径提取遍历这些参数，将 include 路径选项按类型分入四个段。所有路径被解析为绝对路径并规范化（消除 <code>.</code> 和 <code>..</code>）。</p><p><code>-iprefix</code> / <code>-iwithprefix</code> / <code>-iwithprefixbefore</code> 三个选项需要按出现顺序配合处理：<code>-iprefix</code> 设置前缀，后续的 <code>-iwithprefix</code> 将前缀拼接到路径前再放入 After 段，<code>-iwithprefixbefore</code> 则放入 Angled 段。</p><p>四段拼接完成后，从 Angled 段开始去重。Quoted 段不参与去重——同一路径同时出现在 Quoted 和 Angled 中是合法的，两者都会被保留。这复制了 Clang 内部的行为。</p><h2 id="faq" tabindex="-1">FAQ <a class="header-anchor" href="#faq" aria-label="Permalink to &quot;FAQ&quot;">​</a></h2><ul><li><p><strong>为什么按选项 ID 分类，而不是字符串匹配？</strong></p><p>clice 的参数解析基于 Clang 自身的选项表（从 <code>Options.inc</code> 生成），通过选项 ID 进行分类判断。字符串匹配容易遗漏边界情况：Clang 的选项语法有多种形式——<code>-std=c++20</code> 是 joined 形式，<code>-I /path</code> 是 separate 形式，<code>-Wall</code> 是 flag 形式，有些选项还有 <code>/</code> 前缀（MSVC 风格）。按 ID 分类意味着所有这些语法变体都被 Clang 的解析器正确处理，分类逻辑只需要关心&quot;这个选项是什么&quot;，不需要关心&quot;它是怎么拼写的&quot;。</p></li><li><p><strong>为什么用户内容选项不参与工具链缓存键？</strong></p><p>这是两级分离设计的核心收益。<code>-I</code> 和 <code>-D</code> 不会改变编译器驱动输出的系统路径、目标三元组或语言默认设置。将它们从缓存键中排除，使得缓存键的数量从&quot;每文件一个&quot;降低到&quot;每配置一个&quot;。一个上万文件的项目通常只有几十个不同的缓存键，启动时只需要几十次子进程调用。</p></li><li><p><strong>为什么搜索路径去重要精确匹配 Clang 的行为？</strong></p><p>如果语言服务器的头文件搜索顺序与实际编译器不一致，可能导致 <code>#include</code> 解析到不同的文件——同名头文件在不同目录中存在时，搜索顺序决定了使用哪一个。<code>#include_next</code> 的语义更是直接依赖于搜索目录的去重结果。严格匹配确保语言服务器看到的代码与编译器看到的完全一致。</p></li><li><p><strong>为什么 GCC 探测要分两步，而不是直接用 GCC 的 <code>-v</code> 输出？</strong></p><p>Clang 前端需要知道 GCC 的目标三元组和安装路径，才能正确找到 GCC 的标准库头文件。直接解析 GCC 的 <code>-v</code> 输出虽然可行，但会引入额外的文本解析逻辑且容易因不同 GCC 版本的输出格式变化而出错。通过 <code>--target=</code> 和 <code>--gcc-install-dir=</code> 将信息注入 Clang 驱动，让 Clang 自身完成搜索路径的组装，更加可靠。</p></li><li><p><strong>为什么编译器家族识别基于可执行文件名而不是路径解析？</strong></p><p>编译器驱动的行为受调用名称影响。例如，<code>/usr/bin/clang++</code> 通常是 <code>/usr/lib/llvm-20/bin/clang</code> 的符号链接，但以 <code>clang++</code> 名义调用时会自动启用 C++ 模式并链接 C++ 库。如果使用 <code>realpath</code> 解析到真实路径后再判断，会丢失调用名称携带的语义信息。同样，<code>arm-none-eabi-g++</code> 如果被解析成某个通用 GCC 二进制的路径，交叉编译的上下文就会丢失。</p></li><li><p><strong>CDB 中没有条目的文件怎么处理？</strong></p><p>合成一个默认命令。根据文件扩展名选择 <code>clang</code> 或 <code>clang++ -std=c++20</code>，并注入 resource dir。这确保即使文件不在 CDB 中，基本的语义分析仍然可用。对于头文件，还会尝试通过依赖图找到包含它的源文件，使用该源文件的编译命令作为上下文（详见<a href="./compilation-context">编译上下文</a>）。</p></li></ul><h2 id="已知局限" tabindex="-1">已知局限 <a class="header-anchor" href="#已知局限" aria-label="Permalink to &quot;已知局限&quot;">​</a></h2><ul><li><p><strong>部分编译器家族支持不完整。</strong> NVCC 和 Intel 编译器（<code>icc</code>、<code>icx</code>、<code>dpcpp</code>）虽然被识别，但目前回退到通用的 Clang 驱动路径，没有专门的探测逻辑。这意味着这些编译器的特殊系统路径可能无法被正确发现。</p></li><li><p><strong>SearchConfig 不支持部分搜索路径选项。</strong> <code>-cxx-isystem</code>（仅 C++ 模式生效的系统目录）、<code>-iwithsysroot</code>（拼接 sysroot 前缀）和 HeaderMap 支持尚未实现。这些选项在实际项目中不常见，但可能在特定的 Apple 或交叉编译工具链中出现。</p></li><li><p><strong>配置规则的全局影响。</strong> <code>clice.toml</code> 中的 <code>[[rules]]</code> 可以向编译命令追加或移除选项。如果用户修改了影响所有文件的规则（例如追加一个全局的 <code>-I</code>），所有文件的编译配置都会改变，可能触发全量重索引。目前没有机制检测哪些规则变更实际影响了哪些文件。</p></li><li><p><strong>MSVC 兼容模式的选项解析。</strong> 在非 Windows 系统上，需要特别处理 MSVC 风格的选项前缀（<code>/U</code>、<code>/D</code>、<code>/I</code>），避免 Unix 绝对路径（如 <code>/Users/...</code>）被误解析为 MSVC 选项。目前通过根据驱动名称动态调整选项可见性来解决，但边界情况仍可能存在。</p></li></ul>',62)])])}const C=o(i,[["render",n]]);export{h as __pageData,C as default};
