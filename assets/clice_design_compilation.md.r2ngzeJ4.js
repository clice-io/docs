import{_ as i,o as a,c as s,a3 as t}from"./chunks/framework.CRbjHCyB.js";const m=JSON.parse('{"title":"Compilation","description":"","frontmatter":{},"headers":[],"relativePath":"clice/design/compilation.md","filePath":"en/clice/design/compilation.md"}'),n={name:"clice/design/compilation.md"};function l(o,e,r,c,d,h){return a(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="compilation" tabindex="-1">Compilation <a class="header-anchor" href="#compilation" aria-label="Permalink to &quot;Compilation&quot;">​</a></h1><h2 id="incremental-parsing" tabindex="-1">Incremental Parsing <a class="header-anchor" href="#incremental-parsing" aria-label="Permalink to &quot;Incremental Parsing&quot;">​</a></h2><p>Every time you modify code, clice must re-parse the file. clice uses a mechanism called preamble to implement incremental compilation. The preamble is a special case of <a href="https://clang.llvm.org/docs/PCHInternals.html" target="_blank" rel="noreferrer">Precompiled Header</a> — it builds the initial preprocessor directives (the <code>#include</code> block at the top of a file) into a PCH cache on disk. Later, when parsing, it loads the PCH directly, skipping the preamble section entirely.</p><p>For example:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;iostream&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::cout </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Hello world!&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::endl;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The <code>iostream</code> header expands to ~20,000 lines. clice builds <code>#include &lt;iostream&gt;</code> into a PCH file, reducing subsequent re-parses from 20,000 lines to just 5 lines. Modifying the preamble section (adding/removing includes) or changing compile flags that affect preprocessing (<code>-D</code>, <code>-I</code>, <code>-std</code>) requires rebuilding the PCH.</p><h3 id="staleness-detection" tabindex="-1">Staleness Detection <a class="header-anchor" href="#staleness-detection" aria-label="Permalink to &quot;Staleness Detection&quot;">​</a></h3><p>PCH freshness is checked via a two-layer scheme:</p><ol><li><strong>Mtime check</strong> — fast path: if no dependency has a newer mtime than the cached PCH, it&#39;s still valid.</li><li><strong>Content hash</strong> — if mtime changed (e.g., a <code>touch</code> without real edits), hash the file contents. Only if the hash differs is the PCH rebuilt.</li></ol><p>This avoids expensive rebuilds on timestamp-only changes (common with build systems that regenerate files in-place).</p><h2 id="multi-process-pipeline" tabindex="-1">Multi-Process Pipeline <a class="header-anchor" href="#multi-process-pipeline" aria-label="Permalink to &quot;Multi-Process Pipeline&quot;">​</a></h2><p>Compilation tasks are distributed across worker processes:</p><ul><li><strong>Stateless workers</strong> handle PCH builds, PCM builds (C++20 modules), completion, and signature help. These are ephemeral — no state persists between requests.</li><li><strong>Stateful workers</strong> hold parsed ASTs in memory and serve queries (hover, semantic tokens, inlay hints, etc.). Files are cached via LRU eviction.</li></ul><p>The master process never performs compilation directly. It dispatches tasks and collects results via bincode IPC over stdin/stdout.</p><h2 id="c-20-modules" tabindex="-1">C++20 Modules <a class="header-anchor" href="#c-20-modules" aria-label="Permalink to &quot;C++20 Modules&quot;">​</a></h2><p>Module compilation follows a dependency graph (<code>CompileGraph</code>). The master resolves module dependencies from the syntax-level <code>DependencyGraph</code>, then builds PCM files in topological order via stateless workers.</p><p>Each module unit has a <code>CompileUnit</code> in the compile graph with:</p><ul><li>A refcount (interest count) tracking how many downstream dependents need it</li><li>A cancellation token that fires when the refcount drops to zero</li></ul><p>When a file is modified, dependent module units are automatically recompiled in dependency order.</p><h2 id="cancel-compilation" tabindex="-1">Cancel Compilation <a class="header-anchor" href="#cancel-compilation" aria-label="Permalink to &quot;Cancel Compilation&quot;">​</a></h2><p>clice uses <strong>interest-counted cancellation</strong> to avoid wasting CPU on compilations that are no longer needed.</p><h3 id="the-problem" tabindex="-1">The Problem <a class="header-anchor" href="#the-problem" aria-label="Permalink to &quot;The Problem&quot;">​</a></h3><p>In a module DAG like <code>A → B → C</code>, if the user opens file C, compilation starts for A, then B, then C. But if the user closes C (or switches to another file) while A is still building, continuing to build B and C is pointless.</p><h3 id="the-mechanism" tabindex="-1">The Mechanism <a class="header-anchor" href="#the-mechanism" aria-label="Permalink to &quot;The Mechanism&quot;">​</a></h3><p>Each <code>CompileUnit</code> in the compile graph maintains a reference count:</p><ul><li>When a file needs a module&#39;s PCM, it increments (<code>acquire</code>) that module&#39;s refcount.</li><li>When it no longer needs it (file closed, edit superseded), it decrements (<code>release</code>).</li><li>When refcount reaches zero, a one-tick grace period fires. If still zero after the grace period, the compilation&#39;s cancellation token is triggered.</li></ul><p>The grace period prevents thrashing — rapid open/close sequences don&#39;t cause redundant cancel-and-restart cycles.</p><h3 id="supersede-logic" tabindex="-1">Supersede Logic <a class="header-anchor" href="#supersede-logic" aria-label="Permalink to &quot;Supersede Logic&quot;">​</a></h3><p>When a file is edited while its previous compilation is in-flight, the old build is &quot;superseded&quot; — its interest is released and a new build starts. The old worker task observes the cancellation token and aborts early.</p>`,29)])])}const u=i(n,[["render",l]]);export{m as __pageData,u as default};
