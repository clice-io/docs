import{_ as s,o as a,c as e,a3 as n}from"./chunks/framework.CRbjHCyB.js";const g=JSON.parse('{"title":"编译上下文","description":"","frontmatter":{},"headers":[],"relativePath":"zh/clice/design/compilation-context.md","filePath":"zh/clice/design/compilation-context.md"}'),t={name:"zh/clice/design/compilation-context.md"};function l(o,i,c,h,p,d){return a(),e("div",null,[...i[0]||(i[0]=[n(`<h1 id="编译上下文" tabindex="-1">编译上下文 <a class="header-anchor" href="#编译上下文" aria-label="Permalink to &quot;编译上下文&quot;">​</a></h1><h2 id="背景" tabindex="-1">背景 <a class="header-anchor" href="#背景" aria-label="Permalink to &quot;背景&quot;">​</a></h2><p>在 C++ 中，同一个文件在不同的条件下可以产生完全不同的编译结果。对于源文件来说，不同的编译命令（<code>-std=c++17</code> vs <code>-std=c++20</code>，不同的 <code>-D</code> 宏定义等）会导致不同的 AST。对于头文件来说，情况更加复杂——同一个头文件被不同的源文件包含时，由于前面的 <code>#include</code> 和 <code>#define</code> 不同，可能产生完全不同的预处理结果。</p><p>然而，现有的所有语言服务器——不仅是 C++ 的——都没有正式处理这个问题。用户在编辑器中打开一个文件时，语言服务器只会选择一个&quot;默认&quot;的编译上下文来提供服务。当这个默认选择不正确时，用户会看到错误的诊断、错误的补全建议、错误的跳转目标。</p><p>以 clangd 为例，社区长期存在大量相关问题：</p><ul><li>头文件打开后显示错误的诊断信息，因为 clangd 选择了错误的宿主源文件（host source）</li><li>多配置项目（如同时支持 Debug/Release、不同平台）中，用户无法选择使用哪套编译命令</li><li>非自包含头文件（non-self-contained headers）缺少正确的上下文，导致大量误报</li></ul><p>clice 将<strong>编译上下文</strong>（Compilation Context）作为一等公民贯穿整个设计，从根本上解决这些问题。</p><h2 id="什么是编译上下文" tabindex="-1">什么是编译上下文 <a class="header-anchor" href="#什么是编译上下文" aria-label="Permalink to &quot;什么是编译上下文&quot;">​</a></h2><p>编译上下文是&quot;正确编译一个文件所需的全部条件&quot;。根据文件类型不同，编译上下文的含义也不同：</p><h3 id="源文件的编译上下文" tabindex="-1">源文件的编译上下文 <a class="header-anchor" href="#源文件的编译上下文" aria-label="Permalink to &quot;源文件的编译上下文&quot;">​</a></h3><p>对于源文件（<code>.cpp</code>），编译上下文就是它的<strong>编译命令</strong>——从编译数据库（CDB）中读取的编译选项。一个源文件可能对应多条编译命令（例如在多配置项目中），每条命令代表一个不同的编译上下文。</p><p>这种情况相对简单：用户选择一条编译命令，即选择了一个编译上下文。</p><h3 id="头文件的编译上下文" tabindex="-1">头文件的编译上下文 <a class="header-anchor" href="#头文件的编译上下文" aria-label="Permalink to &quot;头文件的编译上下文&quot;">​</a></h3><p>对于头文件（<code>.h</code>），情况分为两种：</p><p><strong>自包含头文件</strong>：如果头文件本身可以独立编译（有自己的 CDB 条目，且不依赖宿主源文件提供的前置 include 或宏定义），则可以当做源文件处理，直接使用其编译命令。</p><p><strong>非自包含头文件</strong>：如果头文件依赖于包含它的源文件提供的前置上下文（前面的 <code>#include</code>、<code>#define</code> 等），则需要<strong>合成</strong>一个编译上下文。这个合成上下文由以下部分组成：</p><ul><li><strong>宿主源文件（host source）</strong>：一个包含该头文件的源文件</li><li><strong>include 链</strong>：从宿主源文件到目标头文件的 include 路径</li><li><strong>前缀代码（preamble）</strong>：宿主源文件中位于 <code>#include &quot;target.h&quot;</code> 之前的所有内容</li></ul><p>这就是<strong>头文件上下文（Header File Context）</strong> 的概念——它本质上是编译上下文在头文件场景下的特化。</p><h2 id="设计方案" tabindex="-1">设计方案 <a class="header-anchor" href="#设计方案" aria-label="Permalink to &quot;设计方案&quot;">​</a></h2><h3 id="编译上下文的解析流程" tabindex="-1">编译上下文的解析流程 <a class="header-anchor" href="#编译上下文的解析流程" aria-label="Permalink to &quot;编译上下文的解析流程&quot;">​</a></h3><p>当一个文件需要编译时，按以下优先级确定其编译上下文：</p><ol><li><strong>用户主动选择</strong>：如果用户通过扩展协议（<code>clice/switchContext</code>）指定了编译上下文，使用用户的选择</li><li><strong>CDB 直接查找</strong>：如果文件本身在编译数据库中有条目，使用该条目的编译命令</li><li><strong>头文件上下文合成</strong>：如果文件不在 CDB 中（典型的头文件场景），自动合成编译上下文</li></ol><h3 id="头文件上下文的合成" tabindex="-1">头文件上下文的合成 <a class="header-anchor" href="#头文件上下文的合成" aria-label="Permalink to &quot;头文件上下文的合成&quot;">​</a></h3><p>头文件上下文的合成过程如下：</p><p><strong>第一步：查找宿主源文件。</strong> 通过 DependencyGraph 的反向 include 图，BFS 向上搜索所有传递包含该头文件的源文件。选择第一个在 CDB 中有条目的源文件作为宿主。</p><p><strong>第二步：计算 include 链。</strong> 从宿主源文件到目标头文件，通过正向 include 图找到最短路径。例如：<code>main.cpp → utils.h → math.h</code>，如果目标是 <code>math.h</code>，则 include 链为 <code>[main.cpp, utils.h, math.h]</code>。</p><p><strong>第三步：合成前缀代码。</strong> 对于 include 链中的每个文件（除了最后的目标文件），读取其内容，扫描找到包含下一个文件的 <code>#include</code> 行，提取该行之前的所有内容，加上 <code>#line</code> 指令用于正确的错误定位。</p><p>例如，<code>main.cpp</code> 的内容是：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;vector&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DEBUG</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;utils.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() { ... }</span></span></code></pre></div><p><code>utils.h</code> 的内容是：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#pragma</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> once</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;string&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;math.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> util_func</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>那么为 <code>math.h</code> 合成的前缀代码为：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#line</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 1 </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;main.cpp&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;vector&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DEBUG</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#line</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 1 </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;utils.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#pragma</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> once</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;string&gt;</span></span></code></pre></div><p><strong>第四步：缓存前缀文件。</strong> 将合成的前缀代码写入磁盘缓存，使用内容的 xxh3 哈希值作为文件名，实现内容寻址的去重。缓存路径为 <code>{cache_dir}/header_context/{hash}.h</code>。</p><p><strong>第五步：注入编译命令。</strong> 使用宿主源文件的 CDB 编译命令，将源文件路径替换为目标头文件路径，并通过 <code>-include</code> 标志注入前缀文件。最终的编译命令形如：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>clang -std=c++17 -Iinclude -include cache/header_context/abc123.h -c math.h</span></span></code></pre></div><p>这样，<code>math.h</code> 就如同在宿主源文件的正确位置被编译一样，获得了正确的预处理器状态。</p><h3 id="编译上下文的切换" tabindex="-1">编译上下文的切换 <a class="header-anchor" href="#编译上下文的切换" aria-label="Permalink to &quot;编译上下文的切换&quot;">​</a></h3><p>clice 通过 LSP 扩展协议支持用户主动切换编译上下文：</p><ul><li><strong>查询可用上下文（<code>clice/queryContext</code>）</strong>：列出一个文件所有可用的编译上下文。对于头文件，返回所有可能的宿主源文件；对于源文件，返回所有匹配的 CDB 条目。</li><li><strong>查询当前上下文（<code>clice/currentContext</code>）</strong>：返回用户主动选择的编译上下文（即通过 <code>switchContext</code> 设置的）；如果使用默认上下文则返回空。</li><li><strong>切换上下文（<code>clice/switchContext</code>）</strong>：用户选择一个新的编译上下文。这会清除该文件的所有缓存编译产物（PCH 引用、头文件上下文、依赖快照），并标记 AST 为脏，触发重新编译。</li></ul><p>切换上下文是一个相对重量级的操作——它会导致重新编译。但这是正确的行为：不同的上下文意味着不同的编译结果。</p><h3 id="与-pch-优化的协作" tabindex="-1">与 PCH 优化的协作 <a class="header-anchor" href="#与-pch-优化的协作" aria-label="Permalink to &quot;与 PCH 优化的协作&quot;">​</a></h3><p>PCH（预编译头文件）在头文件上下文中扮演重要角色。头文件的前缀代码本质上就是一个 preamble，它可以被编译为 PCH 以加速后续编译。</p><p>PCH 文件在磁盘上以前缀内容的<strong>哈希值</strong>命名，实现文件级的内容寻址去重——相同内容的前缀只会在磁盘上存储一份 PCH 文件。但目前 PCH 缓存的查找仍以文件的 path_id 为键（每个文件独立缓存），尚未实现跨文件的缓存共享。将缓存键改为内容哈希以实现真正的跨文件 PCH 共享是一个计划中的改进。</p><h3 id="编译上下文与索引的交互" tabindex="-1">编译上下文与索引的交互 <a class="header-anchor" href="#编译上下文与索引的交互" aria-label="Permalink to &quot;编译上下文与索引的交互&quot;">​</a></h3><p>索引系统对编译上下文有天然的感知。同一个头文件在不同的编译上下文下可能产生不同的符号关系（例如，某个函数可能在一个上下文中通过条件编译被定义，而在另一个上下文中不存在）。MergedIndex 将同一文件在不同编译上下文下产生的索引数据合并存储，使得用户可以在一个统一的视图中查询跨上下文的符号信息。</p><h2 id="核心依赖" tabindex="-1">核心依赖 <a class="header-anchor" href="#核心依赖" aria-label="Permalink to &quot;核心依赖&quot;">​</a></h2><ul><li><strong>DependencyGraph</strong>：提供 include 关系的正向和反向查询，是确定宿主源文件和 include 链的基础</li><li><strong>CompilationDatabase</strong>：提供源文件的编译命令</li><li><strong>PathPool</strong>：文件路径的内部化标识</li><li><strong>Session</strong>：存储每个打开文件的编译上下文状态（<code>header_context</code>、<code>active_context</code>）</li><li><strong>Workspace</strong>：存储 PCH 缓存，供多个 Session 共享</li></ul><h2 id="设计决策与权衡" tabindex="-1">设计决策与权衡 <a class="header-anchor" href="#设计决策与权衡" aria-label="Permalink to &quot;设计决策与权衡&quot;">​</a></h2><p><strong>为什么用 <code>-include</code> 注入而不是修改文件内容？</strong> 使用 <code>-include</code> 标志可以让 Clang 自然地处理前缀代码，不需要修改目标文件的缓冲区。这保持了文件内容的一致性，同时 <code>#line</code> 指令确保了诊断信息指向正确的原始文件和行号。</p><p><strong>为什么不使用&quot;编译宿主源文件然后在 include 处停止&quot;的方案？</strong> 这个方案（即直接编译宿主源文件，在解析到目标头文件时设置 hook 停止）虽然概念上更简单，但存在一个根本性问题：它不支持 PCH 优化。由于是以宿主源文件为编译主体，很难细粒度地做 preamble 分割和 PCH 缓存。而对于频繁编辑的文件，PCH 缓存是性能的关键。</p><p><strong>为什么选择最短 include 链？</strong> 多条可能的 include 链中，最短的链产生最少的前缀代码，意味着更小的 PCH、更快的编译。同时，更短的链通常意味着更直接的包含关系，选择它作为默认上下文更符合直觉。</p><h2 id="已知限制" tabindex="-1">已知限制 <a class="header-anchor" href="#已知限制" aria-label="Permalink to &quot;已知限制&quot;">​</a></h2><ul><li><strong>递归包含</strong>：如果 include 图中存在循环（通过 include guard 或 <code>#pragma once</code> 打断），前缀合成需要正确处理去重。目前的依赖扫描已经处理了循环，但前缀合成路径尚未完全验证这一场景。</li><li><strong>宿主选择的确定性</strong>：当多个宿主源文件可用时，当前选择第一个找到的。更好的策略可能是优先选择与目标头文件&quot;关系最近&quot;的宿主（例如同目录下的源文件），但这需要启发式规则。</li></ul>`,54)])])}const k=s(t,[["render",l]]);export{g as __pageData,k as default};
