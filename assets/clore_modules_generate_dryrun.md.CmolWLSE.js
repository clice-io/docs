import{_ as a,o as t,c as o,a3 as r}from"./chunks/framework.aYYe0dfI.js";const p=JSON.parse('{"title":"Module generate:dryrun","description":"The generate:dryrun module simulates a documentation generation pass without producing final output, enabling accurate estimation of the LLM request counts and resource costs required for full generation. It implements the dry‑run logic by analyzing a PagePlanSet and a configuration to compute a RequestEstimate that breaks down anticipated requests by category (module summaries, symbol analyses, implementation summaries, etc.), and then assembles summary texts and structured LLMs‑compatible pages that present the estimate to the user.","frontmatter":{"title":"Module generate:dryrun","description":"The generate:dryrun module simulates a documentation generation pass without producing final output, enabling accurate estimation of the LLM request counts and resource costs required for full generation. It implements the dry‑run logic by analyzing a PagePlanSet and a configuration to compute a RequestEstimate that breaks down anticipated requests by category (module summaries, symbol analyses, implementation summaries, etc.), and then assembles summary texts and structured LLMs‑compatible pages that present the estimate to the user.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/generate/dryrun.md","filePath":"en/clore/modules/generate/dryrun.md"}'),n={name:"clore/modules/generate/dryrun.md"};function c(d,e,s,i,l,u){return t(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="module-generate-dryrun" tabindex="-1">Module <code>generate:dryrun</code> <a class="header-anchor" href="#module-generate-dryrun" aria-label="Permalink to &quot;Module `generate:dryrun`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>generate:dryrun</code> module simulates a documentation generation pass without producing final output, enabling accurate estimation of the LLM request counts and resource costs required for full generation. It implements the dry‑run logic by analyzing a <code>PagePlanSet</code> and a configuration to compute a <code>RequestEstimate</code> that breaks down anticipated requests by category (module summaries, symbol analyses, implementation summaries, etc.), and then assembles summary texts and structured <code>LLMs</code>‑compatible pages that present the estimate to the user.</p><p>The public‑facing implementation scope includes four entry points: <code>build_dry_run_page_summary_texts</code>, <code>build_request_estimate_page</code>, <code>build_llms_page</code>, and <code>page_summary_cache_key_for_request</code>. These functions accept page plans or request identifiers (passed as <code>const int &amp;</code> references) and return integer status codes or cache keys. The module owns all logic for tallying prompt use, generating labeled sections for the <code>LLMs</code> output, deriving project names from configuration, and producing fallback summaries when primary results are unavailable.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../config/"><code>config</code></a></li><li><a href="./../extract/"><code>extract</code></a></li><li><a href="./analysis"><code>generate:analysis</code></a></li><li><a href="./model"><code>generate:model</code></a></li><li><code>std</code></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./scheduler"><code>generate:scheduler</code></a></li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-generate-build-dry-run-page-summary-texts" tabindex="-1"><code>clore::generate::build_dry_run_page_summary_texts</code> <a class="header-anchor" href="#clore-generate-build-dry-run-page-summary-texts" aria-label="Permalink to &quot;`clore::generate::build_dry_run_page_summary_texts`&quot;">​</a></h3><p>Declaration: <code>generate/dryrun.cppm:11</code></p><p>Definition: <code>generate/dryrun.cppm:316</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function iterates over each <code>PromptRequest</code> in <code>prompt_requests</code> and filters out those for which <code>page_summary_cache_key_for_request</code> does not yield a value (i.e., no previously computed cache key). For each remaining request, it invokes <code>fallback_page_summary_for_request</code> to obtain a dry‑run summary string; if the result is non‑empty, it stores the summary in the output map keyed by <code>prompt_request_key(request)</code>. The core logic relies on two dependency functions: <code>page_summary_cache_key_for_request</code> determines whether a request is eligible for a dry‑run summary, and <code>fallback_page_summary_for_request</code> produces the actual text. The function returns a mapping from request keys to their summary texts, skipping any request that lacks a cache key or produces an empty summary.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>plan</code> (const reference to <code>PagePlan</code>)</li><li><code>prompt_requests</code> (const reference to <code>std::vector&lt;PromptRequest&gt;</code>)</li><li><code>page_summary_cache_key_for_request(plan, request)</code> return value</li><li><code>fallback_page_summary_for_request(plan, request)</code> return value</li><li><code>prompt_request_key(request)</code> return value</li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>local variable <code>summary_texts</code> (type <code>std::unordered_map&lt;std::string, std::string&gt;</code>) which is returned</li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during dry-run page generation to pre-populate summary texts without real LLM inference</li><li>Used to provide fallback summaries for prompt requests in a dry run context</li></ul><h3 id="clore-generate-build-llms-page" tabindex="-1"><code>clore::generate::build_llms_page</code> <a class="header-anchor" href="#clore-generate-build-llms-page" aria-label="Permalink to &quot;`clore::generate::build_llms_page`&quot;">​</a></h3><p>Declaration: <code>generate/dryrun.cppm:19</code></p><p>Definition: <code>generate/dryrun.cppm:333</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::build_llms_page</code> assembles a machine‑readable index page (<code>llms.txt</code>) for the generated C++ reference. It first retrieves the project name via <code>project_name_from_config(config)</code> and builds a Markdown string containing a top‑level heading, a brief description, and a link to the API reference index. If <code>request_estimate_path</code> is non‑empty, it also appends a link to the dry run request estimate page. The core algorithm then delegates to three calls of <code>append_llms_section</code>, each passing the shared <code>plan_set</code> and a distinct <code>PageType</code> (Module, Namespace, File) to produce labeled subsections with their entry lists. The final <code>GeneratedPage</code> is returned with the constructed <code>content</code>, the project name as the title, and the fixed relative path <code>&quot;llms.txt&quot;</code>.</p><p>Internally, the function depends on several local helpers and types: <code>project_name_from_config</code> for the project name, <code>append_llms_section</code> (defined in the anonymous namespace) to format each section, and <code>PagePlanSet</code>, <code>config::TaskConfig</code>, <code>PageType</code>, and <code>GeneratedPage</code> from the surrounding module. The control flow is purely sequential, reserving an initial capacity of 2048 bytes for the <code>content</code> string to reduce reallocations.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>plan_set</code></li><li><code>config</code></li><li><code>request_estimate_path</code></li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>the returned <code>GeneratedPage</code> content</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called during documentation generation to produce the llms<code>.txt</code> page</li><li>used as part of the page generation pipeline</li></ul><h3 id="clore-generate-build-request-estimate-page" tabindex="-1"><code>clore::generate::build_request_estimate_page</code> <a class="header-anchor" href="#clore-generate-build-request-estimate-page" aria-label="Permalink to &quot;`clore::generate::build_request_estimate_page`&quot;">​</a></h3><p>Declaration: <code>generate/dryrun.cppm:15</code></p><p>Definition: <code>generate/dryrun.cppm:230</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function first calls <code>estimate_request_count</code> with the <code>plan_set</code> and <code>model</code> parameters to obtain a <code>RequestEstimate</code> containing the breakdown of prompt tasks. It then retrieves the project name by invoking <code>project_name_from_config</code> on the <code>config</code>. A <code>std::string</code> named <code>content</code> is pre-allocated with a capacity of 2048 and built sequentially: frontmatter (title, description, layout, <code>page_template</code>) followed by Markdown body that uses <code>std::format</code> to embed every integer field from the estimate—such as <code>total_requests</code>, <code>page_requests</code>, <code>symbol_requests</code>, <code>namespace_page_requests</code>, <code>module_summary_requests</code>, subtotals for function/type/variable symbols and their associated analysis and summary request counts—into explanatory lines. No conditional branches or loops are present; the control flow is purely a linear sequence of appending formatted strings. Finally, the assembled content is moved into a <code>GeneratedPage</code> struct with a fixed title (&quot;Dry Run Request Estimate&quot;) and relative path (&quot;request-estimate<code>.md</code>&quot;) and returned.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>const clore::generate::PagePlanSet&amp; plan_set</code></li><li><code>const clore::extract::ProjectModel&amp; model</code></li><li><code>const clore::config::TaskConfig&amp; config</code></li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Returns a <code>clore::generate::GeneratedPage</code> object</li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during dry-run page generation to produce a request estimate page</li><li>Used to inform users about the number of prompt tasks before actual execution</li></ul><h3 id="clore-generate-page-summary-cache-key-for-request" tabindex="-1"><code>clore::generate::page_summary_cache_key_for_request</code> <a class="header-anchor" href="#clore-generate-page-summary-cache-key-for-request" aria-label="Permalink to &quot;`clore::generate::page_summary_cache_key_for_request`&quot;">​</a></h3><p>Declaration: <code>generate/dryrun.cppm:23</code></p><p>Definition: <code>generate/dryrun.cppm:293</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>The function <code>clore::generate::page_summary_cache_key_for_request</code> computes an optional cache key string based on the kind of a <code>PromptRequest</code> and the associated <code>PagePlan</code>. Internally, it switches on <code>request.kind</code>. For <code>PromptKind::NamespaceSummary</code> and <code>PromptKind::ModuleSummary</code>, if <code>plan.owner_keys</code> is non-empty, the function returns the first element of that container; otherwise it yields <code>std::nullopt</code>. For all other enumerated kinds listed in the switch—<code>ModuleArchitecture</code>, <code>IndexOverview</code>, <code>FunctionAnalysis</code>, <code>TypeAnalysis</code>, <code>VariableAnalysis</code>, <code>FunctionDeclarationSummary</code>, <code>FunctionImplementationSummary</code>, <code>TypeDeclarationSummary</code>, and <code>TypeImplementationSummary</code>—the function immediately returns <code>std::nullopt</code>. A final fallback return outside the switch also produces <code>std::nullopt</code>, ensuring that any unlisted or default prompt kind also results in no cache key. The only dependencies are the <code>PagePlan</code> type (specifically its <code>owner_keys</code> member) and the <code>PromptRequest</code>&#39;s <code>kind</code> field; no external state or additional queries are involved.</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>request.kind</code></li><li><code>plan.owner_keys</code></li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>generates cache key for page summaries</li><li>called when constructing cache entries for dry-run or generation</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>generate:dryrun</code> module is decomposed into a small set of public entry points and a collection of private helpers in an anonymous namespace. The public surface comprises <code>build_dry_run_page_summary_texts</code>, <code>build_request_estimate_page</code>, <code>build_llms_page</code>, and <code>page_summary_cache_key_for_request</code>, each returning an <code>int</code> result. Internally, the module relies on the helper struct <code>RequestEstimate</code> to aggregate counts of various request types (module, function, type, variable, index, namespace), and on functions such as <code>estimate_request_count</code>, <code>append_llms_section</code>, <code>llms_entry_label</code>, <code>fallback_page_summary_for_request</code>, and <code>project_name_from_config</code> to compute estimates, format LLM-specific pages, and derive project metadata. These private utilities operate over <code>plan</code>, <code>plan_set</code>, <code>config</code>, and <code>estimate</code> variables, and build summary content in <code>content</code> strings and <code>LabeledPage</code> structures.</p><p>The module imports <code>config</code>, <code>extract</code>, <code>generate:analysis</code>, and <code>generate:model</code> to obtain project configuration, extraction results, analysis data, and core page/model types. Its internal layering separates estimation logic from page-construction and summary-text generation, with the anonymous namespace handling all private computation while the exported functions provide a narrow, stable interface to the rest of the <code>generate</code> subsystem.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../config/">Module config</a></li><li><a href="./../extract/">Module extract</a></li><li><a href="./analysis">Module generate:analysis</a></li><li><a href="./model">Module generate:model</a></li></ul>',65)])])}const h=a(n,[["render",c]]);export{p as __pageData,h as default};
