import{_ as o,o as a,c,a3 as d}from"./chunks/framework.aYYe0dfI.js";const p=JSON.parse('{"title":"Module extract:merge","description":"该模块负责将多个来源的符号提取结果合并到统一的模型表示中。它公开发布了 merge_symbol_info、rebuild_model_indexes 和 rebuild_lookup_maps 等核心函数，用于将传入的符号信息整合到目标模型内，并重建内部索引与查找映射以保持查询逻辑的一致性。此外，模块还提供了 append_unique、append_unique_range 和 deduplicate 等辅助模板变量，用于处理容器中元素的去重追加，以及 LocalIndexData 结构体以支持并行环境下的局部索引构建。整体上，该模块是提取流水线的关键组成部分，在 config、extract:filter 与 extract:model 等模块的基础上，实现了从原始提取数据到可查询模型状态的转换。","frontmatter":{"title":"Module extract:merge","description":"该模块负责将多个来源的符号提取结果合并到统一的模型表示中。它公开发布了 merge_symbol_info、rebuild_model_indexes 和 rebuild_lookup_maps 等核心函数，用于将传入的符号信息整合到目标模型内，并重建内部索引与查找映射以保持查询逻辑的一致性。此外，模块还提供了 append_unique、append_unique_range 和 deduplicate 等辅助模板变量，用于处理容器中元素的去重追加，以及 LocalIndexData 结构体以支持并行环境下的局部索引构建。整体上，该模块是提取流水线的关键组成部分，在 config、extract:filter 与 extract:model 等模块的基础上，实现了从原始提取数据到可查询模型状态的转换。","layout":"doc","template":"doc"},"headers":[],"relativePath":"zh/clore/modules/extract/merge.md","filePath":"zh/clore/modules/extract/merge.md"}'),r={name:"zh/clore/modules/extract/merge.md"};function t(l,e,i,n,s,u){return a(),c("div",null,[...e[0]||(e[0]=[d('<h1 id="module-extract-merge" tabindex="-1">Module <code>extract:merge</code> <a class="header-anchor" href="#module-extract-merge" aria-label="Permalink to &quot;Module `extract:merge`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>该模块负责将多个来源的符号提取结果合并到统一的模型表示中。它公开发布了 <code>merge_symbol_info</code>、<code>rebuild_model_indexes</code> 和 <code>rebuild_lookup_maps</code> 等核心函数，用于将传入的符号信息整合到目标模型内，并重建内部索引与查找映射以保持查询逻辑的一致性。此外，模块还提供了 <code>append_unique</code>、<code>append_unique_range</code> 和 <code>deduplicate</code> 等辅助模板变量，用于处理容器中元素的去重追加，以及 <code>LocalIndexData</code> 结构体以支持并行环境下的局部索引构建。整体上，该模块是提取流水线的关键组成部分，在 <code>config</code>、<code>extract:filter</code> 与 <code>extract:model</code> 等模块的基础上，实现了从原始提取数据到可查询模型状态的转换。</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../config/"><code>config</code></a></li><li><a href="./filter"><code>extract:filter</code></a></li><li><a href="./model"><code>extract:model</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="variables" tabindex="-1">Variables <a class="header-anchor" href="#variables" aria-label="Permalink to &quot;Variables&quot;">​</a></h2><h3 id="clore-extract-append-unique" tabindex="-1"><code>clore::extract::append_unique</code> <a class="header-anchor" href="#clore-extract-append-unique" aria-label="Permalink to &quot;`clore::extract::append_unique`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:12</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The variable <code>clore::extract::append_unique</code> appears in the context of symbol merging and deduplication within the <code>clore::extract</code> module. It is referenced as a standalone entity; however, the provided evidence does not describe its initialization, mutation, or specific usage patterns. It likely serves as a helper or policy object for appending unique elements, but no direct consumption is observed in the snippets.</p><h4 id="mutation" tabindex="-1">Mutation <a class="header-anchor" href="#mutation" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h3 id="clore-extract-append-unique-range" tabindex="-1"><code>clore::extract::append_unique_range</code> <a class="header-anchor" href="#clore-extract-append-unique-range" aria-label="Permalink to &quot;`clore::extract::append_unique_range`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:19</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>The variable&#39;s role cannot be determined from the provided evidence. It may be related to appending unique elements to a range during merge operations, but no initialization, mutation, or usage is documented.</p><h4 id="mutation-1" tabindex="-1">Mutation <a class="header-anchor" href="#mutation-1" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h3 id="clore-extract-deduplicate" tabindex="-1"><code>clore::extract::deduplicate</code> <a class="header-anchor" href="#clore-extract-deduplicate" aria-label="Permalink to &quot;`clore::extract::deduplicate`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:49</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>{ &quot;<code>overview_markdown</code>&quot;: &quot;A template function <code>deduplicate</code> is declared as <code>void deduplicate</code>.&quot;, &quot;<code>details_markdown</code>&quot;: &quot;No additional details are available from the evidence.&quot;, &quot;<code>is_mutated</code>&quot;: false, &quot;<code>mutation_sources</code>&quot;: &quot;&quot;, &quot;<code>usage_patterns</code>&quot;: &quot;&quot; }</p><h4 id="mutation-2" tabindex="-1">Mutation <a class="header-anchor" href="#mutation-2" aria-label="Permalink to &quot;Mutation&quot;">​</a></h4><p>No mutation is evident from the extracted code.</p><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-extract-merge-symbol-info" tabindex="-1"><code>clore::extract::merge_symbol_info</code> <a class="header-anchor" href="#clore-extract-merge-symbol-info" aria-label="Permalink to &quot;`clore::extract::merge_symbol_info`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:55</code></p><p>Definition: <code>extract/merge.cppm:215</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p><code>clore::extract::merge_symbol_info</code> 的实现主要委托给 <code>merge_symbol_info_impl</code>，后者是实际合并逻辑的所在。该函数将传入的符号信息 <code>incoming</code> 合并到当前模型 <code>model</code> 的符号 <code>current</code> 中，处理符号属性（如名称、定义、代码片段）的并集或覆盖，并依据 <code>prefer_incoming_snippet</code> 和 <code>prefer_incoming_definition</code> 等配置决定优先级。合并过程中会调用 <code>ensure_namespace_hierarchy</code> 和 <code>find_enclosing_namespace</code> 来维护命名空间层级结构，利用 <code>append_unique</code>、<code>append_unique_range</code> 和 <code>deduplicate</code> 去重和追加数据。在多线程场景下，<code>merge_symbol_info_impl</code> 通过 <code>run_parallel_chunks</code> 和 <code>per_thread</code> 结构并行处理，并在合并完成后触发 <code>rebuild_model_indexes</code> 和 <code>rebuild_lookup_maps</code> 重建模型的索引和查找表，确保一致性。</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Modifies <code>current</code> by merging data from <code>incoming</code>.</li></ul><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>current</code> (also written)</li><li><code>incoming</code> (read only)</li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>current</code> (mutated)</li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Updating an existing symbol info with data from another symbol info.</li><li>Consolidating symbol information from multiple sources.</li></ul><h3 id="clore-extract-merge-symbol-info-1" tabindex="-1"><code>clore::extract::merge_symbol_info</code> <a class="header-anchor" href="#clore-extract-merge-symbol-info-1" aria-label="Permalink to &quot;`clore::extract::merge_symbol_info`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:54</code></p><p>Definition: <code>extract/merge.cppm:211</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>该函数是 <code>clore::extract</code> 命名空间中符号信息合并的核心入口。它接收一个当前符号信息引用 <code>current</code> 和一个临时符号信息对象 <code>incoming</code>，通过 <code>std::move</code> 将 <code>incoming</code> 的所有权转移后，立即委托给内部实现函数 <code>clore::extract::(anonymous namespace)::merge_symbol_info_impl</code>。后者是一个模板函数，依据配置选项（如 <code>prefer_incoming_snippet</code> 和 <code>prefer_incoming_definition</code>）决定冲突解决策略，并通过 <code>append_unique</code> 和 <code>append_unique_range</code> 维护符号项的唯一性。</p><p>在实现过程中，<code>merge_symbol_info_impl</code> 会调用 <code>ensure_namespace_hierarchy</code> 和 <code>find_enclosing_namespace</code> 以确保命名空间层级结构的一致性，同时利用 <code>run_parallel_chunks</code> 将工作分派至多个线程（基于 <code>hardware_threads</code> 和 <code>num_threads</code>）。合并完成后，会触发 <code>rebuild_model_indexes</code> 或 <code>rebuild_lookup_maps</code> 以更新模型索引，保证后续查询的正确性。整个流程依赖于 <code>LocalIndexData</code> 结构内的 <code>explicit_namespaces</code> 和 <code>known_namespace_names</code> 等中间数据。</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>修改了 <code>current</code> 参数所引用的 <code>SymbolInfo</code> 对象，其内容被来自 <code>incoming</code> 的数据合并更新。</li></ul><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>current</code> 参数（作为合并的目标，其原有数据被读取以参与合并）</li><li><code>incoming</code> 参数（其资源通过移动操作被窃取）</li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>current</code> 参数所引用的 <code>SymbolInfo</code> 对象（被修改以包含合并后的数据）</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>在符号提取过程中，用于将新解析的符号信息合并到已有的符号信息结构中</li><li>作为高效更新接口，利用右值引用避免拷贝开销</li></ul><h3 id="clore-extract-rebuild-lookup-maps" tabindex="-1"><code>clore::extract::rebuild_lookup_maps</code> <a class="header-anchor" href="#clore-extract-rebuild-lookup-maps" aria-label="Permalink to &quot;`clore::extract::rebuild_lookup_maps`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:59</code></p><p>Definition: <code>extract/merge.cppm:428</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>函数 <code>clore::extract::rebuild_lookup_maps</code> 首先清空 <code>model</code> 中的 <code>symbol_ids_by_qualified_name</code> 与 <code>module_name_to_sources</code> 两个查找映射。随后遍历 <code>model.symbols</code>，对每个拥有非空 <code>qualified_name</code> 的符号，将 <code>symbol_id</code> 追加到对应限定名的列表中。接着对每个限定名下的 <code>symbol_ids</code> 执行稳定排序，比较依据依次为 <code>signature</code>、<code>declaration_location.file</code>、<code>declaration_location.line</code> 以及 <code>SymbolID</code> 自身，再调用 <code>std::unique</code> 移除连续重复，若列表长度大于 1 则通过 <code>logging::info</code> 报告重载候选数。</p><p>之后遍历 <code>model.modules</code>，将每个非空 <code>name</code> 对应的源文件追加至 <code>module_name_to_sources</code> 列表，再对每个模块名称下的 <code>sources</code> 排序并去重。若检测到同一模块名称存在超过一个接口源文件（<code>is_interface</code> 为真），则调用 <code>logging::warn</code> 输出重复接口警告，包含前两个接口文件的路径。该函数不依赖外部并行工具或复杂数据结构，完全基于 <code>ProjectModel</code> 内部的容器（<code>std::unordered_map</code> 和 <code>std::vector</code>）与标准库算法实现。</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>modifies <code>model.symbol_ids_by_qualified_name</code></li><li>modifies <code>model.module_name_to_sources</code></li><li>allocates memory for container elements</li><li>logs info messages about overload candidates</li><li>logs warning messages about duplicate module interfaces</li></ul><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>model.symbols</code></li><li><code>model.modules</code></li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>model.symbol_ids_by_qualified_name</code></li><li><code>model.module_name_to_sources</code></li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called after symbol extraction to update lookup structures</li><li>called to prepare the model for subsequent lookups by qualified name or module name</li></ul><h3 id="clore-extract-rebuild-model-indexes" tabindex="-1"><code>clore::extract::rebuild_model_indexes</code> <a class="header-anchor" href="#clore-extract-rebuild-model-indexes" aria-label="Permalink to &quot;`clore::extract::rebuild_model_indexes`&quot;">​</a></h3><p>Declaration: <code>extract/merge.cppm:57</code></p><p>Definition: <code>extract/merge.cppm:219</code></p><p>Declaration: <a href="./../../namespaces/clore/extract/"><code>Namespace clore::extract</code></a></p><p>该函数首先重置 <code>model</code> 中的索引数据：清空所有文件的 <code>symbols</code> 与 <code>includes</code>，清空 <code>model.namespaces</code>，并对每个符号的 <code>calls</code>、<code>references</code>、<code>derived</code>、<code>called_by</code>、<code>referenced_by</code> 执行 <code>deduplicate</code> 操作，同时清空 <code>children</code> 字段。随后，通过并行分块处理所有符号，为每个符号计算其在文件系统上的归属路径（结合 <code>config.filter</code> 和 <code>filter_root_path</code> 过滤），若匹配则将其 <code>SymbolID</code> 加入 <code>file_symbols</code> 映射；若非命名空间符号，则利用 <code>find_enclosing_namespace</code> 推断其所在的命名空间名并存入 <code>namespace_symbols</code>；若符号有父符号，则记录 <code>parent_children</code> 对。同时，显式声明的命名空间名被收集到 <code>explicit_namespaces</code> 中。</p><p>各线程的局部结果通过 <code>LocalIndexData</code> 结构聚合后，依次填充 <code>model.files</code> 的文件符号列表、通过 <code>ensure_namespace_hierarchy</code> 建立命名空间层级并将符号加入 <code>model.namespaces</code>，最后利用 <code>parent_children</code> 重建符号间的父子关系。整个过程中，多次调用 <code>run_parallel_chunks</code> 进行并发去重（对文件符号、命名空间符号及子项、符号的 <code>children</code> 和 <code>derived</code>），依赖 <code>deduplicate</code> 和基于 <code>std::thread</code> 的并行调度机制。</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Modifies the input <code>ProjectModel</code> by clearing and repopulating all index fields (file symbols, namespace symbols, symbol children, derived, etc.)</li><li>Creates threads for parallel execution via <code>std::thread</code></li><li>Calls <code>std::thread::hardware_concurrency</code> to determine number of threads</li></ul><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>config</code> parameter: <code>config.filter</code> and result of <code>filter_root_path(config)</code></li><li><code>model</code> parameter: <code>model.files</code>, <code>model.symbols</code>, <code>model.namespaces</code></li><li>Symbol fields: <code>kind</code>, <code>qualified_name</code>, <code>declaration_location.file</code>, <code>parent</code>, <code>calls</code>, <code>references</code>, <code>derived</code>, <code>called_by</code>, <code>referenced_by</code></li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li><code>model.files</code>: clears and repopulates <code>symbols</code> and sets <code>path</code> for each file entry</li><li><code>model.namespaces</code>: cleared then repopulated with namespace info</li><li><code>model.symbols</code>: clears and repopulates <code>children</code>, <code>derived</code>, <code>calls</code>, <code>references</code>, <code>called_by</code>, <code>referenced_by</code> (deduplicates)</li><li>Local data structures merged into global maps</li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called after merging symbol data to rebuild all derived indexes</li><li>Part of the extraction pipeline to ensure model consistency</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>extract:merge 模块实现了将提取的符号信息合并到统一模型的核心逻辑，分解为公共接口与内部辅助函数两层。公共接口包括 <code>merge_symbol_info</code>、<code>rebuild_model_indexes</code> 和 <code>rebuild_lookup_maps</code>，分别负责符号数据的融合、索引重建及查找映射刷新。内部实现借助匿名命名空间中的 <code>merge_symbol_info_impl</code>、<code>ensure_namespace_hierarchy</code> 和 <code>find_enclosing_namespace</code> 完成细节处理，并引入 <code>LocalIndexData</code> 结构管理每线程局部索引状态，支持基于硬件线程数的并行分块处理。模块直接导入 <code>extract:model</code>、<code>extract:filter</code>、<code>config</code> 和 <code>support</code> 等依赖，形成自底向上的数据流：原始符号经过过滤后，由合并逻辑整合到模型中，再重建索引以供后续查询。</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../config/">Module config</a></li><li><a href="./filter">Module extract:filter</a></li><li><a href="./model">Module extract:model</a></li><li><a href="./../support/">Module support</a></li></ul>',84)])])}const h=o(r,[["render",t]]);export{p as __pageData,h as default};
