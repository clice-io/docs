import{_ as a,o as t,c as i,a3 as n}from"./chunks/framework.CRbjHCyB.js";const k=JSON.parse('{"title":"IPC 协议","description":"","frontmatter":{},"headers":[],"relativePath":"zh/catter/design/ipc-protocol.md","filePath":"zh/catter/design/ipc-protocol.md"}'),e={name:"zh/catter/design/ipc-protocol.md"};function d(p,s,l,h,o,r){return t(),i("div",null,[...s[0]||(s[0]=[n(`<h1 id="ipc-协议" tabindex="-1">IPC 协议 <a class="header-anchor" href="#ipc-协议" aria-label="Permalink to &quot;IPC 协议&quot;">​</a></h1><p>Catter 使用进程间通信在守护进程（<code>catter</code>）和代理实例（<code>catter-proxy</code>）之间传递信息。守护进程作为服务端，每个代理实例作为客户端连接。</p><h2 id="传输层" tabindex="-1">传输层 <a class="header-anchor" href="#传输层" aria-label="Permalink to &quot;传输层&quot;">​</a></h2><p>传输层的实现因平台而异：</p><table tabindex="0"><thead><tr><th>平台</th><th>机制</th><th>路径 / 名称</th></tr></thead><tbody><tr><td>Linux / macOS</td><td>Unix 域套接字</td><td><code>$XDG_DATA_HOME/pipe-catter-ipc.sock</code>（通常为 <code>~/.local/share/pipe-catter-ipc.sock</code>）</td></tr><tr><td>Windows</td><td>命名管道</td><td><code>\\\\.\\pipe\\catter-ipc</code></td></tr></tbody></table><p>守护进程在启动时创建监听套接字/管道。每个 <code>catter-proxy</code> 实例启动时作为客户端连接。连接在代理进程的整个生命周期内持续存在。</p><h2 id="序列化" tabindex="-1">序列化 <a class="header-anchor" href="#序列化" aria-label="Permalink to &quot;序列化&quot;">​</a></h2><p>所有消息使用 <a href="https://github.com/bincode-org/bincode" target="_blank" rel="noreferrer">Bincode</a> 编码，这是一种紧凑的二进制序列化格式。实现使用 <code>kota::ipc::BincodePeer</code>，它在流传输（<code>kota::ipc::StreamTransport</code>）之上提供了请求 - 响应抽象。</p><p>选择 Bincode 是因为其高效性 -- 消息体积小、编解码速度快。这一点很重要，因为每个被拦截的构建命令都会导致多次 IPC 往返。</p><h2 id="请求-响应模型" tabindex="-1">请求 - 响应模型 <a class="header-anchor" href="#请求-响应模型" aria-label="Permalink to &quot;请求 - 响应模型&quot;">​</a></h2><p>通信遵循严格的请求 - 响应模式。客户端（代理）发送请求并等待服务端（守护进程）响应后才继续执行。每种请求类型都有明确定义的参数类型和结果类型。</p><p>协议在 <code>src/common/util/data.h</code> 中使用 C++ 模板特化定义：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">enum</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RequestType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> : </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint8_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    CHECK_MODE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    CREATE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    MAKE_DECISION</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    REPORT_ERROR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    FINISH</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>每种请求类型映射到一个 <code>Request&lt;Type&gt;</code> 特化，声明 <code>Params</code>（请求负载）和 <code>Result</code>（响应负载）。</p><h2 id="请求类型" tabindex="-1">请求类型 <a class="header-anchor" href="#请求类型" aria-label="Permalink to &quot;请求类型&quot;">​</a></h2><h3 id="check-mode" tabindex="-1">CHECK_MODE <a class="header-anchor" href="#check-mode" aria-label="Permalink to &quot;CHECK_MODE&quot;">​</a></h3><p>查询守护进程当前活动的服务模式。</p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><strong>Params</strong></td><td><code>ServiceMode</code>（枚举）</td><td>要检查的模式（当前仅有 <code>INJECT</code>）</td></tr><tr><td><strong>Result</strong></td><td><code>bool</code></td><td>如果守护进程处于请求的模式则返回 <code>true</code></td></tr></tbody></table><p>这是代理连接后发送的第一个请求。它确认守护进程已准备好处理被拦截的命令。</p><h3 id="create" tabindex="-1">CREATE <a class="header-anchor" href="#create" aria-label="Permalink to &quot;CREATE&quot;">​</a></h3><p>在守护进程中注册一个新的进程会话。</p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><strong>Params</strong></td><td><code>ipcid_t</code>（<code>int32_t</code>）</td><td>父进程会话 ID</td></tr><tr><td><strong>Result</strong></td><td><code>ipcid_t</code>（<code>int32_t</code>）</td><td>分配给此代理实例的唯一会话 ID</td></tr></tbody></table><p>父 ID 在会话树中建立父子关系。对于第一个代理（注入模式），父 ID 由守护进程在生成代理时提供。对于包装模式的代理，父 ID 来自 <code>__key_catter_command_id_v1</code> 环境变量（Windows 上为 <code>CATTER_IPC_ID</code>），由钩子设置。</p><h3 id="make-decision" tabindex="-1">MAKE_DECISION <a class="header-anchor" href="#make-decision" aria-label="Permalink to &quot;MAKE_DECISION&quot;">​</a></h3><p>核心请求。代理发送捕获到的命令，守护进程决定如何处理。</p><p><strong>Params</strong> -- <code>command</code>：</p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>cwd</code></td><td><code>string</code></td><td>被拦截进程的工作目录</td></tr><tr><td><code>executable</code></td><td><code>string</code></td><td>已解析的可执行文件绝对路径</td></tr><tr><td><code>args</code></td><td><code>string[]</code></td><td>完整参数数组（包含 <code>argv[0]</code>）</td></tr><tr><td><code>env</code></td><td><code>string[]</code></td><td>环境变量（<code>KEY=VALUE</code> 格式）</td></tr></tbody></table><p><strong>Result</strong> -- <code>action</code>：</p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>type</code></td><td><code>uint8_t</code> 枚举</td><td><code>DROP</code>、<code>INJECT</code> 或 <code>WRAP</code> 之一</td></tr><tr><td><code>cmd</code></td><td><code>command</code></td><td>要执行的命令（可能已被脚本修改）</td></tr></tbody></table><p><strong>动作类型</strong>：</p><ul><li><strong><code>DROP</code>（0）</strong> -- 不执行命令。代理立即返回退出码 0。用于脚本判定命令无关紧要的情况（例如用户想跳过的编译器调用）。</li><li><strong><code>INJECT</code>（1）</strong> -- 挂载钩子库后执行命令。代理重新添加 <code>LD_PRELOAD</code>（或在 Windows 上执行 DLL 注入），使此命令的子进程也被拦截。这是需要监控子进程的构建命令的默认动作。</li><li><strong><code>WRAP</code>（2）</strong> -- 直接执行命令，不挂载钩子。代理运行命令并捕获标准输出/标准错误，但不注入钩子。用于叶子命令（如实际的编译器调用），这些命令不会生成更多的构建子进程。</li></ul><p>守护进程可能会修改返回动作中的命令。例如，脚本可以更改编译器标志、重定向输出路径或替换可执行文件。</p><h3 id="report-error" tabindex="-1">REPORT_ERROR <a class="header-anchor" href="#report-error" aria-label="Permalink to &quot;REPORT_ERROR&quot;">​</a></h3><p>将来自钩子或代理的错误状况报告给守护进程。</p><p><strong>Params</strong>：</p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>parent_id</code></td><td><code>ipcid_t</code></td><td>父进程的会话 ID</td></tr><tr><td><code>error_msg</code></td><td><code>string</code></td><td>人类可读的错误描述</td></tr></tbody></table><p><strong>Result</strong>: <code>null</code>（无响应负载）</p><p>当钩子遇到无效状态（例如缺少环境变量）或代理在命令处理过程中捕获到异常时使用。守护进程记录错误并可通知用户。</p><h3 id="finish" tabindex="-1">FINISH <a class="header-anchor" href="#finish" aria-label="Permalink to &quot;FINISH&quot;">​</a></h3><p>报告命令执行完成。</p><p><strong>Params</strong> -- <code>process_result</code>：</p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>code</code></td><td><code>int64_t</code></td><td>进程退出码</td></tr><tr><td><code>std_out</code></td><td><code>string</code></td><td>捕获的标准输出</td></tr><tr><td><code>std_err</code></td><td><code>string</code></td><td>捕获的标准错误</td></tr></tbody></table><p><strong>Result</strong>: <code>null</code>（无响应负载）</p><p>守护进程收到此请求后，调用 <code>onExecution()</code> JavaScript 回调并传入结果数据。随后代理断开连接。</p><h2 id="典型消息序列" tabindex="-1">典型消息序列 <a class="header-anchor" href="#典型消息序列" aria-label="Permalink to &quot;典型消息序列&quot;">​</a></h2><p>一个完整的代理生命周期包含如下 IPC 消息序列：</p><h3 id="注入模式-第一个代理" tabindex="-1">注入模式（第一个代理） <a class="header-anchor" href="#注入模式-第一个代理" aria-label="Permalink to &quot;注入模式（第一个代理）&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>代理 -&gt; 守护进程:  CHECK_MODE(INJECT)</span></span>
<span class="line"><span>守护进程 -&gt; 代理:  true</span></span>
<span class="line"><span>[代理启动挂载了钩子的构建命令]</span></span>
<span class="line"><span>[代理等待构建完成]</span></span>
<span class="line"><span>[构建完成后代理退出]</span></span></code></pre></div><p>注入模式的代理同样会为顶层构建命令发送 <code>CREATE</code>、<code>MAKE_DECISION</code> 和 <code>FINISH</code>，因此构建系统命令本身也会像其他被拦截的命令一样经过 <code>onCommand</code>/<code>onExecution</code>。</p><h3 id="包装模式-被拦截的命令" tabindex="-1">包装模式（被拦截的命令） <a class="header-anchor" href="#包装模式-被拦截的命令" aria-label="Permalink to &quot;包装模式（被拦截的命令）&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>代理 -&gt; 守护进程:  CHECK_MODE(INJECT)</span></span>
<span class="line"><span>守护进程 -&gt; 代理:  true</span></span>
<span class="line"><span>代理 -&gt; 守护进程:  CREATE(parent_id)</span></span>
<span class="line"><span>守护进程 -&gt; 代理:  new_session_id</span></span>
<span class="line"><span>代理 -&gt; 守护进程:  MAKE_DECISION(command)</span></span>
<span class="line"><span>守护进程 -&gt; 代理:  action {type, cmd}</span></span>
<span class="line"><span>[代理执行或丢弃命令]</span></span>
<span class="line"><span>代理 -&gt; 守护进程:  FINISH(process_result)</span></span>
<span class="line"><span>守护进程 -&gt; 代理:  null</span></span>
<span class="line"><span>[代理断开连接]</span></span></code></pre></div><h3 id="错误情况" tabindex="-1">错误情况 <a class="header-anchor" href="#错误情况" aria-label="Permalink to &quot;错误情况&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>代理 -&gt; 守护进程:  REPORT_ERROR(parent_id, error_message)</span></span>
<span class="line"><span>守护进程 -&gt; 代理:  null</span></span>
<span class="line"><span>[代理以退出码 -1 退出]</span></span></code></pre></div><h2 id="会话模型" tabindex="-1">会话模型 <a class="header-anchor" href="#会话模型" aria-label="Permalink to &quot;会话模型&quot;">​</a></h2><p>守护进程维护一棵与构建进程树对应的会话树：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>会话 0（根 -- 注入模式代理）</span></span>
<span class="line"><span>  +-- 会话 1（make -&gt; gcc file1.c）</span></span>
<span class="line"><span>  +-- 会话 2（make -&gt; gcc file2.c）</span></span>
<span class="line"><span>  +-- 会话 3（make -&gt; ar rcs libfoo.a file1.o file2.o）</span></span>
<span class="line"><span>  +-- 会话 4（make -&gt; sh -c &quot;gcc file3.c&quot;）</span></span>
<span class="line"><span>       +-- 会话 5（sh -&gt; gcc file3.c）</span></span></code></pre></div><p>每个会话由唯一的 <code>ipcid_t</code>（32 位整数）标识。会话树随着 <code>CREATE</code> 请求的到达而增量构建，每个请求都指定了 <code>parent_id</code>。这种结构支持以下功能：</p><ul><li><strong>目标树重建</strong> -- 通过了解哪些目标文件链接到哪些目标</li><li><strong>构建性能分析</strong> -- 通过跟踪每个会话的计时数据并可视化并行度</li><li><strong>错误归因</strong> -- 通过追溯错误到引发错误的构建命令</li></ul><h2 id="连接生命周期" tabindex="-1">连接生命周期 <a class="header-anchor" href="#连接生命周期" aria-label="Permalink to &quot;连接生命周期&quot;">​</a></h2><ol><li><strong>守护进程启动</strong> -- 创建并绑定 Unix 域套接字或命名管道。</li><li><strong>代理连接</strong> -- 使用 <code>kota::pipe::connect()</code> 打开到套接字/管道的连接。</li><li><strong>守护进程接受连接</strong> -- 对于每个传入连接，守护进程创建一个 <code>kota::ipc::BincodePeer</code> 并为每种请求类型注册请求处理器（<code>on_request&lt;Request&lt;Type&gt;&gt;(...)</code>）。</li><li><strong>请求处理</strong> -- 守护进程的事件循环将传入请求分发到相应的处理器。处理器可以是异步的（使用 <code>co_await</code>）以与 JS 运行时或其他守护进程状态交互。</li><li><strong>对端断开</strong> -- 当代理进程退出时，传输层关闭，<code>BincodePeer::run()</code> 协程完成。守护进程记录断开事件并清理会话。</li></ol><h2 id="数据类型" tabindex="-1">数据类型 <a class="header-anchor" href="#数据类型" aria-label="Permalink to &quot;数据类型&quot;">​</a></h2><p>跨 IPC 边界使用的核心数据结构：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// IPC 会话的唯一标识符</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ipcid_t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int32_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 从构建系统捕获的命令</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> command</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string cwd;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                   // 工作目录</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string executable;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // 已解析的可执行文件路径</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::vector</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> args;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // 参数数组</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::vector</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> env;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // 环境变量（KEY=VALUE 条目）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 命令执行结果</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> process_result</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int64_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> code </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 // 退出码</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string std_out;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               // 捕获的标准输出</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string std_err;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               // 捕获的标准错误</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 守护进程返回的决策</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    enum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> : </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint8_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        DROP</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 不执行</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        INJECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 挂载钩子后执行</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        WRAP</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 不挂载钩子直接执行</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } type;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    command cmd;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                       // 可能已修改的命令</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div>`,63)])])}const g=a(e,[["render",d]]);export{k as __pageData,g as default};
