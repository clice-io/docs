import{_ as a,o,c as i,a3 as t}from"./chunks/framework.aYYe0dfI.js";const u=JSON.parse('{"title":"Module config:validate","description":"The config:validate module is responsible for checking whether a configuration value satisfies the applicable validation rules. Its public interface consists of the function clore::config::validate, which accepts a const int & and returns an integer status code (zero for success, non‑zero for an error identifier), and the struct clore::config::ValidationError, which contains a message field to describe the failure. Internally, the module relies on anonymous‑namespace helpers validate_nonzero and validate_nonempty to perform specific constraint checks. It depends on the config:schema module for schema definitions and on the C++ standard library.","frontmatter":{"title":"Module config:validate","description":"The config:validate module is responsible for checking whether a configuration value satisfies the applicable validation rules. Its public interface consists of the function clore::config::validate, which accepts a const int & and returns an integer status code (zero for success, non‑zero for an error identifier), and the struct clore::config::ValidationError, which contains a message field to describe the failure. Internally, the module relies on anonymous‑namespace helpers validate_nonzero and validate_nonempty to perform specific constraint checks. It depends on the config:schema module for schema definitions and on the C++ standard library.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/config/validate.md","filePath":"en/clore/modules/config/validate.md"}'),r={name:"clore/modules/config/validate.md"};function n(c,e,d,s,l,f){return o(),i("div",null,[...e[0]||(e[0]=[t('<h1 id="module-config-validate" tabindex="-1">Module <code>config:validate</code> <a class="header-anchor" href="#module-config-validate" aria-label="Permalink to &quot;Module `config:validate`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>config:validate</code> module is responsible for checking whether a configuration value satisfies the applicable validation rules. Its public interface consists of the function <code>clore::config::validate</code>, which accepts a <code>const int &amp;</code> and returns an integer status code (zero for success, non‑zero for an error identifier), and the struct <code>clore::config::ValidationError</code>, which contains a <code>message</code> field to describe the failure. Internally, the module relies on anonymous‑namespace helpers <code>validate_nonzero</code> and <code>validate_nonempty</code> to perform specific constraint checks. It depends on the <code>config:schema</code> module for schema definitions and on the C++ standard library.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./schema"><code>config:schema</code></a></li><li><code>std</code></li></ul><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-config-validationerror" tabindex="-1"><code>clore::config::ValidationError</code> <a class="header-anchor" href="#clore-config-validationerror" aria-label="Permalink to &quot;`clore::config::ValidationError`&quot;">​</a></h3><p>Declaration: <code>config/validate.cppm:8</code></p><p>Definition: <code>config/validate.cppm:8</code></p><p>Declaration: <a href="./../../namespaces/clore/config/"><code>Namespace clore::config</code></a></p><p>The struct <code>clore::config::ValidationError</code> is implemented as a trivial value type containing a single data member <code>message</code> of type <code>std::string</code>. No invariants beyond basic string validity are enforced; the default constructor and copy/move operations are compiler-generated, and the <code>message</code> member is directly accessible for both reading and writing. The struct serves solely as a lightweight carrier for a human-readable description of a validation failure, with no additional metadata, error codes, or nesting.</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>The <code>message</code> member always contains a non-empty string when used</li><li>No other members or state exist</li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>clore::config::ValidationError::message</code></li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Returned from validation functions to indicate failure</li><li>Used as the error type in <code>std::expected</code> or similar patterns</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-config-validate" tabindex="-1"><code>clore::config::validate</code> <a class="header-anchor" href="#clore-config-validate" aria-label="Permalink to &quot;`clore::config::validate`&quot;">​</a></h3><p>Declaration: <code>config/validate.cppm:12</code></p><p>Definition: <code>config/validate.cppm:42</code></p><p>Declaration: <a href="./../../namespaces/clore/config/"><code>Namespace clore::config</code></a></p><p>The implementation of <code>clore::config::validate</code> performs a series of sequential validation checks against the provided <code>TaskConfig</code> object. Each required field (<code>compile_commands_path</code>, <code>project_root</code>, <code>output_root</code>) is first tested for emptiness; if empty, the function returns a <code>std::unexpected</code> containing a <code>ValidationError</code> with an appropriate message. For fields that are not empty, filesystem existence and type checks are performed using <code>std::filesystem::exists</code> and <code>std::filesystem::is_regular_file</code> or <code>std::filesystem::is_directory</code>, returning an error on failure. The <code>output_root</code> field is only validated if it already exists (a missing directory is permitted). After the filesystem checks, the LLM sub‑configuration is validated by calling the local helper functions <code>validate_nonempty</code> on <code>config.llm.system_prompt</code> and <code>validate_nonzero</code> on <code>config.llm.retry_limit</code>; if either returns an unexpected result, that result is propagated. On successful completion of all checks, the function returns a default‑constructed <code>std::expected</code> (empty value).</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>TaskConfig</code> parameter</li><li>filesystem state (via <code>std::filesystem::exists</code>, <code>is_regular_file</code>, <code>is_directory</code>)</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called after loading configuration to ensure validity before use</li><li>May be called on both initial configuration and after modifications</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The module <code>config:validate</code> depends externally on <code>std</code> for basic utilities and on <code>config:schema</code> for the core schema types (<code>TaskConfig</code>, <code>LLMConfig</code>, <code>FilterRule</code>). It imports these via module imports, establishing a clear dependency from validation logic to the schema definition layer without circular coupling.</p><p>Internally, the module is decomposed into a public entry point and private helper functions in an anonymous namespace. The public function <code>clore::config::validate</code> accepts a configuration value (represented as <code>const int &amp;</code> for the scalar case) and returns an <code>int</code> status code: <code>0</code> for valid, non‑zero for an error identifier. The anonymous namespace contains specialized validation routines—<code>validate_nonzero</code> and <code>validate_nonempty</code>—each handling a particular constraint logic. These helpers are invoked iteratively as the validator walks the fields of the schema struct, using the variable names <code>config</code>, <code>field</code>, and <code>value</code> to track the current object, schema field definition, and runtime value respectively. The struct <code>ValidationError</code> is used to capture failure messages, likely attached to a returned error identifier. This design isolates constraint implementations from the traversal logic, keeping each validation rule focused and testable.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./schema">Module config:schema</a></li></ul>',34)])])}const m=a(r,[["render",n]]);export{u as __pageData,m as default};
