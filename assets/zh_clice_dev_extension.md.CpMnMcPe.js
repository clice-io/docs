import{_ as i,o as e,c as a,a3 as n}from"./chunks/framework.CRbjHCyB.js";const r=JSON.parse('{"title":"Extension","description":"","frontmatter":{},"headers":[],"relativePath":"zh/clice/dev/extension.md","filePath":"zh/clice/dev/extension.md"}'),t={name:"zh/clice/dev/extension.md"};function l(p,s,d,o,h,c){return e(),a("div",null,[...s[0]||(s[0]=[n(`<h1 id="extension" tabindex="-1">Extension <a class="header-anchor" href="#extension" aria-label="Permalink to &quot;Extension&quot;">​</a></h1><p>本节汇总各编辑器扩展（VSCode / Neovim / Zed）的开发与发布流程。</p><h2 id="vscode" tabindex="-1">VSCode <a class="header-anchor" href="#vscode" aria-label="Permalink to &quot;VSCode&quot;">​</a></h2><p>VSCode 扩展使用 Node/npm/VSCE 工具链。请在 pixi 的 <code>node</code> 环境中操作，以确保版本一致。</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># prepare environment (install pixi first)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pixi</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> shell</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> node</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># install deps (uses package-lock)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pixi</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install-vscode</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># package the extension; outputs editors/vscode/*.vsix</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pixi</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build-vscode</span></span></code></pre></div><p>发布到 VSCode Marketplace（需要 <code>VSCE_PAT</code> 环境变量）：</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pixi</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> publish-vscode</span></span></code></pre></div><div class="important custom-block github-alert"><p class="custom-block-title">IMPORTANT</p><p>开发构建和本地打包的扩展不会内置 clice 服务端（发布 CI 才会按平台注入），所以请在 VSCode 设置中配置 <code>clice.executable</code>（或 <code>CLICE_EXECUTABLE</code> 环境变量）指向本地构建的二进制。否则扩展会提示找不到服务端。</p></div><p>开发与调试：</p><ol><li><code>pixi shell -e node</code></li><li>在 <code>editors/vscode</code> 下运行 <code>npm run watch</code>（增量构建）</li><li>在 VSCode 中使用“Run Extension/Launch Extension”配置，或运行 <code>code --extensionDevelopmentPath=$(pwd)/editors/vscode</code></li></ol><p>常用脚本（在 <code>pixi shell -e node</code> 下）：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> package</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # same as pixi run build-vscode</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> publish</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # same as pixi run publish-vscode</span></span></code></pre></div><p>如果不使用 pixi，请自行安装 node.js &gt;= 20（自带 npm）。扩展是仓库 npm workspace 的一部分，请在仓库根目录安装依赖，然后在 <code>editors/vscode</code> 下打包：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # at the repo root</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> editors/vscode</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> package</span></span></code></pre></div><h2 id="neovim" tabindex="-1">Neovim <a class="header-anchor" href="#neovim" aria-label="Permalink to &quot;Neovim&quot;">​</a></h2><p>Neovim 扩展位于 <code>editors/nvim</code>，使用 Lua 编写。目前仍在演进中。</p><ul><li>将仓库路径加入 <code>runtimepath</code>，例如：<code>set rtp+=/path/to/clice/editors/nvim</code></li><li>或创建本地符号链接：<code>~/.config/nvim/pack/clice/start/clice</code> -&gt; <code>&lt;repo&gt;/editors/nvim</code></li><li>确保 <code>clice</code> 可执行文件能在 <code>$PATH</code> 中找到</li></ul><p>开发提示：代码量较小——直接在 Neovim 中加载，观察 <code>:messages</code>/LSP 日志；使用 <code>stylua</code> 格式化（配置已包含）。</p><h2 id="zed" tabindex="-1">Zed <a class="header-anchor" href="#zed" aria-label="Permalink to &quot;Zed&quot;">​</a></h2><p>Zed 扩展位于 <code>editors/zed</code>，使用 Rust 和 <code>zed_extension_api</code>。</p><p>建议的本地验证流程：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> editors/zed</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cargo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --release</span></span></code></pre></div><p>然后按 Zed 官方指南加载本地扩展（需要 Zed CLI）。启动前确保 <code>clice</code> 在 <code>PATH</code> 中。发布时遵循 Zed 扩展发布流程。</p>`,23)])])}const F=i(t,[["render",l]]);export{r as __pageData,F as default};
