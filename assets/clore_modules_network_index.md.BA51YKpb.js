import{_ as o,o as t,c as n,a3 as a}from"./chunks/framework.aYYe0dfI.js";const u=JSON.parse('{"title":"Module network","description":"The network module provides the asynchronous networking infrastructure for interacting with large language model (LLM) providers, specifically Anthropic and OpenAI. It encapsulates provider detection from environment variables, configuration validation, and the initiation of non‑blocking LLM requests and completion operations. The public interface consists of three key functions: validate_llm_provider_environment ensures the runtime environment is correctly set up, call_llm_async dispatches an LLM request, and call_completion_async handles completion‑style calls. All asynchronous operations are scheduled on a kota::event_loop and return an integer handle or status code. Internally, the module uses a Provider enum (with Anthropic and OpenAI members), environment variable constants for API keys and base URLs, and helper functions to detect the active provider and route requests accordingly.","frontmatter":{"title":"Module network","description":"The network module provides the asynchronous networking infrastructure for interacting with large language model (LLM) providers, specifically Anthropic and OpenAI. It encapsulates provider detection from environment variables, configuration validation, and the initiation of non‑blocking LLM requests and completion operations. The public interface consists of three key functions: validate_llm_provider_environment ensures the runtime environment is correctly set up, call_llm_async dispatches an LLM request, and call_completion_async handles completion‑style calls. All asynchronous operations are scheduled on a kota::event_loop and return an integer handle or status code. Internally, the module uses a Provider enum (with Anthropic and OpenAI members), environment variable constants for API keys and base URLs, and helper functions to detect the active provider and route requests accordingly.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/network/index.md","filePath":"en/clore/modules/network/index.md"}'),r={name:"clore/modules/network/index.md"};function i(c,e,d,l,s,p){return t(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="module-network" tabindex="-1">Module <code>network</code> <a class="header-anchor" href="#module-network" aria-label="Permalink to &quot;Module `network`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The <code>network</code> module provides the asynchronous networking infrastructure for interacting with large language model (LLM) providers, specifically Anthropic and <code>OpenAI</code>. It encapsulates provider detection from environment variables, configuration validation, and the initiation of non‑blocking LLM requests and completion operations. The public interface consists of three key functions: <code>validate_llm_provider_environment</code> ensures the runtime environment is correctly set up, <code>call_llm_async</code> dispatches an LLM request, and <code>call_completion_async</code> handles completion‑style calls. All asynchronous operations are scheduled on a <code>kota::event_loop</code> and return an integer handle or status code. Internally, the module uses a <code>Provider</code> enum (with <code>Anthropic</code> and <code>OpenAI</code> members), environment variable constants for API keys and base <code>URLs</code>, and helper functions to detect the active provider and route requests accordingly.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><code>std</code></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./../agent/"><code>agent</code></a></li><li><a href="./../generate/scheduler"><code>generate:scheduler</code></a></li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-net-call-completion-async" tabindex="-1"><code>clore::net::call_completion_async</code> <a class="header-anchor" href="#clore-net-call-completion-async" aria-label="Permalink to &quot;`clore::net::call_completion_async`&quot;">​</a></h3><p>Declaration: <code>network/network.cppm:24</code></p><p>Definition: <code>network/network.cppm:150</code></p><p>Declaration: <a href="./../../namespaces/clore/net/"><code>Namespace clore::net</code></a></p><p>The implementation of <code>clore::net::call_completion_async</code> follows a straightforward sequential coroutine flow. It first invokes the internal helper <code>detect_provider_from_environment</code> to determine the LLM provider (e.g., <code>Provider::Anthropic</code> or <code>Provider::OpenAI</code>) by inspecting environment variables such as <code>kAnthropicApiKeyEnv</code> and <code>kOpenAIApiKeyEnv</code>. If provider detection fails (returns an error), the function immediately <code>co_awaits</code> <code>kota::fail</code> to propagate the error. Otherwise, it moves the detected <code>provider</code> and delegates the actual network request to <code>dispatch_completion</code>, passing the provider, the <code>CompletionRequest</code>, and the <code>kota::event_loop</code>. The result of this dispatch is then wrapped through <code>detail::unwrap_caught_result</code> to convert a cancellation (via <code>catch_cancel</code>) into a structured <code>LLMError</code> with a descriptive message, before being <code>co_returned</code> as a <code>kota::task&lt;CompletionResponse, LLMError&gt;</code>.</p><p>Key dependencies include the anonymous-namespace helper functions <code>detect_provider_from_environment</code>, <code>dispatch_completion</code>, and <code>detail::unwrap_caught_result</code>. The <code>detect_provider_from_environment</code> function itself relies on <code>has_provider_env</code> and <code>has_nonempty_env</code> to check for provider‑specific environment variables like <code>kAnthropicBaseUrlEnv</code> and <code>kOpenAIBaseUrlEnv</code>. The overall control flow is entirely linear: environment validation first, then provider‑aware completion dispatch, each step using coroutine <code>co_await</code> or <code>co_return</code> for asynchronous error handling.</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Initiates an asynchronous network request via <code>dispatch_completion</code>.</li></ul><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>Environment variables via <code>detect_provider_from_environment</code></li><li><code>CompletionRequest</code> parameter <code>request</code></li><li><code>kota::event_loop</code> reference <code>loop</code></li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used to start an async LLM completion request.</li><li>Called within a coroutine context where <code>co_await</code> is available.</li></ul><h3 id="clore-net-call-llm-async" tabindex="-1"><code>clore::net::call_llm_async</code> <a class="header-anchor" href="#clore-net-call-llm-async" aria-label="Permalink to &quot;`clore::net::call_llm_async`&quot;">​</a></h3><p>Declaration: <code>network/network.cppm:18</code></p><p>Definition: <code>network/network.cppm:126</code></p><p>Declaration: <a href="./../../namespaces/clore/net/"><code>Namespace clore::net</code></a></p><p>The implementation of <code>clore::net::call_llm_async</code> begins by invoking the anonymous-namespace helper <code>detect_provider_from_environment()</code>, which inspects environment variables (such as <code>kAnthropicApiKeyEnv</code> and <code>kOpenAIApiKeyEnv</code>) to determine the intended LLM provider. If detection fails (e.g., no recognized API key is set), the function propagates the error via <code>kota::fail</code>. On success, it retrieves a human-readable label for the provider using <code>provider_label</code>, then delegates the actual asynchronous request to <code>request_provider_text_async</code>. That function is called with a completion lambda that maps a <code>CompletionRequest</code> to a <code>CompletionResponse</code> by calling <code>dispatch_completion</code>, which handles the per-provider network interaction (e.g., Anthropic or <code>OpenAI</code>). The entire coroutine chain is wrapped with <code>.or_fail()</code> to convert internal errors into the expected <code>LLMError</code> type. The returned <code>kota::task</code> yields a plain <code>std::string</code> upon success.</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Initiates HTTP request via <code>dispatch_completion</code></li><li>Reads environment variables for provider detection</li></ul><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>model parameter</li><li><code>system_prompt</code> parameter</li><li>request parameter</li><li>environment variables for provider detection</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used for async text completion requests to LLM providers</li><li>Called within event loop coroutines</li></ul><h3 id="clore-net-validate-llm-provider-environment" tabindex="-1"><code>clore::net::validate_llm_provider_environment</code> <a class="header-anchor" href="#clore-net-validate-llm-provider-environment" aria-label="Permalink to &quot;`clore::net::validate_llm_provider_environment`&quot;">​</a></h3><p>Declaration: <code>network/network.cppm:28</code></p><p>Definition: <code>network/network.cppm:118</code></p><p>Declaration: <a href="./../../namespaces/clore/net/"><code>Namespace clore::net</code></a></p><p>The function <code>clore::net::validate_llm_provider_environment</code> delegatess all validation logic to the anonymous‑namespace helper <code>detect_provider_from_environment</code>. This helper probes for a supported provider by checking environment variables such as <code>kAnthropicApiKeyEnv</code>, <code>kAnthropicBaseUrlEnv</code>, <code>kOpenAIApiKeyEnv</code>, and <code>kOpenAIBaseUrlEnv</code> via <code>has_provider_env</code> and <code>has_nonempty_env</code>. It returns a <code>std::expected&lt;void, LLMError&gt;</code> that either indicates success or carries an error describing which provider environment is misconfigured or absent. The outer function simply propagates that result without additional processing, making its control flow a straight pass‑through to the detection step.</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>environment variables via <code>detect_provider_from_environment()</code></li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called to check whether the LLM provider environment is properly configured before making API calls</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>network</code> module is implemented as a C++20 module in <code>network.cppm</code> and exposes three public entry points: <code>validate_llm_provider_environment</code>, <code>call_llm_async</code>, and <code>call_completion_async</code>. These form a thin asynchronous API for interacting with large language model (LLM) providers (Anthropic and <code>OpenAI</code>). The module imports the standard library and relies on an external <code>kota::event_loop</code> abstraction for scheduling non‑blocking operations.</p><p>Internally, the module uses an anonymous namespace to encapsulate the provider detection and dispatch machinery. An enumeration <code>Provider</code> distinguishes between <code>Anthropic</code> and <code>OpenAI</code>, and a set of static helpers—such as <code>detect_provider_from_environment</code>, <code>has_provider_env</code>, <code>has_nonempty_env</code>, <code>provider_label</code>, and <code>dispatch_completion</code>—handle environment variable validation, provider selection, and generic completion dispatching. The template function <code>request_provider_text_async</code> bridges the public API to the concrete provider‑specific logic. This layering keeps the public interface clean while allowing the implementation to be extended with additional providers or backends without affecting callers.</p>',45)])])}const m=o(r,[["render",i]]);export{u as __pageData,m as default};
