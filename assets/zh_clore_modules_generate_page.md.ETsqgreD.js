import{_ as r,C as t,o,c as l,a3 as a,b as i,w as d,a as n,E as s,a4 as p}from"./chunks/framework.aYYe0dfI.js";const q=JSON.parse('{"title":"Module generate:page","description":"该模块是文档生成管线中负责页面渲染的核心模块，归属 generate/render/page.cppm。它提供了一组公共函数，用于构建各种类型页面（如文件页、模块页、命名空间页、索引页）的根结构（build_file_page_root、build_module_page_root、build_namespace_page_root、build_index_page_root），并将这些根结构渲染为 Markdown（render_page_markdown）或页面捆绑包（render_page_bundle），最终通过 write_page 持久化到输出目标。模块内部依赖 generate:model、generate:symbol、generate:markdown 等下层模块提供的数据结构和渲染工具，同时依赖 config 获取配置、extract 获取符号提取结果、support 获取基础文件和文本处理能力。其公开范围涵盖了从页面计划构建到渲染输出的完整流程，调用者只需提供符合内部约定的资源标识符即可驱动页面生成。","frontmatter":{"title":"Module generate:page","description":"该模块是文档生成管线中负责页面渲染的核心模块，归属 generate/render/page.cppm。它提供了一组公共函数，用于构建各种类型页面（如文件页、模块页、命名空间页、索引页）的根结构（build_file_page_root、build_module_page_root、build_namespace_page_root、build_index_page_root），并将这些根结构渲染为 Markdown（render_page_markdown）或页面捆绑包（render_page_bundle），最终通过 write_page 持久化到输出目标。模块内部依赖 generate:model、generate:symbol、generate:markdown 等下层模块提供的数据结构和渲染工具，同时依赖 config 获取配置、extract 获取符号提取结果、support 获取基础文件和文本处理能力。其公开范围涵盖了从页面计划构建到渲染输出的完整流程，调用者只需提供符合内部约定的资源标识符即可驱动页面生成。","layout":"doc","template":"doc"},"headers":[],"relativePath":"zh/clore/modules/generate/page.md","filePath":"zh/clore/modules/generate/page.md"}'),u={name:"zh/clore/modules/generate/page.md"};function m(g,e,_,h,f,b){const c=t("Mermaid");return o(),l("div",null,[e[1]||(e[1]=a('<h1 id="module-generate-page" tabindex="-1">Module <code>generate:page</code> <a class="header-anchor" href="#module-generate-page" aria-label="Permalink to &quot;Module `generate:page`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>该模块是文档生成管线中负责页面渲染的核心模块，归属 <code>generate/render/page.cppm</code>。它提供了一组公共函数，用于构建各种类型页面（如文件页、模块页、命名空间页、索引页）的根结构（<code>build_file_page_root</code>、<code>build_module_page_root</code>、<code>build_namespace_page_root</code>、<code>build_index_page_root</code>），并将这些根结构渲染为 Markdown（<code>render_page_markdown</code>）或页面捆绑包（<code>render_page_bundle</code>），最终通过 <code>write_page</code> 持久化到输出目标。模块内部依赖 <code>generate:model</code>、<code>generate:symbol</code>、<code>generate:markdown</code> 等下层模块提供的数据结构和渲染工具，同时依赖 <code>config</code> 获取配置、<code>extract</code> 获取符号提取结果、<code>support</code> 获取基础文件和文本处理能力。其公开范围涵盖了从页面计划构建到渲染输出的完整流程，调用者只需提供符合内部约定的资源标识符即可驱动页面生成。</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><a href="./../config/"><code>config</code></a></li><li><a href="./../extract/"><code>extract</code></a></li><li><a href="./common"><code>generate:common</code></a></li><li><a href="./markdown"><code>generate:markdown</code></a></li><li><a href="./model"><code>generate:model</code></a></li><li><a href="./symbol"><code>generate:symbol</code></a></li><li><code>std</code></li><li><a href="./../support/"><code>support</code></a></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./scheduler"><code>generate:scheduler</code></a></li></ul><h2 id="dependency-diagram" tabindex="-1">Dependency Diagram <a class="header-anchor" href="#dependency-diagram" aria-label="Permalink to &quot;Dependency Diagram&quot;">​</a></h2>',8)),(o(),i(p,null,{default:d(()=>[s(c,{id:"mermaid-67",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20M0%5B%22generate%22%5D%0A%20%20%20%20I0%5B%22config%22%5D%0A%20%20%20%20I0%20--%3E%20M0%0A%20%20%20%20I1%5B%22extract%22%5D%0A%20%20%20%20I1%20--%3E%20M0%0A%20%20%20%20I2%5B%22support%22%5D%0A%20%20%20%20I2%20--%3E%20M0%0A"})]),fallback:d(()=>[...e[0]||(e[0]=[n(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=a('<h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><h3 id="clore-generate-build-file-page-root" tabindex="-1"><code>clore::generate::build_file_page_root</code> <a class="header-anchor" href="#clore-generate-build-file-page-root" aria-label="Permalink to &quot;`clore::generate::build_file_page_root`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:345</code></p><p>Definition: <code>generate/render/page.cppm:345</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数按结构组装一个 <code>SemanticSection</code> 节点树，其根节点使用 <code>make_section</code> 以 <code>SemanticKind::File</code> 创建，以表示文件页面的顶层内容。控制流首先检查 <code>plan.owner_keys.front()</code> 是否存在于 <code>model.files</code> 中，若存在则依次生成“Includes”和“Included By”两个列表节：前者遍历当前文件的所有 <code>include</code> 项并通过 <code>append_file_item</code> 构建条目，后者遍历 <code>model.files</code> 中所有其他文件，筛选出包含当前文件的候选路径，经 <code>std::sort</code> 排序后再用 <code>append_file_item</code> 填充列表。随后，若 <code>render_file_dependency_diagram_code</code> 返回非空字符串，则嵌入一个“Dependency Diagram”节并插入 Mermaid 代码块。核心符号部分由 <code>append_standard_symbol_sections</code> 完成，它通过 <code>collect_implementation_symbols</code> 谓词收集符号，并利用两个回调处理声明链接和附加内容。接着，如果 <code>find_module_for_file</code> 成功找到模块名称，则创建一个“Module Information”节，根据 <code>links.resolve_module</code> 的结果选择纯文本或超链接形式显示模块名。最后，通过 <code>build_related_page_targets</code> 填充“Related Pages”列表节。<code>root</code> 的最终结构反映了自上而下的文件信息组织顺序，依赖于 <code>model</code>、<code>analyses</code>、<code>links</code> 及 <code>config</code> 中的项目根路径等数据，并广泛调用匿名命名空间中的辅助函数。 该函数按结构组装一个 <code>SemanticSection</code> 节点树，其根节点使用 <code>make_section</code> 以 <code>SemanticKind::File</code> 创建，以表示文件页面的顶层内容。控制流首先检查 <code>plan.owner_keys.front()</code> 是否存在于 <code>model.files</code> 中，若存在则依次生成“Includes”和“Included By”两个列表节：前者遍历当前文件的所有 <code>include</code> 项并通过 <code>append_file_item</code> 构建条目，后者遍历 <code>model.files</code> 中所有其他文件，筛选出包含当前文件的候选路径，经 <code>std::sort</code> 排序后再用 <code>append_file_item</code> 填充列表。随后，若 <code>render_file_dependency_diagram_code</code> 返回非空字符串，则嵌入一个“Dependency Diagram”节并插入 Mermaid 代码块。核心符号部分由 <code>append_standard_symbol_sections</code> 完成，它通过 <code>collect_implementation_symbols</code> 谓词收集符号，并利用两个回调处理声明链接和附加内容。接着，如果 <code>find_module_for_file</code> 成功找到模块名称，则创建一个“Module Information”节，根据 <code>links.resolve_module</code> 的结果选择纯文本或超链接形式显示模块名。最后，通过 <code>build_related_page_targets</code> 填充“Related Pages”列表节。<code>root</code> 的最终结构反映了自上而下的文件信息组织顺序，依赖于 <code>model</code>、<code>analyses</code>、<code>links</code> 及 <code>config</code> 中的项目根路径等数据，并广泛调用匿名命名空间中的辅助函数。</p><h4 id="side-effects" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>plan</code> (<code>PagePlan</code>)</li><li><code>config</code> (<code>config::TaskConfig</code>)</li><li><code>model</code> (<code>extract::ProjectModel</code>)</li><li><code>analyses</code> (<code>SymbolAnalysisStore</code>)</li><li><code>links</code> (<code>LinkResolver</code>)</li><li><code>model.files</code></li><li><code>plan.owner_keys</code></li><li><code>plan.title</code></li><li><code>plan.relative_path</code></li><li><code>config.project_root</code></li></ul><h4 id="writes-to" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Constructed <code>SemanticSectionPtr</code> (return value)</li><li>Intermediate <code>make_section</code>, <code>make_mermaid</code>, <code>make_link</code>, etc. objects</li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during file page generation to create the root section.</li><li>Used as part of the page building pipeline for file documentation.</li></ul><h3 id="clore-generate-build-index-page-root" tabindex="-1"><code>clore::generate::build_index_page_root</code> <a class="header-anchor" href="#clore-generate-build-index-page-root" aria-label="Permalink to &quot;`clore::generate::build_index_page_root`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:447</code></p><p>Definition: <code>generate/render/page.cppm:447</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p><code>clore::generate::build_index_page_root</code> 构造索引页面的语义根节。它首先调用 <code>make_section</code> 创建 <code>SemanticKind::Index</code> 类型的根节点，然后依次追加子节：一个从 <code>prompt_output_of</code> 提取 <code>PromptKind::IndexOverview</code> 输出的概述区；若 <code>model.uses_modules</code> 为真，则构建“Modules”列表，过滤出接口模块且可通过 <code>links</code> 解析的名称，排序后生成链接项；接着构造“Files”列表，按相对路径排序并检查 <code>links.resolve</code> 的有效性；随后是“Namespaces”列表，排除匿名命名空间；再后是“Types”列表，筛选 <code>is_type_kind</code> 为真的符号并排序，调用 <code>build_symbol_link_list</code> 生成链接。最后，若 <code>render_module_dependency_diagram_code</code> 返回非空字符串，则追加一个包含 Mermaid 图的“Module Dependency Diagram”子节。整个函数依赖于 <code>plan.relative_path</code>、<code>config</code> 以及 <code>links</code> 解析器来生成正确的相对链接，控制流完全由条件判断和排序后的迭代驱动。</p><h4 id="side-effects-1" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-1" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>allocates and populates <code>SemanticSection</code> and <code>MarkdownNode</code> objects via <code>make_section</code>, <code>build_list_section</code>, <code>build_symbol_link_list</code>, <code>make_mermaid</code>, and <code>make_link</code></li></ul><h4 id="reads-from-1" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-1" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>plan</code></li><li><code>config</code></li><li><code>model</code></li><li><code>outputs</code></li><li><code>links</code></li><li><code>plan.relative_path</code></li><li><code>plan.title</code></li><li><code>model.uses_modules</code></li><li><code>model.modules</code></li><li><code>model.files</code></li><li><code>model.namespaces</code></li><li><code>model.symbols</code></li><li><code>config.project_root</code></li></ul><h4 id="writes-to-1" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-1" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>the returned <code>SemanticSectionPtr</code> root</li><li><code>root-&gt;children</code> (via <code>push_back</code>)</li><li><code>section-&gt;children</code> for module diagram</li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called to generate the top-level index page of a documentation site</li><li>produces the root <code>SemanticSection</code> for index page rendering</li></ul><h3 id="clore-generate-build-module-page-root" tabindex="-1"><code>clore::generate::build_module_page_root</code> <a class="header-anchor" href="#clore-generate-build-module-page-root" aria-label="Permalink to &quot;`clore::generate::build_module_page_root`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:255</code></p><p>Definition: <code>generate/render/page.cppm:255</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::build_module_page_root</code> 首先创建一个 <code>SemanticKind::Module</code> 类型的 <code>root</code> 部分，其标题由 <code>plan.title</code> 确定，并从 <code>plan.owner_keys.front()</code> 获取模块标识。接着，它立即添加一个由 <code>build_prompt_section</code> 生成的 &quot;Summary&quot; 提示节。通过 <code>find_module_by_name</code> 在 <code>model</code> 中查找该模块；若存在，则依次添加 &quot;Imports&quot; 和 &quot;Imported By&quot; 列表节，其中 “Imported By” 列表通过遍历所有模块收集引用当前模块的名称，并去重排序后使用 <code>append_module_item</code> 生成链接。随后，基于 <code>render_import_diagram_code</code> 产生的非空 mermaid 代码，插入一个 &quot;Dependency Diagram&quot; 小节，其中包含一个 mermaid 节点。</p><p>接下来，调用 <code>append_standard_symbol_sections</code> 向 <code>root-&gt;children</code> 追加标准符号部分。该调用依赖三个回调：一个通过 <code>collect_implementation_symbols</code> 在 <code>plan</code> 和 <code>model</code> 中收集符号；一个利用 <code>find_declaration_page</code> 与 <code>push_optional_link_paragraph</code> 为每个符号添加声明链接；另一个使用 <code>add_symbol_doc_links</code> 根据 <code>symbol_doc_view_for</code> 添加文档链接。随后添加 &quot;Internal Structure&quot; 提示节。最后，通过 <code>build_related_page_targets</code> 生成 &quot;Related Pages&quot; 列表节，使用 <code>make_link</code> 创建每个目标链接。整体流程依赖于 <code>build_prompt_section</code>、<code>build_list_section</code>、<code>append_module_item</code>、<code>append_standard_symbol_sections</code>、<code>render_import_diagram_code</code>、<code>collect_implementation_symbols</code>、<code>find_declaration_page</code>、<code>add_symbol_doc_links</code>、<code>symbol_doc_view_for</code> 以及 <code>build_related_page_targets</code> 等辅助函数，并依赖 <code>plan</code>、<code>config</code>、<code>model</code>、<code>analyses</code>、<code>links</code> 和 <code>layout</code> 参数完成上下文组装。</p><h4 id="side-effects-2" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-2" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-2" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-2" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>Reads from the <code>plan</code>, <code>config</code>, <code>model</code>, <code>outputs</code>, <code>analyses</code>, <code>links</code>, and <code>layout</code> parameters</li><li>Reads module data from <code>model.modules</code> and <code>module-&gt;imports</code></li><li>Reads from <code>find_declaration_page</code> and <code>build_related_page_targets</code></li></ul><h4 id="writes-to-2" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-2" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Constructs and returns a <code>SemanticSectionPtr</code> representing the root section of a module page</li><li>Writes to the <code>children</code> vector of the root section</li></ul><h4 id="usage-patterns-2" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-2" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called during documentation generation to produce the page content for a module</li><li>Used by higher-level page rendering functions</li></ul><h3 id="clore-generate-build-namespace-page-root" tabindex="-1"><code>clore::generate::build_namespace_page_root</code> <a class="header-anchor" href="#clore-generate-build-namespace-page-root" aria-label="Permalink to &quot;`clore::generate::build_namespace_page_root`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:165</code></p><p>Definition: <code>generate/render/page.cppm:165</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数首先通过 <code>make_section</code> 构造一个语义类型为 <code>SemanticKind::Namespace</code> 的根节点，并将其深度设为一级标题。接着调用 <code>build_prompt_section</code> 生成一个名为“Summary”的二级提示输出节，从 <code>outputs</code> 中选取 <code>PromptKind::NamespaceSummary</code> 对应的内容。然后尝试通过 <code>render_namespace_diagram_code</code> 渲染命名空间内类型的继承关系图（Mermaid 格式），若结果非空则创建一个名为“Diagram”的二级节并插入。随后通过 <code>build_list_section</code> 构建“Subnamespaces”列表，遍历模型的 <code>namespaces</code> 字典，排除匿名命名空间，利用 <code>links.resolve</code> 解析每个子命名空间的相对链接，生成带链接的列表项。核心部分调用 <code>append_standard_symbol_sections</code>，该函数接受两个回调：一个用 <code>collect_namespace_symbols</code> 收集该命名空间下的所有符号（函数、类、变量等）；另一个回调分别通过 <code>find_implementation_pages</code> 和 <code>add_symbol_doc_links</code> 为每个符号添加实现链接和文档链接。最后，调用 <code>build_related_page_targets</code> 收集相关页面（如父命名空间、模块等），生成“Related Pages”列表作为节点的最后一个子元素，整体返回构造好的 <code>SemanticSectionPtr</code>。</p><h4 id="side-effects-3" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-3" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-3" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-3" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>plan</code> (<code>PagePlan</code>)</li><li><code>config</code> (<code>config::TaskConfig</code>)</li><li><code>model</code> (<code>extract::ProjectModel</code>)</li><li><code>outputs</code> (<code>std::unordered_map&lt;std::string, std::string&gt;</code>)</li><li><code>analyses</code> (<code>SymbolAnalysisStore</code>)</li><li><code>links</code> (<code>LinkResolver</code>)</li><li><code>layout</code> (<code>PageDocLayout</code>)</li></ul><h4 id="usage-patterns-3" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-3" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as the entry point for building the namespace page root.</li><li>Called from higher-level page generation logic to compose the namespace documentation tree.</li></ul><h3 id="clore-generate-build-page-root" tabindex="-1"><code>clore::generate::build_page_root</code> <a class="header-anchor" href="#clore-generate-build-page-root" aria-label="Permalink to &quot;`clore::generate::build_page_root`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:546</code></p><p>Definition: <code>generate/render/page.cppm:546</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::build_page_root</code> 充当页面类型分发的中心调度器。它接收 <code>PagePlan</code>、<code>config::TaskConfig</code>、<code>extract::ProjectModel</code>、输出映射、<code>SymbolAnalysisStore</code>、<code>LinkResolver</code> 和 <code>PageDocLayout</code> 共七个参数，通过检查 <code>plan.page_type</code> 的枚举值将调用转发至四个具体的构建函数：<code>build_index_page_root</code>、<code>build_namespace_page_root</code>、<code>build_module_page_root</code> 或 <code>build_file_page_root</code>。每个分支仅传递该函数所需的相关参数子集，并依赖这些函数完成不同页面根节点的生成。如果页面类型无法识别，则回退到调用 <code>make_section</code> 创建一个通用的 <code>SemanticSection</code>。该函数本身不执行数据计算或格式化，其全部逻辑是围绕页面类型的 switch 分派，外部依赖就是这四个构建函数以及默认的段创建函数。</p><h4 id="side-effects-4" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-4" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-4" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-4" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>plan</code></li><li><code>config</code></li><li><code>model</code></li><li><code>outputs</code></li><li><code>analyses</code></li><li><code>links</code></li><li><code>layout</code></li></ul><h4 id="usage-patterns-4" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-4" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called by page generation routines to obtain the root <code>SemanticSectionPtr</code> for a given page plan.</li><li>Used in the rendering pipeline to construct the top-level section of a page.</li></ul><h3 id="clore-generate-render-page-bundle" tabindex="-1"><code>clore::generate::render_page_bundle</code> <a class="header-anchor" href="#clore-generate-render-page-bundle" aria-label="Permalink to &quot;`clore::generate::render_page_bundle`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:565</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数首先通过 <code>PagePlan</code> 遍历所有待渲染的页面条目，按计划顺序依次处理。对于每个页面，它调用内部渲染器，该渲染器将 <code>TaskConfig</code> 中的模板与 <code>ProjectModel</code> 提供的源代码实体合并，并利用 <code>prompt_outputs</code> 映射中的 AI 生成文本进行内容填充。随后通过 <code>SymbolAnalysisStore</code> 执行跨文件符号的依赖分析，确保所有内联链接（由 <code>LinkResolver</code> 处理）被正确解析为目标 URL。渲染过程中遇到任何错误（如模板缺失、符号未解析）会立即短路，返回 <code>std::expected</code> 的错误状态。所有成功渲染的页面最后被打包为 <code>render::PageBundle</code>，该过程依赖外部配置 <code>config</code> 控制输出文件的总数和布局顺序。</p><h4 id="side-effects-5" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-5" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-5" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-5" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>PagePlan</code> <code>plan</code></li><li><code>config::TaskConfig</code> <code>config</code></li><li><code>extract::ProjectModel</code> <code>model</code></li><li><code>std::unordered_map&lt;std::string, std::string&gt;</code> <code>prompt_outputs</code></li><li><code>SymbolAnalysisStore</code> <code>analyses</code></li><li><code>LinkResolver</code> <code>links</code></li></ul><h4 id="usage-patterns-5" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-5" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called from page generation pipeline (e.g., <code>generate_pages</code> or <code>generate_pages_async</code>) to produce a single page bundle.</li><li>Used as the main rendering step after all analysis and link resolution is available.</li></ul><h3 id="clore-generate-render-page-bundle-1" tabindex="-1"><code>clore::generate::render_page_bundle</code> <a class="header-anchor" href="#clore-generate-render-page-bundle-1" aria-label="Permalink to &quot;`clore::generate::render_page_bundle`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:573</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数通过依次遍历 <code>PagePlan</code> 中的页面定义，从 <code>ProjectModel</code> 中提取对应的模型数据，并根据 <code>TaskConfig</code> 中的模板与渲染参数生成每个页面的内容。内部依赖 <code>prompt_outputs</code> 映射提供动态提示结果的替换，并借助 <code>LinkResolver</code> 解析跨页面链接和锚点，从而保证生成文档中引用的正确性。所有页面的渲染结果被组合为一个完整的页面捆绑包，最终以 <code>std::expected</code> 的形式返回，错误路径由内部校验和异常捕获机制统一处理。</p><h4 id="side-effects-6" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-6" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>Writes rendered pages to the filesystem</li><li>May create directories for output</li></ul><h4 id="reads-from-6" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-6" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>const <code>PagePlan</code>&amp; plan</li><li>const <code>config::TaskConfig</code>&amp; config</li><li>const <code>extract::ProjectModel</code>&amp; model</li><li>const <code>std::unordered_map&lt;std::string, std::string&gt;</code>&amp; <code>prompt_outputs</code></li><li>const <code>LinkResolver</code>&amp; links</li></ul><h4 id="writes-to-3" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-3" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>Filesystem (output pages)</li></ul><h4 id="usage-patterns-6" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-6" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Called by higher-level page generation functions like <code>generate_pages</code></li><li>Used to render a single page bundle as part of a larger documentation generation pipeline</li></ul><h3 id="clore-generate-render-page-markdown" tabindex="-1"><code>clore::generate::render_page_markdown</code> <a class="header-anchor" href="#clore-generate-render-page-markdown" aria-label="Permalink to &quot;`clore::generate::render_page_markdown`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:602</code></p><p>Definition: <code>generate/render/page.cppm:602</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数通过对 <code>plan</code>、<code>config</code>、<code>model</code> 和 <code>prompt_outputs</code> 的组合解析，将页面内容逐步构建为最终的 Markdown 字符串。内部控制流围绕 <code>plan</code> 中的结构进行迭代：首先调用 <code>build_page_root</code> 生成页面根节点，接着根据 <code>plan</code> 的组件类型（如模块项、文件项或命名空间分支）分发到 <code>append_module_item</code>、<code>append_file_item</code> 或 <code>build_*_page_root</code> 系列函数。每个分支会利用 <code>select_primary_description_source_page</code> 选取主描述源，再通过 <code>render_page_bundle</code> 或 <code>prompt_output_of_local_page</code> 组装子章节。所有生成的片段经由 <code>write_page</code> 函数写入最终的布局输出。依赖包括 <code>LinkResolver</code> 和 <code>SymbolAnalysisStore</code>，后者用于在部分重载中提供符号分析数据；若未提供，则默认传入空存储。</p><h4 id="side-effects-7" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-7" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-7" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-7" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li><code>plan</code></li><li><code>config</code></li><li><code>model</code></li><li><code>prompt_outputs</code></li><li><code>links</code></li></ul><h4 id="usage-patterns-7" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-7" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used when no precomputed symbol analysis data is available</li></ul><h3 id="clore-generate-render-page-markdown-1" tabindex="-1"><code>clore::generate::render_page_markdown</code> <a class="header-anchor" href="#clore-generate-render-page-markdown-1" aria-label="Permalink to &quot;`clore::generate::render_page_markdown`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:582</code></p><p>Definition: <code>generate/render/page.cppm:582</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>该函数的核心实现围绕两步：调用 <code>clore::generate::render_page_bundle</code> 生成完整的页面集合（bundle），然后根据调用方传入的 <code>plan.relative_path</code> 在 bundle 中定位当前页面的内容。若 bundle 生成失败（<code>std::unexpected</code>），或 bundle 中不存在与 <code>plan.relative_path</code> 匹配的 <code>GeneratedPage</code> 对象，函数都会立即返回一个 <code>RenderError</code> 错误。成功时则直接返回匹配页面的 <code>content</code> 字段。这一设计使得 <code>render_page_markdown</code> 复用 <code>render_page_bundle</code> 的整页生成逻辑，同时仅提取调用者所需的那一页内容。</p><h4 id="side-effects-8" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-8" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><p>No observable side effects are evident from the extracted code.</p><h4 id="reads-from-8" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-8" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>plan</li><li>config</li><li>model</li><li><code>prompt_outputs</code></li><li>analyses</li><li>links</li></ul><h4 id="usage-patterns-8" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-8" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used in page generation pipeline to retrieve the final Markdown string for a specific page</li></ul><h3 id="clore-generate-write-page" tabindex="-1"><code>clore::generate::write_page</code> <a class="header-anchor" href="#clore-generate-write-page" aria-label="Permalink to &quot;`clore::generate::write_page`&quot;">​</a></h3><p>Declaration: <code>generate/render/page.cppm:666</code></p><p>Definition: <code>generate/render/page.cppm:666</code></p><p>Declaration: <a href="./../../namespaces/clore/generate/"><code>Namespace clore::generate</code></a></p><p>函数 <code>clore::generate::write_page</code> 实现将渲染后的页面持久化到文件系统的核心逻辑。它首先对输入参数进行严格的路径安全性校验：验证 <code>page.relative_path</code> 为相对路径且不包含 <code>&quot;.&quot;</code> 或 <code>&quot;..&quot;</code> 片段，若无效则立即返回 <code>RenderError</code>。通过校验后，将输出根路径 <code>output_root</code> 与相对路径拼接并归一化得到目标文件路径，然后调用 <code>std::filesystem::create_directories</code> 创建父目录（忽略已存在目录，失败时返回错误）。最后委托 <code>clore::support::write_utf8_text_file</code> 将 <code>page.content</code> 写入目标文件，并透传该函数的错误信息。</p><p>该函数无复杂分支，依赖标准库文件系统操作和内部工具函数，其设计将路径验证、目录创建与文件写入分离，确保输出文件的路径安全性与目标目录的自动创建。</p><h4 id="side-effects-9" tabindex="-1">Side Effects <a class="header-anchor" href="#side-effects-9" aria-label="Permalink to &quot;Side Effects&quot;">​</a></h4><ul><li>creates directories on the filesystem</li><li>writes a UTF-8 text file to disk</li></ul><h4 id="reads-from-9" tabindex="-1">Reads From <a class="header-anchor" href="#reads-from-9" aria-label="Permalink to &quot;Reads From&quot;">​</a></h4><ul><li>the <code>page.relative_path</code> field</li><li>the <code>page.content</code> field</li><li>the <code>output_root</code> parameter</li><li>the filesystem to check for existing directories</li></ul><h4 id="writes-to-4" tabindex="-1">Writes To <a class="header-anchor" href="#writes-to-4" aria-label="Permalink to &quot;Writes To&quot;">​</a></h4><ul><li>the filesystem (directories and file)</li><li>the <code>RenderError</code> value via <code>std::unexpected</code> on failure</li></ul><h4 id="usage-patterns-9" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-9" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>called to output a single generated page file</li><li>used in the page generation pipeline after building the page content</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p><code>generate:page</code> 模块是文档生成管线中页面渲染的核心实现，它通过导入 <code>config</code>、<code>extract</code>、<code>generate:model</code> 等模块获取页面计划、符号分析与配置数据，并依赖 <code>generate:markdown</code> 和 <code>generate:common</code> 提供的结构化文档节点与链接工具来构建最终输出。模块内部采用清晰的分层结构：一组公共构建函数（ <code>build_namespace_page_root</code>、<code>build_module_page_root</code>、<code>build_file_page_root</code>、<code>build_index_page_root</code> 以及通用的 <code>build_page_root</code>）分别负责不同页面类型根节点的装配；在此之上，<code>render_page_markdown</code> 和 <code>render_page_bundle</code> 将页面根转换为完整的 Markdown 内容，而 <code>write_page</code> 则负责将结果持久化。这些公共接口通过匿名命名空间中封装的辅助例程（如 <code>append_module_item</code>、<code>append_file_item</code>、<code>build_frontmatter_page</code>、<code>select_primary_description_source_page</code> 以及模板化的 <code>append_standard_symbol_sections</code>）实现逻辑复用，从而在保持内聚性的同时，将页面组装与输出细节与底层模型和符号处理分离。</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../config/">Module config</a></li><li><a href="./../extract/">Module extract</a></li><li><a href="./common">Module generate:common</a></li><li><a href="./markdown">Module generate:markdown</a></li><li><a href="./model">Module generate:model</a></li><li><a href="./symbol">Module generate:symbol</a></li><li><a href="./../support/">Module support</a></li></ul>',125))])}const P=r(u,[["render",m]]);export{q as __pageData,P as default};
