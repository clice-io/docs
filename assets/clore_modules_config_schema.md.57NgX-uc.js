import{_ as o,o as a,c as t,a3 as i}from"./chunks/framework.aYYe0dfI.js";const p=JSON.parse('{"title":"Module config:schema","description":"The module config:schema defines the core data structures that represent the configuration schema for the clore system. It owns the public types TaskConfig, LLMConfig, and FilterRule, which hold settings such as project paths, LLM parameters, and include/exclude filters. These structs are designed to be the canonical representation of user-provided configuration after validation and normalization.","frontmatter":{"title":"Module config:schema","description":"The module config:schema defines the core data structures that represent the configuration schema for the clore system. It owns the public types TaskConfig, LLMConfig, and FilterRule, which hold settings such as project paths, LLM parameters, and include/exclude filters. These structs are designed to be the canonical representation of user-provided configuration after validation and normalization.","layout":"doc","template":"doc"},"headers":[],"relativePath":"clore/modules/config/schema.md","filePath":"en/clore/modules/config/schema.md"}'),c={name:"clore/modules/config/schema.md"};function r(n,e,d,s,l,h){return a(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="module-config-schema" tabindex="-1">Module <code>config:schema</code> <a class="header-anchor" href="#module-config-schema" aria-label="Permalink to &quot;Module `config:schema`&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The module <code>config:schema</code> defines the core data structures that represent the configuration schema for the clore system. It owns the public types <code>TaskConfig</code>, <code>LLMConfig</code>, and <code>FilterRule</code>, which hold settings such as project paths, LLM parameters, and include/exclude filters. These structs are designed to be the canonical representation of user-provided configuration after validation and normalization.</p><h2 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to &quot;Imports&quot;">​</a></h2><ul><li><code>std</code></li></ul><h2 id="imported-by" tabindex="-1">Imported By <a class="header-anchor" href="#imported-by" aria-label="Permalink to &quot;Imported By&quot;">​</a></h2><ul><li><a href="./load"><code>config:load</code></a></li><li><a href="./normalize"><code>config:normalize</code></a></li><li><a href="./validate"><code>config:validate</code></a></li></ul><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><h3 id="clore-config-filterrule" tabindex="-1"><code>clore::config::FilterRule</code> <a class="header-anchor" href="#clore-config-filterrule" aria-label="Permalink to &quot;`clore::config::FilterRule`&quot;">​</a></h3><p>Declaration: <code>config/schema.cppm:7</code></p><p>Definition: <code>config/schema.cppm:7</code></p><p>Declaration: <a href="./../../namespaces/clore/config/"><code>Namespace clore::config</code></a></p><p>The <code>clore::config::FilterRule</code> struct is a plain aggregate that holds two disjoint collections of pattern strings: <code>include</code> and <code>exclude</code>, each stored in a <code>std::vector&lt;std::string&gt;</code>. No special initialization or invariant enforcement is present; the two vectors are independent and expected to be populated by the caller according to the filtering semantics required. The struct serves solely as a container for these two pattern lists, with no member functions beyond the implicitly generated ones.</p><h4 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>The <code>include</code> and <code>exclude</code> vectors can be empty.</li><li>No constraints exist on the content of the strings beyond being valid pattern representations.</li></ul><h4 id="key-members" tabindex="-1">Key Members <a class="header-anchor" href="#key-members" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>include</code>: list of patterns to include</li><li><code>exclude</code>: list of patterns to exclude</li></ul><h4 id="usage-patterns" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Used as a configuration parameter to specify which items should be included or excluded in some processing.</li><li>Typically populated from a configuration file or user input.</li><li>Accessed by other code to filter collections based on the include/exclude lists.</li></ul><h3 id="clore-config-llmconfig" tabindex="-1"><code>clore::config::LLMConfig</code> <a class="header-anchor" href="#clore-config-llmconfig" aria-label="Permalink to &quot;`clore::config::LLMConfig`&quot;">​</a></h3><p>Declaration: <code>config/schema.cppm:12</code></p><p>Definition: <code>config/schema.cppm:12</code></p><p>Declaration: <a href="./../../namespaces/clore/config/"><code>Namespace clore::config</code></a></p><p><code>clore::config::LLMConfig</code> is a plain data structure that aggregates two configuration parameters: <code>system_prompt</code>, a <code>std::string</code> holding the system-level instruction text, and <code>retry_limit</code>, a <code>std::uint32_t</code> that defaults to <code>0</code> (interpreted as unlimited retries). The struct imposes no additional invariants beyond the natural type constraints of its members; the default value for <code>retry_limit</code> is the only implementation‑level choice, establishing a fallback semantics when the field is not explicitly set. All member initialization is trivial.</p><h3 id="clore-config-taskconfig" tabindex="-1"><code>clore::config::TaskConfig</code> <a class="header-anchor" href="#clore-config-taskconfig" aria-label="Permalink to &quot;`clore::config::TaskConfig`&quot;">​</a></h3><p>Declaration: <code>config/schema.cppm:17</code></p><p>Definition: <code>config/schema.cppm:17</code></p><p>Declaration: <a href="./../../namespaces/clore/config/"><code>Namespace clore::config</code></a></p><p>The struct <code>clore::config::TaskConfig</code> is implemented as a plain aggregate that holds the configuration for a single task. Its fields are stored by value: <code>compile_commands_path</code>, <code>project_root</code>, <code>output_root</code>, and <code>workspace_root</code> are <code>std::string</code> path entries, while <code>filter</code> and <code>llm</code> are embedded <code>FilterRule</code> and <code>LLMConfig</code> objects respectively. The struct imposes no internal invariants; all validation, normalization, and semantic checks on the field values are delegated to external parsing or application logic. No custom constructors, assignment <code>operator</code>s, or other member functions are defined, so the type relies entirely on compiler-generated special member functions for construction, copy, and move.</p><h4 id="invariants-1" tabindex="-1">Invariants <a class="header-anchor" href="#invariants-1" aria-label="Permalink to &quot;Invariants&quot;">​</a></h4><ul><li>All string fields are expected to contain valid filesystem paths</li><li><code>FilterRule</code> and <code>LLMConfig</code> are expected to be default-constructible</li></ul><h4 id="key-members-1" tabindex="-1">Key Members <a class="header-anchor" href="#key-members-1" aria-label="Permalink to &quot;Key Members&quot;">​</a></h4><ul><li><code>compile_commands_path</code></li><li><code>project_root</code></li><li><code>output_root</code></li><li><code>workspace_root</code></li><li><code>filter</code></li><li><code>llm</code></li></ul><h4 id="usage-patterns-1" tabindex="-1">Usage Patterns <a class="header-anchor" href="#usage-patterns-1" aria-label="Permalink to &quot;Usage Patterns&quot;">​</a></h4><ul><li>Loaded or populated by configuration parsing code</li><li>Consumed by task execution logic to determine paths and behavior</li></ul><h2 id="internal-structure" tabindex="-1">Internal Structure <a class="header-anchor" href="#internal-structure" aria-label="Permalink to &quot;Internal Structure&quot;">​</a></h2><p>The <code>config:schema</code> module provides the core data types that define the structure of the configuration system. It is decomposed into three simple, publicly-exported structs: <code>FilterRule</code>, <code>LLMConfig</code>, and <code>TaskConfig</code>, which together form a hierarchical schema. <code>FilterRule</code> encapsulates include/exclude path patterns, <code>LLMConfig</code> holds model‑specific parameters (such as <code>system_prompt</code> and <code>retry_limit</code>), and <code>TaskConfig</code> aggregates a filter rule, an LLM configuration, and several filesystem path fields (<code>project_root</code>, <code>workspace_root</code>, <code>output_root</code>, <code>compile_commands_path</code>). The module depends solely on the C++ standard library (<code>std</code>) and contains no additional logic or internal layering; it serves as a pure data definition layer upon which parsers and validators can build.</p>',37)])])}const m=o(c,[["render",r]]);export{p as __pageData,m as default};
