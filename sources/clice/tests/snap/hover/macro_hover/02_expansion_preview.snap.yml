---
created_at: 2026-08-23
input_file: macro_hover/02_expansion_preview.cpp
---
01_expansion: { range: "13:8-13:11" }
### macro `MAX`  

---
```cpp
#define MAX(a, b) ((a) > (b) ? (a) : (b))

// Expands to
( ( x ) > ( y ) ? ( x ) : ( y ) )
```
