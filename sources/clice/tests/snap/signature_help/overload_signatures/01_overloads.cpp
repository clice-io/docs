/// # Function overloads
///
/// - status: supported
///
/// Signature help lists every overload with its parameter list and return type

void foo();
void foo(int x);
void foo(int x, int y);

int main() {
    foo(§(pos));
}
