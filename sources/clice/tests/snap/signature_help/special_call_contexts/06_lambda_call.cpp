/// # Lambda call
///
/// - status: supported
///
/// Calling a lambda variable offers the closure's operator() parameters

int main() {
    auto square = [](int n) {
        return n * n;
    };
    square(§(pos)3);
}
